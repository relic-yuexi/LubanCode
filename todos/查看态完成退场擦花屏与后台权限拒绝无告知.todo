# 查看态完成退场擦花屏卡死,与后台代理权限拒绝无告知

状态：待做（2026-08-17 用户真机:后台 general-purpose #1 写象棋,查看态盯完,任务收尾后整屏空白卡死)

## 现场一:完成退场后"什么东西都没了"

后台代理跑完(最终报告已流进查看态),任务 delivered、坞行退场、自动回 main——然后**整屏空白、按键无响应**,只能杀进程。

### 嫌疑(已核到的双重擦账)

退场自动回 main 的 tick 路(console_input.cpp:1438 一带):

```text
retire_idle_chrome()
erase_previous_view_body()   ← console 侧账:view_body_top 起擦到缓冲底
print_view_frame(0)          ← hook → PrintViewedTranscript
reanchor_prompt_and_redraw()
```

而 0.26.11 给 `PrintViewedTranscript` 加的 retained 擦账是 **app 侧另一本账**(`view_frame_top_/rows_/vp_y_`,interactive_session.cpp):进门先按自己的旧账擦一遍再画。两把擦子先后下手:

- console 侧刚把 view_body_top→height 清空、光标落在 top;
- app 侧接着按自己那份(可能因实时流重铺/静默回流轮滚屏而失准的)绝对行账再擦一矩形——账错就擦掉刚铺的 main 帧、甚至擦到别处;
- `reanchor_prompt_and_redraw` 之后的整帧重画若锚点已被第二次擦弄乱,画面即全空。

"按键无响应"待查:是 ReadLine 循环真的卡死(锁/状态机),还是画面空了、光标丢了,键其实进了编辑器但无回显。诊断须真终端驱动器复现钉死。

### 要做

1. **一本账**:擦除只留一个权威(console 侧 view_body_top 或 app 侧帧账,二选一),另一侧只打印不擦;或退场回 main 这条路完全绕开 app 侧 PrintViewedTranscript 的擦账(传"已擦净"标记)。落地方式诊断后定,但**不许两本账并存**。
2. **驱动器复现**:agent_stream_driver/viewport_driver 补一幕——查看态盯后台任务到完成退场,断言退场后 composer/坞/状态行在位、按键有回显、退出码 0。现在这幕没有,才让这病溜到真机。
3. **卡死定性**:若真有线程卡死(而非纯花屏),抓栈定性——idle wake 路与 hook 的锁序、AgentViewSwitchHook 重入,都要推演一遍并写进注释。
4. **修复后**:完成退场回 main 一拍内画面完整(帧头 main 可辨、composer 恰一份、Dock 无已完成行),键入立即回显。

## 现场二:后台代理的权限拒绝无声无息

后台代理两次 write_file 全被拒(后台不能弹确认是设计),用户直到最终报告才从"均被用户拒绝"里得知——**看起来像用户拒绝,其实是没人问过用户**。烧了 101k tokens、687 秒,产物为零。

### 要做

1. **拒绝即告**:后台任务的 needs_confirm 工具被拒那一刻,main 侧 toast 一枚("后台 #N 请求 write_file 未放行,已拒——/permissions 预放行或让其前台重试"),transcript 记一条有归属事件;不许攒到最终报告。
2. **报告措辞改口**:子代理的失败报告把"被用户拒绝"改成如实("后台任务无法弹确认,未预放行被拒");工具结果给子代理的拒绝文案同样写明原因与出路(重试无意义,应报告受阻并停止,或改走只读产出)。
3. **预放行通道顺手可见**:toast 里点名 /permissions(settings.local 的 allow)这条路;文档 hooks/tools 页补一句"后台代理的写操作须预放行"。
4. agent 工具描述已有此句,校对与实际行为一致。

## 验收

- 驱动器新幕:查看态完成退场后一帧完整,键入回显,退出码 0;真终端复刻用户场景(后台写象棋)不花屏不卡死。
- 后台 needs_confirm 被拒:当场 toast + transcript 事件;最终报告措辞不再说"用户拒绝"。
- 既有回归(查看帧 retained/活跃度/退场)全绿;两把擦账并存的代码痕迹清零。
