# 渐进式上下文装载、分层压缩与 cheap/normal/lao 模型分工

状态：待做（2026-08-16 调研 Claude Code、Claude Platform 官方文档后立单）

## 一句话

别再把所有上下文一股脑塞给模型，也别让一只模型包办计划、干活、摘要和记忆抽取。

LubanCode 要补两根梁：

1. 大文本先落盘，主上下文只留索引、摘要和少量预览；模型需要证据时，再搜索、分块取回。
2. 分出 `cheap_model`、`normal_model`、`lao_model` 三种任务角色；前后二者没配置时，一律回落到 `normal_model`。

这不是把 agent 分成三六九等。模型跟着任务阶段走，不跟着 main/subagent 身份走。子代理仍与 main agent 同级。

## 调研结论：Claude Code 到底做了什么

以下只记官方资料能坐实的事。

### 1. Auto memory 只先装索引头部

Claude Code 把自动记忆存成普通 Markdown。每个项目有一份 `MEMORY.md`，另可拆出主题文件。

- 会话启动只装 `MEMORY.md` 前 200 行或前 25KB，谁先到就听谁。
- `MEMORY.md` 充当索引。Claude 在会话中读写目录里的其他文件，需要时再打开。
- 文件都在本机，用户可以查看、修改、删除。

这正是一种渐进装载：先给目录，后取正文。

但它只管自动记忆。不能由此推断 Claude Code 已把所有历史、工具结果、代码文件都切成同一种块。

官方资料：

- https://code.claude.com/docs/en/memory
- https://platform.claude.com/docs/en/agents-and-tools/tool-use/memory-tool

### 2. Skill 走渐进披露

Claude Code 启动时只装 Skill 名字和 description。`SKILL.md` 正文等命中或手动调用时才装；正文引用的资料又可继续按需读。

这套机制省的是常驻 prompt。它跟记忆分块相似，却不是同一存储协议。

官方资料：

- https://code.claude.com/docs/en/skills
- https://code.claude.com/docs/en/context-window

### 3. 工具定义也能延迟装载

Claude Platform 的 tool search 可把大批工具 schema 留在上下文外。模型先搜工具，再装所需定义。官方把这叫按需发现，不叫记忆。

官方资料：

- https://platform.claude.com/docs/en/agents-and-tools/tool-use/manage-tool-context
- https://platform.claude.com/docs/en/agents-and-tools/tool-use/tool-search-tool

### 4. 大工具输出会落盘

Claude Code 官方 CHANGELOG 明写过两次改动：

- 大 Bash 输出不再直接截掉，改为落盘，模型凭文件引用读取全文。
- 大工具输出改为落盘，不再只留一截；后来又把触发线从 100K 字符降到 50K 字符。

这最接近用户所说的“存成 txt 一样，先给一部分，后头再搜”。

CHANGELOG 只公开外部行为，不公开内部分块格式、索引算法和完整阈值策略。TODO 不冒充知道它的私有实现。

官方资料：

- https://github.com/anthropics/claude-code/blob/main/CHANGELOG.md

### 5. 微压缩有可借的机制，`snip` 却不是已确认术语

现行 Claude Platform 文档把上下文治理分成几层：

- tool result clearing：超过阈值后，按时间清掉旧 `tool_result`，留下“已清除”的占位。
- thinking block clearing：按策略清理旧 thinking。
- client-side compaction：SDK 生成摘要，替换旧历史。
- server-side compaction：达到 input token 触发线后，服务端生成 compaction block；它前面的内容随即失效。

网上有人把清旧工具结果叫 microcompact。官方现行页面不把 `snip` 定成一项稳定协议。LubanCode 可以采用这两个产品名，但须在本项目里自己下定义，不能声称照搬 Claude Code 内部类名。

官方资料：

- https://platform.claude.com/docs/en/build-with-claude/context-editing
- https://platform.claude.com/docs/en/build-with-claude/compaction
- https://platform.claude.com/docs/en/build-with-claude/context-windows

### 6. 全局 compact 确实先认窗口和触发预算

Claude Code 会在接近窗口时自动 compact，也允许 `/autocompact <token count>` 明设触发点。`/context` 会列各类占用。模型、窗口和自定义网关会影响触发线。

Claude Platform 的 compaction API 也先吃一枚 input-token trigger。token counting API 可在请求前算有效输入。压缩本身另花一轮采样，usage 要单独记。

需要留意：Claude Platform 服务端 compaction 明说使用当前请求模型，不能另指定便宜模型。下文“compact 交给 cheap_model”是 LubanCode 自己要做的路由，不是 Claude 原样已有的能力。

官方资料：

- https://code.claude.com/docs/en/context-window
- https://code.claude.com/docs/en/model-config
- https://platform.claude.com/docs/en/build-with-claude/compaction
- https://platform.claude.com/docs/en/build-with-claude/token-counting

### 7. Plan 用大模型、执行用常规模型已有先例

Claude Code 的 `opusplan` 在 Plan Mode 用 Opus，转入执行后换 Sonnet。另有 Haiku alias 和默认模型映射，供轻活、后台功能与特定 subagent 使用。

可借的是“按任务阶段路由”，不是厂牌和型号。

官方资料：

- https://code.claude.com/docs/en/model-config
- https://code.claude.com/docs/en/costs

## LubanCode 现状

这几项已经有了，不能重造：

- `src/agent/context_events.*` 会从历史派生事件账。超长 tool result 在请求视图里变成头尾预览和 `[artifact eN ...]` 引用。
- 原始全文仍留在活历史与 session JSONL。结构压缩只改发给模型的视图。
- 重复读取按精确键和 hash 收敛；tool use/result 仍成对。
- `src/agent/compact.*` 已有分层 map/reduce、热区保留、manifest 守恒、失败不换史。
- compact 已先算 `window_tokens - output_reserve_tokens - protocol_headroom_tokens`。
- `AgentLoop` 发请求前会估 system、tools、history 和输出预留，达到压力线便触发 auto compact。
- 顶不住时仍有 `TrimHistory` 硬裁安全网，并向界面报告。
- 配置已有 `compact_model`。不填便用当前会话模型。

缺口也很清楚：

- 眼下的 `[artifact eN]` 只是展示字样，不是一枚可调用的 artifact。
- 模型不能按 `eN` 搜索全文，不能读某段，也不能知道有哪些块。
- 全文虽在 JSONL，却只能靠 `/export` 给人看；agent 自己没有定向召回口。
- 超大正文仍常驻进程历史，还会写进单只 JSONL。省了 wire token，没省内存、恢复解析和存档膨胀。
- 只有 `compact_model` 一项散装特例。标题、记忆抽取、检索扩词、任务总结仍各拿当前模型。
- `compact_model` 只改 model 字符串，仍走当前 provider。不能完整表达“便宜模型在另一 provider、另有 effort 与窗口”。
- 当前 compact 输入预算主要扣输出预留和协议头。摘要 prompt、模型指令、工具声明、估算误差该如何入账，还没有一张统一预算单。
- 结构压缩是确定性头尾预览。还没有介于预览和全局 compact 之间的局部语义摘要。

## 产品不变量

### 原文不丢

任何 snip、microcompact、global compact 都只能改“模型眼下看哪本账”。原始工具输出和压缩前历史须有可核的真本。

- 引用必须能追回全文。
- `/export` 必须说明哪些内容来自 artifact、哪些来自摘要、哪些已经遗失。
- 只有用户明令清理，或命中公开 retention，才可删真本。
- 磁盘写失败时，不得把内存全文先换成空引用。

### 子代理仍与 main 同级

模型角色按工作分，不按 agent 身份分。

- main 做计划，走 `lao_model`。
- 子代理做计划，也走 `lao_model`。
- main 写代码，走 `normal_model`。
- 子代理写代码，也走 `normal_model`。
- 任一 agent 触发后台摘要，才走 `cheap_model`。

不能写成“子代理默认 cheap”。那会把生命周期差异偷换成能力降级。

### 路由看得见

每次后台模型调用都须记：任务种类、逻辑角色、有效 provider/model/effort、回退缘由、输入输出 token 与耗时。

用户不必看一屏日志。`/context`、`/model roles`、agent 面板和诊断页要能翻出这笔账。

## 三档模型

### 有效值

先定死回退链：

```text
effective_normal = normal_model ?? 当前会话 model
effective_cheap  = cheap_model  ?? effective_normal
effective_lao    = lao_model    ?? effective_normal
```

`cheap_model`、`lao_model` 没设，不报错，不禁功能，径直用 `normal_model`。`normal_model` 也没设，才沿用现有会话 model。

这里的“空”包括字段缺失、空字符串和显式 `null`。三者读入后归一，诊断页仍要显示原始来源。

### 配置外形

简单写法只填模型名，沿用当前 provider：

```json
{
  "normal_model": "qwen3.8-27b",
  "cheap_model": "qwen3.8-4b",
  "lao_model": "qwen3.8-72b"
}
```

高级写法要能带完整路由：

```json
{
  "model_roles": {
    "normal": {"provider": "local", "model": "qwen3.8-27b", "effort": "high"},
    "cheap": {"provider": "local_fast", "model": "qwen3.8-4b", "effort": "low"},
    "lao": {"provider": "remote", "model": "reasoner-large", "effort": "xhigh"}
  }
}
```

旧的三个字符串字段可作 shorthand。解析后都变成一份 `ModelRoute`：

```text
ModelRoute {
  provider
  model
  effort
  context_window
  max_output_tokens
  source
}
```

provider 留空便继承 active provider。model 留空才走上面的角色回退。跨 provider 路由须重建对应 backend，不能只在当前 backend 上换一串 model 名。

### 各自干什么

`cheap_model` 默认接这些活：

- 会话标题与 resume 列表摘要。
- 回合记忆候选抽取、检索扩展词、低风险分类。
- tool result 的 microcompact。
- global compact 的 map 阶段与默认终稿。
- 计划/子任务的机械格式整理。

`normal_model` 默认接这些活：

- 普通对话与实现。
- 工具选择、编辑、运行、排错。
- compact 校验失败后的修补或回退。
- 未另分型的所有调用。

`lao_model` 默认接这些活：

- Plan Mode 的需求澄清、架构权衡和完整计划。
- 用户显式要求的深度诊断、复杂评审。
- 计划返工。normal 执行时发现前提不成立，退回 Plan Mode 再用 lao 修计划。

不要凭 prompt 长短硬猜大模型。路由由明确 `TaskKind` 决定，调用点必须登记。

### Plan 与执行的交接

进入 Plan Mode 后，`lao_model` 只产计划与决策记录，不动工作树。用户验收或模式切换后，把一份有 schema 的 plan artifact 交给 `normal_model`。

plan artifact 至少含：

- 目标和非目标。
- 已确认约束。
- 涉及文件与证据引用。
- 分步改动。
- 测试与验收。
- 未决问题和风险。
- 计划版本、产出模型、时间与来源会话。

normal 执行时不可只拿一段自然语言摘要。它须能追回 lao 看过的证据块。发现证据已变，标计划 stale，再回到计划态；不能悄悄脑补。

## 渐进式上下文仓

### 第一阶段：先让现有 artifact 真能读

保留 session JSONL 为真本，另建内容寻址索引。先不急着改旧存档格式。

```text
~/.lubancode/sessions/<session-id>/context/
  index.jsonl
  blobs/<sha256>.txt
  chunks/<sha256>.json
```

每枚 `ArtifactRef` 至少记：

```text
artifact_id
session_id
tool_use_id
tool_name
mime
encoding
bytes
lines
sha256
created_at
source_message_index
preview
blob_path
chunk_index_path
```

新长结果先原子写 blob，再写 index，最后才把请求视图换成引用。任一步失败，就保留内存正文，照旧发送或触发现有安全网。

第一阶段允许 JSONL 与 blob 各存一份全文，先换来兼容和可恢复。跑稳后再做第二阶段：JSONL 只存 `ArtifactRef`，`/resume`、`/export`、审计工具按需解引用。迁移前须量出磁盘收益，不能只凭好看改格式。

### 分块不能一刀切字节

按内容类型选边界：

- 纯文本、日志：按行，保住时间戳与异常堆栈连续段。
- Markdown：优先按标题，再按段落和行。
- 源码：按行与符号范围；超长函数才续切。
- JSON：先试结构化路径；超大数组按元素窗口。
- 命令输出：把 stdout、stderr、exit code 和截获时间分栏。

每块要有稳定 `chunk_id`、行号、字节范围、局部 hash。UTF-8 不能从码点中腰劈开。

### 给模型两把只读钥匙

新增或复用工具：

```text
context_search(artifact_id, query, max_results)
context_read(artifact_id, chunk_id | line_start+line_count)
```

- `context_search` 只搜当前会话和显式授权的父/子任务 artifact。
- `context_read` 只认稳定 id，不让模型递任意磁盘路径。
- 搜索结果回 chunk id、命中行、小片预览和相关分；不把整份正文顺手塞回来。
- 一次 read 仍守 token 预算。读太大便拒绝，并给可用范围。
- agent 看见 `[artifact ...]` 时，系统提示要说清：预览不足便搜，再读证据；不可把省略号当全文。

### 索引先轻后重

第一版用行索引、关键词和 BM25 就够。别一上来绑向量库。

以后若上 embedding：

- embedding 只是候选召回，精确 id、路径、符号、错误码仍优先。
- 索引可重建，blob 才是真本。
- 模型或 embedding 服务不通，精确检索仍能用。
- secret 扫描和 scope 过滤须在召回前做，不可召回后再遮。

## 四级压缩阶梯

项目内把名词钉死：

### L0：原样与按需装载

短内容照常进上下文。Skill、记忆、工具定义和大 artifact 只先放索引，命中后再装正文。

### L1：snip

`snip` 专指确定性裁面，不调用模型。

- 大工具结果落盘。
- 请求里留元数据、头尾、关键错误行和 artifact id。
- 能按块追回。
- 同一 epoch 内表示钉死，免得破坏前缀缓存。

它不生成语义结论，也不声称“摘要”。

### L2：microcompact

`microcompact` 专指局部语义压缩。默认交给 `cheap_model`。

- 一次只处理冷区里一枚 tool result、一个 episode 或一小组重复观察。
- 产物写成 versioned summary，带 source artifact/chunk 引用。
- 保住决定、错误、路径、符号、命令、退出码、未完成项和用户纠正。
- 原文仍在。模型觉得摘要不够，能回读证据。
- cheap 失败、超时、输出坏格式时，退回 L1，不得顺势删原文。

不要反复拿摘要再摘要。能找到 source artifact 就从原文重做；确实只有旧摘要时，须标 `derived_from_summary=true`，防止信息一轮轮走样。

### L3：global compact

全局 compact 才替换旧会话历史。默认 `cheap_model` 做 map 与 reduce，manifest 校验不降级。

- 先盘预算，再分块，再发模型。
- 当前用户轮、最近工具对、活动 todo、用户纠正、权限决定、文件改动和测试结果进热区。
- 压缩结果带 plan、open items、constraints、next action、artifact refs 和 source epoch。
- cheap 产物校验不过，可用 normal 修一次；仍不过便保留旧历史，报错收场。
- 不因配了 lao 就拿 lao 自动 compact。用户可显式改路由，默认不花这笔钱。

### L4：hard trim

只作最后安全网。它真会丢模型可见内容，必须打警告、记丢了几轮几字节，并指向仍在磁盘的真本。

## 全局 compact 预算

先做一份统一 `ContextBudgetPlan`：

```text
window
- stable_system
- model_instructions
- tool_schemas
- current_user_turn
- protected_hot_zone
- requested_output_reserve
- compact_prompt_overhead
- protocol_headroom
- tokenizer_error_margin
= compactable_history_budget
```

还要另算两项：

```text
summary_target_budget
compact_call_input_budget
```

不能拿“压缩后的目标大小”和“压缩请求本身装得下多少”混成一只数。

### 预算来源

- provider 有 token counting 接口时，优先实数。
- 没接口时走本地 tokenizer；模型目录声明 tokenizer 家族。
- 两者都没有才用 UTF-8 估算，并加公开误差边。
- window、max output 和 reasoning 是否共用预算，从有效 `ModelRoute` 取。
- cheap 与 normal 窗口不同，compact 分块按 cheap 的窗口算，压完再校验 normal 接手后的窗口。

### 触发线

触发线至少分三道，不要只写一枚百分比：

- snip 线：单条大结果刚出来便处理。
- microcompact 线：冷区或旧工具结果占用越线，渐次收拾。
- global compact 线：下一请求 projected usage 连同输出预留逼近窗口。

阈值须能按模型目录和 provider 覆盖。`/context` 展示实际 token 数、百分比、下一条线与还差多少。用迟滞避免刚压完又立刻重压。

## 调用点收拢

建立一只统一 `ModelRouter` 或等价服务。调用方只报 `TaskKind`，不自行拼 model 字符串。

至少收拢这些散点：

- 普通 agent turn。
- Plan Mode。
- `/compact` 与 auto compact。
- microcompact。
- 回合记忆抽取。
- 会话标题、resume 摘要。
- 检索扩展词与候选分类。
- main、前台 subagent、后台 agent。

`compact_model` 迁移为 `cheap_model` 的兼容别名时要谨慎：

- 用户同时写两者，报冲突或按清楚的优先级取值。
- 旧 `compact_model` 只影响 compact，不该突然接管记忆和标题。
- 可先保留旧字段，新增三角色；下一大版本再给迁移命令。

## 界面

### `/model roles`

画一张短表：

```text
角色     provider    model          effort   来源
cheap    local_fast  qwen3.8-4b     low      项目配置
normal   local       qwen3.8-27b    high     当前会话
lao      local       qwen3.8-27b    high     回落到 normal
```

回落须写“回落到 normal”，不能把同名再印一遍，让用户猜。

### `/context`

除 system/tools/history 外，再列：

- inline 正文。
- artifact 预览。
- 可追回全文字节数。
- microcompact 摘要数与原文数。
- 当前预算、输出预留、下一触发线。
- 最近一次 compact 所用角色、模型、前后 token、耗时和校验结果。

### 运行提示

后台小活别刷屏。状态栏短闪即可：

```text
记忆抽取 · cheap:qwen3.8-4b
压缩 62k -> 18k · cheap:qwen3.8-4b
计划 · lao:reasoner-large
执行 · normal:qwen3.8-27b
```

若发生回退，必须留痕：

```text
cheap 不可用，compact 已回落 normal:qwen3.8-27b
```

## 失败与安全

- cheap provider 不通：低风险后台活可跳过；compact 先试 normal，再失败则旧史不动。
- lao provider 不通：Plan Mode 可问用户是否回落 normal；非交互模式按配置回落并在结果里明写。
- artifact 落盘失败：保住内存全文，不生成假引用。
- artifact index 坏了：可从 session JSONL 或 blob 重建；hash 不合立即隔离。
- 跨 provider 切模型：不转发 provider 私有 reasoning 块；先归一成中立消息。
- secret：写 blob 前沿用现有敏感信息策略；路径和索引不得把 key 露进提示、日志或文件名。
- 清理：按 session retention 走，父子任务仍在引用的 blob 不删。内容寻址 blob 要做引用计数或 mark-and-sweep。
- prompt injection：artifact 内容始终标成工具数据，不可混成 system 指令。

## 测试：mock 能测出什么

这些可以靠 mock 稳稳测出：

- 三角色解析、配置优先级和 `cheap/lao -> normal` 回退。
- 跨 provider route 选中哪只 backend，发出的 model/effort/max output 是否正确。
- main 与 subagent 在同一 `TaskKind` 下拿到同一有效角色。
- Plan Mode 用 lao，切执行后用 normal；计划 artifact 原样交接。
- 标题、记忆抽取、microcompact、global compact 是否走 cheap。
- compact_model 旧配置的兼容与冲突提示。
- 预算公式、触发线、迟滞和窗口不够时的拒绝。
- UTF-8、Markdown、代码、JSON、日志分块边界。
- blob/index 原子写、hash 校验、崩溃恢复、重建和 retention 引用保护。
- `context_search/read` 的 scope、行范围、大小上限、越界拒绝。
- snip 后工具对仍配套，原始全文可按 artifact id 逐字追回。
- microcompact 坏 JSON、漏 manifest、超时、空文本时退回 L1 或 normal。
- global compact 漏 todo、漏约束、漏 next action 时拒绝换史。
- `/model roles`、`/context` 和回退提示的终端快照。
- usage 分角色记账，compact 额外采样不混进普通 turn。

mock 做不到这些：

- cheap 模型写出的摘要到底够不够好。
- 一份摘要能否让 normal 模型隔几十轮仍接得上活。
- lao 计划是否真比 normal 好，是否值这份耗时和价钱。
- 各家真实 tokenizer、上下文窗口、reasoning 占用与服务端限额是否同目录声明一致。
- 跨 provider 后，真实 API 对历史块、tool schema、stream 和错误码是否全兼容。
- 用户看终端时是否觉得提示过密、计划切执行是否顺手。

后五项须另设可选的真端评测与人工场景，不可拿 mock 绿灯冒充结案。

## 真端评测

准备一套不进默认 CI 的矩阵。凭环境变量显式开启，不把 key 写 fixture。

场景至少有：

1. 100K 行构建日志落盘，主上下文只见预览；模型搜到指定错误，再读前后 30 行。
2. 连续二十轮读文件、跑测试，先触发 microcompact，再触发 global compact；最后仍说得出改过哪些文件、哪条测试未过。
3. cheap 故意停机，compact 回落 normal；原历史不丢，界面说清。
4. lao 产计划，normal 执行；中途改动计划证据文件，系统能判 stale。
5. main 与 subagent 跑同类长任务，模型角色、窗口、artifact 工具和 compact 策略一致。
6. cheap 窗口小于 normal，分块按 cheap 重算，不撞 `prompt too long`。

评测记 recall：随机抽取原历史里的路径、错误码、用户纠正、未完成项，压缩后提问，统计能否凭摘要或 artifact 追回。不要只看摘要长度。

## 分期

### 第一期：统一模型路由

- 加三角色配置、有效值与来源诊断。
- 建 `ModelRouter`，先接普通 turn、Plan、compact、memory extract。
- 保留 `compact_model` 兼容。
- 补 usage 分账和终端显示。

### 第二期：可追回 artifact

- 把现有 `[artifact eN]` 接上 blob/index。
- 加 `context_search/read`。
- 保住 JSONL 兼容、export 与 resume。
- 量内存、磁盘、恢复时间和 wire token。

### 第三期：microcompact

- 定义冷区和触发线。
- cheap 生成局部摘要，source refs 钉牢。
- 做失败回退、摘要版本和避免摘要套摘要。

### 第四期：预算总账与全局 compact

- 统一 tokenizer、窗口、输出预留和 trigger。
- global compact 接角色路由。
- `/context` 展示预算和各层占用。
- 跑真端 recall 评测，调阈值。

### 第五期：存档瘦身

- 看第二期数据，再决定 JSONL 是否改存 ArtifactRef。
- 提供迁移、回滚、重建、清理和版本兼容。
- 未证明收益前，不动旧真本。

## 验收

- 未配置 cheap/lao 时，所有功能照跑，二者诊断为“回落 normal”。
- Plan Mode 确实由 lao 产计划，执行确实切回 normal；两边模型与 provider 都看得见。
- main 与 subagent 不因身份暗降模型。相同任务种类走相同路由。
- 50K 以上工具结果不再常驻请求正文；模型凭 artifact id 能搜、能分块读、能核 hash。
- snip 不调用模型，microcompact 默认用 cheap，global compact 先算预算再调用。
- compact 前后活动 todo、用户纠正、文件清单、测试结果和下一步守恒。
- cheap 输出坏摘要时，旧历史不动；回退 normal 有记录。
- `/context` 能解释 token 花在哪、何时会压、压缩用了谁、原文还能去哪找。
- 默认单测只靠 mock 也能覆盖路由、预算、分块、恢复和 UI 状态；文档不谎称它验证了摘要质量。
- 至少跑过一轮真端长会话矩阵，并留 recall、token、耗时、压缩次数和失败记录。

## 不做

- 不照搬 Claude 私有目录名和内部阈值。
- 不把“渐进加载”做成一只包打天下的向量库。
- 不把 subagent 默认绑 cheap。
- 不让 cheap 摘要成为唯一真本。
- 不因有 global compact 就删硬安全网。
- 不把模型字符串当完整路由，漏掉 provider、effort、窗口和输出上限。
- 不拿 mock 通过证明真实模型摘要可靠。
