# 终端思考活动条重画与输入框光标跳动

- 状态：真机复测不过（2026-09-03 实录 0.26.175 仍跳——见第八节；一轮源码 0.26.174 已落、单测全绿，二轮修复待 driver 取证开批）
- 日期：2026-09-02
- 级别：P1（终端交互体验与光标所有权）
- 类型：LubanCode 产品缺陷（终端画面重绘）
- 现场版本：当前 `main`，`1a048bd2`（版本线已到 `0.26.172`）
- 范围：`src/cli/console_input_stream_footer.cpp`、`src/cli/terminal_frame.cpp`、`src/platform/terminal_batch.*`、`src/platform/console*`、终端帧测试与真终端驱动
- 关联：`86f9fa87` 引入 turn 级活动条与 200ms 心跳；`25fd55a9` 调整输入区模式行与活动行排位后，现场更容易看见
- 不在本单：模型请求、输入队列语义、IME 文本提交、正文 Markdown 渲染、Agent Panel 数据隔离

## 一、现场

回合运行时，输入框上方显示：

```text
• 思考中  esc 取消 (20s)
确认 shift+tab 切换到 auto 模式
────────────────────────
> 键入并回车 排队下一条 · Esc 打断
```

真机可见光标在蓝色活动点/“思考中”一带与输入框之间反复跳。输入框仍能收字，尚未见草稿丢失；眼前却像焦点被两处争抢，约每秒闪五次。截图只证明画面症状，不证明终端型号，也不证明 DEC 2026 支持情况。

## 二、当前源码路径

1. `StreamFooterHeartbeat::ThreadMain()` 每隔 200ms 推进 `frame`，调用 `UpdateTurnActivityElapsed(frame, elapsed)`。
2. `BuildFooterWorkingLine()` 用 `working_highlight % glyphs.size()` 逐字扫亮“思考中”。秒数没变，亮字也会变，故每拍都改活动行指纹。
3. `RedrawStreamFooterLocked()` 发现活动行变化，走 `QueueInlineFrameDiff()`。
4. `QueueInlineFrameDiff()` 先发隐藏光标，再把终端光标移到活动行清写，随后移回 composer 光标，最后显示光标。
5. `ProbeStdoutConsole()` 只给出 `is_console` 与 `vt_enabled`。它没有“支持 DEC 2026 synchronized output”这档能力。
6. `TerminalBatch` 却默认 `synchronized_output=true`，无条件包 `CSI ? 2026 h/l`。终端若只支持普通 VT，不支持或没有正确实现 DEC 2026，中间移光标步骤便可能露到屏上。
7. 批次落完后，`RedrawStreamFooterLocked()` 还会再调一次 `SetCursorPos()` 钉回输入框；这笔虽不移去活动行，却说明 VT 批与平台光标 API 仍在混用。

源码链与现场节奏相合，故“200ms 活动行重画 + 同步输出能力误判/宿主实现差异”为首要嫌疑。未录制原始 VT 字节与真终端逐帧画面前，不把它写成唯一已证根因。

## 三、现有测试缺口

`tests/unit/cli/test_terminal_frame.cpp` 只断言字节顺序：隐藏光标早于新行、显示光标晚于新行、批次外包 DEC 2026。它没有回答：

- 宿主终端认不认 DEC 2026；
- 一次 `stdout.write` 内的中间 CUP 是否会露帧；
- 不支持同步输出时，实体光标是否曾停到活动行；
- Windows Terminal、经典 conhost、VS Code/xterm.js 与 POSIX 终端是否同款。

纯函数/字节测试即使全绿，也不能冒充真终端已验。

## 四、修复裁决

先止血，再治根；不拿调大刷新间隔遮住所有权问题。

### P0：减少无意义重画

- [x] 活动行只有秒数、阶段或中断态变化时才重画；普通 turn 不再为逐字扫光每 200ms 改一帧。
  （`BuildFooterWorkingLine` 逐字轮换高亮已删，标签恒一色；判据抽成纯函数
  `TurnActivityRowChanged`（terminal_frame），心跳/Spinner 同秒闲拍在入口就收手；
  指纹跳帧闸照旧兜底。）
- [x] 若保留动画，须在不移动实体输入光标的绘制路上跑；做不到便先关闭扫光，只留静态蓝点与秒钟。
  （裁决：关扫光，留静态蓝点 + 秒钟。降级档（未确认 2026）本就不该跑动画，
  确认档也没必要为不可见动画付 5Hz 重画——验收口径"同一秒零新增落笔"一刀切。）

验收：同一秒内无状态变化时，footer 帧审计为零新增落笔；输入框光标不因活动动画离位。
（单测钉 `TurnActivityRowChanged` 同秒假/秒变真；`EndStreamFooter` 帧账行新增
`paint=` 档位字段备真机对账。）

### P0：拆开终端能力

- [x] 将普通 VT、原地重画、DEC 2026 同步输出分开建模；不得再拿 `vt_enabled` 代替同步输出能力。
  （`StdoutConsoleProbe` 补 `sync_output` 档；新增 `ProbeSyncOutputSupport()`——
  DECRQM `CSI ?2026$p` 主动一问、DECRPM 应答 1..4 才算认得，答不上/夹按键一律
  当不支持，结论进程级缓存；交互会话开场（RunInteractiveSession）预热。选路收在
  `platform::PlanInlineRepaint`：vt×sync 四档一张表。）
- [x] 不支持同步输出的宿主走明确降级路：隐藏动画，或用不会搬动实体光标的原生屏幕写入；不得仍发送一串会漏中间态的 CUP。
  （扫光全档已撤；未确认 2026 时 Windows 退原生控制台 API 路
  （PaintInlineFrameLegacy，FillConsoleOutput 一族），POSIX 无原生路、退 VT 批
  不包 2026 且只在真状态变化时单笔落帧；`TerminalBatch` 同步输出改显式入参
  （无默认值），散装 `kSyncOutputBegin/End` 用法全部按档位开关。）
- [x] 收拢 VT 批与 `SetCursorPos()` 的所有权。同一帧只留一条权威光标恢复路，末态恒为 composer 的真实软换行坐标。
  （批有命令时批末笔 MoveTo+ShowCursor 是唯一恢复路，原生路 legacy 末笔
  SetCursorPos 唯一；批零命令才由平台口补钉；指纹跳帧路的每拍无条件原生钉
  改成"探测发现真离位才补钉"。）

验收：支持与不支持 DEC 2026 两档都能跑；降级有测试、有诊断，不静默假装支持。
（四档矩阵单测钉选路；帧账行 `paint=vt-sync|vt|native` 即降级诊断，不静默。）

### P1：测试与真机

- [x] 补能力矩阵单测：VT=true/false、sync=true/false，分别断言选路与输出合同。
  （test_terminal_frame.cpp：`PlanInlineRepaint` 四档 + `TerminalBatch` sync
  true/false 的 2026 包裹合同。）
- [x] 补帧测试：活动行变、composer 不变时，最终光标恒归输入框；无动画档同一秒内零输出。
  （同文件：diff 批末笔 CUP 恒为 composer 坐标、其后仅 ShowCursor/2026l；
  `TurnActivityRowChanged` 同秒零变化钉"同一秒零输出"的判据半边——
  fingerprint 跳帧半边是既有机制。）
- [ ] 用真实交互程序跑 Windows Terminal、经典 conhost、VS Code 终端；能取到环境时再补 Linux/macOS 终端。（待真机：本单在无交互控制台的沙箱/worktree 收口，跑不了真终端；stream_footer_driver G0 幕判据已换新合同——秒钟走动 + 光标全程钉输入框，真机跑时直接用）
- [ ] 真机录制至少 10 秒静置思考与 10 秒边输入边思考；核对无光标跳行、无字符丢失、无残行、无输入延迟突刺。（待真机：同上；帧账行 `paint=` 档位字段与 G0/G7 幕就是录证抓手）

验收：保留命令、终端版本、退出码、录屏/逐帧证据与帧审计数字。源码检查、生成字节、单测全绿都不能代替真机结论。

## 五、预计改动边界

- `src/platform/console.hpp` 与 Windows/POSIX 实现：能力模型。
- `src/platform/terminal_batch.*`：同步输出显式入参及降级合同。
- `src/cli/console_input_stream_footer.cpp`：活动行刷新节奏与光标恢复单一写口。
- `src/cli/terminal_frame.cpp`：差量帧的光标所有权。
- `tests/unit/cli/test_terminal_frame.cpp`、相关 footer 测试：字节与选路。
- `tests/manual/stream_footer_driver.cpp` 或新窄驱动：真终端复现与验收。

## 七、真机复测操作单（源码已收口，剩此一项，留用户真终端跑）

前置：主树先编出驱动器与正式程序（`*_driver` 是 EXCLUDE_FROM_ALL，常规 build 不带）：

```powershell
cd D:\lubancode
cmake --build --preset release --target lubancode stream_footer_driver
```

**第一路：driver 自动判（每款终端各跑一遍）**

```powershell
cd D:\lubancode\build\release\Release
.\stream_footer_driver.exe .\lubancode.exe <一个临时工作目录> <报告文件路径>
.\stream_footer_driver.exe .\lubancode.exe <一个临时工作目录> <报告文件路径> g7
```

三款宿主终端：

1. **Windows Terminal**：新开 WT 标签页直接跑。
2. **经典 conhost**：Win+R 敲 `conhost.exe cmd.exe` 回车，在弹出的老控制台里 cd 过去跑。
3. **VS Code 终端**：VS Code 集成终端里跑。

判据：G0 幕过"秒钟走动 + 活动行至少变两拍 + 物理光标全程钉输入框"；g7 幕帧账行 `paint=vt-sync|vt|native` 记下各终端档位（WT 应 vt-sync，conhost 大概率 native，VS Code 看 xterm.js 实现）。三款各留一份报告文件原文。

**第二路：真交互人眼场景**

`lubancode.exe` 起真会话，问一个答得长的问题：

1. 思考期间**手离键盘静置 10 秒**，录屏——核对光标不跳、活动行只按秒动。
2. 思考期间**连续打字 10 秒**（打一段长草稿别删），录屏——核对无字符丢失、无残行、无输入延迟突刺、光标不离输入框。

**收尾**：两款路子都过了，勾掉第四节 P1 两个留白项，把终端名与版本、报告原文、录屏存档记回本单；任一终端不过，带着报告文件与录屏回来开续单。

## 八、真机复测实录（2026-09-03）——不过，二轮待证

用户真机实测 `lubancode 0.26.175`（已含 0.26.174 双 P0）：长 turn（64s+）、工具面板滚动更新中，光标仍在活动行蓝色小点与输入框首行之间反复跳。终端型号待确认（疑似 Windows Terminal）。

判读与嫌疑：

1. 同秒零重画已单测钉死、扫光已删——还跳，说明跳的那一帧不是动画闲拍。秒钟每秒一帧是设计内的合法重画：这一帧若在真终端露中间态（藏光标→CUP 到活动行→清写→CUP 回 composer→显光标），肉眼即见"每秒一跳"。
2. 嫌疑一：DEC 2026 探测误判或未生效——探测判"不支持"走了降级档，而降级档的落笔路在真终端（WT 一类）上仍露中间态。
3. 嫌疑二：正文区/工具面板更新走的是另一条绘制路（非 footer 活动行路），那条路也在搬光标；单 2 一轮只收了 footer 路的所有权。
4. 一锤定音的取证：driver G0/g7 报告——帧账 `paint=` 档位直接暴露选路，G0 幕全程光标采样直接暴露离位。见第七节操作单，三款终端各跑一遍，报告带回即开二轮修复批。

### 8.1 二轮取证三连（2026-09-03 深夜，主控自跑 driver 判决）

1. **帧账档位**：driver 场景（新开 conhost）`paint=vt-sync`——探测判"支持 2026"走 VT 批+2026 包裹。
2. **conhost 原始应答**（手搓 DECRQM 探针，build/probe_2026.py）：答 `\x1b[?2026;2$y`——mode 2 = 认得该模式、当前关着。微软新 conhost 确已实现 DEC 2026，探测判"支持"没冤枉它。
3. **高频光标轨迹**（driver 补紧密循环轨迹线程，几十 kHz 采样）：每秒秒钟换帧时光标**真实经过活动行行首 (x=0, y=10)——蓝球左边——再回到输入行 (x=2, y=13)**。0.26.174 的"光标所有权"合同在字节层就不成立：footer 帧序列里的 CUP 到活动行照样把 buffer 光标搬过去，2026 同步输出在 conhost/WT 的实现里只缓冲文本渲染，不缓冲光标位置。
4. **旧 G0 判据是漏检假绿**：50ms 轮询抓不到批内毫秒中间态——三轮全 PASS 与用户肉眼所见并不矛盾，是采样率不够。高频轨迹采样已进 driver（G0 段），回归闸从"末态对"升格为"轨迹无中间态"。

### 8.2 二轮修法裁决（2026-09-03 已实施）

- **Windows 上 footer 行级重画改原生 `WriteConsoleOutput` 族**（CHAR_INFO 块按坐标直写，光标原地不动）；VT CUP 序列从 footer 帧序列清出去。四档表重裁：vt-sync 档的"2026 保护光标"前提已被实验否定，Windows 真 console 上一律 native 写行；VT 批只留给 POSIX（无原生 API）且接受"真状态变化才落帧"的低频档。
- driver G0 判据补"高频轨迹无活动行落点"；现有三处 G1/G2 FAIL（状态行 shift+tab、落队框结构、G4 取回标记）须甄别是判据过时还是真回归，一并处理。
- 录屏佐证留真机（用户侧随手可补）。

### 8.3 二轮实施记录（2026-09-03，worktree agent-a01535cf8864cbab3）

1. **平台层原生行直写**（`src/platform/console.hpp` / `console_win.cpp` / `console_posix.cpp`）：新增 `NativeRowCell`（码点 + 16 色属性位 + 宽字半格旗标，属性位与 FOREGROUND_*/BACKGROUND_*/COMMON_LVB_* 逐位同值，console_win.cpp 有 static_assert 钉死）与 `WriteNativeRow(x, y, cells, count)`——Windows 走 `WriteConsoleOutputW` 一次成块，全程不碰光标；POSIX 无缓冲区 API 恒 false（退 VT 批）。越界按交集裁剪，交集为空不算失败。
2. **SGR→单元格构建器**（`src/cli/terminal_frame.cpp` 新纯函数 `BuildNativeRowCells`）：把 footer 行"UTF-8 + SGR"翻成按格单元格——认 0/1/2/22、30-37/90-97、38/48 的 5;N 与 2;R;G;B（xterm-256 调色板近似最近 16 色）、39/49；bold/dim 独立记账、落色时折算强度位（"1;32"先加粗后给色不丢亮档，裸 90-97 自带亮档不被抹）；宽字双格带 LEADING/TRAILING 旗标，放不下整字就截断；尾部铺默认属性空格——"擦行 + 落字"合并成一次直写。
3. **原生画帧路**（`PaintInlineFrameNativeRows`，同文件）：行级 diff 与 `QueueInlineFrameDiff` 共用新抽的 `ComputeInlineRowChanges` 纯核（两条写路看到的"哪行脏"永远一致；VT 批字节合同不变，既有帧测试原样全绿）。直写不可用（非真 console/写失败）退 `PaintInlineFrameLegacy` 老路。
4. **选路重裁**（`src/platform/console_common.cpp` `PlanInlineRepaint` + console.hpp 注释）：Windows 真 console `vt_batch`/`sync_output` 一律 false（footer 走原生直写）；POSIX 保留 VT 批（确认 2026 才包同步输出）。探测（ProbeStdoutConsole/ProbeSyncOutputSupport）保留——POSIX 选路还用，Windows 侧只作诊断档案。`RedrawStreamFooterLocked` 的画帧块改为原生优先（`src/cli/console_input_stream_footer.cpp`），帧落定后一笔 `SetCursorPos` 钉回输入行是本帧唯一光标移动（末态）；帧账审计直写帧照记帧数与脏行、字节恒 0，G7 帧数帽在直写路同样成立。
5. **G0 判据升格**（`tests/manual/stream_footer_driver.cpp`）：高频轨迹"活动行落点"从 INFO 升格为硬 Check（门按轮询首见时刻放行帧滚屏前该行还是正文行时的合法落点，不误伤）。新增 `g0` 单跑模式（进程内假 anthropic 服务供思考剧本，不碰真网络不依赖真后端），剧本带 4.5 秒静默窗(正文停笔、秒钟照走)，采样窗整段落在这段静默里——静默中光标的任何活动行落点都不可能是正文续写。判据灵敏度用它验，三组对照见第 7 条。
6. **三处既有 FAIL 甄别——全是判据过时，不是真回归**：依据 `25fd55a9`（改造输入区模式与思考状态行）把模式行从"下横线之下"挪到了"上横线正上方"（队列区与上横线之间），driver 判据没跟版式走，行号算术整体偏了一格：
   - G1 状态行 shift+tab：旧判据读 `bottom_rule+1`（现在那是空行/坞行），改读 `top_rule-1`（模式行新家）。取证报告 G3 诊断 row[26] 的"确认 shift+tab…"与 row[15] 无 shift+tab 互为印证。
   - G2 落队框结构（正文挪队列区/横线完整/占位复位）+ G2b slash 提示前置 + G4 取回标记：队列消息行从 `rule-1` 挪到 `rule-2`、标题行 `rule-2`→`rule-3`（rule-1 被模式行占了），四组行号同修；G2 落队后的框结构检查（上横线/输入行/下横线/占位提示）从 `status-N` 镜像翻转为 `status+N`。G3 打断后框结构同款翻转。
   - 甄别过程中确认代码无回归：取证 run 里 G2 排队回显、G6 思考预览、G7 帧账（g7 单跑三轮 ALL PASS）、G5 四条全送都绿，排版功能本身健在。
   - 连带两笔（非本单三处，但同场红、同根判据/家伙什过时，一并修）：
     (a) "两轮 turn 尾分界线"——工具边界送达的现行语义是"注入当前轮下一次模型请求，不另起新任务"（interactive_session_assembly.cpp 注释原文），排队消息不产生第二条 Worked for；判据改验会话泵送达时打的用户回显 "> 正文"。
     (b) "G7 帧账行落在 stderr 日志"（全幕独有、g7 单跑恒绿）——ReadAuditFileUtf8 只开 FILE_SHARE_READ，全幕里 G7 子进程偶发挂在 exit 被强杀，咽气瞬间写句柄还在，共享违规读成空串；补 FILE_SHARE_WRITE。基线 0.26.175 取证全幕同款红，非产品回归。
7. **帧前奏拨针也闸掉(实施中发现的一半真凶)**:RedrawStreamFooterLocked 原先每拍重画前都先 `SetCursorPos(bx, by)` 把光标拨到正文续写位——正文以换行收尾时 (bx=0, by) 恰是帧顶(活动行行首),这一拨每秒把 buffer 光标放到活动行一次。灵敏度验证的三组对照(driver g0 幕,高频轨迹硬判据):
   - 旧选路(VT 批)+原拨针 → 红(3 落点,复现取证签名);
   - 旧选路(VT 批)+闸掉拨针 → 绿——说明 conhost 把一次 WriteFile 的 VT 批原子处理,批内 CUP 跨进程采样看不见;取证 8.1 轨迹里那串"活动行落点"其实是这笔拨针(在 WT/ConPTY 下批内 CUP 另算,那里 2026 只缓冲文本渲染);
   - 新路(原生直写)+拆闸恒拨 → 红(1 落点)——证明闸门灵敏、拨针即签名。
   落法:腾位前置判定 `ComputeViewportReveal`(帧装得下、不平移不滚屏时)跳过拨针与腾位后重探;真要腾位照旧先拨后探(滚屏对账靠它)。锁内无并发写屏,前置判定与腾位内部重算无漂移。
8. **单测跟进**（`tests/unit/cli/test_terminal_frame.cpp`）：`PlanInlineRepaint` 四档测试改按 8.2 新裁决（Windows 全 false / POSIX 保留 vt、vt-sync）；新增 `BuildNativeRowCells` 五组合同（明文默认属性、主题 SGR 译位、加粗补亮压暗去亮、宽字双格与整字截断、256 色近似与非 SGR CSI 跳过）；新增 `PaintInlineFrameNativeRows` 合同（管道拒直写退老路、真控制台下越界行无笔可落、构建器产物无光标语义——"写行不动光标"的正面合同由 driver G0 高频轨迹幕在真 console 钉）。

**终验（2026-09-03，本批收口）**：ctest 362/362 全绿（`100% tests passed, 0 tests failed out of 362`）；stream_footer_driver 全幕 58 项 ALL PASS——G0 高频轨迹 0 活动行落点（全幕 G0 段轨迹仅 9 次变化、活动行行号无一落点），G1/G2/G2b/G4/G5/G6 全绿，G7 帧账 `paint=native frames=35 bytes=0`（直写路零 stdout 字节、帧数帽成立）；g0 单跑灵敏度三组对照见第 7 条。另两笔环境发现随单存档：(a) 轨迹账本在工具边界送达 turn 上有既有 turn_overlap 偶发（基线取证全幕同款），会连带队列回队/exit 挂起——渲染验收不依赖它，属轨迹子系统旧账；(b) `TrajectoryEnvOpinion`（trajectory_session.cpp）是死码，LUBANCODE_TRAJECTORY 环境开关没接线——想拿它关轨迹是关不动的，续单可查。

**留白（真机项，不冒充）**：Windows Terminal / 经典 conhost / VS Code 终端三款宿主的全幕 driver 报告与录屏、用户真机肉眼复测（第七节操作单照旧），待用户侧随手补。

## 九、不做什么

- 不删 busy composer，也不禁止回合运行时排队输入。
- 不把活动行挪远一点便算修好；光标仍会漏帧，只是更难看见。
- 不凭截图断言某一款终端必定不支持 DEC 2026。
- 不把旧 `LubanCode真实实测问题记录_20260829.todo` 复活；那本十票已在 `54bf47be` 清账删档，本票单独立账。
