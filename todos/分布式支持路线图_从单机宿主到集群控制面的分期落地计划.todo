# 分布式支持路线图：从单机宿主到集群控制面的分期落地计划

- 状态：研究与规划中（本单不含代码改动，先把既有畅想单落成可执行分期）
- 日期：2026-09-24
- 级别：P1（方向性摸底，不抢占近期 P0 单子的排期）
- 范围：`src/runtime`、`src/app_server`、`src/gateway`、`src/peers`、`src/channel`、`src/sessions`、`src/workspace`、CMake 目标分层（`lubancode_engine/runtime/core/app`）
- 依赖：`LubanCore与CLI分离_核心库独立成宿主底座设计.todo`（硬前置，未完成前本单四~六节不可开工）
- 关联：`多用户多节点集群控制面_NodeBridge与LubanCore宿主畅想.todo`（目标形态与对象模型以它为准，本单不重复画图，只把"远期畅想"切成可排期的批次）、`多租户隔离_身份授权与隔离Worker落地设计.todo`、`多前端外壳与浏览器Tab_AppServer承载设计.todo`、`持久Agent_Gateway接入SessionV3_常驻助理与可靠自动任务总装计划.todo(旧总装单已删,2026-09-17)`
- 不在本单：重画集群对象模型（已有畅想单）、动手实现 LubanCore SDK 分离本身（那是它自己的单子）、承诺具体发布日期

## 一、先下断语

- LubanCode 现在是单机单进程宿主，这是架构决策，不是遗漏——不为赶进度绕开这层判断硬凑。
- 分布式能力不是加一个 RPC 端口就有的，是身份/租约/调度这层缺失，本单不假装能跳过。
- 唯一务实的路径：先把 LubanCore 从 CLI 里剥离成可嵌入 SDK，再谈第二个宿主（Node Bridge）挂上去；顺序颠倒会把单机 Gateway 和集群控制面焊成一坨改不动的代码。
- 本单产出是"排期表"，不是"设计图"——设计图已经有了（见畅想单），缺的是把它切成可以一批一批验收、每批都对当前 HEAD 可核实的路标。

## 二、现状证据（2026-09-24 复核）

- CMake 已有四层目标：`lubancode_engine -> lubancode_runtime -> lubancode_core -> lubancode_app`（`CMakeLists.txt:419/1071/1236/1358`），层次骨架在，但 `lubancode_core` 仍夹带部分 `src/cli/` 纯逻辑，不是可独立发布的宿主底座。
- `src/app_server/server.hpp` 用 `ThreadRecord` 证明"一进程多 Session"可行，但 WS 只服务单条前端连接、断线打断在跑回合、无 durable cursor 补流、无 tenant/ACL——离"多用户订阅总线"还差一整层。
- `src/gateway/` 的持久性分五层（P1 状态持久 ~ P5 运维持久），按 `docs/architecture/gateway/README.md` 批次地图：V0/V1/V2/V4 已落，V3（渠道总装与真 transport）、V5（长任务/Goal/Workflow）、V6（发布验收）仍待实现——单机这条线自己都没走完。
- `src/peers/`（peer_registry / peer_mailbox）是唯一现成的"跨会话"通信，但源码首行注释即"同机首版"：注册表是同用户主目录下的 JSON 文件，传输是 Windows Named Pipe / POSIX Unix socket，协议设计上就不具备跨机器条件。
- `src/channel/transport/`（tcp_socket、tls、ws_client）和 `src/net/http_transport` 已有可用的网络原语（TLS、WebSocket 客户端），是 Node Bridge 回连 Control Plane 时可复用的底层积木，但目前只服务于"连第三方 IM 平台"这一个方向，从未承担过"节点对控制面"的语义。
- `src/channel/work_ledger.hpp` + Gateway 的 durable outbox 模式（先落盘、再上报、失败重试不重跑模型）已经是"本地事实优先、网络只做同步"的正确范式，集群设计里 Node Bridge 的"本地先落盘，再上报"就是同一思路的跨机器版本，可以直接抄作业而不是重新发明。
- 三份关键设计文档状态原样未变：集群控制面"远期畅想，暂不实施"、LubanCore/CLI 分离"设计待实现"、多租户隔离"源码审查与设计，未实施"——过去三周（截至本次复核）没有任何提交触碰这三份单子或 `NodeBridge`/`ControlPlane`/`RuntimeLease` 相关代码。

## 三、为什么必须按这个顺序（阻塞链）

```text
LubanCore/CLI 分离（宿主 API + 组件 SPI 冻结）
        │  没有这层，Node Bridge 只能反过来耦合 CLI 内部类
        ▼
单机 Gateway 收官（V3/V5/V6，P1~P5 持久性全绿）
        │  连单机崩溃恢复都没做扎实，集群故障恢复无从谈起
        ▼
身份与租户层（Tenant/User/ACL，独立于会话执行）
        │  Node/Project/Session 的所有权判定都要靠它
        ▼
Node Bridge MVP（单控制面 + 单节点，mTLS 回连、心跳、租约）
        │  先验证"节点主动连回控制面"这条最小闭环能不能打通
        ▼
多节点调度与配额（Node Registry、Scheduler、Capability Registry）
        │
        ▼
生产化（断线续看、故障恢复裁决、审计、72h soak、发布验收）
```

后一环开工前，前一环没进"验收门"严禁排期——这是畅想单"硬前置"条款的具体化，不是本单新加的限制。

## 四、分期落地（迁移批次）

### P0：地基盘点与合同冻结（不写新分布式代码，只做冻结与复用）

- [ ] 核对 `lubancode_core` 当前仍夹带的 `src/cli/` 纯逻辑清单，产出"待迁出"文件表 → 落点并入 `LubanCore与CLI分离...todo` 现有的现状小节，不在本单另存一份。
- [ ] 给 `src/channel/transport/`（tcp_socket/tls/ws_client）补一份"可否作为 Node Bridge 回连传输层"的能力核查：TLS 双向认证（mTLS）当前是否支持客户端证书，缺口记录。
- [ ] 给 `src/channel/work_ledger.hpp` 与 Gateway durable outbox 补一句"跨机器版复用备注"，供 P3 阶段直接引用，不重写文档。

验收：产出三份核查记录（文件清单 + mTLS 能力表 + 复用备注），不涉及编译产物，评审通过即算完工。

### P1：LubanCore SDK 化（严格跟随 `LubanCore与CLI分离...todo` 自身分期，本单只做"是否已满足集群前置"的验收复核）

- [ ] 宿主 API（创建会话/提交回合/取消/审批/查询/订阅/关闭）四种接入面中的"远程协议"面是否已有 AppServer 之外的第二个实现者验证过（哪怕是测试桩）。
- [ ] 组件 SPI（存储/执行环境/身份/调度适配）是否已有至少一种"非默认实现"通过统一合同测试，证明"可换组件"不是纸面承诺。

验收：两项皆为"是"，方可视为畅想单硬前置满足，进入 P2。任一为"否"，本单在此长期挂起，不允许绕过。

### P2：单机 Gateway 收官（跟随 `docs/architecture/gateway/README.md` 自身批次表，本单只做验收复核）

- [ ] V3 渠道总装与真 transport 落地。
- [ ] V5 长任务/Goal/Workflow 与异步工具执行账打通。
- [ ] V6 发布验收（回归、72h soak、容量扫点、安全、能力表）全绿。

验收：Gateway 批次地图三项全部由"待实现"转为"已落"，且当次复核未发现新增待实现批次。

### P3：身份与租户层（对齐 `多租户隔离_身份授权与隔离Worker落地设计.todo`，本单只做接口面复核）

- [ ] Tenant/User/Group/Role 的最小模型是否已有非集群场景（例如单机多用户共用一台 Gateway）先落地验证，避免第一次实现就绑定在集群协议上。
- [ ] `SecretRef`/`Policy`/`Quota` 是否已有独立于 Session 执行路径的存取合同（不与 Trajectory 正文混账）。

验收：身份层可在无 Node Bridge 的情况下独立跑通单元/集成测试。

### P4：Node Bridge MVP（单控制面 + 单节点）

- [ ] 定义 Control Plane ↔ Node Bridge 的最小信封协议（复用 `src/peers/peer_mailbox.hpp` 的信封/去重/限速设计思路，不是照搬同机实现）。
- [ ] Node Bridge 主动出站连接控制面（mTLS），实现注册、心跳、租约续期三个动作，不要求节点开放入站端口（与畅想单第四节目标形态一致）。
- [ ] 本地事实先落盘（复用 P0 核查过的 work_ledger/outbox 模式），网络仅做同步，断线重连后从 durable cursor 续传，不重放已执行副作用。
- [ ] 最小验收场景：一台控制面 + 一台节点，节点跑一个 headless Session，杀节点进程重启后，控制面视图与节点本地账本恢复一致。

验收：单控制面单节点场景下，断线、重启、命令重放三类故障注入均不产生重复执行或状态丢失。

### P5：多节点调度与配额

- [ ] Node Registry 支持多节点注册与租约到期回收。
- [ ] Scheduler 按节点配额（同场单飞、跨场并发上限）分配 Session，遵循畅想单"在线不等于正在烧算力"的判断。
- [ ] Capability Registry 只搬运已冻结的能力快照与授权，不把"发现到了"当"可以执行"（畅想单第三节 3.4 的边界照搬）。

验收：三节点场景下，配额耗尽时新 Session 排队而非抢占，节点掉线后其上 Session 转入"待重新调度"而非静默丢失。

### P6：生产化验收

- [ ] 断线续看：浏览器/客户端断线重连后，Session 事件按 durable cursor 补齐，不丢事件也不重复。
- [ ] 故障恢复裁决：节点崩溃后，未知副作用的 Session 停在 `NeedsReview`，不假定可安全重跑（与 Gateway P3 执行持久层同一裁决原则）。
- [ ] 审计与安全：AuditEvent 覆盖控制命令来源，通过一轮安全评审。
- [ ] 三平台真机验证 Node Bridge 的服务安装/监督路径（参照 Gateway V4 已有的三平台 supervisor 经验）。

验收：满足畅想单第一节"断线回来还能续看，宿主重启后还能按事实账恢复"这句总纲，且不是靠人工介入达成。

## 五、不做什么

- 不在 LubanCore 分离完成前动手写 Node Bridge 代码——这会把集群协议和 CLI 内部类焊死，畅想单已明确排除。
- 不在单机 Gateway 持久性（P1~P5）没打满之前承诺集群故障恢复——单机都靠不住，集群只会把问题放大到多机。
- 不把 `src/peers/` 现有的同机机制直接改成跨机——它的信封/限速/去重设计可以抄思路，但底层传输（Named Pipe/Unix socket）本身不具备跨机条件，需要在 P4 用 TLS+WS 重新实现。
- 不首版就要求跨节点热迁移（Session 从节点 A 无缝搬到节点 B）——畅想单已明确排除，本单不重新引入。
- 不为了"看起来有进展"而提前开放公网入站端口——Node Bridge 首版只做出站回连。

## 六、最终验收门

P0~P6 全部勾选，且：
- 三份既有设计文档（LubanCore/CLI 分离、集群控制面畅想、多租户隔离）状态字段全部从"未实施/设计待实现/远期畅想"转为"已落"或"进行中有明确批次"；
- P4/P6 涉及的故障注入场景（断线、重启、命令重放）有可重跑的自动化测试，不是一次性人工验证；
- CHANGELOG 有对应发布记录，不是只改了 `todos/` 文档就算数。

在此之前，"LubanCode 支持分布式"这句话只能说"有蓝图、地基未完成"，不能说"已支持"。
