# Agent 任务级 Turn 预算与旧 Step 限制迁移设计

- 状态：P0-0~P0-3 已落地(2026-09-01,worktree agent-a119a426f9bfa566a);P1-0/P1-1 已落地(2026-09-02,worktree agent-a51160f43464368fe);P2 按窗判留档(删除窗未到,不删;两项评估只写结论,见 P2 批次注)。树级预算(max_tree_total_tokens/max_tree_turns 一族)按主人 2026-08-31 裁决跳过,透明展示走 Token 账本单;P0 批次未涉树级,消费接口留待树级预算单。
- 日期：2026-08-31
- 级别：P0（预算硬线的作用域与账实一致）；兼容迁移、诊断与文案为 P1
- 范围：自定义 Agent 定义、`agent` 派工、Workflow Agent 节点、AgentLoop 请求边界、TurnHarness 续投、TaskLedger 预算账、配置与诊断
- 依赖：`子代理递归派工与结构化任务交接设计.todo` 已落地的 `AgentTaskCoordinator`、显式 lineage、`WaitingChildren`、父 mailbox 与统一 TurnHarness
- 关联：`子代理监督器_agent_watch与停滞恢复设计.todo`、`LubanCode使用洞察_Token账本与Prompt审计设计.todo`
- 不在本单：树级 token 总帽、计费金额上限、provider 限流重试策略、主会话总寿命、模型自行填写预算

## 一、先下断语

自定义 Agent 对外不再暴露 `runtime.max_steps_per_turn` 作为主要预算合同。新合同叫：

```yaml
runtime:
  max_turns: 12
```

它只认一层意思：**从一只 Agent 接到任务，到它交回终态，最多准入 12 次逻辑模型请求。**

父代理补话、孩子回信、`SubagentStop` hook 要求续跑，都吃同一本累计账。不得每开一次 `Agent::Run()`，便把计数清零，再领一份额度。

内部仍可保留 `step_index`、`OnModelStepStarted` 等名字。它们是一次 `Run()` 里的循环坐标，不是用户合同。不得再拿内部循环变量替外部任务预算命名。

本单不把主会话的一次用户输入与一只后台任务混为一谈：

- 主会话可以保留“单次用户输入内最多循环几次”的本地保险；
- Subagent 与 Workflow Agent 节点须有“整项任务共可调用模型几次”的总闸；
- 两道闸可以共存，却必须字段分家、账本分家、文案分家。

旧字段先兼容，绝不悄悄换义。迁移窗走完，再删旧路。

## 二、为什么现状不成立

### 2.1 三层概念混在了一处

当前代码里至少有三层“轮”：

| 层次 | 含义 | 当前落点 |
|---|---|---|
| 输入轮 | 一批 user/inbox 输入交给 `Agent::Run()` | `src/agent/loop.cpp::Agent::Run()` |
| 模型 turn | 一次逻辑 provider 请求及其 assistant 应答 | `Agent::Run()` 内的 `step_index` |
| 任务 | 一只 Subagent 从注册到终态，可能含多批输入 | `AgentTool::RunTask()` + `DriveTurn()` |

`max_steps_per_turn` 的现行语义是“每个输入轮最多几次模型请求”。可自定义 Agent 的用户真正想限的是任务总成本与总自治长度。两者不是一回事。

### 2.2 每次 Run 都重置，续投可以重新领预算

`src/agent/loop.cpp::Agent::Run()` 眼下这样记账：

```text
steps_used = 0
for step_index in [0, max_steps_per_turn):
  发模型请求
  收 assistant message
  steps_used++
```

`src/agent/turn_harness.cpp::DriveTurn()` 又可能反复调用 `Agent::Run()`：

```text
初始任务 -> Run A
父消息/孩子结果 -> Run B
新的 mailbox 批次 -> Run C
```

每个 `Run` 都从零起算。`DriveReport.steps_used` 虽会把 A、B、C 加起来，却只作事后展示，不在下一次请求前拿累计值拦门。

于是 `max_steps_per_turn: 12` 可能跑成：

```text
Run A: 12
Run B: 12
Stop hook Run C: 12
任务累计: 36
```

配置写 12，任务却能烧 36。账面虽会显示累计 36，硬闸却仍是每轮 12。字段、执行与结果三处没有说同一件事。

### 2.3 Stop hook 另有重领口

`src/agent/turn_harness.cpp::RunStopContinuation()` 会再调一次 `agent.Run()`。它把续跑步数并进 `DriveReport`，却仍吃一份重置后的 `max_steps_per_turn`。

Hook 不能成为预算豁免口。若 Agent 已用完任务总 turn，`SubagentStop` 可以留下阻断理由，却不得再向 provider 发请求。

### 2.4 旧名 max_turns 曾指向错误作用域

配置解析层仍兼容 `max_turns`，但它只是 `max_steps_per_turn` 的旧别名。若现在直接把旧键改成任务总预算，老配置会在不知情时变严：过去每个 `Run` 各有 N 次，如今整项任务合计 N 次。

这虽更像用户原意，仍属行为变化。不能借“改正命名”暗中改合同。

### 2.5 当前计数只数成功收到的 assistant message

`steps_used` 在完整 assistant message 组好后才加一。请求已经送到 provider，若流中断或接口报错，当前账可能仍记 0。

预算硬线若要管成本，须区分：

- `turns_attempted`：宿主准入并发出过的逻辑模型请求；
- `turns_completed`：完整收回 assistant message 的请求。

硬闸认 `attempted`。诊断同时列两笔。失败请求可能花钱，不能当没发生。

## 三、术语与计数合同

### 3.1 对外术语

本单统一以下名词：

| 名词 | 精确定义 |
|---|---|
| `task` | 一只 Agent 从任务注册到终态的完整寿命 |
| `input round` | 初始任务或一批 inbox/hook 增量触发的一次 `Agent::Run()` |
| `model turn` | 一次宿主准入的逻辑模型请求；请求对应至多一条 assistant message |
| `tool call` | assistant message 中的一枚工具调用；同一 model turn 可有多枚 |
| `transport retry` | provider/backend 内部为同一逻辑请求做的网络重试，不另算 model turn |

用户文档与 YAML 只写 `turn` 时，一律指 `model turn`。若写输入轮，必须写全 `input round`，不许只写 `turn`。

### 3.2 什么算一 turn

在真正调用 backend 之前，宿主须先预留一枚 `ModelTurnPermit`。预留、发出、完成三步分开：

```text
准备请求
  -> TryReserveModelTurn(task_id)
       denied  -> 不发请求，按 TurnLimitExhausted 收场
       granted -> turns_reserved += 1，拿到 permit
  -> 写 durable request boundary
       失败且请求未发 -> AbortBeforeSend，reserved -= 1
  -> CommitModelTurnSent(permit)
       reserved -= 1，turns_attempted += 1，定下 task_turn_index
  -> backend logical request
       完整 assistant message -> turns_completed += 1
       API/流错误             -> attempted 保留，completed 不加
```

一枚 permit 对应至多一次逻辑 backend 调用。backend 肚里的连接重试、退避重发若没有回到 AgentLoop，不得重复扣 turn。

硬帽判断认 `turns_attempted + turns_reserved`，防两条线程同时占到最后一枚。用户可见消费数认 `turns_attempted`；`reserved` 只在运行态诊断中露出，正常收场必须归零。

### 3.3 工具怎样计数

同一 assistant message 里并行调用一枚、三枚或十枚工具，都只消耗一枚 model turn。

工具执行完，若模型还须读结果并继续判断，下一次 provider 请求再消耗一枚 turn。

工具本身另走 `tool_calls` 账。本单不把工具调用数混进 turn。

### 3.4 输出续写怎样计数

因 `length`、空正文或输出预算触发的新 provider 请求，只要回到 AgentLoop 发出一枚新的逻辑请求，就消耗一枚 model turn。

`length_continuations` 仍管“最多准许几次输出续写”；`max_turns` 管“所有种类模型请求合计还能发几次”。两道门取先到者。

### 3.5 0、缺席与负数

- 字段缺席：继承宿主给此类任务的默认值；
- `0`：不设 turn 硬上限；
- 正整数：任务总 turn 硬上限；
- 负数、浮点、字符串：定义不可用，给稳定诊断；
- 上限须有宿主硬帽，防整数溢出与误配超大值，首版沿用现有整数硬帽，不另拍新数。

## 四、对外配置合同

### 4.1 Agent Definition

新写法：

```yaml
schema: 1
name: code-reviewer
description: 审查最近代码改动，查缺陷、安全风险与漏测。

runtime:
  max_turns: 12
  max_output_tokens: 8192
  max_context_chars: 600000
  context_window_tokens: 0
  length_continuations: 1
  execution_mode: auto
  isolation: none
```

`runtime.max_turns` 是任务总 turn。它不得写进 Prompt，不让模型自行修改。

### 4.2 宿主默认

为避开旧 `subagent.max_turns` 别名的历史包袱，新默认键先用无歧义名字：

```json
{
  "subagent": {
    "default_max_turns": 0
  }
}
```

解析优先级：

```text
派工方宿主显式 override
  > Agent Definition runtime.max_turns
  > subagent.default_max_turns
  > 0（不限）
```

模型可见的 `agent` 工具 schema 不暴露 override。宿主、Workflow 或测试夹具可走 typed API 显式收窄；模型不能为自己加预算，也不能把 12 改成 0。

### 4.3 Workflow Agent 节点

Workflow 的 Agent 节点新增：

```yaml
turn_limit: 12
```

旧 `step_limit` 暂保留原语义，标弃用。两者同时出现时不得静默择一：解析失败，要求作者删掉一枚，免得两种作用域并存时猜错。

Workflow 运行器与 `AgentTool` 必须走同一个 `AgentProfileResolver` 和同一份任务预算对象，不许再各算一套。

### 4.4 主会话原有保险

顶层 `max_steps_per_turn` 暂不在本单强改。它继续表示“主会话一次用户输入内，AgentLoop 最多走几次模型请求”。

但须在文档与诊断里改称“主回合局部保险”，不得再拿它充作 Subagent 任务总预算。后续若要改名，应另立兼容单，候选名为：

```text
max_model_calls_per_input
```

本单不借子代理修正，顺手破坏主会话老配置。

## 五、旧字段迁移

### 5.1 兼容窗原则

旧字段不偷换含义：

```yaml
runtime:
  max_steps_per_turn: 12
```

在兼容窗内仍表示“每个 input round 最多 12 次模型请求”。加载时给弃用诊断，并明确说它不是任务总预算。

新旧字段同时出现，定义直接不可用：

```text
agent.turn_budget_conflict:
runtime.max_turns 与 runtime.max_steps_per_turn 不能同现；
前者限制整项任务，后者是待移除的单轮旧限制。
```

不采用“新字段优先”。两者作用域不同，静默覆盖会藏错。

### 5.2 迁移阶段

#### 阶段 A：双读，不双写

- 解析 `runtime.max_turns`；
- 旧 `runtime.max_steps_per_turn` 照旧生效，并给 warning；
- 新建示例、文档、向导只写 `max_turns`；
- `/agent doctor` 列明生效的是 task turn 预算还是 legacy per-run step 预算。

#### 阶段 B：旧字段只读

- CLI 与自动生成器不再产出旧字段；
- `/agent inspect` 给可复制的迁移片段；
- CHANGELOG 提前写明删除版本窗；
- 统计测试覆盖，不做遥测外传。

#### 阶段 C：删旧 Agent YAML 字段

- `runtime.max_steps_per_turn` 在 Agent Definition 中变成稳定解析错误；
- 主会话顶层同名配置仍按自己的兼容计划走，不跟本阶段绑删；
- 历史 session/trajectory 仍能读取旧快照字段，不可据此重放成新任务。

### 5.3 旧 tool JSON 入参

`AgentTool::execute()` 当前还兼容隐藏的 `max_turns` / `max_steps_per_turn` JSON 入参。它们不在模型 schema，却可能被老脚本手写调用。

迁移时：

1. typed 派工请求新增 `max_turns`，含义为任务总 turn；
2. 老 JSON 的 `max_steps_per_turn` 保持旧义并告警；
3. 老 JSON 的 `max_turns` 因历史上是旧别名，不能无版本直接改义；先报弃用并要求改走 typed API 或 Agent YAML；
4. 到明确破坏版本，再让 JSON `max_turns` 与新合同同义；
5. 模型 schema 始终不露预算字段。

## 六、预算所有权

### 6.1 一本真账

任务总 turn 预算归任务记录，不归某次 `Agent::Run()`，也不归 UI、hook 或 `DriveReport`。

建议新增：

```cpp
struct ModelTurnBudgetSnapshot {
    int limit = 0;       // 0 = unlimited
    int reserved = 0;    // 已占名额、尚未发出的逻辑请求
    int attempted = 0;   // 已准入的逻辑模型请求
    int completed = 0;   // 已收到完整 assistant message
};

struct ModelTurnPermit {
    std::uint64_t permit_id = 0;
    int limit = 0;
};
```

`AgentTaskSnapshot`、`TaskSummary`、`TaskOutcome` 都投影这三笔，不另养 `step_limit/steps_used` 平行真账。

### 6.2 请求边界接口

engine 层不能反向依赖 `tools::TaskLedger`。在 `TurnWiring` 或一枚窄接口中注入预算门：

```cpp
struct ModelTurnBudgetGate {
    std::function<std::expected<ModelTurnPermit, TurnBudgetDenied>()> try_reserve;
    std::function<std::expected<int, TurnBudgetCommitError>(const ModelTurnPermit&)> commit_sent;
    std::function<void(const ModelTurnPermit&)> abort_before_send;
    std::function<void(const ModelTurnPermit&)> mark_completed;
    std::function<ModelTurnBudgetSnapshot()> snapshot;
};
```

依赖方向：

```text
AgentLoop
  -> ModelTurnBudgetGate（engine 抽象）
       -> task-scoped adapter
            -> AgentTaskCoordinator / TaskLedger（唯一事实）
```

主会话没有 task ledger 时，可装一枚 input-round local gate；Subagent 与 Workflow Agent 必须装任务级 gate。

### 6.3 原子准入

`try_reserve` 必须在同一锁域完成：

```text
验任务仍为活态
  -> 验 limit==0 或 attempted+reserved<limit
  -> reserved += 1
  -> 生成唯一 permit_id
  -> Touch revision
  -> 返回 permit
```

`commit_sent` 须在同一锁域核对 permit 仍为 reserved，再做 `reserved -= 1; attempted += 1`，并返回从 1 起的 `task_turn_index`。`abort_before_send` 只撤 reserved，不碰 attempted。三口均须幂等或稳定拒绝重复调用。

不得先读 remaining，再到锁外自增。虽同一任务当前串行发模型请求，hook、恢复与未来并行执行不可拿这个前提作永久安全合同。

### 6.4 完成与失败

- 完整 assistant message 入 history 后，`mark_completed` 加一；
- provider/API 错误不回滚 `attempted`；
- 用户在请求发出前取消，abort permit，不计 attempted；
- 用户在流中取消，permit 已消耗，`attempted` 保留；
- durable request boundary 落盘失败时，若请求尚未发出，abort permit；若已跨 `commit_sent`，必须保留 attempted，并记不确定投递状态；
- 同一 permit 的 `mark_completed` 须幂等，防重复回调加两次。

### 6.5 任务恢复

若日后常驻任务支持进程恢复，`attempted/completed/limit` 必须随任务 durable state 恢复。恢复不得把 attempted 清零。

本单先把字段与接口留稳；真正跨进程恢复由 `持久Agent_Gateway自动任务与可靠恢复总装计划.todo` 接手。

## 七、执行流程

### 7.1 初始任务

```text
Register task
  -> freeze max_turns into task snapshot
  -> RunTask
  -> AgentLoop prepares request
  -> task gate reserves a permit
  -> durable request boundary
  -> commit_sent returns turn #1
  -> backend request
  -> assistant committed
  -> completed=1
```

预算须在任务注册时冻结。会话中途改配置，只影响后来派出的任务，不改正在跑的任务。

### 7.2 工具循环

```text
turn #1 assistant -> tools A/B/C
tools settle
before next model request -> reserve permit -> commit turn #2
```

若 turn #1 已是最后一枚额度，工具照常执行并落账。工具结果回来后，下一次请求申请 permit 被拒，任务按 `TurnLimitExhausted` 收口，带回已得工具结果与检查点。不得执行完工具却冒充已有最终结论。

### 7.3 父消息与孩子结果

`DriveTurn()` 从 mailbox 领到新批次后，先不把批次标成已送达。进入下一次 `Agent::Run()` 时：

- permit 获准并真正发出请求，才确认该批已送达；
- turn 预算已尽，批次退回未送达账；
- 收场报告列明“尚有 N 条父消息/孩子结果未送入模型”；
- 不为消耗完预算的任务无限等待新消息。

### 7.4 WaitingChildren

父任务 turn 用完时若仍有活孩子：

1. 向下取消 attached children，或按现有父任务终态策略请求收树；
2. 不再进入 `WaitingChildren` 等一个注定无法吸收的结果；
3. 已完成但未送达的 child outcome 留进部分结果/未送达清单；
4. 父任务收成 `BudgetExhausted(TurnLimitExhausted)`；
5. 不 reparent，不把孩子结果越级交 main 冒充父已整合。

这条须与递归派工单的“父不得甩手先走”共同裁决：预算硬线先到时，父没有资格再花模型 turn 整合，故须收树并如实交部分结果。

### 7.5 SubagentStop hook

Stop hook 可以在预算尚余时要求一次续跑。若 remaining=0：

- hook 仍可被调用并留下阻断理由；
- 宿主不得申请到新 permit；
- 不发 provider 请求；
- 终态保持 turn budget exhausted；
- hook 不得把预算改大，也不得用新任务暗中替原任务续命。

### 7.6 嵌套派工

父子各有自己的任务 turn 账。孩子不从父任务的 remaining 中逐枚扣，因为两只任务可能并行，合同也各自独立。

但这不等于整树成本有了上限。树级总 turn 预算若要做，应另增：

```text
subagent.max_tree_turns
```

它归 root tree ledger，与 `max_tree_nodes/max_tree_total_tokens` 同层。本单只预留消费接口，不顺手实现，免得把单任务修正拖成整树成本项目。

## 八、收场状态与文案

### 8.1 稳定枚举

新增/替换：

```cpp
TaskOutcomeReason::TurnLimitExhausted
TurnVerdict::Reason::TurnLimit
```

旧 `StepLimitExhausted` 只服务 legacy per-run step 路。兼容窗内两者不能混写。

### 8.2 TaskOutcome

新字段：

```cpp
int turn_limit = 0;
int turns_reserved = 0;
int turns_attempted = 0;
int turns_completed = 0;
```

兼容窗内保留旧字段只作历史/legacy 投影：

```cpp
int legacy_step_limit = 0;
int legacy_steps_completed = 0;
```

新任务结果不得同时把 task turn 与 legacy step 两套都写成“生效硬线”。

### 8.3 展示文案

预算尽时：

```text
[budget_exhausted]
模型 turn 预算已用满（12/12 次请求，完整返回 11 次）。
已保留部分结果；另有 1 条孩子结果未送入模型。
```

Dock 运行中：

```text
#7 复核迁移 | running | turn 5/12 | 工具 search
```

无限制：

```text
turn 5/不限
```

旧 Agent：

```text
legacy step 5/12（每输入轮；待迁移）
```

中英文一起改，不留 `step`、`turn` 混账。

## 九、Prompt 与模型行为

### 9.1 模型不决定预算

`agent` 工具 schema 不暴露 `max_turns`。父模型只写任务语义，不替宿主拍成本上限。

宿主可在系统提示中给当前 Agent 看剩余 turn，但那只是事实投影，不能成为权限来源。

### 9.2 将尽提示

当前 `ShouldNudgeStepLimit()` 按单轮 step 注入“步数将尽”。改成按任务 remaining 判定：

```text
remaining == min(limit, 3)
  -> 一次性提示“任务还可调用模型 N 次，请收束并交付”
```

一只任务全程最多提示一次。跨 input round 不得重复念，免得白烧剩余 turn。

### 9.3 提示不代替硬闸

模型无视催办，硬闸照样在请求准入前落下。不得靠 Prompt 约束冒充宿主预算。

## 十、解析、Resolver 与权限收窄

### 10.1 AgentDefinition

`AgentDefinition` 新增：

```cpp
std::optional<int> max_turns;
std::optional<int> legacy_max_steps_per_turn;
```

解析器给稳定码：

```text
agent.turn_budget_type
agent.turn_budget_range
agent.turn_budget_conflict
agent.legacy_step_budget
```

### 10.2 ResolvedAgentProfile

运行档不要再只带一枚含糊整数。建议拆成：

```cpp
struct AgentTurnBudgetProfile {
    int max_turns = 0;
    BudgetValueSource source = BudgetValueSource::Default;
    std::optional<int> legacy_max_steps_per_input;
};
```

AgentTool 与 Workflow 走同一 resolver。两路逐字段对账，确保同一 YAML 得到同一预算。

### 10.3 只能收窄

宿主 typed override 只能把定义的正数改小，不能改大；定义是 0（不限）时，宿主可给正数；定义是正数时，override=0 属放宽，稳定拒绝。

错误码：

```text
agent.turn_budget_widening
```

父子任务各有独立预算，不用“父剩 3，所以孩子最多 3”这条假规则。若要管整树，交给树级共享预算。

## 十一、轨迹、Hooks 与可观测性

### 11.1 轨迹事件

每次 permit 准入落一条结构化边界：

```text
model.turn.started
  task_id
  agent_run_id
  task_turn_index
  turn_limit
  input_round_index
  request_id
```

完整收回：

```text
model.turn.completed
  task_turn_index
  stop_reason
  usage
```

失败：

```text
model.turn.failed
  task_turn_index
  error_class
  delivery_state
```

不得靠数 assistant message 猜 attempted。

### 11.2 Hook 字段

`SubagentStart` 增：

```text
turn_limit
```

`SubagentStop` 增：

```text
turns_attempted
turns_completed
turn_limit
budget_exhausted_reason
```

字段只读。Hook 不能回写预算。

### 11.3 `/doctor agents`

新增：

```text
subagent task turn budget: 12 (source: project Agent definition)
legacy per-input step budget: unset
budget owner: TaskLedger task record
continuations share task budget: yes
stop-hook continuation shares task budget: yes
```

若 Agent 仍用旧字段，doctor 黄字警告并给迁移片段。

## 十二、失败与并发合同

| 场景 | turn 账 | 收场 |
|---|---|---|
| permit 前用户取消 | 不加 | stopped |
| reserve 后、请求前 durable boundary 失败 | abort reserved；attempted 不加 | failed |
| 请求已发，API 报错 | attempted +1，completed 不加 | failed/api_error |
| 流到一半用户取消 | attempted +1，completed 不加 | stopped，留部分文本 |
| assistant 完整返回 | attempted +1，completed +1 | 继续或完成 |
| 同一 assistant 并行三工具 | 不另加 | 工具各自入账 |
| mailbox 续投时额度尽 | 不发请求 | budget_exhausted，批次退回 |
| Stop hook 要续跑但额度尽 | 不发请求 | budget_exhausted |
| 两线程误对同 task 申请最后一枚 | 只许一枚成功 | 另一枚稳定 denied |
| `max_turns=0` | 只记 attempted/completed，不拦 | 靠其他终止条件 |

“请求是否已发”须由请求边界给出明确状态，不靠错误字符串猜。

## 十三、测试计划

### 13.1 先铆缺口

- [ ] 加回归：`max_steps_per_turn=2` 的初始 Run 用满后，mailbox 续投可再跑 2；证明现状确会重领预算。
- [ ] 加回归：初始 Run 正常收口后，Stop hook 续跑重新拿整份 step 上限。
- [ ] 加回归：`DriveReport.steps_used` 可大于 `TaskOutcome.step_limit`，证明展示累计与硬闸作用域打架。
- [ ] 缺口测试先放明确注释；不把预期失败塞进默认 CTest。

### 13.2 纯函数与台账

- [ ] `TryReserveModelTurn(limit=2)` 连取两枚成功，第三枚稳定拒绝。
- [ ] `limit=0` 连取不拒，commit 后的 `task_turn_index` 单调。
- [ ] 两线程争最后一枚，恰一枚成功，attempted 不超限。
- [ ] reserve 后 durable boundary 失败，abort 归还名额，reserved 回零。
- [ ] 同一 permit 不可 commit 两次；commit 后不可 abort。
- [ ] `mark_completed` 幂等，重复完成不加二次。
- [ ] API 错误不回滚 attempted。
- [ ] 任务终态后再申请稳定拒绝，不改 revision 之外的账。

### 13.3 AgentLoop

- [ ] 一次模型响应并行三工具，只消耗一个 turn。
- [ ] 工具结果后的下一请求再消耗一个 turn。
- [ ] 最后一枚 turn 返回 ToolUse 时，工具可执行；其后不得再发模型请求。
- [ ] 请求前 budget denied 时，backend 捕获请求数不增加。
- [ ] 流错误：attempted=1、completed=0。
- [ ] 完整正文：attempted=1、completed=1。
- [ ] provider 内部 transport retry 不重复申请 permit。
- [ ] 将尽提示全任务只注入一次，跨 Run 不重发。

### 13.4 TurnHarness 续投

- [ ] 初始 Run 与 mailbox Run 共吃 max_turns=3，累计第三次后封门。
- [ ] child completion 批次在预算尽时退回未送达账。
- [ ] `WaitingChildren` 遇 turn 耗尽不死等，向下收树并终态。
- [ ] Stop hook 续跑吃剩余额度，不重置。
- [ ] Stop hook 在 remaining=0 时 backend 零调用。
- [ ] `DriveReport` 的 attempted/completed 与 task snapshot 完全一致。

### 13.5 前后台与递归

- [ ] 前台 Subagent、后台 Subagent 同一口径。
- [ ] 后台父收到前台孩子结果，续投消耗父剩余 turn。
- [ ] 后台父收到后台孩子结果，续投消耗父剩余 turn。
- [ ] 父 turn 尽时取消 attached child，不越级投 main。
- [ ] 两只并行任务各守自己的额度，不串账。
- [ ] 子任务额度不误扣父任务额度。

### 13.6 自定义 Agent 与 Workflow

- [ ] YAML `runtime.max_turns` 正例、0、负数、错型、超帽。
- [ ] 新旧字段同现报 `agent.turn_budget_conflict`。
- [ ] 旧字段单独出现仍按 legacy 语义跑，并给 warning。
- [ ] AgentTool 与 Workflow 对同一定义解析全等。
- [ ] typed override 收窄成功，放宽报 `agent.turn_budget_widening`。
- [ ] Workflow `turn_limit` 真能跨续投封总账。
- [ ] Workflow 新旧限制同现明拒。

### 13.7 展示与历史

- [ ] Dock 显示 task turn attempted/limit，不拿 completed 冒充消费数。
- [ ] 结果页同时写 attempted/completed。
- [ ] `/agents`、`/agent doctor`、`/agent inspect` 显示来源与 legacy 警告。
- [ ] 中英文文案一一对账。
- [ ] 旧 session/trajectory 的 `step_limit/steps_used` 仍能查看与导出。
- [ ] 新轨迹 verifier 核对 started/completed/failed 不重号、不超 limit。

## 十四、实施批次

### P0-0：冻结现状缺口

- [x] 补三枚“续投重领预算”缺口回归。（tests/unit/agent/test_agent_turn_budget.cpp 顶部两用例:续投重领 2+2、Stop 钩子重领 2+2,并断言 DriveReport.steps_used(4)>limit(2);legacy per-run 语义钉死,注释写明是兼容窗现状不是榜样）
- [x] 给现有 `step/turn/run/task` 术语落一份代码注释词表。（src/agent/turn_budget.hpp 文件头:task/input round/model turn/step 四条,含记账口径与旧名陷阱）
- [x] 盘清所有 `step_limit/steps_used/max_turns/max_steps_per_turn` 消费方，生成迁移清单并钉进测试注释。（test_agent_turn_budget.cpp 顶部“旧 step 限制的迁移清单”:写口 7 处、读口 12 处逐文件钉账,搬动前先对册）

验收：测试能精确证明当前限制只管单次 Run，不能管整项任务。✔（缺口回归用例 + 新账对照用例同册并存）

### P0-1：任务级 turn 真账

- [x] 在 engine 层立 `ModelTurnBudgetGate` 与 typed permit。（src/agent/turn_budget.hpp:ModelTurnPermit/ModelTurnBudgetSnapshot/TurnBudgetAccount 状态机 + ModelTurnBudgetGate 窄接口;引擎不认 tools）
- [x] 在 TaskLedger 任务记录加入 limit/attempted/completed。（TaskRecord::turn_account,含 reserved/completed 分账与 permit 幂等轨迹）
- [x] 实现原子 reserve/commit_sent/abort_before_send 与幂等 `MarkModelTurnCompleted`。（台账锁内"验活态->验额度->占额"同一锁域;commit 两次/commit 后 abort/重复完成均稳定拒绝;tests/unit/agent/test_turn_budget_account.cpp 11 用例含两线程争最后一枚）
- [x] 任务注册时冻结预算来源与数值。（Register/TryRegisterChild 事务内 FreezeLimitLocked(snapshot.turn_limit);来源链在 Resolver(AgentTurnBudgetProfile.source)）
- [x] AgentLoop 在请求发出前取 permit，assistant 落 history 后确认 completed。（loop.cpp 请求边界三口:reserve->boundary(失败 abort)->commit_sent->send;PushMessage 后 mark_completed）
- [x] API 错、取消、流断按 §6.4 对账。（commit 后失败保留 attempted、completed 不加;取消/流断/报错三路各有用例）

验收：任何任务的 attempted 永不超过正数 limit；provider 捕获请求数与 permit 账逐枚对应。✔（denied 时 backend 捕获数不增的用例钉死）

### P0-2：续投与 Stop hook 共账

- [x] `DriveTurn` 的初始、mailbox、child completion 各轮共用 task gate。（RunTask 装一枚 task-scoped adapter 进 turn_wiring,整任务寿命同一本账;轮边界先问有无活儿再问额度——末枚额度交出结论且无待办按自然完成收口,有挂起批次才翻 TurnLimit）
- [x] `RunStopContinuation` 共用同一 gate。（同一条 turn_wiring 递进续跑环;额度尽时 backend 零调用、钩子理由仍留痕）
- [x] 额度尽时退回 inflight continuation，不确认送达。（领到批后撞门 restore 退回未送;mid-run 拒绝同样退批）
- [x] `WaitingChildren` 与取消树按 §7.4 收口。（continuation 源在进 WaitingChildren 前查账,额度尽即返;收尾补判旗 + 向下 CancelTask 收树;child outcome 留未送达清单,不 reparent 不越级）
- [x] TurnVerdict/TaskOutcome 接 `TurnLimitExhausted`，保住部分结果。（TurnVerdict::Reason::TurnLimit + TaskOutcomeReason::TurnLimitExhausted,收场带"attempted/limit(完整返回 completed)"账面与检查点兜底）

验收：初始任务、父消息、孩子结果、Stop hook 合计只准发 N 枚逻辑模型请求。✔（引擎级 max_turns=3 封门/Stop 零调用用例 + AgentTool 任务级 2 枚封门用例）

### P0-3：自定义 Agent 与 Workflow 接线

- [x] Agent Definition 解析 `runtime.max_turns`。（agent_definition.*:非负整数、沿用 int 硬帽;新旧同现明拒归 P1-0）
- [x] AgentProfileResolver 合并预算与来源。（AgentTurnBudgetProfile{max_turns,source,legacy_max_steps_per_input};链:override>定义>subagent.default_max_turns>0）
- [x] AgentTool typed 派工快照接任务总预算。（SubagentBudget.max_turns -> snapshot.turn_limit -> 注册冻结;JSON 入参不暴露）
- [x] Workflow Agent 节点接 `turn_limit`，与 AgentTool 对账。（parser/definition 双解析器 + 同现明拒;host_executors 装节点寿命本地 TurnBudgetAccount;workflow_wiring 经同一 Resolver,配置默认取 agent_tool->default_max_turns() 只读口;对账测试扩进 CheckProfilesIdentical）
- [x] override 只可收窄。（Resolver 解析口判 agent.turn_budget_widening:改大或抹 0 均拒;定义 0(不限)时可给正数）

验收：同一 Agent 定义从 AgentTool 与 Workflow 两路跑，预算数、来源、收场原因逐字段一致。✔（test_agent_profile_resolver 两路对账用例,turn_budget 三笔入 CheckProfilesIdentical）

### P1-0：兼容迁移

- [x] 保留 legacy per-run step 路，隔离类型与字段。（P0 已拆 AgentDefinition.max_turns / max_steps_per_turn 两枚独立 optional + AgentTurnBudgetProfile.legacy_max_steps_per_input 影子账;本批补上同现明拒后隔离闭环——两条路各自执法、各自投影,不混写）
- [x] 新旧同现明拒，不静默选边。（agent_definition.cpp:同现报 agent.turn_budget_conflict,定义不可用;单独旧键按旧义生效 + agent.legacy_step_budget 弃用警告;max_turns 类型/越界带 agent.turn_budget_type/range 稳定码。用例:tests/unit/agent_catalog/test_agent_definition.cpp"新旧预算字段同现"等三册）
- [x] 老 JSON 入参按 §5.3 给弃用提示。（agent_tool.cpp ExecuteDispatch:两枚键均按 legacy 旧义收值,随结果带回一行 [弃用提示] 并落 LogSink 警告;max_turns 未改义,改义等明确破坏版本。用例:test_agent_tool.cpp 两处 [弃用提示] 断言）
- [x] `/agent doctor` 与 `/agent inspect` 给迁移建议。（doctor:runtime 行登 max_turns + "预算合同"三行(哪条路生效/迁移建议/预算归属);inspect:legacy 定义给可复制迁移片段。/doctor agents 另列 subagent task turn budget 与 legacy 影子账。用例:test_agent_commands.cpp P1-0 两册）
- [x] fixtures、示例与文档全改写新字段。（complete.yaml/smoke-probe.yaml/catalog 与 packages 夹具、code-reviewer.yaml、browser-reviewer.yaml 改 max_turns;5 份 workflow 示例 step_limit→turn_limit;agents.md/configuration.md/workflow-schema.md/tools.md/naming.md 同步）
- [x] CHANGELOG 写明兼容窗与删除条件。（v0.26.160 条目:删除窗=自本版起至少跨两个 minor(预计 0.29.0 起转稳定解析错误),窗内老定义不突变、历史照读）

验收：老定义不突变；新定义不再掉进每轮重置漏洞；用户一眼看得出自己走哪条路。✔

### P1-1：可观测性与文案

- [x] Dock、详情、通知、TaskOutcome 全换 task turn 账。（agent_panel_presenter.cpp AgentStateWord 带 turn_limit/attempted/completed 三参:运行中"turn N/M"(不限"turn N/不限"),耗尽"耗尽 · turn N/M(完整返回 C)";legacy 步数只在没设任务总 turn 时投影且明标"每输入轮;待迁移"。task_ledger.cpp ComposeOutcomeText/CompletionNoticeLines/DrainCompletionNotices 同口径:turn 行是唯一"生效硬线",不再与步数行并写）
- [x] hooks 增 turn limit/attempted/completed。（SubagentStart 带 turn_limit;SubagentStop 带 turn_limit/turns_attempted/turns_completed/额度尽时 budget_exhausted_reason="turn_limit";前台 EmitWith 与后台 Emit 两路都带,只读）
- [x] trajectory 落 started/completed/failed 边界。（LoopBoundaryRecorder 新口 OnRequestSentWithTurn:commit_sent 提到 sent 前(顺带治了"commit 失败先记 sent"的糊账),model.request.sent 载荷带 task_turn_index/turn_limit/input_round_index;completed/failed/cancelled 收口按 request_id 对回带 task_turn_index。TurnWiring.input_round_index 由 DriveTurn 每轮钉(旧调用方保持 0)。用例:test_agent_turn_budget.cpp 三册 + test_runtime_wiring.cpp 一册）
- [x] verifier 核对 `task_turn_index`、limit 与终态。（ScanStream 收 sent 的 turn 序列:不从 1 严格递增报 turn.index_repeated/skipped,越限报 turn.index_over_limit,终态后新 sent 报 turn.sent_after_terminal,终态后无收口报 turn.request_dangling;活账(无终态)不判悬空。用例:test_session_verifier.cpp 四册）
- [x] 中英文与帮助页同步。（i18n.cpp 中英成对新增 agent_status.turn_* 一族与 doctor.agents.turn_budget/legacy_step_budget;"步数"行正名"主回合局部保险(每输入轮)";doctor.agents.note 改口;无 /help 条目涉预算,查过）

验收：运行中、收场后、导出轨迹三处看到同一组数字，不再出现“36/12 步”这类假账。✔（三处都从 TaskRecord::turn_account 或其请求簿取数;legacy 投影只在 turn_limit==0 的任务上出现）

### P2：旧路退场与树级预算预留

- [ ] 到明确版本窗后，Agent YAML 删除 `runtime.max_steps_per_turn`。（**窗未到,不删**。CHANGELOG v0.26.160 定的窗:自该版起至少跨两个 minor 版本,预计 0.29.0 起转稳定解析错误。条件:窗内 /agent doctor 的迁移建议至少观察一个 minor 周期、存量定义的 legacy 警告不再新增(可由 doctor 汇总判断)。到窗后另立删除单,按 §5.2 阶段 C 办:主会话顶层同名配置不跟绑删,历史 session/trajectory 照读旧快照字段）
- [ ] 清掉 `StepLimitExhausted` 在自定义 Agent 主路的消费。（**同窗判,不删**。现状:自定义 Agent 主路的收场分型里 StepLimit 只在没装任务 turn 账时出现(turn_limit>0 时 TurnLimit 先到先收,ClassifyTurnEnd 的序钉着);真正清消费须等 legacy 键删除窗到,同上一条一起办）
- [ ] 评估 `subagent.max_tree_turns`，另立实施单，不在本批偷做。（**评估结论:另立单,本单不做**。理由:主人 2026-08-31 已裁决树级预算(max_tree_turns 一族)跳过,透明展示走 Token 账本单;§7.6 只要求预留消费接口——P0 的 ModelTurnBudgetGate 已是消费口,root tree ledger 挂同一只接口即可,不必现在动。建议单名"树级Turn预算与root账单设计",范围:root tree ledger 归属(与 max_tree_nodes/max_tree_total_tokens 同层)、父子并行的准入次序、树级耗尽的收树分型)
- [ ] 评估主会话顶层 `max_steps_per_turn` 是否另改 `max_model_calls_per_input`。（**评估结论:暂不改,要改另立兼容单**。理由:§4.4 已定"本单不借子代理修正顺手破坏主会话老配置";现行为"主回合局部保险"已在 configuration.md 与 /doctor agents 正名,语义无歧义;改名收益(名字自释)小于兼容成本(顶层配置+env+两层配置文件三处双读)。若日后改:候选名 max_model_calls_per_input,走 config 命名规范单的兼容窗流程,与本单 legacy 键删除窗错开)

验收：对外 Agent 合同只剩任务级 turn；内部 step 只作局部循环坐标。（**部分达成,按窗递进**:自定义 Agent 的对外合同已是 runtime.max_turns(示例/文档/向导只写新键);legacy 键与 StepLimitExhausted 的最终退场等 0.29.0 窗,见上）

## 十五、预计改动边界

| 文件/模块 | 职责 |
|---|---|
| `src/agent/loop.*` | 请求前取 permit、完成确认、局部 step 退回内部概念 |
| `src/agent/turn_harness.*` | 多 input round 共用任务 gate、续投退回、Stop hook 共账 |
| `src/agent/runtime_profile.hpp` | 拆 task turn 与 legacy per-input step 档案 |
| `src/agent/agent_definition.*` | 新 YAML 字段、冲突与弃用诊断 |
| `src/agent/agent_profile_resolver.*` | 合并 max_turns、来源与只可收窄规则 |
| `src/tools/task_ledger.*` | 任务 turn 唯一真账、原子准入、结果投影 |
| `src/tools/agent_task_coordinator.*` | task-scoped gate adapter 与生命周期拒绝 |
| `src/tools/agent_tool.*` | 派出冻结、收场分型、legacy JSON 迁移 |
| `src/workflow/*` | `turn_limit` 与 AgentTool 同源解析 |
| `src/app/agent_panel_presenter.*` | Dock/详情投影 |
| `src/app/commands/agent_commands.*` | list/doctor/inspect 诊断与迁移建议 |
| `src/runtime/trajectory_*` | turn 请求边界与 verifier |
| `src/hooks/*` | SubagentStart/Stop 预算字段 |
| `src/config/*` | `subagent.default_max_turns`，保留旧配置语义 |
| `src/cli/i18n.cpp` | 中英文文案 |
| `docs/reference/agents.md` | Agent Definition 新合同 |
| `docs/reference/configuration.md` | 主回合局部保险与子任务总预算分家 |
| `docs/reference/workflow-schema.md` | Workflow `turn_limit` |
| `tests/unit/agent/*` | loop/harness/递归/预算回归 |
| `tests/unit/agent_catalog/*` | 解析、Resolver、命令诊断 |
| `tests/unit/workflows/*` | Workflow 对账 |

超出此表须先在本单补边界，再动代码。不得一边实现，一边把预算逻辑散进 UI、hook 与 provider 各养一份。

## 十六、不采用的方案

### 16.1 只把字段改名

不采用。`max_steps_per_turn -> max_turns` 若只换皮，`Agent::Run()` 仍会每轮重置，漏洞一字不动。

### 16.2 用 DriveReport 累计后再判断

不采用。那是请求结束后的账。硬闸须落在下一枚 provider 请求发出之前。

### 16.3 只靠 Prompt 叫模型收口

不采用。Prompt 可催办，不能作成本与安全边界。

### 16.4 把工具调用也算 turn

不采用。一次 assistant message 可并行发多枚工具；把工具数混进 turn 会让同一模型行为因批处理方式不同而改变预算。

### 16.5 API 错误不扣 turn

不采用。请求既已发出，可能已经产生费用。attempted 须保留；completed 另记。

### 16.6 直接复用旧 max_turns 并偷换语义

不采用。旧配置里的 `max_turns` 已被当作 per-run step 别名，直接改义会让老任务无声变严。

### 16.7 父子共用一枚 task turn 计数

不采用。父子可以并行，又是两只独立任务。整树成本另立 root budget，不能拿父任务局部账硬凑。

### 16.8 把预算 owner 放进 AgentLoop

不采用。AgentLoop 会被多次调用，正是重置漏洞的根。它只消费 gate，不拥有任务总账。

## 十七、最终验收门

以下硬门全过，方可把本单标成收官：

1. 一只 Agent 无论经历多少次 `Agent::Run()`、mailbox 续投、孩子回流与 Stop hook，正数 `max_turns=N` 下 provider 逻辑请求总数不超过 N。
2. `turns_attempted` 与实际逻辑请求边界逐枚相合；`turns_completed <= turns_attempted` 永真；正常终态 `turns_reserved == 0`。
3. API 错误、流中断、用户取消各有测试，不把可能已计费的请求抹成 0。
4. 最后一枚 turn 返回工具调用时，工具结果与部分结论不丢；下一枚模型请求被硬闸挡住。
5. 父任务预算尽时不死等孩子、不越级交付、不重领预算。
6. AgentTool 与 Workflow 对同一 Agent Definition 解析结果全等。
7. 新旧字段同现稳定拒绝；旧字段单独使用不偷换语义并给弃用提示。
8. `/agents`、`/agent doctor`、`/agent inspect`、Dock、TaskOutcome、hook、trajectory 数字一致。
9. 中英文文案不混用 step/turn；文档把 input round、model turn、task 三层写清。
10. 相关 focused tests 全绿；完整 CTest 全绿。多配置生成器用正确 `-C Release`，不得拿错配置的旧二进制冒充验证。
11. Windows 与 Linux 至少各跑一次 fake backend 并发/取消回归；ThreadSanitizer 能跑则留结果，不能跑就明写未验。
12. 至少做一场真实 provider 冒烟：额度 3，工具往返后第 4 枚逻辑请求确实未发。记录 provider、model、请求数、usage 与未测项，不拿单场冒烟冒充性能结论。
13. CHANGELOG 写清行为、兼容窗与迁移命令；旧历史仍可读。

## 十八、收官自查表

- [ ] 产品合同写的是 task-wide model turns，不是内部 step。
- [ ] 预算 owner 在 task ledger，不在单次 Run。
- [ ] 请求前原子 reserve，发出时 commit，未发时 abort，请求后幂等确认。
- [ ] attempted/completed 分账。
- [ ] mailbox、child completion、Stop hook 共账。
- [ ] legacy 字段不偷换含义。
- [ ] 模型不能填写或放宽预算。
- [ ] 部分结果与未送达批次不丢。
- [ ] 主会话局部保险与子任务总预算分家。
- [ ] 树级预算没有被本单假装做完。
- [ ] 测试、文档、诊断、轨迹一起落地。
