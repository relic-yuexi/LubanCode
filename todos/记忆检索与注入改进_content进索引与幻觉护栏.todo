# 记忆检索与注入改进：content 进索引与幻觉护栏

- 状态：待实施（2026-09-03 立，LoCoMo-MC10 评测发现驱动）
- 级别：P1（记忆系统从"有净增益"到"增益更高且不帮倒忙"）
- 类型：LubanCode 产品改进（检索与注入策略）
- 现场基线：LoCoMo E1 全量基线 R@1=0.341 / R@3=0.554 / R@5=0.641（498 可定位题，零命中 0）；E2 双态 98 题配对——可答四桶 +0.20~0.40，adversarial 桶 B 幻觉率 80% vs A 50%
- 证据：`eval/locomo/retrieval_baseline.md`、`eval/locomo/report.md`（数据本地 `eval/locomo/`，账本与逐题账可复算）
- 关联：《记忆系统评测_LoCoMo-MC10防作弊基准.todo》（验收基建）、《智能体记忆效果_个性化适配续接快照与在线评测.todo》（§五检索与预算演进的大方向）
- 不在本单：向量库/嵌入检索（另议）；记忆写入侧抽取质量；训练模型

## 一、三个 bad case 与修法

### 1.1 content 不进索引（P1 首刀）

`EntryIndexText`（src/memory/project_memory.cpp 一带）只拼 title+summary+keywords——正文完全不参与 BM25。LoCoMo 题问的细节（"她研究过什么""那天说了什么"）大多在正文，summary 只有一句概述，R@1 天花板被索引缺项钉死。

- [ ] content 进 `EntryIndexText`：全文太肥则按句/段抽词（与现有中文二元片段分词同款手艺），索引词带权重（title > keywords > summary > content，量级以 E1 复跑调）
- [ ] 命中后的注入策略配对改：条目命中靠正文时，注入除 summary 外带正文相关段截断（现在只注 summary+元数据）；预算内截断规则明确
- [ ] 索引体积与召回延迟对账：catalog 大小、BM25 扫描耗时的前后对照（记忆单 §五"不能只追 Recall"）

验收：E1 复跑，R@1 0.341 → **≥0.45**（五类分桶同报）；R@5 不退；索引体积增幅与召回延迟记进报告。

### 1.2 弱线索放大幻觉（P1 第二刀）

adversarial 桶（正解"答不出"）：A 组裸底 50% 认怂全对；B 组注入记忆后幻觉率 80%——检索给的线索诱导模型"有线索就硬答"，压过"该认怂"的判断。

- [ ] 注入模板加护栏话术：记忆段头部/尾部明写"以下是历史背景线索；若线索不足以确定答案，如实回答不知道，不要从线索外推"
- [ ] 低分候选不注入：检索分低于现有门槛（kMemoryMinRecallScore 一带）的候选直接丢弃，宁缺毋滥——弱线索比无线索更危险，它给模型"编"的抓手
- [ ] adversarial 场景的注入策略单测：给一条沾边但不含答案的候选，断言模板带护栏、低分候选被拦

验收：E2 复跑 adversarial 桶，幻觉率 80% → **≤55%**（接近裸底水位）；可答四桶增益不退（护栏话术不许把正常增益也吓没——四桶复跑同报）。

### 1.3 预算选条策略（与 1.2 同刀落地）

平均注入 2.9 条/7.8KB 打满 8KB 生产预算，adversarial 桶拦截 10980 次——预算紧张时"选谁注入"直接决定帮忙还是帮倒忙。

- [ ] 预算分配规则明确化：按检索分排序装填，单条超预算截断的下限（保 summary 完整）；分数并列时的稳定性（同分按 topic id 排序，可复算）
- [ ] 注入账（recall-traces）带"预算丢弃"明细：被拦候选与理由可见，不静默

验收：recall-traces 能逐条回答"这轮注了什么、拦了什么、为什么"；E1 注入字节分布前后对照。

## 二、实施批次

- [ ] P1：content 进索引 + 注入截断策略；E1 复跑出对比表
- [ ] P1：护栏话术 + 低分拦截 + 预算规则；E2 复跑（adversarial 桶 + 可答四桶）
- [ ] P2：报告三张表进 `eval/locomo/`（改进前后 R@k、幻觉率、预算分布），单子勾账只勾复跑验证过的

## 三、不做什么

- 不为 LoCoMo 单独调参——改动必须对通用检索路径成立，LoCoMo 只是验收尺
- 不上向量库/嵌入——BM25 先吃下该吃的分，不够再议
- 不拿单桶改善冒充全局改善——四桶+幻觉率+预算三面同报
- 不动记忆写入侧（抽取质量另单）
