# GUI Agent 与 Browser Agent：两套可复制的扩展示例

状态：待开工（2026-08-28）

## 一句话

做两套真能跑、真能改的案例，教用户怎样替 LubanCode 添特殊能力：

- GUI Agent 用本地 process plugin。每次截图、移动、点击、输入只起一只短命进程，
  桌面与目标程序自己保存状态，不必搭 MCP。
- Browser Agent 用常驻 MCP。浏览器页签、DOM ref、Cookie、下载与事件都握在服务进程
  手里，须留一只长命控制器。

两例不只摆源码。还要带 Skill、配置、夹具、测试、排错与改造指南。用户照着走一遍，
便能判断自己的扩展该用 Skill、plugin，还是 MCP。

## 这张单为何另立

`todos/MCP富结果与专属浏览器.todo` 管底层能力：富工具结果、图片、资源、
`structuredContent`、Browser MCP 与可选 GUI Shell。

这张单管“怎样教会用户”：

```text
能力建设单
  -> LubanCode 能不能接

本示例单
  -> 用户怎样照着做
  -> 为什么这样选
  -> 怎样改成自己的特殊工具
```

两张单互相引用，不复制两套协议设计。富图片结果若尚未落地，案例可以先搭骨架、跑
动作与文本结果；不得把“只返回截图路径”冒充模型已经看见图片。

## 教学目标

用户跑完两例，应当能答清六件事：

1. 一次性本地函数为何适合 process plugin。
2. 有独立状态与生命周期的服务为何适合 MCP。
3. manifest/schema 是什么，模型看见什么，宿主又保管什么。
4. 截图、鼠标、键盘、DOM、Cookie 各归谁持有。
5. 怎样给危险动作上确认、范围、取消与审计。
6. 怎样把示例改成设计软件、办公软件、测试工具、网页研究或内网业务工具。

## 总目录

目标目录：

```text
examples/
  agents/
    README.md
    shared/
      fixture-site/
      evidence/
    gui-agent/
      README.md
      plugin.json
      runner.py
      requirements.txt
      test_runner.py
      SKILL.md
      fixtures/
      scripts/
    browser-agent/
      README.md
      package.json
      package-lock.json
      tsconfig.json
      src/
        server.ts
        browser_session.ts
        tools.ts
        result.ts
      tests/
      SKILL.md
      config.example.json
```

若实现时已有统一 plugin bundle 格式，再按新格式归档。没有便照现行规则：GUI Agent
放 `~/.lubancode/plugins/gui-agent/`；Browser Agent 的 server 独立放置，用户在
`mcpServers` 里登记。不能为了目录好看，先偷造一套未落地的包协议。

## 总导航 `examples/agents/README.md`

- [ ] 开头先摆一张选择表，不先灌协议史。

| 你的能力 | 首选 | 状态放哪儿 |
| --- | --- | --- |
| 截一张图、点一下、跑一条本地函数 | process plugin | OS/目标程序 |
| 一枚可信 Lua/C 函数 | Lua/native plugin | 宿主进程 |
| 浏览器、数据库、连接池、登录态 | MCP | 长命服务 |
| 只教模型一套步骤 | Skill | 无运行状态 |

- [ ] 写清“工具每次只调用一次”不等于“工具没有副作用”。点击一次也可能付款、删除、
  发信。
- [ ] 写清“进程短命”不等于“任务无状态”。GUI Agent 的状态在桌面；Browser Agent
  的状态在 Browser MCP。
- [ ] 写清“截图保存成功”不等于“模型看见截图”。须经过富工具结果或显式图片输入。
- [ ] 给两条最短启动路径，每条十分钟内能跑到第一项可见结果。
- [ ] 给一张源码地图，点名 manifest、runner/server、Skill、测试与配置各管什么。
- [ ] 给一张改造地图：改工具名、改 schema、改实现、改权限、补测试，五步走完。
- [ ] 标明平台。GUI Agent 首版 Windows 优先；Browser Agent 按 Playwright 支持范围走。
- [ ] 不把第三方依赖藏起来。Python、Node、Playwright browser binary 的版本与体积明列。

## 案例一：GUI Agent 本地 plugin

### 一句话

模型反复走“看一眼，动一下，再看一眼”。截图、鼠标与键盘各是一件短命工具。Windows
桌面替插件保存窗口、光标与应用状态，所以不需要常驻 MCP server。

### 教学场景

首个演示不用真实微信、银行、邮箱或生产软件。仓里带一只本地 GUI fixture：

```text
窗口标题：LubanCode GUI Fixture

[名字输入框]
[颜色下拉框]
[提交按钮]

提交后：Hello, 阿明 / green
```

Agent 要完成：

1. 列窗口，找到 fixture。
2. 聚焦窗口。
3. 截图并看见控件。
4. 移到名字输入框，点击，输入文字。
5. 选择颜色。
6. 点击提交。
7. 再截图，核结果。

这条链短，动作可见，又不碰真实数据。

### 为什么选 process plugin

- 每件动作都是一次本地调用。
- 目标程序本来就在桌面上，状态不在插件内。
- Python 写起来短，用户容易照抄。
- 进程崩了只坏本次调用，不带倒 LubanCode。
- 不需要 MCP initialize、tools/list、JSON-RPC 与常驻收尸。

示例 README 也要讲明反例：若工具需要保存 DOM handle、订阅事件、维持数据库连接或
接收异步下载，process plugin 便不合适，应改 MCP。

### P0：平台与依赖决策

- [ ] 首版只承诺 Windows 10/11。Linux/macOS 可留接口，不写“跨平台可用”空话。
- [ ] Python 版本与依赖锁死。候选依赖：`mss` 截图、`pyautogui` 鼠标键盘、
  `Pillow` 图像校验；实现前核多屏、DPI 与中文输入行为。
- [ ] 若 `pyautogui` 在目标矩阵不能稳定处理虚拟桌面/DPI，Windows 路改用 `ctypes`
  调 `SendInput` 与系统截图 API。README 如实写平台分支。
- [ ] 第三方库只管成熟底层动作，不把权限、坐标合同、日志与结果协议交给它猜。
- [ ] `requirements.txt` 固定版本；另给 `python -m pip install -r requirements.txt`。
- [ ] fixture 不依赖管理员权限，不触发 UAC secure desktop。

### P1：`plugin.json`

- [ ] id 固定为 `gui-agent-example`，避免与正式产品名抢命名空间。
- [ ] runtime 用 `process`，command 指用户选定 Python，args 用 `${plugin_dir}/runner.py`。
- [ ] timeout 按动作设总墙钟；单次动作不该跑几十秒。
- [ ] env allowlist 只放示例真要用的开关，例如 `LUBANCODE_GUI_DRY_RUN`。
- [ ] permissions.network 设 false。
- [ ] 每件工具各写窄 schema，`additionalProperties=false`。
- [ ] schema 不收任意 shell、任意 Python、任意脚本正文。
- [ ] 工具 description 写明何时用、坐标口径与危险动作；不只写“点击鼠标”。

### P2：GUI 工具面

#### `gui_list_windows`

- [ ] 返回窗口 id、标题、进程名、矩形、可见/最小化/前台状态。
- [ ] 不返回窗口正文、密码字段或剪贴板。
- [ ] 标题过长截断，外来文本过 UTF-8 清洗。
- [ ] 窗口 id 只在当前桌面现场有效，不许写成跨会话永久 id。

#### `gui_focus_window`

- [ ] 入参只收 window id。
- [ ] 找不到、已关闭、系统拒绝前台切换，各有独立错误。
- [ ] 聚焦后复查前台窗口；没成功不能回 ok。
- [ ] 不恢复/激活系统敏感窗口。

#### `gui_screenshot`

- [ ] 支持 `target=window|screen`。默认 window，少拍无关隐私。
- [ ] 返回截图图片、artifact 引用与结构化元数据：尺寸、虚拟原点、DPI、窗口矩形、
  window id、时间戳、SHA。
- [ ] 截图先验 PNG/JPEG 魔数、尺寸与字节帽，再交宿主。
- [ ] 当前 plugin 协议若仍只认 text，明确标 `blocked_by: MCP富结果与专属浏览器/P0`。
- [ ] 过渡期可以返回安全路径供人工 `/image` 验证，但 README 必须写“模型尚未自动看见”。
- [ ] 多显示器测试含负坐标原点。
- [ ] 最小化/遮挡窗口的截图语义写清：截屏幕所见，还是离屏抓窗；首版只承诺一种。

#### `gui_move_mouse`

- [ ] 入参收 `x/y`、coordinate_space、可选 duration、可选 window id。
- [ ] coordinate_space 至少分 `virtual_screen` 与 `window_client`。
- [ ] window_client 坐标在执行前换算；窗口矩形已变便拒绝或重算，并把决定写进结果。
- [ ] 移动鼠标不自动点击。
- [ ] 越界坐标在调用前拒绝。

#### `gui_click`

- [ ] 入参收坐标、button、clicks、可选 window id、可选 expected_rect。
- [ ] 默认单击左键；双击、右键须显式写。
- [ ] 执行前复查目标窗口仍存在、矩形仍合、前台仍对。
- [ ] expected_rect 不合时回 `stale_observation`，不拿旧图坐标硬点。
- [ ] 点击完成只报告动作事实，不猜按钮业务是否成功；下一步必须截图复验。

#### `gui_scroll`

- [ ] 入参收方向、刻度、可选坐标/窗口。
- [ ] 上限钉死，防一次滚出几万格。
- [ ] 执行前聚焦策略写清，不在未知窗口上滚。

#### `gui_type_text`

- [ ] 入参收 text、可选 interval、可选 window id。
- [ ] 字节与字符数设帽。
- [ ] 结果与日志不回显完整文本；只报字符数与目标窗口。
- [ ] 密码、OTP、密钥不准经普通 tool args 输入。示例遇密码框便停。
- [ ] 中文 IME 若不能稳定注入，首版明报限制，不偷偷改剪贴板粘贴。

#### `gui_key`

- [ ] 只收枚举键与受限组合，不收任意脚本。
- [ ] `Alt+F4`、`Win+R`、系统组合键列高风险档或禁用。
- [ ] `Esc` 可作普通目标按键，但 LubanCode 自己的取消键与发送给目标窗口的 Esc 分开。

#### `gui_status`

- [ ] 回平台、依赖版本、虚拟桌面、显示器、DPI、dry-run 状态。
- [ ] 不回用户名、完整环境变量或无关进程表。

### P3：观察与动作合同

截图须带一份 `observation`：

```json
{
  "observation_id": "obs-17",
  "window_id": "0x0012049A",
  "window_rect": [100, 80, 1300, 900],
  "client_size": [1200, 760],
  "virtual_origin": [-1920, 0],
  "dpi_scale": 1.5,
  "image": {
    "artifact_id": "sha256:...",
    "width": 1200,
    "height": 760,
    "mime_type": "image/png"
  }
}
```

- [ ] 动作工具可收 `expected_window_rect`/`observation_id`。
- [ ] process plugin 本身不常驻，不能在内存里查 observation id。可把必要现场数据放入
  有签名/校验的短 token，或由动作时重查 window id + rect + DPI。
- [ ] 不得把 observation token 当权限 token。它只防坐标过期，不代表用户批准点击。
- [ ] 截图到动作间目标窗口若换进程、换标题、换矩形，默认拒绝。
- [ ] 动作之后一定再观察。Skill 不许连续盲点五次。

### P4：权限与保险

- [ ] `LUBANCODE_GUI_DRY_RUN=1` 时动作只校验并报告，不真注入输入。README 先教 dry-run。
- [ ] screenshot/list/status 属观察类；move 属低风险动作；click/type/key 属写动作。
- [ ] 当前 plugin adapter 若只能整件 `needs_confirm=true`，示例先照实每次确认；另在 TODO
  记“per-tool effect metadata”改造，不靠用户一把全白名单掩过去。
- [ ] fixture 可给专用 allowlist；真实桌面不得在教程里教“允许所有 gui_*”。
- [ ] 用户按 Esc 后，尚未执行的动作取消；已经发出的单次 SendInput 不冒充可撤销。
- [ ] 鼠标移到屏幕角落可作第三方库 failsafe，但不得取代 LubanCode 取消链。
- [ ] 不提升权限，不操作 secure desktop，不绕 UAC。
- [ ] 不碰系统剪贴板；若后续要用，另列读写权限与恢复原值规则。
- [ ] 默认 window-only 截图。整屏截图前显示会拍哪些显示器。

### P5：GUI fixture 与测试

- [ ] fixture 可一条命令启动，标题固定，窗口尺寸固定，控件布局稳定。
- [ ] fixture 显示当前收到的 click/type/key 事件，方便人工对账。
- [ ] fixture 提供重置按钮与启动参数，测试可每回从同一状态起步。
- [ ] runner 单测不碰真鼠标：注入 fake desktop backend，断言坐标换算与调用参数。
- [ ] dry-run 集成测试真枚举 fixture、真截图，但不注入点击。
- [ ] 专用桌面/CI 环境才跑真输入 E2E；普通开发机默认 skip，免抢用户鼠标。
- [ ] 截图验宽高、四角像素、非空、目标窗口入画。
- [ ] 多屏负原点、125%/150% DPI、窗口移动、窗口关闭、失焦各一册。
- [ ] stale observation 必须拒绝；不能点完才说坐标旧了。
- [ ] 中英文输入分开验。中文不支持时测试断言明确错误。
- [ ] 所有测试结束后鼠标、窗口、fixture 进程收回可预测状态。

### P6：GUI Agent Skill

`SKILL.md` 只教流程，不藏实现：

- [ ] 先 `gui_status`，再列窗口。
- [ ] 先聚焦，再截图。
- [ ] 每次只做一项动作，动作后重截。
- [ ] 以截图坐标与元数据为准，不凭上回记忆猜。
- [ ] 见窗口/坐标过期便重观察。
- [ ] 见提交、删除、付款、发送、上传先停下来问。
- [ ] 不请求密码、OTP、验证码解答。
- [ ] 完成后给用户说清做了哪些动作，不声称没复验的结果。

### P7：GUI README 教学章法

- [ ] 第一屏便写“它为何不是 MCP”。
- [ ] 给安装依赖、复制目录、重启、`/plugins`、`/tools`、第一回 dry-run 的完整命令。
- [ ] 展开一份 tool call 请求与一份响应，讲 call_id、arguments、content、stderr。
- [ ] 画一次 screenshot -> click -> screenshot 数据流。
- [ ] 讲坐标空间、DPI、窗口矩形与 stale observation。
- [ ] 讲“路径不是图片”：富结果未落地时如何人工核图，落地后怎样自动闭环。
- [ ] 给三项改造练习：控制计算器、控制自家测试工具、给图片编辑器添重复动作。
- [ ] 每项练习写安全边界，不拿真实账号/生产数据开玩笑。

### GUI 案例完成定义

- [ ] 干净 Windows 环境照 README 能安装并在 fixture 上跑通。
- [ ] Agent 能完成“填名字、选颜色、提交、截图复验”。
- [ ] 每项工具仍是一调用一进程；没有偷起后台 daemon。
- [ ] 截图真作为图进入模型；若依赖未落地，案例不得标完成。
- [ ] 窗口挪动后，旧 observation 的点击被拒。
- [ ] dry-run 默认不动真实鼠标键盘。
- [ ] 用户能从 README 看懂怎样改成自己的本地 GUI 工具。

## 案例二：Browser Agent MCP

### 一句话

浏览器操作虽也能靠截图与坐标做，专攻网页时却有更好的状态：page、locator、DOM、
Cookie、下载与导航事件。它们都活在 BrowserSession 里，所以用一只常驻 MCP server。

### 教学场景

复用 `examples/agents/shared/fixture-site/`：

1. 打开本地站点。
2. 读取语义 snapshot。
3. 按 ref 填表，不靠像素猜输入框。
4. 点击提交，等页面变化。
5. 截图复验。
6. 打开第二标签页，下载一只文件。
7. 列标签页与下载账。
8. 关闭会话，再以 persistent profile 启动，验证测试 Cookie 尚在。

### 为什么选 MCP

- Browser/context/page 是长命对象。
- `tools/list` 可一次发现整套浏览器工具。
- 多次 `tools/call` 必须命中同一份 BrowserSession。
- 下载、dialog、popup、导航是异步事件。
- Playwright runtime 与 browser child 应和 LubanCode 地址空间隔开。

### P0：技术栈

- [ ] 首版 TypeScript + 官方 MCP SDK + Playwright。
- [ ] `package.json` 与 lockfile 入仓；Node 版本写入 README。
- [ ] browser binary 用 Playwright 安装命令，下载体积与缓存位置明报。
- [ ] 引擎支持 `chromium|webkit`；首发默认值经本地矩阵后定，不拿“WebKit”当品牌装饰。
- [ ] server stdout 只走 JSON-RPC，日志写 stderr。
- [ ] 不依赖公网做验收，fixture-site 足够跑核心链。

### P1：MCP server 骨架

- [ ] 实现 initialize 与 tools/list/call；协议版本按 LubanCode client 能力协商。
- [ ] BrowserSession lazy launch。只查 tools 时不下载/启动浏览器。
- [ ] 所有 page 动作串进一只 actor queue。
- [ ] server 收 SIGTERM/stdin EOF 时关 page/context/browser，限时后强收。
- [ ] 浏览器崩溃与 server 崩溃分开报。
- [ ] 每次结果带 server version、browser generation、page id，供 trace 对账。
- [ ] 工具 schema 顶层 object，必填与 additionalProperties 写全。

### P2：浏览器配置

- [ ] `engine=chromium|webkit`。
- [ ] `headless=true|false`。
- [ ] `profile=ephemeral|persistent`。
- [ ] persistent profile 用独立目录，不指向用户日常 Chrome/Edge profile。
- [ ] 同一 profile 加锁；第二只实例明确失败。
- [ ] 下载目录、artifact 目录、默认 viewport、locale、timezone 各有显式配置。
- [ ] proxy、证书、扩展等高级项首例不开放；避免教程一上来变配置大全。

### P3：Browser 工具面

#### `browser_open`

- [ ] 打开 URL，可选新页。
- [ ] 只许 `http/https/about:blank`。
- [ ] 返回 page id、最终 URL、标题、generation。

#### `browser_snapshot`

- [ ] 返回语义/可访问性快照与稳定 ref。
- [ ] ref 绑定 page generation 与 snapshot id。
- [ ] 导航后旧 ref 报 stale，不默默重找相似元素。
- [ ] 默认不回完整 HTML。

#### `browser_click`

- [ ] 首选 ref；目标不唯一或已失效便拒绝。
- [ ] 点击后按明确条件等待，不用固定 sleep 糊住竞态。
- [ ] popup/new tab 在结果里明报新 page id。

#### `browser_type`

- [ ] 分 fill 与逐键输入。
- [ ] 密码值不回显、不落日志。
- [ ] 文件上传不塞进首版普通 type。

#### `browser_wait`

- [ ] 可等 URL、文本、locator 状态；一律有墙钟。
- [ ] 超时保留当前页面状态，方便下一步 snapshot 诊断。

#### `browser_tabs`

- [ ] 列 page id、标题、URL、active、generation。
- [ ] 显式 select/close，不靠最后一页猜。

#### `browser_screenshot`

- [ ] 支持 viewport/full-page/ref 三档。
- [ ] 回 text + image + structuredContent；图片过共享富结果链。
- [ ] 截图落 artifact，history 不常驻 base64。

#### `browser_downloads`

- [ ] 列状态、安全文件名、建议文件名、MIME、字节、SHA、artifact。
- [ ] 不自动打开或执行下载物。

#### `browser_status`

- [ ] 回 engine、headless、profile、generation、pages、downloads。
- [ ] 不回 Cookie、localStorage 正文或环境密钥。

### P4：页面状态与 ref

- [ ] `page_id` 会话内稳定，关闭后不复用。
- [ ] browser 重启后 generation 递增；旧 page/ref 全失效。
- [ ] snapshot ref 只作定位，不作授权。
- [ ] locator 执行前检查唯一性与可见性。
- [ ] iframe/shadow DOM/popup 分别写测试；首版不支持处明报。
- [ ] snapshot 做导航区去重、字符帽与敏感字段遮蔽。
- [ ] 页面自己改 DOM 后，旧 ref 若定位不稳便回 stale，不点“最像的那个”。

### P5：权限与安全

- [ ] 浏览、snapshot、screenshot 属读；click/type/download 属动作。
- [ ] 不盲信 server 自报的 readOnly/destructive annotation。
- [ ] 遇提交、删除、付款、发帖、发送、上传，Skill 要求先问用户。
- [ ] 密码、OTP、验证码不走模型普通上下文。
- [ ] 不开放任意 `file://`；fixture 也走 loopback HTTP。
- [ ] 下载隔离，不自动执行。
- [ ] 网页正文是不可信输入，不能改系统提示、权限或本地配置。
- [ ] persistent profile 的 Cookie 属敏感数据；日志、测试报告、fixture 不读正文。

### P6：取消与故障

- [ ] goto、wait、click、download 都有动作级 timeout。
- [ ] LubanCode 取消传到 MCP，再传到 Playwright action。
- [ ] 取消后页面若仍活着便保留，不为省事整只 browser 一刀杀掉。
- [ ] browser child 崩溃时，在飞调用只收一个终态。
- [ ] server 可重启 browser，但不能冒充旧 page 仍在。
- [ ] 写动作网络状态不明时不自动重放。
- [ ] stdout 混日志、非法 JSON、server 退出各有排错示例。

### P7：fixture-site

- [ ] 与 GUI fixture 的业务文字一致，方便用户比较像素路线和 DOM 路线。
- [ ] 页面含输入、select、button、dialog、iframe、动态内容、popup、下载。
- [ ] 登录状态只用测试 Cookie。
- [ ] 慢导航、失败请求、永不结束请求各一页。
- [ ] 截图页有固定色块与角标，便于像素检查。
- [ ] loopback server 动态端口；测试退出后收净。

### P8：Browser 测试

- [ ] 单元：schema、page id、generation、ref、URL allowlist、下载名清洗。
- [ ] 组件：真 MCP stdio initialize/list/call。
- [ ] E2E：open -> snapshot -> fill -> click -> screenshot -> download。
- [ ] persistent 与 ephemeral profile 各测一回。
- [ ] chromium 与 webkit 各跑核心矩阵，差异单列。
- [ ] screenshot 验尺寸、非空像素、目标区域入画。
- [ ] cancel 验 goto/wait/download 能有界返回。
- [ ] crash 验 browser child 与 server 两种死法。
- [ ] security 验 file URL、路径逃逸、危险下载、日志泄密。
- [ ] Browser 测试报告与 C++ Release ctest 分开写，不能一句“全绿”糊过去。

### P9：Browser Agent Skill

- [ ] 打开页后先 snapshot。
- [ ] 用 ref，不凭 CSS/XPath 或截图坐标乱猜。
- [ ] 导航、popup、DOM 大改后重 snapshot。
- [ ] 动作后等明确条件，再观察。
- [ ] screenshot 用来视觉复验，不替代 DOM 状态真账。
- [ ] 提交、删除、付款、发送、上传先问。
- [ ] 下载后只报告 artifact，不自动执行。
- [ ] page/ref 失效时重观察，不循环重试同一旧句柄。

### P10：Browser README 教学章法

- [ ] 第一屏写“它为何用 MCP，而 GUI Agent 不用”。
- [ ] 给 Node、依赖、Playwright browser 安装命令。
- [ ] 给 `config.example.json` 到用户 config 的完整片段。
- [ ] 教 `/mcp`、`/tools` 看状态。
- [ ] 展开 initialize、tools/list、tools/call 三枚脱敏协议帧。
- [ ] 画 BrowserSession 生命周期图。
- [ ] 讲 profile、page id、generation、snapshot ref。
- [ ] 讲 screenshot 富结果与纯文本 snapshot 两条观察路。
- [ ] 给三项改造练习：网页回归测试、内部系统表单、资料收集。
- [ ] 明说何时不该用浏览器：有稳定 API 时先接 API，少走脆弱 UI。

### Browser 案例完成定义

- [ ] 干净环境照 README 能启动 fixture-site 与 Browser MCP。
- [ ] Agent 能凭 snapshot/ref 完成表单，不靠坐标。
- [ ] 第二次工具调用仍命中第一次打开的页面。
- [ ] screenshot 真进入多模态模型；纯文本模型靠 snapshot 也能走完。
- [ ] persistent profile 留测试 Cookie，ephemeral 不留。
- [ ] chromium/webkit 核心矩阵各有报告。
- [ ] 用户能从 README 看懂怎样改成自己的有状态服务。

## 两例必须同台比较

总 README 不只各讲各的。须拿同一 fixture 走两遍：

| 维度 | GUI Agent plugin | Browser Agent MCP |
| --- | --- | --- |
| 观察 | 截图像素 | DOM/语义 snapshot + 截图 |
| 动作定位 | 坐标 | ref/locator |
| 状态主人 | OS 与目标窗口 | BrowserSession |
| 进程寿命 | 一调用一进程 | 一会话一服务 |
| 可控范围 | 任意桌面程序 | 网页 |
| 脆弱点 | DPI、遮挡、窗口移动 | DOM 改版、ref 失效 |
| 适合教材 | 本地工具 plugin | 有状态 MCP |

- [ ] 用同一句任务分别跑两例，保存脱敏 transcript 与截图证据。
- [ ] 记录工具调用数、总耗时、失败点，不先定谁一定更快。
- [ ] GUI 窗口挪动一次，展示 stale observation 怎样收住误点。
- [ ] Browser DOM 改动一次，展示 stale ref 怎样收住误点。
- [ ] 总结：选择边界，不评“plugin 与 MCP 谁更高级”。

## 共享富结果依赖

两例都要图片闭环，依赖 `todos/MCP富结果与专属浏览器.todo` 的 P0：

- [ ] `Tool::Result` 能带 image/resource/structured content。
- [ ] process plugin 协议升版后能回图片；旧 v1 text-only 仍兼容。
- [ ] MCP client 能接 image 与 structuredContent。
- [ ] 图片落 artifact，不在 history/trace 常驻 base64。
- [ ] provider 能看图时真送；不能时投文本说明，不发必败请求。

在这五项未齐之前：

- GUI 示例可完成窗口枚举、dry-run、动作与截图落盘测试；不可标“视觉闭环完成”。
- Browser 示例可凭 DOM snapshot 完成任务；截图只作人读证据，不冒充模型看过。

## 示例代码质量

- [ ] 示例不是玩具片段。错误码、超时、取消、UTF-8、大小帽、日志分流都齐。
- [ ] 代码宁可多一只小函数，也不把协议、业务、平台调用挤在一个 800 行 runner。
- [ ] README 里的每条命令在干净目录真跑一遍。
- [ ] 版本号、路径、工具名由测试校验，免得文档与代码两张皮。
- [ ] fixture 不联网，不含密钥，不读用户数据。
- [ ] evidence 默认落 `build/test-evidence/agent-examples/`，不进仓。
- [ ] 示例源码带许可证/第三方依赖说明。
- [ ] Windows 路径含空格、中文目录各跑一册。

## 文档与索引

- [ ] `examples/README.md` 若不存在便建总入口；若已有便登记 agents。
- [ ] `examples/agents/README.md` 作两例总导航。
- [ ] `examples/plugins/README.md` 添 GUI Agent 链接，并说明它是较完整 process 案例。
- [ ] `docs/features/extensions/README.md` 在 plugin/MCP 两节分别引这两例。
- [ ] `docs/architecture/extensions/plugin-runtime.md` 用 GUI Agent 解释“状态在外部”的短命工具。
- [ ] `docs/architecture/extensions/tool-extension.md` 用 Browser Agent 解释长命 MCP。
- [ ] `docs/getting-started/troubleshooting.md` 添 GUI DPI/焦点与 Browser profile/ref 排错。
- [ ] `docs/catalog.txt`、各级 README/feature-index 一并登记。
- [ ] `skills/lubancode-config` 的文档地图若列扩展入口，也要补链接。

## 建议提交切片

1. `examples/agents` 总 README、共同 fixture 与两例空骨架。
2. GUI fake backend、plugin manifest、status/list/screenshot。
3. GUI move/click/scroll/type/key + dry-run。
4. GUI fixture、单测、README、Skill。
5. Browser MCP server、session、status/open/tabs。
6. Browser snapshot/ref/click/type/wait。
7. Browser screenshot/download/profile/cancel。
8. Browser fixture E2E、README、Skill。
9. 两例同台对照证据与总教程。
10. 文档索引、打包检查、Release 全量。

每片独立能测。不得先写两篇“将来会有”的 README，隔十个提交才补代码。

## 风险与停手线

- GUI 真输入测试会抢用户鼠标。默认只跑 fake/dry-run；真 E2E 须专用桌面。
- 截图可能带隐私。默认 window-only，证据只用 fixture。
- 密码、OTP、验证码一律不进普通 tool args。
- process plugin 没有内存状态。若实现开始偷偷落一堆会话文件维持对象句柄，停下来评估 MCP。
- Browser MCP 若只剩 screenshot + 坐标，没有 DOM/ref/事件价值，便与 GUI 例重复，应缩回一例。
- 有正式 API 的业务，不教用户优先点 UI。
- 示例若必须关闭全部确认才能跑，权限设计便不合格，不能拿 README 警告代替修正。
- 富结果未完成时，绝不把路径字符串说成模型视觉输入。
- WebKit/Chromium 行为不同，分别记；不拿一边通过盖住另一边失败。

## 总完成定义

- [ ] 仓里有 `examples/agents/gui-agent` 与 `examples/agents/browser-agent` 两套完整工程。
- [ ] 两例共用一只安全 fixture，能演示同一任务的两条扩展路线。
- [ ] GUI Agent 真走 process plugin，一调用一进程，不依赖 MCP。
- [ ] Browser Agent 真走常驻 MCP，多次调用共用 BrowserSession。
- [ ] 两例各带 Skill、README、依赖锁、配置、单测、E2E 与排错。
- [ ] GUI 的截图坐标合同、Browser 的 page/ref 合同都能拦过期动作。
- [ ] GUI dry-run 默认不碰真实输入；Browser 默认不碰用户日常 profile。
- [ ] 图片富结果打通后，两例截图都能被多模态模型看见；纯文本模型仍有降级路。
- [ ] 用户照教程改出第三件特殊工具时，不必再读 LubanCode 全仓源码。
- [ ] 定向测试、案例 E2E、Release ctest 各留真报告，静态检查不冒充交互验收。

