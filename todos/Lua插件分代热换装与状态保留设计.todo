# Lua 插件分代热换装与状态保留设计

日期：2026-09-14
状态：设计待实现。本单只添设计，不代表热换装已跑通。

## 一、定案

C++ 管入口、寿命、权限、状态和提交。Lua 写工具逻辑、Hook 策略与显式数据迁移。
稳定入口 + 独立新 VM + 不可变整代快照 + 宿主状态仓，是主线。
不在旧 VM 上清 package.loaded 再执行文件；不靠 debug.upvaluejoin 保住业务数据。
首版只接 manifest-backed Lua。裸 Lua 沿用旧加载合同；原生 DLL 仍须重启。
不借热更新另造 Session、权限系统、MCP 执行器或调度器。

## 二、源码基线（本次静态核对）

- src/tools/lua_tool.hpp/.cpp：LuaTool 持有 lua_State、execute_ref_ 和 execute_mutex_。
  加载走 luaL_loadbuffer + lua_pcall；执行从 Lua registry 取固定引用。
- src/runtime/plugin_lua.hpp：EmbeddedLuaRuntime 持有工具；LuaToolAdapter 保存裸指针。
  因而不能先释放旧工具，再指望 adapter 自动转向新工具。
- src/runtime/plugin_lua_manifest.hpp：ManifestLuaRuntime 管 manifest Lua 实例和适配器。
- src/runtime/plugin_lua_host.hpp：LuaHostState 已有受控加载、调用上下文、串行锁及 Hook 调用口。
- src/package/code_mounting.hpp：已有包级 Stage/Publish/Rollback 和暂存 Lua 成品。
  复用预装与校验思路，但首次挂载事务不等于运行中换代事务。
- src/app/commands/package_commands.cpp：/package reload 提示 code 组件须新会话。
- src/app/commands/workspace_commands.cpp：插件 reload 仍走提示分支。
- CMakeLists.txt：当前拉取 Lua v5.4.7。

既有 LuaHook TODO 与受控 Host API 继续沿用；旧 TODO 中“待实现”须逐项对源码，不能全盘照搬。

## 三、边界与承诺

“随时加入”：运行中发现候选；信任、校验通过后，在下个安全边界供新工作使用。
“自动感知”：监听已配置扩展根目录；启动枚举一次，事件丢失时补扫目录。
“保留状态”：保住宿主已提交业务数据；不承诺搬运 Lua 栈、闭包、协程或 C 指针。
“无缝”：进程不停、旧工作可收尾；迁移窗口可短暂停收该插件新工作。
不承诺零等待，也不宣称 DSH 采用此方案；没有它的源码依据。

## 四、对象分工（下列类型均为拟新增）

ExtensionWatcher：将目录变化变成脏标记，只通知，不执行 Lua。
ReloadCoordinator：合并事件、排队、校验、预装、停收、迁移、提交、收尾。
ExtensionSnapshot：generationId、内容摘要、工具定义、Hook 链、实例所有权组成一份不可变快照。
GenerationLease：一次工作持有快照；活动租约未清零，旧 VM 不释放。
StableToolAdapter：保存逻辑工具 ID；依据本工作租约找实现，不保存可被换掉的裸实例指针。
PluginStateStore：按来源/包/插件/作用域隔开数据，提供版本读取与事务提交。

PackageSnapshot 管包内容；ExtensionSnapshot 管活实例。两者显式关联版本，不能拿同一指针混充。
调度器用一处发布锁完成“读取当前代 + 登记租约”和“关闸/发布”，消除取指针与计数间的竞态。
shared_ptr 保寿命，关闸规则保调度；只有 shared_ptr 或只有原子换指针都不够。

## 五、一次换装

1. 监听线程登记脏包 ID。建议初始合并窗口 300ms，可配置；连续写入不能无限延后。
2. 重新枚举受影响包，算 manifest、脚本和依赖内容摘要；摘要未变就收工。
3. 将完整代码集固定到只读版本目录，从同一份字节算摘要、查信任、编译、加载。
   编辑器连写多份文件时，防抖不能证明同一版本：正式安装以完整版本目录原子交付为准；
   开发目录做前后摘要复核，变化就重试。候选代不得运行时读取活动目录中的新旧混合模块。
4. 新 VM 内预装。顶层和验证阶段禁止 HTTP、MCP、写文件、注册定时器等外部动作。
   检查 Host API 版本、工具 schema、Hook 定义、依赖闭包、资源预算和现有信任规则。
5. 关掉受影响扩展的新工作入口。旧租约仍可完成其后续调用，不能把旧工作卡在关闸处。
6. 等受影响旧租约归零；达到期限则取消本次换装，恢复入口。旧代继续服务。
   取消旧工作只走显式操作与既有取消链；禁止强关仍在 C/HTTP 调用中的 VM。
7. 在冻结写入的状态版本上执行迁移，结果只进暂存事务。校验新状态，再准备发布。
8. 持发布锁提交状态版本与代激活记录，再换当前快照，最后开放入口。
   提交窗口不运行用户 Lua，不发网络请求。崩溃恢复以持久激活记录重建快照。
9. 无租约、无 Host 回调后释放旧 VM。清理由宿主管；Lua 清理失败不能卡住宿主退场。

首版统一“等旧工作收尾再换代”，包括无迁移更新。以后有测量依据，再放开无状态双代并行。
同时只准备一个候选代，并设合并内存上限；连续保存保留最新候选，过时代不得晚到覆盖新代。

## 六、一次工作到底钉多久

工具：模型请求装配时固定工具定义与实现代；该响应引出的整批工具调用、结果收集都用同代。
延迟工具搜索也须沿该快照查询；新工具在下个请求装配边界出现，不改正在发送的 schema。
Hook：一条中间件链从开始到 next() 收回都用同代，不能中途换 handler。
Workflow：已有运行钉快照；长任务不强换，新启动任务用新代。
后台任务：持同类租约与取消链。租约可能很长，界面应明示“等待旧任务结束”。
权限撤销另走实时安全检查，旧租约不能保住已撤销权限。
删除/停用：停止分配新租约，撤下新快照中的入口，旧工作收尾后卸载；数据不自动删除。
缺依赖时整组拒绝新发布。首版按包换装；跨包依赖未能一起原子发布就暂缓，禁止半组更新。
包内含不能换代的 code 组件时，整包报告须新会话/重启，不能只换其中 Lua。

## 七、状态如何留下

状态分四类：
- 可丢缓存：留在 VM，换代重建。
- 业务数据：宿主管版本化数据，如计数、配置、游标；按需持久落盘。
- 外部资源：连接、文件句柄、订阅由宿主管；Lua 只拿不透明 ID，调用时核权限和寿命。
- 正在跑的执行：旧代跑完或取消；不复制 continuation、执行权或工具调用。

新增状态合同：stateSchemaVersion + migrate(oldVersion, oldData) -> newData。
这是拟新增接口，不能写成当前 plugin.json 已支持字段。
迁移只收可序列化数据副本，只返回新数据；禁止访问网络、Secret、时钟随机源和活动资源。
限制字节数、深度、条目数、指令数、墙钟；不收函数、userdata、循环表。
同 schema 沿用数据，不必调用迁移。schema 改了却无迁移函数，拒绝更新。
插件状态必须隔开用户/项目来源及会话作用域，不能只凭插件名共用一份。
业务写入带 expectedVersion，事务冲突明确返回，不默默覆盖。
Session 的消息、工具结果、权限和执行事实仍归现有账本；状态仓不能伪造第二份事实来源。

持久激活事务同时写入：插件身份、代码摘要、代 ID、状态版本、迁移结果引用。
代码快照先持久保存，再提交激活记录；启动按记录恢复，残留候选可回收。
提交前失败：丢候选，旧数据和旧代码继续用。
提交后新代已经处理请求：禁止盲目切回旧数据，否则会吞掉新写入。
此时回退视作一次新迁移，须有逆向迁移或兼容证据；外部效果不能靠换代码撤销。

## 八、回调与宿主能力

C++ 注册稳定逻辑入口；真正调用时携租约定位 Lua 函数。
异步请求发出时固定 generationId、invocationId、取消/截止时间和租约，完成后回到原代。
回调在原 VM 执行队列上运行，不让 I/O 线程直接碰 lua_State。
若旧代工作已取消，宿主结清结果并释放租约，不能把旧请求结果投给新函数。
未来周期订阅须由宿主管唯一订阅权；换装只更新路由，不重复注册监听器。
HTTP、Secret、MCP 继续走既有 Host API；新权限仍走已有信任和授权规则。
不能为热更新开放 debug、任意 require、package.loadlib 或系统执行口。
不可信代码若需崩溃隔离，应走独立进程；独立 lua_State 不是操作系统隔离。

## 九、目录监听

Windows：原生 C++ 用 ReadDirectoryChangesW；FileSystemWatcher 是 .NET 类。
Linux：inotify；macOS 后续接 FSEvents。统一报 Dirty/Overflow/Unavailable。
先启动监听，再枚举并归并期间事件，补上“枚举完才监听”的空窗。
缓冲溢出、根目录移动/重建、监听失败都触发扩展根目录复核与重新订阅。
仅扫已配置扩展目录，不扫全盘。网络盘或不支持监听时可降级定时核对摘要，并明示状态。
新代码发现与执行授权分开；项目内容摘要变更继续按现有信任账处理。

## 十、实施顺序与验收

[ ] P0：固定上述合同，先改实例所有权和稳定入口；接入现有主/子代理 registry 装配。
    验收：原有工具名字、schema、权限、取消行为不变；旧租约不会访问释放后的 VM。
[ ] P1：手动 reload，无状态，同包整代预装、停收、发布、超时退回。
    验收：v1 慢调用结束前不换代；下一批调用用 v2；语法错、缺 handler 均保留 v1。
[ ] P2：状态仓、纯迁移、持久激活与恢复。
    验收：计数 41 升级后继续到 42；迁移失败仍为旧版 41；提交各断点崩溃均可恢复。
[ ] P3：目录监听、内容固定、事件合并和补扫。
    验收：新增、编辑器 rename 保存、删除、溢出、根目录重建、权限变更均不漏账。
[ ] P4：Hook 链、后台回调、长 Workflow 与诊断界面。
    验收：next 前后同代；旧异步回调不进新 VM；长任务阻挡时有明示且超时可退回。
[ ] P5：在目标 Windows/Linux 环境做真监听与并发回归，再评估无状态双代并行。

诊断至少显示 current/candidate generation、摘要、状态版本、租约数、等待原因、最近失败。
记录 reload.detected/staged/deferred/committed/failed 等宿主诊断事实，不记 Secret 或完整状态。
测试重点是竞态、失败原子性、崩溃恢复及 schema/实现同代；不拿“加载成功”冒充验收。

## 十一、依据与本次验证

- Lua 5.4 手册：https://www.lua.org/manual/5.4/manual.html#pdf-debug.upvaluejoin
  upvaluejoin 让两枚 Lua 闭包共享指定 upvalue；它不负责迁移数据结构或外部资源。
- Windows 目录监听：https://learn.microsoft.com/en-us/windows/win32/api/winbase/nf-winbase-readdirectorychangesw
  缓冲溢出可丢事件，官方要求重新枚举目录核对变化。

本次只核对源码并写设计，执行文档差异检查；未改运行代码，未跑热更新或迁移测试。
