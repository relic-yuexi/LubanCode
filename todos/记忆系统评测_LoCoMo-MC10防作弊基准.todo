# 记忆系统评测：LoCoMo-MC10 防作弊基准

- 状态：方案已立（2026-09-03 主控研究成文，待实施）
- 级别：P1（记忆系统效果的第一份外证）
- 类型：LubanCode 评测基建（离线检索 + 端到端配对）
- 数据源：https://huggingface.co/datasets/Percena/locomo-mc10（CC BY-NC 4.0，492MB，raw/locomo10.json）
- 关联：《智能体记忆效果_个性化适配续接快照与在线评测.todo》M4 批（MemoryBench v1 与 off/on 配对 runner——本单就是它的第一块实地）
- 不在本单：向量库/嵌入检索改造；记忆系统本身的功能修复；训练模型

## 一、数据集底细

LoCoMo-MC10 是 LoCoMo（Snap Research 2024，长对话记忆基准）的十选一改版：1,986 题，每题带：

- `question_type` 五类：single_hop 14.2% / multi_hop 16.2% / temporal_reasoning 4.8% / open_domain 42.3% / adversarial 22.5%
- `choices` 十个选项 + `correct_choice_index`（无歧义判分）
- `haystack_sessions`：完整对话原文（约 19 个 session，跨数月，300 轮/9K token 量级）+ 逐 session 摘要 + 时间戳
- adversarial 类答案为 "Not answerable"——专门量幻觉

十场对话（conv-1…conv-50 子集），每场约 200 题。**LoCoMo 是公开出名基准，前沿模型预训练大概率背过原题；HF 上已有用它微调的模型。**这是防作弊的根由。

## 二、三重作弊与防法

### 2.1 数据污染（模型背过原题）——扰动破坏字面匹配

- **实体改名**：人名（Caroline/Melanie）、地名、机构名整场一致替换（Caroline→Dorthe，Melanie→Astrid 一类同风格随机名）；同一场对话映射恒定，跨场可不同。
- **日期平移**：全场时间戳统一平移随机天数（如 +417 天）；选项里的日期同天平移，保持相对时间关系不破题——temporal 类选项全是日期，平移必须与对话一致否则答案错位。平移账本入库。
- **题目改写**：问题句式同义改写（"When did Caroline go to…"→"Which date did the support group visit fall on…"），避开原题原文。
- **产出**：perturbed 数据集 + 变换账本（原名→新名、平移天数、改写对照）；账本与答案分离存放，答题流程永不读账本。
- 扰动后 ground truth 同步变换（answer 文本与选项同改），改完抽样人工核对 20 题不错位。

### 2.2 流程作弊（答案漏进上下文/对话原文全量塞回）——写入与答题硬隔离

- **只灌对话不灌题**：记忆写入阶段只投 haystack_sessions（扰动后），QA 对永不进记忆、永不进答题上下文。
- **跨会话答题**：灌完对话开全新会话逐题问——不带原对话上下文。带上下文答对测的是长窗口，不是记忆。
- **答题不回灌原文**：答题时只允许记忆召回注入（走 LubanCode 正常召回路与注入预算），不得全文检索对话原文塞 prompt。
- **判分自动**：答选项序号，对 correct_choice_index（扰动后同 index——只改文本不改位次；若扰动时重洗选项则账本记新 index）。

### 2.3 归因作弊（高分不知是谁的功劳）——off/on 配对

- **A 组（裸底）**：memory off，新会话只给问题与选项。A 的高分 = 先验水位（含残余污染）；A 高 B 不高 = 记忆没帮上忙或帮倒忙。
- **B 组（记忆在岗）**：memory on，同一批题。
- **主指标 B−A 增量**，按五类分桶；adversarial 桶单看"Not answerable"选择率（幻觉率）。
- 同模型、同温度 0、同预算；逐题结果落盘可复算（记忆单 M4：bootstrap 置信区间，不只报平均）。

## 三、评测流水线（三段）

### E0 离线准备（零模型成本）

- [x] 下载 raw/locomo10.json（走代理）；解析十场对话。（实测:raw/ 是无选项原始版,真 MC10 十选一在 data/locomo_mc10.json 240MB,一并下载;底细与单子 §一 对齐:1986 题、五类占比吻合）
- [x] 扰动器 `scripts/eval_locomo_perturb.py`：改名+平移+改写，产 `eval/locomo/perturbed.jsonl` + `eval/locomo/ledger.json`（含答案映射）+ `eval/locomo/answers.sha256`。（answers.sha256=95d397cc…;同 seed 字节级可复现）
- [x] 一致性自检：同场同名恒定、日期相对关系保持、20 题人工抽验。（四项自检全绿:sha256/旧名零残留含实体扫描/带年日期平移双射 10 场/session 日期序列;20 题清单人工过目,改写保义选项对位;修过三处错位:月年式只换年、级联二次平移、实体先于人名替换）

### E1 检索层评测（零模型成本，先掐软柿子）

- [x] 把每场对话的 sessions 按 session 粒度灌成记忆 topics（走 memory worker 正常抽取路——不许旁门直写正文塞答案，抽取产什么灌什么）。（C++ driver tests/manual/locomo_retrieval_driver.cpp:每场临时 workspace,EnqueueSave 正门灌入+RunPendingMemoryJobs 落盘;summary=官方摘要截500B,content=正文截8KiB,keywords=说话人名;零模型成本）
- [x] 每题用 question（改写后）当查询跑 BM25 召回，记 Recall@1/3/5（命中含答案证据的 session）、注入字节、零命中率。（走真实现 BuildTurnContext+LastTrace,不走 python 复刻;全量 1986 题:可定位 498 题 R@1=0.341/R@3=0.554/R@5=0.641,零命中 0,平均注入 2.9 条/7.8KiB 预算打满;表在 eval/locomo/retrieval_baseline.md）
- [x] 产出基线表：五类各 Recall@k。检索不行端到端白搭——这步便宜，先跑先看。（E1 走 C++ 真实现而非 python 直调的理由:python 复刻 BM25/分词/门槛测的不是 LubanCode 现状,违背量现状红线）

### E2 端到端配对（烧模型，抽样起步）

- [ ] runner `scripts/eval_locomo_runner.py`：管道模式起 `lubancode one_shot`——第一阶段按时间序灌对话（模拟跨月多会话，记忆沉淀），第二阶段开新会话逐题问（A/B 两态各跑一遍，同 seed）。
- [ ] 抽样首批：2 场对话 × 五类各 10 题 = 100 题双态，约 200 次答题请求；跑通管线看分布再放量。
- [ ] 报告：整体/五类准确率（A、B、B−A）、adversarial 幻觉率、召回命中与答对的相关性、token 消耗。

## 四、验收线

- [ ] 扰动数据集人工抽验零错位；A 组（裸底）adversarial 桶接近满分而其他桶接近随机——若 A 组裸分显著超随机，说明扰动不足，加扰动强度重来。
- [ ] E1 检索表五类齐全；E2 抽样首批双态跑完，逐题账可复算。
- [ ] 结论三句话说得清：记忆系统净增益多少、哪类最弱、幻觉率多少。

## 五、不做什么

- 不拿 A 组裸分冒充记忆效果；不报平均不报区间。
- 不为刷分改检索算法——本单量现状，改进另立单。
- 不把 492MB 原数据或扰动后数据提交进仓（.gitignore 排除 eval/locomo/，只进脚本与账本骨架）。
- 不做 LoCoMo 官方榜对比口径（各家 cherry-pick 严重）——自家 A/B 配对自己跟自己比。
