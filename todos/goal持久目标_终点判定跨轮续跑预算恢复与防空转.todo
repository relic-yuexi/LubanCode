# `/goal` 持久目标：终点判定、跨轮续跑、预算、恢复与防空转

状态：待做（2026-08-23 查阅 OpenAI Codex `/goal` 官方文档，并巡检
LubanCode slash、Agent Loop、TurnRuntime、usage、session JSONL、compact 与消息泵）

## 一句话

给 LubanCode 添 `/goal`。用户交一项有终点的长活，Agent 做一轮，留一枚检查点；
宿主收集工具实绩，再让独立 evaluator 判 `continue / achieved / blocked /
needs_user`。未达便从主线程安全边界另起一轮。达了、卡住、预算见底，立刻收手。

`/goal` 不是一句永不停止的 prompt，也不是零间隔 `/loop`。它是一套
completion-driven orchestration。

## 先答面试官

面试官问：“让模型一直做，直到它说完成，不就成了？”

答：不成。模型既是干活人，又是验收人，最容易自说自话。真系统至少要有八层：

1. **目标合同**：做什么、不动什么、拿什么验、何时停，先写清。
2. **迭代账**：每一轮有 goal id、iteration id、turn id，不能只看聊天顺序猜。
3. **检查点**：这一轮改了什么、验了什么、还欠什么，须结构化落账。
4. **证据账**：测试退出码、文件 diff、工具结果由宿主采，不信 assistant 自报。
5. **终点判定**：evaluator 只吃合同与证据，按 JSON Schema 下判词。
6. **续轮调度**：只有 `continue` 才排下一轮；一场 session 仍只跑一枚主 turn。
7. **刹车**：时间、token、迭代数、无进展、同一 blocker 都有上限。
8. **崩溃恢复**：每道转折先写 append-only event，重启后不重做已发生的副作用。

一句短答可说：

> `/goal` 把普通 Agent Loop 外头再套一层目标状态机。执行模型负责推进，宿主负责
> 取证，evaluator 负责判终点，scheduler 负责续轮。四者分家，目标才跑得久、停得准、
> 崩了接得回。

## OpenAI 官方合同

资料查于 2026-08-23。以下只记 OpenAI 官方文档明说的事。

- `/goal <objective>` 把一项 durable objective 挂在当前 chat 上。
- 裸 `/goal` 查看当前目标。
- `/goal edit` 修改目标；`pause/resume/clear` 管生命周期。
- objective 非空，最多 4,000 字符。长说明应放文件，再从目标里引用。
- `/goal` 适合跨 turn 长跑，须有可验证 stopping condition。
- 好目标应写明边界、验证命令或产物、检查点与停止条件。
- 它适合迁移、大重构、实验、原型、带 eval 的反复改进；不适合一把零散 backlog。
- Codex 可独立工作多轮。它认为终点已达便停。
- 功能若未出现，可用 `features.goals` 打开。这说明当前能力仍有 feature gate。

官方资料入口：

- Follow a goal：
  `https://learn.chatgpt.com/use-cases/follow-goals`
- Developer commands：
  `https://learn.chatgpt.com/docs/developer-commands?surface=cli`

下文的预算默认值、evaluator Schema、JSONL 形状、公平调度等，都是 LubanCode
拟议设计。不可冒充 OpenAI 已公开的内部实现。

## `/goal`、`/loop`、普通 turn 分清

| 能力 | 触发下一轮 | 终止条件 | 适合 |
| --- | --- | --- | --- |
| 普通 turn | 用户再说一句 | 本轮模型停 | 一问一答、短活 |
| `/loop` | 时间或监控事件到 | 用户停、过期、任务报告完成 | 看 CI、盯部署、轮询 |
| `/goal` | evaluator 判尚未达标 | 达标、阻塞、需人、预算尽 | 迁移、重构、反复优化 |
| provider retry | 同一请求遇瞬时错误 | 成功或重试耗尽 | 传输恢复 |
| Stop Hook | 一轮刚收口 | Hook 判可停 | 项目自订的 turn 尾规则 |

不得这样做：

- 不把 `/goal` 翻成 `/loop 0s`。那没有终点 evaluator，只会烧 token。
- 不把 provider retry 算新 iteration。它仍是同一轮的 attempt。
- 不把普通 tool error 当 goal terminal。先看它是不是可换路、可修复。
- 不让 Stop Hook 独占终点权。Hook 可补证据，不可越过 GoalCoordinator 续跑。
- 不用 `todo_write` 代替 checkpoint。Todo 是施工清单，goal checkpoint 是跨轮合同。

## LubanCode 当前底子

### 已经有的

| 能力 | 现状 |
| --- | --- |
| slash | `ParseSlashCommand` 与 `DispatchSlashCommand` 已分层 |
| 主会话泵 | `InteractiveSession::Run()` 单线程逐轮消费 |
| Agent Loop | 一轮内可多次模型/工具往返，有 step 上限与取消 |
| turn 结果 | `RunOutcome` 有 cancel、step limit、stop reason、output budget |
| usage | `TurnUsageStats` 按每次模型请求记 token 与 request id |
| 权限 | Confirm / Auto / Yolo，逐工具裁定 |
| 交互 | `ask_user` 与 Runtime InteractionBroker 已有 |
| 队列 | SteeringQueue 可在安全边界自动送主会话消息 |
| Stop Hook | 一轮收尾可给 continuation prompt |
| session | append-only JSONL，消息与 compact/title/cwd/queue event 可回放 |
| compact | compact_v2 manifest 已有 goal/constraints/open_items 一类字段 |
| Runtime | typed ClientCommand / ServerEvent 合同已立 |
| 子代理 | 有 task id、inbox、完成回流与后台唤醒 |
| 模型路由 | TaskKind 可给 compact、标题等小活另配模型 |

### 还欠的

- 没有 `GoalTask`、`GoalIteration`、`GoalCheckpoint`、`GoalEvaluation`。
- 没有 `/goal` slash 枚举、命令解析、帮助与 i18n。
- 普通 `RunTurnResult` 只有 status/cancelled；会话层拿不到完整 `RunOutcome` 分型。
- usage 只在当轮，尚未按 goal 汇总，也没硬预算闸。
- 没有“执行一轮收口后自动起下一轮”的 completion trigger。
- 没有独立 evaluator，也没有结构化终点 Schema。
- session JSONL 不记 goal 定义、revision、iteration 与判词。
- compact 里虽有 `goal` 字段，却没有宿主真本，仍可能被摘要漏掉或改写。
- SteeringQueue 只认用户消息/子代理目标，不认 goal source、iteration、幂等键。
- Stop Hook 若与 goal 一起续跑，会有两只尾巴争方向。
- 工具 trace 尚须接“逐枚追踪”大单，才够给 evaluator 喂可靠证据。

## 产品边界

### 首版命令面

```text
/goal <objective>
/goal
/goal status
/goal edit <objective>
/goal pause
/goal resume
/goal clear
```

兼容 OpenAI 的表面语义：

- `/goal <objective>`：创建并启动。
- `/goal` 与 `/goal status`：看账，不发模型。
- `/goal edit`：进入编辑器；带正文时直接改。
- `/goal pause`：停止排新 iteration。正在跑的轮在下一个安全边界收口。
- `/goal resume`：从最后 checkpoint 续，不重放旧 iteration。
- `/goal clear`：先确认，再摘掉 active goal；历史审计账不删。

### 首版只许一只 active goal

一场 thread 同时只挂一只非终态 goal。理由很直白：

- main history 只有一条；两只目标交替写，会互相污染上下文。
- 同一工作树并发改，证据与 diff 归属说不清。
- 用户消息到底 steer 哪只，也会含混。

已有 active goal 时再敲 `/goal <objective>`，返回 `goal_already_active`。提示用户
用 `edit`、`clear`，或另开 fork。不可默默覆盖。

### feature gate

首版藏在：

```toml
[features]
goals = false
```

同时保留总闸：

```text
LUBANCODE_DISABLE_GOALS=1
```

环境变量只关功能，不改存档。resume 读到 active goal 而功能已关，目标留为
`SuspendedByPolicy`，不自动续跑；用户仍可查、导出、clear。

## 命令解析

### objective

- Trim 后不得为空。
- 上限按 Unicode scalar/UTF-8 字符策略先定死，并在 CLI、Runtime 共用一只校验器。
- 为贴 OpenAI 合同，产品面报“最多 4,000 characters”。实现不可拿 bytes 冒充。
- 含换行可收；存 JSONL 时保原文。
- 不做 shell 拆词，不展开 `$VAR`、反引号、`@file`。
- objective 以 `pause/edit/clear/status/resume` 开头时有歧义，支持 `--`：
  `/goal -- clear the obsolete cache safely`。
- `/goal edit -- <text>` 中 `--` 后全算正文。
- 长稿建议写 `docs/goal.md`，目标只写“按该文件实施并满足某终点”。宿主不替用户
  自动读文件拼进 objective；读取仍走正常工具与权限。

### busy 时的命令

- TUI composer 若当前 turn 正跑，普通 slash 仍走既有规则。
- typed Runtime 可随时收 `PauseGoal`。它只翻 `pause_requested`，不从别线程硬改 history。
- `ClearGoal`、`EditGoal` 遇 Running，先记 pending command，等安全边界再生效。
- `/goal resume` 在已有主 turn 时只记 ready，不并发起第二轮。
- 命令幂等：重复 pause/resume/clear 返回当前状态，不添重复 terminal event。

## 领域模型

### GoalTask

```cpp
enum class GoalState {
    Preparing,
    Active,
    Running,
    Evaluating,
    Pausing,
    Paused,
    AwaitingApproval,
    AwaitingUser,
    Blocked,
    Achieved,
    BudgetExhausted,
    SuspendedByPolicy,
    Failed,
    Cleared,
};

struct GoalTask {
    std::string id;                 // goal-<session-local monotonic id>
    int revision = 1;
    std::string objective;
    std::string objective_sha256;
    GoalContract contract;
    GoalState state;
    GoalBudget budget;
    GoalCounters counters;
    GoalCheckpoint checkpoint;
    std::optional<GoalEvaluation> last_evaluation;
    std::string workspace_root;
    std::string workspace_identity;
    std::int64_t created_at_ms = 0;
    std::int64_t updated_at_ms = 0;
    std::optional<std::int64_t> started_at_ms;
    std::optional<std::int64_t> terminal_at_ms;
};
```

规则：

- id 一旦分配不复用。
- edit 只涨 revision，不换 id。
- 每枚 iteration 记自己吃的 revision；旧轮回来不可覆盖新目标。
- terminal 状态不自动复活。`resume` 只收 Paused/AwaitingUser/Blocked；Achieved 要新建目标。
- `clear` 是终态事件，不抹审计内容。

### GoalContract

```cpp
struct GoalContract {
    std::string objective;
    std::vector<std::string> in_scope;
    std::vector<std::string> out_of_scope;
    std::vector<GoalCriterion> criteria;
    std::vector<std::string> validation_commands;
    std::vector<std::string> required_artifacts;
    std::vector<std::string> constraints;
    std::vector<std::string> checkpoints;
    std::vector<std::string> pause_conditions;
};
```

首轮先做 preflight：模型从 objective 与 repo 现状拟出合同。宿主不准它偷添产品需求。

- objective 已有明确验收，直接 Active。
- 可从仓库查明的路径/命令，先查再填。
- 真缺产品选择，进 AwaitingUser，用 `ask_user` 问。
- 目标只有“把项目做完”“持续优化”一类空话，拒绝自动长跑，状态 AwaitingUser。
- 用户确认合同后，合同也记 hash。后续 evaluator 永远按该 revision 判。

首版若不做单独 preflight UI，也至少把 contract 放进第 0 iteration，并在首次有副作用工具前
冻结。不可跑到半程才换 stopping condition。

### GoalIteration

```cpp
enum class GoalIterationPhase {
    Scheduled, Running, Checkpointed, Evaluating, Finished, Interrupted
};

struct GoalIteration {
    std::string id;                 // goal-3/iter-7
    std::string goal_id;
    int index = 0;
    int goal_revision = 0;
    std::string turn_id;
    GoalIterationPhase phase;
    int attempt = 0;
    std::int64_t started_at_ms = 0;
    std::int64_t finished_at_ms = 0;
    GoalUsage usage;
    std::string before_workspace_fingerprint;
    std::string after_workspace_fingerprint;
    std::vector<std::string> evidence_ids;
    std::optional<GoalCheckpoint> checkpoint;
};
```

- index 每个 goal 从 1 起，单调增加。
- provider retry 只增 attempt，不增 index。
- evaluator retry 另有 evaluation attempt，不冒充新 execution iteration。
- iteration 与 turn 一对一。一次 turn 内原有 AgentLoop 可走多 step、多 tool。

### GoalCheckpoint

```cpp
struct GoalCheckpoint {
    int version = 1;
    std::string summary;
    std::vector<std::string> completed;
    std::vector<std::string> remaining;
    std::vector<std::string> validations;
    std::vector<std::string> evidence_ids;
    std::string next_action;
    std::optional<std::string> blocker_key;
    std::optional<std::string> question;
    std::string progress_fingerprint;
};
```

Checkpoint 要短。它是下轮路标，不是整段聊天摘要。

- completed 只写已落地且有证据的事。
- validations 写命令、退出码、对应 evidence id。
- remaining 须能收敛，不能每轮无故扩单。
- blocker_key 用稳定归一值，如 `missing_credential:DEPLOY_TOKEN`。
- progress fingerprint 由宿主算，不信模型自己填。

### GoalEvidence

```cpp
enum class EvidenceKind {
    ToolResult,
    CommandExit,
    TestReport,
    FileDigest,
    GitDiffSummary,
    Artifact,
    UserDecision,
    RuntimeError,
};

struct GoalEvidence {
    std::string id;                 // ev-<monotonic>
    EvidenceKind kind;
    std::string goal_id;
    std::string iteration_id;
    std::string tool_use_id;
    std::string producer;
    nlohmann::json facts;
    std::string content_sha256;
    std::int64_t observed_at_ms = 0;
    bool fresh = true;
    bool truncated = false;
};
```

证据由宿主从 tool trace、git 状态、测试结果、用户回答里摘。assistant 正文不是证据。

- `run_command` 记 argv/command、cwd、exit code、duration、stdout/stderr digest 与截断标记。
- write/edit 记路径、前后 hash、diff 摘要，不把密钥或整份大文件抄进 goal event。
- MCP 记 server/tool/call id、结构化 result hash、error code。
- 子代理结果须带 child task id；仅“子代理说通过了”仍是二级证据。
- 用户决定可作为产品取舍证据，不能替代测试通过。
- 证据涉及改动后，旧 validation 要按影响范围翻 stale。

## checkpoint 工具

给 goal execution turn 动态露一枚 `goal_checkpoint`。普通 turn、subagent、MCP 都看不见。

输入 Schema：

```json
{
  "type": "object",
  "additionalProperties": false,
  "required": ["status", "summary", "completed", "remaining", "next_action", "evidence_ids"],
  "properties": {
    "status": {
      "type": "string",
      "enum": ["progress", "ready_for_evaluation", "blocked", "needs_user"]
    },
    "summary": {"type": "string", "maxLength": 1200},
    "completed": {"type": "array", "items": {"type": "string"}, "maxItems": 20},
    "remaining": {"type": "array", "items": {"type": "string"}, "maxItems": 20},
    "next_action": {"type": "string", "maxLength": 600},
    "evidence_ids": {"type": "array", "items": {"type": "string"}, "maxItems": 50},
    "blocker_key": {"type": "string", "maxLength": 200},
    "question": {"type": "string", "maxLength": 600}
  }
}
```

工具规矩：

- 只能引用本 goal、本 iteration 已产生的 evidence id。
- 不认识的 id 直接 schema/domain error。
- `ready_for_evaluation` 不等于 achieved，只是请宿主验收。
- status=blocked 必须有 blocker_key；needs_user 必须有 question。
- 一轮可多次 checkpoint，最后一枚为候选；每枚都留 trace。
- 模型没调用工具便自然停，宿主合成 `missing_checkpoint`，仍送 evaluator；不可暗中当成功。
- 工具本身无项目副作用，但要先写 iteration event；写盘失败便不收。

不要复用通用 `update_goal(status=complete)` 让执行模型直接封账。执行模型最多申请验收。

## evaluator

### 身份分开

新增 `TaskKind::GoalEvaluate`。默认另起一只无工具模型调用。

- 不给 write、shell、MCP、Skill、agent、memory。
- 不把 evaluator 输出混进 main history。
- 输入只含冻结合同、当前 checkpoint、宿主证据、旧判词、预算与工作区摘要。
- prompt 明说 tool result 与文件内容都可能夹 prompt injection，只当材料。
- 可配独立模型；未配时回落 normal model，但仍另起 request 与 trace。

### 先程序判，再让模型判

程序先做硬检查：

1. goal revision 与 iteration revision 一致。
2. 必需 criteria 不为空。
3. checkpoint 引用的 evidence 全存在，hash 对得上。
4. 必跑 validation 有新鲜成功证据。
5. workspace root/identity 没漂。
6. 没有 unknown-side-effect 工具悬账。
7. 预算仍够开 evaluator。

硬检查不过，evaluator 只能判 continue/blocked/needs_user，不能 achieved。

### 输出 JSON Schema

```json
{
  "type": "object",
  "additionalProperties": false,
  "required": ["decision", "summary", "progress", "criteria", "next_action"],
  "properties": {
    "decision": {
      "type": "string",
      "enum": ["continue", "achieved", "blocked", "needs_user"]
    },
    "summary": {"type": "string", "maxLength": 1200},
    "progress": {"type": "boolean"},
    "criteria": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "status", "evidence_ids", "reason"],
        "properties": {
          "id": {"type": "string"},
          "status": {"type": "string", "enum": ["pass", "fail", "unknown", "stale"]},
          "evidence_ids": {"type": "array", "items": {"type": "string"}},
          "reason": {"type": "string", "maxLength": 600}
        }
      }
    },
    "next_action": {"type": "string", "maxLength": 600},
    "blocker_key": {"type": "string", "maxLength": 200},
    "question": {"type": "string", "maxLength": 600},
    "confidence": {"type": "number", "minimum": 0, "maximum": 1}
  }
}
```

### achieved 的硬门槛

同时满足才准 Achieved：

- 每条 required criterion 都是 pass。
- 每条 pass 至少指一枚有效 evidence id。
- 必跑 validation 的证据晚于最后一次相关改动。
- 没有 remaining critical item。
- 没有未收口的主工具、子代理、审批或后台命令。
- evaluator decision=achieved。
- confidence 达配置阈值；阈值只可抬高，不可拿来盖过硬检查。

若 evaluator 说 achieved，程序门槛不够，改判 `continue`，错误码
`goal_achievement_evidence_insufficient`，并把缺证据项写成下一步。

### blocked 的门槛

一轮碰墙不急着封 Blocked。先看是否还能换路。

- 同一 blocker_key 连续出现三轮，且三轮都无实质进展，才进 Blocked。
- 缺凭据、缺用户选择、要外部授权，直接 AwaitingUser，不空转三轮。
- 编译错、测试错通常是 continue；除非证明无法在 scope 内修。
- provider/rate limit 是基础设施态，走 Backoff/Pause，不叫业务 Blocked。
- evaluator 给 blocked 却没 blocker_key，schema 不收。

Blocked 不是死档。用户可改目标、补条件后 `/goal resume`。resume 要清连续 blocker
计数，却保留旧账。

### evaluator 失败

- 坏 JSON/Schema：同一 evaluation request 做一次 repair。
- provider 瞬时错：有限退避重试，仍用同一 evaluation id。
- 两次都坏：目标进 Paused，reason=`evaluator_failed`。
- 不得因 evaluator 挂了便默认 achieved，也不得盲开下一轮。

## GoalCoordinator 状态机

```text
Created
  -> Preparing
  -> Active
  -> Scheduled(iteration N)
  -> Running(turn N)
  -> Checkpointed
  -> Evaluating
       -> Continue ------> Scheduled(iteration N+1)
       -> Achieved ------> Achieved
       -> Blocked -------> Blocked
       -> NeedsUser -----> AwaitingUser
       -> BudgetHit -----> BudgetExhausted

任意非终态
  -> pause requested -> Pausing -> Paused
  -> clear requested -> Cleared
  -> store failure   -> Failed/Paused(fail closed)
```

状态转换集中进 `GoalCoordinator::Apply(event)`。CLI、TUI、Hook、模型工具都不得直接
改 `GoalTask.state`。

非法转换返回稳定码：

- `goal_not_found`
- `goal_already_active`
- `goal_terminal`
- `goal_revision_conflict`
- `goal_busy`
- `goal_invalid_transition`
- `goal_budget_exhausted`
- `goal_scope_changed`
- `goal_store_unavailable`

## 主循环怎样续轮

### 单飞铁律

- 一场 session 同时只跑一枚 main turn。
- GoalCoordinator 不从 evaluator callback 或 timer thread 直接调 `AgentLoop::Run()`。
- evaluator 收口只产 decision event，并唤醒 session pump。
- session pump 回到安全边界，才取下一枚 goal continuation。
- 当前有用户 turn、审批、队列编辑或 compact，goal 等着，不抢 history 锁。

### completion-driven pump

```text
RunTurn 收口
  -> flush assistant/tool trace
  -> close GoalIteration execution
  -> build host evidence
  -> run evaluator
  -> flush evaluation
  -> coordinator applies decision
  -> if Continue: enqueue GoalReady(goal_id, next_iteration)
  -> main session pump starts synthetic turn
```

synthetic user message 带元数据：

```json
{
  "source": "goal",
  "goal_id": "goal-3",
  "goal_revision": 2,
  "iteration_id": "goal-3/iter-8",
  "checkpoint_version": 7,
  "dedupe_key": "goal-3:r2:i8"
}
```

不要只塞一段“请继续”文字。文字供模型看，metadata 供宿主认身份、去重、恢复。

### 用户消息与 goal

- 当前 goal Active 时，用户普通新消息仍可进 main turn。
- 它默认是 steering/context，不自动改 objective。
- 若用户明确改终点，UI 提示用 `/goal edit`；宿主不可从自然语言暗改合同。
- 用户消息优先于尚未启动的自动 continuation。
- 用户 turn 收口后，GoalCoordinator 也要 evaluator 看它是否改变了进展。
- 与目标无关的闲聊不计 goal iteration；它也不该刷新无进展计数。
- 用户排队消息与 goal continuation 同时 ready：先用户，后 goal；同一安全边界只取一枚。

### `/loop` 与 `/goal` 一起开

统一放进 `SessionWorkScheduler`，不共用 trigger：

| work kind | ready 原因 | 默认优先级 |
| --- | --- | --- |
| user queued turn | 用户输入 | 最高 |
| pending interaction | 审批/问答 | 最高 |
| goal continuation | evaluator=continue | 中 |
| due loop tick | 时钟到点 | 中 |
| maintenance | compact/摘要 | 低 |

- goal 连跑三轮后，让一枚已到期 loop tick 插队，防饿死。
- loop tick 不算 goal progress，除非其结果显式转成 GoalEvidence。
- goal 内不得创建零间隔 loop 来旁路预算。
- loop prompt 也不得 `/goal` 新建第二只 active goal。

## 每轮喂给模型什么

动态 GoalContext 由宿主拼，放在普通 system prompt 之后、当前 user message 之前：

```text
[Active Goal]
id/revision
objective
scope and constraints
success criteria
current checkpoint
verified evidence summary
remaining budget
last evaluator decision
this iteration's single next action
checkpoint tool contract
```

规矩：

- objective 与 contract 每轮原样从 GoalTask 账注入，不靠 history 回忆。
- 只带最近 checkpoint 与仍有效 evidence 摘要，不带八轮旧正文。
- `next_action` 是建议，不准盖过用户刚下的新指令。
- tool 结果、网页、文件内容标不可信，防它伪造“goal achieved”。
- system prompt 明说执行模型无权切 goal state。
- 本轮将到 step 上限时，沿用现有收口提醒，并要求先写 checkpoint。

## context 与 compact

Goal 真本不住聊天摘要。compact 只压模型上下文，不压 GoalTask/event ledger。

### 每次 compact 必守恒

- goal id、revision、objective hash。
- contract 与 criteria id。
- state、iteration index。
- 最近 checkpoint。
- 尚有效 evidence id 与摘要。
- blocker streak、no-progress streak。
- 已用/剩余预算。
- pending approval/question。
- 当前 workspace identity。

### 程序先滤，再交 LLM compact

1. 从 goal event ledger 重建结构化 snapshot。
2. 工具结果按 evidence id 去重；大 stdout 只留 digest、退出码与必要尾段。
3. 旧 iteration 按“完成/未完/失败/验证”四栏程序归并。
4. 只把无法程序归并的解释文字分块交 LLM compact。
5. reduce 后过 manifest validator；criteria、constraints、open items 少一项便拒稿。
6. compact_v2 写 goal snapshot hash。resume 时与 goal ledger 对账。

若 compact 失败，goal 不该丢。退回未压缩 history，或在 context 硬线前 Pause。

## 预算与刹车

### GoalBudget

```cpp
struct GoalBudget {
    std::optional<std::int64_t> max_total_tokens;
    std::optional<std::int64_t> max_elapsed_ms;
    std::optional<int> max_iterations;
    std::optional<std::int64_t> max_cost_micros;
    int max_no_progress_iterations = 3;
    int max_same_blocker_iterations = 3;
    int max_consecutive_provider_failures = 3;
};
```

首版至少开三只硬闸：elapsed、iterations、no-progress。token 未回报时，不能拿 0
冒充没花；此时 time/iteration 仍能收口。

建议默认值先经产品确认，再写 config：

```toml
[goals]
max_elapsed = "2h"
max_iterations = 40
max_no_progress_iterations = 3
max_same_blocker_iterations = 3
max_consecutive_provider_failures = 3
```

不把未经压测的 token 数写死成天条。CLI `/goal` 首版也不塞一串 budget flag；先从
config 读，并在 `/goal status` 明示。后续 typed API 可带 per-goal override。

### budget 结算

- execution 与 evaluator 都计入 goal usage，分角色列账。
- cache read/creation 与 input/output 分开存；总量口径沿用 TurnUsageStats。
- 子代理由该 iteration 派生，其 usage 归 goal；旁支用户任务不归。
- provider 不报 usage，记 `reported=false`，保 request count 与 duration。
- 开新 iteration 前先估最小 headroom；不够便 BudgetExhausted，不等请求打半截。
- 当前 iteration 中途撞 output/step limit，先保 checkpoint；不可当 achieved。
- cost 只有 provider 给价目/账单口径时才启用；不凭模型名硬猜价格。

### 无进展判定

`progress_fingerprint` 由这些稳定材料算：

- workspace tracked diff digest。
- GoalCheckpoint completed/remaining 的规范化 hash。
- 新 evidence 集。
- criteria 状态向量。
- blocker key。

下面不算进展：

- 只换一句 summary。
- 同一失败命令原样再跑，输出 hash 不变。
- todo 来回改措辞。
- 新建子代理却没拿到结果。
- 大量思考文本，没有文件、证据或判定变化。

连续三轮无进展，目标进 Paused，reason=`no_progress`。状态页列出三轮 fingerprint
与最后动作，方便人看哪里打滑。

## 权限与无人值守

Goal 是任务意图，不是授权。

- 每枚工具照常过 ModePolicy、Hook、PermissionMode。
- Confirm 模式遇需确认工具，goal 进 AwaitingApproval，绝不因用户不在就默认 yes。
- 一次性允许只管那枚 tool_use_id；不会变成后四十轮通行证。
- “本会话总是允许”沿用现有账，但 `/goal status` 要显出 active grants。
- Yolo 仍压不过 isolation、deny list、Plan Mode 硬闸与 OS sandbox。
- background/non-TTY 首版禁自动 goal；以后须有明示 noninteractive policy。
- pending approval 时不算无进展，也不烧 iteration。
- pause/clear 遇 pending approval：先取消 InteractionFuture，记 Declined/Cancelled，工具未跑。
- 有副作用工具结果 unknown，目标立即 Paused，等人查明；不得自动 retry。

Plan Mode 与 Goal 的关系：

- Plan Mode 里可创建 goal draft，不可启动 execution。
- 批准 Plan 后，可把 ImplementationBrief 变成 GoalContract，再切 Default。
- Plan 硬闸尚在时，GoalCoordinator 即使 ready 也不派执行轮。
- `/goal resume` 不得暗中退出 Plan。

## session JSONL

### 事件形状

```json
{"type":"goal_v1","event":"created","goal_id":"goal-3","revision":1,"objective":"...","objective_sha256":"...","budget":{},"workspace":{},"ts":"..."}
{"type":"goal_v1","event":"contract_ready","goal_id":"goal-3","revision":1,"contract":{},"contract_sha256":"...","ts":"..."}
{"type":"goal_iteration_v1","event":"scheduled","goal_id":"goal-3","iteration_id":"goal-3/iter-1","revision":1,"dedupe_key":"...","ts":"..."}
{"type":"goal_iteration_v1","event":"started","goal_id":"goal-3","iteration_id":"goal-3/iter-1","turn_id":"turn-9","before_fingerprint":"...","ts":"..."}
{"type":"goal_evidence_v1","event":"observed","goal_id":"goal-3","iteration_id":"goal-3/iter-1","evidence_id":"ev-7","kind":"command_exit","facts":{},"sha256":"...","ts":"..."}
{"type":"goal_checkpoint_v1","goal_id":"goal-3","iteration_id":"goal-3/iter-1","checkpoint":{},"sha256":"...","ts":"..."}
{"type":"goal_evaluation_v1","goal_id":"goal-3","iteration_id":"goal-3/iter-1","evaluation_id":"eval-1","decision":"continue","criteria":[],"evidence_sha256":"...","ts":"..."}
{"type":"goal_iteration_v1","event":"finished","goal_id":"goal-3","iteration_id":"goal-3/iter-1","outcome":"continue","usage":{},"after_fingerprint":"...","ts":"..."}
{"type":"goal_v1","event":"achieved","goal_id":"goal-3","revision":1,"evaluation_id":"eval-8","ts":"..."}
```

### 写盘栅栏

1. created flush 成功，才向用户报 goal 已立。
2. contract_ready 成功，才开首轮有副作用工具。
3. iteration scheduled 先落，main pump 才能取。
4. started 先落，再发 synthetic turn，dedupe key 同时写。
5. tool trace/evidence 随发生追加，不等轮末攒一把。
6. checkpoint 与 execution terminal 都落，才开 evaluator。
7. evaluation 先落，coordinator 再改内存 state。
8. continue decision 已落，next scheduled 未落：恢复器负责补一枚。
9. achieved/blocked/paused 先落，才从 ready queue 摘任务。

SessionStore append 失败后，fail closed：

- 当前已在跑的工具按既有规则收口。
- 不开新工具、不跑 evaluator、不排下一轮。
- goal 进 Failed 或 Paused(StoreUnavailable)。
- 普通聊天可继续，但 UI 常驻红条；用户可导出内存快照。

### 回放器

按 append-only event 重建，不信最后一条 summary：

- created 无 contract：回 Preparing。
- scheduled 无 started：可安全启动一次。
- started 无 turn terminal：先走现有 turn/tool trace 恢复；不另发 prompt。
- execution terminal 无 checkpoint：合成 missing checkpoint，送 evaluator。
- checkpoint 无 evaluation：补 evaluator。
- evaluation=continue 无下轮 scheduled：补一次。
- terminal goal 后还有迟到 continue：忽略并记 warning。
- edit revision 已落，旧 evaluator 后到：判 stale，不改新 revision。
- 尾行截断：跳过坏尾；上一枚完整 event 为准。
- evidence 行 hash 坏：该证据 invalid，不能 achieved。

## pause、resume、edit、clear

### pause

- Idle/Active：立刻 Paused。
- Running：记 pause_requested；在下一个 tool/step 安全边界收口。
- Evaluating：可等 evaluator 回来再丢判词，也可 cancel request；首版选“等回但不续”。
- AwaitingApproval/User：取消悬问，记 paused reason。
- pause 不清 checkpoint、budget、blocker 账。

### resume

resume 前做 preflight：

- feature 仍开。
- workspace root/identity 对得上。
- objective 引用的关键文件仍在。
- budget 未尽。
- 没有未知副作用悬账。
- 当前不是 Plan-only 禁写态。

通过后从 now 开一枚新 iteration。旧 scheduled time 没意义；Goal 不追“漏掉的轮”。

### edit

- 先存新 objective，revision +1，重跑 contract preflight。
- 当前 Running 时先 pause at boundary。
- 旧 checkpoint 可作背景，但 completed/remaining/criteria 要逐项 rebase。
- 旧 evidence 默认保留，却标适用 revision；能否复用由新 criteria validator 判。
- 目标改得面目全非时提示 clear + new，别拿同一 id 糊审计。
- edit 后 no-progress/blocker streak 清零；usage 不清零。

### clear

- 二次确认显示 objective preview、iteration、已耗预算、未收工具。
- Running 时 cooperative cancel，等工具安全收口。
- 写 cleared terminal event，不删 JSONL。
- 清 active pointer 与 ready continuation。
- 不撤销已经改过的文件；clear 不是 rollback。

## crash、resume、fork、clear thread

- `/resume` 恢复同 thread 的非终态 goal；默认先展示状态，再问是否继续自动跑。
- `--continue` 若产品要求自动续，须有显式 config；首版默认 paused-on-resume 更稳。
- `/clear` 起新 thread。旧 thread 的 active goal 先 Paused(ThreadCleared)，不可悄悄跟过去。
- fork 复制聊天与 goal snapshot，却把新分支状态设 PausedFork。用户明确 resume 才跑。
- 原 thread 与 fork 不可共享同一 active goal id；fork id 加 lineage，避免两边 event 撞号。
- cwd/worktree 变化先比 workspace identity。变化在同一批准 worktree 内可 rebase；跨 root 暂停。
- Git branch/HEAD 漂移不必一律停，但会让旧 validation stale，必须重验。
- 进程被 kill 后，started-before-effect 与 effect-before-finished 的窗靠 tool trace 幂等/unknown
  规则处理；Goal 层不自行猜工具是否执行。

## 失败、重试与回溯

### 三层失败分账

| 层 | 例子 | 处理 |
| --- | --- | --- |
| attempt | 连接断、429、临时 5xx | 同 request/iteration 有限退避 |
| iteration | 编译失败、方案走不通 | 留证据，evaluator 判换路或 blocked |
| goal | 预算尽、长期无进展、外部条件缺失 | pause/block/terminal |

### retry

- provider 请求尚未到服务端：可安全 retry。
- 服务端已收、响应未知：依 provider idempotency 能力判；不能一概重发。
- 纯 evaluator 请求无工具副作用，可有限 retry。
- read-only tool 可按工具合同 retry。
- 写/命令工具结果 unknown 不 retry，目标 PausedAwaitingReconcile。
- retry-after 优先于本地指数退避；仍受 max elapsed。
- 每次 attempt 都带 stable request/iteration/tool ids，状态页能追。

### 回溯不是“回滚世界”

Goal 层的回溯先指追踪：

```text
goal_id -> iteration_id -> turn_id -> step/request_id -> tool_use_id -> evidence_id -> evaluation_id
```

出了错，可沿这条链找到哪轮、哪次请求、哪枚工具、哪份证据、哪道判词。

自动 rollback 首版不做。Git reset、反向 SQL、外部 API 补偿各有风险。若工具声明
compensation contract，未来可加 Saga；没声明便只报告 partial effects，等人处置。

## Hooks

可添这些中立事件：

```text
GoalCreated
GoalIterationStart
GoalIterationEnd
GoalEvaluated
GoalPaused
GoalCompleted
```

边界：

- Hook 可补 additional context、添 evidence、请求 pause。
- Hook 不可直接写 Achieved；仍要过 evaluator 与硬门槛。
- GoalIterationStart Hook 失败，按 hook policy 明报 blocked/failure，不静默跳。
- GoalCompleted Hook 在 terminal event 已落后跑。它失败不把 Achieved 改回 Active。
- Stop Hook 在 goal turn 中只作“本轮还须收口”的局部规则；最终 continuation 归 GoalCoordinator。
- Hook 自己的副作用不算 agent tool guarantee，审计照现有 HookRunRecord 记。
- 不让 Hook 另起私有 while loop，与 `/goal` 争同一 session。

## Skill、MCP、Plugin、PTC

- Skill 可教执行法，不可改 GoalContract、预算或 terminal state。
- Skill 要求“永不停止”时，宿主预算照压，Skill 压不过硬闸。
- MCP/tool 每枚调用带 goal/iteration/turn/tool id，逐枚归因。
- MCP annotations 只能辅助分 effect，不是可信验收证据；真实 result/trace 才是。
- 原生 Plugin 函数若绕过 ToolRegistry，无法归 evidence。Goal 模式首版禁这类旁路。
- PTC 编排出的多 tool 仍逐枚 trace；一枚失败，不把整批只记成“PTC failed”。
- 子代理不能创建/clear 根 goal。它只接 scoped subtask，结果回主 iteration。
- 子代理结束未回流前，主 evaluator 不准 Achieved。
- memory 可提供背景，不得凭一条旧记忆判 criteria pass；须用当前证据复核。

## Runtime 与多前端合同

新增 `ClientCommandKind`：

```text
CreateGoal
GetGoal
EditGoal
PauseGoal
ResumeGoal
ClearGoal
```

payload 不让前端拼 slash：

```json
{
  "kind": "create_goal",
  "thread_id": "thread-1",
  "payload": {
    "objective": "...",
    "budget": {},
    "expected_revision": 0
  }
}
```

新增 Runtime 事件或 Item：

```text
GoalCreated
GoalUpdated
GoalStateChanged
GoalIterationStarted
GoalCheckpointed
GoalEvaluated
GoalCompleted
```

若沿现有通用 Item 协议，给 `ItemKind` 添 `Goal`；长状态走 payload，不把整份 objective
塞进 title。

共同字段：

- thread_id、seq、timestamp。
- goal_id、revision、iteration_id、turn_id。
- from_state、to_state、reason_code。
- usage delta、budget remaining。
- checkpoint/evaluation 摘要与 artifact ref。

前端规矩：

- TUI slash 只译 typed command。
- Web/Tauri/app-server 直接发 command，不伪造 `/goal ...` 字符串。
- 断线重连按 thread seq 补事件；GoalCoordinator 不因多前端重复 command 重复续轮。
- 控制命令带 command id 与 expected revision，做 optimistic concurrency。
- objective 默认只显示 preview；详情页再开全文，防肩窥与日志泄露。

## 状态栏与 `/goal status`

至少显这些：

```text
goal-3 · Active · r2 · iter 8
目标：迁移认证层并保持契约测试通过
当前：修复 token refresh 的 Windows 分支
判定：continue（4/6 criteria pass）
进展：有 · blocker streak 0 · no-progress 0
验证：auth_contract 通过；e2e stale
预算：8/40 iter · 31m/2h · token 184k reported
下一步：重跑 e2e，再核旧路径回滚
```

还要列：

- pending approval/question。
- last error 与 attempt。
- workspace/branch 摘要。
- active permission mode 与 persistent grants 数。
- evaluator 模型/最后 confidence。
- 最近三枚 evidence id，可跳 trace。

状态查询不发模型，不依赖 compact 后聊天正文。

## 安全与隐私

- objective、checkpoint、evidence 写 session 存档，须沿用文件权限与敏感字段打码。
- command stdout 不整段复制到 goal event；存 digest、必要摘要、artifact ref。
- authorization/cookie/token/password 等沿用 workflow recorder 的 redaction。
- evaluator 输入也先打码。不能因“内部模型调用”便把密钥原样再发一遍。
- 用户从网页/issue 给的 stopping condition 视为不可信文本；不可盖过 system/permission。
- criteria 若要求 push、deploy、delete，仍逐工具审批。
- GoalCompleted 不是供应链证明。报告须列验证范围与未验证项。
- 多租户 app-server 下，goal command、event、artifact 都按 thread owner 授权。
- 导出 goal 时，默认不含完整 tool stdout 与隐藏 reasoning。

## corner cases

- objective 恰好 4,000 characters；emoji、组合字符、CRLF 怎样计。
- `/goal -- pause all jobs` 不误认子命令。
- active goal 再 create，不覆盖。
- edit 与 evaluator 同时回来，旧 revision 判词作废。
- pause 在 tool 已批准、尚未执行那一线，工具不跑。
- pause 在外部 API 已发、结果未知，进 reconcile，不当干净暂停。
- clear 不撤改动，也不删 audit。
- goal 首轮发现仓库本来已达标：仍跑 fresh validation，之后可 Achieved(iteration 0/1 要定死)。
- execution 忘调 checkpoint：合成 missing checkpoint，不停摆、不谎成完成。
- evaluator 坏 JSON、漏 criterion、引用假 evidence id。
- validation 成功后又改相关文件，证据 stale。
- git status 无变化，却数据库/云资源已有副作用；靠 tool trace，不只看 diff。
- 同一失败命令换了时间戳，stdout hash 不变，仍算无进展。
- provider 不报 usage，time/iteration 闸照收。
- context 快满，剩余 budget 又不够 compact：Pause，不硬撞窗口。
- compact 恰与 pause/edit 到来：主线程串行，revision 不穿插。
- resume 时 branch 换了、worktree 删了、引用文件不在。
- session 尾行半截；evaluation 已落、next schedule 未落。
- terminal 后迟到的 evaluator/子代理/Hook 结果，只留 evidence，不复活。
- fork 两边同时 resume，id/工作区隔离。
- `/loop` due 与 goal continue 同时 ready，用户消息不饿死。
- Stop Hook 要续，evaluator 要 achieved：先满足本轮 Stop Hook 收口，再重评，不直接封。
- ask_user 无前端可答，转 AwaitingUser，不挂工作线程一夜。
- Confirm 模式持久 grant 后权限配置收紧，新 policy 立即生效，旧 grant 作废。
- GoalCompleted Hook 发布失败：goal 仍 Achieved，另报 post-completion failure。
- 机器休眠不影响 completion trigger；elapsed budget 却按 wall time 走，醒来可能直接 Exhausted。
- 系统时间回拨：duration 用 steady clock；存档时间用 wall clock。
- evaluator 自己撞 token limit：不采半截 JSON，retry 或 pause。
- 目标要求永不停止：preflight 拒绝，必须改成可验证终点。

## 实施分期

### 第 0 期：合同与 golden sessions

- [ ] 写 `GoalState`、合法转换表、错误码与术语表。
- [ ] 定 objective 4,000 characters 的准确计数法。
- [ ] 定“一只 active goal”、resume 默认 paused、fork paused 三项产品边界。
- [ ] 写已达标、正常三轮、需人、同 blocker、无进展、预算尽、崩溃中途 golden。
- [ ] 把 `/goal` 与 `/loop`/retry/Stop Hook 的触发图钉进架构文档。
- [ ] 给 feature gate 与禁用环境变量定优先级。

### 第 1 期：内存状态与命令面

- [ ] `src/runtime/goal_types.*`：GoalTask/Contract/Budget/Checkpoint/Evaluation。
- [ ] `src/runtime/goal_coordinator.*`：纯状态机与 transition tests。
- [ ] slash enum/parser/help/i18n 加 set/view/edit/pause/resume/clear。
- [ ] 一场只许一只 active goal；revision CAS。
- [ ] `/goal status` 纯本地输出，不发模型。
- [ ] 配置 `features.goals` 与总闸。

### 第 2 期：execution iteration 与 checkpoint

- [ ] 主 session pump 接 GoalReady，不并发调 AgentLoop。
- [ ] synthetic goal message 加 metadata/dedupe key。
- [ ] 动态注入 GoalContext。
- [ ] goal turn 才注册 `goal_checkpoint`。
- [ ] 从 `RunTurn` 透出完整 RunOutcome/usage/turn id，别只剩 status bool。
- [ ] step/output limit 前强制收口 checkpoint。
- [ ] 用户 queued turn 优先与 goal fairness tests。

### 第 3 期：证据与 evaluator

- [ ] 接工具逐枚 trace，建 GoalEvidence normalizer。
- [ ] run_command/test/write/MCP/subagent 各有 evidence adapter。
- [ ] validation freshness 与 criteria hard gate。
- [ ] `TaskKind::GoalEvaluate`，无工具独立调用。
- [ ] strict JSON Schema、一次 repair、provider retry。
- [ ] achieved/continue/blocked/needs_user 四路闭环。
- [ ] evaluator 假 evidence、漏 criterion、prompt injection 测试。

### 第 4 期：JSONL 与恢复

- [ ] goal_v1/iteration/evidence/checkpoint/evaluation serialize/parse。
- [ ] SessionStore append+flush API 与 replay reducer。
- [ ] 九道写盘栅栏逐点 fault injection。
- [ ] started 无 terminal、evaluation 无 schedule 等恢复补账。
- [ ] event hash、tail truncation、unknown future event 兼容。
- [ ] append 失败 fail-closed，不开下一轮。

### 第 5 期：预算、防空转与权限

- [ ] elapsed/iteration/no-progress/same-blocker/provider-failure 硬闸。
- [ ] execution/evaluator/subagent usage 分角色汇总。
- [ ] usage 未报告的显示与 fallback 收口。
- [ ] AwaitingApproval/User，不把等待算 iteration。
- [ ] unknown side effect 进 reconcile pause。
- [ ] pause/resume/edit/clear 在每种 phase 的状态测试。

### 第 6 期：compact、resume、fork

- [ ] compact_v2 manifest 加 goal snapshot/hash。
- [ ] 程序过滤旧 iteration/evidence，再分块 LLM compact。
- [ ] manifest 守恒 validator。
- [ ] `/resume` 重建后默认 paused，显式续跑。
- [ ] fork lineage/new id/PausedFork。
- [ ] workspace/branch drift 与 validation stale。

### 第 7 期：Runtime 与多前端

- [ ] typed Goal commands/receipts，expected revision 与 command id。
- [ ] Goal events/ItemKind::Goal/seq 重连补账。
- [ ] TUI status/card/审批/问答。
- [ ] app-server schema、Web/Tauri adapter。
- [ ] 断线期间继续跑、重连不重复派 iteration。
- [ ] redaction 与导出格式。

### 第 8 期：与 Loop/Hook/Subagent 合流

- [ ] SessionWorkScheduler 统一公平调度，trigger 仍分开。
- [ ] Goal/Loop/用户队列饥饿测试。
- [ ] Stop Hook 只管本轮，GoalCoordinator 管跨轮。
- [ ] Goal Hook events 与 failure policy。
- [ ] 子代理 usage/evidence 回主 iteration。
- [ ] Plugin/PTC/MCP 不得旁路 trace/permission/budget。

## 测试矩阵

### parser 与命令

- [ ] create/view/status/edit/pause/resume/clear 正常路。
- [ ] 空 objective、全空白、恰 4000、4001、emoji、CRLF。
- [ ] `--` 消歧与多行正文。
- [ ] active 再 create。
- [ ] terminal resume/edit/clear 的合法/非法组合。
- [ ] busy 时 pause/edit/clear 的 pending command。
- [ ] feature off、env disable、resume 旧 active goal。

### 状态机

- [ ] 每条合法转换走到；每条非法转换稳定拒绝。
- [ ] revision CAS 冲突。
- [ ] 旧 evaluator/子代理/Hook 迟到不复活 terminal。
- [ ] pause requested 在各安全边界收口。
- [ ] clear 幂等，terminal event 唯一。
- [ ] achieved 后不再有 GoalReady。

### evaluator

- [ ] 四种 decision。
- [ ] criteria 全 pass + fresh evidence 才 achieved。
- [ ] assistant 自报通过、宿主无证据，不能 achieved。
- [ ] 假 evidence id、跨 goal id、hash 错。
- [ ] stale validation。
- [ ] 坏 JSON repair 一次；再坏 pause。
- [ ] prompt injection 要求 evaluator 忽略合同。
- [ ] 同 blocker 三轮与第一轮可换路。
- [ ] needs_user 缺 question、blocked 缺 key，schema 拒绝。

### 调度与并发

- [ ] 一场只有一枚 main turn。
- [ ] 用户消息优先 goal continuation。
- [ ] 连跑三轮后 loop tick 得到机会。
- [ ] evaluator callback 只发 ready event，不直调 AgentLoop。
- [ ] duplicate ready/dedupe key 不开两轮。
- [ ] pause 与 ready 同时发生，pause 胜。
- [ ] edit revision 与旧 running turn 回来，旧结果只留审计。

### 预算与防空转

- [ ] elapsed、iteration、no-progress、same-blocker 各自触发。
- [ ] provider usage 未报告仍靠 time/iteration 停。
- [ ] evaluator 与子代理 usage 计入。
- [ ] 等审批/用户不计 no-progress。
- [ ] 同命令同失败 hash 不算进展。
- [ ] criteria 从 fail 到 pass 算进展。
- [ ] budget 开轮前 headroom 不足。

### session 与恢复

- [ ] 每种 event serialize/parse/roundtrip。
- [ ] 坏一行跳过，坏 hash 证据失效。
- [ ] 九道写盘栅栏逐点 crash。
- [ ] scheduled 未 started 只启动一次。
- [ ] started 未 terminal 不重发副作用。
- [ ] evaluation continue 未 scheduled 补一枚。
- [ ] terminal 后迟到 event 忽略状态变更。
- [ ] store append 失败 fail-closed。

### compact/resume/fork

- [ ] 多次 compact 后 goal id/revision/criteria/checkpoint/budget 守恒。
- [ ] 大 tool output 只留 digest/artifact ref。
- [ ] compact validator 缺 constraint 拒稿。
- [ ] resume 默认 paused，显式 resume 才动。
- [ ] fork 新 id、lineage、PausedFork。
- [ ] branch/HEAD/worktree 漂移让 validation stale。

### 权限与工具

- [ ] Confirm 无人答进 AwaitingApproval。
- [ ] 一次性 approval 不跨 tool_use。
- [ ] Yolo 压不过 isolation/deny/Plan。
- [ ] unknown side effect 不 retry。
- [ ] MCP 五枚工具中一枚失败，可按 tool_use/evidence 找准。
- [ ] PTC 多调用逐枚归账。
- [ ] 子代理未回流不能 achieved。
- [ ] GoalCompleted Hook 失败不改 terminal。

## 验收

- `/goal <objective>` 可立一项当前 thread 持久目标，裸 `/goal` 能看全账。
- 目标跨多轮自动推进；每轮都有 iteration/turn/tool/evidence/evaluation 链。
- 模型一句“完成”不能封账；required criteria 与新鲜证据齐了才 Achieved。
- continue 才排下一轮；用户消息、loop tick、compact 不与主 turn 并发。
- pause/resume/edit/clear 各有明确、幂等、可恢复的语义。
- 时间、iteration、无进展、同 blocker、provider failure 都能刹车。
- crash 可落在任一道栅栏；resume 不重复副作用、不漏 evaluator、不多开下一轮。
- compact 后目标合同、checkpoint、证据引用、预算仍在，不靠 LLM 摘要碰运气。
- 权限逐工具生效；Goal 不冒充授权，无人可答时不挂死。
- `/goal` 与 `/loop` 触发/终止分清，却共用一只公平的 session work pump。
- TUI、app-server、Web/Tauri 共用 typed contract，不各写一套状态机。
- 面试时能从 goal id 一路追到哪一轮、哪枚工具、哪份证据、哪道终点判词。

## 不做

- 不把 `/goal` 做成无限 prompt。
- 不把 `/goal` 做成 `/loop 0s`。
- 不让执行模型自己写 Achieved。
- 不把 assistant 正文当测试证据。
- 不用 todo/checklist 代替 GoalContract。
- 不在同一 thread 同时跑两只 active goal。
- 不并发跑两枚 main turn。
- 不因 feature off 删除旧 goal 存档。
- 不拿 token 未报告冒充零消耗。
- 不对 unknown-side-effect 自动 retry。
- 不把 clear 当 rollback。
- 不默认 fork 两边一起续跑。
- 不把 Hook/Skill/MCP annotation 当越权通行证。
- 不在首版做通用 daemon、关机后可靠执行或分布式 leader election。

## 源码入口

| 责任 | 文件 / 符号 |
| --- | --- |
| slash 解析/帮助 | `src/cli/slash_commands.*` |
| 会话主泵/命令分派 | `src/app/interactive_session.cpp` |
| 一轮执行/结果 | `src/app/turn_runner.*`、`RunTurnResult` |
| Agent Loop/RunOutcome | `src/agent/loop.*` |
| usage/权限/PromptGate | `src/runtime/turn_runtime.*` |
| typed command/event | `src/runtime/command.hpp`、`event.hpp` |
| 交互 Broker | `src/runtime/interaction_broker.hpp` |
| session JSONL | `src/agent/session_store.*` |
| compact | `src/agent/compact.*`、`microcompact.*` |
| system prompt | `src/agent/prompt_assembler.*`、`src/prompts/` |
| 用户/自动消息队列 | `src/cli/queue_model.*` |
| 工具与子代理 | `src/app/tool_runtime.*`、`src/tools/agent_tool.*` |
| Hook | `src/hooks/dispatcher.*`、`src/hooks/types.hpp` |
| 模型路由/TaskKind | `src/agent/model_router.*`、`src/app/model_router.*` |
| `/loop` 调度单 | `todos/loop会话定时循环_调度恢复权限与goal分流.todo` |
| Plan 模式单 | `todos/Plan模式_只读研究硬闸_计划审阅与执行交接.todo` |
| 工具追踪/恢复 | `todos/工具调用逐枚追踪_五工具故障归因崩溃恢复与副作用补偿.todo` |
