# 请求重试预算加厚：五次重试与指数退避拉长

- 状态：待实现（2026-09-02 主人立单）
- 日期：2026-09-02
- 级别：P1（可靠性——重试预算对不上真实故障的恢复窗口）
- 范围：`src/api/model_request_recovery.*`、退避阶梯、监督器面板的"重连 N/M"上限联动、测试
- 依赖：P0-1 请求级恢复（已落，0.26.157）
- 关联：《子代理监督器_agent_watch与停滞恢复设计.todo》
- 不在本单：改 IsRetryableError 白名单口径；流空闲超时与总墙钟的尺子

## 一、先下断语

现在的重试预算是"首发 + 2 次重试，退避最长等 2 秒"（`model_request_recovery.hpp:68` 的 `kMaxRequestAttempts = 3`；阶梯 250~750ms / 1~2s，见 `model_request_recovery.cpp:85`）。

这笔预算对付瞬时抖动够，对付真实故障不够。中转站重启、上游限流恢复、网关抖一抖——常见恢复窗口在 30~60 秒开外。现在 3 秒内烧完两回重试就报死，用户看到的是失败，再等半分钟本来就能活。**主人定案：重试提到 5 次，每次等久一点。**

## 二、定案

1. **重试 5 次**：总尝试 6 次（首发 + 5 回重试）。`kMaxRequestAttempts = 6`。
2. **退避指数拉长带抖动**（建议案，数值主人可裁）：
   ```text
   重试 1 → 1~2s
   重试 2 → 4~8s
   重试 3 → 15~30s
   重试 4 → 30~60s
   重试 5 → 60~120s
   ```
   均匀抖动照旧（防齐步走）。最长单次退避 2 分钟封顶，不再往上叠。
3. **白名单不动**：401/400 一类确定性错误照旧零重试——重试更久也没用，这不是本单要治的病。
4. **可取消照旧**：总墙钟、用户取消随时打断退避（10ms 粒度可取消等待既有）；子代理取消链同根旗打断不回归。

## 三、联动改（容易漏的三处）

1. **监督器面板写死的上限**：`agent_panel_presenter.cpp` 的 SupervisionSegment 里"重连 N/3"按 `kMaxRequestAttempts` 同数值写死并注释拴住（监督器单缝合时留的话）——常量一改，这里与 i18n 词条 `agent_supervision.recovering` 的分母跟着动。改法：把这处改成引用真常量（引 api 头）或至少同步改数值+注释。
2. **监督尺与长退避的互动**：第 3 回起的退避 15s+，监督拍按"等首字节"尺子会判 Quiet/Suspect——恢复态本有 attempt 数可看，确认健康状态机把"退避等待中"归 Recovering 而非 Suspect，别一边在退避一边被监督器喊停滞。
3. **报错文案**："已自动重试 N 次"照旧带真实次数；总时长（含退避）一并报，用户好判断。

## 四、实施批次

### P0：预算与阶梯
- [ ] `kMaxRequestAttempts` 3→6，退避表换五档指数（带抖动、2 分钟封顶）
- [ ] 面板"重连 N/M"与 i18n 分母联动真常量
- [ ] fake clock 单测：5 次重试的等待序列断言；第 6 次尝试后收口；退避期间取消立即返回

### P1：监督与文案
- [ ] 退避等待中的健康态归 Recovering（不判 Suspect）用例
- [ ] 报错带重试次数与总时长；中英文案
- [ ] 假后端 5xx 序列：第 4 次尝试恢复成功的场景绿

## 五、验收

- 网络错/超时/5xx 各造一路：重试满 5 回才报死；任一回成功即恢复。
- 401 等确定性错误零重试（现有用例零回归）。
- 退避中途取消/墙钟到点：立即收口，不留挂起等待。
- 全量 ctest 零回归。
