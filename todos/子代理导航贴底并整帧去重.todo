# 子代理导航贴底并整帧去重

状态：待做（终端布局与重绘回归）

## 现场

当前 LubanCode 把子代理导航塞在输入框上方：

```text
主会话正文 / 流式输出
↑/↓ 选择 · Enter 查看 · x 停止/清除
● main  主会话
● Explore #1  学习 CLRS 算法笔记库  完成(...)
● Explore #2  学习 PCM 数学笔记库    完成(...)
────────────────────────────────────
> 键入并回车
────────────────────────────────────
确认模式 · model · cwd · branch · context
```

这层级放反了。导航贴着正文，瞧来像模型刚吐出的几行内容。输入框一重画，导航又向上长，挤进旧正文。

Claude Code 的层级更清楚：正文归正文，输入归输入，代理树另在输入区下方贴底。大意如下：

```text
主会话正文 / 流式输出
────────────────────────────────────
> 键入并回车
────────────────────────────────────
用量 / 模式 / 状态
↑/↓ 选择 · Enter 查看
● main
○ general-purpose  上下文压缩分层可回溯升级
○ general-purpose  agent_message 主代理传话工具
```

用户最新截图还露出另一桩实错。操作提示、`main`、代理列表一遍遍留在屏上；详情、正文、列表挤成一团：

```text
↑/↓ 选择 ...
● main
↑/↓ 选择 ...
● main
❯ ● Explore #1 ...
...
Enter 收起 ...
任务标题：...
[后台]
↑/↓ 选择 ...
❯ ● main
↑/↓ 选择 ...
❯ ● main
● Explore #1 ...
```

用户已分不清哪段属主会话，哪段属某只代理。这不是配色小事，也不是少一条横线。旧帧没有擦净，详情又混入正文，画面账已经乱了。

## 已查到的根子

### 1. 现行布局明写着“面板向上长”

`src/cli/agent_panel.cpp` 的 `LayoutAgentPanel()` 按下列优先级算行数：

```text
操作提示 > 至少一条条目 > 详情 > 更多条目
```

注释也写明“面板向上长，不把输入框顶出视口”。这不是偶然排错一行，乃是当前设计。

`src/cli/console_input.cpp` 的流式 footer 又把顺序写死成：

```text
正文
Working
代理面板
待发队列
输入框上横线
输入行
输入框下横线
状态栏
slash 提示
```

空闲与流式两条路都照此画。故而只改一处打印顺序不够。

### 2. 测试也把旧错当成契约

现有屏幕驱动明确断言：

- 代理行必须在 composer 上横线之上。
- 选中标记必须落在上横线之上。
- 流式 title 必须在输入框上方。
- 详情文本跟在代理列表后面展开。

代码若挪，测试必红。须一并改契约，不能为过旧测试又塞回去。

### 3. 面板高度变了，旧帧清账没跟稳

导航聚焦、打开详情、退出详情、任务增删、状态由运行转完成，都会改变面板行数。当前 footer 先按旧 `row/rows` 擦，再按正文锚点重画。空闲 composer 与流式 footer 又各走一条重画路。

截图所见重复行，至少说明有一条转换没有守住下列不变量：

```text
擦除范围 = 上一帧完整占用范围
新帧起点 = 唯一底栏锚点
提交一帧后，屏上只能有这一帧
```

不能靠多打一串换行遮过去。那只会把残骸推入终端历史，用户往上一翻，仍是一地旧账。

### 4. 详情没有自己的归属

当前 Enter 展开后，`任务标题`、prompt、来源等详情直接缀在列表后。列表本就挨着主正文，详情一开，三类信息便混在一起：

- 主代理正文；
- 子代理导航；
- 某只子代理详情。

仅凭缩进和颜色，分不开。

## Claude Code 实查

本节不是照截图猜。2026-08-15 查了三处：

- 官方《[Orchestrate teams of Claude Code sessions](https://code.claude.com/docs/en/agent-teams)》；
- 官方《[Interactive mode](https://code.claude.com/docs/en/interactive-mode)》；
- 官方仓库 issue《[Team mode: selecting teammate with Enter... #24245](https://github.com/anthropics/claude-code/issues/24245)》；
- 本机 Claude Code `2.1.220` 打包程序中可读的编译后组件树。

Claude Code 主程序没有完整公开源码。下文只记公开文档与本机已安装包里能直接核实的结构，不冒充上游源码考据。

### 1. 官方明说：panel 在 prompt input 下面

官方 agent teams 文档写得直白：lead terminal 把 teammates 列在 agent panel 里，panel 位于 prompt input 下方。

这便定了主次：

```text
conversation
prompt / status
agent panel
```

不是：

```text
conversation
agent panel
prompt / status
```

同页还定了交互：上下选择，Enter 打开所选代理 transcript 并可直接发消息，Escape 打断所选代理当前回合，`x` 停止所选代理。

### 2. 它把“查看谁”与“给谁发话”绑成同一状态

官方说明：Enter 进入某 teammate 后，文字发给该 teammate；内置命令仍归 lead。故而 Claude Code 不是在列表下面展开几行静态详情，而是切换当前会话视口。

官方 issue #24245 又露出按键路由底线：Enter 既切视图又误发输入框建议文字，被明确报作 bug。LubanCode 须把这条写死：

```text
Enter 被导航消费
-> 只切 viewed_agent_id / composer_target
-> 不顺手提交 composer 当前正文
```

视图、收件目标、输入框标签必须从同一枚 task id 推出来。三处各记一份，迟早串话。

### 3. 它不把所有闲置代理永远摊开

官方文档给出一套克制的收纳规矩：

- 仍有人工作时，闲置 teammate 行继续留着，方便回看 transcript 或再派活。
- 所有代理都闲置后，闲置行过 30 秒暂时隐藏；下次开工再出现。
- 同时闲置超过三只，只单列前三只，其余折成 `N idle agents`。
- 正在工作、失败、当前正在查看的代理始终单列，不能折进去。
- 汇总行可 Enter 展开，Esc 再收起。

这是“隐藏画面”，不是删任务。会话、transcript、task id 仍在。LubanCode 可照此收纳终态行，却不可借机清台账。

### 4. task checklist 与 agent view 分家

官方 interactive mode 文档特意提醒：`Ctrl+T` 的 task checklist 只是 Claude 的待办表，不是后台任务视图；运行中的 shell 与 subagent 另走 `/tasks`。

LubanCode 也要守住边界：

```text
待发队列       将要送出的用户消息
项目 TODO      主代理的工作计划
agent navigator 会话与执行者
```

三者可同屏，不可共用行模型、选择下标或状态文案。当前截图之所以难认，正因几层东西没有清楚分家。

### 5. 本机 `2.1.220` 的组件布局

本机包是 Bun 打成的 exe，并非可维护源码。不过其中编译后的 React/Ink 风格组件调用仍可读，能核实这些结构：

- prompt/status 组件先返回，agent list 组件紧跟其后；两者是兄弟区块，不把 agent 行混进 transcript。
- agent list 是纵向容器，顶部留一行呼吸，不拿横线和说明文字包成大框。
- 主行、任务行、闲置汇总行各有独立组件。
- “选中”与“正在查看”分成两种状态：选中用 pointer；正在查看另换实心标记并加粗。其余行降亮度。
- 任务行分四段：标记/树枝、身份、描述、右侧状态。
- 身份列定宽；描述列 `flexGrow`，吃剩余空间并 `truncate`；状态列设最小宽度，贴右，不随标题抖来抖去。
- 描述优先显示最新 progress summary，没有才显示任务说明。
- 右侧状态把耗时、token、queued count 用中点隔开；queued 用警示色。
- 名称列按当前数据算宽，最窄 4 列，最宽 28 列；不会让一只长名字吞掉全屏。
- 可见任务窗口上限为 5 行；选中项始终留在窗口里，顶部与底部用 `N more` 报数。
- 单列闲置代理上限为 3，余者变成一行汇总。
- 行支持 click 与 hover；键盘仍是主路。
- 操作提示随焦点变化。没选中时只说上下选择、Enter 查看；选中后才添 Enter、`x`、Esc 等动作。
- 宽度不足时藏次要快捷键；本机包在 90 列以上才把“停止全部”放进提示行。
- 活跃或等候过期的任务用约 1 秒粒度刷新耗时，不随每个 token 疯狂重画整块。

LubanCode 是 C++，不必为此引入 React 或 Ink。该学的是 retained UI 的账法：先从状态生成一棵完整底栏，再由 painter 提交。不要让十几个事件回调各拿光标补一笔。

### 6. Claude Code 也留了整屏自救键

官方 interactive mode 把 `Ctrl+L` 定为“重画整屏”，保留输入与会话历史。根修仍是帧账正确；此外应补一条同类自救路：

```text
Ctrl+L
-> 作废所有终端锚点
-> 清理可视区
-> 从 transcript 快照与 bottom chrome 快照重建整屏
-> 恢复 composer 正文、光标、选择与收件目标
```

这不是拿清屏遮 bug。它管终端 resize、外部程序乱写、SSH/ConPTY 丢控制序列等不可控场面。

## 目标

把子代理区域改成输入区下方的活动导航坞。它与 composer、状态栏合成一只完整底栏，由同一套帧账原地画、原地擦。

任何时刻，屏上只留一份导航提示、一行 `main`、每只单列代理各一行、至多一行闲置汇总。聚焦、查看、退回、完成、缩放，都不得留下旧帧。

真正短标题、统一任务台账、前后台来源、消息投递、停止/清除等现成功能照旧。本单不推倒那些功能，只收拾空间层级与重画边界。

## 固定布局

### 一、空闲态

从上到下只准这样排：

```text
[主会话正文与工具记录]

[待发队列，可空]
────────────────────────────────────
> composer
────────────────────────────────────
[模式 · 模型 · cwd · branch · context]
[↑/↓ 选择 · Enter 查看 · x 停止/清除 ...]
[● main  主会话]
[○/● 子代理行 1]
[○/● 子代理行 2]
```

要点：

- 代理导航必须在 composer 下横线与状态栏之后。
- `main` 与子代理同属一棵导航树，不得插进正文。
- 待发队列仍在 composer 上方。它是即将发送的内容，不属于代理导航。
- slash 补全、确认菜单等短命 UI 另按所有权处理，见下文。
- 没有任何子代理时，整块导航可不画；不得只剩一条孤零零的操作提示。

### 二、流式态

模型正在说话、思考或跑工具时，次序不变：

```text
[持续增长的正文 / 工具记录 / Working]
[待发队列，可空]
────────────────────────────────────
> composer（可排队下一条）
────────────────────────────────────
[模式 · 模型 · cwd · branch · context · 打断]
[代理导航提示]
[main 与代理树]
```

流式开始、工具开始、工具结束、模型收尾、转回空闲，导航坞都不得跳回输入框上方。

### 三、底部锚点

整只底栏贴住终端可视区底部。高度公式须写成一处纯逻辑：

```text
bottom_chrome_rows =
    queue_rows
  + composer_rows
  + status_rows
  + agent_dock_rows
  + transient_hint_rows
```

正文可视区截止于底栏上沿。导航增高时，压缩正文视口；不得把 composer 挤出屏，也不得靠终端自然滚屏把半只旧导航留进正文。

## 导航坞

### 一、只放导航，不堆长详情

紧凑坞只放：

- 一行按键提示；
- `main`；
- 代理短标题、状态、必要计数与耗时；
- 选中标记；
- 列表开窗提示。

完整 prompt、任务来源、长结果、工具明细不得缀在树后面向下铺。Enter 的“查看”要名副其实：

```text
选中 main       -> 主内容视口显示主会话
选中某只代理   -> 主内容视口显示该代理的会话/任务详情
Esc             -> 回主会话；导航坞仍在原位
```

若第一期尚不能切换完整会话，也须给详情一块有明确边界、固定上限的内容视口。它只能替换主内容区，不能插在导航坞、composer 或旧正文之间。

进入某只代理后：

- 选中行须一直亮着。
- composer 收件人切给该代理。
- 输入框边界可挂真正短标题，提示消息发给谁。
- 屏上须有清楚的“当前查看：main / 某代理”标识。
- 返回 main 后，收件人、标题与选中态一并复位。

### 二、谁是谁，须一眼认出

行内至少保住三件事：

```text
身份       Explore #2 / general-purpose / main
短标题     学习 PCM 数学笔记库
状态       运行中 / 排队 / 完成 / 失败 / 已停
```

不得再拿 prompt 前几十字冒充标题。此规矩已有旧 TODO 与实现，本单只要求贴底后继续守住。

照 Claude Code 的四段式排法收紧一行：

```text
❯ ●  Explore #2    学习 PCM 数学笔记库          26.5s · 6.5k tokens
│ ○  general       正在核查压缩边界              4m 16s · 1 queued
```

- 标记与树枝定宽。
- 身份列同屏对齐，设 4～28 显示列范围。
- 真正短标题或最新进度占中段，优先吃宽，也优先截断。
- 耗时、token、queued 靠右；同一帧内起点对齐。
- 选中用 pointer；正在查看再加粗或换实心标记。不能只靠颜色。
- 非选中行可降亮度，失败与 queued 仍保留警示色。

正文或详情切到子代理时，每一段输出都须有明确归属。不能把下列内容无标记地连在一起：

- 主代理答复；
- 子代理 prompt；
- 子代理工具输出；
- 子代理结论；
- 面板操作提示。

### 三、焦点与按键

沿用现有语义，不另造一套：

```text
↑/↓       composer 为空时进入/移动代理选择
Enter     查看选中会话；再按则照既定语义返回或收起
Esc       先退详情，再退导航焦点，最后才清输入或打断
x         运行中代理停止；终态代理清除
Ctrl+X K  两段确认停止全部运行中代理
```

正文非空时，上下键仍归编辑器与历史。导航贴到输入框下面后，不能因视觉上更近便抢走打字按键。

提示行也须随状态收放，别把所有快捷键一气塞满：

```text
未聚焦     ↑/↓ 选择 · Enter 查看
已选中     Enter 查看 · x 停止/清除 · Esc 返回
展开闲置   Enter 查看 · Esc 收起
宽屏且多只 Ctrl+X Ctrl+K 停止全部
```

短屏或窄屏先藏低频动作。动作仍可用，只是不必全写在一行。

### 四、闲置与终态收纳

尽量照 Claude Code：

- 活动、失败、queued、正在查看的行永不折叠。
- 闲置/完成行最多单列三只；更多行折成“另有 N 只闲置代理”。
- Enter 展开汇总，Esc 收起；展开态不改 task id 与消息目标。
- 尚有任何代理工作时，三只闲置行留在导航中。
- 全部闲置 30 秒后，闲置行可暂隐；用户打开代理导航、进入 `/tasks` 等入口或代理再开工时恢复。
- 隐藏不等于清除。`TaskDetail()`、transcript、消息地址与完成结果仍可查。

若 LubanCode 暂无“idle”与“completed”区别，第一期只做三行折叠与显式展开；不要拿一个模糊状态硬套 30 秒计时。

## 整帧重画

### 一、只认一份帧描述

空闲 composer 与流式 footer 不得各拼一套行序。抽出等价于下列结构的纯逻辑帧：

```text
BottomChromeFrame
  queue_rows
  composer_rows
  status_rows
  agent_dock_rows
  transient_rows
  total_rows
  selected_task_id
  revision
```

名字可另取，账只能一本。布局函数只产行与高度；终端 painter 只管锚点、擦除、落笔。

`LayoutAgentPanel()` 可改成 `LayoutAgentDock()`，也可保留函数名；但“向上长”这条假设必须删净。调用方决定底部次序，布局层不得暗自把导航塞回 composer 上方。

帧模型宜直接带结构化行，而非先拼 ANSI 字符串：

```text
AgentDockRow
  id / kind
  selected / viewed
  marker / tree_connector
  identity
  title_or_progress
  status_segments[]
```

先算身份列宽、状态列宽，再把余宽交给中段标题。这样任务状态每秒跳一次，标题起点也不会左右乱晃。

### 二、原子换帧

每次更新照一条路走：

```text
锁住 stdout
-> 取上一帧 row / rows / revision
-> 擦净上一帧完整矩形
-> 按当前终端尺寸生成新帧
-> 必要时一次滚屏，并同步所有正文/工具锚点
-> 从唯一锚点画完整新帧
-> 保存新 row / rows / revision
-> 恢复输入光标
-> 解锁
```

不得只覆写“估摸着变了的几行”。导航高度、详情模式或终端宽高一变，必须整块换帧。

启用同步输出时，一帧只能有一对 begin/end。中途报错或拿不到屏幕信息，也要收束同步输出，恢复光标，不得留半帧。

### 三、擦除须认旧高度

下列变化都要先按旧帧高度擦：

- 无焦点 -> 聚焦；
- 切换选中行；
- 列表 -> 查看态；
- 查看态 -> 列表；
- 代理新增、完成、失败、清除；
- 操作提示文案变化；
- 队列新增、编辑、送达；
- composer 单行变多行或反之；
- slash 提示出现或消失；
- 终端 resize；
- 流式与空闲互换。

验收不变量：新帧画完后，旧帧范围里凡不属新帧的格子，全为空白。不能残留第二份提示、第二行 `main` 或过期状态。

### 四、挂起与恢复

工具确认、`ask_user`、模型菜单、diff 预览等 UI 临时接管终端时：

```text
挂起 -> 擦掉整只 bottom chrome -> 临时 UI 独占 -> 退出 -> 重算并重画一帧
```

挂起期间只记 dirty/revision，不准后台线程偷偷画半只导航。恢复时也只画一次最新快照。

### 五、不要污染滚屏历史

底栏是活动界面，不是对话记录。重画时不得用换行把旧帧顶走。用户向上滚，只应看见真正的主会话、工具记录与已提交结果，不应看见十来份 `↑/↓ 选择` 和 `main`。

会话结束或终端降级到 pipe/plain 模式时，可打一份简短终态摘要；不能把每次动态帧都写进 stdout。

### 六、限制刷新频率

事件到来只更新状态与 revision。painter 可合并同一小段时间内的多次 dirty 通知：

- 文字输入仍须即时回显。
- 任务新增、失败、完成、queue 变化即时画。
- 耗时按 1 秒跳一次便够。
- token 流不必每个 delta 都重画 agent dock；按节流后的快照更新。

同一时刻只准一只 bottom chrome painter 在画。若重画请求撞在当前帧中，记 dirty，当前帧落稳后再画最新快照，不许递归套画。

### 七、整屏重画

新增 `Ctrl+L` 或等价入口。它须走“从状态重建”，不能调用一串普通增量回调假装重放：

- 保住 transcript 数据、composer 草稿、队列、导航焦点与 viewed task id。
- 作废正文、工具、footer 的所有物理锚点。
- 清可视区后一次生成正文与底栏。
- 重画后光标回 composer 原位置。
- pipe/plain 模式不吞用户输入，可退成常规换页或不处理。

## 窄屏与矮屏

导航坞设最大高度，并对列表开窗。优先级如下：

```text
composer 与状态栏
> 当前选中行
> main 或返回路径
> 操作提示
> 其余代理行
> 次要计数
```

要求：

- 选中行永不因开窗消失。
- 代理多时只滚列表窗口，不让整块无限长。
- 标题按显示列截，不能截坏 UTF-8；状态与次要数字可先缩。
- 极矮屏至少保住 composer、状态栏、选中对象与“还有 N 项”提示。
- resize 后重算整帧；旧宽度留下的尾字须擦净。
- 多行 composer 增高时，导航窗口相应收小；二者不得重叠。
- 常态最多单列 5 只代理；再多便开窗，仿 Claude Code 的密度。
- 闲置代理超过 3 只先汇总，不让“完成”行吃光 5 行窗口。
- 90 列以下可从提示中摘掉“停止全部”等低频长文案。

## 建议改动边界

重点查看：

- `src/cli/agent_panel.cpp/.hpp`
- `src/cli/console_input.cpp/.hpp`
- `src/cli/terminal_frame.cpp/.hpp`
- `src/cli/live_transcript.hpp`
- `src/cli/tool_display.hpp`
- 空闲 composer 与流式 footer 的屏幕驱动

现有 `StreamFooterPaintScope`、正文滚屏 hook、工具锚点和 panel session 可复用。先把所有 bottom chrome 入口收归一只 painter，再谈局部优化。若两条路继续各画各的，重复残帧迟早再来。

## 测试

### 一、纯布局

把旧断言翻过来：

```text
正文行
< composer 上横线
< composer 输入行
< composer 下横线
< 状态行
< 导航提示
< main
< 代理行
```

覆盖：

- 无代理时整坞消失；
- 一只与多只代理；
- 列表开窗始终含选中项；
- 中英文、宽字符、超长短标题；
- 终端变窄、变矮；
- composer 单行、多行；
- 队列有无；
- 运行、排队、完成、失败、已停。

### 二、空闲屏幕驱动

改写 `agent_panel_driver`。至少断言：

- 提示、`main`、代理行都在状态栏下方。
- 屏上每种导航行只出现一份。
- 连按上下键二十次，不多出第二份提示或 `main`。
- Enter、Esc 往返二十次，不留详情残骸。
- Enter 切视图时不得顺手提交 composer 里已有文字。
- x 清除一项后，余下行上移，旧行尾巴清空。
- resize 前后，旧宽度字符清空。
- 6 只闲置代理只列前三只与一行汇总；展开、收起无残帧。
- 活动、失败、queued、正在查看的代理不进闲置汇总。
- `Ctrl+L` 后状态、草稿、选择与收件目标不丢，重复行归零。

### 三、流式屏幕驱动

改写 `agent_stream_driver`。至少断言：

- 流式开始后，导航仍在状态栏下方。
- tool start/done、token 与耗时刷新一百次，导航不复制。
- 运行转完成只改原行，不把旧状态推进正文。
- 流式结束转空闲，整块位置不跳，屏上仍只一份。
- 队列消息增删、编辑、送达，不碰乱导航行。
- 查看某代理后，正文区与 composer 目标一致；返回 main 后一同复位。
- token 连续刷新时，agent dock 按节流频率重画，不随每个 delta 抖动。

### 四、残帧扫描

加一条专门封住本次截图：

```text
CountVisible("↑/↓ 选择") == 1
CountVisibleMainRows() == 1
CountVisibleAgentRow(task_id) <= 1
CountVisibleIdleSummaryRows() <= 1
NoDetailTextInsideDock()
NoDockTextAboveComposer()
```

空闲、流式、详情开合、任务完成、resize 后都跑一遍。不能只查“最后一次 FindRow 找到了”，那会放过前面堆着的旧副本。

### 五、手工验收

开三只名字相近的代理，依次做：

```text
上下切换
进入第二只
排一条消息
返回 main
让第一只完成
停止第三只
清掉一只终态代理
拉窄终端
拉矮终端
打开并退出确认菜单
等当前回合收尾
```

全程须看得出：正在看谁，消息发给谁，谁仍在跑。屏上不得再长出第二份导航。

## 不做

- 不重做 `agent` 工具 schema。
- 不重做真正短标题；更不准退回 prompt 前 34/40 字。
- 不重做前后台统一台账。
- 不借本单改 agent queue / inbox 的送达时机。
- 不照抄 Claude Code 的颜色、英文文案或每个快捷键。
- 不靠清屏、刷一串空行或隐藏历史掩盖残帧。
- 不把暂时隐藏的闲置代理从台账删掉；显隐与清除是两回事。
- 不照搬 Claude Code 打包程序里的代码；只借组件边界、密度、列宽和状态规矩。

## 验收线

以下条件一条不差，方可销单：

1. 空闲与流式画面里，代理导航都在 composer 与状态栏下方贴底。
2. 主正文、子代理详情、代理导航各有边界，不再混写。
3. 任意状态转换后，提示、`main`、每只单列代理与闲置汇总在可视区至多一份。
4. 导航高度变化不会留下旧行，不会污染终端滚屏历史。
5. Enter 查看时，主内容视口与 composer 收件目标指向同一会话。
6. Esc 返回 main 后，视口、标题、目标与选中态一同复位。
7. 窄屏、矮屏、多行输入、队列、resize 下不重叠、不丢 composer。
8. 空闲与流式屏幕驱动改成新层级，并加残帧计数回归。
9. 既有真正短标题、任务状态、停止/清除、消息投递能力不退步。
10. 常态最多五只单列代理；闲置超过三只会汇总，活动/失败/queued/正在查看的行不被藏。
11. 身份、中段标题、右侧状态三列稳定；耗时刷新时整行不左右乱跳。
12. `Ctrl+L` 能从状态重建干净画面，又不丢草稿、选择与目标。

## 与旧 TODO 的关系

《子代理面板漏接前台任务、流式导航与真正短标题》管“数据从哪来、标题是什么、两态能不能操作”。本单管“导航摆在哪、详情归哪里、每次重画会不会留下尸首”。

两单不可混销。前一单纵然做完，导航仍可能摆反、残帧仍可能堆屏；本单纵然做完，也不得借口布局收拾好了，便放弃真正短标题或统一台账。
