# 模型侧 worktree 工具与子代理隔离

状态：待做

## 一句话

让模型的代理自己会进 worktree：主代理调 `worktree enter` 便整场会话搬进隔离目录干活，子代理领任务时带一枚 `isolation: worktree` 便各住各的隔离房——不再手搓 `git worktree add` 加 `Set-Location` 那套把戏。

## Claude Code 怎么办的（官文实测细节）

依据 <https://code.claude.com/docs/en/worktrees>（v2.1.2xx 行为）：

**建房**

- 房一律开在仓库根的 `.claude/worktrees/<名字>/`，分支名 `worktree-<名字>`；名字缺省自动生成（如 `bright-running-fox`，形客-动-物三词）。
- 基准缺省不是本地 HEAD，是**远端默认分支**（`origin/HEAD`，"fresh"）：距上次 fetch 超 24 小时便补 fetch 一次（5 秒封顶，失败用本地缓存 ref），再不行回落本地 HEAD。可配 `worktree.baseRef: "head"` 改成从当前 HEAD 起（不认任意分支名）。
- 房里是全新 checkout，gitignore 的东西（`.env` 之类）不在。仓库根放一份 `.worktreeinclude`（gitignore 语法），建每间房时把**既匹配它又被 gitignore** 的文件拷进去；tracked 文件永不拷。
- 名字撞了已有房不报错，直接打开那间旧房。fresh 基准下若旧房干净、还在原分支、无自有提交（或 PR 已并入），重开时重置到默认分支；其余情形从旧 tip 继续。

**进房与会话**

- 起手 `--worktree <名字>`（或 `-w`）直接住房里开会话；会话中模型用 `EnterWorktree` 工具进房。
- 已在房里时可再 EnterWorktree 直达 `.claude/worktrees/` 下另一间，旧房原样留在盘上。
- **进自家目录之外的房，先问用户**——搬的是会话 cwd、写权限和项目配置（CLAUDE.md、settings）。权限规则、「别再问」都压不住这一问，只有 bypassPermissions 免。这是硬安全线。
- 会话转录跟着 cwd 走（同 `/cd`），`--resume`、`/desktop` 按新目录能找到它。

**出房与清理**

- `ExitWorktree`：`keep` 留房，`remove` 删房。交互退出时查房：干净且无名会话自动删；干净但具名，先问；有活（改动/未跟踪/新提交），问留还是删。
- 管道/`-p` 模式无退出询问，不清理，留给 `git worktree remove`。
- Windows 删房不越界：房里若嵌着 NTFS junction/目录符号链接，只删链接本身，不追进指向的目录。

**隔离的四道闸（进了房，还得拦住跑出来的）**

隔离对会话与其派生的**所有子代理**一体生效，交互/后台皆然：

1. 文件编辑闸：`Edit`/`Write`/`NotebookEdit` 目标路径落主 checkout 的，拦。
2. 命令工作目录闸：命令 cwd 解析回主 checkout 的，拦；解析不出、无法证明在房外的，也拦。
3. git 改道闸：`git -C`、`--git-dir`、`GIT_DIR`/`GIT_WORK_TREE` 环境变量、`cd` 进主 checkout 再跑 git——凡是把 git 指回主 checkout 的改道，拦。
4. 命令形状闸：静态追不住"必在房内"的 shell 构造（花括展开、未引 heredoc 等）拦，让模型拆成平直命令。此闸关不掉。

每次拦截都以工具错误回给模型，报上房名与正确出路。PowerShell 只走第 2 道闸。

**子代理隔离**

- 自定义子代理 frontmatter 写 `isolation: worktree`，或当场嘱咐"给你的代理用房"；spawn 时宿主自动建房，基准与 `--worktree` 同源（`worktree.baseRef` 也管它）。
- 代理干完**没改动**：房自动删。**有改动**：房留在盘上，交给定期清扫。
- 定期清扫按 `cleanupPeriodDays` 回收子代理/后台会话的陈房；有活（改动/未跟踪/未推提交）的房跳过永不删；用户 `--worktree` 手起的房永不碰。
- 代理跑着时对其房上 `git worktree lock`，防并发清扫误删；代理收工解锁。被杀会话留下的锁，清扫时一并释放（用户手上的锁永不放）。

**恢复**

- resume 把会话送回原房（交互、`-p --continue/--resume`、SDK 皆然）。回房前先验房：读 git 元数据确认它仍是独立 checkout、不是解析回主 checkout 的马甲（`.git` 文件指向主仓、`core.worktree` 改道之类都拒），拒了报 `Refusing to use <path>` 并列出救法。
- 房没了：回落到启动目录，告知一声，解除会话与房的绑定。
- `--fork-session` 的分叉在启动目录开新篇，不动原会话的房。

**别的**

- 非 git 版本库（SVN/Perforce…）用 `WorktreeCreate`/`WorktreeRemove` 钩子整体替换建房逻辑。
- `.claude`、`.claude/worktrees`、房目录本身若是符号链接，拒绝建房并指名路径。
- 项目级插件、"别再问"权限规则存主 checkout、全仓共享——房里批准的规则落主 checkout 的 settings。

## LubanCode 眼下有什么

- `src/cli/worktree.hpp/.cpp` 的 `WorktreeSession`：`Create`（`git worktree add -b worktree/<名字>` 到 `<repo>/.lubancode/worktrees/<名字>`，随后 `current_path(target)` 一把 chdir）、`List`、`Exit(keep|remove)`、脏树删除要确认。这半边是真的，但只挂在用户 slash 命令 `/worktree` 上（`src/main.cpp` 一处）。
- 模型工具表里没有 worktree。模型要隔离只能自己起 shell 敲 git——路径、分支、清理全凭自觉，状态行也不知道自己"在房里"。
- 子代理（`src/tools/agent_tool.cpp`）是**进程内 std::thread**：独立工具表加嵌套 `AgentLoop`，同住一个进程。
- 工具层没有 per-call cwd：`run_command`、`write_file`、`search` 全认 `std::filesystem::current_path()`——全进程一份。

## 难点在哪（先认清楚再动手）

**chdir 是进程级的。** 主代理 enter 时 chdir 没毛病（整场会话一起搬，跟用户敲 `/worktree` 一个语义）。可子代理是同进程的线程——它一 chdir，主会话的读写、用户正排队的活全被带进沟里。多个后台子代理并行时更互相踩脚。

所以子代理隔离**不能走 chdir**，得走参数与包装：子代理的工具调用全部包一层，相对路径 resolve 到本代理的 base_dir，`run_command` 子进程的工作目录设为 base_dir。不动各工具内部实现，在 agent_tool 建子代理工具表时套装饰层。

再照 Claude Code 补一层**闸**：光"搬进去"不叫隔离，还得拦住写主树、命令绕回主树、git 改道回主树这几条歪路（见下面"隔离的闸"，首版取三道，"命令形状闸"暂缓——LubanCode 的命令安全层已有静态检查底子，往上贴）。

## 怎么做

### 模型工具

给模型一件 `worktree` 工具（确认档与 `/worktree` 同级）：

```text
worktree enter [名字] [base]   建房或进已有房,整场会话搬进去,状态行亮房名
worktree status                在不在房里、路径、分支、基准、脏没脏
worktree list                  列所有工作树(带最后修改时间,提示清陈房)
worktree exit keep             搬回原处,房留着
worktree exit remove           搬回原处,干净才删;脏了回"需确认",yolo 也不豁免
```

- 名字缺省自动生成；`base` 缺省 `fresh`（远端默认分支，fetch 5 秒封顶失败回落本地 HEAD——照 Claude Code 的取法），可写 `head`。
- 进 `<repo>/.lubancode/worktrees/` 之外的已有房，必须用户确认，确认档压不住这一问（对齐 Claude Code 硬安全线）。
- 名字撞旧房：直接开旧房（fresh 且旧房干净无自有提交则重置，否则旧 tip 继续）。
- 放 `src/tools/worktree_tool.cpp` 薄壳，翻译参数、调 cli 层 `WorktreeSession`、结果摆成模型能读的文本。
- 与用户 `/worktree` **共用同一个 `WorktreeSession` 实例**，互斥：一边 active 另一边回 AlreadyActive，不许各建各的账。系统提示补一段：大改动先 enter、干完 exit、别把 build 产物带进房。

### 子代理隔离

`agent` 工具入参加 `isolation`（缺省 `none`）：

```json
{"agent_type": "general-purpose", "prompt": "……", "isolation": "worktree"}
```

- spawn 时宿主建房（`.lubancode/worktrees/agent-<短id>`，基准与主代理同规），子代理 base_dir 即房路径；子代理的工具表套包装层（路径 resolve + 子进程 cwd）。
- 代理跑着时 `git worktree lock`，收工解锁；被杀会话的锁由清扫释放。
- 干完没改动：房与分支自动删。有改动：留着，结果文本附房路径与分支名，主代理或用户后续去收。
- 陈房定期清扫（对齐 cleanup 周期思路，LubanCode 侧可挂会话启动时扫一遍）：有活跳过，只清自家命名规约（`agent-` 前缀）的房，用户手起的永不碰。
- 子代理再嵌子代理：允许房中房，从当前 HEAD 起。
- 后台子代理（`run_in_background`）同样适用；结果回流那条路（冻死修复）不受影响。

### 隔离的闸（首版三道）

对住在房里的会话及其全部子代理生效：

1. **文件闸**：`write_file`/`edit_file` 目标 resolve 后落主 checkout 的，拦，工具错误报房名。
2. **cwd 闸**：`run_command` 的工作目录 resolve 回主 checkout 的，拦。
3. **git 改道闸**：命令串里 `git -C <主树>`、`--git-dir`、`GIT_DIR`/`GIT_WORK_TREE`、`cd <主树>` 后接 git 的组合，拦（静态识别，识别不出的组合按第 2 道 cwd 闸兜底）。

"命令形状闸"（无法静态证明在房内的构造一律拦）首版不做，先在命令安全层留钩子。

### 房的配置件

- `.worktreeinclude`（gitignore 语法，仓库根）：建每间房时把匹配且被 gitignore 的文件拷进去。LubanCode 自己的集成测试就有真钥匙环境变量的坑，这个件对 dogfood 尤其实用。
- 状态行在房里时亮 `WT <名字>`（纯文本，不靠特殊字形）。

### 会话与恢复

- 会话档 manifest 记 `cwd` 与房的绑定。`/resume` 时房还在且验明正身（读 git 元数据确认是独立 checkout，防 `.git` 指回主仓的马甲）则回搬；验不过报原因不硬进；房没了回落启动目录、解除绑定、说一声。
- Windows 删房只删房内：嵌着的 junction/目录符号链接只删链接不追删——用 `std::filesystem::remove_all` 前先验 `is_symlink`/reparse point，逐项下刀。

## 代码落点

- `src/tools/worktree_tool.hpp/.cpp`：模型工具薄壳。
- `src/cli/worktree.hpp/.cpp`：补"进已有房""fresh/head 基准""房是否干净/验明正身""lock/unlock"几枚；chdir 语义只归主代理路径。
- `src/tools/agent_tool.cpp`：`isolation` 参数、建房、base_dir 包装层、收工清理、lock。
- `src/tools/command_safety.cpp`：三道闸挂进现有静态检查。
- `src/agent/session_store.*`：manifest 记 cwd。
- `src/main.cpp`：工具注册、状态行房名、resume 回搬、陈房清扫，接线克制。
- `src/cli/i18n.cpp`：文案中英成对。

## 分两步做

### 第一步：模型侧 worktree 工具

工具薄壳、主代理 enter/exit、与 `/worktree` 共账户互斥、状态行房名、三道闸。不动子代理，主代理立刻能正经住进房里。

### 第二步：子代理 isolation 与房务

`agent` 工具加参数、base_dir 包装层、lock、收工清理、陈房清扫、resume 带 cwd 回搬。这步碰 agent_tool 与工具执行链，是难点，单独验。

## 验收

- 主代理 `worktree enter` 后：读写落在房里，`run_command` 在房里跑，状态行亮房名，分支是房的分支。
- 住房期间：写主 checkout 的 `edit_file` 被拦且回错误说明；`run_command` cwd 指主树被拦；`git -C <主树> …` 被拦。
- `exit keep` 房还在；`exit remove` 干净即删，脏则要确认，拒绝后房还在；删房不追删 junction 指向的目录。
- 进 `.lubancode/worktrees/` 之外的房：用户确认后才进，确认档压不住。
- 名字撞旧房：开旧房；fresh 且干净无自有提交时重置到默认分支。
- `.worktreeinclude` 列的 gitignore 文件进了房，tracked 文件没被拷。
- 模型在房里干活时用户 `/worktree list` 看得见同一间；反之亦然。
- 子代理带 `isolation: worktree`：`write_file` 写进房，主会话 cwd 分毫不动；多个隔离子代理并行互不踩脚；跑着时房上着锁。
- 隔离子代理干完：干净则房与分支已清；有改动则结果带房路径。
- `/resume` 恢复住房会话：房在且验明正身则回搬；`.git` 指回主仓的马甲房拒进并报原因；房没了回落并提示。
- 会话存档、后台任务台账、跨会话传话、结果回流不受牵连。

## 首版不做

- 不按 PR 号起房（`#123` 拉取 pull head 那套）。
- 不做非 git 版本库的 WorktreeCreate/Remove 钩子。
- 不做"命令形状闸"（先在命令安全层留钩子）。
- 不做 `--worktree` 起手参数（用户已有 `/worktree`，够了）。
- 不在 yolo 下自动删脏房。
