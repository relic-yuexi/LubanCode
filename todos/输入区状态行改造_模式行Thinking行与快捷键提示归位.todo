# 输入区状态行改造：模式行、thinking 行与快捷键提示归位

- 状态：待实现（2026-09-01 主人立单）
- 日期：2026-09-01
- 级别：P2（终端体验；改动面小但布局与配色要钉死）
- 范围：`src/cli/` 的输入区周边状态行渲染（footer/spinner/模式提示相关件，开工先定位现渲染点）、i18n 词条、主题配色
- 依赖：既有 keymap/permission_mode/spinner 渲染件
- 关联：无
- 不在本单：改键位行为本身；改 spinner 动画逻辑；改消息流渲染

## 一、先下断语（主人 2026-09-01 原话定案）

提示信息各归各位，三层从下往上：

```text
┌────────────────────────────────────────────────┐
│ thinking …（esc to cancel，x m xs）   ? for shortcuts │ ← 第②层(thinking 行)
│ yolo shift+tab to auto mode            xx skills │ ← 第①层(模式行,紧贴输入框上方)
│ ┌──────────────────────────────────────────┐ │
│ │  输入框                                    │ │
│ └──────────────────────────────────────────┘ │
└────────────────────────────────────────────────┘
```

1. **`? for shortcuts` 放输入框右上角**——最终落位在第②层（thinking 行）的最右端（主人第三句细化了第一句）。
2. **`shift tab to xx mode` 放输入框正上方**（第①层），同行**最右边**放 `xx skills`（当前可用技能数）。
3. 样例：`yolo shift+tab to auto mode`——**`yolo` 一词用红色标出**（危险模式的视觉警示；非 yolo 档不红）。

## 二、当前真相（开工先核）

现布局里 `? for shortcuts`、模式切换提示、thinking/spinner 行各在哪渲染（footer？divider？spinner_backend？）、各自何时显示/隐藏——先照相再动刀，逐件列现状。注意：thinking 行只在模型输出思考中显示，第①层模式行常驻；两层同时在场时按上图叠放，thinking 不在场时第②层整个让位（`? for shortcuts` 跟着 thinking 行走——开工时向主人确认这一点：**thinking 不在时 `? for shortcuts` 是隐藏还是回落到第①层最右**，默认按"跟随 thinking 行同显隐"实现）。

## 三、清单

- [x] 定位现状渲染点并照账（每行谁画的、何时显隐）。
- [x] 第①层模式行：左端 `<mode> shift+tab to <next> mode`（yolo 档 `yolo` 红色，其余档用主题默认），右端 `<N> skills`（N=当前会话可用技能数，来源与 /skills 同账）。
- [x] 第②层 thinking 行：左端 `thinking …（esc to cancel，x m xs）`（沿用现有 spinner/thinking 文案），右端 `? for shortcuts`。
- [x] 显隐规矩：thinking 行随思考状态同显隐（含上面那条待确认项的默认案）；模式行常驻输入区上方。
- [x] 中英文词条成对；窄终端折行不串行（按屏宽收缩，右端信息优先保）。
- [x] 渲染对照截图/文本快照进测试（既有终端帧测试路数）。
验收：三层归位如上图；yolo 红色可辨；窄屏不破行。

## 四、不做什么

不改键位语义；不动消息流/工具输出渲染；不做可配置布局（先钉死一套）。

## 五、最终验收门

ctest 零红；帧快照对照过；真机终端目验（含 yolo 档红字、thinking 在场/不在场两幕、窄窗一幕）。
