# 内嵌浏览器调试工作台：BrowserRuntime 与 Package 边界

- 状态：阶段 0-3、5 已落地（拆三层、两本 journal、协议 1.1 + sidecar、官方包）；阶段 4 翻案归 `todos/多前端外壳与浏览器Tab_AppServer承载设计.todo`
- 日期：2026-08-29
- 目标：让 LubanCode 像桌面 Codex 一样，能在宿主界面里打开网页、操作页面、查看截图、Console 与 Network，并把同一份观察送给用户和模型
- 相关现状：`browser/server.js`、`src/mcp/rich_result.*`、`src/agent/tool_result_images.*`、`src/app_server/`、`examples/packages/browser-agent/`
- 相关设计：`todos/统一Package封装与组件挂载系统设计.todo`、`todos/自定义Agent与PromptProfile设计.todo`

## 一、先下断语

这套能力**不是一只 Package**。

Package 是箱子。它管发现、校验、记账、挂载、卸载。它可以装 Agent、Prompt Profile、Skill、Workflow、Plugin 与 MCP。它不该持有浏览器窗口，不该画界面，也不该接管宿主生命周期。

内嵌浏览器调试须分两层：

```text
核心产品层
  BrowserRuntime + App Server 协议 + Desktop Browser Tab

可分发内容层
  Package: Agent + Skill + Workflow + Prompt Profile + 可选兼容适配器
```

一句话：**Runtime 是本事，Package 是装法。**

推荐终局：

```text
LubanCode Desktop
├── Chat / Plan / Diff / Agent
├── Browser Tab
│   ├── Page
│   ├── DOM
│   ├── Console
│   ├── Network
│   └── Screenshot
└── app-server
    └── BrowserRuntime
        ├── BrowserSession
        ├── Page/Tab registry
        ├── Profile + downloads
        ├── Console/Network journal
        ├── Screenshot/artifact bridge
        └── MCP compatibility adapter

Official Browser Package
├── agents/browser-reviewer.yaml
├── skills/browser-debugging/SKILL.md
├── workflows/web-smoke-test/workflow.yaml
├── prompts/profiles/browser-reviewer/
└── assets/practice-page/
```

## 二、为何不能只做 Package

现行 Package 合同写得明白：

- Package 不参与执行，不授予权限。
- Agent、Skill、Workflow、Plugin、MCP 各管各的。
- Package 只是外层安装与分发单位。

现行实现又比合同少一截：

- `/package list/show/doctor` 已有。
- `src/app/commands/package_commands.*` 明写“阶段 1 只读”。
- Package 眼下不挂 Agent、Skill、Workflow、Plugin 或 MCP。
- `trust/enable/disable/reload` 尚未落完。
- `PackageMountPlan` 与 code-bearing 事务挂载尚未落地。

因此，即便今天做一只“浏览器全家桶 Package”，它也只能被盘点，不能把 BrowserRuntime 与界面装进产品。

更要紧的一层：Package 将来能启动 MCP，也仍画不出宿主 Browser Tab。页面嵌入、焦点、窗口生命周期、截图卡片、Console 面板、Network 面板，都归前端宿主。

## 三、各概念各管什么

| 名称 | 该管什么 | 不该管什么 |
|---|---|---|
| BrowserRuntime | 浏览器进程、Context、页签、状态、动作、事件、截图、下载、崩溃恢复 | 不写领域操作教程 |
| Desktop Browser Tab | 页面显示、地址栏、标签、视口、Console/Network UI、截图预览 | 不自己重写模型与权限逻辑 |
| App Server | 前端与 Runtime 的方法、事件、审批、取消、断线补账 | 不保存浏览器业务规则 |
| Package | 安装、版本、来源、内容哈希、组件组合 | 不持有窗口，不直接授权 |
| Agent | 谁来测、模型与工具收窄、预装哪些 Skill | 不实现浏览器引擎 |
| Skill | 教模型“快照→动作→复验”、停手线与排错 | 不保存页签与 Cookie |
| Workflow | 排烟测、视觉回归、Console/Network 检查步骤 | 不接管 Playwright |
| Plugin | 补单次工具或本地转换器 | 不适合持有跨回合浏览器状态 |
| MCP | 给第三方客户端暴露 BrowserRuntime，或兼容现有工具协议 | 不该成为内嵌界面的唯一入口 |

## 四、眼下已有的底子

### 4.1 Browser MCP

`browser/server.js` 已有十二件工具：

```text
browser_status
browser_open
browser_snapshot
browser_click
browser_type
browser_select
browser_wait
browser_tabs
browser_select_tab
browser_close_page
browser_screenshot
browser_downloads
```

已有能力：

- Chromium 与 WebKit。
- headed 与 headless。
- persistent 与 ephemeral profile。
- 专属 user-data 目录，不碰日常 Chrome/Edge profile。
- page id、generation、snapshot id、stale ref。
- 多页签、下载账、SHA-256。
- 取消、超时、崩溃终态与 profile 进程锁。
- 视口截图、整页截图、元素截图。

2026-08-29 真跑：

```text
npm test
PASS=104 FAIL=0 SKIP=0
```

### 4.2 图片富结果

MCP screenshot 已不再只回路径：

```text
MCP image block
-> MIME 与魔数复核
-> 会话 mcp-artifacts 落盘
-> history 留 ArtifactRef
-> 下一轮请求副本重灌 base64
-> provider 原生 image block
```

这条链已有大小帽、边长帽、请求总帽、伪 MIME 拒绝、内容寻址和测试。

### 4.3 App Server

`app-server` 已有：

- thread、turn、workflow、trace。
- 工具条目、diff、usage、context 事件。
- permission/request 与 user/ask 反向请求。
- 用户图片输入。
- stdin/stdout JSON 协议与断线收口。

它还没有 browser 方法、browser 事件，也没有把工具富图片作为可展示条目完整送给前端。

### 4.4 Browser Agent Package

`examples/packages/browser-agent/` 眼下是一只 content-only Package：

- 一枚 Browser Skill。
- 一张练习页。
- 不带浏览器本体。
- README 叫用户另配 `browser/server.js` MCP。

这只包边界是对的。它可继续留作“模型操作章法”。别拿它冒充 BrowserRuntime。

## 五、可选方案

### 方案 A：只做 Browser Package

做法：把 `browser/server.js`、Skill、Agent、Workflow 与练习页全塞进一只 code-bearing Package。

好处：

- 分发方便。
- 复用现有 MCP，改动少。
- 第三方 LubanCode CLI 也能装。

坏处：

- 当前 Package 尚不能挂载代码组件。
- 即便将来能挂，也没有内嵌页面。
- Console、Network、截图卡片仍无处显示。
- 浏览器生命周期仍由外部 MCP 持有，桌面宿主只看见工具调用。

结论：可作发行方式，不能作产品架构。**不选作主方案。**

### 方案 B：继续 MCP 为主，桌面前端给它套一层面板

做法：桌面端调用现有 Browser MCP；MCP 返回截图、DOM 与事件，前端画面板。

好处：

- 最快复用现有 104 项测试。
- CLI、第三方客户端、桌面端共用同一工具面。
- 可先做出截图、DOM、Console 文本面板。

坏处：

- MCP 是工具协议，不是高频 UI 状态协议。
- 页面每次滚动、焦点、加载事件都经模型工具口，绕远。
- 前端与 MCP 谁持有当前页、活动页、用户点击，容易双账。
- 大量 Console/Network 事件会挤 stdio 工具通道。
- 用户直接操作页面时，模型侧 ref 与 generation 要频繁作废。

结论：适合过渡，不适合终局。

### 方案 C：核心 BrowserRuntime + MCP 兼容适配器

做法：抽出宿主拥有的 BrowserRuntime。桌面端与 Agent 都调它。现有 MCP 变成一只薄适配器，给 CLI 与第三方客户端用。

好处：

- 状态只有一本账。
- 桌面页面、模型动作、截图、Console、Network 共用同一页签。
- App Server 可发高频事件，不拿工具协议硬扛 UI。
- 现有 Playwright、profile、ref、下载、取消、崩溃恢复都能保住。
- MCP 不删，兼容入口仍在。

坏处：

- 要拆 `browser/server.js` 的 Session 与 MCP transport。
- App Server 协议须升版。
- 前端须处理用户与 Agent 抢焦点、抢导航、抢输入。

结论：**推荐主方案。**

### 方案 D：桌面宿主直接持有浏览器，完全仿 browser-client

做法：浏览器运行时住进桌面应用。前端持有 tab/web contents，后端只发结构动作或通用脚本请求。

可选宿主：

- Electron `WebContentsView` / CDP：Node 生态近，跨平台快，包体较重。
- Windows WebView2 + CDP：Windows 贴身，C++ 好接；跨平台另做。
- CEF：控制深，工程与升级代价大。
- 独立 Playwright Chromium + 前端镜像：最快见效，却不算真正内嵌。

好处：

- 页面就在用户眼前。
- Console、Network、DOM、截图都从同一宿主取。
- 最像 Codex。

坏处：

- 先得有 LubanCode Desktop 前端。
- UI 与浏览器升级、安全补丁、沙箱都成长期责任。
- 终端版仍需 BrowserRuntime 的远程/无界面入口。

结论：是方案 C 的桌面落法，不应另起一套业务协议。

### 方案 E：先做“可见调试”，暂不真嵌入

做法：BrowserRuntime 仍开 headed Playwright 窗口；LubanCode 前端只显示页签账、截图、DOM、Console、Network。

好处：

- 不先啃 WebView 嵌入。
- 一两期便能让用户看见浏览器在动。
- Console、Network 与截图证据能先闭环。

坏处：

- 浏览器另开一扇窗。
- 页面与聊天不能同屏贴合。
- 用户操作与 Agent 操作仍要仲裁。

结论：**推荐 MVP。** 做完再换真正内嵌视图。

## 六、推荐路线

推荐走：

```text
E（可见调试 MVP）
-> C（核心 BrowserRuntime）
-> D（桌面内嵌浏览器）
-> Package 装配 Agent/Skill/Workflow
```

不能反过来先做 Package。箱子还没装上挂载器，先雕箱花无益。

## 七、核心架构

### 7.1 BrowserRuntime 接口

先立中立接口，不把 MCP JSON、Playwright Node 对象或前端 DOM 类型漏进来：

```text
BrowserRuntime
  Start(profile, engine, headed, viewport)
  Stop(session_id)
  Status(session_id)
  Open(url)
  Navigate(page_id, url)
  Back(page_id)
  Forward(page_id)
  Reload(page_id)
  ListPages()
  SelectPage(page_id)
  ClosePage(page_id)
  Snapshot(page_id, options)
  Screenshot(page_id, options)
  Click(page_id, ref|point)
  Type(page_id, ref, text)
  Select(page_id, ref, value)
  Scroll(page_id, delta)
  Press(page_id, keys)
  ConsoleQuery(page_id, cursor)
  NetworkQuery(page_id, cursor, filters)
  DownloadsQuery(session_id)
```

首版可不急着把 Node 改写成 C++。先让核心 Runtime owner 起一只 Node sidecar，把现有 BrowserSession 从 MCP handler 中剥出来。MCP 与 App Server 两只 adapter 共用它。

### 7.2 状态真值

Runtime 持有：

```text
browser_session_id
engine / headed / profile
page_id / generation / active
snapshot_id / ref map
viewport
console_seq
network_seq
download_seq
crash_epoch
```

前端不另造活动页。MCP adapter 也不另造。

### 7.3 用户与 Agent 仲裁

页面同时给用户和 Agent 操作，必须立规矩：

- 用户按住页面输入时，Agent 写动作排队。
- Agent 即将点、输、导航时，前端亮出动作。
- 登录、支付、发送、删除仍过审批。
- 用户一动 DOM，generation 或 observation epoch 递增；旧 ref 明报 stale。
- screenshot、DOM、Console、Network 都带 page id 与 generation。
- 用户可一键暂停 Agent 浏览器动作，不停整个模型回合。

## 八、App Server 协议

协议版本须从 `1.0` 升档。浏览器不是临时字段，不能悄悄塞进旧 item。

建议方法：

```text
browser/start
browser/stop
browser/status
browser/page/open
browser/page/list
browser/page/select
browser/page/close
browser/page/navigate
browser/page/back
browser/page/forward
browser/page/reload
browser/snapshot
browser/screenshot
browser/action
browser/console/query
browser/network/query
browser/downloads/query
```

建议事件：

```text
browser/started
browser/stopped
browser/crashed
browser/page/created
browser/page/updated
browser/page/closed
browser/navigation
browser/console/event
browser/network/event
browser/download/event
browser/screenshot/ready
browser/action/started
browser/action/completed
```

事件全带 `seq`。Console、Network 可断线补账。截图正文不走 JSON base64 洪水，只发 artifact id、MIME、尺寸、字节、SHA-256 与可读取句柄。

App Server 还须把工具富结果图片送给前端。模型看见的图与用户点开的图须指向同一 artifact，不许各截一张，各说各话。

## 九、桌面界面

### 9.1 Browser Tab

最低控件：

- 地址栏。
- 后退、前进、刷新、停止。
- 页签条。
- 桌面/平板/手机视口切换。
- 适配窗口、100%、放大、缩小。
- 截图。
- DOM、Console、Network 三个调试页。
- “Agent 正在操作”状态与暂停按钮。

不要把它做成一张装饰卡。页面须吃满工作区。聊天与浏览器可左右或上下分栏。

### 9.2 截图卡片

每次 `browser_screenshot`：

- 用户能在工具条目里看缩略图。
- 点开看原图。
- 显示 viewport、page id、generation、时间、SHA-256 短码。
- 模型拿同一 artifact 的图块。
- 若模型不支持图片，界面仍给用户看，并在模型侧明报“只收到文本投影”。

### 9.3 Console

至少收：

- log、info、warn、error。
- 未捕获异常。
- source URL、行、列。
- 时间与 page id。
- 清空前后分 epoch。

默认遮掉疑似 token、Authorization、Cookie 与长 payload。

### 9.4 Network

至少收：

- method、URL、status、type、duration、size。
- request/response headers 的脱敏视图。
- failed request 与错误原因。
- XHR/fetch 的小体积文本 body，超帽落 artifact。

不得默认把 Authorization、Cookie、Set-Cookie、请求体密码送给模型。

## 十、Package 应当装什么

BrowserRuntime 入核心后，再做官方 Package：

```text
examples/packages/browser-debug/
├── package.yaml
├── agents/
│   ├── browser-reviewer.yaml
│   └── visual-regression-reviewer.yaml
├── skills/
│   ├── browser-debugging/SKILL.md
│   └── accessibility-review/SKILL.md
├── workflows/
│   ├── smoke-test/workflow.yaml
│   └── responsive-check/workflow.yaml
├── prompts/profiles/
│   └── browser-reviewer/
└── assets/
    └── practice-page/
```

包内不复制 BrowserRuntime，不另起浏览器进程。Agent 通过 `requires.tools` 点名核心 Browser 工具。

另放一件可选 MCP adapter，只用于：

- 终端版没有 Desktop 时。
- 第三方 MCP 客户端。
- 旧配置兼容。
- 远程无界面运行。

这件 adapter 若进 Package，算 code-bearing，照 Package 信任门走。核心 BrowserRuntime 本身随 LubanCode 发布，不吃项目 Package 信任。

## 十一、安全账

- 永不接用户日常 Chrome/Edge profile。
- persistent profile 按项目或明确命名隔离。
- profile、下载、artifact 都有 owner 与生命周期。
- 密码框不回显，不进 DOM snapshot，不进日志。
- Console 与 Network 默认脱敏。
- 下载物不自动执行，不自动解压。
- `file://`、`javascript:` 等协议默认拒绝。
- localhost 与公网分别记权限。
- 页面弹窗、文件上传、摄像头、麦克风、地理位置另过审批。
- 用户与 Agent 同时操作时，所有写动作带 owner。
- 崩溃后旧 page id、ref、screenshot observation 全作废。
- screenshot 进入模型前继续走 MIME、魔数、大小、边长与总请求帽。

## 十二、实现次序

### 阶段 0：冻结边界

- [x] 写 `docs/reference/browser-runtime.md`。
- [x] 定下 BrowserRuntime、Package、MCP adapter、Desktop Tab 的职责。
- [x] 冻结 session/page/generation/snapshot/ref/seq 术语。
- [x] 定下用户与 Agent 操作仲裁。
- [x] 把 `examples/packages/browser-agent` 标成 content-only，不再暗示它承载浏览器本体。

验收：实现者不看本 TODO，也不会把 Runtime 塞回 Package 或 MCP handler。

### 阶段 1：从 MCP 中抽 BrowserSession

- [x] 把 `browser/server.js` 分成 session、transport、tool adapter。
- [x] Playwright 生命周期只归 session。
- [x] MCP adapter 不保存第二份 page registry。
- [x] 保住现有 104 项测试。（拆后 104 项零回归，加直调 130 项两遍皆绿。）
- [x] 增加 Runtime 直调测试，不经 MCP 也能 open/snapshot/click/screenshot。

验收：同一 Runtime 可被 MCP adapter 与测试宿主调用，结果一致。

### 阶段 2：可见调试 MVP

- [x] 默认可选 headed 浏览器。
- [x] Console journal。
- [x] Network journal。
- [x] screenshot artifact 进入 App Server 事件。
- [x] 前端先画页签账、截图、DOM、Console、Network。
- [x] Agent 动作在前端逐项亮出。

验收：浏览器虽另开窗，用户已能在 LubanCode 面板里看证据和调试账。

### 阶段 3：App Server Browser 协议

- [x] 协议升版。（1.0 -> 1.1，additive；版本账记在 protocol.hpp 与 app-server README）
- [x] browser 方法与事件落 `protocol.hpp`、schema、dispatcher。（方法 18 枚 + 事件 13 族；真 Runtime 在 `browser/sidecar.js` 的 Node sidecar，C++ 侧 `src/app_server/browser_service.*` 只转发）
- [x] 高频事件有界、可合并、可丢项明报。（journal 在 sidecar 源头批量：单批帽 200、40ms 一冲、在飞帽 64 丢老明记；出站队列兜底有界 + 同页批合并 entries 拼接 dropped 求和）
- [x] Console/Network 支持 cursor 与断线补账。（`browser/console|network/query` 的 sinceSeq；条目 seq 页内单调，`browser/console|network/event` 批量带 lastSeq）
- [x] screenshot 只发 artifact 引用，不塞大 base64。（字节走 sidecar 内部通道，C++ 落 `<HomeLubancodeDir>/browser-artifacts` 内容寻址，事件与方法结果只带 image.artifact 引用）
- [x] browser action 接 permission broker 与 cancellation。（owner=agent 写动作过 permission/request，acceptForSession 按方法名记账；`browser/action/cancel` 贯通审批段（悬空收口+擦账，迟到答复报 stale）与 sidecar 段（cancelled 通知，轮询型动作见旗即停）；thread/stop 取消该 thread 名下动作）

验收：独立协议客户端能完整控制页面、看事件、断线重接。

### 阶段 4：真正内嵌 Browser Tab（已翻案，归 `todos/多前端外壳与浏览器Tab_AppServer承载设计.todo`）

原思路"选 Electron/WebView2/CEF 三选一"作废：AppServer 本就是为多前端设计的缝，
内核不为任何一家外壳加一行动作。Tauri、纯 Web、Android 各自连协议，浏览器 Tab
由外壳渲染（镜像流或真嵌入是外壳自己的事）。本阶段剩余目标（页面同屏、面板、
仲裁、截图原图）在新单的阶段 A-D 里接续。

### 阶段 5：官方 Browser Package

- [ ] 等 PackageMountPlan 与内容组件挂载落地。
- [ ] 迁移现有 Browser Skill 与练习页。
- [ ] 添 browser-reviewer Agent。
- [ ] 添 smoke、responsive、console-error、network-failure Workflows。
- [ ] Package 只声明核心工具依赖，不复制 Runtime。
- [ ] MCP adapter 作为可选 code-bearing 组件，供兼容场景启用。

验收：卸掉 Package，核心 Browser Tab 仍能用；只是少了专门 Agent、Skill 与 Workflow。

## 十三、测试账

### Runtime

- [ ] Chromium、WebKit。
- [ ] headed、headless。
- [ ] persistent、ephemeral。
- [ ] page create/select/close/navigation。
- [ ] stale generation/ref/observation。
- [ ] screenshot viewport/full-page/element。
- [ ] profile lock、取消、超时、崩溃恢复。
- [ ] 用户动作与 Agent 动作竞态。

### Console 与 Network

- [ ] console 四级、异常、source 行列。
- [ ] fetch/XHR/document/image/script 分类。
- [ ] 200/404/500/连接失败/超时。
- [ ] header 与 body 脱敏。
- [ ] 大 body 落 artifact。
- [ ] 事件断线补账与 cursor 去重。

### 图片

- [ ] screenshot 给用户显示。
- [ ] 同一 screenshot 给真实多模态模型识别。
- [ ] 不支持图片的模型明报降级。
- [ ] 坏 MIME、坏魔数、超 5 MiB、超 8000px、总量超帽。
- [ ] session 只存 ArtifactRef，不存大 base64。

### App Server 与 UI

- [ ] 协议版本与能力协商。
- [ ] 页面、Console、Network 高频事件不淹没 turn 终态。
- [ ] 窄屏、宽屏、缩放、高 DPI。
- [ ] 页面与聊天分栏不重叠。
- [ ] 截图、DOM、Console、Network 都标 page id 与 generation。
- [ ] 暂停浏览器动作不误停整场会话。

### Package

- [ ] 核心 Runtime 不依赖 Package。
- [ ] Browser Package 装上后 Agent/Skill/Workflow 可发现。
- [ ] Package 卸掉后核心 Browser Tab 仍在。
- [ ] MCP adapter 未信任时不启动。
- [ ] adapter 启动失败不伤核心 Runtime。

## 十四、真机验收剧本

起一套本地网页，埋下：

- 一枚 Console error。
- 一条返回 500 的 API。
- 一张手机端溢出卡片。
- 一只 Shadow DOM 控件。
- 一只 iframe 表单。
- 一次文件下载。
- 一次需要确认的删除按钮。

让 LubanCode：

1. 在内嵌 Browser Tab 打开网页。
2. 切 `390x844`，截一张图。
3. 模型指出溢出位置，用户能点开同一张图。
4. 从 Console 找到异常。
5. 从 Network 找到 500 请求与响应摘要。
6. 修代码，等待热更新。
7. 重载页面，复验 Console、Network 与截图。
8. 下载一份报告，对账路径、字节与 SHA-256。
9. 用户中途点页面，旧 ref 立即失效，Agent 重新 snapshot。
10. 杀浏览器进程，界面报 crashed；重启后旧 page id 不复用。

完成线：不是“工具返回成功”。须同时看见：

- 页面真变。
- 用户界面有截图。
- 模型真读到截图。
- Console/Network 账能指出故障。
- session 与 artifact 可追溯。
- 断线、取消、崩溃都能收口。

## 十五、不做的事

- 不把 BrowserRuntime 塞进 Skill。
- 不让 Agent YAML 内联浏览器启动命令。
- 不让 Package manifest 授权浏览器写动作。
- 不另造一套与现有 Browser MCP 完全不同的 page/ref 语义。
- 不一上来删 MCP；它仍是 CLI、第三方与远程场景的好入口。
- 不拿“能保存 PNG 文件”冒充“模型看过图”。
- 不拿“开了 headed 窗口”冒充“已经内嵌”。
- 不先做 Package 市场、远端安装与自动更新。

## 十六、最后定案

产品本体：

```text
BrowserRuntime + App Server Browser 协议 + Desktop Browser Tab
```

官方能力包：

```text
Browser Agent + Skills + Workflows + Prompt Profiles + 练习资产
```

兼容入口：

```text
MCP adapter
```

三者各守一摊。Runtime 托住状态，桌面端亮出画面，Package 教 Agent 办事，MCP 接旧路与外客。这样搭，眼下的 Playwright 家底不丢，往后也不会被一只 Package 箱子困住。
