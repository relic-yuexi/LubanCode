# 思考活动条扫光复活：原生直写路逐字高亮

- 状态：待实施（2026-09-03 立）
- 级别：P2（锦上添花——视觉活性感，非缺陷）
- 类型：LubanCode 产品改进（终端 footer 动画）
- 背景：0.26.174 一轮砍逐字扫光（当时 footer 走 VT 路，扫光=每 200ms 搬光标=光标跳动病根，止血一刀切留静态蓝点+秒钟）；0.26.179（cf4c4f54）二轮 footer 改原生 `WriteConsoleOutputW` 直写后，扫光的技术障碍已清——每格 CHAR_INFO 自带颜色属性，逐字高亮只需重写活动行几个格子：光标不动、零 stdout 字节、无中间态。
- 关联：《终端思考活动条重画与输入框光标跳动.todo》（8.2/8.3 二轮实施——本单在其地基上复活动画，不许回退任何二轮合同）
- 不在本单：footer 布局/结构（常驻）；秒钟节律（每秒一跳不动）；光标所有权合同（一字不许松）

## 一、实现要点

- [ ] 高亮位计算：活动行"思考中"标签按 200ms 心拍逐字轮换高亮格（复用一轮删掉的 `working_highlight` 思路，只算格位不碰布局）
- [ ] 判据升档：`TurnActivityRowChanged`（terminal_frame 纯函数）加"高亮位变化"档——现在同秒零变化收手，扫光复活后高亮位变化即触发重画（仅此一档，秒数/阶段判据不动）
- [ ] 原生直写画高亮：`BuildNativeRowCells` 产单元格时对高亮格给亮色属性（沿用现有 16 色折算，主题语义色可加 `activity_highlight` 键或复用亮蓝）；每拍只重写活动行（整行直写，光标不动）
- [ ] 心拍预算：200ms 拍内高亮位没变（标签尽头的回绕拍）不落笔——帧账不许有无变化帧
- [ ] 降级档照旧无动画：非真 console（管道/POSIX VT 路）不跑扫光——动画只在原生直写档生效，VT 批档维持静态蓝点+秒钟

## 二、验收

- [ ] 单测：高亮位轮换纯函数（边界回绕、标签长度 1/多字、宽字双格占两拍）；`TurnActivityRowChanged` 高亮位变化档真/假
- [ ] driver G0 判据全绿不退：高频轨迹零活动行落点（动画回来光标也不许动——这是二轮合同的回归闸）
- [ ] driver G7 帧账：帧数从每秒 1 帧升至 ≤每秒 5 帧（200ms 拍），字节恒 0；无变化拍零帧
- [ ] 真终端（WT/conhost）肉眼：扫光流动、无光标跳动、无残影——留用户随手验

## 三、不做什么

- 不动秒钟与阶段文案
- 不为扫光增加任何 stdout 输出（原生直写字节恒 0 是底线）
- 不在 POSIX/管道档做任何动画变体
