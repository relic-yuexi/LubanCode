# 重排 provider 添加与切换界面，并支持返回上一步

状态：待做（2026-08-16 据实际录入体验补单）

## 缘起

现有 `/provider add` 能把配置写进去，手感却生硬。每问一项便往下添几行。输错后，错误、旧答案、新提示挤作一处。空行又多，前后关系反倒看不清。

更要紧的是，向导只许往前走。走到 `model` 才发觉 `wire`、`base_url` 或密钥填错，只能退出重来。前头七八项也跟着白填。

一段实录里，已经露出几处毛病：

- 名字先输中文，报错后又从空白的“名字:”起头，看不出这是原步骤重试。
- `base_url` 的格式规矩等输错才说；改正后，旧错仍夹在正文里。
- 模型列表拉取失败后，向导径直掉进手填；若错在接口格式或地址，此处无路回头。
- `api_key` 留空再问环境变量，层级不明，容易叫人猜“刚才那次回车算不算选中了什么”。
- 全程没有步骤数、当前项、返回键与取消键提示。

## 要做成什么样

把 provider 向导收成一块稳定的交互区。上下各落一条分隔线，标题、进度、当前字段、简短说明、错误和按键提示各守一行。步骤切换时，在原地清掉旧内容，再画下一步；不要把每次试填都往下堆，更不要写进永久 transcript。看上去是一块界面，不是一串问答日志。

大致可排成这样：

```text
------------------------------------------------------------
添 provider                                      第 3/8 步

请输入 base_url。OpenAI 兼容接口一般要带到 /v1。
模型列表将从 http://127.0.0.1:8000/v1/models 读取。

base_url:
> http://127.0.0.1:8000/v1

Esc 上一步  Ctrl+C 取消
------------------------------------------------------------
```

用户按下回车，整块区域就地换成下一步。旧说明、旧输入和旧报错一并擦掉。屏幕上始终只留当前步骤。最后确认写入时，再把一份干净的配置摘要落进 transcript；中间试填不落。

这块界面不必铺满全屏，也不必为了“高级”开一层花哨弹窗。一上一下两条线，一段短说明，一只输入框，一行 footer，便够了。窄窗时说明自然换行，边线随终端宽度收放，不写死六十列。

模型列表若拉取失败，不要只留一句“改成直接输入模型名”。应把去路摆明：

```text
未能读取模型：HTTP 404 Not Found

> 手动输入模型名
  返回检查接口格式
  返回检查 base_url
  重试
```

错误要短，要贴着当前字段。底层响应可折进详情，默认只露状态码与摘要。用户改过以后，旧错误随重画退场；调试日志仍可另处查，不必污染对话。

## 返回规矩

- 每一步都能回到上一步。选择器认 `Esc` 或左方向键；文本输入须给终端确实能送达的返回动作，并在 footer 写明。若沿用 `Esc`，先核清现有 `ReadLine` 不会把它吞掉。
- 返回只挪步骤，不清已经填过的字段。再往前走时，用旧值作默认值，允许直接回车确认。
- 在 `model` 拉取失败页，可直接跳回 `wire` 或 `base_url`，不必一层层倒退。
- 回到 `wire`、`base_url`、密钥后若改了值，旧模型列表须作废。再次前进时重新拉取，不可沿用上回缓存。
- 回到 `api_key / key_env` 时，不回显明文 key。只写“已设置明文密钥”或“读取环境变量 XXX”；若要换 key，重新输入。
- 第一步再退，弹出“退出向导？”；默认不写盘。`Ctrl+C` 在任何一步都走同一条取消路。
- 最后的汇总页允许选中某一项跳回修改。改完再回汇总，不必把后续无关步骤重走一遍。

## 字段与提示

步骤顺序改为：名字、接口格式、`base_url`、密钥来源、模型、推理档位、额外参数、确认。先定接口格式，再问地址，`/v1` 提示和最终模型 URL 才能算准。

### 名字

- 在输入前就写清可用字符：字母、数字、下划线、点、短横线。
- 中文或空格不合规时，可给一个 slug 建议，如 `local-server`。只建议，不暗改。
- 报错分清“字符不合规”和“名字已存在”。重试时仍停在 `[1/8] 名字`。

### base_url

- 提示先写在输入框上方：须带 `http://` 或 `https://`。Responses、Chat Completions 这类 OpenAI 兼容接口一般还要带到 `/v1`，本地示例应写作 `http://127.0.0.1:8000/v1`。
- 不能不分协议便强行补 `/v1`。现有模型探测对 Anthropic 请求 `{base_url}/v1/models`，对 Responses、Chat Completions 请求 `{base_url}/models`。若 Anthropic 的地址也盲补 `/v1`，便会拼成 `/v1/v1/models`。
- 用户选定接口格式后，在输入框旁直接展示最终探测地址，如 `将读取 http://127.0.0.1:8000/v1/models`。这样少没少 `/v1`，一眼就能瞧出来。
- OpenAI 兼容格式若填成 `http://127.0.0.1:8000`，可当场提示“这个服务通常还要 `/v1`”，并给出采用 `http://127.0.0.1:8000/v1` 的选择。只建议，不暗改；有些服务确实不用这层路径。
- 保留现有去尾斜杠规矩。校验失败时，把用户刚输的内容留在编辑框里，方便补上协议头，不叫人从头再敲。
- 此单不擅自接受无协议地址。若以后要自动补 `http://`，另定明确规矩，并在确认页展示归一化结果。

### 接口格式

- 三个选项须同时写出短名与常见路径：Anthropic Messages、OpenAI Responses、OpenAI Chat Completions。
- 改动接口格式后，默认环境变量名、探测模型的路径与已拉模型列表一道刷新。

### 密钥

- 把二段问答改成三项明白的选择：无需鉴权、从环境变量读取、贴入 API key。
- 自建服务常常不要 key。“无需鉴权”是一等选项，不藏在报错页，也不逼用户填写 `dummy-key`。地址是 localhost、127.0.0.1 或局域网时，可把光标默认停在这一项；仍须由用户按 Enter 明确确认，不可光看地址便暗自判定。
- 选环境变量时展示按接口格式算出的默认值。环境变量未设置，也要在拉模型前说清；不要等请求失败才叫用户猜。
- 明文 key 不进 transcript，不进错误，不在返回步骤时回显。汇总仍只露掩码。
- 选“无需鉴权”后，汇总写 `auth = none`。模型探测和正式请求都不发 `Authorization`、`x-api-key` 等鉴权头；不能发一条空的 `Authorization: Bearer ` 冒充无鉴权。

### 模型

- 拉取前展示最终的模型列表 URL 与接口格式，不只展示原始 `base_url`，也不展示 key。
- 404 要提示多半该检查地址或接口格式；401/403 指向密钥；连接失败指向服务是否启动。只作诊断引路，不把猜测说成定论。
- OpenAI 兼容接口在不带 `/v1` 的地址上收到 404 时，给出“加上 `/v1` 后重试”这一项，并把改后的完整探测 URL 摆出来。用户确认后才改值。
- 拉取失败后保留“手填模型名”这条路，也要留“重试”和“返回修改”。
- 手填留空仍报错，但提示留在本步骤内，不再追加“model 不能为空,再输一遍”一类流水账。

## `/provider switch` 也要接住用户

现有体验可叫“意图断裂”。用户已经敲了 `/provider switch`，意思很明白：我要换一家。程序却因少了名字，把整本 `/provider` 用法倒出来。用户只得从一大排 `add/remove/set` 里找回刚才那一行，再记住内部名字，重新敲一遍。

第二次敲成 `/provider switch custom`，程序又只说环境变量没值，随即收场。它说清了哪里错，却没接着说怎么补、怎么退、怎么换另一家。这是一条死胡同。每一步单看都“合乎用法”，连起来却像程序把解析器的脾气甩给用户。

### 裸敲就开选择器

TTY 中裸敲 `/provider switch`，不要判作 `Invalid`，也不要打印总帮助。直接打开同款原地面板：

```text
------------------------------------------------------------
切换 provider

> local        gpt-5.5       127.0.0.1:8000   无需鉴权
  minimax      MiniMax-M2.5  api.minimax.io   可用
  anthropic    claude-opus   api.anthropic.com 可用  当前

↑↓ 选择  Enter 切换  Esc 取消  输入文字筛选
------------------------------------------------------------
```

- 列出 provider 名、默认模型、短地址、鉴权状态与“当前”标记。莫把完整配置和长 URL 塞满一行。
- 默认光标停在当前 provider；输入文字可按名字、模型和地址筛选。
- 选中后才切换。切换成功，整块面板收起，transcript 只留一行结果。
- 没有任何 provider 时，给“添加 provider”和“取消”两项，不倒用法全文。
- 非 TTY 下不能开选择器，才给一条短提示：`用法: /provider switch <名字> [模型]`。只说这一支，不打印 `add/remove/set` 全家桶。
- 显式写了 `/provider switch custom` 仍走快捷路径；不用强迫熟手进菜单。

### 配置缺口不能只报完就走

选中的 provider 若缺密钥，不要退出选择器。原地换成一页可处理的状态：

```text
------------------------------------------------------------
还不能切到 custom

所需环境变量：OPENAI_API_KEY
当前进程没有读到值。

> 现在输入 API key
  改用另一个环境变量
  设为无需鉴权
  查看设置方法
  返回 provider 列表

Esc 返回  Ctrl+C 取消
------------------------------------------------------------
```

- “现在输入 API key”须先讲明保存去处：只供本次会话，或写入用户配置。写盘仍按现有明文 key 风险提示和掩码规矩办，不可含糊。
- “改用另一个环境变量”只存变量名。若当前进程仍取不到值，当场标成“未设置”，不要假装修好了。
- “设为无需鉴权”须明确写回 provider，再继续切换。它不是“忽略这次错误”，下次启动仍应认得。
- “查看设置方法”按 PowerShell、cmd、POSIX shell 给当前终端对应的一条命令，并说清设完后是否须重启 LubanCode。不要再铺整页 provider 用法。
- 返回列表后，刚才的选择与筛选词仍在。取消不改当前 provider，也不写配置。
- `/provider switch <名字>` 找不到名字时，也可打开已筛选的 provider 列表，并把“不存在”贴在面板内；不要报一行便断。

### 无鉴权是一等配置

`127.0.0.1` 上的兼容服务有些不要 key，有些照样要。不能看见本地地址便自行放行，也不能叫用户随手塞一枚假 key。

现有 `ProviderConfig` 强制 `key_env` 非空，切换时又强制取到非空 key，尚无“明确无鉴权”这一态。本单须补齐：

- 给 `ProviderConfig` 添显式鉴权模式，至少分清 `none`、`env`、`inline`。字段名可用 `auth` 或 `auth_mode`，落盘值须稳定。
- 旧配置没有鉴权模式时，按旧语义迁移：`api_key` 非空算 `inline`，否则算 `env`。绝不能把旧配置里一次缺失的环境变量擅自迁成 `none`。
- `none` 模式允许 `key_env` 为空；`env` 模式要求变量名非空；`inline` 模式要求 key 非空。校验错误按模式说人话。
- 把 `ProviderApiKey` 这类只回 `optional<string>` 的接口改成可区分“无需鉴权、已经取到 key、该有 key 却缺失”的结果。否则调用方仍会把前两者混为一谈。
- `RequireApiKey`、`RequireConfigured`、provider 合并、全局配置读写、`/config` 掩码展示都要认得 `none`。
- Anthropic、Responses、Chat Completions 三套正式请求，以及 `ListModels` 模型探测，都按鉴权模式组装 header。`none` 时彻底省去鉴权头。
- 添加向导、provider 列表、切换选择器和最终摘要统一显示“无需鉴权”，不再显示“环境变量未设置”。
- 给熟手留命令入口，如 `/provider set <名字> auth none|env|inline`。切到 `env` 或 `inline` 时若还缺变量名或 key，接着开相应输入页，不落半截配置。
- 无鉴权必须由用户明确选择。不能凭 localhost、HTTP 地址或一次 401/404 自动猜。

## 实现边界

现有 `RunProviderAddWizard` 把八步顺次写死，`Resolve*` 里又各自循环。要支持返回，不宜在每个函数里塞一枚特殊字符串硬跳。

- 抽出 `ProviderWizardState`：保存当前步骤、各字段、字段是否有效、模型列表来源与失效标记。
- 用显式 step enum 驱动前进、后退、跳转、取消与确认。校验函数只回结果，不自行困在 `while (true)` 里。
- 把现有 `base_url -> wire` 的次序调成 `wire -> base_url`。预填值与旧测试须跟着迁移，不能只换屏上文案、底层仍按旧次序吃输入。
- 给 provider 命令补 `SwitchInteractive`，或让 `Switch` 接受空名字并交给交互层。`/provider switch` 不再落进笼统的 `Invalid -> cmd.provider.usage` 分支。
- 添加向导和切换选择器共用 panel renderer、导航事件与鉴权状态检查。不可一边原地重画，一边仍拿 `std::cout` 刷屏。
- 鉴权解析须下沉成一份共享结果。模型探测、切换预检和正式请求都吃这份结果，不能三处各猜一遍空 key 是什么意思。
- 给 `WizardIO` 添可测试的导航事件，分清“提交空串”“返回”“取消”“EOF”。不要拿某个普通文本值冒充返回键。
- TTY 下给向导单独的 panel renderer：记住面板起点与已占行数，每次输入、校验或换步后清除旧区域，再从同一处整帧画回。须接现有 screen driver 或统一 ANSI/Win32 渲染层，不在 provider 逻辑里四处手写光标转义。
- 终端缩放或说明软换行后，按新宽度重算面板高度，整帧重画，不能残留半截边线和旧字。
- 非 TTY、管道输入或终端不支持光标控制时，退回朴素逐行模式。步骤号、返回与错误仍要有；切不可为了好看破坏脚本化输入。
- `RunProviderPresetWizard` 与初次 setup 向导复用同一套导航原语。先把 provider add 做稳，再决定是否迁移别处。
- session transcript、日志与配置文件都不得留下明文密钥。重画也不能把秘密从内存意外打印出来。

## 验收

- 从名字一路走到模型，退回改 `base_url`，再前进；名字、密钥选择等旧值仍在，模型列表重新拉取。
- 模型请求返回 404 后，可重试、手填，也可直达接口格式或地址。三条路都能走通。
- Responses、Chat Completions 填 `http://127.0.0.1:8000/v1` 时，界面展示并请求 `http://127.0.0.1:8000/v1/models`；Anthropic 不会拼出重复的 `/v1/v1/models`。
- 名字输中文、地址漏协议、模型留空，各只在当前步骤显示一份错误。改正后界面不留旧错残影。
- 连走八步，TTY 里始终只见一块当前面板；换步后旧字段、旧输入、旧错误都被原地替换。最后只留一份确认结果。
- 从汇总页跳回任一字段，改完回汇总。最终只写一条 provider，不多写，不漏写。
- 返回密钥页不见明文 key；取消、EOF、异常与测试失败输出也不见。
- `Ctrl+C` 从八个步骤退出，均不改内存配置，不落盘。
- 窄终端、中文宽字符、Windows Terminal、VS Code Terminal、conhost 各走一遍。标题、进度、输入和 footer 不重叠。
- 非 TTY 与脚本化 `WizardIO` 仍有确定行为；旧测试改为 step/event fixture，并补齐前进、后退、跳转、失效、取消五组状态测试。
- 裸敲 `/provider switch` 会打开选择器；不会出现包含 `add/remove/set` 的总用法。
- 选择可用 provider 后只切换一次，并保留既有 history；选择缺密钥 provider 后仍留在面板里，可补密钥、看设置方法、返回或取消。
- `/provider switch custom` 缺 `OPENAI_API_KEY` 时给出同一补救页；取消后当前 provider、模型、后端与配置文件均不变。
- 非 TTY 裸敲 `switch` 只打印 switch 专用短用法。解析器与交互处理各有单测。
- 新建无鉴权 provider 后，不设 `OPENAI_API_KEY` 也能拉模型、切换并发起 Responses/Chat 请求；抓包中没有空 Bearer 头。
- Anthropic 无鉴权模式同样不发 `Authorization`。三套 client 和 `ListModels` 都有 header 单测。
- 旧 provider 配置不写 `auth` 时仍按原规则读 key；环境变量缺失仍报缺失，不会悄悄变成无鉴权。
- 把现有 `custom` 从 `env` 改成 `none` 后可立即切换，重启后仍是 `none`。

## 不做

- 不自动猜测用户想用哪种接口格式。
- 不因地址在本机就自动关闭鉴权。
- 不因模型列表拉不到便拦住手填模型。
- 不拿退格键清空字段来冒充“返回上一步”。
- 不把向导每一帧写进永久对话或日志。
