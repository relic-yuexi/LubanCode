# 会话管理器仿 Codex `/resume`:检索、筛选、排序、归档与安全删除

状态:待做(2026-08-22 用户定调:LubanCode 照 Codex 的会话管理规矩设计)

## 一句话

把裸 `/resume` 从“最近二十场的通用方向键菜单”换成一张全屏会话台账。
它只管找、看、续。归档与永久删除另开明路，不能在浏览时一键误删。

```text
Resume a previous session

Type to search                         Filter: [Cwd] All   Sort: [Updated] Created

> 1m ago    看一下 docs 目录。里面一定要有一些文档……
  8h ago    git push 一下，set http_proxy=……
  1d ago    讲一下这里面的 context 管理吧。

enter resume   esc exit   tab focus sort/filter   <-/-> change option
ctrl+o comfortable view   ctrl+t transcript   ctrl+e expand   up/down browse
                                                               1 / 24 · 0%
```

## Codex 的规矩

- `/resume` 选择器摆搜索、Cwd/All 筛选、Updated/Created 排序、相对时间、
  首句预览、选中行、页码与键位提示。画面密，眼睛一扫便能定人。
- 选择器里不摆 delete。不是 Codex 没有删，而是把危险动作挪出了浏览面。
- Codex 另设 `codex archive <SESSION>`、`codex unarchive <SESSION>`；归档只
  从活动列表藏起，转录仍留着。
- Codex 另设 `codex delete <SESSION>` 与 `/delete`；这是永久删除，要走
  明白命令与确认。`/archive`、`/delete` 针对当前会话，做完退出。
- 官方命令参考:
  https://learn.chatgpt.com/docs/developer-commands?surface=cli

LubanCode 也守这条线:浏览器不兼任碎纸机。日常收拾先 archive；当真不要了，
再显式 delete。

## 现状账(2026-08-22 已翻码)

- `SessionStore` 把每场会话写进 `~/.lubancode/sessions/<id>.jsonl`。首行
  meta 记 started_at/cwd/model/wire，后头追加消息、title、cwd 与 compact
  事件。
- `ListSessions()` 扫 `*.jsonl`，凭文件名时间倒序，最多取二十场。按 cwd
  筛时只读 meta；入选后再读整份文件，补标题、首句与消息数。
- `/sessions` 只会打印本目录或 all；`/resume` 裸敲把本目录最近二十场塞进
  `ReadChoiceMenu()`。只有上下、Enter、Esc，没有搜索、分页、筛选切换、
  排序切换或预览。
- 显式 `/resume <编号|id>` 与 `--continue` 已能续档；直接给 id 可跨 cwd。
- `SessionListEntry` 眼下只有 id/path/started_at/cwd/title/first_user_text/
  message_count。没有 updated_at、archived、损坏状态或可分页查询合同。
- Runtime 合同已有 `thread.list/read/resume` 名字，却没有 archive、unarchive、
  delete。终端 handler 仍边查盘、边开菜单、边写 stdout。
- 会话文件长期开着 append 句柄。Windows 上若不先 flush/close，移动或删除
  很容易吃 sharing violation。这条须由生命周期服务收口，不能让 UI 自己删。

## 产品定案

### 一、`/resume` 只做无损动作

- 裸 `/resume` 打开全屏 `SessionPicker`。Enter 恢复；Esc 原路返回。
- 不在主列表放“按一下便永久删除”的键，也不在行尾堆危险图标。
- `Ctrl+T` 看所选会话转录；`Ctrl+E` 展开长标题、cwd、id、模型、消息数与
  最近更新时间；再按一次收起。
- `Ctrl+O` 在紧凑行与舒展行间切换。切换只改画法，不改筛选结果与选中项。
- `/resume <id>`、`--continue` 与非交互脚本用法照旧，免得老用法断掉。

### 二、搜索、筛选、排序

- 输入即搜。命中 title、首条用户文本、session id 与 cwd；ASCII 不分大小写，
  中文按原字匹配。搜索不重读磁盘，不因每敲一字卡一下。
- Tab 轮换 Search / Filter / Sort 焦点；左右键改当前选项。
- Filter 首批认 `Cwd | All`。归档入口另有 `Archived`，不可混进缺省活动表。
- Sort 认 `Updated | Created`。Updated 取最后一条合法事件时间；缺失时退文件
  mtime。Created 取 meta.started_at；再缺便退 id 时间。
- 上下键浏览，PageUp/PageDown 翻页，Home/End 到头尾。筛选变化后尽量按 id
  留住选中项；它消失了才落到最近一行。
- 底栏常驻“当前序号 / 命中总数 · 滚动百分比”。窗口变窄时先收 cwd、模型等
  次要字段，标题与键位不许互相压住。
- 空列表、搜索无命中、坏 meta、读档失败各有自己的画面。坏一场只标一场，
  不拖垮整张表。

### 三、归档先行

- 顶层命令补:
  `lubancode archive <SESSION>`、`lubancode unarchive <SESSION>`。
- 会话内补 `/archive`，只归档当前会话。先刷盘、关句柄，再把 JSONL 搬进
  `~/.lubancode/sessions/archive/`，成功后退出当前交互会话。
- 另给 `/sessions archived` 或等价只读入口。Archived 视图里 Enter 不直接
  resume；先 unarchive，再恢复，账才清楚。
- 归档保留文件字节、id、标题与时间。重名目标拒绝覆盖；半路失败时原文件仍
  可用，不能两头各剩半份。
- `--continue`、普通 `/sessions`、默认 `/resume` 一概略过 archive 目录。

### 四、永久删除另开明路

- 顶层命令补 `lubancode delete <SESSION>`；交互确认后才删。
  `--force` 只给脚本显式使用，帮助里写明不可恢复。
- 会话内补 `/delete`，只删当前会话。回合正在流、工具还在跑、审批还悬着时
  拒绝；闲下来后先确认，再关句柄、删文件、退出。
- SESSION 优先按完整 id 解。若认标题，只许唯一命中；重名便列出短 id，叫
  用户点明，绝不猜一场来删。
- 确认屏须写标题、完整 id、cwd 与“永久删除”。缺省选取消。EOF、Esc、空答
  都算取消。
- 删除只碰已验明位于 sessions 根或 archive 子目录里的那一份 `.jsonl`。
  canonical path 越界、符号链接绕出根、后缀不对，一律拒绝。
- context artifact 可能按 hash 共用。首版不跟会话连坐删 blob；以后另做引用
  计数与 GC，不能为清一场顺手伤了别场真本。
- app-server / Web / Tauri 可把 Archive、Unarchive、Delete 放进各自管理界面，
  仍须走同一领域命令与确认策略。终端 picker 本身不添隐蔽删键。

## 代码边界

### `SessionCatalog`:只查账

新立中立查询层，不 include Theme、ChoiceMenu 或 stdout:

```text
SessionQuery
  scope: cwd | all
  state: active | archived
  sort: updated | created
  search, cursor, limit

SessionSummary
  id, title, first_user_text, cwd, model
  created_at, updated_at, message_count
  state, health, file_fingerprint
```

- 打开 picker 时扫一回，按 `path + size + mtime` 缓存摘要。键盘搜索只筛内存。
- 文件变化才重读那一场。首版不急着造常驻数据库；JSONL 仍是唯一真账。
- 分页、筛选、排序做纯函数测试。相对时间只在 Terminal renderer 算，协议层
  留 RFC3339/现有稳定时间串，Web 可按自己的 locale 画。

### `SessionLifecycle`:只管搬与删

```text
ArchiveSession(ref)
UnarchiveSession(ref)
DeleteSession(ref, confirmation)
```

- 解析引用、校验根目录、检测当前活动句柄、flush/close、rename/remove、错误码
  全聚在这里。UI、slash handler 与 app-server 不直接碰 filesystem。
- 结果用稳定 code: not_found / ambiguous / active_turn / confirmation_required /
  path_outside_root / target_exists / io_error。
- 成功后发 `thread.updated`，payload 带 state；永久删除另发 `thread.deleted`。
  Runtime 命令补 `ArchiveThread / UnarchiveThread / DeleteThread`。
- `thread.list` query/receipt 返回结构化 SessionSummary。终端、Web、Tauri 共吃
  一碗饭，不各自重扫 JSONL。

### `SessionPicker`:只画与收键

- 不把这张复杂页面硬塞进通用 `ReadChoiceMenu()`。新做 controller + view，复用
  现有 raw input、宽度探测、主题、Unicode 截断与安全重绘。
- controller 只持 query、焦点、选中 id、viewport 与 preview mode；resume 结果
  返回 id。它不知道存档在哪，也没有 delete API。
- 重绘走单一 frame。resize、搜索增删字、切筛选、开预览都不能残行、闪屏或把
  旧 transcript 打乱。
- 无 TTY 时不进全屏，打印清楚用法；stdout 被重定向时不漏 ANSI 与光标码。

## 落地次序(一批一枚 commit)

1. 扩 `SessionSummary/SessionQuery`，从现有 ListSessions 收拢出 SessionCatalog；
   钉住 created/updated、损坏单项、cwd、search、sort 与分页测试。
2. 做 SessionPicker 框架，先还原截图里的标题、搜索、列表、焦点、底栏与 resize；
   Enter 接回现有 ResumeSession，显式 id 用法不动。
3. 接 transcript/expand/comfortable 三种查看态；大文件按需读，取消后仍回原行。
4. 做 archive 目录与 SessionLifecycle；补顶层 archive/unarchive、`/archive`、
   archived 列表及 Windows 开句柄回归。
5. 做 delete：引用消歧、根目录校验、二次确认、`--force`、活动回合拒绝与错误码。
6. 把 query/lifecycle 接入 Runtime 与 app-server；slash handler 退成 typed command
   适配器，给将来 Web/Tauri 留齐合同。
7. 更新 commands、sessions-and-context、terminal-ui、README、双语 i18n 与 shell
   completion；跑全量测试和 Windows/POSIX 终端驱动器。

## 验收

- 二百场中输入三四个字，列表即时缩小；标题、首句、id、cwd 都能命中。
- Cwd/All 与 Updated/Created 用 Tab、左右键切得动；选中行不乱跳，页码与百分比
  始终对得上。
- 中英长句、emoji、双宽字符、窄窗与 resize 都不越界、不残行、不切坏 UTF-8。
- Enter 恢复正确 id；Esc 不动任何存档；`/resume <id>`、`--continue` 老路全绿。
- picker 里没有一键永久删除。浏览、搜索、看 transcript 都不改磁盘。
- archive 后默认列表不见，archived 列表能见，unarchive 后原 id、标题、时间、
  消息一字不丢。
- delete 缺确认不动盘；选取消不动盘；完整确认只删目标一场。路径越界、重名、
  当前回合在跑、Windows 文件柄未关都给明白错误。
- 一份坏 JSONL 只显示 damaged，别的会话照搜照续。千场目录打开与键入搜索无
  肉眼可见停顿，并有基准数留档。
- Terminal 与 app-server 对同一 query 给出相同 id/顺序/状态；归档、反归档、
  删除都经 SessionLifecycle，代码里搜不到第二条私删路径。

## 不做

- 不把归档冒充删除，也不把永久删除做成可误触快捷键。
- 不在本单重写 JSONL、历史回放、compact、export 或 artifact 存储。
- 不照抄 Codex 私有协议字段；只学交互规矩与安全边界。
- 不让 Web/Tauri 直接读 `~/.lubancode/sessions`。它们只调 Runtime/app-server。
