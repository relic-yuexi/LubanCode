# subagent_recursion 时序抖动：回流等待的常数界收敛

- 状态：待实现（三犯立案，欠账清单"时序家族"条目升格）。
- 日期：2026-09-09
- 级别：P2（CI 稳定性；不涉生产行为，只在测试的等待合同）。
- 前科：run 33988795509（2026-09-06 attempt 1 红、attempt 2 绿）；run 34270604648（2026-09-09，PR #3，重跑绿）；run 34281593474（2026-09-09，PR #5 windows-msvc）。
- 范围：`tests/unit/agent/test_subagent_recursion.cpp` 及其依赖的回流等待设施；不改生产回流语义。

## 一、病相

四断言连坐：`root->result` 停在"阶段结论：等孩子"、`child->delivered` 假、`saw_child_completion` 假、drained 无"根的最终结论"——孩子完成后的回流唤醒没在测试时限内送达父侧。三案同码重跑皆绿（前两案已证），纯异步时序脸谱，且三犯全在跑机较慢的腿（windows-msvc 两犯、macos 一犯同族 runtime 册）。

## 二、修法方向（先核后落）

1. 读该册的等待机制：现在多半是"固定 sleep + 轮询到期限"或裸 sleep 一锤子。定位 `test_subagent_recursion.cpp` 里等回流的那几处常数。
2. 收敛办法二选一（按现状择低者）：
   - 轮询条件等待：期限放宽（如 10s 上限）、步长收细（如 10-25ms），早到早退——慢机不误伤，快机不多等；
   - 若测试设施已有条件变量/事件口，改事件等待 + 宽期限，废裸 sleep。
3. 同册其他裸 sleep 顺手同治；只动等待合同，不动断言语义与脚本。
4. 回归：本册连跑 N 次（本地或 CI 矩阵里加 --repeat 若已有先例）不红。

## 三、验收

- 三犯场景（子代理完成回流唤醒父侧）在放宽后的等待下稳定过；
- 测试总时长不显著上涨（早到早退）；
- 不改生产码一行（若核实发现生产回流真有病，另立单，不在本单夹带）。

## 四、边界

时序家族前四案（app_server 锁饥饿 / ui_event_pump 常数界 / lua 墙帽 / rg 竞速窗）的修法先例可抄；本单只管这一册。
