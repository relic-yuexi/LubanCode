# 子代理与 Main Agent 同级，并修复 vLLM/Qwen 流式、思考与输出预算

状态：待做（2026-08-16 据本机 vLLM 0.27.1 + qwen3.8-27b 实测补单）

## 产品不变量

子代理不是低配代理，也不是“短命跑腿”。它只是另开一份独立上下文，接一项任务，通常完成后自动退出。

除此以外，默认待遇应与 main agent 相同：

- 同一模型能力与 provider 能力。
- 同一输出预算策略、上下文窗口与压缩策略。
- 同一流式协议、思考解析与 usage 记账。
- 同一工具能力、项目指令、Skills、记忆召回与安全规则。
- 同一续跑、失败恢复和结果保全。

“同级”不等于共享同一份 history，也不等于绕过权限。子代理有自己的任务、历史、todo 与生命周期；权限、sandbox、确认档仍照常守。Explore 这类明确声明“只读”的角色可以收窄工具，隔离 worktree 也可限制路径。限制须来自角色、任务或安全策略，必须看得见；不能只因它叫 subagent，便暗中降模型、砍 token、拿掉能力。

模型和服务端终究有物理上限。“同级”不是无限 token，而是 main 与子代理吃同一份显式策略，不再给子代理另藏一堵矮墙。

## 这次现场

子代理接到“写中国象棋网页游戏”的任务。第一步跑了约 67 秒，没有工具调用，也没有正文，随后报：

```text
最后一轮没有文本结论(stop_reason=max_tokens)
模型 stop_reason: max_tokens
0 次工具调用 · 0 tokens
```

当时 provider 为：

```text
model = qwen3.8-27b
wire = chat_completions
base_url = http://127.0.0.1:8000/v1
model_reasoning_effort = xhigh
stream_usage = 未声明
```

这不是 vLLM 不支持 SSE，也不是 LubanCode 完全没做流式。

## 本机实测

对同一台 `vLLM 0.27.1` 直接发 Chat Completions 流式探针：

### 关闭 thinking

请求带：

```json
{"max_tokens":128,"stream":true,"stream_options":{"include_usage":true},"chat_template_kwargs":{"enable_thinking":false}}
```

服务端逐块返回正文 `OK`，最后：

```json
{"finish_reason":"stop"}
{"usage":{"prompt_tokens":15,"completion_tokens":2,"total_tokens":17}}
data: [DONE]
```

### 开启 thinking，并照现状发 `reasoning_effort=xhigh`

服务端先逐块返回：

```json
{"delta":{"reasoning":"We"}}
{"delta":{"reasoning":" need"}}
...
```

随后返回正文 `OK`、`finish_reason=stop` 与 27 个 completion tokens。

结论有三条：

1. vLLM 支持 SSE，LubanCode 的 Chat 请求也确实发了 `stream=true`。
2. 这版 vLLM/Qwen 用 `delta.reasoning` 送思考，不是 LubanCode 目前只认的 `delta.reasoning_content`。
3. `stream_options.include_usage=true` 时，服务端能回完整 usage。界面显示 0，是 capability 没开加上“未报告”和“真为零”混作一类。

## 根因一：输出上限写死 4096

`AgentLoop` 默认 `max_tokens=4096`。main 在 `interactive_session.cpp` 显式传 4096，子代理又在 `AgentTool::RunTask` 另传一遍 4096。

这不是“只限子代理”，眼下 main 也挨同一刀。毛病仍很重：

- 数值写死在两处，不认模型目录、provider 声明和真实最大输出。
- 用户不能配置，也看不见本轮上限。
- 推理模型把 thinking 与正文共用这 4096。思考一旦吃满，工具调用和结论还没来，服务端只能回 `finish_reason=length`。
- 子代理拿到长任务后，第一步就可能倒在输出预算上。步数虽是“不限”，连第一步都走不完。
- main 与子代理各自构造 loop，往后改一边极易漏另一边。

### 要改

- 抽出一份 `AgentRuntimeProfile` 或等价运行策略。main、general-purpose agent、后台 agent、单发模式统一从这里拿输出预算、上下文预算、压缩、模型与 provider 能力。
- 配置名用 `max_output_tokens`，别再跟 `max_steps_per_turn` 混叫“预算”。支持全局、provider、模型目录三级来源，并在 `/config`、`/context`、agent 查看态中写明实际值与来源。
- 子代理默认继承 main 的有效值。若 `agent` 调用允许单任务 override，只能在参数和面板里明写；不能暗自缩小。
- 协议允许省略上限时，可用 `unset` 表示交给服务端/模型默认。Anthropic 等协议若必填，则取模型/provider 声明值；没有声明时给公开、可配置的兜底，不能藏一枚 4096。
- 上限要纳入 projected context 计算：输入窗口、thinking、正文和工具调用须留得下。不能只看总 context_window。
- 模型目录可声明 `max_output_tokens`、是否共享 reasoning 预算、建议保留的 final/tool token。provider 可收窄或覆盖。

## 根因二：`delta.reasoning` 被漏掉

现有 Chat SSE parser 只认：

```json
{"delta":{"reasoning_content":"..."}}
```

vLLM 0.27.1 + qwen3.8-27b 实际送：

```json
{"delta":{"reasoning":"..."}}
```

于是模型确实在流式思考，LubanCode 却既不展示，也不把它攒成 `ThinkingBlock`。撞上 `max_tokens` 后，任务记录里没有思考检查点，只剩一张空失败单。

### 要改

- Chat parser 同时认 `reasoning_content` 与 `reasoning`。同一 chunk 两者都有时须定优先级并去重，不能吐两份。
- 两种字段都转成 `ThinkingDelta`，进入同一显示、折叠、录制、task event 与 compact 链。
- provider 能声明 `reasoning_delta_field` 与 `reasoning_replay_field`。自动兼容常见只读别名；回传历史时不能想当然把所有服务都写成 `reasoning_content`。
- 若服务端返回结构化 `reasoning_details`，先留原始兼容 fixture，再另定映射；不可静默吞掉。
- thinking 不得混进最终正文。`ExtractLastText` 仍只取 TextBlock；失败检查点则可另附“已产生 N 字节思考”与末段摘要。

## 根因三：usage 未报告却显示成 0

当前 custom provider 未声明 `stream_usage`，LubanCode 便不发：

```json
{"stream_options":{"include_usage":true}}
```

vLLM 因而不送末尾 usage chunk。内部计数默认是 0，代理面板又把它画成 `0 tokens`。这句话会误导：像是服务端一枚 token 都没跑，实则模型很可能已经烧满 4096。

### 要改

- usage 数据须有“是否报告”这一位。`not_reported`、真实 `0`、正数三态分清，不能只用一组零初始化整数。
- 未收到 usage 时显示“tokens 未报告”，不要显示 `0 tokens`。
- Chat provider 添加/切换到本地端时，可在用户确认后跑现有 `/doctor cache usage` 探针；支持便写回 `stream_usage=true`。
- vLLM 这类本地端若已在 provider 目录声明支持，直接带 `include_usage`，不必每场重探。
- usage-only chunk 的 `choices` 常为空。parser 必须照收 usage，不能因没有 choices 便丢掉。
- agent 主面板、子代理面板、最终失败摘要与 `/usage` 共用同一份“已报告/未报告”语义。

## 根因四：`max_tokens` 后直接判死

`AgentLoop` 收到非 `tool_use` stop reason 便结束。子代理最后没有 TextBlock，`AgentTool` 随即判 `NoFinalText`。哪怕已有几千 token 思考，也没有续一轮交结论的机会。

### 要改

- 把 `max_tokens` 从普通 end turn 中拆出来，形成明确的 `OutputBudgetExhausted` 状态。main 与子代理共用同一状态机。
- 保住已收到的 TextBlock、ThinkingBlock、tool use 与 usage。不得为了续跑丢掉半截现场。
- 若已有完整 tool use，照常执行并续轮；finish reason 与 block 冲突时仍“信块不信帧”。
- 若只有 thinking、没有正文和工具调用，可按配置续一轮，追加一条宿主标记：要求停止展开思考，先调用下一枚工具或给出简短检查点。它不是用户输入，不得伪装成用户原话。
- 自动续跑须有显式次数与成本账，main 和子代理同值。默认次数要经实测再定，不能从“4096 一刀切”换成“无限自动烧 token”。
- 续跑仍耗尽时，失败页展示实际上限、已续次数、usage 是否报告、thinking 是否收到、最后检查点。给“继续”“提高本场输出上限”“降低/关闭 thinking”“拆小任务”四条去路。
- 若服务端完全没回 usage，也要根据收到的 chunk 类型说明“服务端报 length，token 数未报告”，不能写成 0。

## 同级能力审计

眼下除了输出上限，还有几处子代理专属差异。须逐项决定，默认向同级收拢：

| 能力 | main 现状 | 子代理现状 | 应有规矩 |
| --- | --- | --- | --- |
| 模型/provider/effort | 当前会话配置 | 基本继承，可 detached override | 默认完全继承；override 明示 |
| 输出上限 | 写死 4096 | 另写死 4096 | 共用 runtime profile |
| step 上限 | 配置，默认不限 | 已继承 main，可单任务设 | 保持；override 明示 |
| context window | 配置同步 | 已同步 window tokens | 保持同值 |
| `max_context_chars` | 读 config | 构造时落回环境默认 | 必须继承同一有效值 |
| 结构压缩 | 默认开，可配 | 用 loop 默认值 | 继承同一开关与参数 |
| compact | 主会话可压 | 已有压力点 compact | 共用预算、模型与报告格式 |
| 基础/MCP/LSP 工具 | 主表 | general-purpose 基本有 | 默认同能力 |
| 再派 agent | 有 | 深度硬限 1，子表无 agent | 允许同级派工；用全局并发/深度策略防失控 |
| todo | 有主会话 todo | 无，注释称“短命跑腿” | 子代理有自己的私有 todo，不乱写 main todo |
| ask_user | 可直接问用户 | 无 | 经任务面板/主会话路由提问并等待 |
| 项目记忆召回 | 每轮按 query 召回 | 不独立召回 | 按子任务 query 独立检索，同预算同安全声明 |
| memory 写入 | 按授权挂 `memory_save` | 无 | 同授权；默认写候选，来源记子任务/session |
| worktree | 主代理可管理 | 只能接受派出时隔离 | 能力差异须由隔离策略明示，不因“子”字拿掉 |
| 生命周期 | 会话常驻 | 任务完自动退出 | 这是正常差别 |

### 递归派工不能再靠拿掉工具解决

子代理同级，便应能再拆任务。防止树无限长，要靠一套全局调度策略：

- 全局并发槽，而非每层各算各的。
- 可配置最大活跃 agent 数；默认值公开。
- 深度可观测、可配置。若用户设了上限，派工前明报；默认不因“子代理”身份硬锁为 1。
- 父任务退出时，子任务如何接管、取消或转交，须有明确账。
- 权限和 sandbox 沿继承链只可等同或收窄，不能由子代理放大。
- 每只 agent 有独立 history、todo、usage 与结果；项目 cwd、指令、skills、provider capability 从 runtime profile 继承。

## 架构落点

- `AgentLoop` 不再接一串易漏的裸参数。改吃不可变 `AgentRuntimeProfile`，或至少由统一 builder 构造。
- `InteractiveSession`、`one_shot`、`AgentTool::RunTask`、compact 与 doctor probe 各声明自己覆盖什么，其余全继承。
- 输出预算、上下文、stream usage、reasoning 字段别名属于 provider/model capability，不属于 UI，也不属于“主/子”身份。
- agent 类型只管 persona、任务目标与显式 capability policy。`general-purpose` 默认 full；`Explore` 明写 read-only。
- 工具注册表可分实例，能力表不能靠散落的 `Register`/不 `Register` 暗示。启动 `/doctor` 或 `/agents config` 应能打印 main 与各 agent type 的差异矩阵。
- 删掉“短命跑腿”这类误导注释，换成“独立任务 agent，默认与 main 同能力，完成后退出”。

## 兼容与配置

建议配置外形：

```json
{
  "agent": {
    "max_output_tokens": null,
    "length_continuations": 1
  },
  "subagent": {
    "max_output_tokens": null,
    "max_steps_per_turn": null
  }
}
```

- `subagent` 字段缺失或为 `null` 就继承 `agent`，不能落回编译期魔数。
- 若保留 subagent override，文档须说这是用户主动收窄/放宽，不是默认低配。
- 旧配置没有新字段时，迁移后 main 与子代理取同一默认策略。
- `/config` 展示 effective 值和来源：模型目录、provider、全局配置、项目配置、单任务 override。

## 测试

### SSE fixture

把本机 vLLM 现场缩成固定测试流：

```text
delta.role
delta.reasoning × N
delta.content
finish_reason=stop
choices=[] + usage
[DONE]
```

另补 `delta.reasoning_content`、两字段同时出现、reasoning-only + `finish_reason=length`、usage 缺席四组。

### 预算

- main 与 general-purpose agent 构造出的 effective `max_output_tokens` 相同。
- 改 provider/模型/项目配置后，两者同轮生效。
- subagent override 未写时不出现 4096；显式写时面板与 request 同值。
- thinking 吃满后续跑一次，ThinkingBlock、usage 与前缀账都保住。
- 续跑仍满时给结构化失败，不重复原 prompt，不丢检查点。

### 同级矩阵

- general-purpose agent 能用 main 的基础、MCP、LSP、Skills、记忆召回与私有 todo。
- 子代理能再派 agent；全局并发与显式深度上限生效，不死递归。
- Explore 仍只读，错误说明写“角色限制”，不写“子代理无权限”。
- ask_user 从子代理发出后能在主界面回答，答案只送回发问那只 agent。
- main 与子代理吃同一 provider、think、stream parser、usage 统计和 context policy。

### 真机

- vLLM 0.27.1 + qwen3.8-27b：thinking 开/关各跑一遍短问与工具任务。
- thinking 开时看得到 `delta.reasoning`；关闭时正文正常。
- `include_usage` 开后 token 不再恒为 0；未开时显示“未报告”。
- 用同一长任务分别交给 main 和 general-purpose agent。两者应拿到同一输出上限和能力，不因 agent 身份一边成功、一边被暗限打断。

## 不做

- 不把 `max_tokens` 简单改成另一枚更大的魔数。
- 不把 reasoning 粗暴拼进正文。
- 不因一次 length 就原样重发整份用户 prompt。
- 不把“usage 未报告”继续冒充 0。
- 不拿取消所有安全边界来冒充 agent 同级。
- 不让 Explore 的显式只读限制反过来定义所有子代理。
- 不让子代理共享并并发修改 main 的同一份 todo/history；同级讲能力，不讲共用可变状态。

## 建议落刀次序

```text
1. Chat parser 兼容 delta.reasoning + usage 三态
2. vLLM SSE fixture 与真机回归
3. 统一 AgentRuntimeProfile，清掉两处 4096
4. max_tokens 续跑与结构化失败
5. max_context_chars / compact / capability 继承审计
6. 子代理私有 todo、记忆、ask_user
7. 分层派工与全局并发/深度治理
```

头四步先治这次实错。后三步兑现“子代理与 main 同级”这条产品规矩。
