# LubanCore 与 CLI 分离：可嵌入、可换组件的 Agent SDK

- 状态：设计待实现。本次只完善方案，不代表 SDK、数据库适配或集群能力已经落地。
- 初立：2026-09-01；修订：2026-09-05、2026-09-27（复核当前装配与 SDK 交付顺序）。
- 级别：P1；近期先交付本地 SDK，生产组件按阶段推进。
- 用户目标：别人能调用 LubanCore 写自己的程序；CLI、Web、后台服务、任务编排都接同一套运行能力。默认本地可用，需要数据库、常驻调度或多节点时，可以接入相应组件。
- 范围：宿主 API、组件 SPI、会话隔离、存储合同、调度恢复、并发、生产部署、构建安装与迁移。
- 关联：`多前端外壳与浏览器Tab_AppServer承载设计.todo`、`持久Agent_Gateway接入SessionV3_常驻助理与可靠自动任务总装计划.todo(旧总装单已删,2026-09-17)`、`分布式支持路线图_从单机宿主到集群控制面的分期落地计划.todo`（原集群畅想单已并入其第二十九章）。
- 本地存储合同以 `docs/development/workspace-storage-v2/P0-0-contracts.md` 为准。原单引用的 `Workspace统一存储_旧Session退场与分级Memory迁移.todo` 当前未找到，不再把它当唯一依据。
- 本单统管 SDK 边界与分期。Gateway 单落实常驻、自动任务和恢复；集群单落实节点、租约与控制面。开工相应阶段前同步关联单，消除旧前置与旧描述，不维护两套冲突合同。

## 本轮复核与开工口径（2026-09-27）

用户重新提出 LubanCore SDK 的解耦问题。结论：可以解耦，应正面推进 SDK；远端进程协议负责通信，不能替代“别人链接库即可写自己的宿主”这个目标。已有结果同步与 CI 小批继续独立推进，不替 SDK 定内部接口。

当前基线 `893e2b1a`（集成分支代码基线，路线图提交不计）已经有 AgentLoop、SessionService、SessionRuntime、InteractionBroker 和会话账，不需另写 Agent 执行器。真正要收敛的是工具/后端装配、会话全局状态和公开交付面。下面源码观察未经过本地编译；构建和执行验证全部交远端 CI。

### 本轮建议的主线

1. 先冻结最小 C++ 宿主合同与拥有关系，同时迁出不带 UI 的共用装配。首版默认本地文件账、可注入数据根/资源根、模型后端与工具；不等所有数据库适配、常驻调度或完整集群完成。
2. CLI、one-shot、AppServer 与远端 Worker 逐批接同一工厂和会话 API。宿主仍决定入口、默认策略和展示；共用装配不意味着把 CLI 的默认全工具权限原封不动给远端。
3. 外部小程序只链接安装后的 `LubanCore::Core` 与公开头，就能跑真实会话流程。先过 B，再过 C 的同进程多会话门；一 Session 一 Worker 可用于部署隔离，但不能拿进程隔离替代 SDK C 批验收。

推荐依赖关系：CLI/AppServer/远端 Worker → SDK → 会话服务与中立装配 → Provider/工具/本地存储等适配。Control 的网页、登录、节点登记、心跳、网络连接和实验 Runner 的 OS 托管都不塞进 Core。完整工具结果由 Core 的可信本地调用方使用；远端 preview/full 是节点出网投影策略，不修改 Core 的模型上下文或本地结果真相。

用户当前约束一并生效：已有项目允许多 Session 共用同目录；GPU 占用/分配不纳管；本地不编译；子 PR 先指向集成分支，不直接合 main。

### 三个可验收的小批（归入 A/B/C，不另造一套总排期）

| 小批 | 改什么 | 退出标准 |
|---|---|---|
| SDK-1：中立装配 | 迁出 BuildBackend、基础内置工具注册与拥有型组件装配；诊断由事件返回，不引用 Theme/Spinner/终端输入；默认权限仍由宿主显式提供 | CLI 与 AppServer 共用迁出的实现；MCP/插件失败与释放顺序有测试；全工具覆盖清单逐项记录，未迁完不宣称 B 已完成 |
| SDK-2：窄门面与可安装产物 | 由中立 SessionFactory 拥有会话材料，归并 AppServer/HeadlessExecutor 的共用回合驱动；在 SessionService 等真实服务上封装受控句柄，公开请求/回执/事件/错误并导出独立无终端目标 | 源码树外项目只用安装产物完成假后端＋真文件工具、审批、取消、恢复、查结果、Shutdown；不 include src，不链接 CLI/AppServer，不拉起子进程 CLI 冒充嵌入 |
| SDK-3：跨 Session 状态与宿主迁移收口 | 去进程级 cwd/语言等可变会话状态；定订阅/回调/关闭次序；补各宿主一致性 | 同目录两场、不同目录两场均能并发；模型/权限/上下文/取消不串；慢订阅、在途审批、关闭竞态有远端三平台证据；共享文件冲突不冒充已解决 |

SDK-1 期间 API 讨论和状态盘点可并行，公共头未定前不让多个分支各造 Session 类型。第一版不要求实现所有 SPI，但实际配置、路径、凭据与生命周期的依赖必须通过明确入口传入；不能把整个内部对象图换个公开命名就交付。

## 一、Core 究竟替别人做什么

LubanCore 是可嵌入的 Agent Runtime SDK。调用方交来输入、配置和授权上下文，Core 管一场会话：装配模型与工具、推进回合、申请审批、处理取消、记录事实、交回结果。

别人不必复制 AgentLoop，也不必从几十个内部类开始接线。默认组件先把本地运行接好；需要别种落盘方式、执行环境或调度来源，再替换明确的一层。

三个典型用法：

1. 写 CLI：收输入，调用会话 API，消费事件，画终端。
2. 写 Web 服务：认证后定位会话，提交任务，订阅事件。网页断开是否停任务，由服务策略决定。
3. 写业务程序：把 Agent 运行放进自己的工作流，传入业务工具，拿结构化结果，不必启动 LubanCode CLI。

Core 管执行与状态规则；宿主管入口、连接、进程托管和业务接线；组件实现外部能力。三者各有接口，不能把所有事情塞进一只 Core 类。

“可换组件”的承诺有边界：满足同一合同、通过能力检查的实现可以替换；不能用改一行配置掩盖数据迁移、事务缺口或故障恢复。

## 二、四种接入面

| 接入面 | 使用者 | 合同 |
|---|---|---|
| SDK API | CLI、服务、业务程序 | 创建会话、提交回合、取消、审批、查询、订阅、关闭 |
| 组件 SPI | 存储、执行环境、身份与调度适配作者 | 实现指定能力，通过统一合同测试 |
| 远程协议 | 浏览器、其他语言客户端 | AppServer 或业务服务暴露 HTTP/RPC/事件流，映射到 SDK |
| Agent 扩展 | Tool、MCP、Skill、Package 作者 | 沿用既有扩展合同，不等同于存储或调度 SPI |

首版交付 C++ SDK，明确编译器、标准库、运行库和 C++ 标准要求。同工具链接入，不先许诺跨编译器 ABI 稳定。

浏览器接服务端协议，不直接链接 C++ 静态库。后续按需求做 Python/Node/C ABI 绑定；绑定复用 SDK，不能另养一套执行逻辑。组件首版由宿主显式注册，不要求动态装卸 DLL。

## 三、当前事实与断口

以下按 `893e2b1a` 代码复核，时间为 2026-09-27。只读源码，不把已有注释或 CMake 库名当作 SDK 已交付。

| 已有落点 | 当前断口 | 迁移方向 |
|---|---|---|
| `CMakeLists.txt:1236` 的 lubancode_core | 源文件仍包含 cli 终端逻辑；`:1345` 公开内部 src；`:1609` 只安装主程序，无 LubanCore::Core 安装导出 | 以真实依赖闭包拆目标；旧兼容库不能直接改别名充当 SDK |
| `src/app/session_stack.hpp:20` 起、`:122` | 装配仍依赖 InteractiveSessionOptions、ToolRuntime、SpinnerBackend 与 ContextTracker | 会话材料与 UI 包装分开，中立工厂不认识终端 |
| `src/app/backend_stack.cpp:17` 的 BuildBackend | 当前放 app，主要依赖 api/config | 先迁共用后端工厂，宿主只传已解释配置 |
| `src/app/tool_runtime.cpp:99` 的 BuildBaseToolRegistry、`:470` 的 ToolRuntime | 完整基础工具注册与 MCP/插件装配仍夹 Theme、cout；交互与 one-shot 在用 | 基础注册和拥有型资源迁中立层；诊断通过接口交宿主 |
| `src/app_server/session_assembly.hpp:66` 的 HeadlessMcpRuntime 与 AssembleSession | 与 CLI 的 MCP owner 同构，因 CLI ToolRuntime 拖 Theme 而重复；当前 headless 部署档工具集合较窄 | 复用显式计划、失败回执与拥有关系，与完整内置工具注册合流；原零工具默认仍保留 |
| `src/runtime/session_service.hpp:123` 起 | 已有持久接纳、同键去重、派发/终态与恢复；仍是内部服务 | 窄 SDK 调用面包住真实服务，不再另写提交账 |
| `src/runtime/session_runtime.hpp:19` | 已管身份/账/权限，后端与工具栈仍由宿主装配 | 保留状态实现，交中立会话拥有者统一接线 |
| `src/runtime/headless_executor.hpp:183`、cpp:420 | 借用 backend/registry，执行段混有自动任务/渠道与 reply selection | 提共用回合驱动，不直接把整只 HeadlessExecutor 改名 SDK |
| `src/runtime/event_sink.hpp:23`、`:53` | 生产线程同步推送，Fanout 只 Add，裸指针保活由宿主承担 | 公开可取消订阅句柄，定在途回调、异常与关闭合同 |
| `src/runtime/interaction_broker.hpp:43` | 已有中立审批问询接口 | 沿用，补宿主身份、过期与取消策略，不造第二套审批 |
| `src/runtime/worktree.cpp:701/751/824/857` 与 cli/i18n.cpp | 仍有进程 cwd 和语言状态 | cwd 与语言归会话；同目录可共享，切目录不改变别场状态 |
| workspace/trajectory | 项目身份和 Session 账可分开，同 workspace 多 Session 不要求项目独占 | 保留 V3 同 ID 续写、V2 迁移边界；数据根可注入，不先重做全部存储 |

AppServer、ChannelSessionHost 和 CLI 已复用一部分领域服务，说明接缝存在。它们仍有不同装配方式，不能据此称“统一 SDK 已实现”。

## 四、目标分层

```text
CLI / AppServer / Gateway / 业务程序
                 |
          LubanCore SDK API
                 |
       会话工厂 + Runtime + 领域服务
                 |
       组件 SPI + 能力校验 + 生命周期
                 |
 本地默认实现 / 数据库 / 执行环境 / 外部服务
```

- Core API：公开请求、结果、句柄、错误、事件与能力查询，隐藏内部对象图。
- Runtime：执行规则、权限裁决、会话/回合状态、取消与事实提交。
- 共用装配：后端、工具、策略、资源与会话工厂。默认装配可直接用，高级调用方可覆盖组件。
- 默认适配：本地文件、进程、Provider、MCP 等实现。没有终端依赖，可以有明确的文件与网络 IO。
- 宿主适配：终端、HTTP/WS、渠道收发、系统服务、节点托管。按需链接，不强迫嵌入方开端口。

core 不 include cli/app/app_server；宿主不相互 include。共同代码迁入中立层，不能复制或用新名字掩盖旧依赖。职责先定，不要求每个接口各建一个库。

“纯库”指没有 main、终端控制和隐式进程托管，不指禁止所有 IO。创建 SDK 不得暗中启动 Gateway、调度器、端口或外送任务。

## 五、SDK 最小合同

下列名字是设计占位，不是现存 API。实现时优先收敛已有类型。

| 操作 | 返回与约束 |
|---|---|
| CreateRuntime(options, components) | 校验组件组合、资源与版本，失败列缺项，不悄悄降级 |
| CreateSession / OpenSession | 返回受控句柄，区分新建、接入活会话、恢复历史 |
| SubmitTurn(session, input, request_key) | 返回接受回执与 turn_id；接受不等于完成 |
| CancelTurn(session, turn_id) | 请求取消；晚到请求不误伤下一轮；终态另查 |
| ResolveInteraction(request_id, response) | 带调用身份；过期、重复、越权分别报错 |
| ExecuteCommand / Query | 类型明确，复用 CommandService，不暴露内部任意方法调用 |
| Subscribe(session, cursor) | 返回订阅句柄，声明持久补流能力、游标过期与重同步规则 |
| GetTurnResult / GetSessionSnapshot | 查询终态与快照，订阅不是拿结果的唯一途径 |
| CloseSession / Shutdown | 指定取消或排空与截止时间，返回未收拢工作 |

首版覆盖文本与现有附件形状、工具事件、usage、错误、审批和取消，不另造缩水协议。

RequestContext 带可信调用身份、租户作用域、request_id、trace_id 与截止时间。SessionContext 带逻辑项目/工作区身份、cwd、存储命名空间、配置版本、语言、授权和能力快照。

身份由可信宿主或身份组件提供，不能相信客户端自报 tenant_id。API 与执行层都守权限，前端按钮隐藏不算授权。

错误至少区分参数错、无权限、不支持能力、忙、队列满、版本冲突、超时、取消、不可用、损坏与 NeedsReview。沿用已有错误码体系，冻结前出映射表。

回执说明可否重试与提交状态。超时可能发生在提交之后，调用方凭 request_key 查询，不能盲目重开回合。幂等键按租户、目标、操作划范围，并校验请求摘要；同键不同内容报冲突。公布去重保留窗口。

## 六、状态、线程与对象寿命

### 6.1 状态分层与并发

- Runtime 管组件实例、线程池、共享只读数据和进程总预算。
- Session 管 history、权限、配置快照、cwd 和单写所有权，不等同于网页连接。
- Turn 管一次执行、取消令牌、预算、工具调用和终态。
- Subscription 管观察者与游标；断开是否取消任务，由显式策略决定。
- Durable Job 管持久工作项，不以线程句柄充当任务身份。

同一 Session 的 main turn 单飞。忙时排队或拒绝由创建策略定死。同场命令、切模型和切目录也经串行状态入口，在回合边界应用快照。

跨 Session 有界并发。限制本系统活跃会话、排队数、模型请求、工具进程与后台任务；排队也计容量。闲置会话不永久占工作线程。GPU 占用、分配与调度不由本系统管理。

同一已有项目可开多场 Session，各场 cwd 可以相同；独立的是会话上下文、权限状态和操作账，不是强制独占目录。仅同一 Session 禁止两只活写者，项目索引用短事务锁；共享文件并发修改的冲突另按工具合同处理。

共享只读数据可进程缓存；凭证、语言、cwd 与可变连接状态按范围隔离。子代理明确快照、预算与取消传播。工具不得读写进程 cwd 来表达会话状态。

### 6.2 关闭合同

顺序：拒绝新工作 → 取消或排空 → 收拢执行器与子任务 → 提交终态/刷账 → 解除订阅 → 释放组件。

组件声明 owned 或 borrowed。借用对象至少活到 Shutdown 完成；异步句柄不得悬挂引用。解除订阅提供等待在途回调结束的办法，禁止回调阻塞等待自身退出。

不强杀任意 C++ 线程。需要硬时限的工具交受监督子进程或隔离 Worker；超时未收拢返回明确状态，不能释放仍被访问的对象。正常路径显式 Shutdown，析构只作兜底。

### 6.3 事件与回调

- 分开命令回执、UI 增量、持久事实与最终结果，各有身份和顺序。
- Session 内持久事实有单调位置，不要求跨 Session 全局排序；UI seq 与持久 cursor 必须有明确映射。
- 公开头说明回调 executor、可否重入 SDK、异常隔离与取消竞态。
- 慢消费者用有界队列；UI 增量可合并，终态和审批不得静默丢弃。溢出报告 gap、断订阅或背压，再按持久游标补流。
- 持久模式先提交事实再通知；通知失败不回滚事实，不重做工具。
- 日志走独立诊断口，stdout 不混协议外文字；默认脱敏，不泄漏凭证或其他租户内容。

## 七、组件 SPI：按语义拆，不做万能 KV

| 组件 | 责任 | 默认或后续实现 |
|---|---|---|
| SessionPersistence | 元数据、事实追加、版本校验、去重与恢复读取 | 本地文件；后续 SQL |
| SessionQuery | 列表、搜索、分页与投影进度 | 本地索引；数据库索引 |
| ArtifactStore | 大文本、图片、工具产物，稳定引用与哈希 | 本地目录；对象存储 |
| WorkspaceResolver / ExecutionEnvironment | 逻辑工作区映射到本机文件与执行边界 | 本地路径；worktree；容器/远程 Worker |
| ProviderFactory / ToolFactory | 按会话配置创建后端与工具 | 复用现有 Provider、工具、MCP |
| Policy / SecretResolver | 授权、审批规则、按范围解析凭证 | 本地配置；业务身份与密钥服务 |
| WorkQueue / AutomationStore | 持久入队、领取、计划与触发记录 | 单机实现；外部队列/数据库 |
| OwnershipCoordinator | 跨进程领取、续租、失效与 epoch | 单机锁；具备 fencing 合同的实现 |
| Clock / Executor / Diagnostics | 时钟、执行资源、诊断与指标 | 默认实现；测试时钟；宿主执行池 |

不要求第一期交付全部组件。先抽真实边界，用第二种实现检验抽象，不给每个内部类加虚接口。

组件描述符声明 SPI 版本、线程模型、能力、持久级别、事务范围与资源需求。组装时校验：要求崩溃恢复却装内存队列，必须失败；要求集群写入却没有 fencing，必须失败。

配置只选已注册组件，不任意加载配置给出的动态库。Start/Health/Stop 有期限；启动半途失败按逆序清理。运行中切存储、执行器或所有者不是普通热配置。

## 八、从本地目录切到数据库

### 8.1 身份与位置分开

逻辑层分别表达 ProjectId、WorkspaceId、SessionId、TurnId。Project 管业务归属，Workspace 管执行工作区，Session 管会话。具体类型须与既有 workspace_key/session id 对照，不另造平行身份。

本地适配保留当前身份裁决与目录布局。云端工作区用显式稳定身份，由 resolver 映射节点目录；某台机器的绝对路径不能直接充当跨机器身份。历史 ID 通过版本化映射迁移，不静默重算。

Session 不再以目录路径作为唯一访问入口。read/write/shell 仍经执行环境访问文件；会话存进数据库，不等于代码与工具执行也进数据库。

### 8.2 唯一事实账

Trajectory 继续承担运行事实账的语义。本地后端的 canonical ledger 仍是现有 main.jsonl；数据库模式由事务提交的事件记录承担同一职责，JSONL 成为按既有格式导出的交换产物。

这是后续显式演进，首期分离不得偷换。事件信封、哈希链、子代理关系、verify、replay、resume 与训练导出须有兼容测试。索引与快照是派生数据，注明覆盖位置，可从事实重建。

禁止本地账与数据库账同时各自认作权威。每场会话钉后端与 schema 版本；运行中不改连接串来“热迁移”。

### 8.3 存储合同

不能只提供 Save/Load，至少定义：

- Create：作用域内唯一，重试可查原结果。
- AppendBatch：校验 expected_revision、writer_epoch 和幂等键，返回提交位置；冲突不部分提交。
- ReadAfter：有序分页，游标过期、损坏、无权限分别报错。
- CommitTransition：需同时成立的状态、事实与 outbox 意图放进一个提交边界，或采用可验证的恢复日志协议。
- Snapshot：标明 schema 与覆盖位置，不越过尚未提交的事实。
- Archive/Delete：明确活会话限制、保留期、产物引用和幂等行为。

各后端写明 durable 含义：刷盘策略、已确认写的崩溃保证、部分尾记录恢复。文件后端不能凭 rename 宣称支持跨文件事务；不支持的组合在启动时拒绝。

Artifact 先写并校验，再提交引用；孤儿用保留窗口与 GC 清理。不能先提交事实，再留下永远读不到的产物。跨存储不假设天然分布式事务。

列表投影允许延迟时返回投影位置。权限、租约与写后确认不得读滞后副本；业务不能靠缓存判断“这轮是否已经执行”。

### 8.4 切换与运维

停接新写并排空 → 导出清单/事实/产物 → 导入 → 校验 ID、数量、哈希、父子边与回放结果 → 记录切换版本 → 单点切写 → 验证读回。

切换前失败仍读原后端。切换后已有新写，回滚须反向迁移或恢复方案，不能直接改回配置。首版不做在线双写迁移。

数据库后端带建表、schema 升级、连接池、超时、备份恢复和合同测试。首种数据库在阶段 D 按事务与部署需求选定，不在本单凭名字拍板。

## 九、Redis 与主从各管什么

Redis 可作缓存、唤醒通知、限流或队列适配候选。用途分别声明合同，不能因装了 Redis 就宣称会话持久、多节点可靠或任务只跑一次。

缓存不是事实账；瞬时通知不是可补读事件；带确认与重投的队列仍须消费去重。若要用 Redis 存持久状态，另验持久、切换和一致性要求。

| 需求 | 谁负责 | 硬约束 |
|---|---|---|
| 数据库主写副本读 | 数据库适配与部署 | 复制延迟、写后读、故障切换、RPO/RTO |
| 调度主备 | Scheduler + OwnershipCoordinator | occurrence 去重、原子领取、换主后扫回漏活 |
| 控制面与多 Worker | 集群宿主 | 按 Session 分配所有者，不同 Session 分布执行 |

不要求整套系统只跑一个主 Worker。一个 Session 同时只有一个有效写者，许多 Session 可以分散执行。

租约不能只做“拿到锁就开跑”。存储提交校验 epoch，阻止旧 Worker 断网后继续写。租约失效后停止接新活并请求取消；账本 fencing 挡不住已在执行的外部副作用，还须执行环境隔离、下游幂等或人工裁决。

租约与存储分属不同系统时，说明 epoch 如何可信地进入写校验；未解决前不交付该组合。锁服务连不上默认停写，不能各节点自行当主。

## 十、定时任务与持久 Agent

目标是可选 PersistentHost 组合：常驻宿主 + 持久计划 + 工作队列 + 会话执行 + 恢复 + outbox。普通 CLI 不因安装组件就变成后台服务。

```text
Cron / 定时 / Webhook / 渠道消息
              |
   durable occurrence / ingress
              |
     有界 WorkQueue + 领取
              |
     SDK SubmitTurn / 查询结果
              |
   事实提交 + durable outbox
              |
         回复/通知交付
```

区分 ScheduleId 与 OccurrenceId，每次触发有稳定去重键。明确时区、夏令时、错过触发的 skip/coalesce/catch_up、追赶上限、重叠策略、暂停与取消。

触发记录与入队原子提交，或用事务 outbox 补投。领取有可恢复状态，崩溃后能找回；进程内 timer 只负责唤醒，不作唯一事实来源。

队列可重投，消费端幂等。工具副作用分只读、可幂等重试、需补偿、未知结果四类。执行后未落结果的未知动作进入 NeedsReview，不承诺端到端 exactly-once。

模型重试也可能重复计费，需限制 deadline、退避、次数和预算。回复发送失败只重试 delivery，不重跑 Agent。

分清断线重订阅活会话、从历史恢复为新运行、崩溃后恢复持久工作项。现有 resume-as-new 保持兼容，新恢复协议另定，不能都叫 OpenSession 后暗中猜测。

恢复不保存 C++ 调用栈。检查事实、checkpoint、所有权、凭证与能力版本，再决定继续、重试或等待审核。MCP 连接、浏览器页和后台子进程不是天然可恢复资源。

计划声明固定能力版本还是运行前重取配置；执行时重新校验授权，权限撤销即拒绝。Secret 按引用在执行边界解析，不写进计划正文、模型上下文或日志。

## 十一、生产运行要求

- 隔离：tenant/project/session 作用域贯穿查询、事件、artifact、审批与工具；列表和删除不能只凭 session_id 放行。
- 资源：线程池、队列、连接池、模型并发、工具进程、产物大小、token/费用预算有上限；排队与拒绝原因可观测。
- 故障：区分暂时不可用、永久错和提交结果未知；重试有预算与幂等范围，下游故障不拖垮全部会话。
- 健康：liveness/readiness 分开。排空、存储不可写、租约失效时撤销相应就绪能力。
- 观测：结构化日志、trace、排队耗时、回合耗时、错误率、提交耗时、事件滞后、资源占用；SessionId 不作默认高基数指标标签。
- 运维：优雅停机、滚动升级、协议/schema 兼容、备份恢复、保留和删除均须演练。
- 安全：审批管授权，沙箱/容器管执行约束；权限声明不等于 OS 隔离。远程宿主负责 TLS、认证与网络暴露。
- 容量：报告机器规格、模型/工具负载、活跃与闲置会话数、吞吐、P95/P99、错误率和内存曲线。压测前冻结目标值，不凭线程数宣称高并发。

## 十二、构建安装与兼容

公开头收在 `include/lubancore/`，导出 `LubanCore::Core` 和 find_package 配置。内部 src 与生成目录不能 PUBLIC 泄漏；公开第三方类型若确有需要，列明版本约束。

提供 Core-only 构建，关闭 CLI/AppServer/Gateway 后仍可构建与安装。默认组件声明依赖；终端原语与显示库不进 Core 传递链接闭包。

资源目录、ripgrep、skills、提示词通过 ResourceLocator/装配选项定位。本地适配保留发行布局，嵌入方不必把资源塞到自己 exe 旁。

API、SPI、远程协议、存储 schema 分别版本化。公开变更给迁移说明与弃用期，内部类不承诺 ABI。首期不拆 Git 仓库，不顺手重写无关模块。

首期保持 CLI 命令、默认配置优先级、wire 协议和文件格式语义。必要内部改造可以做，列明差异，不再要求“内部一行行为不变”。

终端快照固定宽度、语言、时间与随机 ID；键位、审批、取消、退出码、stdout/stderr 分流另测，不拿时序噪声下逐字节比对当唯一门。

## 十三、分期与交付门

### A：盘点与合同冻结

- [ ] 按 target 列源文件、传递 include/link、公共头、终端与资源依赖。
- [ ] 列全局状态、cwd/home/env、单例、线程、借用指针与析构次序，每项定归属。
- [ ] 冻结 API/SPI、身份映射、状态图、错误与能力矩阵。
- [ ] 同步关联单：集群硬前置改为本单 B/C 的 SDK 基线，后续合同分别对齐 D/E/F，避免互等最终验收。
- [ ] 记录改造前构建、测试和 CLI 基线，区分历史失败与本批回归。

验收：每个断口有归宿，不用“实现时再看”代替所有权、事务或线程合同。

### B：本地 SDK 与共用装配

- [ ] 抽 SessionFactory 与工具/后端装配，移出 Theme、Spinner、终端 IO。
- [ ] CLI、one_shot、AppServer 共用装配，领域命令不复制。
- [ ] 默认本地组件与资源可注入，保留现有文件账语义。
- [ ] 公开头、安装包与 Core-only 构建落地。
- [ ] 源码树外示例只用安装产物，完成假后端工具调用、审批、查询终态、取消与关闭。

验收：新增宿主不 include 内部 src、不链接 CLI、不复制运行栈。可称本地 SDK 可嵌入，不称生产多会话就绪。

### C：隔离与并发基线

- [ ] 工具与 worktree 不改进程 cwd；配置、语言、权限、凭证隔离。
- [ ] 同场串行、跨场有界并发、背压、预算与截止时间落地。
- [ ] 生命周期、订阅解除、回调异常、慢消费者和关闭期间审批有合同测试。
- [ ] 同一已有目录开两场，以及不同目录开两场；分别使用不同模型、语言和权限并发，切目录、取消、关闭互不串状态。共享目录模式不自动复制项目，不加项目运行长锁。
- [ ] Windows 与至少一种 POSIX 环境验证，测真实工具进程回收，建立并发容量基线。

验收：可称同进程多会话 SDK 基线，不外推多租户安全或集群高可用。

### D：第二种存储与替换证明

- [ ] 冻结事务、幂等、cursor、snapshot、artifact 与投影合同。
- [ ] 选择并交付一种真实数据库后端，内存 mock 不算数据库验收。
- [ ] 文件与数据库运行同套合同测试，verify/replay/export/resume 语义一致。
- [ ] 演练停写迁移、损坏、提交响应丢失、并发冲突、断连与备份恢复。
- [ ] 示例只换组件配置即可创建数据库会话；旧数据迁移另走明确命令。

验收：可称存储可替换，不要求一次支持所有数据库，Redis 不作必选依赖。

### E：单机持久宿主

- [ ] 与 Gateway 单共用 Automation/Ingress/Outbox 合同，补队列与恢复接线。
- [ ] 测定时、重投、错过触发、无人审批、撤权与 delivery 重试。
- [ ] 在领取、工具执行、提交、通知前后杀进程，核对恢复结果。
- [ ] 网页断开与任务寿命解耦，普通 CLI 默认行为保持原样。

验收：可称单机持久 Agent 组合；触发、会话、未知副作用与交付结果均可查账。

### F：生产与多节点

- [ ] 集群单落实身份隔离、epoch、Worker 托管、容量与排空。
- [ ] 按需接数据库主从、调度主备、Redis 适配，各有能力检查与运维说明。
- [ ] 演练脑裂、旧 Worker 晚到提交、重复消息、主库切换、网络分区与滚动升级。
- [ ] 冻结并验证 SLO、RPO/RTO、租户限额与容量报告。

验收：只按实测拓扑声明生产能力。跨节点热迁移、透明分布式事务、任意工具 exactly-once 不在默认承诺内。

## 十四、最终对账

- [ ] 用户照指南便能写自己的 CLI、服务或业务调用，不先读完内部源码。
- [ ] 本地默认可用，数据库、调度、远程执行与协调按能力接入。
- [ ] 换组件不改 AgentLoop，不兼容组合启动时拒绝。
- [ ] 每种模式只有一份权威事实账，导出与投影不争真值。
- [ ] 同场单飞、跨场受限，审批、取消、提交与关闭均有可查状态。
- [ ] 依赖守门、Core-only、安装后外部消费、相关 focused 与完整回归通过，列明平台及跳过项。
- [ ] B/C/D/E/F 分别列证据，不把文档完成、单测通过、真机恢复与集群压测混作一项。

根本判据：调用方只需管自己的入口与业务。LubanCore 提供可调用的运行能力，组件承接外部系统，合同守住替换前后的行为。
