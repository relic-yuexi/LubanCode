# 自定义 Agent 与 Prompt Profile 设计

- 状态：阶段 0/1/2/3/4/5 已落地（0.26.77/0.26.81/0.26.88/0.26.95/阶段 4 本笔/阶段 5 本笔），阶段 6 待做
- 日期：2026-08-28
- 范围：Agent 定义、Prompt Profile、加载与解析、运行时接线、命令与诊断、示例迁移
- 不在本单：实现新的 MCP 服务、重写 Skill 格式、建立 solutions 运行时概念、引入多 Agent 编队

## 一、先定边界

LubanCode 里已有几套东西。各管一摊，不要混账。

| 名称 | 管什么 | 不管什么 |
|---|---|---|
| Agent | 谁来做、带什么上下文、用什么模型和工具、受什么限制 | 不实现工具，不收藏大段通用教程 |
| Prompt Profile | Agent 怎样拼系统提示词 | 不调度步骤，不授予权限 |
| Skill | 某类事怎样做，可带说明、脚本、模板和资料 | 不占一套独立上下文，不决定模型 |
| Workflow | 先做什么、后做什么、何时分支 | 不另造一套 Agent 运行时 |
| Plugin | 可执行工具组件，承载 native、Lua 或 process 工具 | 不打包 Agent、Skill、Workflow |
| Package | 安装与分发单位，可收纳 Agent、Prompt、Skill、Workflow、Plugin、MCP | 不参与执行，不授予权限 |
| MCP | 把外部工具接进来 | 不决定谁能调用工具 |

关系如下：

```text
Package -> Skill -------------+
       -> Plugin tools -------+
       -> MCP tools ----------+----> Agent Definition
       -> Prompt Profile -----+
Model catalog ----------------+
                               |
                               v
                         Resolved AgentProfile
                               |
                         Agent + AgentLoop
                               |
                  AgentTool / Workflow Executor
```

一句话：Agent 选能力，也选 Prompt；Skill 教它办事；MCP 给它工具；Workflow 排它出场。

`solutions` 可以留作教程、配方、样例集合。它不该进运行时类型系统。

Package 外壳另见 `todos/统一Package封装与组件挂载系统设计.todo`。

## 二、外部参照

### Claude Code

Claude Code 把自定义 subagent 写成 Markdown 加 YAML frontmatter。Agent 有独立上下文，可选模型、工具、权限、Skill、MCP、记忆和隔离方式。

可取之处：

- Agent 有名字和描述，主 Agent 可据描述派活。
- 工具、Skill、模型、权限都能按 Agent 收窄。
- 用户级与项目级 Agent 分层加载。
- 插件也能带 Agent，但插件 Agent 不可夹带任意 MCP 配置和权限钩子。

不照搬之处：

- Claude 把正文当 Agent system prompt。LubanCode 已有一套可拆、可换、可分层覆盖的 Prompt Library，不能压扁成一段正文。
- LubanCode 的 Agent 文件应当薄些。它引用 Prompt Profile，不吞掉 Prompt 系统。

资料：<https://code.claude.com/docs/en/sub-agents>

### Codex

Codex 用 `.codex/agents/*.toml` 定义自定义 Agent。定义里可放模型、推理强度、沙箱、MCP 与 Skill 配置。它把 Agent 文件当配置层，不当工具包。

可取之处：

- Agent 本质上是一层运行配置。
- 项目级与用户级配置分开。
- 内置 Agent 与自定义 Agent 走同一调度入口。

不照搬之处：

- Codex 主要靠 `developer_instructions` 承载提示词。LubanCode 要保住模块化 Prompt 装配。
- LubanCode 已用 YAML 解析 Workflow，也已引入 `yaml-cpp`。此处无需再添 TOML 解析器。

资料：<https://learn.chatgpt.com/docs/agent-configuration/subagents>

### OpenAI Skills 与 Plugins

OpenAI 把 Skill 当可复用流程包。Skill 可声明 MCP 工具依赖。Plugin 则是安装包，可带 Skills 和 MCP。Skill 里的 `agents/openai.yaml` 只是 UI 与依赖元数据，并非自定义 Agent。

这恰好印证本单边界：Agent、Skill、Plugin、MCP 不该互相冒名。

资料：

- <https://learn.chatgpt.com/docs/build-skills>
- <https://developers.openai.com/plugins/concepts/plugins>

## 三、LubanCode 眼下有什么

### Prompt 已经分层

`src/prompts` 眼下分为：

- `core/`：身份、工作法、写作风格。
- `features/`：文件、Shell、委派、TODO、Skill、Web、MCP、LSP 等能力说明。
- `platforms/`：Anthropic、Responses、Chat Completions 差异。
- `modes/`：default、plan 等宿主管控模式。
- `tools/`：按语言保存工具描述与 schema 文本。
- `models/`：只留说明；模型专属指令来自 `models.json`。

`PromptAssembler` 已有两条要紧规矩：

1. 用户 Prompt 文件可逐模块覆盖内置文件。
2. `system_prompt.md` 或 persona 可替换 core，但 environment、feature、platform、mode 等段仍由宿主接上。

此外，model instructions、deferred tool index、Soul 还会在请求前继续拼接。Agent 设计不可打乱这条次序。

### Agent 运行时已经成形

`AgentProfile` 已装着 provider、request、runtime、system prompt、model instructions、Soul、工具过滤器等信息。`AgentTool` 已能开独立上下文，跑前台或后台，也能用 worktree。Workflow 的 `AgentExecutor` 又复用了同一 Agent 引擎。

眼下缺的不是第二套执行器。缺的是：

- 可落盘的 Agent Definition。
- 可分层的 Agent Catalog。
- 能保住现有 Prompt 自由度的 Prompt Profile。
- 一处统一的 AgentProfile 解析与构造入口。

## 四、文件格式

### 4.1 每个 Agent 一份 YAML

推荐：

```text
~/.lubancode/agents/browser-tester.yaml
<project>/.lubancode/agents/browser-tester.yaml
```

不要再套一层 `browser-tester/agent.yaml`。Agent 自己不该收藏脚本和资料；那些东西归 Skill 或 Plugin。单文件便于列举、覆盖、校验和展示来源。

首版 schema：

```yaml
schema: 1
name: browser-tester
description: Inspect a web application, reproduce UI failures, and report evidence.

prompt:
  profile: browser-tester
  project_instructions: inherit
  soul: inherit

model:
  role: inherit
  effort: inherit

skills:
  preload:
    - browser-testing

tools:
  allow:
    - context_read
    - todo_read
    - todo_write
    - mcp__browser__navigate
    - mcp__browser__screenshot
  deny:
    - shell

mcp_servers:
  - browser

requires:
  tools:
    - mcp__browser__navigate
    - mcp__browser__screenshot

runtime:
  max_steps_per_turn: 24
  execution_mode: auto
  isolation: none

permissions:
  mode: read_only
```

字段规矩：

- `schema`：必填，只收 `1`。
- `name`：必填，使用小写 kebab-case；它是稳定 ID。
- `description`：必填。写何时派它出场，不写宣传话。
- `prompt.profile`：可省。省了就继承父 Agent 的 Profile 或落回 `default`。
- `project_instructions`：只收 `inherit`、`omit`。
- `soul`：首版只收 `inherit`、`off`。不要在 Agent 文件里另塞 Soul 正文。
- `model.role`：引用现有模型角色。`inherit` 沿用父 Agent。
- `model.effort`：引用现有推理强度；不得越过 provider 能力。
- `skills.preload`：启动 Agent 时把 Skill 正文装进上下文。
- `tools.allow`、`tools.deny`：只收完整工具名。首版不做 glob。
- `mcp_servers`：只引用已配置、已信任的服务名。不得内联 command、args、env。
- `requires`：只作启动前检查。缺项就报错，绝不悄悄放宽工具。
- `runtime`：复用现有 AgentRuntimeProfile 概念。
- `permissions.mode`：只能比父 Agent 更窄，不能更宽。

首版不收 Markdown 正文，也不收 `developer_instructions`。临时提示词仍可由调用任务传入；稳定提示词放 Prompt Profile；可复用做法放 Skill。

### 4.2 严格解析

用 `yaml-cpp`。不要拿 Skill 那套简易 frontmatter 扫描器来凑。

解析器须做到：

- 未知字段报错，不可静默吞掉。
- 类型不合报文件、行、列。
- 同层重名报冲突来源。
- 名称与文件名不一致时，以 `name` 为准，但给 warning；日后可升为错误。
- 数组去重，同时保住原次序。
- 所有路径都经规范化与越界检查。
- 诊断里打印来源层，不打印密钥和环境变量值。

## 五、Prompt Profile

### 5.1 Profile 不是一段字符串

新增概念 `PromptProfile`。它是一组稀疏覆盖。它沿用 `src/prompts` 现有模块树，只改自己点名的文件。

内置 Profile：

```text
src/prompts/profiles/browser-tester/core/10-identity.md
src/prompts/profiles/browser-tester/features/web.md
```

用户覆盖：

```text
~/.lubancode/prompts/profiles/browser-tester/core/10-identity.md
~/.lubancode/prompts/profiles/browser-tester/features/web.md
```

项目覆盖：

```text
<project>/.lubancode/prompts/profiles/browser-tester/core/10-identity.md
```

没有 `profiles/default/` 也能跑。现有 `src/prompts/{core,features,platforms}` 就是隐式 `default`，老配置不受影响。

### 5.2 解析次序

同一模块按下列次序找。越往后，权越大：

```text
内置 default 模块
-> 用户全局 default 覆盖
-> 内置选中 Profile 覆盖
-> 用户选中 Profile 覆盖
-> 项目选中 Profile 覆盖
```

`system_prompt.md` 与 CLI persona 仍可替换 core。它们只替 core，不抹掉动态环境、有效能力说明、platform、mode、model instructions、tool index 和 Soul。

拼装总次序须写成测试，不可只写在注释里：

```text
resolved core/persona
-> runtime environment
-> project instructions（若继承）
-> effective feature modules
-> platform module
-> host-owned mode module
-> deferred tool index
-> model instructions
-> Soul（若启用）
```

### 5.3 哪些可以换，哪些不能换

Agent Profile 可以换：

- core 模块。
- feature 模块文案。
- platform 文案。
- 是否继承 project instructions。
- 是否启用 Soul。

Agent Profile 不可以换：

- `modes/`。Mode 是宿主策略，不能让项目 Agent 改写。
- 模型能力与模型专属指令。它们仍从 `models.json` 和 provider 解析。
- 工具 schema。它归工具注册表。
- 权限判断、沙箱、审批、步数硬上限。

### 5.4 只给有效能力配说明

当前 PromptAssembler 会常驻装入 files、shell、delegation、todo 等 feature 文案。自定义 Agent 一旦裁掉工具，这样便会说一套、做一套。

实现 Agent 时，新增 `PromptCapabilities`。它从过滤后的有效 ToolRegistry 推导：

```text
effective tools -> PromptCapabilities -> feature modules
```

例：Agent 没有 Shell，就不装 shell feature；没有 `agent` 工具，就不装 delegation feature；没有 todo 工具，就不装 todo feature。

这条只先用于自定义 Agent。验证稳了，再决定是否收拢主 Agent 的旧逻辑。不要借本单顺手大改主 Prompt。

### 5.5 保留来源账本

不要只产出最终字符串。解析结果须带一份 `PromptSourceLedger`：

```text
core/10-identity.md <- project profile browser-tester
core/20-workflow.md <- user global default
features/web.md     <- embedded profile browser-tester
platforms/responses.md <- embedded default
modes/default.md    <- embedded host policy
```

这份账本供 `/agent inspect`、`/prompt`、日志与测试使用。出了 Prompt 覆盖问题，一眼便能看见是谁压了谁。

## 六、运行时接法

### 6.1 新增四个职责

建议落下这些类型。命名可随代码风格调整，职责不可混：

```text
AgentDefinition       只装解析后的 YAML
AgentCatalog          扫描、分层、去重、列举、刷新
PromptProfileResolver 解析 Prompt 模块和来源账本
AgentProfileResolver  合并父上下文、定义、模型、工具、权限，产出 AgentProfile
```

可以再放一层 `AgentFactory`。它只做实例化，不重算业务规则。

核心调用链：

```text
AgentTool / Workflow AgentExecutor
        -> AgentCatalog::Resolve(name)
        -> AgentProfileResolver::Resolve(parent, definition)
        -> AgentFactory::Create(resolved_profile, wiring)
        -> Agent
```

一条业务规矩只许有一处权威。工具过滤、Prompt 拼装、模型继承、权限收窄，都不可在 `AgentTool` 与 Workflow 各写一遍。

### 6.2 内置 Agent 也进 Catalog

眼下 `agent_type` 只认 `general-purpose` 与 `Explore`。实现后，把二者注册成内置定义，再叠加磁盘定义。

加载优先级：

```text
project > user > builtin
```

同一层重名直接报错。跨层同名属于显式覆盖，`inspect` 须列出被盖住的来源。

### 6.3 动态工具 schema

`agent` 工具的 `agent_type` 不再写死两项。它从 AgentCatalog 取可用 Agent 名称与描述。

若定义太多，别把几十段全文都塞进 schema。首版只放 `name + description`。日后再做 deferred catalog。

### 6.4 Workflow 复用同一解析器

Workflow 节点可写：

```yaml
type: agent
agent: browser-tester
task: Reproduce the reported checkout failure.
```

Workflow 只负责调度。它把 `agent` 名交给同一 AgentProfileResolver。不要在 Workflow compiler 里再拼 Prompt、再筛工具。

## 七、加载范围与信任

扫描三层：

```text
<embedded resources>/agents/*.yaml
~/.lubancode/agents/*.yaml
<project>/.lubancode/agents/*.yaml
```

项目根目录须复用现有项目配置或 instruction 发现规则。不要各自猜 cwd。

安全规矩：

- 项目 Agent 只能引用已启用、已信任的 Plugin、MCP 和工具。
- Agent 文件不得内联 MCP 启动命令、环境变量或密钥。
- 子 Agent 的权限、工具、沙箱只能继承或收窄。
- `allow` 点到一个父 Agent 没有的工具，不算授权；解析时报清楚。
- 依赖缺失时，Agent 标为 unavailable；调用时报缺什么，不能退化成全工具 Agent。
- Prompt Profile 只能改模型所见文字，不能改宿主 ModePolicy 与权限门。
- 每次解析都保留定义来源、Prompt 来源、有效工具与有效权限，方便审计。

首版不做自动热重载。启动时加载，另给显式 reload 命令。这样好查，也少竞态。

## 八、命令与可见性

至少补齐：

```text
/agents
/agent inspect <name>
/agent doctor <name>
/agent reload
```

`/agents` 显示：

- 名称、描述、来源层。
- 是否可用；不可用则写第一条原因。
- 模型角色与 Prompt Profile。
- 有效工具数、预装 Skill 数。

`/agent inspect` 显示：

- 原始定义路径。
- 覆盖链。
- 最终模型、effort、runtime、isolation、permissions。
- 工具 allow/deny 后的最终名单。
- Skill 与 MCP 依赖。
- PromptSourceLedger。

`/agent doctor` 做静态预检：

- Agent schema。
- Profile 是否存在。
- Skill、MCP、工具是否存在且已启用。
- 模型角色是否能解析。
- 权限是否越界。
- Prompt 模块是否越过允许根目录。

`/agent reload` 原子替换 Catalog。新目录有错，就保留旧 Catalog，并把错报出来。

## 九、实现次序

### 阶段 0：契约先落纸

- [x] 写 `docs/reference/agents.md`，收录术语、schema、覆盖顺序、安全边界。（todo 钦定顶层 `docs/agents.md`，因仓库法顶层只留 README.md，落 reference/。）
- [x] 加一份完整 Agent YAML，一份最小 Agent YAML，一份故意出错的诊断样例。
- [x] 把本单中的字段名与现有 `AgentProfile`、`AgentRuntimeProfile` 对齐。
- [x] 定下 schema version 与兼容策略。

验收：实现者、Reviewer 只看文档，能说清 Agent、Skill、Workflow、Plugin、MCP、Prompt Profile 各管什么。

### 阶段 1：Definition 与 Catalog

- [ ] 新建 AgentDefinition 数据结构与 yaml-cpp 解析器。
- [ ] 实现 builtin、user、project 三层扫描。
- [ ] 做稳定排序、重名检查、来源记录与 unavailable 状态。
- [ ] 把 `general-purpose`、`Explore` 登进 Catalog，先不改行为。
- [ ] 加 `/agents` 与 `/agent doctor` 的只读骨架。

验收：不启动模型，也能列全 Agent，指出每份定义从哪里来、哪里错了。

### 阶段 2：Prompt Profile

- [x] 为 PromptAssembler 加 Profile overlay roots。（内置 `src/prompts/profiles/<名>/` 嵌进 kProfileModules；用户 `~/.lubancode/prompts/profiles/<名>/`；项目 `<项目根>/.lubancode/prompts/profiles/<名>/`。同一模块按契约 §6.2 五层次序解析。）
- [x] 引入 PromptSourceLedger。（`AssembleSystemPrompt(options, &ledger)` 逐段记账；`BuildPromptProfileLedger` 整表记账供 `/agent inspect`。）
- [x] 明定 core/persona、feature、platform、mode、model instructions、Soul 次序。（九段次序写成断言：test_prompt_profile.cpp"拼装总次序"，含 deferred tool index。）
- [x] 从过滤后工具表推导自定义 Agent 的 PromptCapabilities。（`DerivePromptCapabilities` 吃 allow/deny 过滤后的有效工具名；feature 段只描述有效工具，web/mcp/lsp 不再吃父会话开关。）
- [x] 保住现有 default Prompt 输出；未选 Profile 时，黄金测试不得无故变化。（黄金基线测试 + prompt_profile_driver 字节级 diff，基线与改后零 diff；CTest 224 册全绿。）
- [x] 扩充 `/agent inspect` 或 `/prompt`，打印来源账本。（`/agent inspect <名字>` 落地：定义来源、覆盖链、prompt 三笔开关、逐模块来源账本；doctor 的 Profile 行同步查覆盖存在性。不新增命令。）

验收：改一个用户 Profile 文件，只影响点名 Agent；删掉覆盖后，稳稳退回下一层。

### 阶段 3：统一 AgentProfile 解析

- [x] 实现 AgentProfileResolver。（src/agent/agent_profile_resolver.*:父皮+定义+环境账 -> ResolvedAgentProfile,合并次序六步写死在头文件;AgentTool 派发与 Workflow 绑定共用,散在 agent_tool 的手工合并尽数收编。）
- [x] 合并父 Agent、Definition、模型角色、Prompt、Skill、工具、MCP、权限与 runtime。（预算四字段按"YAML 显式 > 父值"并流,steps 按"入参 > YAML > 配置默认";模型角色照现有回落链 normal ?? 父、cheap ?? normal、lao ?? normal;prompt 三笔、execution_mode/isolation 缺省档、permissions 一并决议。）
- [x] 所有权限按“只可收窄”校验。（宽窄序 confirm < auto < yolo;比父宽报 agent.permission_widening,派发口结构化明拒;父档经环境账从会话确认档现读。）
- [x] 缺依赖时报结构化错误。（requires.tools/requires 缺项、MCP 未挂载、预装技能不在清单、allow 越权各报稳定码(agent.missing_dependency/agent.tool_not_granted),不悄悄放宽;宿主没递环境账的旧调用跳过技能/MCP/权限查账,requires 恒查。）
- [x] 保持 deferred tool index、model instructions、Soul 的现有请求期行为。（Resolver 只出静态决策,system_prompt/model_instructions/Soul/deferred index 一律不搬运;拼装次序一字未动,黄金测试照旧。）

验收：同一 Agent Definition 从 AgentTool 与 Workflow 解析，所得 Profile 完全一致。（test_agent_profile_resolver.cpp 对账册:同一份 complete.yaml 走 BuildSubagentResolveRequest 与 BuildWorkflowAgentResolveRequest 两口,逐字段比,含谓词行为等价;workflow_commands 的 default binding 生产路已真走 Resolver。）

### 阶段 4：AgentTool 接入

- [x] 把写死的 `agent_type` 校验换成 AgentCatalog。（接了解析口一律问它,"查得到即可派,查不到报没有这名看 /agents";码内内置两枚由宿主标 builtin 走内置快路,行为一字不动;user/project 层覆盖内置名的按定义走自定义路,契约 §6.2 跨层覆盖由此生效。AgentFaceIsReadOnly 同步换序:resolver 优先,没接退回旧答案。）
- [x] 动态生成 Agent 类型说明。（agent 工具 schema 的 agent_type 说明追加"当前可派的类型"清单,从 Catalog 拉可用条目(内置+自定义,各带一句 description,换行压平、按码点截 160)。性能口径照 skill 工具 enum 先例:清单进缓存快照,SetHooks(回合边界)翻新,一回合至多扫一遍盘;没配清单源零追加,schema 与从前零 diff。）
- [x] 自定义 Agent 支持前台、后台、取消、消息、结果回收。（前台/后台本就同一条 RunTask;测试册 test_custom_agent_dispatch.cpp 逐件对账:后台派出(台账记 resolved name)、x 取消(CancelTask 贯通到任务线程,终态 Cancelled)、agent_message 送达(工具收尾后下一请求注入)、结果回收(完成通知排走、delivered 置位)。）
- [x] 自定义 Agent 支持现有 worktree isolation。（YAML runtime.isolation 与入参显式都真开房:写不出房、预算断房保留并附路径;入参显式压过 YAML 缺省档。）
- [x] 证明工具收窄、Skill 预装、Profile 选择都已生效。（工具面收窄+Capabilities 说明面收窄在前台(既有册)与后台(新册)两路口都钉;预装技能正文进两路系统提示;Profile 选择的五层回路阶段 2 已钉。）
- [x] 权限收窄强制执行（R 单遗留,本笔接线）：子定义档比父严时（父 yolo 子 confirm），子代理循环里真把确认拉回——AgentTool::Hooks 新增 on_tool_confirm_floored 口,turn_runner 同一颗 ConfirmToolUse 多带一枚档位下限,会话档向下并到下限再裁定;宿主没接口或档不比父严时原样转发,行为不变。

验收：主 Agent 能按描述派出自定义 Agent；子 Agent 看不见未授权工具，也不会收到那些工具的 Prompt 说明。（schema 清单带 name+description 供模型挑人;派发按名即达;工具面与说明面两路口收窄均有测试钉。）

### 阶段 5：Workflow 接入

- [x] Workflow Agent 节点支持 `agent: <name>`。（名字解析链:Package 挂载层已把包内短引用折成 canonical(mounting.cpp 的 CanonicalizeRef),宿主解析口按 canonical/裸名查 AgentCatalog——查名复用会话 agent 工具的同一只 custom_agent_resolver。AgentExecutor::Options 加 custom_agent_resolver + subagent_prompt_material;回执身份(resolved 名)进 NodeExecResult.agent_name 与 journal 完成事件。）
- [x] compiler 只校验引用，executor 调统一 resolver/factory。（validator 的 CapabilityTable 加 agent_names,`agent:` 点名不在表里报 unknown_agent(编译期能查就编译期报);executor 调 BuildWorkflowAgentResolveRequest,系统提示与 agent 工具路共用同一只 BuildSubagentPromptOptions/AppendPreloadedSkills(自 agent_tool.cpp 匿名 namespace 提出)。）
- [x] 前台失败、超时、取消、后台回收维持现有语义。（test_workflow_custom_agent.cpp 逐件对账:前台失败 agent_error、预算尽 budget_exhausted、取消(打断不是错误,半截收场,事件流 Cancelled)、async 外壳换线程跑自定义 body 产物挂回——自定义路与 role 路同一条收口链。）
- [x] 未知或 unavailable Agent 在编译期或最早可知时失败。（编译期 unknown_agent;运行时首知报 agent_unresolved(查无此名/定义解析不过,带 FormatResolutionIssues 的稳定码);宿主没接解析口报 not_configured;一律不静默退回 default binding。另补 0.26.92 的欠账:定义 JSON roundtrip 收 agent 字段。）

验收：同一 Agent 既能由 `agent` 工具唤起，也能由 Workflow 节点唤起；Prompt 与工具完全同源。（端到端对账册:同一 YAML 两路各跑一轮(假后端),request.system 逐字节一致、工具表逐枚一致;冒烟 driver workflow_custom_agent_driver + 夹具 smoke-probe.yaml/custom-agent-smoke 走真 Catalog 扫描与一幕回执身份。todo_write 的私有台账 schema 差异(agent 工具路每任务换独占实例)是台账机制不是定义差异,对账面不进。权限下限(R 单遗留)一并接线:TurnWiring 加 on_tool_confirm_floored,宿主 BuildWorkflowAgentCallbacks 装 ConfirmToolUse 带下限档,executor 按节点的 permission_floor 包装——自定义定义比会话严时真收紧。）

### 阶段 6：示例归位

眼下 `examples/agents/` 混着 Plugin、Skill、README 与工具夹具。功能落地后再迁，不要提前搬空。

目标样子：

```text
examples/agents/
  browser-tester.yaml
  gui-operator.yaml
  README.md

examples/skills/
  browser-testing/SKILL.md
  gui-operation/SKILL.md

examples/plugins/
  gui-agent/...

examples/browser/
  server.js
  README.md
```

- [ ] `examples/agents/` 只放 Agent Definition 与说明。
- [ ] `agent-browser/skills/`、`agent-gui/skills/` 拆回 `examples/skills/`。
- [ ] Plugin 示例归 `examples/plugins/`。
- [ ] Browser MCP 仍归 `examples/browser/`。
- [ ] 更新 README，删掉“另单在建”这类过期话。
- [ ] 更新依赖旧路径的测试；不靠兼容软链遮问题。

验收：读目录名就知道东西归谁。没有同一 Skill 复制两份，也没有 Agent 目录暗藏 MCP 实现。

## 十、测试账

### 解析与加载

- [ ] 最小 YAML、完整 YAML、未知字段、错类型、错 schema。
- [ ] 文件名与 name 不同的 warning。
- [ ] 同层重名报错；跨层覆盖可追溯。
- [ ] 扫描次序确定；不同文件系统枚举顺序不改结果。
- [ ] 路径穿越、符号链接越界、非法绝对路径。
- [ ] reload 失败时保住旧 Catalog。

### Prompt

- [x] default 输出兼容黄金测试。（test_prompt_profile.cpp 黄金基线 + prompt_profile_driver 字节 diff）
- [x] builtin default、user default、builtin profile、user profile、project profile 逐层覆盖。
- [x] 稀疏 Profile 只改点名模块。
- [x] persona/system_prompt 只替 core。
- [x] mode 不可覆盖。
- [x] platform 按 wire 选择。
- [x] model instructions、deferred index、Soul 次序不变。（九段次序断言）
- [ ] project instructions 的 inherit/omit。（omit 已实现于派发链,尚未单独成测）
- [x] 工具被裁后，相应 feature 文案也消失。
- [x] PromptSourceLedger 每段都指向真实来源。

### 能力与安全

- [ ] allow 与 deny 交叠时 deny 胜出。
- [x] 子 Agent 不能取到父 Agent 没有的工具。（阶段 3:allow 越权报 agent.tool_not_granted,resolver 与派发口两层测试钉;有效面保父面次序。）
- [x] 项目 Agent 不能抬高权限、取消沙箱或绕过审批。（阶段 3:permissions.mode 比父宽报 agent.permission_widening,派发口明拒;阶段 4 接上运行期强制收窄——子档比父严时经 on_tool_confirm_floored 把确认真拉回,宿主按 min(会话档,下限) 裁定,测试钉。）
- [ ] MCP 只许引用已有服务，拒绝内联启动配置。
- [x] 缺 Skill、MCP、tool、model role 时标 unavailable。（阶段 3:缺依赖报 agent.missing_dependency 结构化错并拒发,不退化成全工具 Agent;doctor 的静态缺项账照旧。）
- [ ] unavailable Agent 不得静默退回 general-purpose。
- [ ] 日志与 doctor 不泄露密钥、环境变量值。

### 执行

- [x] 内置 general-purpose、Explore 行为回归。（阶段 4:接了 Catalog 解析口后内置两枚走 builtin 快路,persona/工具面/Explore+worktree 拒绝口径照旧,测试钉。）
- [x] 自定义 Agent 前台执行。（既有册 test_agent_budget.cpp + 新册 test_custom_agent_dispatch.cpp。）
- [x] 自定义 Agent 后台执行、消息、等待、回收。（新册逐件对账:派出/agent_message 送达/结果回收。）
- [x] 取消与超时。（取消:CancelTask 贯通到自定义 Agent 后台线程,终态 Cancelled;超时:预算三线的自定义 Agent 变体在 worktree 册一并钉——预算断房保留,0.26.83 先例的延伸。）
- [x] worktree isolation。（YAML 缺省与入参显式两路:写不出房、预算断不丢房。）
- [x] AgentTool 与 Workflow 解析同一 Definition，所得 Profile 一致。（阶段 3 对账册 test_agent_profile_resolver.cpp:两口各解析同一份 YAML 逐字段比,含 runtime 五字段/工具谓词行为/权限档/缺项错。）
- [ ] Workflow 未知 Agent 报错位置清楚。

### 真机验收

- [ ] 跑 Release 全量 CTest，记下真实命令与通过数。
- [ ] 用真实 `luban` 启动，不只跑单测。
- [ ] 用 `/provider ccmoon`，至少挑两种模型角色各跑一次。
- [ ] 实测 `/agents`、`inspect`、`doctor`、前台 Agent、后台 Agent、Workflow Agent。
- [ ] 截图留证：定义来源、Prompt 来源账本、工具收窄、缺依赖报错、成功执行结果。
- [ ] HTTP 成功、模型复述配置、单张截图，都不算能力已生效；须看实际工具调用与产物。

## 十一、兼容与发布

- [ ] 未发现任何 Agent YAML 时，行为与当前版本相同。
- [ ] `general-purpose`、`Explore` 名称先保留。
- [ ] schema 带版本；未知高版本拒绝加载，并写升级提示。
- [ ] 新字段先按严格解析走。要加字段，就加 schema 测试和文档。
- [ ] Agent 定义不写进会话历史正文；历史只记稳定 name、定义来源摘要与解析后版本标识。
- [ ] 若 reload 后同名 Agent 变了，已运行 Agent 持有旧快照，新任务才用新定义。
- [ ] 发布说明写清：Agent YAML 是运行配置；Prompt Profile 才是提示词覆盖入口。

## 十二、完成线

做到下面几条，本单才算收口：

- [ ] Agent、Skill、Workflow、Plugin、MCP 五者边界写进文档，也落进代码结构。
- [ ] 自定义 Agent 能从用户层、项目层加载，并能覆盖内置定义。
- [ ] Agent 能选 Prompt Profile，且不破坏现有 Prompt 分层与自由覆盖。
- [ ] AgentTool 与 Workflow 共用一套 Catalog、Resolver、Factory。
- [ ] 权限只缩不涨，MCP 不可内联，缺依赖不偷着降级。
- [ ] Prompt、工具、模型、权限均可 inspect、可 doctor、可追根。
- [ ] 示例目录归位，旧 README 与测试一并更新。
- [ ] 单测、集成测试、Release CTest、真实 CLI、多模型与截图证据齐全。

## 十三、留到后头

这些事先不做。等首版跑稳，再另开单：

- Agent 继承另一个 Agent。
- Agent 组合、团队与自动路由器。
- Agent 专属长期记忆。
- Agent Profile 市场与远端安装。
- 通配工具权限、条件权限与细粒度 MCP resource 过滤。
- 自动热重载与运行中迁移。
- `solutions/` 教程索引。它仍是内容组织，不进运行时。
