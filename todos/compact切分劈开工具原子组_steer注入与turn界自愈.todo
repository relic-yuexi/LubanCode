# compact 切分劈开工具原子组:steer 注入与 turn 界自愈

- 状态:已实施(2026-09-09,分支 worktree-agent-compact-split)。
- 日期:2026-09-09
- 级别:P0(自动压缩整链瘫痪:拒收 → 原史原样重发 → 预检再爆 → 再拒,死循环)。
- 事故现场:v0.26.224-beta.1 内测真机——工具循环中途触发自动 compact,报"map 块 t1-t6 内工具原子组不完整(切分缺陷),该次 compact 拒绝,历史未动",随后按原历史继续发送(0.26.219 的降级救了请求,但 compact 永远成不了)。
- 范围:`src/agent/compact.cpp`(turn 规划/分区/map 块切分/防线)、`src/agent/context.hpp`(IsUserTurnStart/SplitIntoTurns 的语义联动)、相关测试。
- 不在本单:compact 重构总账(另有前置研究单)、摘要质量、热区策略。

## 〇、用户定案(2026-09-09,主架构——本节优先于 §二的修补取向)

1. **触发线**:自动 compact 在 `窗口×80% − 4k(压缩提示词) − 8k(压缩结果预留)` 触发。例:200k 窗即 148k 触发。现行触发(ContextTracker 80% / projected overflow)的口径与此对齐,把提示词与结果预留两笔算进账。
   - [x] 已落:`agent/context.hpp` 立 `kAutoCompactPromptReserveTokens=4096`/`kAutoCompactSummaryReserveTokens=8192` 与纯函数 `AutoCompactTriggerLine`(小窗扣不动夹 0);`ContextTracker::ShouldAutoCompact`、loop 的 projected 双闸 A 线、/context 两处显示(FormatContextBreakdown 行、cmd.context.next_line)全换同一只。BuildContextBudgetPlan 的口径核实过:那份算"压缩请求自身的输入预算"(按会话现场配置取数),与本线"触发时机"的固定档各管各账,不重复扣。
2. **末轮豁免**:最近一个可能含未闭合工具交互的 turn(`user,assistant,tool,tool…` 形)整只不动——工具组天然完整,劈组问题从架构上消失。
   - [x] 已落:`BuildTurnPartitionPlan` 的末分区恒为最后一枚 turn(切点落上一 turn 边界);冷区=末轮之前全部 turn,按 token 平衡切成至多 partition_count−1 份。
3. **压缩范围**:只压"末轮之前"的全部历史,切点落在上一 turn 边界。
   - [x] 已落:同上,冷区分区只盖 turns[0..n-2],map 块=整 turn 的并集;分区不变量(连续盖满/每份至少一枚/末份热区)不动。
4. **拼接**:压缩产物拼在 **system 之后**(不再是并入热区首条 user 消息),其后接豁免的那轮原样,请求照发。
   - [x] 已落:`BuildCompactedHistory`(plan 版)不再并入,archive 自成 durable history 头一条(独立存档头);请求拼装侧新口 `TakeStandaloneArchiveHead`(loop.cpp,sanitize 后、预检/指纹前)把头一条独立存档摘下拼进 system 之后——messages 从豁免轮的 user 轮头起算,角色交替不破。判定从严(`IsStandaloneArchiveMessage`):并入式旧档不认,免偷用户正文;夹非文本块不认。持久档、/resume、下一次压缩剥旧档三处不改形状。

§二的 turn 界归属修正与防线自愈保留为底层卫生(steer 归属当前 turn 的账仍要理清,防线仍要自愈兜底),整体架构以本节为准重排。

## 一、证据与定向

1. 防线原文(compact.cpp 旧 2161-2170):"分区/再切都只落 turn 边界,这里不该拦得到;拦到了就是切分 bug"——防线自己注释了前提:**turn 边界 ⇒ 工具组完整**。真机打脸:这个前提不成立。
2. `IsUserTurnStart`(context.hpp:64-67):user 角色且含 TextBlock/ImageBlock 即算新 turn 开头;注释明知"空壳 user 消息插在 tool_use 与 tool_result 之间会把工具原子组劈开",所以空内容不算——**但带正文的 steer 消息照样算**。
3. steer 注入点(interactive_session_assembly.cpp 一带):工具结果之后、下一次模型请求之前。多工具批次(一次 assistant 多枚 tool_use、结果分多条 user 消息回来)里,steer 正文恰插在**两个 tool_result 之间**:判界函数见文字开新 turn → 同一批工具组劈过 turn 界 → map 块界落在 turn 界 → 防线拦截。
4. 规划器账目(compact.cpp 旧 1055-1101 一带):`ToolExchangeGroupInfo` 只记一枚 `group.turn`(按某条消息落界),组跨 turn 时账面看不出;`has_incomplete_tool_exchange` 只认悬垂/组内缺件,不认"组被 turn 界劈开"。

候选凶手不止 steer 一张脸,修复以**形状**立法,不点名 steer:凡 turn 界把一枚 tool_use 与其任一 tool_result 分在两侧,即病。

## 二、修复合同

### 2.1 turn 界不劈组(主修)

- [x] 已落(走"规划器内后处理收口"一支):`HealTurnBoundariesOverToolGroups`(compact.cpp,纯函数)把落在任一完整工具原子组 [from,to) 内部的界点并回前一轮——工具循环里插入的用户正文(steer 一类)归当前进行中的 turn,不开新 turn。`BuildTurnPartitionPlan` 在 SplitIntoTurns 之后调它,自愈笔数记 `plan.healed_turn_boundaries`。
- [x] 取舍如实记:**IsUserTurnStart/SplitIntoTurns 的公共语义没有动**——热区(HotZoneStartIndex)、事件账(context_events)、episode 切分(SplitEpisodes)、chat wire(request.cpp 自家一份)四处共用户,改语义牵连面大且"谁开了一轮话"的语义本身没错;错在 compact 把"轮界 ⇒ 组完整"当前提。收口放在 compact 规划器内。context.hpp 的 IsUserTurnStart 注释已互注两把尺的分工。

### 2.2 防线自愈(兜底)

- [x] 已落:`HealMapChunkToolGroups`(compact.cpp,纯函数)——块界沿消息粒度挪移,result 落块外的往后吞整组、use 落块前的往前接,迭代到不动为止;挪得动记一行日志(`[map-defense]`,LogSink Info),挪完仍不完整(悬垂 use/悬空 result/result 早于 use)才拒收,文案指形点名 id 与形状。map 防线(compact.cpp CompactTurnPartitioned 的块循环)拦到时先自愈、块界与 turn/事件标签随挪随重钉,极端形状才拒收。
- [x] 滞回:拒收文案带稳定标记 `kMapDefenseRejectMarker`("map 防线拒收");会话层 `CompactHysteresis.map_path_held` 挂旗,本会话自动压缩(midturn+turn 间)静默收兵不再重试 map 路;cheap→normal 的修一次对防线拒收不再白试(形状判定与模型无关);手动 /compact 不受限、成功换账即解旗;自动成功同样解旗。子代理路(agent_tool 的压力钩)就地立 shared_ptr 旗,同口径。取值不新造体系:旗是布尔一枚,与 kCompactHysteresisFloorTokens 一族的滞回思路同门。

### 2.3 测试

- [x] 事故形状复现册:SteerAccidentHistory(前情两轮 + 多工具批次被 steer 劈开 + 收尾轮)——BuildTurnPartitionPlan 断言 healed==1、组整只随轮、steer 归进行中 turn、末分区恰为末轮;CompactTurnPartitioned 全链 3 map + 1 reduce 成功,块内配对完整,steer 正文进了所在轮的 map 材料。
- [x] 变体:steer 插在 use 与首个 result 之间(healed==1);一组内连插两条 steer(healed==2);steer 落两组之间(healed==0,公共尺照旧开新 turn——界点不劈组就不病)。
- [x] 自愈册:HealMapChunkToolGroups 六档(完整不动/use 在块内 result 在块外往后吞/result 在块内 use 在块前往前接/两头一起挪/悬垂指形/悬空指形);防线拒收册(冷区悬垂 use→标记+指形+零请求)。
- [x] 滞回册(test_app_commands):防线拒收一次挂旗零请求;攒足新内容(越过普通滞回带)仍拦;手动 RunCompactCommand 成功解旗。
- [x] 末轮豁免与 system 拼接回归册:plan 级(17 turn 四分:冷区 5-6/份、末分区恰 1 turn;新史=[独立存档][豁免轮原样];kept 不含存档);loop 级(独立存档头收编进 system 之后、messages 从豁免轮起算、无相邻 user、工具对原样);独立存档判定/收编从严册(并入式不偷正文、夹图不认、单条不收编);独立存档头不算 turn 册(第二次压缩 turn 数对账)。
- [x] 触发线数值册:AutoCompactTriggerLine 对表(200k→147712、20000→3712、16000→0);ContextTracker 册(100k 窗 67712 过线/差 1 不到线;小窗夹 0);loop A 闸册(128k 窗:B 过 60% 而 A 未到线不触发;32k 窗双闸同开仍触发——重调窗口取数,小窗上参考线被两笔预留压到 B 线之下,双闸并一闸)。
- [x] 既有册改判:17 turn 分区份数、BuildCompactedHistory(plan)新史形状与 kept 账、第二次压缩的 reduce 脚本来源上限(t1..t9)、单分区超预算再切的块数(8 turn 冷区 7→块 7)、/context 自动压缩线行(192.5k(80%-12k))、ContextTracker 80% 册。老 BuildCompactedHistory(热区预算档)与 Compact()/CompactHierarchical 的既有册一字未动,全绿。

## 三、验收矩阵

- 事故原形状(t1-t6 段含 steer 劈组):自动 compact 成功,历史换账。
  - [x] 册:BuildTurnPartitionPlan + CompactTurnPartitioned 全链(见 §2.3)。
- 无 steer 的普通工具循环:切分结果与旧版逐字节等价(不回归)。
  - [x] 既有册全绿即证(17 turn 四分等分区册按新架构改判,冷区切法/边界算法/并列取早与旧码同构;UniformTurns 无工具史 healed==0)。
- steer 在循环各位置插入:组永不被劈;steer 正文归属当前 turn 的摘要。
  - [x] 变体册 + steer_mapped 断言。
- 极端形状(自愈也失败):拒收文案指形,滞回生效不刷屏。
  - [x] 防线拒收册 + 滞回册(挂旗后静默收兵,提示只在拒收当时打一遭)。
- 单发路(装得下)不受影响。
  - [x] Compact()/CompactHierarchical 路未动,既有册全绿。

## 四、边界

不重写分区算法骨架(贪心装块照旧,只修界点归属与自愈);不动 steer 注入点本身(那是 P3 队列的正面行为);不碰保命索与热区预算。

## 五、实施纪要(2026-09-09 销账)

- **定案四条的落法**:① 触发线立公共尺 `AutoCompactTriggerLine`(agent/context.hpp),ContextTracker 的 turn 间触发、loop 的 projected 双闸 A 线、/context 两处显示四处换同一只;B 闸(真实水位 60%)不动,大窗上 A=80%−12k 与 B=60% 的先后次序不变,小窗(<60k)上 A 线落到 B 线之下、双闸并成 B 一闸,属定案口径的自然结果。②③ 末轮豁免=BuildTurnPartitionPlan 末分区恒为最后一枚 turn,冷区=其余全部 turn 切至多 partition_count−1 份(有冷区时至少一份,分区无缝盖满的不变量不动);map_calls 语义随之从"分区数−1"变为"冷区份数"(数值同)。④ archive 自成 durable 头一条,请求拼装时 `TakeStandaloneArchiveHead` 收编进 system 之后——角色交替靠这一刀保住:messages 从豁免轮 user 轮头起算;独立档在下一次压缩的 prior archive 剥离里整条不进 turn(turn_base 偏移),并入式旧档(旧会话/单发路产出)照旧剥离路径,两形并存都认。
- **界点归属取舍**:公共尺不动,收口在规划器内(理由见 §2.1 勾账)。HealTurnBoundariesOverToolGroups 只并回**完整组**内的界点;不完整组(悬垂)并了也白并,留给防线指形。steer 落两组之间不并——界点没劈组就不病,公共语义照旧。
- **自愈与滞回的取值**:自愈块界只在消息粒度挪(不拆消息、不造新块),挪不动=悬垂/悬空/result 早于 use 三种极端形状,拒收文案点名 id 与形状;滞回旗 `map_path_held` 一枚布尔,挂在既有 CompactHysteresis 上,不新造配置;子代理路同口径(shared_ptr 旗)。拒收提示只在当时打一遭(`compact.map_hold`),后续自动触发静默收兵,手动 /compact 与自动成功都会解旗。
- **改动文件**:src/agent/context.hpp、src/agent/compact.hpp、src/agent/compact.cpp、src/agent/loop.cpp、src/cli/context_tracker.cpp、src/cli/format_utils.cpp、src/cli/i18n.cpp、src/app/interactive_session.cpp(注释)、src/app/commands/session_commands.{hpp,cpp}、src/tools/agent_tool.cpp;测试:tests/unit/agent/test_compact.cpp(改判+新增)、tests/unit/agent/test_context_tracker.cpp(改判)、tests/unit/cli/test_format_utils.cpp(改判)、tests/unit/app/test_app_commands.cpp(新增滞回册)、tests/integration/compact_turn_sharding(拒收诊断 + 夹具修)。
- **CI 过程三笔(已全绿,run 34372069494)**:① 两处新册写在双账夹具命名空间之前,前向引用编不过,挪位;② 触发线夹 0 后零占用恒真(加 current>0 前置)、零窗显示行判词随新口径改判;③ 集成评测册 task=28 十种子全拒——根因是评测夹具 RangeStartTurn 落点差两字节(针串"来源 turn t"13 字节,老实现 pos+11 落在空格上恒走 fallback=1),旧架构靠热区原文的真 turn 号兜住没露馅,末轮豁免后纠正轮进 map 被错标、与原文撞成"覆盖倒序"——修夹具(按针串长度起跳),顺带把拒收缘由+map 标签两行诊断改为拒收时必打。
