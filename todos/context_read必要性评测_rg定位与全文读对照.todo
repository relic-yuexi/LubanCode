# 源码读取策略与 context_read 追回能力评测：真实组件对照

- 状态：方案订正（2026-09-05，待实施；无本项真实模型成绩）
- 级别：P1
- 类型：LubanCode 真实组件与 Agent 闭环评测
- 用户硬要求：评测必须跑到真正的组件。假后端只验装置，手写近似实现、人工拼预览、脚本替模型选证据，均不能充当组件效果。
- 本轮订正：拆开源码阅读与 artifact 追回；废止旧 A 禁搜索/B 限 40 行的主对照，以及小样本差 5%/10% 即下结论的规则。
- 关联：《LoCoMo评测纠偏_判分修复数据隔离与端到端重跑.todo》《LubanCode使用洞察_Token账本与Prompt审计设计.todo》
- 范围：夹具、真实组件接线、隔离、轨迹审计、判分与报告。允许补评测专用装配和观测入口，不改产品默认读窗、搜索或压缩策略；产品改动另立单。

## 一、先分清要测什么

| 实验 | 问题 | 真正受测组件 |
|---|---|---|
| R：源码读取策略 | 相同搜索能力下，单次小窗与自由读取怎样影响修复成功率、往返和成本？ | SearchTool + BundledRipgrepRunner、ReadFileTool、真实写改工具、Agent/AgentLoop、ContextManager |
| A：artifact 追回 | 长输出折叠后，模型调用 context_search/context_read 能追回多少证据，花多少成本？ | CompressWorkingView、ContextArtifactStore、ContextSearchTool、ContextReadTool、Agent/AgentLoop、真实模型后端 |

read_file 读工作区文件；context_read 读先前工具输出落盘后的 artifact。两者不再混称。
“源码全文有益”不能推出“context_read 有益”；“工具返回全文”也不能推出“模型收到全文”。

## 二、真实组件接线：实施前必须过门

以下为 2026-09-05 静态核对，实施前按冻结 commit 重查；不是运行验证：

| 源码落点 | 当前行为 | 评测要求 |
|---|---|---|
| src/tools/read_file.cpp | 默认最多 2000 行，输出约 1 MiB 封顶，支持 offset/limit | 包装真实 execute，只约束参数，不另写读取器；记录真实截断 |
| src/tools/search.cpp、search_ripgrep_run.cpp | 真 rg 子进程；命中数、总字节与长行有限制 | 使用构建交付的 rg，记录版本/hash 与截断；不得换成 Python 搜索 |
| src/agent/context_manager.cpp、context_events.cpp | 工具结果经过真实请求视图压缩 | 观测压缩后、发给模型前的请求；原始结果不能冒充实际注入 |
| src/tools/context_tools.cpp、src/agent/artifact_store.cpp | 按 artifact id 搜索，按块或行窗追回 | id 由真实落盘产生；搜索、读取都调用生产组件 |
| src/app/interactive_session_assembly.cpp | 挂载 context 工具并给 ContextManager 接 artifact store | 作为完整追回接线的核对入口 |
| src/app/one_shot.cpp、tool_runtime.cpp | 本轮未见与交互路等同的 context 工具及上下文 store 接线 | 有轨迹/产物落盘不等于追回链已通；补齐并验证后才可当入口 |
| src/agent/agent.cpp、loop.cpp | 工具定义与执行均有过滤入口 | 暴露与执行双重约束，不能只藏 schema |
| src/runtime/trajectory_session.cpp | 有 usage、工具事件与请求元数据 | 复用账本，补实际正文审计；元数据不等于完整请求内容 |

### 2.1 三层证据分栏，不许混算

1. 装置回归：可用 fake backend 验夹具、过滤、预算、轨迹与判分；标 fake=true，不入模型效果表。
2. 组件闭环：真模型经 AgentLoop 调真工具，结果经真实 ContextManager 回喂，模型完成修复或作答。这是本单主成绩。
3. 产品入口复核：走实际 CLI/交互装配复核代表题，列清与 driver 的差异。未过此层，只能称受控组件评测，不能称完整产品效果。

合成仓、合成日志可以作输入；受测读取、搜索、压缩、落盘、追回与模型决策不能用替身。
独立 C++ eval driver 链接 lubancode_core；Python 只负责编排、隔离、判分和报表。
缺接线就补接线，不能在脚本里仿出一条“同款”生产链交差。每场记录真实组件版本、配置与启用状态。

## 三、R 实验：搜索相同，只改读窗

| 组 | 搜索 | 读取规则 | 用途 |
|---|---|---|---|
| R-L40 | 同一真实 SearchTool | 单次正文最多 40 行，可继续翻页 | 主比较 |
| R-L160 | 同上 | 单次正文最多 160 行，可继续翻页 | 区分窗口过窄与策略问题 |
| R-Free | 同上 | 原版 ReadFileTool 参数与默认行为 | 受控工具集内的主基线 |
| R-Full | 同上 | 选定文件后强制从首行读到末行，仍调用 ReadFileTool | 可选辅助组，单独报告 |

- 主比较是 L40/L160 对 Free。其余工具、提示词、压缩、追回、预算与测试反馈相同；只改变读窗限制及相应说明，保存工具定义 hash。
- 调用真实 ReadFileTool 前规范化并钳制 limit，覆盖省略、null、负值、超大值等情况；保留可解释的 offset/错误规则。记录原始与有效参数、真实正文行数及字节。
- L40 连续翻页读完整文件不算违规；它测单次小窗策略。累计独立行覆盖、重复阅读和调用数必须报告，不再宣称“零全文阅读”。
- search 也暴露正文。各组沿用相同生产限制；宽泛匹配、搜索截断及搜索带来的覆盖都入账，不得只审 read_file。
- 主组均接相同的真实压缩与 artifact 追回链。若追回成为主要阅读渠道，单列诊断，不悄悄只对一组封堵。
- Free 不等于完整产品所有工具全开；它与其余组共享隔离限制。完整产品另做入口复核。
- Full 的分页、文件上限与耗尽预算规则先冻结。正文若在请求视图里折叠，标“全文返回但未全文注入”；另设原文直入时，作为额外消融，不混算。

## 四、A 实验：真折叠、真落盘、真追回

主链：冻结长输出 -> 合法 tool_use/tool_result 历史 -> ContextManager/CompressWorkingView
-> ContextArtifactStore 真落盘 -> 真模型决策 -> ContextSearchTool/ContextReadTool 真执行
-> 同一 ContextManager 生成后续请求 -> 模型最终作答 -> 独立判分。

| 组 | 实际交付 | 回答的问题 |
|---|---|---|
| A-Raw | 同一原始输出，评测开关跳过结构折叠，原文进入请求 | 信息完整对照；这是实验处理，不冒充生产默认 |
| A-Recover | 真实生产折叠、落盘、预览；开放两把追回工具 | 追回后的质量与成本 |
| A-Preview | 与 Recover 同一折叠与落盘；执行层关闭两把追回工具 | 无追回能力的消融 |

- 折叠阈值、预览、分块沿用冻结版本生产值，不为扩大组差改数字。主实验固定关闭可选 summarize 能力；摘要另组，走真实摘要后端并全额记账。
- 合法历史只固定起点；预览、artifact、搜索命中和读回正文均由真实组件生成，不许人工拼装或提前挑证据交给模型。
- 三组原始输出 hash 相同，每场独立 session/store。Recover 与 Preview 核对除工具可用性外的初始可见内容一致。
- Raw 须容得下原始输入、协议与输出预算；超窗题单列，不把输入过长错误算作“追回战胜全文”。
- 覆盖中段事实、跨块聚合、新旧冲突、长构建/测试日志定位；按位置与规模分层，不只埋预览外单个关键词。
- 原始日志及 artifact 文件不可经 read_file、shell、网络等旁路取得；追回组只经真实 context 工具访问。
- 另做真实命令生成长输出 -> 产品入口折叠 -> 追回 -> 回答的复核，区分受控起点与完整输出生成链。
- 未调用追回仍答对，最终质量照常计分并标未追回；调用数和追回后答对率只作机制诊断，不代替主指标。

## 五、隔离：先验证边界，再烧模型

- 每场独立工作区。真实读写、搜索的包装层限定访问根；验证绝对路径、父目录、符号链接/Windows junction。只换 cwd 不算隔离。
- R 开放真实受控读写、搜索与宿主测试入口。任意命令、子代理、MCP、LSP、PTC、插件、网络、后台输出等旁路默认关闭；新增入口须经过同一审计。
- 写改工具限定候选源码路径。错误消息、diff、构建反馈若吐出正文，照样计入可见账。
- 宿主选冻结测试命令，在独立环境执行。候选脚本或程序仍可能读环境、吐源码，故命令白名单不能代替文件系统、网络和反馈隔离。
- 公开自测可反复运行；隐藏测试、参考补丁、答案、题库、宿主凭据、其他场次和评测源码不可访问。最终取候选源码，在干净环境运行冻结隐藏验收。
- 不复制用户真实 config；按白名单生成评测配置。凭据由受保护宿主进程注入，候选构建/测试子进程不继承；关闭跨场记忆、外部技能和非任务说明。
- 先用恶意调用及候选程序验证越界读取、改测试、泄漏隐藏数据、动态工具绕过均不成立。隔离未过门，只验离线装置。
- 被拒绝的越界调用保留，正常判卷；若违规内容实际进了模型请求，记 treatment_invalid，保留原账。修装置后按预定规则重跑整个受影响配对批次，不挑有利单场。

## 六、任务与判分先冻结

### 6.1 R 任务

首版用 C++ 与仓内工具链，先导 16 题，每类 4 题：

| 形态 | 样例 | 验收 |
|---|---|---|
| 单点修复 | 边界值、错误分支 | 隐藏正常与边界输入 |
| 散点联动 | 多入口共同遵守新规则 | 分入口验证行为义务 |
| 结构契约 | 接口、实现、调用方一致 | 编译及跨模块行为 |
| 隐性依赖 | 注册、生命周期、别名链 | 触发实际依赖的运行测试 |

每题标文件规模、证据分布、跨文件数、修改义务、关键词可发现程度。短窗即可解决与跨窗题都要有。
不预设 rg 找不到别名，也不预设全文必能找到依赖。题面不泄露读法、目标行号或参考补丁。
冻结前验证：原始版本失败、参考修复通过、常见漏修被隐藏测试抓住。判行为，不按标准 diff 长相判分。
行为义务覆盖率对所有组统计；无法确定就标 unknown，不拿访问某行或模型自述当作漏改证据。
真实仓先取至少 3 道历史 bug 校准，从修复前版本导出并移走 Git 历史与答案材料；这几题不是总体分布终审。
与造仓结果冲突时先查规模、泄漏、装配与判分，再补样本，不能仅凭“真实仓”盖过另一批结果。

### 6.2 A 任务

冻结原始输出、生成命令或受控输入来源、问题、答案、冲突旧值、证据位置与 hash。
判模型最终回答，不扫压缩视图有无答案词，不从整段终端输出捞数字。
短答案可机判；聚合题列齐必需事实与矛盾条件，先定归一化及额外错误内容的处置规则。
从正确 run/turn 的模型完成事件取最终回答；缺终态、拒答、格式错按规则记失败，不回退挑中间答案。

## 七、统一账本：原始返回不等于模型可见

每场保存 manifest、轨迹、可见正文审计、最终候选/回答、判分及失败原因，报告可从原账复算。

- 身份：代码 commit、构建/rg hash、fixture/hash、配置及工具定义 hash、模型/provider、推理档、温度是否实际支持、种子、组别、重复号。
- 预算：模型请求数、工具调用数、token、墙钟上限及计数口径。组间一致，不能把一次用户 turn 当作全部内部轮次。
- 三层正文：原始工具返回；压缩后新增可见正文；各次实际请求累计携带正文。分别统计，不可互换。
- R 覆盖：工具名、call/request id、文件内容 hash、源行区间、去重覆盖、重复读取、搜索正文与截断。修改后按新版本记，旧行号不能硬并。
- A 追回：artifact id/hash、落盘状态、真实预览/命中/读取返回及后续请求可见范围。没有调用链证据，不算追回验证。
- 成本：复用生产 usage，按 provider 口径分列 input/output/cache/reasoning；缺项写 unavailable，不填 0。重试、辅助模型及失败场次全入账；未核价只报用量。
- 质量：最终验收通过率；分列模型失败、编译/测试失败、超预算、拒答、违规和基础设施错误。
- 请求元数据不足以证明正文可见。补受保护的实际请求快照或等价可复算观测，关联 request/call/artifact id，脱敏且不落凭据。

## 八、样本与判读

- 每场新工作区、新会话、新 store；同题跨组配对，随机组序并交错运行。固定 provider/model 配置，记录服务端可见版本和重试。
- R 先导：16 题 × 3 主组 × 3 次，共 144 场；先两题验真链，再放量。重复用于观察波动，不把同题三次当三道独立题。
- A 先跑四种题型及跨折叠阈值样本的最小真链，再依据先导冻结正式题量、重复和停止规则，不能看胜负挑题。
- 主表逐题逐组列成败和成本；区间以任务为配对/聚类单位。区间过宽就写“证据不足”，不硬判等效。
- 正式检验“不劣于 Free”前，冻结可接受退步幅度、区间方法、样本量依据和主比较；看差值区间是否越界，不只看点估计小于 5%。
- 模型失败和预算耗尽保留在预定任务分母。基础设施错误的有限重跑次数、原账保留与最终统计口径先冻结，不挑最好一次。
- 成本同时报全部场次及双方成功的配对场次，后者注明条件统计；早失败省下的 token 不能当成策略优势。
- 结论限定任务、模型、预算与装配。“某类下降 10 个百分点”不自动证明全文不可缺，R 也不能替 A 下结论。
- 题集、判分或装配修正后换版本，保留旧账并标失效范围，不得跑后挪验收线。

## 九、实施批次

- [ ] E0：冻结问题与 commit；列真实组件、接线图、替身清单及 driver/产品入口差异。
- [ ] E1：实现链接生产组件的 driver、策略包装、隔离与正文观测；接通真实 artifact 工具/store，不能用近似实现顶替。
- [ ] E2：冻结 R 夹具/隐藏判分和 A 输入/答案；验证原版失败、修复通过、漏修失败；fake 只验装置与失败注入。
- [ ] E3：隔离攻击回归过门；跑 R 两题及 A 四类最小真模型链，逐项核对真实工具、rg 子进程、落盘/追回、实际请求与判分。
- [ ] E4：R 144 场先导；A 按冻结先导矩阵跑；产逐题账和机制诊断，fake 与真模型成绩分栏。
- [ ] E5：走真实产品入口复核代表题及至少 3 道历史 bug；A 补真实命令输出的折叠追回闭环，明确装配差异。
- [ ] E6：据先导估计扩样，预注册后再跑正式批次；分别产 R/A 报告。改产品默认另立单。

建议目录：tests/eval/context_read/ 放 driver 与离线回归；eval/context_read/ 下 R/A 分目录，放任务说明、manifest 与可发布报告。
隐藏判分材料与原始运行数据放独立受保护目录，不暴露给候选进程，不误提交凭据。

## 十、验收线

- [ ] R 回答“相同搜索下，小窗/自由读取的成功率、成本和累计阅读范围差多少”。
- [ ] A 回答“真实折叠后，两把追回工具带来多少回答增益，代价多少”。
- [ ] 真模型成绩逐场能追到真实组件、输入/输出 hash、实际请求与最终验收；替身场次不得混入。
- [ ] 产品入口与组件 driver 差异有实测记录；未接通的层明确留空，不拿装置通过冒充产品效果。
- [ ] 隔离、隐藏验收、配置冻结、预算与重跑规则先过门；有效场次无旁路取答案。
- [ ] 原始返回、模型可见内容、累计请求成本分开记；判分可复算，所有失败留账。
- [ ] 不预设 rg 或全文胜出；小样本只报先导，证据不足就不改默认。
