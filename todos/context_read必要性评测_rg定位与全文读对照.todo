# context read 必要性评测：rg 定位加局部读 对照 全文读

- 状态：方案已立（2026-09-03，待实施）
- 级别：P1（工具策略的数据地基——默认行为怎么定，拿证据不定感觉）
- 类型：LubanCode 评测基建（工具面强制对照）
- 疑问源：search（rg）已回行号与片段，按理 `read_file(offset, limit)` 读目标行附近就够——那"整文件读进上下文"（context read）还必要吗？怎么证明？
- 关联：《记忆系统评测_LoCoMo-MC10防作弊基准.todo》（同代评测基建）、《LubanCode使用洞察_Token账本与Prompt审计设计.todo》
- 不在本单：改 read_file/search 的产品行为（本单出证据，改默认另立单）；改 rg 本体

## 一、问题拆开

"必要性"不是一句话——分四种任务形态，答案可能各不相同：

| 形态 | 例子 | rg+局部读的软肋 |
|---|---|---|
| 单点事实 | 改一个函数内的逻辑 | 应该够——最可能"不必要"的场 |
| 散点聚合 | 跨文件重命名、多个调用点全改、一致性核对 | rg 词法匹配漏调用点（别名/间接调用/宏），漏改编译才炸 |
| 全局结构 | 梳理模块职责、接口契约、头文件与实现对齐 | 片段读瞎子摸象——看不到整体就得猜，猜就错 |
| 隐性依赖 | include 链、模板特化、隐式转换、约定俗成的用法 | 关键词搜不到的依赖关系，只在那读全文时"顺带"看见 |

要证的不是"context read 永远必要/永远不必要"，而是**分形态的必要性边界**：哪类任务局部读就够、哪类掉成功率、差多少 token。

## 二、三组强制对照

同模型、同温度 0、同任务集，工具面强制（不是提示引导——引导不控变量）：

- **A 组 full-read**：禁 search（工具面移除），只许 read_file 全文读。模型被迫通读。
- **B 组 local-read**：read_file 硬限 `limit ≤ 40`（工具层强制截断并明说），search 可用。模型被迫 rg 定位+局部读。
- **C 组 free（现状）**：工具面全开，模型自主。C 是基线水位，看 A/B 与它的差。

强制口子：工具面配置或测试注册表里做（A 摘 search、B 给 read_file 套 limit 钳制）；钳制要留痕（每次截断在轨迹里记一笔），事后能审"B 组真没偷读全文"。

## 三、任务集（成败线机判）

双层来源：

1. **造仓（可控）**：`eval/context_read/fixture_repo/`——一个小 C++/python 仓，内置测试。任务按四形态各造 3-4 个，共 12-16 个，每个任务的成败线=跑它的测试绿（编译+ctest 一把过）。散点任务故意埋"rg 搜不全的调用点"（通过函数指针/别名/间接层调用）；隐性依赖任务埋"改了 A 处必须连带 B 处"的暗契约。
2. **真实仓（抽样校准）**：拿 lubancode 自家 3 个已修历史 bug（git log 里翻有测试钉住的），A/B/C 各跑——真实分布下的复核。

任务书措辞中性：不提示"先搜后读"也不提示"先通读"，只给任务与验收命令。

## 四、指标

- 任务成功率（测试绿=成功；编译红/测试红/超轮数=败）
- **漏改率**（B 组专属）：散点任务里 rg 应到没到的点——事后 diff 对标准答案点数
- token：input/output 分列；工具结果注入字节
- 工具调用数、read_file 均次行数、总轮数、墙钟
- 每任务跑 3 遍（同温度），报逐次结果与区间，不只报平均

## 五、判读口径（先冻结后跑，防事后找补）

- **B ≈ A 且 token 大省**（成功率差 < 5% 且省 > 30%）：单点+散点形态上"全文默认读"可收紧——另立产品单改工具策略（默认建议 rg 先行/读文件默认窗）。
- **B 掉成功率**（任一形态差 ≥ 10%）：该形态 context read 必要性得证，产品侧保留全文读自由，文档写明分野。
- **A/B 都不如 C**：模型自主选择优于强制——评测结论就是"别强制"，现状保持。
- 真实仓抽样与造仓结论冲突时，以真实仓为准并回修任务集。

## 六、防作弊与防偏

- 成败线（测试命令与判定脚本）在跑之前冻结进夹具，跑后不改。
- B 组截断钳制的轨迹审计：事后统计 read_file 调用，任何一次实际注入 > 40 行即判该 run 无效重跑。
- 任务措辞中性、四形态任务混序呈交（防模型学会"这类题该用哪招"的位置偏置）。
- 判分脚本只看测试红绿与 diff 点数，不看模型自述。
- 模型、provider、预算全记录进报告；跑不了三遍至少跑一遍并在报告标"单次"。

## 七、实施批次

- [ ] E0：造仓+任务集+冻结成败线（12-16 任务，四形态齐）；工具面强制口子（A 摘 search / B 限 read）与截断留痕
- [ ] E1：runner（python，管道 one_shot 三组各跑，钥匙走临时 USERPROFILE 拷真 config，不进 git）；先 2 任务试跑通管线
- [ ] E2：全量三组×3 遍 + 真实仓 3 题复核；报告 `eval/context_read/report.md`（逐任务×逐组表、漏改率、token、区间）
- [ ] E3：按 §五判读出结论；若涉产品行为变更，立后续单（本单不动产品）

## 八、验收线

- [ ] 报告能一句话回答用户原问："rg+局部读在哪些形态够用、哪些不够、贵/省多少 token"——带数据。
- [ ] B 组零偷读（轨迹审计过）；成败线跑后未动。
- [ ] 判读按冻结口径，不事后挪门。

## 九、不做什么

- 不在本单改 read_file/search 行为或默认策略。
- 不拿单次结果下结论；不拿 token 省冒充"更好"（成功率同权）。
- 不造"rg 天然赢"或"全文读天然赢"的偏题任务集——四形态对称。
