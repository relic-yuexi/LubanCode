# cpr 并发请求挂死:子代理长轮询哑火冻住主回合

状态：代码已并入(2026-08-22,0.26.15):三 client 硬墙钟+配置贯通+退出有界收口,单测六案全绿;余 Windows 真控制台跑一遍 agent_stream_driver 四幕收官

## 现场(退场单留下的铁证)

agent_stream_driver 第四幕(完成退场)在本机卡死,埋点痕迹(build/run-*.md 已留档):

- 主会话空闲时,后台子代理长轮询的第 N 轮请求与主回合回流请求**同拍**发出;
- 主回合的响应正常到达,子代理那枚请求**进了 cpr::Post 再不返**——超时也不触发;
- 主回合随后冻住,/exit 退出码 3,只能杀进程。
- 拿旧代码(repro-old)复现三轮同位同症——**既有病,不是退场单踩出来的**。
- 240 接受 = 240 请求:连接根本没到假服务监听口——疑本机网络栈(代理/TUN 截胡 127.0.0.1)偶发重置/吞掉回环连接,cpr 无超时兜底就永挂。

与用户真机"后台象棋任务烧 101k tokens 后只能杀进程"是近亲。

## 要做

1. **复现与定性**:拿退场单留的痕迹(run-*.md 埋点)与 `LUBANCODE_DEBUG_SUBAGENT` 日志续挖;区分三种现场——本机代理/TUN 截胡回环、cpr/libcurl 连接池并发缺陷、DNS/套接字栈挂起。
2. **每请求硬超时兜底**:子代理 detached 后端的每枚请求必须有不可穿透的墙钟(独立于 LowSpeed 空闲超时——连不上/连上了不回字节都算),超时翻 TaskOutcome::ApiError 带超时原因,任务收口不占坞。
3. **主回合解耦**:主回合不得因任何后台请求的挂死而冻——回流轮与子代理请求若共享连接池/句柄,查 cpr 多线程用法(perform 在各线程应独立 easy handle;cpr::Post 线程安全性核实),必要时后台请求走独立会话。
4. **CI/驱动器稳定性**:agent_stream_driver 第四幕在本机因这病灶跑不完——修后完整跑通,四幕全绿进常态。
5. 网络栈取证写进 docs/troubleshooting(代理/TUN 环境跑假服务的注意事项)。

## 验收

- 杀掉假服务/断网场景:子代理请求在硬超时内翻 ApiError 终态,dock 行换短因,主回合照常收口,/exit 退出码 0。
- 两枚并发请求(主+子)同拍,不再出现"一枚永挂"。
- agent_stream_driver 四幕全绿;1845+ 例全绿不回归。

## 不做

- 不为此换 HTTP 库。
- 不在本单修本机代理环境(那是用户的网,程序只须不挂)。
