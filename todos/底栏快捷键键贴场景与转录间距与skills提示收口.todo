# 底栏键贴场景 + 转录空行 + /skills 提示收口

- 状态：待修
- 日期：2026-09-03
- 类型：LubanCode 产品缺陷（交互 affordance + 转录间距）
- 用户现场：四件并发现象，按编号独立可验；本单不分发，关 P0/P1/P2 一并立项
- 主要落点：
  - `src/cli/console_input.{hpp,cpp}`、`src/cli/bottom_chrome.{hpp,cpp}`
  - `src/cli/turn_renderer.{hpp,cpp}`、`src/cli/transcript.{hpp,cpp}`
  - `src/app/agent_panel_presenter.{hpp,cpp}`
  - `src/cli/i18n.{hpp,cpp}`（新文案）
  - `src/cli/console_input_internal.hpp`（必要时公开新构造器）
- 关联测试：
  - `tests/unit/cli/test_bottom_chrome.cpp`
  - `tests/unit/cli/test_console_input_composer.cpp`
  - `tests/unit/cli/test_keymap.cpp`
  - `tests/unit/cli/test_transcript.cpp`、`test_render_boundaries.cpp`
  - `tests/unit/agent/test_agent_panel.cpp`
- 不在本单：键位本身的改绑（`/keymap set`）；Trajectory 格式；Trajectory 重放投影；`/help` 表；`Ctrl+K` 复合键停代理语义

## 一、四件并发现象

1. **底栏三键提示抖一下就消失**。空 composer 时底栏左槽铺一行
   `Ctrl+R 搜历史 · Ctrl+O 展开/收起 · Ctrl+G 编辑器`，用户一打字，整行
   立刻消失；切搜索/菜单/状态也不见。
2. **主 Agent「思考」紧贴下一行**。`思考 1.8s(Ctrl+O 展开)` 与下一行
   模型正文/工具卡之间没有空行，逼仄。
3. **Subagent 工具卡全挤一起**。切进某只 Subagent 看账，多枚工具卡之间
   没有间距，与 Main agent 工具卡之间那口气不一样。
4. **`/skills`（`{0} skills`）上方没有快捷键提示**。skills 行本来该带一段
   「按 ? 查看快捷键 / Ctrl+K …」一类的 hint，不知道哪次重构改没。

四件互不重叠、可独立验收；可并行实施（见 §六）。

## 二、现状诊断

### 2.1 底栏三键错放与绑死空 composer

`src/cli/console_input.cpp:569` 的 `BuildComposerAssistRow()` 固定把三键拼
一行（用 `keymap::BuildKeyHints`），唯一显示门槛在 `console_input.cpp:606`：

```cpp
if (scene.mode == ComposerMode::Idle && scene.framed && composer_empty && !menus_open) {
    model.assist_row = BuildComposerAssistRow();
}
```

——「composer 为空 + Idle + 无面板」三者同时成立才填；任一不满足整行被
清空。一打字（composer 非空）→ 整行被擦 → 屏幕上闪一下消失。

三键 ActionId 与现行绑定（`src/cli/keymap.cpp:218-235`）：

| 键       | ActionId                       | 真实作用对象             |
| -------- | ------------------------------ | ------------------------ |
| Ctrl+R   | `ChatSearchHistory`            | 历史搜索面板             |
| Ctrl+O   | `TranscriptToggleExpand`       | 最近一条工具卡 / 思考块  |
| Ctrl+G   | `ChatExternalEditor`           | 当前 composer 草稿      |

——三者作用对象都不是「底栏本身」。把它们挤在底栏左槽一行本身就不合理。

用户之前定下的规则（对话中确认）：

- **Ctrl+O 贴工具卡 / 思考块自己头上**——底栏不再展示。
- **Ctrl+R 在搜历史场景提示**——`history_search` 面板/搜索条出场时挂上。
- **Ctrl+G 在 composer 有内容且够长时提示**——默认 4 行或 80 字符为门槛；
  位置在 composer 框内（右下/右下脚注），不是底栏。

### 2.2 主 Agent「思考」后没空行

主 Agent 走 `src/cli/turn_renderer.cpp` 的循环渲染（170-227 行）。它在
UserPrompt 之后显式调 `GapBetween(UserPrompt, Thinking)` 垫空行
（198-202），但**相邻两条 item（Thinking ↔ Tool / Thinking ↔ AssistantText /
Tool ↔ Tool）之间没有显式调 `GapBetween`**——只有 `step_id` 不同才塞一行
step 间隔（208-210）。

也就是说：同 step 内的 `思考 …` 与下一枚工具卡之间、工具与工具之间，仅靠
`FormatTranscriptItem` 末尾自带的一个 `\n`，**没有第二口空行**。

对照 `src/cli/transcript.cpp:357` 的 `FormatTranscriptItems`——它内部有
`printed_any` + `GapBetween`，**两层行为不一致**，是 0.29.x 同构渲染单之后
留下来的裂缝（主面板走 turn_renderer，Subagent 面板走 FormatTranscriptItems
+ RenderSessionBlocks）。

测试覆盖：tests/unit/cli/test_transcript.cpp:738 的
`FormatTranscriptItems 间距:顶层 Tool 之间恰一口` 仅校验
`FormatTranscriptItems` 这一条路径，**没**覆盖 `turn_renderer` 的主面板
路径——所以这条裂缝一直没被测到。

### 2.3 Subagent 工具卡挤一起

Subagent 走
`agent_panel_presenter.cpp::TaskTranscriptLines` →
`RenderSessionBlocks`（`transcript.cpp:394`）→ `FormatTranscriptItems`
（`transcript.cpp:357`）。`FormatTranscriptItems` 内部
`printed_any` + `GapBetween(previous_role, role_of(item))` 会按表垫空行
（Tool→Tool=1）。**理论上**应该有空行。

待办：实施时**先在真终端复现**，确认是不是 (a) `MakeAgentTaskToolItem`
返回的 `item.kind` 实际不是 Tool、(b) `pending_items` 全部冲组到同一
SessionBlock 致 block 间没有 GapBetween、(c) `flush_items()` 在 step 切换
时的行为让前后两次 Tool 块之间没空行、(d) 单纯是 `summary_lines` 为空时
视觉上没有「换行感」。

注：同构渲染单（`主Agent与Subagent面板工具调用同构渲染.todo`）已经把
`MakeAgentTaskToolItem` 的 kind 改成「调用方按查看根定」，described
说「Subagent 自己的工具 = Tool，不再无条件 SubTool」。理论上 item.kind 是
正确的——可能根因在 (b) 或 (d)。

### 2.4 `/skills` 上方没有快捷键 hint

`BuildComposerModeLine`（`src/cli/console_input.cpp:435`）是模式行的唯
一生产路：左槽 = 审批档位 + 切档提示，右槽 = `{0} skills`
（i18n `status.skills_count`，`src/cli/i18n.cpp:165`）。skills 行就是模
式行的右槽——它**上面**没有额外的快捷键 hint 行。

「按 ? 查看快捷键」这条 hint 历史上由 `BuildComposerAssistRow` 右槽承担
（`row.right = keymap::FormatKeyChord(*help) + " " + tr("hint.keys.help");`
`console_input.cpp:577-579`）。但 §2.1 改完后，**右槽 hint 也得重做**——
不要让它消失，要么换位置（独立成行），要么换承载（帮助层）。

`/skills` 上方的快捷键 hint 大概率是同一段历史的另一头：可能之前在模式
行右槽 `{0} skills` 之后拼了一段「按 ? …」，某次重构把 `BuildComposerAssistRow`
的 left 槽拆出来后，模式行的 right 槽也跟着瘦身，hint 一起掉了。

待办：实施时跑 `git log --all -p -- src/cli/console_input.cpp | grep -B2 -A2 'for shortcuts'`
或 `git log -S 'shortcuts' src/cli/` 找回归 commit。

## 三、修法（分 P0/P1/P2，独立验收）

### P0：把 §2.1 改成「键贴场景」（用户当下最痛）

`BuildComposerAssistRow` 改为**不再拼三个键**——它现在只负责：

- 右槽：帮助入口（保留「按 ? 查看快捷键」），无绑定则整段不画。
- 左槽：空 composer 时**不**铺任何快捷键（留白，让眼睛歇）。

`Ctrl+R` / `Ctrl+O` / `Ctrl+G` 各自迁移到对应场景：

- **Ctrl+G 编辑器**：在 composer 框内显示（位置：composer 框右下内角；
  实现：扩 `ComposerBoxChrome`，在 `BuildComposerFrame` 时按
  `composer.lines` 长度 / 总字符数决定是否画「`Ctrl+G` 编辑器」小注）。门
  槛常量放 `console_input_internal.hpp`，默认 4 行或 80 字符。
- **Ctrl+R 搜历史**：`HistorySearchSession` 打开时（`HistorySearchActivate`
  或等价路径）在搜索面板头行挂一行「`↑/↓` 选择 · `Enter` 接受 · `Ctrl+R`
  搜历史 · `Esc` 取消」一类的 hint。
- **Ctrl+O 展开/收起**：从底栏彻底拿掉。工具卡/思考块自身头部加角标
  （`*`/`►`/`…` 之类）与 Ctrl+O 的提示文本（`思考 1.8s · Ctrl+O 展开`）。
  这块**也可以**作为单独后续单（P1-2），本单先做到底栏不再误展示。

实现步骤：

1. 在 `console_input.cpp` 新增 `ComposerGHint`（小构造器，仅 Ctrl+G，
   阈值门控）和 `HistorySearchHint`（仅 Ctrl+R）。
2. `BuildComposerAssistRow()` 退化为「只填右槽帮助入口」。
3. `BuildComposerFrame` / `BuildComposerBox` 加一行 `model.g_hint` 槽，
   决定是否在 composer 框内画 Ctrl+G。
4. `BottomChromeModel` / `BottomChromeFrame` 扩字段 `composer_g_hint` /
   `history_search_hint`（仅 Idle/搜索开时填），装配器按场景决定来源。
5. 更新 `tests/unit/cli/test_bottom_chrome.cpp`：
   - 删掉「三键拼一行」相关 CHECK；
   - 加「空 composer 不再有左槽三键」CHECK；
   - 加「composer 长度 ≥ 4 行 / 80 字符时画 Ctrl+G」CHECK；
   - 加「Ctrl+R 不在底栏」CHECK。
6. 更新 `tests/unit/cli/test_keymap.cpp`（行 339）改期望为「右槽帮助
   入口」独立校验。

不在本 P0：把 Ctrl+O 角标贴到工具卡/思考块头部（见 §四 P1-2）。

### P1-1：主 Agent「思考」后留一口气

`src/cli/turn_renderer.cpp` 的循环渲染（208-227）改为：每条非首条 item
**之前**按 `GapBetween(previous_role, role_of(item))` 垫空行
（与 `FormatTranscriptItems` 同一张表）。

实现步骤：

1. 把 `BlockRole role_of(TranscriptKind)` 的 lambda 提到模块作用域或
   `transcript.hpp`（与现有 `role_of` 共享，避免两份）。
2. 在 turn_renderer.cpp 循环内：
   ```cpp
   if (first_printed) {
       const auto role = role_of(item);
       for (int g = 0; g < GapBetween(previous_role, role); ++g) {
           lines.push_back(std::string());
       }
   }
   previous_role = role_of(item);
   ```
   注意 `previous_role` 初始值需重新考虑（之前是 `UserPrompt`，第一条是
   UserPrompt 时刚好 0；改成跟随实际最后一条更稳）。
3. step 间隔（208-210）保留——它表达的是「step 换拍」语义，与 GapBetween
   不冲突，可视为上一条 → 下一条的「额外半口气」；如果两者都触发，确认
   不重复（一行就好）。
4. 测试：tests/unit/cli/test_transcript.cpp 新增
   `RenderTurnView 间距:思考 → 工具 之间留一口` 用例，喂 `[{thinking},
   {tool}]`，断言中间有 `\n\n`。

### P1-2：Subagent 工具卡补回间距

步骤：

1. **真终端复现**：Subagent 跑两个连续 tool，截图抓行序列（用 ANSI 测试
   harness 或直接看 RenderSessionBlocks 输出）。
2. 若是 (a) item.kind 错——`MakeAgentTaskToolItem` 调用方 + factory 双方
   加测试。
3. 若是 (b) flush_items 切分不对——给 step 切换加显式空行 / item 间
   GapBetween。
4. 若是 (d) 视觉错觉——给 Tool 卡的 summary_lines 至少留一行（即使没有
   结果也补 `[OK]`/失败兜底，主重放同款口径已写，见 test_transcript.cpp:779）。
5. 测试：tests/unit/agent/test_agent_panel.cpp 加
   `TaskTranscriptLines 间距:连续两枚 Tool 之间恰一口` 用例，断言中间
   有空行。

P1-2 的根因要 §三 P1-1 跑完才能完全定位——若 P1-1 引入共享
`role_of` 后看到一处真相，立刻同步过来。

### P2：`/skills` 上方快捷键 hint

步骤：

1. `git log -S 'shortcuts' src/cli/` 找回归点。
2. 找不到（纯用户口述回忆）则按现状恢复：在 `BuildComposerModeLine` 的
   skills 右侧或下方加一行「`?` 快捷键 · `Ctrl+K` 技能面板」之类的小字。
   位置：模式行的右槽右下方（与 `BuildStatusLine` 资料行合并）；窄屏时
   折叠。
3. i18n：新增 `status.skills_hint`（zh + en）。
4. 测试：`tests/unit/cli/test_console_input_composer.cpp` 加
   `BuildComposerModeLine 添加 skills hint` 用例。

## 四、并行边界

可并行：

- **P0（`/ctrl+g` 框内提示 + `Ctrl+R` 搜历史面板 hint + 底栏退化）**：
  主改 `console_input.cpp` + `bottom_chrome.*` + 测试。**独立文件面**，
  与 P1-1 / P1-2 / P2 文件交集 = 0。
- **P1-1（turn_renderer GapBetween）**：主改 `turn_renderer.cpp`，
  文件交集 = 0。
- **P1-2（Subagent 工具卡间距）**：主改 `agent_panel_presenter.cpp`，
  跨 P1-1 仅共享 `role_of` 提取——先开 P1-1 抽 `role_of`，P1-2 复用。
- **P2（/skills hint）**：主改 `console_input.cpp`（`BuildComposerModeLine`）
  + i18n，文件交集 = 0。

**串行依赖**：仅 `role_of` 提取必须 P1-1 先做（P1-2 等）。建议 P1-1 与 P0
并行开工，P1-2 等 P1-1 `role_of` 提交后并入。

## 五、验收（独立可红/绿）

- **P0**：
  - `tests/unit/cli/test_bottom_chrome.cpp` 增/改 ≥ 3 个用例，全部红→绿。
  - 手工：composer 空时，底栏不再有 `Ctrl+R/Ctrl+O/Ctrl+G` 三键；composer
    输到第 5 行 / 80 字，composer 框内右下出现「Ctrl+G 编辑器」；打开历史
    搜索，搜索面板头部出现「Ctrl+R 搜历史」。
- **P1-1**：
  - `tests/unit/cli/test_transcript.cpp` 新增 `RenderTurnView 间距:思考 →
    工具` 用例，断言 `\n\n` 出现。跑现有 focused suite 全绿。
- **P1-2**：
  - `tests/unit/agent/test_agent_panel.cpp` 新增
    `TaskTranscriptLines 间距:连续两枚 Tool 之间恰一口`。真终端 Subagent
    跑两个 tool，截图证空行存在。
- **P2**：
  - `tests/unit/cli/test_console_input_composer.cpp` 新增
    `BuildComposerModeLine:skills 行旁附 hint` 用例。手工看屏：模式行右槽
    `2 skills · ? 快捷键` 或同义文本。

## 六、派工建议

- 子代理 A：P0（底栏键贴场景）
- 子代理 B：P1-1（主 Agent 思考后空行 + `role_of` 提取）
- 子代理 C：P2（/skills 上方 hint）——可在 P0 改 console_input.cpp 后接续
  提交，避免同一文件二次冲突
- 子代理 D：P1-2（Subagent 工具卡间距）——等 P1-1 `role_of` 提交后开工

聚焦测试：P0 + P1-1 + P2 同时开工；P1-2 等 P1-1 `role_of` 提交后再开。
