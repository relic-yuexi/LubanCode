# 向 Claude Code 与 Codex 补齐终端交互小细节

状态：待做（交互抛光总账，2026-08-15 按官方文档核过）

## 为什么另立这张单

大功能人人看得见，小细节才管手感。少按一次键，少丢一份草稿，少猜一回当前焦点，用久了便差出一截。

这张单只收 Claude Code、Codex 已有，而 LubanCode 尚缺或只做了半截的交互。已有专单不在这里重写：

- `Ctrl+C` 有字先清字：见 `todos/CtrlC优先清空非空输入框.todo`。
- 忙碌时排队、工具边界送达、取回编辑：见 `todos/排队消息在工具边界送达并可Shift左键编辑.todo`。
- 子代理列表、停止、切入与定向介入：见 `todos/子代理面板移到输入框上方.todo`。
- `Ctrl+O` 展开思考与工具：见 `todos/思考过程CtrlO展开.todo`。

LubanCode 已有 `/resume`、`/title`、`/export`、`/background`、可配置状态栏、图片 `@路径`，也不重造一遍。下文若提到它们，只补缺的那一寸。

## 总规矩

1. 先补发现性，再添快捷键。每枚键都须在当前场景的 footer 或 `?` 帮助里找得到。
2. 快捷键只是一层皮。底下先有可测试的语义动作，再映射 Win32、ConPTY、POSIX 键序列。
3. 草稿、焦点、目标、确认档须各自存账。切模式不能丢字，也不能把给甲写的话送给乙。
4. 新功能先服从现有权限、sandbox、命令安全分类与会话存档。`!` 不是逃确认的暗门。
5. Windows Terminal、VS Code Terminal、conhost、常见 POSIX 终端都要测。终端截键时，给替代键与诊断，不装作按到了。

## 第一批：费力小，回报大

### 1. `Ctrl+R` 反向搜索提问历史

现下只有 `Up/Down` 逐条翻。本场一长，找一句旧提示要翻半天。

要补一只原地搜索框：

- `Ctrl+R` 打开，边输边筛；再次 `Ctrl+R` 往更早一条走。
- `Ctrl+S` 可轮换范围：本会话、本项目、全部项目。若 `Ctrl+S` 被终端流控截走，屏上换成实际可用键。
- 匹配词高亮，条目旁写时间、会话标题与项目短名。
- `Tab` 或 `Esc` 接受结果回 composer 继续改；`Enter` 接受并提交；`Ctrl+C` 取消并还原原草稿。
- 历史索引只读 session 事件账，不把工具结果、密钥打码前文本或未发送草稿混进来。
- 搜索开着时终端缩放、中文宽字符、长提示软换行，光标都不能跑偏。

验收：三种范围各能命中；取消后原草稿一字不少；恢复旧提示再编辑，不改写原 session 事件。

### 2. `@` 项目文件与目录模糊提及

LubanCode 眼下把 `@路径` 主要当图片入口。该把它扩成统一提及菜单：

- composer 任意位置键入 `@`，按 cwd/Git 根模糊搜文件与目录；方向键走，Enter/Tab 选，Esc 收起。
- 结果写清图标或类型、相对路径、命中片段；忽略 `.git`、build 产物与配置排除项。
- 文本文件、目录、图片分清：图片沿用视觉附件；文本与目录插入结构化 mention，不靠模型猜一串裸路径。
- 带空格、中文、反斜杠、同名文件都能选。显示用相对路径，内部留规范绝对路径与项目根校验。
- mention 可用 Backspace 整枚删，也能左右越过；提交前目标消失或跑出工作区，要明报错。
- `/image` 继续可用；旧的 `@image.png` 不回归。

验收：键入 `@main` 能选 `src/main.cpp`；键入 `@src/cli` 能提及目录；图片、文本、目录送到 wire 后类型不串。

### 3. `/copy` 复制上一段完整答话

工具全文已有 `Ctrl+E`，可用户常要拿走最后一段助手答话。补 `/copy`：

- 默认复制最近一条已完成的 assistant 文本，不夹 ANSI、spinner、token 统计与工具摘要。
- 答话含 Markdown 时复制原始 Markdown；另可留 `/copy plain`，复制终端可见纯文。
- 尚在流式时，默认拒绝并提示“答话未完”；若以后支持快照，要明写只复制到当前处。
- 剪贴板失败须报错，不得打印“已复制”后空着。
- Windows 用 Unicode clipboard；POSIX 依次探 OSC 52、平台命令，皆不可用便给明白提示。

验收：中文、代码块、超长答话、远程 SSH、无剪贴板环境各有确定结果。

### 4. `!` 本地 shell 快捷模式

在空 composer 首字符敲 `!`，后文当本地命令，不发给模型：

- 输出实时落在转录里，结束后写退出码与耗时。
- 命令和输出可选写入本轮上下文，让下一句“解释上面报错”真能看见；状态行须标明“将加入上下文”或“只在本地”。
- 仍走 `ClassifyCommand`、确认档、cwd、超时、输出上限与 UTF-8 清洗。不可另开一条无护栏执行路。
- `Ctrl+B` 可把运行中命令转后台；转后台后复用现有任务 registry、日志与 `/background`。
- 输入半截历史命令按 Tab 补全；空命令按 Esc、Backspace 或 `Ctrl+U` 退回普通 composer。
- 粘贴一段以 `!` 开头的正文，不能未经确认便执行多行脚本。bracketed paste 须保住“粘贴”和“逐键敲入”的区别。

验收：安全命令直跑，危险命令照问；输出能供下一轮引用；后台后输入框立刻归还；多行粘贴不误执行。

### 5. 长提示外部编辑器

补一枚可配置动作，默认可取 `Ctrl+G`：

- 把当前完整草稿写入权限收紧的 UTF-8 临时文件，调用 `$VISUAL`，没有再看 `$EDITOR`。
- 编辑器正常退出后读回 composer，保住多行、光标与目标；文件非法 UTF-8 时不吞原草稿。
- 启动失败、非零退出、文件被删，原草稿都留着，并给短错。
- 临时文件退出后清理；崩溃残件下次启动可扫，但不能误删别家文件。
- 给 main、流式 footer、subagent 定向 composer 共用同一实现。

验收：VS Code、Vim/Neovim、Notepad 各走一遍；原草稿与新草稿来回不丢换行。

### 6. 暂存 composer 草稿

补 `stash` 动作，快捷键可配置，不急着硬占 `Ctrl+S`：

- 一键把当前草稿收起，composer 变空；再按取回。
- 草稿带目标、cwd、会话 id、创建时间。main 与各 subagent 分账，不能串收件人。
- 最少支持一格；若做多格，用轻量选择器，不在状态栏铺一长串。
- 退出或 `/clear` 遇到 stash，要提示保留还是丢弃；默认不把未发内容写进模型 history。

验收：main 存一份、subagent 存一份，来回切换各归各；进程异常退出后按既定隐私策略恢复或明确不恢复。

### 7. 多行历史的边缘手感

现下 `Up/Down` 写作“单行状态翻历史”。改成编辑器常见规矩：

- 多行内按 `Up/Down` 先在逻辑行间移动光标。
- 只有光标已在第一行再按 Up，才翻到上一条历史；在末行再按 Down，才往新处走。
- `Ctrl+P/Ctrl+N` 可作明确的上一条/下一条历史别名，不受多行位置影响。
- 从历史取回后动一个字，立刻落成新草稿，不改历史原件。

验收：中英文宽字符、软换行与显式换行都按视觉位置走，不突然跳走整段。

### 8. 剪贴板图片直贴

除 `/image` 和 `@图片路径` 外，再补粘贴图片：

- Windows 可用 `Alt+V`，macOS/Linux 按终端能力映射；普通 `Ctrl+V` 文本粘贴不受影响。
- 从剪贴板取位图，转成受限临时 PNG，先画附件 chip，再随消息提交。
- 明写尺寸、压缩后大小与张数；超过 5 MiB 或尺寸上限先提示，不暗降到糊成一团。
- 删除 chip 就删临时件；发送、取消、崩溃恢复都有清理账。

验收：截图后不落盘也能贴；文本与图片混贴；模型不支持视觉时在发送前便拦住。

## 第二批：让界面会说话

### 9. `?` 场景帮助与统一按键提示

所有可聚焦面板都认 `?`：composer、slash 菜单、工具全文、subagent、排队区、确认菜单、历史搜索。

- 只列当前场景有效键，不倒整本 `/help`。
- footer 先摆三四枚常用键，`?` 再展开全表。
- 按键文案从 keymap 反查，用户改键后提示跟着改，不能写死。
- 不支持的终端能力不露假快捷键。

验收：任何面板里按 `?` 都能开、Esc 能退，原焦点和草稿不丢。

### 10. 可检查、可改、可落盘的 keymap

补 `/keymap`：

- 列动作名、当前键、作用域、冲突与终端是否能送达这枚键。
- 可搜索、改绑、复位单项或全部；写入用户配置，项目配置不得暗改全局键位。
- 同一场景冲突就拒绝保存；不同场景可复用。
- 动作名按语义取，如 `chat.search_history`、`chat.external_editor`、`agent.stop`，不可把 `VK_` 常量漏到配置层。
- 预留 Vim composer 模式，但另立实现，不拿一堆 if 临时拼。

验收：改绑后 footer、`?`、实际输入三处同日生效；重启仍在；坏配置回退并报具体项。

### 11. 短通知、终端标题与“轮到你了”

长任务跑完，或 `ask_user`、工具确认等着答时：

- 终端未聚焦超过可配置时长，发桌面通知；拿不到桌面能力便响 bell 或改终端标题。
- 通知只带项目短名、会话标题、状态与安全摘要，不塞提示词、命令输出、文件正文。
- 同一状态去重并节流；窗口正聚焦时不吵。
- 点击通知若平台支持，带回对应终端；做不到便不假装。
- `/title` 仍管会话名；另给终端 tab 标题模板，可选项目、分支、状态、当前目标。

验收：后台完成、等待确认、等待用户、失败四态各发一次；连续流事件不刷通知风暴。

### 12. 临时 toast 不污染 transcript

“已复制”“已切模型”“后台任务完成”“队列已送达”这类短消息，只在 footer 上方露一会儿：

- 带级别、短文、过期时间与可选动作。
- 新 toast 替旧 toast 或排有限小队，不能一路写进永久对话，把正文顶走。
- 严重错误仍进 transcript；哪些算严重须有统一枚举，不由各调用点随手 `cout`。
- toast 出现、消失、终端缩放都走同一帧布局，不留残影。

验收：连做十次复制，历史里不多十行；失败信息仍可找回。

### 13. 后台任务清单再补三行尾巴

现有 `/background` 已能列 id、状态、PID、命令、日志。再学 Codex `/ps` 那点省眼力的手法：

- 每项附最近三行非空输出、启动时间与已跑时长。
- 运行中任务可选中查看全文、停止、清理终态；停止须接现有 registry 正式接口。
- 命令很长先折，完整内容在详情里看；输出正增长时只重画当前项，不刷整屏。
- 可留 `/ps`、`/stop` 作短别名；帮助只把一套语义说清。

验收：十只后台任务也能扫清；日志文件被删、非法 UTF-8、进程已死都不会把菜单带崩。

### 14. 转录跳转、导出到 scrollback、外部查看

`Ctrl+O` 管展开层次，另补“在长对话里找路”：

- `{` / `}` 跳上一条、下一条用户提问；状态行写“第 N/M 轮”。
- `[` 把当前完整转录写入原生终端 scrollback，方便用终端自带搜索。
- `v` 把转录写临时 Markdown，交 `$VISUAL/$EDITOR` 只读查看。
- 导出的内容须注明折叠、截断、compact 点与缺失工具全文；不可把屏上省略号冒充全本。
- 这只是查看，不改活 history；正式存档仍走 `/export`。

验收：百轮会话能从末尾跳回第一问；退出查看后回到原 composer 与光标。

### 15. 强制重画与终端能力诊断

终端偶有残影，不能只叫用户重开：

- 提供 `redraw` 动作，一帧重建 transcript 可见区、代理区、队列、composer 与状态栏，保住草稿和焦点。
- `/config` 或 `/doctor` 列 TTY、颜色、同步输出、bracketed paste、modifyOtherKeys/kitty keyboard、OSC 52、桌面通知、终端尺寸与检测来源。
- tmux/screen/SSH 下给针对性提示；不能识别便写“不知”，不瞎报“支持”。
- 重画不重复写 session event，也不重复触发工具或通知。

验收：故意往终端写坏一截 ANSI 后能一键收净；诊断输出可贴进 bug 单，且不含密钥。

## 第三批：有状态风险，另排设计评审

### 16. 回改上一条与从旧处分叉

Codex 的空 composer 双 Esc 可回改上一条用户消息并从那里 fork；Claude Code 还能把对话或代码退到先前 checkpoint。这事好用，也最容易误伤，须拆开做：

第一阶段只做“会话分叉”：

- 空 composer 连按两次 Esc，打开用户消息列表，不立刻改东西。
- 选中旧消息后，把正文装进 composer；提交时新建 session id，从选中点复制中立 history，再接新消息。
- 原 session 与工作区一字不动；标题明写“已从某时某轮分叉”。
- 工具调用与 tool result 必须成对截断，不能留孤儿块。

第二阶段才议“代码 checkpoint 回退”：

- 只回退 LubanCode 自己明确记账的文件变更；用户外部修改、未跟踪文件、并行 agent 改动不可一锅覆盖。
- 先展示将恢复、保留、冲突的文件，默认只回对话不回代码。
- 不借 `git reset --hard` 偷懒；要有可恢复备份、路径边界与冲突处理。

验收：从第 20 轮分叉后，两场会话都能独立恢复；原工作树不变；工具块不破；代码回退必须二次确认并可撤销。

### 17. `/new`、`/fork`、`/side` 三种会话动作分清

现有 `/clear` 清活 history，`/resume` 接旧 session。再补三种不会混淆的动作：

- `/new [标题]`：同一进程里开全新 session，不清终端 scrollback。
- `/fork [标题]`：复制当前 history 与上下文设置，换新 id 后继续。
- `/side [问题]`：从当前点开临时岔路；主任务可继续跑，岔路答完回主线，不把岔路闲聊塞回主 history。

每种动作都须显示 session id、父会话与 cwd；stash、队列、subagent 目标如何带走，要先定规矩。默认：未发送草稿不复制，运行中任务不克隆，项目记忆只按 cwd 重新召回。

验收：三种动作在 `/sessions` 里看得出关系；恢复父子会话不会串 history；`/side` 结束后主 composer 原草稿仍在。

### 18. 离开太久后的会话小抄

终端失焦三分钟以上，再切回来时，在后台生成一份短 recap：

- 只写“刚做完什么、正在跑什么、等你决定什么、有哪些未送消息”。
- 不写成新的 user/assistant history，不吃主对话上下文；可缓存成 UI 状态或 session 辅助事件。
- 一次失焦只生成一份；任务没有新变化便不重复。
- recap 失败静默降级，不拦主任务，不另刷模型请求风暴。

验收：长任务回来一眼能接上；一直盯着窗口时不平白冒总结；compact 前后指向同一批真实状态。

## 建议落刀次序

```text
1. 先抽 Action / Keymap / Capability 三层
2. Ctrl+R + /copy + 多行历史边缘
3. @ 提及 + 外部编辑 + stash + 图片剪贴板
4. ? 帮助 + toast + 通知 + 后台清单 + 转录导航
5. ! shell 模式
6. 会话 fork / side
7. 最后单独评审 checkpoint 代码回退
```

头四步只抛光交互。第五步碰命令权限。第六步碰会话谱系。第七步碰工作区数据。三层莫混成一只巨型 `switch`。

## 测试总账

- 纯逻辑：Action 分发、keymap 冲突、历史搜索范围、mention 解析、stash 分账、session 分叉截断。
- 平台键位：Win32 `KEY_EVENT_RECORD`、ConPTY、POSIX CSI、tmux、IME、终端流控。
- screen driver：窄窗、宽字符、软换行、菜单叠层、终端缩放、toast 过期、焦点恢复。
- session fixture：fork/new/side 的 id、父子关系、tool use/result 配对、compact 点。
- 权限：`!` 与外部编辑器不绕确认，不越 cwd，不泄密。
- 真机：Windows Terminal、VS Code、conhost；Linux 终端与 tmux；SSH 至少一回。

## 不做

- 不照抄别家的键位后硬占现有 `Ctrl+O`、`Ctrl+E`、`Shift+Tab`。
- 不把每个小功能塞回 `interactive_session.hpp` 的大 switch；命令只分发，状态各归模块。
- 不为了一枚快捷键造第二套执行、权限、会话或渲染通道。
- 不把 UI 提示写进模型 history。
- 不用隐藏的 destructive Git 命令冒充 checkpoint。
- 不因“竞品有”便一次全开。每批做完，先跑真机，再开下一批。

## 官方参考（核对日期：2026-08-15）

- OpenAI Codex CLI：`https://learn.chatgpt.com/docs/codex/cli`
- OpenAI Codex developer commands：`https://learn.chatgpt.com/docs/developer-commands?surface=cli`
- Claude Code interactive mode：`https://code.claude.com/docs/en/interactive-mode`
- Claude Code keybindings：`https://code.claude.com/docs/en/keybindings`
- Claude Code status line：`https://code.claude.com/docs/en/statusline`
- Claude Code checkpointing：`https://code.claude.com/docs/en/checkpointing`

这些页面会变。开工前再核一次，莫拿旧截图当协议。
