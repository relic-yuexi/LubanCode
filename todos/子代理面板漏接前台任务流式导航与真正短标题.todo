# 子代理面板漏接前台任务、流式导航与真正短标题

状态：待做（交互回归与未完成功能）

## 现场

主代理在隔离 worktree 里派一只子代理干活。流式画面如下：

```text
房间建好了（分支 worktree/memory-upgrade）。派子代理 A 干第一期：

● agent(你在一个 C++ 项目的隔离 git worktree 里实施“项目记忆系统升级...”)
● 你在一个 C++ 项目的隔离 git worktree 里实施“项目记忆系统升级...
  ⎿ 运行中(7 次工具调用 · 17.0k tokens · 43.4s)
  (ctrl+o 展开明细)

────────────────────────────────
> 键入并回车 排队下一条
────────────────────────────────
```

该有的东西没出来：

- 没有真正的任务短标题，例如“项目记忆升级一期”。
- 没有 `main / 子代理 A / 子代理 B` 这张可选择列表。
- 没有 `↑/↓ 选择 · Enter 查看` 提示。
- 流式期间按上下键不能在代理之间切换。
- 进入代理后，输入框上沿右端也没有当前代理短标题。

这不是用户记错。已经销掉的旧单《子代理面板移到输入框上方》明写过：空闲与流式共用一套面板；上下可切；Enter 可看；输入框右上角挂短标题。现行实现只落了空闲后台任务这半边，旧单却在 `6a3ae99` 被当成完工单删除。

## 三个病根

### 1. 新面板只认后台任务

`InteractiveSession::BuildAgentPanelEntries()` 的数据来自 `AgentTool::TaskSummaries()`。

`TaskSummaries()` 又只遍历 `AgentTool::tasks_`。这本台账只在 `LaunchBackground()` 里建 `TaskRecord`。前台路径 `ExecuteForeground()` 直接调用 `RunTask()`，不会分 task id，也不会进入 `tasks_`。

于是：

```text
run_in_background=true
-> TaskRecord
-> TaskSummaries
-> 新 AgentPanel

run_in_background=false
-> ToolDisplay::AgentStatusBoard
-> 旧三行状态条
-> 新 AgentPanel 完全看不见
```

截图里的父级 `agent(...)` 工具四十多秒没有返回，正是前台子代理。故而只画出旧状态块。

### 2. 新面板控制器只活在空闲 composer

`AgentPanelController` 是 `ReadLineKeyByKey()` 的局部对象。空闲时它能接 `Up/Down/Enter/Esc/x`。

模型流式输出期间，按键由 `TurnInputListener::ThreadMain()` 接走。这条路没有 `AgentPanelController`，也不读 `AgentPanelProvider`。`Up/Down` 仍按旧规矩处理排队消息编辑、旧的“上键取回”别名或输入历史。

所以流式画面里纵然有后台任务，新面板也无法真正聚焦、切换或查看。空闲与流式没有共用状态，更谈不上从流式回到空闲后保住选择。

### 3. 所谓“短标题”根本不是标题

现有两处都拿完整 prompt 生截：

- `AgentStatusBoard::Start()`：清掉换行后截前 40 个码点。
- `BuildAgentPanelEntries()`：清掉换行后截前 34 个码点。

这只能得到“你在一个 C++ 项目的隔离 git worktree 里实施……”之类任务说明开头。多只代理若共用一段模板，前几十字一模一样，根本分不清谁干什么。

**不准再拿 prompt 前 34/40 字冒充标题。**

真正短标题必须是独立、语义完整的任务字段。例如：

```text
项目记忆升级一期
检索阈值回归
修复流式代理导航
核查 UTF-8 崩溃链
```

## 需求

### 一、`agent` 工具增加必填语义标题

工具入参改成：

```json
{
  "title": "项目记忆升级一期",
  "prompt": "你在一个 C++ 项目的隔离 git worktree 里……",
  "agent_type": "general-purpose",
  "run_in_background": false,
  "isolation": "worktree"
}
```

`title` 与 `prompt` 各司其职：

- `title`：给人看。短、准、能彼此区分。
- `prompt`：给子代理执行。须自包含，可长，可带路径、范围与验收。

标题规矩：

- 新建任务必须传 `title`。缺失、空串、只有空白便拒绝工具调用，并提示主模型补标题后重试。
- 中文建议 4～16 字；英文建议 2～6 个词。trim 后不得含换行、回车或制表符，硬上限 40 个显示列；超过便拒绝，不替调用方截成另一句话。
- 标题必须是名词短语或短命令，不得照抄 prompt 首句，不得塞路径清单、验收全文、模型人格或“你在某项目里”这类套话。
- 保存进 `AgentTaskSnapshot` 与 `AgentTaskSummary`。列表、详情、右上角标签、完成回流与日志都读同一字段。
- 终端窄时可以按显示宽度截**标题字段本身**，加省略号；这只是排版，不是从 prompt 造标题。
- 旧 session 或旧任务没有 `title` 时，只显示“未命名子代理 #N（旧任务）”。绝不回退到 prompt 前若干字。
- 工具描述与主系统提示写清：派子代理时先概括 title，再写完整 prompt。不要额外起一次模型请求专门生成标题；调用工具的主模型已经知道任务主题。

测试也要封死歪路：给一段以“你在一个 C++ 项目的隔离……”开头的长 prompt，再给 title“项目记忆升级一期”；屏上只能出现 title，不得把 prompt 开头拿来当列表名。

### 二、前台与后台任务共用一份台账

不能再让前台走 `AgentStatusBoard`、后台走 `TaskRecord` 两本账。

`AgentTool` 每次调用，无论前台后台，都先建一枚稳定 task id 与 `TaskRecord`：

```text
CreateTaskRecord(title, prompt, type, foreground/background)
-> Running
-> 工具调用、usage、实时输出都写这条
-> Done / Failed / Cancelled
-> 面板继续可查，用户 x 后才清
```

要求：

- 前台任务也出现在 `TaskSummaries()` 和 `TaskDetail()`。
- 前台任务的本地 `sub_loop` 也接自己的 inbox，用户进入该代理后发的话能在下一工具边界送达。
- 前台任务仍可阻塞父级工具调用等结论；“进面板台账”不能偷偷把它改成后台语义。
- 后台结果回流、`delivered`、完成通知原义不变。
- `TaskRecord` 增 `foreground` 或等价来源字段。详情里可看，列表不必铺。
- tool call 次数、token、耗时只认统一台账。旧 `AgentStatusBoard` 要么删除，要么退成统一台账的纯 painter；不能再自己分 id、自己抄 prompt。
- 一轮里先后起多只前台代理，面板须留下各自终态，不能新一只起来便清空上一只。

### 三、空闲与流式共用同一枚面板状态机

`AgentPanelController` 不能再是 `ReadLineKeyByKey()` 的短命局部对象。把它提成会话级、线程安全的面板控制器，或放进一枚 `AgentPanelSession`：

```text
AgentPanelSession
  controller       选中、聚焦、详情、停止全部确认
  selected_task_id 不只记易漂移的数组下标
  composer_target  当前消息收件人
  revision         数据与画面修订号
```

空闲 composer 与 `TurnInputListener` 都调同一套 `HandleKey()`。任务增删后按 task id 修正选择，不能只靠下标硬夹回界内。

两态须有同一语义：

```text
↑/↓       在 main 与各子代理间循环选择
Enter     进入/收起当前代理详情；进入后 composer 目标切给该代理
Esc       先退详情，再退代理焦点，最后才走普通打断/清输入
x         运行中代理 = 停止；终态代理 = 清除
Ctrl+X K  两段确认停止全部运行中代理
```

正文非空时不抢上下键、Enter 与普通字母 `x`。但进入代理查看态后，composer 本来就是给该代理写消息，标签与目标须一直保住。

### 四、定清流式期间的按键优先级

现有流式 `Up/Down` 同时背着队列编辑、旧上键取回与历史浏览。合流后须定死优先级：

1. 正在编辑一条排队消息：上下键只在排队消息间走。
2. 面板已经聚焦或详情已开：上下键只切 main/代理。
3. composer 为空、面板有代理、未处于队列编辑：上下键进入面板焦点并切换。
4. composer 非空：上下键归编辑器/历史。
5. “空输入按 Up 取回最后一条队列消息”只是旧别名；有代理面板时不得抢键。主入口仍是 `Shift+←`。

Enter、Esc 也照状态分层：

- 队列编辑态的 Esc 先取消编辑。
- 代理详情态的 Esc 先退详情，不中断模型。
- 代理焦点态的 Esc 再退焦点。
- 三者都没有，Esc 才打断当前流式响应。

屏上提示必须跟当前层同步，不能写“↑/↓ 选择”却让键落去取回队列。

### 五、流式 footer 真正画新面板

流式期间的固定次序：

```text
模型正文 / 工具转录
代理面板（main + 前台/后台子代理）
代理详情（若打开，受高度预算约束）
排队消息
输入框上横线（右端可挂当前代理 title）
composer
输入框下横线
状态栏
```

要求：

- 流式 painter 也从统一 `AgentPanelProvider`/session 取条目，调用同一 `LayoutAgentPanel()`。
- 不再同时画旧三行 `AgentStatusBoard` 与新列表。只能有一套状态灯、计数与耗时。
- 面板按终端高度窗口化。输入框与状态栏始终留在视口里。
- 任务状态、选择、详情或标题变化时原地重画，不铺重复残骸。
- ask_user、工具确认打开时暂停面板重画；菜单退场后恢复。
- 流式转空闲时保住 selected task id、详情态与 composer target。不能一回到空闲就跳回 main。
- plain、管道、重定向不夹 ANSI。不能定位光标时降级为只打印紧凑状态，不承诺交互切换。

### 六、真正短标题出现在该出现的地方

以 title“项目记忆升级一期”为例：

列表：

```text
↑/↓ 选择 · Enter 查看 · x 停止/清除
  ● main  主会话
❯ ◌ general-purpose #1  项目记忆升级一期  运行中(7 次工具调用 · 17.0k tokens · 43.4s)
```

进入查看态：

```text
──────────────────────────────────────── 项目记忆升级一期
> 别先上向量库，先完成 review 档
────────────────────────────────────────────────────────────
```

详情里另列完整 prompt：

```text
任务标题：项目记忆升级一期
任务说明：
  你在一个 C++ 项目的隔离 git worktree 里实施……
```

列表与横线标签只认 `title`。完整 prompt 只在详情出现。两者不得混用。

### 七、停止、清理与消息投递都按稳定 task id

- 上下切换后，`x` 必须停/清屏上选中那只，不能因任务刚完成、列表重排而打到邻居。
- 进入代理后提交消息，按 task id 投进该任务 inbox，不经 main，不串给别只。
- 前台任务也能收信。当前工具收尾后、下一次子代理请求前注入。
- 任务先结束，明确拒收，不改投 main。
- 任务被清理、`/clear`、退出或切 worktree 时，面板关闭目标，未送消息给清楚回执。
- foreground/background 混排时，选择、取消与投递照样准确。

## 代码落点

- `src/tools/agent_tool.hpp/.cpp`
  - `agent` schema 墘必填 `title`。
  - `AgentTaskSnapshot/Summary` 增 `title` 与前后台来源。
  - 前台也建 `TaskRecord`、task id、inbox 与统一统计。
  - 收掉从 prompt 生成显示名的路径。
- `src/cli/agent_panel.hpp/.cpp`
  - controller 按稳定 task id 选择。
  - 空闲/流式共用会话级状态。
  - 行与右上角标签只用 title。
- `src/cli/console_input.cpp`
  - `ReadLineKeyByKey` 不再私藏 controller。
  - `TurnInputListener` 接面板按键与布局。
  - 定死面板、队列编辑、composer 的键位优先级。
- `src/cli/agent_status.*`
  - 删除旧独立台账，或改成统一 task snapshot 的纯格式器。
  - 禁止 `Start(prompt)` 再截前 40 字。
- `src/cli/tool_display.hpp`
  - `agent` 工具起停改写统一任务状态，不另起显示 id。
- `src/app/interactive_session.cpp`
  - provider 同时给前台与后台。
  - 详情显示 title + 完整 prompt。
  - 会话收场、worktree 切换与 composer target 接稳定 task id。
- `src/cli/i18n.cpp`
  - 标题缺失、旧任务无标题、流式面板提示中英成对。

## 验收

- 截图同款前台 `agent` 运行时，旧三行 prompt 状态块不再出现；输入框上方出现新面板。
- 面板行显示“项目记忆升级一期”，不显示“你在一个 C++ 项目的隔离……”这段 prompt 开头。
- 新 `agent` 调用缺 `title` 时明确报错，补 title 后才能启动。
- 前台一只、后台一只并行存在时，列表含 `main + 2` 条，可用上下键逐条选到。
- 模型流式期间，composer 为空按上下键，当场出现选中标记；Enter 展开正确代理详情。
- 详情里看得到完整 prompt；列表与右上角只见真正 title。
- 进入第二只代理后，横线右端挂它的 title；发消息只进第二只的下一次 request。
- Esc 依次退出详情、代理焦点，再按才打断模型。不可第一下便掐掉整轮。
- 队列编辑与代理选择同时存在时，按既定优先级走，不串目标、不误取回。
- 前台代理跑完后状态变 Done，工具次数、token、耗时与真实值一致；没有新旧两套重复条目。
- 代理完成、失败、取消、清理时，选择按 task id 保住；选中项消失才回 main 或相邻项。
- 流式转空闲后，面板位置、选择、详情与 title 不跳、不重铺、不留残影。
- 80/120/160 列与矮终端都测；title 可排版截宽，prompt 不得趁机混进标题。
- Windows Terminal、VS Code Terminal、conhost 真机通过。

## 测试

### 纯逻辑

- `agent` schema：title 必填，空白、多行与超过 40 显示列一律拒绝。
- title 与 prompt 完全不同，快照、摘要、列表、标签始终读 title。
- 旧任务缺 title 显示“未命名子代理 #N”，不截 prompt。
- controller 按 task id 走首尾、任务重排、当前项结束与清除。
- 面板/队列/composer 的按键优先级表。

### 任务台账

- 可阻塞 fake 前台 agent 启动后，`TaskSummaries()` 当场出现 Running 条目。
- 前台与后台混排；两边工具调用、usage、终态各自落准。
- 给前台 #2 的消息只进 #2 下一次 captured request，main 与后台 #1 均无。
- 前台取消后父级工具正常收到取消结果，台账变 Cancelled，不留悬线程。

### 真控制台

补一只流式前台代理 screen driver：

1. fake 主模型调用带 title 的前台 `agent`。
2. fake 子代理卡在可控工具里至少数秒。
3. 抓屏断言面板在输入框上方，title 可见，prompt 前缀不在列表。
4. 发 Up/Down/Enter/Esc，逐帧断言选中、详情、右上角标签与退出层级。
5. 放开子代理，断言 Running 原地变 Done，回到空闲不重复。

现有 `agent_panel_driver` 只用假 provider 验空闲画面，不足以覆盖这次 bug；必须新增“真前台任务 + 真流式监听”这一条。

## 不做

- 不再用 prompt 前 N 字、首句、第一处标点或正则摘要冒充真正短标题。
- 不额外调用一次模型只为起标题；派活的主模型在 `agent` 入参里直接给 title。
- 不靠把所有 agent 强改成后台来绕过前台漏账。
- 不保留旧 `AgentStatusBoard` 与新面板同时画，制造双份状态。
- 不让流式 Up/Down 一会儿切代理、一会儿取队列，却不给屏上状态提示。
- 不按易漂移的列表下标执行取消或投递。
- 不把给子代理的消息改投 main。
