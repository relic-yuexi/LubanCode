# GitHub Release 自动更新：用户技能保护与整包回滚

- 状态：P0 已落（2026-09-19，PR #132：manifest/所有权决策表/旧装迁移/扫描预演+脚本测试 167 项进 CI）；P1 已落（2026-09-19，一键整包更新：updater.py 事务引擎+lubancode update 子命令+固定启动器+平铺迁移+Gateway 固定入口+CI 集中发布门禁，脚本测试进 install-scripts 腿）；P2-P3 待排。本单记录方案，不代表更新器全部交付。
- 日期：2026-09-19。
- 起因：每次更新都要下载 ZIP、解压、运行 install.ps1；用户担心安装目录内自行添加或修改的技能遭到覆盖。
- 首批范围：Windows x64 安装版；元数据、清单、事务协议预留 Linux/macOS。便携版、包管理器安装、源码构建分别识别，不套用默认安装路径。
- 相关：`todos/Tauri与Android外壳打包与发布账.todo` 管桌面壳与 Android，本单管 LubanCode 主程序与随包资源，不混用两套更新器。

## 一、必须守住的底线

1. 用户文件不能丢。无论技能装在推荐目录，还是手动塞进安装目录，更新器都要查明、保留、告知去向。
2. 不按目录名判定所有权。名叫 skills 并不代表里面每个文件都归发行包。
3. 官方未修改文件可以换新；额外文件必须保留；官方文件经过本地修改，先留原件，再处理冲突，不能静默覆盖。
4. 程序、技能、网页、文档、rg 必须整套切换。下载失败、缺件、文件占用、断电，都不能留下半套安装。
5. 用户配置、会话、记忆、凭据、自建技能不属于发行包清理范围。程序回滚与数据回滚分开处理。
6. 自动下载不等于立刻重启。正在跑的 CLI、Gateway、任务不能遭到强杀。
7. 后台更新不得执行模型生成命令。更新源、目标目录、安装步骤由宿主固定代码掌管。

## 二、现状与证据

以下为 2026-09-19 调研快照，开发前重新核对。

- `src/config/update_checker.cpp`：已有 GitHub Latest 查询、语义版本比较；当前返回版本与发布页，不下载、不安装。
- `src/app/commands/settings_commands.cpp`：`/update [check]` 只查版本、打印提示。
- `docs/reference/commands.md`：已有 `--check-update`；`/skill install` 默认落到 `~/.lubancode/skills/<name>`。
- `scripts/install.ps1`：优先安装脚本旁边 EXE；旁边没有 EXE 才下载 Latest。重复运行旧解压目录内脚本，可能再次装入旧版。
- `Sync-OfficialDirectory` 先复制到 staging，再删除目标目录、移入新版；没有整套事务与回滚。EXE 先覆盖，随后逐个同步资源，途中失败可能新旧混装。
- 当前整目录同步范围：skills、docs、web、libexec、licenses；另覆盖 EXE 与 THIRD_PARTY_NOTICES.md。安装根内其他旁文件不主动删除。
- `/skill install` 走用户技能根，默认不写程序自带 skills。项目 `.lubancode/skills`、用户及项目 `.agents/skills` 也应纳入保护边界。
- `src/config/config.cpp`：LUBANCODE_HOME、LUBANCODE_DATA_HOME 可重定向配置与状态根，不能只保护默认用户目录。
- `.github/workflows/release.yml`：已有三平台归档、资源检查、rg 冒烟；各矩阵任务分别上传 Release，发布完整性需另加门禁。
- 调研时 Latest 为 v0.26.251，源码版本为 0.26.277。Latest 不等于比本地新；禁止仅凭“查到了 Latest”就安装。
- 调研时 Windows ZIP 资产带 `digest: sha256:...`；Release `immutable=false`。这些是当时状态，不写成长期常量。

官方资料：
- https://docs.github.com/en/rest/releases/releases
- https://docs.github.com/en/code-security/concepts/supply-chain-security/immutable-releases
- https://docs.github.com/en/repositories/releasing-projects-on-github/managing-releases-in-a-repository

## 三、入口与用户体验

以下命令均为拟新增，实施时对齐现有 CLI 解析与帮助页。

- [ ] `lubancode update --check`：查版本、通道、安装来源、可用资产；不改安装。
- [ ] `lubancode update --dry-run`：列出下载、替换、保留、备份、迁移、冲突项目；不改安装或用户数据。
- [ ] `lubancode update`：下载、校验、准备事务；遇到运行中进程，说明阻塞者，等待安全时机。
- [ ] `lubancode update --rollback`：切回上次可用整包；先检查数据兼容，不强行回退。
- [ ] 保留 `/update check` 和 `--check-update`；交互 `/update` 后续接同一更新服务，不另写一套流程。
- [ ] 输出当前版本、目标版本、下载进度、用户文件处理摘要、备份位置、何时生效及失败原因。
- [ ] 更新状态可供 CLI/Web 展示，但不得携带凭据、配置正文或会话正文。
- [ ] 默认稳定通道；预发布通道显式选择。版本倒退须走单独、明确的回退入口。
- [ ] 第一批交付手动一键更新；自动模式另行开关，默认先用“检查并提醒”。
- [ ] 自动模式分 off / notify / download / install-on-exit，具体配置名实施时定；开启后无需每次重复确认普通更新。
- [ ] 自改文件冲突、未知旧安装、数据不兼容时，自动模式暂停安装并提示，不自行猜测。

## 四、发行包与文件所有权清单

- [ ] Release 增加版本化更新元数据：schema、版本、通道、OS/架构、最低更新器版本、包名、大小、SHA-256、资源清单摘要、数据兼容范围。
- [ ] 包内 manifest 列出每个官方文件：相对路径、大小、SHA-256、用途；覆盖 EXE、skills、docs、web、libexec、许可证及其他正式随包文件。
- [ ] 安装记录保存来源仓库、Release/asset 标识、版本、包摘要、官方原始 manifest、安装方式与事务编号。
- [ ] 文件所有权以“上次可信官方清单”判定；不能更新后把用户自改内容重新记成官方基线。
- [ ] 旧清单有、当前哈希相同：官方未改文件，可更新；新版删除该文件时可随旧版本退役。
- [ ] 旧清单有、当前哈希不同：本地修改文件，备份原件，保留差异，进入冲突处理。
- [ ] 旧清单无、当前存在：用户/未知文件，禁止删除。新版恰好出现同路径，也算冲突。
- [ ] 官方文件本地缺失：视为本地差异，提示恢复；不悄悄撤销用户有意删除的定制。
- [ ] 清单路径拒绝绝对路径、盘符、UNC、..、Windows ADS、大小写碰撞与保留名；防止符号链接、junction、reparse point 把写入导向目录外。
- [ ] 哈希校验用于确认下载内容一致，不能冒充独立签名。签名/证明校验与信任根轮换另设明确实现项。
- [ ] asset digest 缺失时，必须取得可信更新元数据内摘要，否则停止自动安装；不静默降级为不校验。

## 五、技能保护与旧安装迁移

### 5.1 推荐存放与读取规则

- 官方资源留在版本目录；用户技能放用户根，项目技能放项目根，继续支持 .agents/skills 共享目录。
- [ ] 保持现有技能层级与重名规则，迁移不能无意改变同名技能实际生效来源。
- [ ] `/skills` 展示来源、实际路径、是否覆盖官方版本，方便用户找回技能。
- [ ] `LUBANCODE_HOME` 指定用户根时沿用既有配置语义，不迁回默认个人目录。

### 5.2 安装目录里额外放了技能

- [ ] 扫描整个技能目录树，不只看一级目录名；技能可能带脚本、模板、图片、引用文件。
- [ ] 先备份完整技能目录，记录原路径、相对结构与哈希，再迁入用户技能根；确认复制完整且加载成功前，不删除源目录。
- [ ] 目标不存在：迁入原技能名，保留相对引用和必要执行权限。
- [ ] 目标已有相同内容：核验后复用，账上记录两处来源。
- [ ] 目标已有不同内容：两份原件都保留，暂停冲突项，交用户选；不得覆盖用户目录现有技能，也不得自动改名后假定仍可加载。
- [ ] 内容混在官方技能目录里时，以完整技能为保护单位，不能只拷走额外文件后留下断裂引用。

### 5.3 用户改过官方技能

- [ ] 保存完整本地技能与官方基线差异；新版官方技能另存，不覆盖本地原件。
- [ ] 建议转成用户层覆盖，保留原技能名；迁移前检查已有用户/项目同名项及加载优先级。
- [ ] 用户可选保留定制覆盖、采用新版官方内容、手工合并。未选前不把冲突内容自动合并或执行。
- [ ] 持续提示“当前使用用户覆盖，官方新版未直接生效”，并提供两份文件路径。

### 5.4 旧安装没有 manifest

- [ ] 若能取得相同版本、相同平台的可信原包，拿它建立基线，再逐文件比较。
- [ ] 版本号相同不代表字节相同；本地重编译 EXE、热修网页、未知旁文件一律按本地差异处理。
- [ ] 找不到可信原包时，不把整棵 skills 当成官方：完整备份旧安装，列出未知内容，首次迁移需用户处理冲突后再切换。
- [ ] 所有权不明时，宁可暂缓首次自动升级，也不能直接套旧脚本整目录删除。
- [ ] docs、web、其他资源同样受清单保护。不能把未知程序资源随手灌入新版导致旧代码混用；保留原件、给出路径，并明确何者继续生效、何者只备份。
- [ ] 更新器自有清理只处理有安装记录的旧版本与临时文件。用户备份、未知旁文件、用户数据不得混入自动清理。

## 六、安装布局与进程协调

拟定安装布局：

```text
<install-root>/
  lubancode.exe                 固定启动器
  updater/                      受管更新助手
  versions/<version-build>/     不可变整包：真实 EXE 与全部官方资源
  current.json                  当前与上次可用版本指向
  install-state.json            安装来源、清单、事务索引
  staging/<transaction>/        未激活下载与解包
  backups/<transaction>/        旧安装与用户修改备份
  updates/<transaction>.json    事务日志
<user-root>/                    配置、会话、记忆、自建技能独立存放
```

- [ ] 真实程序从自己的版本目录定位随包资源，不能拿启动器目录误当资源根。
- [ ] 启动器保留参数、工作目录、环境、标准输入输出、退出码和 Ctrl+C 行为；覆盖重定向与交互终端。
- [ ] current 指针在同一文件系统原子替换，写前持久化事务与回退指针；采用平台支持的替换机制，不把多次移动称作原子操作。
- [ ] 更新器对安装根持独占锁；启动与激活协调，防止刚判定空闲又启动旧进程。
- [ ] Windows 助手独立存活，等待相关 CLI/Gateway 退出再切换；不替换正在运行的 EXE。
- [ ] 旧进程使用固定版本资源；仍有进程占用的版本不能清理。
- [ ] Gateway 注册路径、服务启动路径、install 记录、doctor 版本核对一并适配固定入口；常驻任务先排空，再正常 stop/start。
- [ ] 启动器和更新助手自身升级走分阶段交接；普通程序更新不能覆盖活跃助手。最低更新器不满足时给出明确升级路径。
- [ ] 自定义安装目录沿安装记录识别，不猜默认位置；多个安装实例分别加锁、分别记账。
- [ ] 源码构建、便携目录、系统包管理器安装先辨来源；不直接覆盖工作区，也不绕过包管理器。

## 七、下载、激活与恢复事务

状态流：checking -> downloading -> verified -> staged -> waiting-for-idle -> activating -> healthy -> committed。
异常分为 failed（旧版继续用）、needs-review（冲突待处理）、rolled-back（已恢复），状态与原因持久保存。

- [ ] 查固定官方仓库，按平台精确选资产；版本比较复用现有语义版本代码；默认不降级、不自动跳预发布。
- [ ] 固定本次 Release ID、asset ID 与摘要；下载期间发布更新不能把几个版本拼成一套。
- [ ] 设置网络超时、重试退避、下载大小与解压大小上限、磁盘空间预检；断点续传必须核对同一资产和摘要。
- [ ] ZIP 解压前检查路径与类型；解压后逐文件核对 manifest、必要资源、EXE 版本与平台。
- [ ] 下载和校验失败只清理本事务受管临时文件，保留旧安装及用户文件。
- [ ] 激活前完成技能保护与迁移预检；冲突未决就停在 needs-review。
- [ ] 切换前记旧版本、新版本、文件处理结果和恢复步骤；每一步可重入，重启后能继续或退回。
- [ ] 首轮健康检查在隔离数据根运行版本查询、资源定位等无副作用探针，不能拿真实用户数据试迁移，也不能启动渠道发消息。
- [ ] 健康检查通过再提交成功；失败恢复旧指针并保留诊断。后续常规运行报错不一概触发无限回滚。
- [ ] 至少保留一份已知可用整包；磁盘清理须跳过当前、上次可用、运行中版本和用户备份。
- [ ] 程序回退前检查配置/数据库 schema 兼容。若新版数据不可逆迁移，先备份并安排迁移事务；未补齐前禁止该版本自动安装。
- [ ] 不允许用旧数据备份直接覆盖升级后产生的新会话；恢复数据需独立决策并保护增量。

## 八、Release 流水线与后台检查

- [ ] 各平台构建上传到 CI 中转资产；集中发布任务等所有必需平台归档、校验摘要、manifest、冒烟通过后再发布。
- [ ] 先建 draft、上传完整资产与元数据，最后发布并标 Latest；避免用户查到已发布但 Windows 包尚未就绪的版本。
- [ ] 稳定与预发布通道分开；预发布不抢稳定 Latest。
- [ ] 评估启用 immutable releases；启用后遵守“草稿补齐资产再发布”，不能继续依赖发布后追加/替换资产。
- [ ] 发布端版本、tag、包内版本、更新元数据一致；上游必要测试失败不得发布可自动安装版本。
- [ ] 后台检查缓存结果，拟每 24 小时一次并加随机抖动；ETag/条件请求、限流退避、离线处理齐备，失败不挡程序启动。
- [ ] 多个 CLI 与 Gateway 共用检查缓存和更新锁；不要每个进程各下载一份。
- [ ] 后台只在版本变化、更新完成、失败或需要处理冲突时提示，不重复刷屏。
- [ ] GitHub 不通时可提示手动指定本地官方包，但仍走同一校验、保护和事务流程，不绕回危险整目录覆盖。

## 九、实施批次

### P0：先封住技能丢失风险

- [x] 在独立 worktree 开工，保留用户当前分支与已有改动。（PR #132 分支 feat/update-p0-skill-protection，主树零接触）
- [x] 补官方 manifest 与安装记录，明确用户数据排除边界。（scripts/generate_manifest.py 打包产出路径/大小/SHA-256/用途+--check 对账；release.yml 解包重算不合即红；install-state.json 安装记录）
- [x] 修 install.ps1/install.sh 的资源覆盖策略；新装、重复安装、旧版迁移复用同一所有权判断。（三实现共用一张决策表：官方未改换新先备份/本地改过保留+备份+冲突/未知一律保留/撞新版同路径算冲突/本地删掉不复活/官方退役件备份后退役）
- [x] 未知文件保留、自改文件备份、冲突可见；来源与目标相同也不能删目录再搬自己。（skip-current 用例+四类冲突用例钉死）
- [x] 给旧安装提供扫描/预演报告和完整备份；先解决“会不会丢”，再接静默安装。（-Scan 只读预演/-BackupOnly 完整备份；无基线旧装先全备份退 needs-review，-Baseline/-AllowUnknownReplace 显式确认）
  证据：PR #132、CI run 35385460013（changes/lane/install-scripts×2/macos-clang 全绿），install.tests.ps1 109 项、install.tests.sh 58 项。未验边界：真机断电/文件占用/进程协调归 P1；manifest 的真包实证待下个 v* tag。

### P1：一键整包更新

- [x] 元数据解析、下载校验、隔离解包、版本目录、启动器、助手、锁与事务日志。（scripts/updater.py：§六布局 versions/<ver>-<sha8>/+current.json+install-state.json(schema2)+staging/backups/updates/；§七状态流 checking→…→committed/failed/needs-review/rolled-back 持久在 updates/<txn>.json,断点续跑同事务不重下；下载钉 Release ID/asset ID/摘要+超时退避+大小上限+磁盘预检+Range 续传核对同资产；解包过 P0 路径契约筛成员、逐文件对 manifest、EXE 探针验版本平台；激活前技能保护预检复用 install_plan.py 决策表,冲突停 needs-review 退 2;健康检查隔离数据根跑 --version;清理跳过当前/上次可用/运行中,删前抢救清单外文件。src/app/launcher.cpp 自举式固定启动器,src/platform/process 新增附着式 RunAttachedProcess）
- [x] CLI update/check/dry-run/rollback 接口与交互入口。（src/cli/update_command.cpp + cli_options/cli_app 接线:lubancode update [--check|--dry-run|--rollback][--prerelease][--from <包>][--json];--check/--check-update 与 /update check 同一检查服务,版本比较复用 config::update_checker.cpp;本地不低于远端不降级;预发布显式;资产缺 digest 时从 Release 的 update-meta.json 取可信摘要,仍缺则拒装;退出码 0/1/2(needs-review)/3(rolled-back)）
- [x] 旧平铺目录迁移、用户技能保护、Gateway 固定入口和恢复流程。（平铺首更自动迁移:受管树完整备份进 backups/<txn>/flat,旧根 EXE 改名挪 backups/<txn>/legacy(Windows 对运行中映像允许改名,不强杀不替换映像),新版 EXE 落根位当启动器,updater/ 树同步到根;gateway install 在版本化布局改钉安装根启动器(service.cpp 记 entry_point=launcher),doctor 对 launcher 记录版本差异降为 Info(随 current 指针走);rollback 命令先探针再换指针,平铺交接可从备份恢复旧根 EXE）
- [x] CI 集中发布门禁、跨版本升级与回滚证据。（release.yml:矩阵腿只传 CI 中转工件,publish 腿等三平台归档齐→逐包重核 manifest+updater 树→compose_update_meta.py 汇总资产摘要账→gh 建 draft→挂资产→发布并标 Latest;updater/ 助手树打进全平台包。测试:scripts/tests/updater_tests.py 十段 74 项进 ci.yml install-scripts 腿(windows+ubuntu 双绿),覆盖状态机/指针原子性/坏包/摘要不符/路径穿越/符号链接/清单外文件/磁盘预检各失败路保旧版/needs-review 续跑/回滚/清理/断点续传/目标已换作废;C++ 册 test_launcher/test_cli_options/test_update_checker 随 macos-clang+windows-msvc 双平台绿。证据:PR #138,ci run 35417677505(rollup success;windows-msvc 首场挂的 integration.telemetry.telemetry_otlp_export 为既有负载时序偶发,与本单改动无关的域,重跑同 run attempt 2 即绿)。未验边界如实交代:真机 Windows 进程占用、断电恢复、release.yml 重排待下个 v* tag 实证、EXE 探针用例仅 POSIX 段(假 exe 是 shell 脚本)、Windows 无 python3 时一键更新退回包内安装脚本路径(P3 再做原生助手)。）

### P2：后台自动更新

- [ ] 检查缓存、通道配置、后台下载、退出后安装与状态提示。
- [ ] 长驻 Gateway 排空策略、异常恢复和数据兼容门禁。
- [ ] 首先小范围启用；失败率与用户文件保护证据齐备后，再决定是否更改默认模式。

### P3：跨平台与分发加强

- [ ] Linux/macOS 使用各自文件替换、权限与进程规则，保持同一事务合同。
- [ ] 完善独立签名/证明校验、信任根轮换与更新助手自升级。
- [ ] 包管理器安装转交对应管理器；桌面壳更新与核心协议版本协商另接相关 TODO。

## 十、验收门槛

本地只做静态检查；构建与测试提交 GitHub CI 跑。真机安装、进程占用、断电恢复另记实测，不拿 CI 绿灯冒充。

- [ ] 默认 `/skill install` 安装的用户技能，更新后逐文件哈希不变且仍可加载。
- [ ] 安装目录额外技能含脚本/模板/图片，更新后原件完整、相对引用可用，迁移去向可查。
- [ ] 官方技能本地修改、官方目录内额外文件、用户根同名技能、新版新增同路径分别覆盖；没有一项静默丢失。
- [ ] 用户/项目 .agents 与 .lubancode 技能、配置、会话、凭据逐项不被安装过程改写。
- [ ] 自定义 HOME/DATA_HOME 与安装根重叠时拒绝危险覆盖；目录链接逃逸、中文路径、空格路径、大小写碰撞都验。
- [ ] 无 manifest、可信旧包缺失、同版本本地构建、同目录安装均不误删。
- [ ] Windows EXE、rg 或网页资源占用时，不强杀、不半更新，等待原因可见。
- [ ] 下载中断、摘要不符、坏包、路径穿越、资源缺失、磁盘不足、无写权限均保持旧版可用。
- [ ] 在下载、备份、迁移、切换、健康检查、提交各阶段注入中断；重启后可恢复，备份与用户增量不丢。
- [ ] 两个更新器同时运行、启动与切换并发、多个安装实例并存，安装账不串。
- [ ] 新版健康失败可退回旧整包；数据 schema 不兼容时明确阻止不安全回退。
- [ ] 本地版本高于 Latest 不降级；预发布不误装；Release 资产未齐、限流、离线不挡启动。
- [ ] Gateway 更新前后注册路径与 doctor 对账通过；在途任务不遭强杀，恢复不重复发送渠道消息。
- [ ] 至少实测一轮旧平铺版 -> 新布局 -> 下一版 -> 回滚，记录包摘要、退出码、路径、技能哈希与截图/日志。
- [ ] 文档注明哪些入口已上线、哪些仍待办；不把上述拟新增命令写成当前已有功能。

## 十一、完成定义

用户运行一条命令，或开启自动模式后退出再启动，即能用上新版。
自行安装或修改的技能有原件、有去向、能继续使用；冲突说清楚，不悄悄替用户挑选。
下载坏了、文件占着、安装中断或新版起不来，旧版仍有退路。
以上必须有 CI 与实际安装证据，缺哪项就留哪项未勾，不以“代码写完”销账。
