# Workspace 收官验收：跨平台、攻击、量级与 E2E

- 状态：已完工（2026-09-04，Windows 主活全落 + Linux/WSL 腿实测，macOS 腿留 CI——见 §二）
- 级别：P1（验收与基线批，无新开发）
- 类型：LubanCode 验收（workspace/统一存储）
- 原单批注：状态行旧记"Wave3×P0-2 合并态 16 册交叠红待缝"——已被后续消化（main 现 380 册全绿为证），不另处理。
- 不在本单：新功能开发；分级 Memory 后续演进（原单未尽设想起另立）
- 验收批注（2026-09-04）：全部证据出自 worktree 分支 `worktree-agent-af9704b2a97bb6f15`（基础 = 本地 main 7028974f 合并后）；测试/驱动/探针/文档/基线表均已进仓，逐项 commit。

## 一、Windows 侧（本批主活，工头做）

- [x] 主会话 + 四只并行子代理 + 嵌套 child + workflow + Memory recall/save 的综合恢复测试（杀进程/断流后 resume/verify 全过；可仿 tests/manual 探针手法）。
  - 册：`tests/integration/workspace/test_comprehensive_recovery.cpp`（ctest `integration.workspace.test_comprehensive_recovery`）。
  - 现场：main 两轮（一轮完整带工具往返，一轮半路断流）+ 4 只并行子代理（成/败/半路/派嵌套孩子）+ workflow 编排账与两节点（一收口一半路）+ memory save→worker 落库→recall（context.injected 进 main.jsonl，快照 inline/ref 双形）。
  - 杀进程模拟：句柄全丢（不 Close）+ 死 PID（4194303）陈旧锁 + 二号子账尾行撕裂（§16.3 形状：末行缺换行）。
  - 验收线全过：RecoverWorkspace 按事实收口（running→incomplete、session.json 补正、旧 main 一字节不追加、死锁不算活人）；VerifySessionDir 撕裂流报 `verify.truncated_tail`、其余流全过；resume-as-new 接上（start_reason=resume、反指源场、悬空分档）；workspace 记忆换场不丢（新场召回命中）；恢复后新场续写再 verify 全绿；干净封口对照场 recover 空转不添笔。
  - 附带发现（记档不改码）：真"半行撕裂"（mid-JSON 断）按 verify.bad_json 判 corrupt、resume 拒收 fail-closed——比尾行缺换行更硬的崩溃形状，现行合同如实拒绝，非遗漏。
- [x] linked worktree 共 Memory/共 workspace、session 各带 checkout 现场的测试。
  - 册：`tests/integration/workspace/test_worktree_shared_storage.cpp`（`integration.workspace.test_worktree_shared_storage`）。
  - 证据：主树+linked worktree（.git 文件 + commondir，逐字照 git 落盘形状）同 key 同 workspace.json；checkouts[] 双登记（规范化 root 逐字）；两场 session 落同一间 workspace、launch_cwd 各带各的检出根；sessions 索引同 workspace 列两场；主树存 fact.deploy、worktree 侧 BuildTurnContext 召回命中——记忆跟 common git dir 走。
- [x] 跨 workspace /worktree 切换、封旧开新与回执测试。
  - 同册第二场：SessionRuntime::NoteWorkingDirectoryChanged 跨仓切换——旧场 closed（session.ended reason=workspace_switch）留旧房；新 workspace 开新场；两间 lifecycle 各记 create_session intent+result（新场回执指认自身 session_id）；新旧 workspace 索引各列各场；两本账 verify 全过、旧账一字不搬。
- [x] 全局 Memory 越权攻击：项目配置、恶意 marker、Hook、Skill、subagent、模型工具均不得写（P0-4 写通道闸的攻击面复验）。
  - 册：`tests/integration/workspace/test_global_memory_attack_surface.cpp`（`integration.workspace.test_global_memory_attack_surface`）。
  - 六路全拒：①memory_save 各式 user 拼写（kind=user/带 traversal value/level 同塞）→ `memory.global_unauthorized` + 指路用户命令，零 job；走私形（kind=project 偷塞 level=user）放行但只落项目层——level 键是惰性字节，不构成通道。②抽取路/候选 accept（不带 user_initiated）全拒。③四只子代理并发借同一引擎写 user 层，32/32 全拒。④Skill 毒指示词唯一通道仍是模型工具，同一道闸拦。⑤Lua 插件摸盘直写全局记忆目录：pure 画像 io 关门，执行报错、零字节落地（Hook 是用户自配外部进程，进程权限等同用户，直接写文件不在应用闸管辖——记入测试注释）。⑥项目 config 写 memory.enabled/user_enabled=true：MergeConfig 后仍全关（enabled 只能全局开，user_enabled 项目级只能收窄成关）；全局开+项目收窄的负路径同测。
  - 附：恶意 marker——空串/超长/坏 JSON 折成"没有这级 marker"（回退 config/cwd）；traversal/分隔符 id 被 SafeName 折字节，workspace_key 恒单段安全名（无 '/'、'\'、'..'），目录出不了圈；对照正路：用户命令+全局授权进 memory/user/。
- [x] 1/1k/10k session 与 1/1k/10k Memory 条目的 list/recall/replay 基线：冷/热耗时与峰值内存记表（`docs/` 或 eval/ 下），不拍脑袋填预算。
  - 驱动：`tests/manual/workspace_scale_driver.cpp`（构建目标 workspace_scale_driver，真 ledger/索引/召回/折叠跑三档，冷热分账，峰值内存 Windows PeakWorkingSet / Linux VmHWM）；渲染脚本 `scripts/workspace_scale_baseline.py`；复现命令在基线文档内。
  - 基线表进仓：`docs/development/workspace-storage-v2/scale-baseline.md`（Windows 主表 + WSL 对照表 + 造数口径 + 读表要点；已入 docs/catalog.txt）。
  - Windows 实测：list 10k 冷 10.7 s / 热 0.66 s；recall 10k ~0.53 s；replay 68 事件 fold 5.0 ms / verify 4.2 ms；峰值 128 MiB。夹具全部落在临时根，不碰真主目录。
- [x] 更新配置、会话、Memory、安全、命令、故障排查、目录一览与发布迁移文档（以当前代码为准，别照抄原单设想）。
  - configuration.md §九目录一览重写（workspaces 树 + memory/user + memory-jobs；旧 sessions/projects 行退场）+ §十表补 `/memory remember global` 行（全局层唯一写入口）；§九尾注明"升级不迁移旧档、旧目录原样保留"。
  - features/sessions/README.md：存储形状（每场一目录、main.jsonl）、workspace 归属（四级裁决、worktree 同 key 同 workspace）、resume-as-new 语义（开新场、源账永不重开追加）、归档=状态转态不搬目录、删除走墓碑+lifecycle 回执、清理对照表更新。
  - reference/commands.md：archive/unarchive/delete 三条与 /sessions、/resume、/export 的行为改准。
  - architecture/memory/design.md §5 身份改统一裁决器四级 + key 算法（SafeName+SHA256 前 16）；§6 布局去重、trace schema 3→4、旧 sessions/ 行删除；context.md 源码锚点表 session_store→trajectory/replay。
  - development/security.md §11：补全局记忆写闸（唯一写入口、六路拒绝、项目配置无权提权）与召回快照账。
  - getting-started/troubleshooting.md：/resume 找不到（workspace 口径）与记忆换目录两行改准。
  - development/build-and-release.md：源码地图补 trajectory/workspace 行、src/sessions 行改 goal 纯函数；新增 §11 发布与存储升级（v2 语义、无迁移器、空主目录首启）。
  - scripts/check_docs.sh 过：62 页，717 条本地链接。
- [x] 空主目录 E2E、带旧数据升级 E2E（旧数据用夹具造，别碰真主目录）。
  - 探针：`tests/manual/workspace_e2e_probe.py`（真二进制 + 临时 USERPROFILE，两幕 12 项全 PASS，退出码 0）。
  - 空主目录：`trajectory usage/doctor` 干净报错（rc=1，无崩溃无栈）、verify 如实找不到、主目录不冒出任何旧式目录。
  - 旧数据升级（tests/fixtures/workspace 夹具铺旧家：平铺会话/旧项目记忆/旧全局记忆/旧 job 队列）：旧场 verify/replay 全找不到（rc=1/2）、doctor 稳定退出、旧 sessions/ 与 projects/ 前后文件清单+sha256 逐字对账一字不动。
  - in-process 册：`tests/integration/workspace/test_legacy_home_upgrade.cpp`——旧根零读零写（文件数+字节对账）、workspaces 新开不含旧 key、旧 user 层照读、v1 session.json 搬错家 → 索引 misplaced_v1+damaged 点名不冒充可恢复。

## 二、跨平台腿（能做的做，做不了的如实标）

- [x] Windows/Linux：workspace identity、路径大小写、symlink/junction、锁与原子迁移测试（Linux 可 WSL/容器跑则跑，跑不了标注留 CI）。
  - 册：`tests/integration/workspace/test_identity_cross_platform.cpp`（同一份册两平台跑）。
  - Windows 实测（本机 VS Release ctest）：大小写折叠同 key；junction（mklink /J 造真 junction）进出同 key；symlink 造不出时报 SKIP 不冒充；锁活人拒/死锁清；workspace.json 并发原子写（8 写手×40 轮 + 轮读）读侧有界重试后恒见整份、末态合法。
  - Linux/WSL2 Ubuntu 实测（g++ 15 + Ninja，独立构建树）：大小写敏感两把 key（POSIX 分支）；symlink 真跑通（规范路径同 key）；锁与原子写同绿；五册 integration/workspace + 12 册相邻 unit（workspace/memory/trajectory）全绿（doctest Status: SUCCESS 逐册）。
  - 附带发现（记档不改码，见 scale-baseline.md 配套节）：Windows ifstream 默认不带 FILE_SHARE_DELETE，读手正握句柄时 MoveFileExW 换名可能回 atomic.replace_failed——生写口如实报错由调用方处置；读侧绝不半截这条守住了。
- [ ] macOS：由 CI 跑 identity/权限/锁/崩溃恢复/release 包；无环境处如实标 CI。
  - 留白：本机无 macOS 环境。identity（POSIX 分支大小写敏感）、锁（进程 token 探测）、原子写（POSIX rename）、崩溃恢复册全部平台无关可在 CI 直接跑；release 包三平台重编本就走 GitHub workflow（docs/development/build-and-release.md §10）。标 CI，不在本批伪造证据。

## 三、验收

- 全量 ctest 零回归；上述各测试的证据（命令/退出码/关键输出）记回本单。
  - Windows 五册：`ctest -C Release -R "integration.workspace"` → 5/5 过（identity_cross_platform 内含换名微窗与写侧被拒次数的 MESSAGE 留证）。
  - 全量：`ctest -C Release`（worktree build/release，387 册——合并 main 9670c4d3 后口径，含本批新增 5 册与 main 侧 token 校准/记忆相关性新册）——`100% tests passed, 0 tests failed out of 387`（2026-09-04 实测，无环境性红）。
  - Linux 腿：五册 workspace 集成册 + 12 册相邻册全绿（构建/运行命令见上）。
- 基线表与文档更新进仓。
  - docs/development/workspace-storage-v2/scale-baseline.md（入 catalog）；8 份既有文档更新；scripts/check_docs.sh 过。

### 留白与移交

- 恢复器（SessionManager::RecoverWorkspace）库与测试册齐备，生产入口未接线（启动不自动恢复，trajectory doctor 只读聚合）——崩溃场的 session.json 状态由索引/doctor 如实展示，"自动收口"留后续批接 CLI 或启动路。
- mid-JSON 撕裂流按 verify.bad_json 整场 corrupt、resume 拒收——fail-closed 是现行为；若要"截到半行也按前缀恢复"，属合同变更，另立单。
- Windows 换名×并发读句柄的 atomic.replace_failed 微窗（见上）——调用方已有稳定码处置；若要消除，需读侧改 FILE_SHARE_DELETE 开口（src/platform 改动面），另立小单。
- macOS 腿标 CI（无环境，不伪造）。
