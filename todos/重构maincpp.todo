# 拆开 `src/main.cpp`

状态：已完成

## 最终实账(2026-08-15 收尾)

`src/main.cpp` 39 行:只剩 wmain 的 UTF-16→UTF-8 与 `lubancode::app::RunCli(args)`。
`src/app/` 已立真实编译边界——每只公开头只放声明,实现全在各 `.cpp`,
CMake 在 lubancode 可执行文件与测试目标两侧明列;测试侧有两只 TU 同
include 全部公共头的边界钉子(test_app_boundary_a/b)专查 ODR 与漏符号。

```text
src/main.cpp                          39 行
src/app/interactive_session.hpp       47 行(只露 Options+RunInteractiveSession)
src/app/interactive_session.cpp     1545 行(InteractiveSession 真对象,详见下)
src/app/cli_app.hpp/.cpp          36 / 502 行(RunCli+向导)
src/app/cli_options.hpp/.cpp      40 /  59 行(ParseCliArgs 纯函数)
src/app/one_shot.hpp/.cpp         31 / 243 行(AskOnce,不 include 交互头)
src/app/backend_stack.hpp/.cpp   180 / 148 行
src/app/tool_runtime.hpp/.cpp    192 / 250 行
src/app/turn_runner.hpp/.cpp     149 / 783 行
src/app/commands/settings_commands.*  163 / 1103 行
src/app/commands/session_commands.*   135 /  507 行
src/app/commands/prompt_commands.*     62 / 266 行
src/app/commands/workspace_commands.*  85 /  288 行
src/app/commands/peer_commands.*       37 / 140 行
src/app/commands/command_flow.hpp      12 行
```

收尾遗留(2026-08-15 app 编译边界一单交接):interactive_session.cpp
1545 行、settings_commands.cpp 1103 行,均超出"700-1000/不过千"目标,
还没按"向导/供应商切换/普通设置"与"memory 命令/转录 UI 聚焦"再分;
PathToUtf8/SameFilesystemPath 仍住在 workspace_commands,没归
platform/tools 路径层。行为、文案、CLI/slash/JSONL/配置格式零改动。

## 眼下是什么样(拆分前的旧账,留档)

`src/main.cpp` 已有 6476 行，约 340 KB。

它不只是入口。后端、工具、终端、命令、会话，全挤在这一个匿名命名空间里。添一桩小功能，往往要从文件头翻到文件尾；改一处状态，还要惦记十几个局部引用活多久。

粗看可分成九块：

| 行号（约） | 体量 | 眼下所管的事 |
| --- | ---: | --- |
| 1-317 | 317 行 | 版本、帮助、后端创建、五层请求包装 |
| 318-1020 | 703 行 | 配置诊断、工具表、MCP、插件、LSP、确认详情 |
| 1021-1953 | 933 行 | ask_user、流式 transcript、agent 状态、正文锚点 |
| 1954-2734 | 781 行 | diff 预览、ToolDisplay、终端原位改写 |
| 2735-3253 | 519 行 | callbacks、权限确认、一轮 RunTurn |
| 3254-4749 | 1496 行 | 向导、model/provider/language、skill、prompt、session 等命令 |
| 4750-5924 | 1175 行 | InteractiveLoop 与整场可变状态 |
| 5925-6096 | 172 行 | AskOnce 单发模式 |
| 6097-6476 | 380 行 | hooks、参数解析、RunCli、main/wmain |

症结不只在行数。

- `InteractiveLoop()` 同时造资源、接 UI、管状态、派命令、发请求、写存档。
- Backend 一层套一层。model、think、魂、模型说明、延迟工具，各靠一只包装器临发前改 Request。
- MCP client、PluginHost、LSP Manager、ToolRegistry 靠“先声明的后析构”保住引用。规矩只写在长注释里，编译器替我们守不住。
- `BuildCallbacks()`、`ToolDisplay` 和几只 painter 紧咬终端锁、footer、transcript、agent 状态。谁该画，谁该跑，边界不清。
- slash 解析已经在 `cli/slash_commands.*`，执行却仍堆在 `main.cpp`。
- InteractiveLoop 与 AskOnce 各造一遍 backend、MCP、LSP、registry、agent tool、tool_search。两边已经有些漂移。
- 大量函数直接读 cwd、写 `std::cout`、问 `ReadLine`。单测很难从入口处下针。

## 目标

最后让 `src/main.cpp` 只剩平台入口：

```cpp
int main(int argc, char** argv) {
    return lubancode::app::RunCli(...);
}
```

Windows 的 `wmain` 仍在这里做 UTF-16 -> UTF-8。其余都交给 `app/`。

验收数目先定下：

- `src/main.cpp` 压到 150 行以内。
- 不造第二只两三千行的大文件。
- 新的编排文件尽量守在 300-900 行；超过 1200 行便再看职责是否混了。
- 行为、文案、快捷键、存档格式、工具 schema 都不变。
- Windows 与 POSIX 入口仍共走同一个 `RunCli`。

行数只是门槛。真正要得的是依赖单向、资源寿命明白、命令各有落脚处。

## 新边界

加一层 `src/app/`。它只做应用编排，不往 `agent/`、`cli/`、`tools/` 里倒灌依赖。

依赖只准这样走：

```text
main
  -> app
       -> agent / api / cli / config / memory / mcp / lsp / tools / platform
```

底下各层不得 include `app/`。

拟拆成这些件：

### `app/backend_stack.*`

先原样收走：

- `BuildBackend`
- `RebuildableBackend`
- `ModelOverrideBackend`
- `ThinkOverrideBackend`
- `ModelInstructionsBackend`
- `SoulOverlayBackend`
- `DeferredIndexBackend`
- `SpinnerBackend`

第一遍只搬，不改包装次序。

待回归站稳，再考虑把前五层合成一只 `SessionRequestBackend`。它拿一份 `SessionRequestState`，按明写的顺序改 model、think、instructions、soul、deferred index。Spinner 仍单放外层，只管请求发出到首字节这段时辰。

不要在搬家 commit 里顺手合并。提示词段落次序一错，测试未必立刻喊。

### `app/tool_runtime.*`

收走这些活：

- `BuildBaseToolRegistry`、`BuildExploreToolRegistry`
- MCP 启动、注册与状态
- PluginHost 挂载与警告
- LSP Manager 与 LspTool 注册
- agent tool、todo、ask_user、memory、tool_search 的装配
- detached agent 的 backend/registry factory

做一只不可复制、不可移动的 `ToolRuntime`。它亲自持有 MCP clients、PluginHost、LSP Manager 和各张 registry。

成员声明顺序要让拥有者先建、后死；引用方后建、先死。若仍有地址不稳的对象，放进 `unique_ptr`，别再靠函数里几十个局部变量的先后次序维持。

`ToolRuntime` 对外只露窄口：主 registry、子代理 registry、Explore registry、agent tool 指针、挂载状态。别把内部容器整包掀出来。

Interactive 与 AskOnce 共用这一套装配。两种模式只把差异作为 options 传进去：

- 能不能挂 `ask_user`
- 能不能挂会话级 memory_save
- 是否启用终端 UI
- 是否允许消息排队

### `cli/live_transcript.*`

收走流式画面：

- `TranscriptPainter`
- `AgentStatusPainter`
- `StreamBodyTracker`
- markdown 收束与重画所需的锚点

它只管“画在哪里、怎样擦、怎样重画”。不调用模型，不碰 session store。

### `cli/tool_display.*`

收走工具展示：

- `FileDiffPreview`
- diff 预览拼装
- `ToolDisplay`
- 确认前的参数摘要
- 工具条目开始、结束、拒绝、打断的画法

纯格式仍留在现有 `cli/transcript.*`、`cli/diff.*`。新文件只管实时展示和状态接线，别复制已有格式函数。

### `app/turn_runner.*`

收走一问一答：

- `UsageStats`
- `BuildCallbacks`
- `RunTurnResult`
- `RunTurn`
- `PromptAskUser`

给它一份明白的 `TurnContext`，内含 registry、权限、theme、context tracker、transcript、todo state、hooks 和 UI 句柄。

眼下 `RunTurn` 的长参数表已经在提醒我们：这些值本来就是一轮上下文。收成结构体后仍由调用方持有资源，`TurnRunner` 不偷延长引用寿命。

### `app/commands/`

别给每条 slash 命令各造一对文件。先按领域收成四组：

```text
commands/settings_commands.*
commands/prompt_commands.*
commands/session_commands.*
commands/workspace_commands.*
```

分法如下：

- settings：config、model、think、provider、language、update、skill。
- prompt：soul、prompt、project instructions。
- session：sessions、resume、export、title、context、compact、clear。
- workspace：worktree、tools、plugins、mcp、lsp、todos、memory、image。

另设一只 `command_dispatcher.*`，接 `ParsedSlashCommand`，调用对应 handler。

handler 吃 `CommandContext&`。Context 只列命令真正能碰的状态，别把整个 `InteractiveSession&` 塞进去。否则只是把全局变量换了个名字。

第一遍允许 handler 继续用 `std::cout` 和 `ReadLine`。搬稳后，再给需要单测的命令添一份小 `CommandIO`：print、read_line、choose、confirm。不要为此重写整套终端。

### `app/interactive_session.*`

把 `InteractiveLoop` 变成不可移动的 `InteractiveSession`。

它持有整场状态：

- 当前 config、model、think、soul、provider
- BackendStack 与 ToolRuntime
- AgentLoop 与 ContextTracker
- transcript、todo、pending queue
- SessionStore、title、persisted count
- UI 开关和会话级权限

构造函数只建资源。`Run()` 只走主循环。

主循环每圈只干五件事：

1. 刷新状态栏与后台通知。
2. 从 pending queue 或 composer 取一句。
3. 若是 slash，交 dispatcher。
4. 若是普通输入，做自动 compact，再交 TurnRunner。
5. 收队列、写存档，回到下一圈。

现有 `process_line`、`ensure_session_started`、`persist_new_messages` 等 lambda，逐个升成私有方法。捕获十几个局部引用的 lambda 不再往外传。

### `app/one_shot.*`

收走 `AskOnce`。

它复用 BackendStack、ToolRuntime、TurnRunner，另配一份 `OneShotOptions`。不做交互会话没有的事：不挂 ask_user，不排下一条，不写 session archive。

### `app/cli_app.*`

收走：

- `PrintVersion`、`PrintHelp`
- CLI 参数解析
- 配置加载与初次向导
- theme、model catalog、persona、soul 的启动准备
- SessionHookScope
- `RunCli`
- 选择 InteractiveSession 或 RunOneShot

把参数解析先拆成纯函数 `ParseCliArgs()`。输入 `vector<string>`，返回 `CliOptions` 或错误。这样 `--help`、`--version`、`--continue`、`--image`、位置参数冲突都能直接单测。

版本号挪进一份小 `app/version.hpp`，别让别处反过来 include `main.cpp`。

## 怎么拆

这事不可一刀劈完。每一步都要能编、能测、能单独回退。

### 第零步：先钉住现状

先跑并记下：

- Release 编译。
- `ctest` 全套。
- `lubancode --version`、`--help`、`--config`。
- 单发一句、不带参数进交互、`--continue`。
- Windows screen drivers：composer、stream footer、markdown、fold/clear。

补几颗便宜的性状测试：

- `ParseCliArgs` 的参数组合。
- 后端请求叠加顺序：model、think、model instructions、soul。
- Interactive 与 OneShot 的工具清单差异。
- slash dispatcher 把每个枚举送到正确领域。

没有这些钉子，后头搬得再整齐，也不知道漏了什么。

### 第一步：搬终端展示

先搬 `TranscriptPainter`、`AgentStatusPainter`、`StreamBodyTracker`、`ToolDisplay` 和 diff preview。

理由：它们虽长，边界已经由现成 `cli/transcript`、`cli/agent_status`、`cli/diff` 托住。大多可原样挪，少碰会话状态。

这一阶段只改 include、namespace、CMake。不要顺手改文案或刷新算法。

### 第二步：搬 BackendStack

把后端创建和包装器原样挪进 `app/backend_stack`。

加请求级测试，钉住系统提示次序。确认无差后，另起 commit 合并多层 request overlay。合不拢也不妨，先把职责从入口拿走。

### 第三步：收 ToolRuntime

把 MCP、插件、LSP、registry、agent tool、tool_search 装配收进去。

这一步最容易踩寿命问题。`ToolRuntime` 先做不可移动对象，构造完后不准塞进会搬家的容器。测试要真起 MCP fixture、真载 DLL fixture，并让 runtime 正常析构。

### 第四步：搬 TurnRunner

把 callback 与一轮执行收走。此时它拿到的 backend、registry、live UI 都已有稳定接口，长参数才能真正缩下来。

先保持回调先后次序：

- text delta
- tool start / confirm / done
- markdown flush
- agent status
- footer begin / erase / end
- usage 与 context 记账

这些次序不可凭“看着差不多”重排。

### 第五步：搬命令

按四组逐批搬。每搬一组，原 switch 仍可调用新 handler。不要等四组齐了才编。

建议次序：

1. session_commands：底层逻辑已有 `agent/session_store`，最容易验。
2. prompt_commands：底层已有 `agent/prompts`、`config/prompt_files`。
3. settings_commands：model/provider 会改活 backend，风险较高。
4. workspace_commands：牵工具 runtime 与终端确认，最后搬。

命令都出去后，再把 switch 收进 dispatcher。

### 第六步：收口命令派发，再收 InteractiveSession

前五步落地后，`main.cpp` 已从 6476 行降到约 2200 行。眼下最大的整块是 `InteractiveLoop()`；其中 `process_line` 里的 slash `switch` 仍有约四百行、三十多个 `case`。

别先把它换成 `unordered_map<Command, std::function<...>>`。命令全是编译期枚举，显式 `switch` 能让编译器查漏项，读起来也直。真正该治的是肥 `case` 与散落状态。

#### 第六步甲：把肥 `case` 收成 handler

已经只有一两行转发的 `Help`、`Model`、`Think`、`Skills`、`Mcp`、`Lsp`、`Tools` 等分支，原样留着。

先搬仍在 `process_line` 里亲手改状态的分支：

1. session：`/clear`、`/sessions`、`/resume`、`/export`、`/title`、`/compact`。
2. workspace/runtime：`/worktree`、`/background`、`/memory`、`/record`。
3. peer：`/peers`、`/send`、`/peerperm`。
4. settings 收尾：缩掉 `/provider` handler 那串 backend、model、catalog、prompt 参数。

不要造一只万能 `CommandContext`。按所有权给窄状态：

```text
SessionCommandState   -> loop / store / meta / title / persisted_count
WorkspaceCommandState -> worktree / tool runtime / recorder / background tasks
PeerCommandState      -> peer runtime / ready queue / permission tier
SettingsCommandState  -> config / backend stack / model state / catalog
```

这些结构只借引用，不持有资源，也不往 `agent/`、`cli/` 倒灌 `app/` 类型。handler 要问人时再拿一份小 `CommandIO`；不必为了几条打印命令重写全套终端。

`WorktreeAction`、`BackgroundTaskStatus`、`PeerDelivery`、`PeerPermissionTier` 那几只短 `switch` 留着。它们只做稳定枚举映射，拆了反倒绕。

#### 第六步乙：让 dispatcher 只管路由

在 `InteractiveSession` 里设 `DispatchSlashCommand()`。顶层 `switch` 暂留，每个 `case` 只许解析后转发，不能再亲手清 history、换 cwd、写 session 或停 peer。

派发结果别再拿裸 `bool` 猜意思：

```cpp
enum class CommandFlow {
    Continue,
    Exit,
};
```

普通文本不进 dispatcher。外来 peer 消息也仍按正文走，不能当 slash 执行。

验收：给每个 `SlashCommand` 做一颗路由测试；再单测 `/clear`、`/provider`、`/resume`、`/send` 的状态变化。这里测的是派给谁、哪些状态留住，不测终端颜色。

#### 第六步丙：落下 InteractiveSession

新增：

```text
app/interactive_session.hpp
app/interactive_session.cpp
```

头文件只露 `InteractiveSessionOptions` 与 `RunInteractiveSession()`。具体类藏在 `.cpp`，免得 backend、MCP、LSP、终端大头全漏进调用方。若构造次序实在拧，可用私有 `Impl`；不要把一千多行实现塞进头文件。

第一只 commit 只把 `InteractiveLoop()` 原样搬过去。第二只 commit 再把局部状态收成成员，把 lambda 一只只升成私有方法：

```text
RebuildLoop
EnsureSessionStarted
PersistNewMessages
DispatchSlashCommand
RunUserTurn
DrainPeerInbox
DrainBackgroundResults
RefreshStatus
```

一次只收一簇：

1. backend、model、think、prompt 与 loop。
2. ToolRuntime、transcript、todo 与 UI 回调。
3. session store、title、persisted count 与 project memory。
4. pending queue、peer runtime 与后台结果。
5. recorder 与退出清场。

成员声明顺序要按反向析构验一遍。UI 回调注册、`loop->SetInbox()`、peer `Start()` 这类“开门后要关门”的事，交给 RAII guard 或析构函数收尾。别再靠 `InteractiveLoop()` 尾巴上几行手工清理。

最后把主循环压成看得完的六步：刷新状态、取输入、分 slash、跑普通轮次、收队列、落存档。

这一阶段不改 `/clear` 语义，不改排队画法，也不改跨会话协议。对应 TODO 另做，免得出了回归找不着账主。

### 第七步：搬 OneShot，收掉双份装配

新增 `app/one_shot.*`，把 `AskOnce()` 原样搬出。它继续复用已经落地的 BackendStack、ToolRuntime、TurnRunner。

这一步只消掉真正重复的材料：

- system prompt 拼接次序。
- model instructions、soul、skills 与 project instructions 的组装。
- ToolRuntime options 的共同底稿。
- ContextTracker 与 `RunTurn` 接线。

交互独有的 ask_user、session store、pending queue、peer、recorder 不准塞进 OneShot。OneShot 也别为了“复用”去构造半残的 InteractiveSession。

验收：拿同一份配置分别构造 Interactive 与 OneShot，断言最终 system prompt 次序一致；再断言工具清单只差设计上允许的交互工具。保住现有管道输入、位置参数、图片输入与退出码。

### 第八步：搬 CliApp，削平 main.cpp

新增：

```text
app/cli_options.*
app/cli_app.*
```

`ParseCliArgs()` 做纯函数：输入 `vector<string>`，返回 `CliOptions` 或一条错误。它不读配置，不探终端，不打印。

`RunCli()` 搬进 `cli_app`，只管：

1. 解析参数。
2. 处理 version、help、config、update 这些早退分支。
3. 加载配置、模型目录、persona、soul 与 project instructions。
4. 挂 session hooks。
5. 选择 `RunInteractiveSession()` 或 `RunOneShot()`。

hooks 也用 scope 对象关门。中途 `return`、抛异常、向导退出，都须发对称的 session_end 或按既定规矩不发，不能靠人肉数出口。

收尾后 `main.cpp` 只留平台入口：Windows 做 UTF-16 -> UTF-8，两边都调 `lubancode::app::RunCli(args)`。目标守在 80 行以内；150 行仍是硬上限。

### 第九步：把 app 实现赶出头文件

眼下 `backend_stack.hpp`、`tool_runtime.hpp`、`turn_runner.hpp` 与四组 command header 都带着大段实现；`settings_commands.hpp` 已有五万多字节。搬出 `main.cpp` 后若仍全塞头文件，只是把编译负担换了门牌。

按模块逐一拆成 `.hpp + .cpp`：

- `.hpp` 留 options、状态结构、公开函数与必要类型。
- `.cpp` 收 nlohmann、具体 backend、终端 painter、文件系统等重 include。
- 能前置声明便前置声明；值成员与模板所需类型才进头文件。
- 测试只准 include 公共头，不准为摸实现 include `.cpp`。

这一步可以跟第六至第八步逐件同行，不必最后一口气大搬。每只 commit 只拆一组，免得 linker 报错铺满全树。

### 第十步：清债与封口

结构站稳后再做：

- 删掉 `main.cpp` 搬空后留下的 using、include 与旧年代注释。
- 复查 `app/commands` 分组；某组若仍过千行，再按真实状态所有权拆，不按“一条命令一对文件”机械切碎。
- 逐步把需要单测的直接 `std::cout` 换成 `CommandIO`。
- 评估多层 request wrapper 是否合进 `SessionRequestBackend`。这另起行为 commit，不同搬家混做。
- 跑 include hygiene，查环依赖与意外的 `app -> main`、底层 `-> app`。
- 更新这份 TODO 的行数、文件表与完成状态。

路径杂务已有一部分先走了：`CurrentDirUtf8` 已归 `platform/paths`。余下 `PathToUtf8`、`SameFilesystemPath` 按定义归位，别在 app 层再留同名转发壳。

## CMake 怎么放

眼下新 app 模块多为 header-only，靠 `main.cpp` 那一条编译单元带进可执行文件。第六步起，新增的 `.cpp` 明列进 `lubancode` executable sources。

先守这条边界：

- 不碰真实终端的纯逻辑，进 `lubancode_core`，方便 doctest 直测。
- `interactive_session.cpp`、`one_shot.cpp`、`cli_app.cpp` 这类终端编排，先编进 `lubancode`。
- 命令 handler 若只依赖 core 接口，可进 `lubancode_core`；若仍直接读终端，先留 executable 一侧。
- 不为“看着整齐”急造 `lubancode_app` 静态库。

待 app 公共接口站稳，又确实有多只测试 executable 要链接，再建内部 app library。build target 跟着依赖走，不抢在代码前头。

## 每个 commit 的规矩

- 一只 commit 只搬一块，或只改一种所有权。
- 搬家 commit 不改算法、不改文案、不批量重命名、不顺手格式化。
- 源文件删多少，目标文件便能对上多少；审 diff 先看是否原样移动。
- 每只 commit 至少过 Release 编译与 `ctest`。
- 动到 live UI、TurnRunner、InteractiveSession，补跑 composer、stream footer、markdown、fold/clear screen driver。
- 动到命令派发，跑 slash 路由测试与相关状态测试。
- CMake、include 与代码同 commit，不留半截树。
- 用户工作区里无关的未跟踪 TODO 不准捎进 commit。

## 风险账

### 构造与析构次序

InteractiveSession 会同时持有 BackendStack、ToolRuntime、AgentLoop、UI 回调、peer runtime 与 recorder。谁借谁的引用，须写在成员旁，并用成员反向析构次序验。至少做一颗真构造、真启动可用部件、真析构的测试。

### lambda 升成员方法

眼下 lambda 靠捕获表借到状态。升成方法后，最容易把“每轮临时量”误升成“整场成员”，或把调用当时的值变成陈旧缓存。每搬一只，先列它读、写、拥有、借用四张小账。

### `/clear`、`/provider`、`/resume`

这三条会重建 loop 或 backend，又只替换一部分会话状态。逐项断言 history、approval、loaded tools、transcript、queue、title、context、memory、session id、peer inbox 各留还是各清。

### UI 回调与线程退场

footer、spinner、markdown、agent ticker、peer/background 完成通知共用终端与锁。对象搬家不能缩掉锁域，也不能让回调活得比 InteractiveSession 久。退出、异常、Ctrl+C、EOF 四条路都要跑一遍清场。

### Interactive / OneShot 漂移

两边应共用 backend、tools、prompt、turn runner，却不能共用交互状态。用构造结果测试钉住共同点与差异，别只看两边都能答一句。

### header 拆 cpp 的 ODR 与链接风险

当前实现多在头文件里。挪进 `.cpp` 时要查 inline、匿名命名空间、模板与测试引用。一次只拆一组，Windows Release 与测试配置都要链接一遍。

## 做成的样子

收尾时应满足：

- `main.cpp` 只管平台入口和 argv 转码，80 行为目标，150 行为硬上限。
- `RunCli` 可单测参数分派，不起真终端。
- Interactive 与 OneShot 共用 backend、tools、prompt 与 turn runner。
- InteractiveSession 自己守住会话状态、资源寿命与退场清理。
- slash 顶层 `switch` 仍可保留，却只剩清楚的领域转发；肥 `case` 全数退场。
- 短小枚举 `switch` 留在所属 handler，不为消灭语法而绕路。
- live UI 不知道 session store，session command 不知道光标锚点。
- app 公开头文件不再驮着数万字节实现。
- 全套单测、Release 编译、Windows screen drivers 都过。
- `git diff` 看不见无关格式 churn。

## 不做

- 不趁机改排队样式、跨会话协议、Record 语义或子代理 UI。
- 不改外部 CLI 参数和 slash 命令。
- 不改 JSONL、配置文件、skill、plugin ABI。
- 不上动态命令注册表；除非插件真要在运行时添 slash 命令，再另立设计单。
- 不造一只包揽全场的 `CommandContext`。
- 不为了“面向对象”给每个自由函数套类。
- 不把 `InteractiveLoop` 原样搬成另一只长期存活的两千行文件。
