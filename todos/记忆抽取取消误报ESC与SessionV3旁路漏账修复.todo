# 记忆抽取取消误报 ESC 与 Session V3 旁路漏账修复

- 日期：2026-09-15
- 状态：已修复，验收见 §五 勾选记账。PR #96（https://github.com/relic-yuexi/LubanCode/pull/96），
  头提交 cb2664eb 的活跃 check suite 全绿（ci run 34973753586：macos-clang/lane/changes=success，
  layout-links-skill-route=success；linux 腿按矩阵 skipped）。
- 级别：P1。提示错报取消来源，关键旁路无账可查。
- 范围：采样取消归因、抽取错误提示、V3 旁路请求记录、抽取评估记录、回归验证。
- 关联：`回合记忆抽取JSON失败_引号转义与解析收口修复.todo`、`记忆写入调度_显式保存门控批处理与成本账.todo`、`失败与恢复_写账闸门工具结果与重试收口.todo`。
- 本单只记录交接事项，不代表修复完成。不要靠放宽超时或换模型掩盖问题。

## 一、现场与证据边界

用户在 D:\MiniHarness 工作目录看到：

```text
── Worked for 23s ──
[memory] 回合总结(research)… · cheap:minimax-m3
[memory] 回合总结失败,本轮跳过: 抽取请求失败: 用户按 ESC 打断了这次请求
```

用户明确表示没有按 ESC。

现场会话文件并不叫 session.jsonl，而按会话编号命名：

```text
C:\Users\moontidef\.lubancode\workspaces\D--MiniHarness-c24e9576\sessions\20260915-113544-QVIAUJ\20260915-113544-QVIAUJ.jsonl
```

检查时共有 309 行，27 条消息、282 条事件，schemaVersion=3。
第 2 行 session.started 记 launchCwd=D:\MiniHarness。
最后一轮与截图耗时相符，时间下表已从 UTC 换成北京时间：

| 行号/seq | 时间 | 记录 |
| --- | --- | --- |
| 226 | 2026-09-15 19:39:45.950 | input.received，turn-3 |
| 227 | 同轮 | 用户问“就是tool执行完的结果是在哪？哪部分会被传回去？” |
| 303 | 19:39:51.579 | model.request.prepared，request-000010，purpose=conversation，model=minimax-m3 |
| 307 | 19:40:09.926 | model.response.completed，finishReason=end_turn |
| 308 | 同次收尾 | assistant 回答落盘 |
| 309 | 19:40:09.932 | context.input.applied；检查时文件到此为止 |

从 input.received 到最终 model.response.completed 共 23.976 秒。
这只能核对主回合，不能拿来计算后续记忆抽取耗时。

全文件 10 条 model.request.prepared 的 purpose 均为 conversation。
按事件 kind 与请求 purpose 检查：memory 事件 0 条，memory_extract 请求 0 条，
cancel/interrupt 事件 0 条。不要用正文出现的 CANCELLED 等词冒充取消事件。
同目录旧会话 20260914-064108-SLBJOF.jsonl 也查过：117 条事件，上述三类均为 0。

检查时在跑的进程：PID 49076，启动时间 2026-09-15 19:35:44，路径：
`C:\Users\moontidef\AppData\Local\Programs\lubancode\lubancode.exe`。
文件修改时间为 2026-09-15 11:15:23；尚未取得其构建提交号。
PID 与文件时间仅作现场定位，不能当成版本指纹。

已查实：主回答正常收尾，现场缺记忆抽取账；当前源码存在下述两处缺陷。
尚未查实：这次抽取具体耗时、真正取消来源、provider 返回情况、运行二进制对应提交。
45 秒看门狗超时最符合当前代码路径，仍属推断。缺少取消事件不能证明用户没按键，
错误文案也不能证明用户按过键。不要把本次事故写成“已确认 45 秒超时”。

## 二、Bug 1：内部超时错报用户按 ESC

当前调用链（行号只供定位，实施时按符号查）：

1. `src/app/commands/memory_commands.cpp:622`：ExtractTurnMemory 设置 timeout_secs=45，
   没给 SampleOptions.cancel 赋外部信号。
2. `src/app/model_router.cpp`：ModelRouterService::Sample 原样传递 SampleOptions。
3. `src/agent/sample_model.cpp:65`：SampleModel 起看门狗，到时置 local_cancel；
   无外部 cancel 时，将本地标记传给 backend.send_stream。
4. `src/api/http_stream_transport.cpp:184`：只要取消标记升起，便返回 Cancelled，
   文案固定为“用户按 ESC 打断了这次请求”，不查来源。
5. `src/agent/sample_model.cpp`：失败结果直接拷贝 sent.error()，没有修正文案与分类。
6. `src/app/memory_extract.cpp:FinishMemoryExtraction`：将错误包成 TransportFailed，
   拼上“抽取请求失败”，终端随后照印。

注意：SampleModel 已在 boundary_recorder 路径区分 UserInterrupt/Internal/StreamError。
现有区分只用于记录，没修正返回给调用方的错误；且 V3 此处没有 recorder。
不要重复另造一套相互矛盾的归因。

- [x] 在采样层保留真实取消原因，区分用户取消、本地 deadline、其他内部停止、来源未知。（sample_model.cpp 四分归因：外部旗升旗人申报 `SampleOptions.cancel_source`（默认 UserInterrupt，起名精炼申报 Internal）＋本地 deadline（local_cancel/合并旗 fired=2）＋来源未知（谁的旗都没升）；goal_evaluator 的组合旗由其看门狗记 fired、采样后就地改回内部超时——不另造第二套归因。测试 `unit.agent.test_sample_model` 的"取消归因(Bug 1)"六案。）
- [x] 底层 HTTP 只知取消信号时，用中性文案，不得臆断按键。（http_stream_transport.cpp:185 改"请求被取消信号中止"；types.hpp 的 ErrorKind::Cancelled 注释同步改中性。）
- [x] 本地 deadline 命中应返回可识别的 timeout 原因；沿用/扩展何种 Error 字段，先核对全库消费者。（全库 api_code 消费者只三处且只在 ErrorKind::Api 分支读（model_request_recovery/session_commands），Cancelled 分支挂 `api_code="local_deadline"` 对它们惰性；抽取侧立 `ExtractionErrorCode::DeadlineTimeout`（稳定码 deadline_timeout），v2/v3 assessed 的 error_code 字段都是自由串，schema 不破。）
- [x] 终端、SampleResult、抽取错误、事件记录共享归因。不能只替换中文字符串。（同一枚 cancel_source 驱动：SampleResult.error（文案+api_code）、OnOutputCancelled(source)、FinishMemoryExtraction（DeadlineTimeout 分流）、终端 trf 行。）
- [x] 内部超时提示应含抽取预算，例如“记忆抽取超过 45 秒，本轮跳过”；只有来源有证据才说用户取消。（i18n `memory.extract.deadline` zh/en，预算 45 抽成 kMemoryExtractTimeoutSecs 单源，prepared 事件同数落 timeoutBudgetSecs。）
- [x] 明确完成与 deadline 同时发生的裁决规则，避免完整成功响应在收尾被误改为取消。（裁决：MessageDone 已见（stop_reason 非空）且无流内错 → 完整成功响应胜，收尾取消旗不改判；测试"完成与 deadline 同场"案。）
- [x] 保留半截响应已有 usage 与结束信息；取消不能伪造成功，也不能把已上报用量抹掉。（usage/text/stop_reason 半截照带回是原语旧口径未动；v3 旁路失败/取消补 model.usage.appended 落 usage（含半截失败），不伪造 assistant。）

## 三、Bug 2：Session V3 记忆抽取请求与评估结果漏账

当前源码明确留下两道空返回，足以解释现场为何没有旁路事件：

- `src/app/interactive_session_controller.hpp:MakeTailContext` 将 session trajectory 交给抽取。
- `src/app/commands/memory_commands.cpp:ExtractTurnMemory` 调 NewBypassBridge，
  将结果交给 sample_options.boundary_recorder。
- `src/runtime/trajectory_session.cpp:3007`：NewBypassBridge 遇 is_v3() 直接返回 nullptr。
  调用仍继续，prepared/sent/usage/output 这条记录链不工作。
- `src/app/memory_extract.cpp:1182`：MemoryTurnLedger::RecordAssessedLocked 调 trajectory_->main()；
  V3 没有旧版 main recorder，随后提前返回。抽取门控、耗时与失败统计也没写。

- [x] 给 V3 接通旁路请求记录，使用现有 V3 writer/typed 事件合同，不往 V3 文件硬塞 V2 行。（TrajectoryBypassBridge 增 v3 写模式：system/转写 user 走消息行（新 purpose=memory_extract，schema 纯追加），prepared/sent/started/终态/usage 走事件行；v2 路一字不动。测试 `unit.trajectory_v3.test_v3_memory_bypass`。）
- [x] 记忆请求保留 purpose=memory_extract，并关联触发它的会话、主回合、独立请求编号。（prepared 载荷 purpose=memory_extract（accounting 合同名）；行 turnId=memory-turn-<n>（writer 新号池前缀，Continue 恢复认前缀）、parentTurnId=触发主回合（V3SessionBooks.active_main_turn_id，主桥 BeginTurn 立、EndTurn 不清）；请求号走 writer 全局 request-<n>。）
- [x] 记录 prepared/sent 与成功、失败、取消终态；usage 按既有规则落账，含半截失败。（成功=started+completed+assistant 成行（usage 归 assistant，§4.12）；失败/取消=裸事件+model.usage.appended（usage owner 账不沉没）；单一终态测试钉死。）
- [x] 记录 timeout 预算、实际耗时、取消来源/原因码、模型和可用的 provider response id。
  字段归属按现有 V3 合同确定，不要求每条事件重复全部字段。（预算=prepared.timeoutBudgetSecs（RequestPreparedContext.timeout_budget_secs 新字段）；实际耗时=assessed.extractWallMs；取消来源=cancelled.reason（internal_cancel/user_interrupt/stream_error 规范名）；原因码=assessed.errorCode（deadline_timeout）；模型=prepared/assistant 的 provider/wire/model；provider response id=assistant.responseModel 与 appended.providerResponseId。）
- [x] 接通 MemoryTurnLedger 的 V3 写口，记录跳过原因、抽取结果、extract_wall_ms、失败原因码。（新 kind memory.extraction.assessed / memory.write.receipted（schema 纯追加，statusless），载荷 camelCase 与 v2 同一套账；RecordAssessedLocked/RecordReceiptLocked 双路分派，v2 老路不动。）
- [x] 请求写账失败时遵守既有发送边界规则；不得静默退回“请求已发、无账可查”。
  评估统计写入失败则进入现有诊断口，别拖垮已经完成的主回答。（prepared/sent 记不住返回空/false，SampleModel 停在边界不发模型（既有栅栏复用）；桥提交失败进 recent_errors/error_sink（/doctor trajectory 可见）+日志；assessed/receipted 落不稳只吞（v2 同口径）。）
- [x] 旁路输入和输出不混入 conversation 历史，不改变主回答与恢复后的模型上下文。（旁路消息不经 AdmitMessages、purpose!=conversation 时 CompleteStreamResponse 不接纳；测试断言链投影只含主回合输入、system 根未被抽取提示词顶掉。）
- [x] 沿用记录隐私规则：不落凭据，不为排障默认保存完整提示、转写正文或响应正文。（旁路消息行与 v2 A1 桥/验收内部回合同一口径：只落模型实际收发的材料本体，无凭据、无额外"排障快照"。）

## 四、邻接项：修复时顺手核查，不能先报成现场根因

- [x] 核查 NewBypassBridge 所有调用者。compact、会话起名、doctor 等也可能受 V3 空返回影响；
  列出真实入口和覆盖情况，能共用修复便共用，超出本单部分另开单。（清册：memory_commands.cpp:630 抽取——本单接通；session_title_refiner.cpp:59 起名——维持 nullptr（system 白名单未放行 session_title 的 system 消息，接通须连 schema 追加，另开单）；doctor_commands.cpp:820/961/1076 探针——维持 nullptr（v3 合同未铺，另开单）；memory_commands.cpp:769 按需摘要（microcompact）与 session_commands.cpp:459 compact 一族——维持 nullptr（v3 有 RunV3Compact 全链，不走旁路桥）；tests/manual/usage_smoke_driver.cpp:137——v2 场不受影响。工厂按 purpose 过门（默认 other_host_request 仍 nullptr），未过门调用方行为零变化，测试"工厂门"案钉死。）
- [x] 核查 SampleOptions 同时带外部 cancel 与 timeout 的行为。当前 effective_cancel 只取外部标记，
  本地看门狗升旗却无人读取；这不是本次 memory 调用的现场根因，但不能在统一归因时遗漏。（收口：合并取消——两链同场时看门狗两头盯（goal evaluator 组合旗同形状），任一升起都掐流并记谁先升；测试补"合并取消"两案（deadline 先到归 internal＋local_deadline、外部先升归申报）。既有双旗调用方 microcompact（默认 timeout 45+cancel）的超时从死档变生效，属修复非回归。）
- [x] 核查外部 cancel 的实际来源，不能仅凭指针非空或标记升起就一概命名 user_interrupt。（升旗人申报制：起名精炼的旗（自家看门狗退役+会话拆除 RequestCancel）申报 Internal 并把 5 秒预算交给采样层合并口；goal evaluator 组合旗由其看门狗记 fired（1=用户侧/2=deadline），采样后按真相同一枚 local_deadline 码就地修正；workflow llm 节点的 request.cancel 升旗链混合（/workflow cancel 与错误中止），暂保默认申报，记缺口待后续单拆。）
- [x] 核对安装版构建提交与源码差异；不要把源码静态检查写成该二进制已经复现。（未取得事故二进制的构建号；本单全部结论止于源码级修复＋可控测试，安装版差异与 provider 慢因归真机验证（§五末条）。）

## 五、验收与交付

- [x] 用可控 backend 复现内部 deadline，不按键也能叫停；断言返回原因、中文提示、落账原因一致，
  不含“用户按 ESC”。使用短预算，避免单测固定等 45 秒。（unit.agent.test_sample_model"本地 deadline"案：timeout_secs=1＋挂死 backend，api_code/文案/事件三口径断言，文案无 ESC 无用户指控；memory.extract.deadline 终端行在 memory_commands 死码路径，预算 45 只进文案不进测试等待。）
- [x] 分别覆盖明确用户取消、来源未知的 Cancelled、普通网络失败、正常成功与结束边界竞态。
  如调整外部取消与 deadline 合并方式，补两者同时在场的测试。（用户取消=申报案甲；来源未知=中性文案案；网络失败=透传案；成功=既有 usage 案＋v3 成功例；结束竞态="完成与 deadline 同场"案；合并两案=合并取消甲乙。）
- [x] 用真实 V3 写入路径跑一轮可控记忆抽取，成功与超时各一例；重读 JSONL 验证请求关联、
  purpose、单一终态、usage、耗时与 MemoryTurnLedger 评估结果均在账上。（unit.trajectory_v3.test_v3_memory_bypass 成功例＋超时例＋ledger 案：真 TrajectorySessionLedger 开 v3 场、FindV3SessionStream 重读 JSONL 逐行断言，VerifyV3File 收卷。）
- [x] 验证主回答先完成、抽取后失败时，主回合仍正常；旁路失败无新候选写入，也不删除旧记忆。（ExtractTurnMemory 失败路只打一行＋NoteExtractionOutcome，无 AddCandidate/EnqueueSave 调用（既有结构，超时例走同一条失败路）；v3 旁路失败不落 assistant、不动链，主回合消息不回写。）
- [x] 以 JSONL 回放检查旁路消息不进入主对话；校验 schema、哈希链及现有读取器兼容。（ReadV3Ledger＋ProjectModelContext 断言链投影只含主回合输入；VerifyV3File 收卷；purpose 枚举与 memory.* kind 名字往返案；CheckPreparedAgainstChain 对非 conversation purpose 只验引用落账（读侧既有合同）。）
- [x] 验证关闭并重开会话文件后，能仅凭事件回答“哪次抽取、预算多久、实际多久、谁叫停”。（重读路径即"重开"：parentTurnId/turnId 答哪次，prepared.timeoutBudgetSecs 答预算，assessed.extractWallMs 答实际，cancelled.reason＋assessed.errorCode 答谁叫停。）
- [ ] 如要验证 minimax-m3 实际耗时，另做安装版/provider 现场验证，记录构建号和时间；
  可控测试通过不代表原事故已复现，也不代表 provider 慢因已查明。（未验——归真机，本单未取得事故二进制构建号。）
- [x] 交付列出改动、测试命令与结果、构建/安装验证范围、仍缺的证据；完成项再勾选。（本节即账；测试经 CI 跑 `unit.agent.test_sample_model` / `unit.memory.test_memory_extract` / `unit.trajectory_v3.test_v3_memory_bypass` 及全量回归；构建验证只到 CI 三腿，安装版未碰。）

本次修复改动面：src/agent/sample_model.{hpp,cpp}（四分归因＋合并取消＋裁决）、src/agent/loop.hpp
（RequestPreparedContext.timeout_budget_secs/SampleOptions.cancel_source）、src/api/http_stream_transport.cpp
（中性文案）、src/api/types.hpp（注释）、src/app/memory_extract.{hpp,cpp}（DeadlineTimeout＋V3 写口）、
src/app/commands/memory_commands.cpp（purpose 过门＋预算单源＋终端行）、src/cli/i18n.cpp
（memory.extract.deadline）、src/app/session_title.{cpp,_refiner.cpp} 与 src/runtime/goal_evaluator.cpp
（升旗人申报/就地修正）、src/runtime/trajectory_session.{hpp,cpp}（旁路桥 v3 写模式＋工厂门＋
active_main_turn_id）、src/trajectory/v3/{envelope,writer,schema3}（purpose=memory_extract＋
memory-turn 号池＋memory.* 两 kind，schema 纯追加）、docs/architecture/trajectory-v3-schema.md。
超时预算 45 秒未放宽、未换模型、未动用户配置与历史会话文件。未验边界：安装版差异、
minimax-m3 慢因、事故二进制的构建号比对（归真机）。
