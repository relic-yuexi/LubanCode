# 欠账与观察清单：各单移交的散件统一记档

- 状态：滚动记档（各单销账时移交的小笔、观察项、隐患雷统一落此；处理一笔勾一笔销一笔）
- 日期：2026-09-06 立账（梳理 todos 目录时归拢）
- 规矩：一笔一档，写清出处（哪张单/哪个 run/哪次会话）；立单实施的大项不在此凑数，够格就另立正单并在此记指针

## 欠账（要做）

- [ ] **恢复器生产入口接线**（出处：Workspace收官验收单移交）——`SessionManager::RecoverWorkspace` 库与测试册齐备，启动路未接；崩溃场 session.json 由 doctor 如实展示，"自动收口"待接 CLI 或启动路。够格时另立正单。
- [ ] **Windows 读侧 FILE_SHARE_DELETE 开口**（出处：同上）——换名×并发读句柄的 `atomic.replace_failed` 微窗，调用方已有稳定码处置；要根除需 src/platform 改读侧开口，小单。
- [ ] **mid-JSON 撕裂前缀恢复**（出处：同上）——现行为 fail-closed 整场拒收；"截到半行按前缀恢复"属合同变更，要做先过设计。
- [ ] **PromptCache legacy_expand 删除作业**（出处：PromptCache 单销账移交，2026-09-05）——迁移窗到期执行；删除清单六条钉在 `docs/reference/tools.md`"迁移窗结束后删什么"节。
- [ ] **假 rg 千轮压力册贴超时线**（出处：rg 竞速窗修复批，2026-09-05）——实测 ~160s 贴 ctest 180s 线，Windows 高负载有超时翻车前科风险；要么拆轮次要么提超时。
- [ ] **BadAppServerWs 的 error_text 无人打印**（出处：警告清扫批 8b34bf15 点名）——疑点已注释在码内，需定"补打还是删字段"。
- [ ] **README 两笔旧错位**（出处：入门双页审计批 afd054d0）——英文 Quick start 末尾多一段 catalog 来源说明；中文常用命令表多 `/archive`、`/delete` 两行。
- [ ] **监督器旧两笔待核**（出处：早期会话移交）——跨平台测试与真实软线校准；年代久远，先核是否已被后续单覆盖再定去留。

## 观察名单（暂不动手，复现两轮以上升级立单）

- [ ] **workspace 集成册 windows 腿连环抽风**（出处：workspace_index_ledger run 34316108135、identity_cross_platform run 34335606083，2026-09-09 两案两只不同册）——同族不同册，各自重跑皆绿；workspace 集成测试对环境/时序敏感，再犯第三案就整族立案（查共享夹具/临时目录/并发读窗）。
- [ ] **取消树册段错误的直接机制悬案**（出处：run 34307354040 PR 腿崩、同 commit push 孪生绿，2026-09-09；第二道不投闸 PR #10 收口后未复犯）——闸拔了时序温床，但哪条指针崩的没钉死；若再犯，先给 CI 加崩溃栈（coredump/atos）再战。
- [ ] **嵌套子代理裸持父栈上 ToolRegistry\***（出处：回流锁缝单追查时的休眠发现，2026-09-09）——孩子线程经 base_registry 指向父任务 RunTask 栈上的 scoped registry，唯一护栏是"父不得先走"不变量，取消路径恰是破门。建议改共享所有权，小单。
- [ ] **忙路 footer 光标钉回未用 NativeColumnForLogical**（出处：conhost 几何单销账移交，2026-09-09）——console_input_stream_footer.cpp 仍用逻辑列，采用映射是一行后续单；空闲路 RedrawEditArea 在 Windows 恒走 legacy 属设计现状非欠账。
- [ ] **subagent_recursion windows 抖动**（出处：run 33988795509 attempt 1，2026-09-06；二犯 run 34270604648，2026-09-09）——真凶已破：生产回流锁缝（子代理回流锁缝单，v0.26.220 根治），非测试等待合同。留档备查，不再单独立案。
- [ ] **cpr 驱动器 Windows 真控制台四幕**（出处：cpr 单验收移交）——真控制台跑 agent_stream_driver 四幕的实机抽验欠着。
