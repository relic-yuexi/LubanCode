# 常驻助理 Web 主界面：C++ 本地服务与 Gateway 总装

日期：2026-09-14。源码核对基线：工作区 `f4559cb1`。
状态：设计与实施清单。W0/W1 已落（2026-09-15，PR 待补链接）：断线合同、任务归属路由、`lubancode assistant` 本地服务 + 随包网页 + 认证 + 聊天；合同文档 `docs/features/assistant-web/README.md`。W2-W5 未动。

## 一、目标先定清

先交付一位能直接使用的常驻助理，再为它添加 QQ、飞书等沟通渠道。用户打开网页，就能聊天、看历史、交代任务、看结果、处理审批；没有任何 IM 配置也能用。

C++ 启动本地 Web 服务，提供 HTML、JavaScript、CSS。页面通过 AppServer 协议调用后台；Gateway 管常驻工作、调度、恢复和结果投递。用户启动一套助理，不必手动拼接两个服务。

产品目标继承[持久助理总计划](持久Agent_Gateway接入SessionV3_常驻助理与可靠自动任务总装计划.todo)：任务记得住，到点能执行，结果找得到，重启后按账处理。不把“页面能打开”“PID 活着”“聊天能回复”当成全部交付。

首版：本机、单用户、单实例/profile、浏览器界面，先验 Windows 发布包。远程访问、多用户、多节点、云端部署另列设计；不以本单为由改变普通 CLI、独立 AppServer 和现有 Gateway 默认行为。

## 二、现有件与真缺口

| 源码/文档 | 本次核对事实 | 本单工作 |
| --- | --- | --- |
| [Web 参考前端](../examples/web-console/README.md) | 已有 index.html、CSS、core/app/touch JS | 复用协议客户端与展示经验，扩成助理主界面；先审查版本、断线及渲染行为，不整份照搬 |
| [AppServer 客户端合同](../docs/reference/app-server-external-client.md) | 1.3 基线已有 initialize、thread/start、turn/start、operation/read、历史和终态事件 | 复用聊天协议，补常驻任务、配置、审批与渠道管理合同 |
| [server.hpp](../src/app_server/server.hpp)、[server.cpp](../src/app_server/server.cpp) | WS 连接与线程/回合由 Server 管理；既有连接服务合同在断线后打断运行回合 | 提取连接订阅与后台工作生命期，助理模式不能直接沿用断线取消 |
| [ws_transport.cpp](../src/app_server/ws_transport.cpp) | 已有 WS 监听、升级与 artifact HTTP 读取 | 核对后复用底层；新增受限静态资源服务和同源认证，不声称现有代码已是完整网页服务器 |
| [cli_app.cpp](../src/app/cli_app.cpp) | AppServer 与 RunGateway 是不同启动分支 | 提取共用装配，新增一只助理宿主同时装控制接口与后台工作泵 |
| [channel_gateway_wiring.hpp](../src/app/channel_gateway_wiring.hpp) | ChannelGatewayWiring/CompositeGatewayPump 负责渠道与自动任务装配 | 复用，不在 Web 控制器另开 QQ 连接、另建工作泵 |
| [headless_executor.hpp](../src/runtime/headless_executor.hpp) | Gateway 已复用 SessionService 执行入口 | 提取足够小的运行服务接口，禁止 Web 与 Gateway 各自重跑同一任务 |
| [持久助理总计划](持久Agent_Gateway接入SessionV3_常驻助理与可靠自动任务总装计划.todo) | V0/V1 已记录实现；周期调度、系统服务与完整恢复仍有待办 | 延续领域账与验收，不新造浏览器任务账冒充完成 |
| [QQ 修复单](QQBot_Windows连接诊断与交互配置修复.todo) | Windows 连接、日志、密钥安全创建与 IM 向导尚待完成 | 作为“渠道设置”子项目纳入主界面 |

## 三、分层与统一协议

```text
浏览器：聊天 / 历史 / 任务 / 结果 / 审批 / 设置
       │ HTTP 取静态资源；WebSocket 收发 AppServer 请求/事件
       ▼
C++ AssistantHost（拟新增，一只常驻宿主）
  ├─ LocalWebServer：页面资源、认证、WS 升级
  ├─ AppServer 控制入口：检查身份、参数、能力、请求幂等
  ├─ 助理服务：会话、任务、审批、配置、渠道状态与事件订阅
  └─ Gateway：工作调度、恢复、渠道收发与 outbox
                  │
            SessionService / Agent / 工具

QQ / 飞书（待实现） → 各自适配器 → Channel Bridge → 同一宿主
```

本单统一的是“所有助理界面使用 AppServer 控制协议”。QQ 官方协议仍由 QQ 适配器处理，适配器与渠道宿主之间仍用 Channel Bridge。助理服务与 Gateway 在同一进程直接调用，不绕回本机 WebSocket。

| 状态 | 唯一负责人 |
| --- | --- |
| 页面筛选、滚动、草稿 | 前端；不得以 localStorage 中的任务副本当真账 |
| 参数与密钥 | 后端配置服务/凭据服务；界面只能提交或读取脱敏投影 |
| 工作区、用户身份与授权 | 宿主，输入来源不能自称 owner |
| 会话与执行事实 | SessionService/V3；沿用既有身份与恢复规则 |
| 计划、到期次数、工作领取 | AutomationStore 与 Gateway 调度服务 |
| QQ 等平台连接 | 适配器；Gateway 汇总状态，页面只订阅 |
| 回复内容选择与投递 | reply selection / outbox，沿用原有两层职责 |
| 运行实例与锁 | AssistantHost 复用 Gateway 的 profile/账号锁 |

- [ ] Web 聊天与 QQ 聊天共用服务与权限规则，不强行合并成同一会话。页面可以查看已授权的渠道会话；会话来源、workspace、任务归属和回复目标必须明确。
- [ ] 每个 task/turn 只由一个执行器持有；AppServer 的立即聊天执行路径与 Gateway 后台路径做明确路由，不能同一输入被两边各跑一次。
  - [x] W0 冻结（2026-09-15）：聊天线只经 turn/start（一场同拍一轮 + clientOperationId 幂等），定时线只经 AutomationStore；合同与路由表见 `docs/features/assistant-web/README.md` §三。测试：`tests/unit/app_server/test_app_server_detached.cpp`（同键零重跑）、`web/assistant/run_e2e.js` 幕3。Gateway 侧装配缝留给共用启动服务提取工位（W0 注记）。
- [ ] 同进程不等于共享所有可变对象。模型连接按会话/回合冻结，任务启动后改模型配置不追改在途请求；工具表、审批 broker、锁的线程与生命周期合同先写再接线。
- [ ] 有界网络/订阅队列与执行线程分开，长模型请求不能堵 HTTP、取消、心跳或停止；慢浏览器不得拖住持久提交与任务执行。

## 四、启动与停止

拟新增入口（名字在实现前与现有参数解析对账）：

```powershell
lubancode assistant
lubancode assistant --no-open
lubancode assistant --port 8765 --profile default
```

- [x] 默认解析参数根/状态根和工作目录，获取 profile 锁，启动 C++ 服务及 Gateway 工作泵；监听成功、接口就绪后才打开浏览器。（W1：`src/app/assistant_host.cpp` `RunAssistantMode`；Gateway 工作泵是装配缝，W0 注记留给提取工位）
- [x] 不指定端口时由系统分配可用本地端口；打印实际 URL。指定端口被占用时准确报错，不偷偷换端口。浏览器打开失败仍保留可复制的地址。（W1：`LocalWebServer::Start` 端口门；测试 `test_local_web_server.cpp` 端口冲突案、`run_e2e.js` 幕1/幕6）
- [x] 默认只绑定 127.0.0.1；若支持 IPv6 回环，单独处理地址和 Host 校验。首版拒绝非回环监听，远程访问不能靠改 host 参数无声开启。（W1：非回环绑定启动即拒，`test_local_web_server.cpp`；IPv6 回环未做，如实留账）
- [x] 重复启动同一 profile：先验证现存实例身份、健康与控制端点，只打开其页面；探测失败或锁不明则报错，不杀原进程、不复用可疑 URL。同一账号仍不能被其他 profile 重复占用。（W1：锁账 + /healthz 对 bootId + /control/open；`run_e2e.js` 幕6；“同账号不被其他 profile 重复占用”归账号锁批次，未做）
- [x] 关标签页、刷新或 WS 断线只撤订阅，不取消已受理的助理工作。`停止任务`、`停止助理`是两个独立操作；关闭页面不能发 shutdown。（W0：`WorkLifetime::Detached`；`test_app_server_detached.cpp` 两模式对照、`run_e2e.js` 幕5）
- [x] 助理模式显式采用 detached/background 工作生命期。独立 `app-server` 保留现有 EOF/断线/exit 合同，新增模式通过能力声明区分，不能全局改旧客户端语义。（W0：capabilities.mode/workLifetime additive；`test_app_server_detached.cpp` 能力声明案）
- [x] 第一阶段前台 C++ 进程随终端退出会停止，界面如实说明。后台服务安装、开机启动与故障拉起归总计划 V4，接好前不得声称“关终端仍运行”。关浏览器继续办事必须在首阶段验收。（W1：启动公告如实说明；关浏览器继续 = 幕5 已验；系统托管归 V4）
- [x] 停止助理先停收新活，再按现有宽限收口、落账、释放监听与锁；不明副作用标 needs_review。重新启动先恢复裁决，再接新任务。（W1：shutdown/Ctrl+C 宽限收口 + 锁释放 + 同 profile 可重启，`run_e2e.js` 幕6；“重启先恢复裁决”是 reconcile 批次的活，未做）

## 五、主界面与首次使用

- [x] 使用本地 HTML + JS + CSS，首版优先原生模块，不要求用户安装 Node/npm，不从 CDN 运行时加载库、字体或脚本。若引入前端构建，只在开发/发布期运行。（W1：`web/assistant/` 原生三件，无构建无 CDN）
- [x] 页面主导航：聊天、任务、结果/通知、待审批、设置。首页显示助理运行状态、当前工作区、待办数和异常，不以 QQ 在线与否决定能否使用。（W1 部分：聊天/设置已实现，任务入口占位且如实标“未开”；结果/审批页归 W2）
- [x] 首次未配模型：进入模型配置，提交到后端安全配置服务，校验后即可聊天。测试连接失败与配置保存成功分开显示；不自动创建 QQ 渠道。（W1：`config/model/set` + `config/test`（TCP 连通，如实不冒充鉴权）；零密钥回显；`run_e2e.js` 幕3）
- [x] 聊天页：历史列表、消息正文、工具进度、输入框、停止按钮、结果文件；输出与历史按服务事实恢复，不能刷新就丢整场。工作区在开场时确认，切换不能挪旧会话归属。（W1：thread/list + thread/read 刷新恢复（幕4）；工具进度按 item 事件渲染；结果文件/工作区开场确认未做，留账 W2）
- [ ] 任务页：任务名称、来源、计划、下次执行、最近结果、运行/暂停/失败状态；创建、立即执行、暂停、取消等只显示已实现能力。单次任务先交付，周期和 heartbeat 随 V2 解锁。（W2）
- [ ] 结果页：区分执行成功、投递成功、投递待重试、需人工核对；任务结果应先在本地界面可见，未配 IM 不应丢失结果。（W2）
- [ ] 审批页：展示动作、目标、所属任务/会话及权限范围。审批过期/重复提交有明确回执，刷新不会自动同意。浏览器离线期间不默认放行；是否等待/拒绝/超时沿用执行策略。（W2；W1 页面只在审批事件在场时显示批准/拒绝行）
- [x] 设置页：模型、工作目录/配置根信息、运行服务、渠道。QQ/飞书是可选项；平台表单复用 IM 配置服务，安全文件权限由后端处理。（W1 部分：模型 + 运行信息 + 停止助理已实现；渠道表单归 W3）
- [x] 适配桌面与窄屏；键盘可达、焦点可见，状态不只靠颜色。消息 Markdown 禁止原始 HTML 注入，危险 URL 拦截，工具输出按不可信文本展示。（W1：390px 媒体查询、focus-visible、textContent 渲染无 HTML 注入；真实浏览器 UI 验收未做，如实留账——见 §九 UI 验收）

## 六、AppServer 接口补齐

以下是待设计方法族，不是已经可调用的方法清单。先复用已有方法，再给缺口定名、schema、错误码与 capability；禁止前端直接读取/修改 config.json、任务 JSONL 或渠道账。

| 方法族 | 服务职责与合同 |
| --- | --- |
| 现有 thread/turn/operation | 复用聊天、幂等受理、历史、取消与结果核对；补助理工作生命期及后台归属 |
| assistant 状态/停止 | 查询实际 boot/profile/workspace/组件健康；停止必须经过本地控制授权 |
| task 列表/读取/创建/更新/暂停/取消/run-now | 调用同一 AutomationStore 控制服务；变更带 revision 与 clientOperationId，重复不新建任务 |
| channel 平台表/账号/配置/检查/启停/配对 | 调用统一渠道服务；状态来自活连接，配置与凭据读回脱敏；未实现平台不可伪装可用 |
| config 模型/工作区/更新/连接检查 | 定点原子修改，处理并发版本；区分需重启与新会话生效，冻结在途任务配置 |
| approval 列表/读取/答复 | 对接实际 InteractionBroker；核对审批归属、版本、时限与一次消费 |
| notification/result 查询与订阅 | 从任务、会话、outbox 构建只读投影；已读属于界面偏好，不能改变执行状态 |

- [ ] Web 与 QQ 创建任务必须进入同一个领域服务，带可信主体、来源会话、workspace、任务所有者与回复目标；自然语言任务创建工具调用同一服务，不能让 Agent 靠写 JSONL 或任意 shell 注册任务。（W2：任务面接线时落）
- [ ] 网页配对批准不等于授予 QQ 用户本机 owner 权限。跨来源查看/操作仍受归属限制；任务投递回原目标，改目标须校验权限与版本。（W2/W3）
- [x] 前端先 initialize 核对版本和 capabilities，不对 pending 方法画可点击控件。扩展采用兼容版本合同与探针，不能破坏现有 stdio/WS 客户端。（W1：`assistant_core.js` checkProtocolCompat 钉 1.3 + assistant 模式；能力声明 additive，`test_app_server_detached.cpp` 能力案）
- [x] 状态恢复采用“领域快照 + 有界事件订阅”。请求响应确认的是受理或提交，不能把 turn/start 回执画成完成；结束以持久终态核对。（W1：受理即回 + 终态事件/operation/read 核对；`run_e2e.js` 幕3/幕5）
- [ ] WS 游标绑定 boot ID，重启后旧 seq 不可直接套用。缺口/队列溢出触发快照重读，关键终态从领域账查回，不要求无限缓存事件。（未做，W2 重连补账批）
- [x] 网络不明时，以原 clientOperationId 查询/重试，不能每次刷新生成新提交 ID。用户明确重跑才创建新工作身份。（W1：1.3 受理幂等复用；同键重发零重跑，`run_e2e.js` 幕3）
- [x] 首版明确只允许一个活跃控制连接；其他标签页显示占用提示，提供显式接管。接管只换订阅/控制连接，不停后台任务；多控制客户端另列验收，不能照搬现有串行连接服务造成第二页一直等待。（W1：`assistant/connection/occupied` 通报 + `?takeover=1` 显式接管，接管不断后台任务；`run_e2e.js` 幕5）

## 七、本地 HTTP、认证与资源

- [x] HTTP 与 WS 用同一本地 origin；静态资源采用随包固定 manifest/白名单。只支持需要的方法和 MIME；拒绝目录穿越、编码绕过、目录列表及任意 workspace 文件路径。（W1：`AssistantWebManifest` 白名单即墙；方法只认 GET/POST 两类口；`test_local_web_server.cpp`）
- [x] 审查现有 ws_transport 的 HTTP 分支并提取本地服务承载，复用经过测试的 socket/HTTP 设施。若要新增第三方库，先定兼容、许可证、TLS/线程需求；不为静态文件手写一套无界 HTTP parser。（W1：`local_web_server.cpp` 复用 ws_sockets/ws_frames/升级后 Session，零新第三方库；头部/body/资源全有界）
- [x] 所有 HTTP 头、body、连接数、读取超时、WS 帧、订阅队列和下载并发有上限；大结果通过授权 artifact 接口流式读取，不塞进事件队列。（W1：16KB/8KB/4MB/64MB 各上限；artifact 口子带会话门复用内容寻址形状）
- [x] 本机也要认证：新助理模式不沿用“回环免鉴权”。严格校验 Host/Origin，拒绝跨站 WebSocket、跨站配置写入及 DNS rebinding；不使用通配 CORS。（W1：Host 必须回环本端口、Origin 在场必同源、静态/WS/artifact 全要会话 cookie；无通配 CORS；`test_local_web_server.cpp` + `run_e2e.js` 幕2）
- [x] 浏览器启动使用短期、一次性 bootstrap 凭据（仅 URL fragment，服务不记访问日志中的秘密），页面交换 HttpOnly、SameSite=Strict 会话 Cookie 后立即清除 fragment；交换只接受本 origin、限定时效并防重放。若浏览器策略阻止 Cookie，则显示本地配对流程，不降级免认证。（W1：#b= fragment → POST /auth/exchange → HttpOnly/SameSite=Strict 会话 cookie，2 分钟时效一次性恒时比较，重放 403；无凭据给配对提示页不降级；`run_e2e.js` 幕2。“浏览器拒 cookie”的真浏览器分支未验，留账）
- [x] CSP 限制为本地受控资源，禁用第三方脚本；密钥表单不进 localStorage、浏览器 URL、分析遥测、日志、trace 或模型上下文。后端只返回已配置与密钥来源，不回送密钥。（W1：应答带 CSP；密钥提交即忘不落页面；`config/*` 全零密钥回显，`run_e2e.js` 幕3 断言）
- [x] artifact 下载按当前授权工作区/任务解析 opaque ID，拒绝路径参数与链接越界；凭据目录、全局配置与信任账不可经下载口读取。（W1：只认 art-<hex>.(png|jpeg|jpg) 形状 + 固定目录 + 会话门；凭据/配置目录天然在口子外）
- [ ] 两个用户/应用根不共享 Cookie、bootstrap、profile 注册和资源权限。运行实例注册只保存必要地址/身份，密钥不落普通状态文件。（部分：profile 锁/会话按状态根隔离；锁文件里的实例控制口凭据落在普通状态文件，与“密钥不落普通状态文件”相抵——收紧方案（文件权限/派生凭据）留账）

## 八、常驻与可靠性

- [ ] 浏览器不是调度器：页面无 setInterval 触发业务任务，关页不影响到期扫描；本地页面已读通知也不能删除 outbox 待投递项。
- [ ] 来自网页、IM、定时任务的执行复用现有队列、并发上限、预算和持久身份；每会话的串行与不同来源公平调度要测，不能让定时任务堵死取消/审批。
- [ ] 自动任务结果默认落本地可查询结果；选择 IM 通知后，再建立独立渠道投递目标。平台拒绝主动发送、回复窗口过期时说明失败，不重跑已完成 Agent。
- [ ] 后端断电/崩溃后按总计划做 reconcile：可恢复已提交事实，不盲重放未知副作用；页面明确显示待核对任务。持久事实与易失订阅分开恢复。
- [ ] 系统服务安装后，前台 launcher 与后台 owner 分离：再次运行命令只打开页面；关闭终端不杀服务。服务账户、参数根、工作区和凭据权限须一致，默认不提权。

## 九、实施批次与验收

1. W0：冻结助理模式/接口/任务归属与断线合同，提取共用启动服务。复用总计划 V0/V1，不先做整套 SDK 重构。——合同部分已落（2026-09-15）：`ServerOptions::WorkLifetime` + 能力声明扩展口 + 路由表（`docs/features/assistant-web/README.md`）；“提取共用启动服务”归并行工位（RunGateway→服务提取），AssistantHost 留装配缝未提取。
2. W1：C++ 本地 HTTP + 随包网页 + 认证 + 聊天。无 IM 完成配置模型、真实对话、刷新恢复；关网页后已受理任务继续执行。——已落（2026-09-15）：`lubancode assistant`（cli_options/cli_app 新增块）+ `LocalWebServer` + `web/assistant/` 三件 + `config/*` 方法面；真 exe + 假模型 e2e（`e2e.assistant.web`）覆盖启动→配置→对话→刷新恢复→关页任务不死→重复启动/端口/接管。真实模型与真实浏览器未验（如实）。
3. W2：单次任务、结果、审批、重连补账。网页创建任务，关闭网页，到点完成，重开后能查到结果；两次提交不重复执行。
4. W3：渠道设置并入主界面，复用 QQ 证书/日志/安全配置修复；平台在线、配对、收发两轮文字验收。IM 快捷命令只做渠道配置/启动便利入口。
5. W4：周期任务与 heartbeat 接总计划 V2，系统托管接 V4，完成后台/开机/崩溃恢复。页面只展示确实交付的能力。
6. W5：发布包与真实常驻验收，完成后再考虑远程访问、多用户、Tauri 或 Android 外壳。

- [ ] 无渠道验收：干净普通 Windows 账号解压发布包 → 一条启动命令 → 浏览器配置模型 → 两轮真实聊天 → 创建单次任务 → 关页 → 执行 → 重开看结果，全程不装 Node、不手改 JSON。（前半已由 `run_e2e.js`（真 exe + 假模型）覆盖：启动→配置→对话→刷新恢复→关页任务不死；单次任务归 W2，发布包实装与真实浏览器归发布验收批次）
- [x] 断线验收：刷新、断 WS、第二页接管、浏览器退出不取消后台任务；显式停止任务才取消；旧 AppServer 客户端生命周期回归通过。（W0/W1：`test_app_server_detached.cpp` 两模式对照钉死旧语义不回归、`run_e2e.js` 幕4/幕5（含占用/接管）；显式停止任务走 turn/interrupt 既有面）
- [ ] 故障验收：端口冲突、重复启动、模型拒绝、磁盘满、坏账、崩溃窗口、重复请求、审批过期与投递失败，均有可核对状态，不画假成功。（W1 已覆盖：端口冲突/重复启动/重复请求幂等（幕3/幕6 + 单测）；模型拒绝/磁盘满/坏账/崩溃窗口/审批过期/投递失败留账 W2+）
- [x] 权限验收：跨站请求、伪造 Origin/Host、bootstrap 重放、下载越界、XSS、密钥回显、错工作区访问；QQ 身份不能借 Web 控制接口变 owner。（W1：跨站/Origin/Host/重放/密钥回显/白名单外 404 全验（`test_local_web_server.cpp` + 幕2）；XSS 靠 textContent + CSP 静态措施，动态用例留真实浏览器批次；QQ 面 W3）
- [ ] UI 验收：390px/1440px 页面、键盘导航、长消息、工具失败、等待审批、断线提示与历史分页；用真实浏览器交互，不仅检查 HTML 字符串。（未做：真实浏览器交互验收留批次，可循 browser.mcp.selftest 路——W1 明确未验边界）
- [x] 发布验收：HTML/JS/CSS 随包、版本 manifest 一致、无外部 CDN、安装后资源路径与启动路径正确。前端资产缺失明确报错，不启动空壳服务。（W1：CMake 镜像 web/assistant 到 exe 旁 + LocalWebServer 启动门缺资源明错拒启（`test_local_web_server.cpp`）；“安装后路径正确”的真包验证留发布批次）
- [ ] 实机区分：mock/单测、真实模型、真实 QQ、系统服务、72 小时运行分别记证据。只完成 W1 时只能声明本地主界面可用，不宣称完整常驻助理已交付。（如实：W1 只有单测 + 真 exe/假模型 e2e 证据，真实模型/浏览器未碰）

## 十、与相邻单分工

- [持久助理总计划](持久Agent_Gateway接入SessionV3_常驻助理与可靠自动任务总装计划.todo)：领域账、周期、恢复、系统服务与长期运行验收；本单负责界面和宿主总装。
- [QQ/IM 修复单](QQBot_Windows连接诊断与交互配置修复.todo)：平台连接、安全凭据及通用配置服务；主界面调用同一服务，不能另造一套 ACL/QQ 登录代码。
- [多前端外壳单](多前端外壳与浏览器Tab_AppServer承载设计.todo)：复用 WS、参考前端、artifact 与浏览器能力。旧“唯一协议缝”只说明应用外壳边界，不能推导所有内部宿主必须走 RPC。
- [LubanCore 分离单](LubanCore与CLI分离_核心库独立成宿主底座设计.todo)：本次按需提取宿主/服务边界；完整 SDK 产品化不是 W1 前置。
- [外壳打包单](Tauri与Android外壳打包与发布账.todo)：Tauri/Android 后续可消费同一协议；本单首版以浏览器为主，不依赖桌面 WebView。
