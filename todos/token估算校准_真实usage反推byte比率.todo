# token 估算校准：真实 usage 反推 byte/token 比率

- 状态：已实施（2026-09-04 立案，2026-09-04 工头落码；单测＋全量 ctest 381/381＋真机探针全过）
- 级别：P2（轻量改进——本地估算器用真实账自校准）
- 类型：LubanCode 产品改进（token 估算精度）
- 提案（用户原话要义）：每次请求的 byte 数是定的、usage token 数也回——两次请求就能反推出该模型的 token/byte 比率。BBPE 下多 byte 可能合一个 token，但统计出的平均水准已经够用；不同模型各异，各算各的。

## 一、现状与价值

本地 token 估算（`EstimateTextTokens`/`EstimateHistoryTokens` 一族）按字符/结构估，与真实分词有偏差——压缩触发线（0.26.190 双闸）、context 百分比、预算预判全都吃这个偏差。usage 账已在轨迹（`model.usage.recorded` 逐请求 input/output/cache 分列），请求侧字节数也可现算——**校准数据白送，只差用起来**。

## 二、修法

- [x] 会话内校准器：每枚请求发完，记 `(本地发送 byte 数, provider 实报 input_tokens)` 一对；滚动窗口（如最近 8 对）取中位数比率 `tokens_per_byte`。首两对之前用现估系数兜底。
  （实施：`src/agent/token_calibrator.hpp/.cpp`。样本三值 `(字节, 默认尺估算, 实报完整输入)`，中位数同时产 `tokens_per_byte`（显示）与 `估算×系数`（注入）两把账；窗口 8 对、两对起校准。）
- [x] 比率按 `(provider, model)` 分桶缓存（进程内即可；是否跨会话落盘另议——落盘要走既有配置/存储正门）。
  （实施：`DefaultTokenCalibrator()` 进程级实例，主会话/子代理/app-server/workflow 四路 TurnWiring 共指一只，同桶共享；落盘未做，留白。）
- [x] 估算口接入：`EstimateTextTokens` 一族加可选校准系数（会话级注入），压缩触发/context 百分比自动吃到。
  （实施：`EstimateUtf8Tokens/EstimateMessageTokens/EstimateHistoryTokens` 加 `calibration=1.0` 缺省参；loop 的固定账预检、A/B 双闸、preflight 三项账与 `/context` 三类估算/预算账全吃系数；不接线恒 1.0，双闸既有单测数字一字不动。）
- [x] 诊断可见：`/context` 加一行"token 估算校准：N 对样本，比率 x.x tokens/byte（估算偏差 ±y%）"；样本不足显示"未校准"。
  （实施：`cmd.context.calibration(_none)` 中英成对；真机形状："token 估算校准：3 对样本 · 比率 0.27 tokens/byte · 估算偏差 +52%（正=默认尺偏高，负=偏低） · 校正系数 ×0.66"。）
- [x] 护栏：比率异常样本剔除（如 cache 命中致 input 虚低时——用非缓存 input 对账，`input_tokens` 而非总数）；比率漂移超阈值重置窗口。
  （实施取舍：对账口径取**完整输入**（`TotalInputTokens`＝非缓存＋缓存读＋缓存写）而非非缓存 `input_tokens`——provider 分词器数的就是整份 prompt，缓存只改计费不改计数；拿非缓存 input 对整份字节才是单子点的"cache 命中致虚低"那个坑。真机轨迹对账证实：三轮主请求 total 8445→8933→9579，缓存链每轮读到的恰是上一轮总量。剔除护栏四道：实报≤0/字节 0/估算<2048（脚手架占比）；硬带 [0.2,5.0] 外弃；窗口中位 ±40% 异常带外弃；连续两枚偏离超中位 2 倍＝真漂移，清窗重起（loop 打一行诊断日志）。）

## 三、验收

- [x] 单测：喂定长请求+定 usage 的假后端，两轮后估算值收敛到校准比率；样本不足走兜底；异常样本剔除。
  （`tests/unit/agent/test_token_calibrator.cpp` 十册：兜底/中位收敛/滚动窗 8 对/坏账与小请求剔除/异常带剔除/漂移重置/分桶/落整规则/假后端两轮收敛（B 闸水位恰＝默认尺×2.0）/不接线零影响。）
- [x] 真机：跑几轮真会话，/context 显示校准行且 context 百分比与 provider 实报逐步贴近（记录前后对照）。
  （真机探针：临时 USERPROFILE 拷真 config，真 ccmoon 三轮对话＋/context 刮屏。默认尺虚高 52%（校正 ×0.66）：/context 预算总账等估算行按 ×0.66 收敛到实报口径（系统提示 1529/工具声明 7215 均为校准后值），占用百分比本就吃真实 usage，两本账就此对齐。）

## 四、不做什么

- 不引分词器/不做精确计数——平均水准够用，别背依赖
- 不改 usage 账本身（只读）
- 不跨模型共用比率
