# Hooks 生命周期与信任协议

状态：待做（扩展协议与安全边界）

## 结论

现行 LubanCode hooks 与 Claude Code、Codex 不一样。

它能做两件实事：工具执行前拦一下，工具执行后跑个命令。会话起落也能敲两下脚本。可它还只是一层早期 shell callback，不是一套完整 hooks 框架。

差距不只在“事件少”。更要紧的有四处：

1. 项目 hooks 无信任审查，启动便能以用户权限执行命令。
2. 项目 `hooks` 整段盖掉全局 `hooks`，团队审计与安全策略可被仓库配置顶走。
3. 输入靠环境变量，输出靠退出码；没有统一 JSON 协议、决策归并与上下文注入。
4. 没有 prompt、compact、stop、subagent、permission、async 等生命周期。

故此不能只往 `HooksConfig` 再塞几只 vector。须先立来源、协议、调度三根梁，再添事件。

## 现行实现实查

### 一、只有四类事件

`src/config/config.hpp` 里的 `HooksConfig` 只有：

```text
pre_tool
post_tool
session_start
session_end
```

`HookEntry` 也只有两项：

```text
matcher
command
```

没有 handler type、timeout、async、status message、失败策略、来源、信任状态或协议版本。

### 二、工具链次序

`src/agent/loop.cpp::RunOneTool()` 现行次序如下：

```text
on_tool_start
查 registry
查是否已挂载
pre_tool
用户确认
tool.execute
post_tool
清洗 UTF-8
on_tool_done
写回 history
```

这有几处含混：

- UI 先报“工具开始”，hook 后来才拦。用户会看见一只压根没执行的工具先亮起来。
- `pre_tool` 不能批准或改写输入，只能用非零退出码拦。
- `PermissionRequest` 没有独立边界。hook 也不知道宿主是否真要问用户。
- `post_tool` 先吃原始结果，UTF-8 清洗反在后面。
- 成功、失败都挤进 `post_tool`，只靠 `LUBAN_TOOL_IS_ERROR` 分辨。
- 未知工具、未挂载工具、流式 hosted tool 不走同一套覆盖表。

### 三、输入输出协议太薄

工具 hook 只收环境变量：

```text
LUBAN_TOOL_NAME
LUBAN_TOOL_INPUT
LUBAN_TOOL_RESULT
LUBAN_TOOL_IS_ERROR
```

`post_tool` 结果只给前 8192 字节。session hook 连这些也没有。

这条路有三桩毛病：

- Windows 环境块有尺寸与编码边界。大 JSON 塞进去，起进程可能失败。
- hook 不知道 `session_id`、`turn_id`、`tool_use_id`、cwd、权限档、transcript 路径、agent id。
- stdout 没有结构化语义。不能回 `allow`、`deny`、`ask`、`updatedInput`、`additionalContext` 或 `systemMessage`。

现行 `pre_tool` 把任何非零退出都当拦截。Claude Code、Codex 的兼容协议以 `0` 为成功、`2` 为阻断；其余退出多半只是 hook 自己坏了，不该误作策略拒绝。

### 四、执行与归并

现行实现逐条串行跑：

```text
第一条命中 -> 等它结束
第二条命中 -> 再等
第一条非零 -> 立刻返回，后面的不跑
```

单条固定等 30 秒。四条慢 hook 可把一次工具调用拖两分钟。

Claude Code、Codex 都把同一事件下匹配的 handlers 当一组处理。多个决策要归并；安全事件通常是 deny 胜。异步 handler 只做通知与记账，不得倒过来阻断已经发生的动作。

### 五、配置来源有安全洞

LubanCode 读两层：

```text
~/.lubancode/config.json
<cwd>/.lubancode/config.json
```

项目出现 `hooks` 后，整段压过全局。两层不相加。

更麻烦的是，项目 hook 不走 workspace trust，不核命令 hash，也不再问一次。用户只要进陌生仓库运行 LubanCode，仓库里的 `session_start` 就能起进程。

这不只是少个提醒。它是一条供应链执行口。

### 六、子代理只继承工具前后 hook

`src/app/turn_runner.cpp` 把父级 `on_pre_tool_hook`、`on_post_tool_hook` 转给 `AgentTool`。所以子代理内部工具大体仍会过两道门。

可它没有：

- `SubagentStart`；
- `SubagentStop`；
- agent id/type；
- 子代理 transcript 路径；
- 独立 stop/compact 事件；
- “这个 hook 在 main 还是子代理里触发”的可靠字段。

同一条 hook 看见 `read_file`，却分不清谁读的。

### 七、会话起落也很粗

`SessionHookScope` 在真正进入 CLI 会话前构造，退出作用域时析构：

```text
构造 -> session_start
析构 -> session_end
```

它没有 `startup/resume/clear/compact` 来源，也没有结束原因。`/clear`、恢复会话、compact 后续上下文，皆不能分别匹配。

崩溃、强杀、断电时，析构也管不着。这点须诚实写进协议，不能许诺“SessionEnd 必到”。

## 三家对照

资料核于 2026-08-15：

- Claude Code 官方 hooks reference：<https://code.claude.com/docs/en/hooks>
- Claude Code 官方 hooks guide：<https://code.claude.com/docs/en/hooks-guide>
- Codex 官方 Hooks：<https://learn.chatgpt.com/codex/hooks>
- Codex 官方配置参考：<https://developers.openai.com/codex/config-reference/>

两家都在快改。事件与字段以官方 release 文档为准，不拿博客与旧截图当契约。

| 项目 | LubanCode 现状 | Codex 现状 | Claude Code 现状 |
|---|---|---|---|
| 事件 | 4 类 | 11 类核心生命周期 | 官方页约 30 类，现页已列到 31 类 |
| 配置 | 全局/项目 JSON，项目整段覆盖 | `hooks.json`、TOML、plugin、managed，多源相加 | user/project/local/managed/plugin/skill/subagent，多源相加 |
| 项目信任 | 无 | 按 hook 定义 hash 审查；变更后重审 | workspace trust；managed 另管 |
| handler | shell command | 当前只执行 command；prompt/agent 暂只解析 | command、HTTP、MCP tool、prompt、agent |
| 输入 | 环境变量 | stdin JSON | stdin JSON 或 HTTP JSON |
| 输出 | 任意非零即拦 | 结构化 JSON + exit 2 | 结构化 JSON + exit 2 |
| matcher | `*` 或工具名精确相等 | regex；按事件匹配不同字段 | 精确集合、regex，并可用参数级 `if` |
| 多 handler | 串行；首个非零短路 | 同事件 command 并发，随后归并 | 同组处理并归并决策 |
| async | 无 | 有；结果在下一安全点送达 | 有；不能拿 async 做阻断 |
| 超时 | 固定 30 秒 | 每 handler 可设；多类有不同默认 | 每 handler 可设 |
| 大输出 | pre 只取前 5 行；post 丢 stdout | 超限 spill 到临时文件，模型只收首尾预览 | 有明确输出与展示协议 |
| UI | `/config` 只报数量 | `/hooks` 可看来源、审查、信任、禁用 | `/hooks` 可看事件、matcher、类型与来源 |
| 子代理 | 只转发 pre/post | Start/Stop；带 agent id/type/transcript | Start/Stop；工具 hooks 带 agent id/type |
| compact | 无 | PreCompact、PostCompact、compact 后 SessionStart | PreCompact、PostCompact、InstructionsLoaded |
| stop | 无 | Stop、SubagentStop 可要求再跑一轮 | Stop、SubagentStop、StopFailure |
| 权限 | pre hook 在确认前，只能拦 | PreToolUse + PermissionRequest | PreToolUse + PermissionRequest + PermissionDenied |

### Codex 核心事件

Codex 当前官方页列出这套共同骨架：

```text
SessionStart
SessionEnd
UserPromptSubmit
PreToolUse
PermissionRequest
PostToolUse
PreCompact
PostCompact
SubagentStart
SubagentStop
Stop
```

它很适合 LubanCode 第一阶段对齐。分量够用，又没有一口吞下三十类事件。

Codex 还有几条值得照办：

- 匹配的 command hooks 并发启动；一只 hook 不能阻止另一只已经启动。
- 项目 hooks 只在项目可信时加载。
- 非 managed hook 按定义 hash 审查。命令一改，信任失效。
- 异步结果只在下一安全点送入上下文。空闲时完成，不会擅自开启模型新回合。
- async 不能阻断、批准或改写触发它的动作。
- `PreToolUse` 能 deny，也能带 `updatedInput` 改写参数。
- `PermissionRequest` 多方决策时 deny 胜；无人表态便走原审批流程。
- 大段 `additionalContext` 会 spill，免得一只 hook 吞完整扇上下文。
- `SubagentStop`、`Stop` 带 `stop_hook_active`，免得继续钩子自咬成死循环。

### Claude Code 扩展面

Claude Code 在共同骨架外又添了一圈：

```text
Setup
UserPromptExpansion
PermissionDenied
PostToolUseFailure
PostToolBatch
Notification
MessageDisplay
TaskCreated
TaskCompleted
TeammateIdle
StopFailure
InstructionsLoaded
ConfigChange
CwdChanged
DirectoryAdded
FileChanged
WorktreeCreate
WorktreeRemove
Elicitation
ElicitationResult
```

这些并非都该首期照搬。LubanCode 有 agent queue、memory、worktree、compact 后，才有相应落点。没有真实生命周期，先造事件名，只会成空壳。

Claude Code 另有五类 handler：command、HTTP、MCP tool、prompt、agent。LubanCode 首期只做 command 足够；数据结构须给 handler type 留门，别又把 `command` 焊死在 `HookEntry` 上。

## 目标架构

### 一、先建统一事件总线

新增一套内核类型，名字可另定，语义不可散：

```text
HookEvent
HookSource
HookMatcherGroup
HookHandler
HookInvocation
HookDecision
HookRunRecord
HookDispatcher
```

业务层只发事件：

```text
dispatcher.Emit(PreToolUse, payload)
dispatcher.Emit(SubagentStop, payload)
```

业务层不得再亲自遍历 `config.hooks.pre_tool`、起进程、猜退出码。解析、匹配、信任、并发、超时、输出解析与决策归并全收进 dispatcher。

### 二、统一 stdin JSON

每只 command hook 从 stdin 收一份 JSON。公共字段至少有：

```json
{
  "schema_version": 2,
  "hook_event_name": "PreToolUse",
  "hook_run_id": "...",
  "session_id": "...",
  "turn_id": "...",
  "cwd": "...",
  "transcript_path": "...",
  "permission_mode": "confirm",
  "agent_id": null,
  "agent_type": null
}
```

工具事件再添：

```json
{
  "tool_name": "run_command",
  "tool_use_id": "toolu_...",
  "tool_input": {},
  "tool_response": {},
  "tool_succeeded": true
}
```

session、compact、stop、subagent 各加本事件字段。公共字段只定义一遍。

旧 `LUBAN_TOOL_*` 环境变量先保留一版作兼容，文档标 deprecated。新能力只走 stdin JSON。

### 三、统一 JSON 输出

command hook 可在 stdout 返回：

```json
{
  "continue": true,
  "stopReason": null,
  "systemMessage": null,
  "hookSpecificOutput": {
    "hookEventName": "PreToolUse",
    "permissionDecision": "deny",
    "permissionDecisionReason": "命令会删工作区",
    "updatedInput": null,
    "additionalContext": null
  }
}
```

不是每个事件都认每个字段。须做逐事件 schema。字段用错，报 hook failure；不可悄悄吞掉，也不可因解析失败反把危险工具放过去而不留痕。

退出码定清：

```text
0  成功；若 stdout 是 JSON，照事件 schema 解
2  阻断；stderr 作阻断理由
其他  hook 自己失败；照 failure_policy 处理
```

旧配置仍按“任意非零拦截”跑一段迁移期。只有新 schema 才切到 `2` 阻断，免得老用户的策略一夜失效。

### 四、逐事件归并决策

不能写一只万能 `if (exit != 0) block`。

```text
PreToolUse
  deny > ask > allow/no-decision
  updatedInput 只能与 allow 同返
  改写后重新过工具 schema、deny_commands 与权限规则

PermissionRequest
  任一 deny -> 拒绝
  无 deny 且至少一只 allow -> 不弹确认
  全无决定 -> 正常问用户

PostToolUse
  不能撤销副作用
  可给模型 additionalContext / feedback

Stop / SubagentStop
  block 的意思是“还不能停，再续一轮”
  stop_hook_active=true 时须防无限续跑

PreCompact
  可阻断 compact

SessionEnd
  只作收尾与审计，不得把会话重新拉起
```

同组 handlers 可并发起。全部收齐后再按固定法归并。日志顺序按配置来源与声明次序排，不能按谁先跑完谁先说话。

### 五、同步与异步分家

同步 handler 才能：

```text
deny
allow
ask
updatedInput
continue=false
```

异步 handler 只许：

```text
审计
通知
产出 additionalContext
产出 systemMessage
```

异步完成后的投递照安全点走：

```text
当前模型请求/工具批次仍在跑 -> 等本批收束，下一次模型请求前送达
当前会话空闲             -> 等下一条用户消息，不自行唤起模型
会话结束                 -> 取消未完成 hook，未送达输出作废并记日志
```

这条可与 agent inbox 共用“安全点投递”抽象。不可另起一套 next-tool-call 私账。

### 六、大输出 spill

hook 输出默认只给模型约 2500 tokens。再长便：

1. 全文落进会话临时目录。
2. 模型只收头尾预览与 artifact 路径。
3. UI 显示“hook 输出已落盘”，可展开查看。
4. 会话清理时按保留策略收拾。

不能继续把 post result 生硬截到 8192 字节，又不告诉 hook 或模型截过。

## 信任与来源

### 一、来源须能追

每条 handler 带：

```text
source_kind: managed | user | project | local | plugin | skill | subagent
source_path
source_order
definition_hash
managed
enabled
trusted
```

同源重复定义可去重。不同来源纵然命令一样，也先保留来源账，再按稳定 hash 决定是否只跑一次。

### 二、hooks 改成相加，不再整段覆盖

全局审计 hook、项目格式 hook 可以一起跑。项目配置不能抹去用户或管理员策略。

优先级只管决策冲突，不管“谁覆盖谁”：

```text
managed deny 永远有效
任一可信 deny 胜
project 不能删除 user/managed hook
local 只能禁自己有权管的非 managed hook
```

这是 breaking change。启动时若侦测到全局、项目两边都有旧 `hooks`，须打印一次迁移说明。

### 三、项目 hook 先审后跑

首期照 Codex 的窄做法：

1. 列出 hook 来源、事件、matcher、完整命令、timeout、async。
2. 用户按 definition hash 信任。
3. 命令、参数、脚本路径、handler 类型一改，hash 改，信任失效。
4. 未信任项目 hook 跳过，并在启动与 `/hooks` 里报明。
5. managed hook 由策略信任，普通用户不能在 UI 里关。

信任记录放用户目录，不能写回仓库。

项目 hook 即使未信任，也不得拖累全局可信 hook。

### 四、命令最好走 exec form

保留旧 shell 字符串：

```json
{"type":"command","command":"python check.py"}
```

新增不经 shell 的 exec form：

```json
{
  "type": "command",
  "command": "python",
  "args": [".lubancode/hooks/check.py"],
  "command_windows": "py",
  "args_windows": ["-3", ".lubancode/hooks/check.py"]
}
```

路径占位至少有：

```text
${LUBANCODE_PROJECT_DIR}
${LUBANCODE_PLUGIN_ROOT}
${LUBANCODE_PLUGIN_DATA}
```

exec form 避开 shell 二次解析，也免去 Windows 引号泥潭。文档把它列为首选。

## 第一阶段事件

先对齐 Codex 那套共同骨架：

### SessionStart

`source` 至少有：

```text
startup
resume
clear
compact
```

plain stdout 或 `additionalContext` 可进 developer context。自动 compact 后，须在紧接着的续请求前送达，不能拖到下一条用户消息。

### SessionEnd

带 `reason`、session id、transcript path、cwd。同步收尾，设短 timeout。它只作 advisory；进程被硬杀时不保证触发。

### UserPromptSubmit

在用户 prompt 送模型前触发。可追加 developer context，也可阻断。原 prompt 与注入文本分字段，不能串成一坨后再猜边界。

### PreToolUse

在 UI 标记“真执行”前、权限确认前触发。可 deny、allow、ask、改写 input、追加 context。

建议把 UI 状态拆成：

```text
requested -> checking_hook -> waiting_permission -> running -> done
```

被 hook 拦住时停在 `blocked`，不冒充“运行过又失败”。

### PermissionRequest

只有宿主本来要问用户时才触发。能 allow、deny，或不表态。改写后的 input 必须重新跑 deny rule，不能借 hook 绕权限。

### PostToolUse

在工具结果已清洗、已结构化后触发。副作用已发生，不能回滚。可把审查反馈替换成模型所见 tool result，原始结果仍进审计 artifact。

首期可继续用 `tool_succeeded` 区分成败。第二阶段再拆 `PostToolUseFailure`。

### PreCompact / PostCompact

带 `trigger=manual|auto`。Pre 可备份与阻断，Post 可审计。compact 后重新注入上下文，走 `SessionStart(source=compact)`，不要靠 PostCompact 硬塞。

### SubagentStart / SubagentStop

带：

```text
agent_id
agent_type
parent_agent_id
agent_transcript_path
last_assistant_message
stop_hook_active
```

SubagentStop 可要求代理再做一次聚焦收口。最多续一次或受独立预算约束，免得 stop hook 与轮次上限互咬。

### Stop

主回合准备停时触发。带 `last_assistant_message` 与 `stop_hook_active`。可要求补跑测试、补写总结。继续理由作为新 continuation prompt 入账，不能伪装成真实用户输入。

## 第二阶段事件

按 LubanCode 真能力补，不求一次吞完：

```text
PostToolUseFailure  工具起不来、超时、协议错
PostToolBatch       一批并行工具全部收束
StopFailure         API/鉴权/限流/最大输出导致回合失败
Notification        权限、空闲、后台完成
TaskCreated         agent task 建账前
TaskCompleted       agent task 销账前
ConfigChange        会话中配置热刷新
InstructionsLoaded  AGENTS.md/规则/skill 指令进上下文
WorktreeCreate      隔离工作树创建
WorktreeRemove      隔离工作树收拾
```

`MessageDisplay`、HTTP、MCP handler、prompt/agent handler、文件监听等，等第一阶段协议跑稳再议。

## 配置草案

新格式尽量贴 Claude Code、Codex 的共同形状，少造方言：

```json
{
  "hooks": {
    "schema_version": 2,
    "PreToolUse": [
      {
        "matcher": "run_command|write_file",
        "hooks": [
          {
            "type": "command",
            "command": "python",
            "args": ["${LUBANCODE_PROJECT_DIR}/.lubancode/hooks/policy.py"],
            "timeout": 20,
            "statusMessage": "检查工具策略"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "write_file",
        "hooks": [
          {
            "type": "command",
            "command": "python",
            "args": ["${LUBANCODE_PROJECT_DIR}/.lubancode/hooks/format.py"],
            "async": true,
            "timeout": 60
          }
        ]
      }
    ]
  }
}
```

matcher 先支持：

```text
省略 / *         全匹配
纯工具名          精确匹配
name1|name2       精确集合
显式 regex 字段   正则匹配
```

不要把“字符串里恰好有特殊字符”偷偷解释成 regex。显式 `matcher_type` 或独立 `regex` 字段更稳，能少踩版本兼容坑。

## 兼容迁移

旧格式继续认：

```text
pre_tool      -> PreToolUse legacy adapter
post_tool     -> PostToolUse legacy adapter
session_start -> SessionStart matcher=*
session_end   -> SessionEnd matcher=*
```

旧 adapter 守旧语义：

- 任意非零仍拦 `pre_tool`。
- 继续导出 `LUBAN_TOOL_*`。
- shell 字符串照跑。
- 固定 30 秒先不变。

同时启动时告知：旧协议已废弃，将来迁到 schema 2。`/hooks` 须能标出 `[legacy]`。

不可暗改旧退出码语义。安全 hook 最怕“升级后看似还在，实则放行”。

## `/hooks` 管理面

首期做一只只读浏览器，至少能看：

```text
事件
matcher
handler type
命令与参数
来源文件
managed / trusted / pending review
definition hash 短码
sync / async
timeout
最近一次结果、耗时与错误
```

可做动作：

```text
信任当前 hash
撤销信任
禁用一只非 managed hook
重新加载配置
查看最近运行记录
```

不必首期在 TUI 里编辑整份 JSON。看得见、审得过、关得掉，先把账立住。

## 失败策略

每只 handler 可设：

```text
failure_policy: warn | deny
```

默认值按事件分：

```text
PreToolUse / PermissionRequest 的 managed policy -> deny
普通项目策略                               -> warn，UI 明报
Post/Session/Notification                   -> warn
```

超时、spawn 失败、JSON 坏、schema 不合，都生成 `HookRunRecord`。不得只往 `std::cerr` 丢一行，滚过去便无处找。

严禁把 timeout 静默当放行。哪怕策略取 warn，也须让用户与日志看见“这次门卫没起来”。

## 测试

### 配置与信任

- user + project hooks 同时加载，不再互相覆盖。
- managed hook 不能被 project/local 禁用。
- 未信任项目 hook 不起进程；user hook 照跑。
- 命令、args、timeout、async 任一改变，definition hash 变化并要求重审。
- repo 移路径后，来源与信任策略照明确规则处理。
- 旧格式解析与旧退出码语义不退步。

### 决策矩阵

- 多只 PreToolUse 同时返 allow/ask/deny，deny 胜。
- 多只 PermissionRequest 无人决定，仍弹用户确认。
- updatedInput 改写后重新过 schema、deny_commands 与权限规则。
- PostToolUse block 不许冒充“撤销了工具副作用”。
- Stop 续跑一次后 `stop_hook_active=true`，不能无限续。
- exit 0/2/1、坏 JSON、空 stdout 各走对路。

### 进程与数据

- stdin 可传超过 Windows 环境块上限的大 tool input。
- UTF-8 坏字节先清洗，再序列化给 hook。
- exec form 的空格、中文路径、引号、反斜杠在 Windows/POSIX 都不串。
- timeout 杀整棵进程树，不留孙进程。
- 多同步 handler 并发，总耗时接近最慢一只，不是全数相加。
- 决策与日志顺序稳定，不受完成先后影响。
- 大 additionalContext 会 spill，预览标明截取与文件路径。

### 生命周期

- startup/resume/clear/compact 四种 SessionStart 各能匹配。
- 自动 compact 中途触发后，追加上下文进入紧接着的续请求。
- SessionEnd 带 reason；硬杀路径明确不作必达断言。
- 主代理与子代理调用同名工具时，hook input 的 agent id/type 分得开。
- SubagentStop 带 transcript path 与末条 assistant 文本。
- UserPromptSubmit block 后，prompt 不发模型。
- PermissionRequest 只在本来需要审批时触发。

### 异步安全点

- 活跃回合中完成：等当前工具批次收口，下一模型请求前送达。
- 空闲时完成：不唤醒模型，下一用户回合再送。
- 会话结束：取消未完成 hook，未送达输出记作 discarded。
- async handler 返回 deny/updatedInput 时，报字段无效，不影响已发生动作。

### 覆盖表

为每类工具写明并测试：

```text
内置本地工具
run_command / 统一 exec
apply_patch / 文件写入
MCP
Lua
C ABI plugin
agent
hosted web/search
transport poll
```

不能笼统写“所有工具都支持”。不走本地 function-tool path 的，文档须明说例外。

## 实施次序

1. 抽 `HookDispatcher`、统一事件与 stdin/stdout 协议；旧四类接 legacy adapter。
2. 做来源相加、hash 信任、`/hooks` 浏览；先堵项目任意执行口。
3. 接 PreToolUse、PermissionRequest、PostToolUse，改正工具状态机与决策归并。
4. 接 UserPromptSubmit、Stop、SessionStart/End。
5. 接 Pre/PostCompact 与 SubagentStart/Stop。
6. 加 async、安全点投递、spill 与运行记录。
7. 按真实产品能力添第二阶段事件。

第一、二步可以先发。没堵信任洞前，不宜再扩项目 hooks 权能。

## 不做

- 不为追求数字，一口气抄齐 Claude Code 三十余类事件。
- 不把 Codex、Claude Code 暂未实现的字段冒充 LubanCode 已支持。
- 不让 async hook 做权限决定。
- 不拿项目配置覆盖全局策略。
- 不把未知退出码一概当 deny，也不把 hook 崩溃悄悄当成功。
- 不只靠 `std::cerr` 记失败。
- 不把大 JSON 继续塞环境变量。
- 不给 shell 字符串拼用户输入；改写参数走结构化 `updatedInput`。
- 不许 hook 绕过宿主工具 schema、deny rule、sandbox 或确认档。
- 不承诺进程被硬杀时 `SessionEnd` 必达。

## 销单线

以下十四条齐了，方可销单：

1. 项目 hooks 未信任时绝不起进程，定义一改便须重审。
2. user、project、managed、plugin hooks 相加加载；项目不能顶掉全局策略。
3. `/hooks` 看得见来源、命令、信任、状态、最近结果，并能撤信与禁用非 managed hook。
4. 新协议统一用 stdin JSON 与结构化 stdout JSON；旧环境变量只作迁移兼容。
5. exit 0、exit 2、普通失败各有清楚语义，逐事件 schema 会校验错误字段。
6. 多 handler 可并发执行，决策按固定法归并，deny 优先，日志顺序稳定。
7. PreToolUse、PermissionRequest、PostToolUse 与 UI 工具状态机次序正确。
8. updatedInput 改写后重新过 schema、deny rules 与权限判断，不能借 hook 越权。
9. SessionStart/End、UserPromptSubmit、Stop、Pre/PostCompact 全部接入真实生命周期。
10. SubagentStart/Stop 带 agent id/type/transcript，主代理与子代理 hook 不串账。
11. async hook 只在安全点送信息，不阻断旧动作，不私自唤起空闲模型。
12. 大输出会 spill；UTF-8、Windows 大输入、路径引号与超时杀树都有回归。
13. 旧四类配置继续能跑，并有明确迁移提示；升级不让旧安全策略静默失效。
14. 文档给出工具覆盖矩阵、信任边界、失败策略与 `SessionEnd` 非必达说明。
