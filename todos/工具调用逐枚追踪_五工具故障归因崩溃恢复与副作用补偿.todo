# 工具调用逐枚追踪：五工具故障归因、崩溃恢复与副作用补偿

状态：待做（2026-08-23 源码巡检；复用现有 `tool_use_id`、`ToolPhase`、
Runtime item 事件与 session JSONL，不另开旁路）

## 一句话

同一 step 来五枚工具，LubanCode 已能按 `tool_use_id` 配请求与结果，也按出现
次序逐枚执行。正常收口时，哪枚 `is_error=true`，一眼便认得。可若第三枚跑到
一半宿主崩了，或五枚都报成功、仓库却从第三枚起走偏，现有账便不够用了。

本单要补一部逐枚工具账。工具何时收到、何时越过权限闸、何时真正踏进副作用
边界、何时拿到原始结果、何时写回 history，都以 append-only 事件落进 session。
恢复时据此判“没跑”“跑完”“结果可恢复”“结局未知”。能重放的才重放，能补偿
的才补偿。任意 shell、MCP 远端写入、原生插件一概不靠猜。

## 先答面试官

面试官问：“模型同拍叫了五个工具，只知道出错，怎样排除是哪一个？有没有回溯？”

现版答案：

```text
assistant: ToolUse(a), ToolUse(b), ToolUse(c), ToolUse(d), ToolUse(e)
user:      ToolResult(a), ToolResult(b), ToolResult(c), ToolResult(d), ToolResult(e)
```

- 每枚调用有独立 `tool_use_id`。
- 五枚按出现次序执行，不并发。
- 每份结果各带 `is_error` 与正文。
- session JSONL 留原始调用块与结果块。
- `/resume` 会删除孤儿 result，并给缺 result 的 use 补
  `[会话恢复:结果缺失]`，保住 Provider 配对协议。

这叫**结果回溯**。它能指出显式失败，不能证明副作用边界，也不能撤销外部状态。

本单完成后的答案：

> 用 `execution_id/tool_use_id` 串起逐枚生命周期。先找最早的稳定失败，再沿
> `blocked_by/retry_of/parent_execution_id` 看传播。崩溃时，`started` 后无
> `finished` 的调用标 `unknown`，绝不自动重跑。语义错误靠 postcondition 与
> 状态指纹找“从哪一步开始偏”，不能拿最后一枚工具当替罪羊。回滚按 effect
> class 分级：只读无须回滚，本地文件可做条件式撤销，shell 与远端写入须有
> 工具自带 compensator；没有补偿协议便只能告警、核验、人工处置。

## 范围

本单覆盖：

- JSON tool calling 与 PTC 共用的 `RunOneTool`；
- 同一 assistant message 中的多枚工具；
- builtin、MCP、LSP、Lua、native plugin、agent、PTC stub；
- PreToolUse、schema 改参、PermissionRequest、PostToolUse；
- session JSONL、Runtime ServerEvent、WorkflowRecorder 与 artifact；
- 正常失败、取消、崩溃、结果丢失、语义偏差、重试与补偿。

本单不承诺：

- 给任意 shell 造通用事务；
- 自动撤销已经发到远端的动作；
- 只凭日志就证明业务因果；
- 默认保存 Provider 原始 SSE、密钥、完整环境或无限工具输出。

## 当前底子

### 已有

| 能力 | 现状 |
| --- | --- |
| 调用身份 | `ToolUseBlock.id`，结果用 `tool_use_id` 配对 |
| 多工具次序 | 同一 step 按内容块次序逐枚执行 |
| 统一执行链 | JSON 与 PTC 都进 `RunOneTool` |
| 运行时相位 | `CheckingHook / WaitingPermission / Blocked / Running` 已发给 UI |
| 统一收尾 | `on_tool_done` 各出口都会收到 |
| session | assistant tool_use 与 user tool_result 落 JSONL |
| 配对修补 | `RepairToolPairs` 补缺失结果、删孤儿结果 |
| Hook 诊断 | `/hooks runs` 有 outcome、exit、duration、stderr 摘要 |
| MCP 内层配对 | JSON-RPC 数字 id + pending map，超时后迟到响应丢弃 |
| artifact | 大结果可按 `tool_use_id` 卸载，带 sha256 与引用 |
| Runtime 合同 | thread / turn / item 三层 id、seq、ItemStarted/Completed |

### 欠账

- session 只在一轮收口后 `PersistNewMessages`。第三枚工具里崩溃时，前两枚结果
  可能只在内存，尚未进 session。
- UI 相位是瞬时回调。重启后无账可查。
- `Tool::Result` 只有 `content + is_error`。超时、spawn、schema、拒绝、输出
  超限常靠中文正文分辨。
- session 没有 execution id、turn id、step、批次序号、工具来源、耗时、
  attempt、状态指纹与副作用等级。
- `RepairToolPairs` 只补协议。它不知道工具没开始、已成功，还是已经改完外部
  状态却没来得及写结果。
- WorkflowRecorder 只记 tool name，不记 tool id。连续五次同名工具无法严谨
  配对；它又是可选录制件，不能当会话审计真本。
- Hook 最近运行账只活进程内。重启后 `/hooks runs` 不能解释旧 session。
- MCP session 只看见外层 tool result。内层 JSON-RPC id、server、transport
  终态没有稳定关联字段。
- `is_error=false` 只说明工具自报成功，不说明 postcondition 成立。

## 定义先钉死

### 回溯、恢复、重试、回滚不是一回事

| 名称 | 含义 | 本单目标 |
| --- | --- | --- |
| 回溯 trace | 查一枚调用走过哪些阶段、在哪儿停 | 要做 |
| 归因 attribution | 找最早的明确失败或状态偏差 | 做保守归因，不冒充因果证明 |
| 恢复 recovery | 崩溃后重建协议历史与调用终态 | 要做 |
| 重试 retry | 再执行一枚新调用 | 只显式做，新 id，不覆盖旧账 |
| 回滚 rollback | 把状态恢复到调用前 | 只做能证明安全的本地补偿 |
| 补偿 compensation | 另作反向业务动作 | 由工具声明，失败也须留账 |
| 重放 replay | 在隔离环境复跑，用来复现或二分 | 只限确定性、可隔离调用 |

### 四种恢复结论

恢复时只准落进四档：

1. `not_started`：尚未越过执行边界，可确定没有调用 `Tool::execute`。
2. `finished`：已有稳定 outcome 与可校验结果。
3. `result_recoverable`：执行已结束，结果落在 trace/artifact，尚未写成
   `ToolResultBlock`；可补结果，不重跑工具。
4. `unknown_after_start`：见过 durable `execution_started`，没见 durable
   `execution_finished`。副作用未知，不自动重跑，不冒充失败或成功。

“看不到 started”不等于永远没跑。只有 started 事件在副作用边界**之前同步落盘
成功**，这份否定证据才站得住。

## 身份模型

只拿 Provider 给的 `tool_use_id` 不够。不同 turn、PTC 或不同 Provider 可能重号。
要分三枚 id：

```text
execution_id      宿主发号，session 内唯一；审计主键
tool_use_id       Provider/PTC 调用 id；只管 wire 配对
item_id           Runtime UI 条目 id；前端路由
```

建议 `execution_id` 直接复用 Runtime 的 tool item id。若 Runtime 尚未接到
AgentLoop，过渡期可由同一 `IdAuthority` 发号，接线后删过渡口。不可再造第二只
计数器。

每枚工具事件至少带：

```text
schema_version
session_id / thread_id
turn_id
step_id 或 step_index
provider_request_id（若有）
batch_id                 # 同一 assistant message
sequence_in_batch        # 0..N-1
execution_id
item_id
tool_use_id
tool_name
tool_source              # builtin/mcp/lsp/plugin-lua/plugin-native/agent/ptc
source_instance          # MCP server、plugin id 等；可空
parent_execution_id      # 子代理/PTC 内层调用归属；可空
timestamp_ms
seq
```

`tool_use_id` 仍写进 tool result。`execution_id` 不发给 Provider，免得污染 wire。

## 工具来源不能靠 RTTI 猜

`ToolRegistry` 眼下主要收 `unique_ptr<Tool>`。诊断层若用 dynamic_cast 猜 MCP、
Lua、Plugin，日后每加一种 runtime 都要漏一次。

引入注册元数据：

```text
ToolRegistration
  tool
  source_kind
  source_instance
  version_or_digest
  effect_class
  idempotency
  recovery_capabilities
```

- builtin 注册时明写 `builtin`。
- MCP 写 server name 与远端 tool name。
- Lua/native plugin 写 plugin id、runtime 与文件 digest；不默认写绝对路径。
- LSP 写 server/language。
- agent/PTC 写 parent execution。
- 延迟工具 wrapper 透传 metadata，不把来源洗成 `deferred`。

## 生命周期事件

不把每一拍都 fsync。运行时可以细，持久账只在能改变恢复结论的关口落。

### 运行时细相位

沿用 `ToolPhase`，补齐稳定枚举：

```text
received
resolving
checking_hook
validating_updated_input
waiting_permission
ready
running
post_hook
committing_result
completed / blocked / declined / cancelled
```

这些相位经 Runtime item event 发给 TUI/Web/app-server。它们管画面与在线诊断，
不全都承担崩溃恢复。

### 持久恢复栅栏

session JSONL 新增一种版本化事件：`tool_trace_v1`。只落四类关键栅栏：

```json
{"type":"tool_trace_v1","event":"scheduled","execution_id":"item-17",
 "tool_use_id":"call_c","sequence_in_batch":2,"tool_name":"run_command"}

{"type":"tool_trace_v1","event":"execution_started","execution_id":"item-17",
 "effective_input_sha256":"...","effect_class":"unknown"}

{"type":"tool_trace_v1","event":"execution_finished","execution_id":"item-17",
 "outcome":"exit_nonzero","error_code":"process.exit_nonzero","exit_code":1,
 "result_ref":{"kind":"inline","sha256":"...","bytes":91}}

{"type":"tool_trace_v1","event":"result_committed","execution_id":"item-17"}
```

规矩：

- `scheduled` 在 assistant tool-use message 落盘之后写。
- `execution_started` 在 `Tool::execute` 前写并 flush。写不成时，副作用工具不
  得继续执行；只读工具可按明确降级策略执行，但须当场告警。
- `execution_finished` 在拿到原始工具结果并完成 UTF-8 规范化后写。PostToolUse
  之前另存 original outcome，免得 Hook 崩溃抹掉工具已完成事实。
- `result_committed` 在对应 `ToolResultBlock` 确实落 session 后写。
- Hook/权限拒绝不写 `execution_started`，直接以 terminal outcome 收 scheduled。
- 未轮到便因 ESC 取消的工具记 `cancelled_before_start`。
- 同一 execution 的相同 event 幂等；重放遇重复行取第一枚合法栅栏，冲突行
  报 corruption，不取“最后一条随便赢”。

## 消息落盘次序要改

现版一轮结束才把 history 新消息逐条落盘。五枚工具跑到第三枚崩溃，会把审计
窗口开得太大。

改成：

```text
1. Provider assistant message 组装完成
2. assistant message append + flush
3. 为五枚 tool use 各写 scheduled
4. 逐枚执行：started -> finished
5. 五枚结果收齐
6. 合并成一条 role=user 的 ToolResult message，append + flush
7. 各写 result_committed
8. 下一 step
```

Wire 语义不变：五枚结果仍放同一条 user message。只是审计按枚及时落。

若第三枚崩溃，resume 用 trace 重建这一批：

- a、b 有 finished：从 result ref 恢复原结果。
- c 有 started 无 finished：补 `unknown_after_start` 错误结果。
- d、e 只有 scheduled：补 `not_started_due_to_crash` 错误结果。
- 五枚仍组成一条配齐的 user message，再喂 Provider。

`RepairToolPairs` 从“统一补结果缺失”升级为 trace-aware repair。旧 session 没
trace 时继续走老逻辑，向后兼容。

## 终态与错误码

`is_error` 留作 Provider/UI 的粗投影。诊断另用稳定 outcome 与 error code。

### Outcome

```text
succeeded
tool_error
unknown_tool
unavailable
schema_rejected
hook_denied
permission_declined
cancelled_before_start
cancelled_during_run
spawn_failed
timed_out
output_limit
transport_error
protocol_error
process_exit_nonzero
plugin_exception
host_error
unknown_after_start
result_store_failed
```

### Error code

用分层字符串，不用中文正文作机器判断：

```text
registry.unknown_tool
registry.not_mounted
hook.pre.denied
hook.updated_input.invalid
permission.declined
process.spawn_failed
process.timeout
process.exit_nonzero
mcp.transport_closed
mcp.timeout
mcp.jsonrpc_error
mcp.response_schema_invalid
plugin.lua_error
plugin.native_exception_boundary
session.trace_append_failed
session.result_commit_failed
```

人话放 `fallback_message`。结构详情放 `details`。UI 可翻译，测试只钉 code。

## `Tool::Result` 要扩，但不打坏插件 ABI

C++ 内部结果升级为：

```text
ToolResult
  content
  is_error
  outcome
  error_code
  details
  effect_summary
  result_artifact
```

迁移办法：

- 保留 `{content, is_error}` 便利构造，旧 builtin 先照常编译。
- adapter 逐个把 MCP/process/Lua/native 的已知失败翻成稳定码。
- native C ABI v1 不添 struct 字段。宿主 wrapper 先把 `is_error` 投影成
  `tool_error/plugin_error`；等 ABI v2 再让插件明报细码。
- Provider wire 仍只发它支持的字段；中立层与 session 保留完整诊断。

## 五工具怎样归因

### 显式失败

按 `sequence_in_batch` 排一张表：

```text
#0 read_file    succeeded
#1 search       succeeded
#2 edit_file    schema_rejected
#3 run_command  not_started_due_to_dependency?  # 只有真有依赖边才这样写
#4 web_fetch    succeeded
```

默认只说“#2 是最早明确失败”。不能自动宣称 #2 是最终业务失败的根因。

### 失败传播

只记录有证据的边：

```text
parent_execution_id   子代理/PTC 包含关系
retry_of              显式重试关系
blocked_by            宿主因前置失败明确跳过
compensates           补偿哪枚调用
```

同一 assistant message 的五枚工具并不天然互相依赖。模型同拍发出它们时，看不见
前一枚结果。宿主当前即便顺序执行，也不能凭顺序臆造 `depends_on`。

### 语义失败

五枚都 `succeeded`，最终构建仍坏。这时 `is_error` 排不出元凶。要靠验证点：

- 文件工具：记录改动路径、preimage/postimage sha256、字节数，不默认存第二份
  正文。
- 命令：记录 cwd、命令 digest、exit、输出 digest、进程终态。
- MCP/Plugin：记录服务/插件、远端资源 id（须脱敏）、返回 digest。
- 工作流关键点：显式 postcondition，如重读文件、跑测试、查端口、回查远端 id。

归因器只报：

```text
last_verified_good = execution b
first_observed_bad = verification after execution d
suspect_window = [c, d]
```

若 c、d 可在隔离环境确定性重放，才可二分。否则把窗口交给模型和用户，不编一
个确定答案。

## Effect class 与恢复策略

每个工具注册时声明保守 effect class：

| effect class | 例子 | 崩溃后默认动作 |
| --- | --- | --- |
| `read_only_local` | read_file、search | 可建议重试 |
| `read_only_remote` | web_fetch、只读 MCP | 不自动重试；考虑费用/限流 |
| `local_reversible` | write_file、edit_file | 查 undo token 与哈希，再询问是否撤销 |
| `local_process_unknown` | run_command | unknown，先核验进程与文件状态 |
| `remote_idempotent` | 带 idempotency key 的 API | 按服务查询原 key，不直接重发 |
| `remote_compensatable` | 支持 delete/cancel 的创建操作 | 可提补偿，另作一枚可见工具调用 |
| `remote_irreversible` | 发信、付款、发布 | 只告警与人工核验 |
| `in_process_unknown` | native/Lua plugin 未声明 | 按未知副作用处理 |

声明不能放宽安全：未声明按最危险档。插件 manifest、MCP metadata 即便声称
read-only，也只影响恢复建议，不越过权限确认。

## 本地文件条件式撤销

可逆不等于随时 `checkout --`。

为 `write_file/edit_file` 产一枚 undo token：

```text
path
preimage_sha256 / preimage_artifact
postimage_sha256
created_new_file
execution_id
```

撤销前重读目标：

- 当前 sha256 等于 postimage，说明其后没人再改，可恢复 preimage。
- 当前 sha256 已变，拒绝自动撤销，产三方 diff 交用户。
- 新建文件只在内容仍等于 postimage 时可移走。
- undo 本身也是工具调用，须确认、留 `compensates` 关系、失败留账。

隔离 worktree 可把 Git diff/commit 当更大粒度检查点。主 checkout 有用户未提交
改动时，绝不拿 Git 命令粗暴回滚。

## run_command、MCP、Plugin 的边界

### run_command

- arbitrary shell 默认 `local_process_unknown`。
- timeout/取消须收进程树，但“进程死了”不等于副作用撤销。
- trace 关联 platform `ProcessOutcome`，复用 shell 生命周期待办，不重复定义。
- crash 后先查原生进程身份、cwd 状态、日志与 postcondition。只凭 PID 不杀。
- 不透明自动重试。

### MCP

外层 execution 要挂内层：

```text
mcp_server
mcp_tool
jsonrpc_request_id
transport_generation
request_started / response_received
stderr_tail_digest
```

- `Client::SendRequestAndWait` 需给 tracing callback 或返回 request metadata。
- 超时删 pending 后，迟到响应另记 `late_response_dropped`，关联原 execution。
- 不默认落完整 params/result；session 已有规范化 tool input/result，trace 只放
  digest 与 bounded/redacted preview。
- server 进程退出、JSON-RPC error、result schema 错分开记。
- 是否可重试由 MCP tool 显式 capability 决定；协议没声明便按远端未知副作用。

### Lua/native plugin

- 记 plugin id、runtime、binary/script digest 与加载代次。
- Lua compile/load/runtime error 分开。
- native ABI 能回的只有 v1 `is_error` 时，不伪造 exception 细节。
- 原生崩溃若掀翻宿主，只能由下次启动从 `execution_started` 判 unknown。将来
  plugin-host 子进程可把 crash 收成 `plugin_host_crashed`。
- 插件提供 compensator 时，manifest 要声明输入映射与版本；宿主不执行任意
  字符串“撤销脚本”。

### agent 与 PTC

- 父 agent 工具调用是一枚 execution，子代理内部每枚工具填
  `parent_execution_id`。
- PTC stub 的合成 tool id 仍须映射宿主 execution id。
- 后台子代理事件通过只读 session trace sink 投递，主线程按 seq 收编；不许多
  线程直接往同一 JSONL 交错写。
- 子代理取消时，未开始工具与运行中工具分开记。

## Hook 账怎样并进来

不把 `/hooks runs` 全文复制五遍。工具 trace 只记引用：

```text
pre_hook_run_ids
permission_hook_run_ids
post_hook_run_ids
merged_decision
updated_input_sha256
```

HookRunRecord 增 `tool_execution_id`。会话退出前，将与本 session 有关的简版
record 作为独立 `hook_trace_v1` 落盘；stderr 只留编码、截断标记、digest 与短
预览，大段进 artifact。

恢复时若工具已 finished、PostToolUse 未收口：

- 不重跑工具。
- 不自动重跑事后 Hook，除非该 Hook 明示 idempotent。
- 用 original result 补 ToolResult，再加“PostToolUse 状态未知”的宿主附注。

## WorkflowRecorder 只作派生账

录制件不是审计真本。它可暂停、取消，也会主动摘要与脱敏。

改签名：

```text
RecordToolCall(execution_id, tool_use_id, tool_name, input)
RecordToolResult(execution_id, outcome, error_code, summary)
```

- 同名五连可配对。
- 从 canonical tool trace 派生，不由 TurnRunner 再手打一遍事件。
- 录制暂停不影响 session trace。
- 起草 Skill 仍只吃脱敏摘要，不吃原始审计内容。

## 一份事件，两路消费

不能让 AgentLoop 同时手调 UI callback、SessionStore、WorkflowRecorder 三份写口。
迟早次序分叉。

定案：

```text
RunOneTool / TurnRuntime
  -> canonical ToolLifecycleEvent
       -> Runtime EventSink（TUI/Web/app-server）
       -> SessionTraceSink（append-only JSONL）
       -> Workflow projection（仅录制开启时）
       -> Hook correlation
```

- 领域事件不带 ANSI，不预先翻中文。
- `seq` 由 Runtime 唯一发号。
- sink 各自失败，各自报稳定错误；UI 失败不拦工具，durable started 写失败则按
  effect class 决定是否拦执行。
- 终态唯一。迟到、重复终态不覆盖原账，只记 protocol violation。

这条要与“统一 SessionRuntime 接 TUI/Web/Tauri”待办合并实施。那里已经定了
ServerEvent 合同与稳定 id；本单只补工具领域 payload、持久 trace 与恢复状态机。

## JSONL 格式与兼容

### 为什么仍放 session JSONL

- 调用、恢复、compact、本轮模型历史本就以 session 为边界。
- append-only 适合崩溃现场。
- 老版本看见未知顶层 `type` 会跳过，不毁消息账。
- `/resume`、`/export`、未来 app-server replay 可共用一份因果顺序。

### 不把 trace 混成项目 memory

project memory 是跨会话稳定知识，正文为 Markdown。工具 trace 是一次执行流水。
它不进 memory catalog，不参与召回，不写进 prompt。

### Durability 口径

`ostream.flush()` 能挡常见进程崩溃，不保证断电落盘。文档须明说两档：

- 默认 `process-crash durable`：关键栅栏 append + flush。
- 可选 `power-loss durable`：started/finished 调 `fdatasync/FlushFileBuffers`，会
  增延迟，不默认开。

五枚工具每枚至少两次关键 flush。要量基准，不许为了省几毫秒又把栅栏攒到批末。

### 并发写

- 一场 session 一只 TraceWriter。
- writer 内部队列按 Runtime seq 排序，单线程落盘。
- 主工具虽顺序，后台子代理、Hook 与 app-server 事件可能并发投递。
- shutdown 先封新事件，再 drain，最后写 session terminal marker。
- 队列满不能静默丢关键栅栏；非关键 delta 可合并或丢弃并记 dropped count。

## 结果保存与 artifact

`execution_finished` 必须足以恢复结果，却不能把 20 MB 输出再抄进 trace。

规则：

- 小结果内联，仍过 session 单行与总字节上限。
- 大结果先入 artifact，trace 写 artifact id、sha256、bytes、头尾短预览。
- 写 artifact 成功、trace 引用失败：blob 是孤儿，下次清理。
- trace 引用成功、blob 缺失/哈希不合：恢复成 `result_artifact_missing/corrupt`，
  不重跑工具。
- result 原文与 post-hook 追加文本分开 digest，便于判断 Hook 改了什么。
- compact 不删 trace 与 blob；按现有 retention 另行清理。

## 隐私与脱敏

新增 trace 不该把现有秘密面翻倍。

- 默认存 input/result sha256、字节数与 bounded preview，不再复制完整正文。
- canonical session message 仍按现有行为保存实际工具块；另立安全单处理它已有的
  明文风险，本单不装作已经解决。
- env、HTTP header、API key、Authorization、cookie 永不进 trace。
- command/path 预览过 `RedactSecrets`，原文若已在 tool input，不再抄第二份。
- MCP stderr、Hook stderr、进程日志只存短尾与 digest。
- artifact 与 session 文件权限只给当前用户。
- `/trace` 默认遮敏；`--raw` 须本机交互确认，非 TTY/app-server 另走权限。

## 用户诊断入口

新增只读命令：

```text
/trace                    上一批工具摘要
/trace errors             本 session 明确失败与 unknown
/trace <execution_id>     一枚调用全链
/trace toolu <tool_use_id>
/trace turn <turn_id>
/trace export <path>      脱敏诊断包
```

五工具摘要示例：

```text
turn-8 / step 2 / batch 4
#0 item-15 read_file   succeeded        12 ms
#1 item-16 search      succeeded        31 ms
#2 item-17 edit_file   schema_rejected   1 ms  <- first explicit failure
#3 item-18 run_command cancelled_before_start
#4 item-19 web_fetch   cancelled_before_start
```

详情要回答：

- 输入来自谁，Hook 是否改参；
- 是否问过权限，谁作决定；
- 有没有越过 execution_started；
- runtime/source 是哪只；
- 原始 outcome、稳定 error code、耗时；
- 结果在 session 还是 artifact；
- 是否有 retry/compensation/parent；
- 恢复结论与建议动作。

不要在 UI 写“根因就是 X”，除非有明确 error 或验证证据。用“最早明确失败”、
“首个坏验证点”、“可疑窗口”这些实话。

## 重试规矩

- 原调用永不改写。重试是一枚新 execution，写 `retry_of`。
- 重新过 registry、Hook、schema、权限。不能沿用旧 allow 越权。
- `not_started` 可由模型重新发调用，但仍是新 id。
- `unknown_after_start` 默认禁止一键重试。先核验 postcondition。
- read-only local 可提供“建议重试”，首版也不在恢复时自动执行。
- remote idempotent 先按 idempotency key 查询，再决定是否补发。
- 补偿失败不得回头覆盖原调用；另写 failed compensation。
- 重试次数、退避与上一枚 error code进 trace，免得死循环。

## 崩溃恢复状态机

对每个 scheduled execution 折叠事件：

```text
scheduled only
  -> not_started

scheduled + started
  -> unknown_after_start

scheduled + started + finished
  -> result_recoverable

scheduled + started + finished + result_committed
  -> finished

blocked/declined/cancelled_before_start terminal
  -> finished（未执行）
```

异常组合：

- finished 无 started：账损坏。若旧版本迁移事件可识别，标 legacy；否则
  `trace_corrupt`。
- committed 无 result message：优先扫描全部消息按 `tool_use_id` 复核；真缺便
  从 finished ref 重建。
- result message 有、committed 缺：视为 finished，并补一枚 recovery marker，
  不修改旧行。
- 同 execution 两枚冲突 finished：不挑一枚冒充真相，标 corrupt，保留行号。
- artifact digest 不合：结果不可恢复，工具仍可能已完成，故不是 not_started。

恢复生成的 ToolResult 正文须标 `[会话恢复]`，同时保留稳定 outcome。模型知道
这是宿主判断，不把它当工具刚刚又跑了一遍。

## 实施分期

### 第 0 期：钉契约与失败样本

- [ ] 给五工具正常/第三枚显错/第三枚崩溃/五枚假成功各写 golden session。
- [ ] 定 `ToolLifecycleEvent`、Outcome、ErrorCode、EffectClass。
- [ ] 定 execution/item/tool_use 三 id 映射。
- [ ] 定 JSONL `tool_trace_v1` schema 与兼容策略。
- [ ] 定默认 durability，只承诺 process crash，不吹断电恢复。

### 第 1 期：持久追踪最小闭环

- [ ] Provider assistant message 在执行工具前 append + flush。
- [ ] AgentLoop/TurnRuntime 产 scheduled、started、finished、committed。
- [ ] SessionStore 增 trace serialize/parse/append。
- [ ] 一场 session 单 writer，seq 排序、线程安全。
- [ ] 五工具逐枚 trace，结果 wire 仍合成一条 user message。
- [ ] `/trace` 先做 batch/detail/errors 三档。

### 第 2 期：恢复

- [ ] loader 建 `ToolExecutionLedger`。
- [ ] `RepairToolPairs` 先看 trace，再走 legacy 补洞。
- [ ] finished 未 committed 从 inline/artifact 恢复结果。
- [ ] started 未 finished 标 unknown，绝不重跑。
- [ ] scheduled 未 started 标未执行。
- [ ] 恢复后的五结果仍按原 sequence 配成一条 user message。
- [ ] trace corruption、artifact missing 有稳定错误与告警。

### 第 3 期：来源与细错误

- [ ] ToolRegistry 注册 metadata，不靠 RTTI。
- [ ] 扩内部 ToolResult，保留旧二字段构造。
- [ ] builtin/process/MCP/Lua/native/LSP/agent/PTC 分别映射 error code。
- [ ] MCP 外层 execution 关联 JSON-RPC id 与 transport generation。
- [ ] HookRunRecord 关联 execution，简版持久化。
- [ ] WorkflowRecorder 改从 canonical trace 派生并带 id。

### 第 4 期：语义验证与条件式撤销

- [ ] 工具声明 effect class/idempotency/recovery capability。
- [ ] write/edit 产 pre/post hash 与 undo artifact。
- [ ] 条件式 undo：当前内容仍等 postimage 才自动恢复。
- [ ] postcondition/verification event 与 suspect window。
- [ ] compensation 作为新工具调用，带 `compensates`。
- [ ] run_command/MCP/native unknown 的核验提示，不做通用 rollback。

### 第 5 期：app-server 与诊断包

- [ ] Runtime EventSink 与 SessionTraceSink 吃同一领域事件。
- [ ] app-server 断线按 seq 补事件，必要时从 session trace 冷回放。
- [ ] `/trace export` 产脱敏包，不带 API key/env/header。
- [ ] retention、artifact 清理、session 删除联动。
- [ ] 指标：tool outcomes、unknown recovery、retry/compensation，不采正文。

## 测试矩阵

### 五工具

- [ ] 五枚全成功，sequence 与 id 配对不乱。
- [ ] 第三枚 `is_error=true`，摘要标最早明确失败；四、五照当前策略执行。
- [ ] 第三枚后 ESC，三收口，四五记 cancelled_before_start。
- [ ] 五枚同名工具，仍按 execution/tool_use id 配准。
- [ ] Provider 给重复/空 tool_use id，宿主不让两枚结果串账。

### 崩溃注入

- [ ] assistant message 落盘前。
- [ ] scheduled 后、started 前。
- [ ] started 写盘失败。
- [ ] started 后、Tool::execute 前。
- [ ] 工具副作用中途。
- [ ] execute 返回后、finished 前。
- [ ] finished 后、result message 前。
- [ ] result message 后、committed 前。
- [ ] 五枚每个序号都跑一遍 fault injection。

### 恢复

- [ ] not_started 不误报副作用。
- [ ] unknown_after_start 不自动重试。
- [ ] result_recoverable 不重新执行工具。
- [ ] 老 session 无 trace 仍按旧逻辑恢复。
- [ ] 重复事件幂等，冲突事件报 corrupt。
- [ ] artifact 丢失、截断、hash 错各有稳定 outcome。
- [ ] compact 前后 trace 不丢，恢复后 history 配对仍过 Provider 校验。

### 来源

- [ ] builtin、deferred、MCP、LSP、Lua、native、agent、PTC metadata 正确。
- [ ] DeferredTool 透传真实来源。
- [ ] MCP 超时、server exit、JSON-RPC error、坏 result 分开。
- [ ] MCP 迟到响应关联旧 request，不能投给新调用。
- [ ] Lua load/runtime 与 native error 分开。
- [ ] 子代理内层工具带 parent_execution_id。

### 回滚与补偿

- [ ] edit 后文件未再变，可按 undo token 恢复。
- [ ] edit 后用户又改，自动撤销被拒，给三方 diff。
- [ ] 新建文件内容变了，不删除。
- [ ] shell unknown 不出现“自动回滚成功”假话。
- [ ] remote compensator 成败都另有 execution 与关系边。
- [ ] retry 新 id、重新过权限，原 trace 不动。

### 并发与性能

- [ ] 主工具顺序、后台子代理并发投递，JSONL 行不交错。
- [ ] seq 单调，未知/迟到终态不覆盖正终态。
- [ ] 一万枚短工具，writer 队列、文件句柄与内存有界。
- [ ] flush 延迟基准；不能把五工具交互拖成肉眼可见停顿。
- [ ] session append 失败不死锁 AgentLoop。

### 隐私

- [ ] API key、Authorization、cookie、env secret 不进 trace/export。
- [ ] command/input/result preview 过脱敏并有字节上限。
- [ ] artifact 与 session 权限只给当前用户。
- [ ] `/trace --raw` 在无交互通道不能静默放行。
- [ ] WorkflowRecorder 仍只吃摘要，不因 trace 扩展抄入全文。

## 验收

- 五枚工具正常收口时，按 id、序号、来源、耗时与 outcome 一一列清。
- 任一崩溃点恢复后，每枚调用只落 not_started、finished、result_recoverable、
  unknown_after_start 四档之一。
- `unknown_after_start` 从不自动重试，也不冒充 exit failure。
- finished 未 committed 能从 trace/artifact 补结果，不再把已完成工具跑第二遍。
- tool use/result 配对在取消、崩溃、compact、resume 后仍满足 Provider 硬约束。
- Runtime、session、WorkflowRecorder 不再各手打一套工具事件。
- MCP 内外两层 id 可关联；Hook 决策可追；插件来源可认。
- “最早明确失败”“首个坏验证点”“可疑窗口”三种话分清，不编造根因。
- 本地文件只做条件式撤销；shell/remote/native 无补偿协议时如实不可回滚。
- trace 不扩大敏感正文面，诊断包默认脱敏。

## 不做

- 不把 `is_error=true` 自动等同根因。
- 不因五枚顺序执行就臆造依赖边。
- 不在恢复时自动重放 started 未 finished 的调用。
- 不给任意 `run_command` 做快照回滚。
- 不替 MCP server 猜幂等，不替 Plugin 猜 compensator。
- 不把 UI 瞬时事件当持久真本。
- 不把 WorkflowRecorder 当 session 审计账。
- 不把 tool trace 写进项目 memory 或下一轮 prompt。
- 不默认保存原始 SSE、完整 stderr、env 与无限输出。

## 源码入口

| 责任 | 文件 / 符号 |
| --- | --- |
| 统一工具执行链 | `src/agent/loop.cpp`，`RunOneTool` |
| ToolPhase 与 callbacks | `src/agent/loop.hpp` |
| session 消息/事件/配对修补 | `src/agent/session_store.*`，`RepairToolPairs` |
| 回合落盘时机 | `src/app/interactive_session.cpp`，`PersistNewMessages` |
| UI/Hook/Recorder 接线 | `src/app/turn_runner.cpp` |
| Runtime 事件合同 | `src/runtime/event.*`、`runtime_contract.*` |
| 统一发号 | `src/runtime/id_authority.*` |
| 工具注册与来源 | `src/tools/registry.*`、`src/app/tool_runtime.cpp` |
| MCP 内层请求 | `src/mcp/client.*`、`transport.*` |
| Plugin runtime | `src/tools/plugin_*`、`lua_tool.*` |
| Hook 运行账 | `src/hooks/dispatcher.*`、`types.hpp` |
| 大结果追回 | `src/agent/artifact_store.*` |
| 可复用流程录制 | `src/agent/workflow_recorder.*` |
| 当前流程文档 | `docs/architecture/tool-calling-flow.md` |
| 可靠性与配对恢复 | `docs/architecture/agent-loop/reliability.md` |
| 相关大单 | `todos/显示系统彻底剥离_统一SessionRuntime接TUI_Web_Tauri.todo` |
| 进程终态相关单 | `todos/run_command跨平台Shell语义_进程生命周期与后台任务收口.todo` |
