# 〈特性名〉设计（示例模板——写新单时复制本文件改名,占位文字换成实际内容,与本单风格不合的节可删）

- 状态：待实现（落一批改一笔：待实现 → 某批进行中 → 已收官（删档或保留由主人定夺））
- 日期：YYYY-MM-DD（重大改判另起日期戳补记,如"(2026-08-31 补)"）
- 级别：P0（核心正确性/性能/安全）｜P1（成本/体验/扩展性）｜P2（锦上添花）
- 范围：点名的文件/模块清单——审单人一眼看出要动谁
- 依赖：前置单子或既有基建（写成 `xxx.todo`、`src/yyy` 形式）
- 关联：相邻但不合并的单子（没有就删行）
- 不在本单：明确划走的诱惑——防干着干着 scope 膨胀

## 一、先下断语

先用三五句把方向钉死,再说理由。写"做什么",更要写"不做什么"。
后续所有章节不得与此节相悖;要翻案就在本节加日期戳补记,注明翻案原因,旧文不删。

例:
- 工具不撤,撤的是它肚里的内核。
- 不走两条平级生产路径,不让模型在两扇门之间猜。
- 缺件、坏件时报稳定错误,不静默退回旧路。

## 二、现状证据（当前真相）

先照相再动刀。逐条列:
- 现有实现在哪几个文件、什么结构（贴关键函数名/行号）;
- 它现在为什么不行——慢在哪、错在哪、断在哪,能量数字就量数字;
- 已有可复用底座（别重造轮子,点名 `src/` 下现成件）。

## 三、外部依据与版本裁决

引外部协议/上游版本时写清:取哪个版本、为何钉死不追 latest、
license 选哪种不混写。不确定的标"待真机核实",不凭猜测断言。

## 四、不可破的合同

列出迁移前后必须不变的行为（工具身份、路径与权限归宿主、
用户配置不得暗改、输出格式……）。每条一句,可测。

## 五~N、设计细节（按需分节）

- 模型可见语义:参数 schema、输出投影,中英文一起审;
- 内部结构:类型/接口/纯函数的分工与依赖方向;
- 错误合同:稳定错误码、用户文案、诊断命令;
- 安全:越界校验、防劫持、权限、注入面。

## 分期落地（迁移批次）

每批:清单 checkbox + 一行"验收:"。批间有依赖就按序,写明"不越次序"。
完工即勾,勾时附一句落点（文件/文档名）——账实一致是铁律,做了没勾等于没做。

### P0：〈批次名〉
- [ ] 〈条目——动词开头,一条一件事,可验收〉
- [ ] 〈条目〉→ 落点见 `docs/development/xxx.md` §n。

验收：〈一句话说清这批做完世界应该是什么样,怎么验〉。

### P1：〈批次名〉
- [ ] ...

## 改动边界（预计）

点名预计要动/新增/删除的文件清单。超界即预警。

## 不做什么

与"不在本单"呼应但更细:列举明确拒绝的方案与理由,
被否掉的中间方案可保留为决策过程（标明"不采用,理由:……"）,
免得后人重新发明一遍。

## 最终验收门

收官的硬门槛:全量 ctest 绿、三平台语法验、冒烟场景、
CHANGELOG 落点、真机项如实标注（没有环境就写"未验",不伪造）。
