# P0 新轨迹记录、可重放与训练投影设计
- 状态：P0-1a 已落地（0.26.118 核心仓七件与 golden fixture）；P0-1b 起六批待实现
- 状态：待实现
- 级别：P0
- 日期：2026-08-30
- 范围：今后新建的 LubanCode 会话与任务
- 不管：旧 Session JSONL、旧账修复、旧格式兼容、旧数据迁移
- 目标：从源头记出一份干净轨迹；可审计、可恢复、可重放、可验收、可训练

## 一、定案

旧账不救。新账另起。

今后不再让 SessionStore、ToolTrace、轮末 history 各写一份。每个执行单元只认
一本强类型事实账：`Trajectory Journal`。

这里的“一本”不是把主会话、子代理、workflow 全塞进一只大文件。恰好相反：

- main 在 session 根写 `main.jsonl`；
- 每只 subagent 在 `subagents/` 各写一份 JSONL；
- 每场 workflow 有自己的编排目录与 Journal；
- workflow 中真正执行的 node attempt 各写一份 node JSONL；
- 父级只留派发、终态、结果引用，不抄子级正文。

```text
Runtime
  user / host / model / tool / verifier / control
                         |
                         v
           Scoped TrajectoryRecorder
          每个 run 各自校验、发号、单写、flush
                         |
                         v
        one stream JSONL + session artifacts/
                         |
             +-----------+-----------+
             |                       |
             v                       v
       Replay Projection       Training Exporter
       恢复、回放、调试         SFT、tool-use、失败样本
```

每个执行流一份 JSONL。一间 session 目录把 main、subagent、workflow 编起来。
两种投影照旧。

- Replay Projection 还原运行现场。
- Training Exporter 生成训练样本。
- 两边都只读 Journal。
- 派生物坏了，删掉重算。
- Journal 不从派生物反推，也不由模型补写。

这套设计不再保留“原始消息账 + 工具旁路账 + 轮末补账”三套真值，也不拿 main
充所有子执行的垃圾桶。

## 二、设计原则

### 2.1 一份真账

每个事实只存一回。

- 模型输出里的 ToolCall 只在 `model.output` 存正文。
- 工具执行事件只引用 `call_id`，不再复制 ToolCall 入参。
- ToolResult 只在 `tool.result` 存正文。
- 会话消息从事件投影出来，不另抄一份。
- 控制事件与训练内容同账分型，不混成 user 文本。

### 2.2 先分型，再落盘

记录器不收来路不明的裸 `api::Message`。调用方须交：

```text
谁产生
为何产生
归哪一轮
由哪件事引起
给谁看
能否进训练
```

字段不全，拒绝提交。不能先落一坨文字，日后再猜它是用户、工具还是宿主。

### 2.3 身份不是正文 hash

同一句话真发两次，便是两件事。正文 hash 只校验内容，不充当事件身份。

稳定身份分五层：

```text
run_id        一次可独立重放的运行
turn_id       一枚外层轮次
request_id    一次 provider 请求
call_id       一次工具调用
event_id      一枚事实事件
```

另有 `seq`，在一场 run 内从 1 递增。身份跨进程恢复后仍不重号。

### 2.4 事实与判断分开

`tool.execution.finished(outcome=succeeded)` 只说明工具跑完。它不说明任务做成。

`verification.recorded(passed=true)` 只说明一项验收通过。它也不自动等于整项任务
做成。最终 `outcome.assessed` 要引用验收证据。assistant 自称“完成”不算证据。

### 2.5 可重放不等于可重做

| 能力 | 含义 |
|---|---|
| `exact_replay` | 不再调模型、工具，按已录事件重放，画面与状态可确定还原 |
| `harness_replay` | 模型输出与工具结果走录制值，重跑宿主状态机 |
| `input_replay` | 还原当时请求与工具入参，重新调用外部系统 |
| `live_rerun` | 真调模型与工具，只能比较结果，不能承诺逐字相同 |

模型采样、网络、时间、远端数据都会变。软件不得把 `live_rerun` 吹成确定重现。

### 2.6 训练是投影，不是运行时格式

运行账要中立。不能为某一家 API，把宿主注入硬塞成 `user`，把工具结果硬塞成
某家专用 JSON。

训练 exporter 再按目标格式翻译：

- OpenAI 风格 messages；
- Anthropic 风格 messages；
- provider-neutral tool-use episode；
- 失败恢复样本；
- verifier/evaluator 样本。

### 2.7 先后次序与十条不变量

遇见取舍，照这条次序断：

```text
事实不丢
  > 副作用可判
  > 崩溃后可恢复
  > 身份与来源清楚
  > replay 确定
  > 训练可用
  > 查询方便
  > 界面好看
```

前一层没站稳，不许拿后一层遮。P0 实现须守十条不变量：

1. 一件语义事实只有一个 owner stream 与一份正文。
2. 每份 JSONL 只有一只 writer；别的线程、进程、父子 agent 只交事件，不碰文件。
3. 已提交事件不改；run/session 封口后永不追加。
4. 外部副作用前先有 durable intent，副作用后必有明确 terminal；unknown 不算 success。
5. control、host injection、tool result、peer message 不冒充真人 user。
6. 跨文件只靠稳定 id + event hash 关联，不靠可搬动的绝对路径。
7. replay/export 都是纯函数式投影，不回写 Journal，不偷偷执行外部动作。
8. schema、recorder、replay、exporter 版本全随产物落盘；不支持便明拒，不能猜着读。
9. canonical 事件不因队列满、磁盘慢、UI 卡顿而静默丢弃。
10. 隐私脱敏在入盘前办；无法安全保存的内容留下 omission 与 replay impact。

设计评审、代码评审、测试验收都拿这十条逐项对。任何模块若要破例，须在事件合同里
明写理由、影响与恢复办法，不能靠注释含混过去。

### 2.8 P0 边界

P0 必须一气落下：

- workspace/session 目录、main/subagent/workflow 分账；
- EventEnvelope、单写者、耐久栅栏、hash/blob；
- clear/resume/exit 生命周期与 slash command 统一适配；
- exact replay、resume-as-new、训练四路与证据门；
- session 锁、背压、磁盘不足、权限、脱敏、schema v1 reader/writer；
- contract/state/durability/integration/security/performance 基线。

这些往后放，不得反过来拖住 P0：

- 旧 Session JSONL 迁移或修复；
- SQLite、全文检索、远端检索服务；
- 跨 session 全局 Blob CAS 与引用计数；
- `/record` 跨 session 续选；
- 应用层加密与密钥托管；
- 自动上传、云同步、数据集发布；
- Journal 原地升级；
- 自动删除 canonical 轨迹；
- 对 live rerun 作逐字确定承诺。

后续功能只能读稳定接口、写派生物或另立版本合同。不得为了省两步，又把第二只真账
塞回来。

## 三、工作目录、Session 与分账目录

### 3.1 两层目录

先按工作目录归档。进程启动一次，开一间 session 目录；执行一次 `clear`，也另开一间。
`clear` 是 session 边界，不是删屏幕、抹 history 后接着写旧账。

```text
~/.lubancode/trajectories/
  workspaces/
    <workspace_key>/
      workspace.json
      lifecycle/
        <operation_id>/
          intent.json
          result.json
      tombstones/
        <deleted_session_id>.json
      sessions/
        <session_id>/
          session.json
          main.jsonl
          subagents/
            <agent_run_id>.jsonl
          workflows/
            <workflow_run_id>/
              workflow.jsonl
              definition.json
              checkpoints/
              nodes/
                <node_run_id>.jsonl
          goals/
            <goal_run_id>.jsonl
          loops/
            <loop_run_id>.jsonl
          checkpoints/
            main-<seq>.json
          artifacts/
            sha256/
              ab/
                <full-sha256>
          indexes/
            relations.json
            timeline.json
          derived/
            records/
              <record_id>/
                selection.json
                draft/
                  SKILL.md
          exports/
            training-v1/
              success.jsonl
              failure.jsonl
              partial.jsonl
              excluded.jsonl
              manifest.json
```

这正是目录制：

```text
工作目录 -> 一次启动或 clear 后的新 session -> main / subagent / workflow 各写各的 JSONL
```

### 3.2 workspace_key

若当前目录在 Git 仓库内，以仓库根作 workspace root；否则用启动 cwd。

```text
workspace_key = <root-basename>-<first12(SHA256(normalized-absolute-root))>
```

例如：

```text
lubancode-7d4c9a18f205
```

`workspace.json` 存规范绝对路径、可读名称、首次创建时间、Git remote 的脱敏标识。
路径搬家后可另立 workspace，也可由显式 migrate 命令合并；运行时不凭 basename 猜。

`lifecycle/<operation_id>/` 只记 session 创建、归档、恢复引用、删除与 tombstone。一次
管理操作只认自己的 create-new `intent.json` 与原子生成的 `result.json`，免得同一
workspace 下两只进程争写一份全局 JSONL。它是 workspace 管理账，不是 agent 训练
轨迹。`/delete` 真删 session 目录时，至少留下 id、末 hash、删除时间与原因；不留正文，
也不拿 tombstone 做重放材料。

session 中途 `/worktree` 或改 cwd，不搬目录。它只在对应 JSONL 落 cwd control 事件。
“这场从哪里启动”与“后来走到哪里”两笔账分开。

### 3.3 session_id 与 session.json

一次启动一枚 `session_id`。每次 `clear` 再领一枚，绝不复用。例如：

```text
20260830-031522-7K4M2P
```

时间只给人看。尾部随机/单调成分负责防撞。

`session.json` 只存 session 级静态材料与最终索引：

```json
{
  "schema_version": 1,
  "workspace_key": "lubancode-7d4c9a18f205",
  "session_id": "20260830-031522-7K4M2P",
  "launch_cwd": "D:/lubancode",
  "main_run_id": "main-0001",
  "start_reason": "process_launch",
  "previous_session_id": null,
  "status": "running",
  "created_at_ms": 0,
  "lubancode_version": "0.26.117"
}
```

动态执行细节不往 `session.json` 塞。状态更新用临时文件 + 原子 rename。即便它坏了，
也能从 `main.jsonl` 与子目录扫描恢复。

#### 3.3.1 `clear` 的换账合同

`clear` 不截断 `main.jsonl`，不清空原目录，也不在原 session 中重置 seq。它办一次
“旧账封口、新账开张”。命令本身属于宿主控制，不算真实用户训练消息。

落盘次序定死：

1. 先申请新 `session_id`，以 create-new 建新目录，写 `session.json(status=preparing)`。
2. 旧 `main.jsonl` 写 `session.clear_requested`，带 `next_session_id`，随后 flush/fsync。
3. 停止旧 session 接新 turn、subagent 与 workflow。已在跑的 child 按取消合同收口，
   各自在旧目录写 terminal event；副作用不明便记 `unknown`，不可假装成功。
   若有 `/record` 选段尚未停止，先以 `interrupted_by_clear` 封住选段；不得跨 session 暗续。
   未送达 queue item 逐枚写 cancelled(reason=clear)，不能清完内存便没了下文。
4. 旧流全收齐便写 `run.completed(reason=clear)`；有 unknown/incomplete 便写 `run.failed`。
   随后写 `session.ended(reason=clear, next_session_id=..., close_quality=...)`，封链。
5. 原子更新旧 `session.json`：干净收口为 `closed`，否则为 `incomplete`。
6. 新 `main.jsonl` 写首条 `run.started`，带
   `start_reason=clear`、`previous_session_id`、`caused_by_event_ref`；第二条写跨 session
   `control.command.completed`，两条一并 flush/fsync。
7. 原子更新新 `session.json(status=running)`，再切换进程内 active session 指针。
8. 最后清 main history、turn state、临时队列与本 session 缓存，向界面报告 clear 完成。

这八步须由 `SessionManager::Clear()` 串行掌管。UI、CLI、app-server 不得各写一套。
同一时刻只许一场 clear；重复请求要排队或回 `clear_in_progress`。

新 session 只承接最少的来历引用，不承接旧会话内容：

- 新建 `main_run_id`，turn/request/call/local seq 全从新命名空间起号；
- 新开的 subagent、workflow 全落新 session 目录；
- 旧 session 的 child 不许改挂到新目录，也不许把正文复制进新 `main.jsonl`；
- workspace 配置须重新快照。首版 artifact 不跨 session 复用物理 blob；需要旧内容时，
  复制进新 session CAS，再落新引用。待全局 CAS 有引用计数与事务后，才谈跨 session 去重；
- `previous_session_id` 只供审计、导航与故障恢复，训练投影默认不把两场拼成一段。

若第 1 步失败，旧 session 仍可用，clear 返回失败。若第 2 至 6 步崩溃，启动恢复器按
`clear_requested.next_session_id` 与两边终态续办：不把两本 JSONL 合并，不复用旧
`session_id`。空的 `preparing` 目录可标 `aborted_before_start`；只要新 `main.jsonl` 已有
合法 `run.started`，它便是一场独立 session。

#### 3.3.2 Session 生命周期

`session.json.status` 只走这张图：

```text
preparing -> running -> closing -> closed <-> archived
                |          |
                |          +-> incomplete
                +------------> incomplete

任意可读状态 -> corrupt       校验失败，只读隔离
已删除目录   -> tombstone     workspace 管理态，不是 session 内状态
```

- `preparing`：目录已占住，main 尚未 durable started；不许接 turn。
- `running`：唯一可接新 turn、child、workflow 与 stateful command 的状态。
- `closing`：拒绝新活，只收已有执行的 terminal、run terminal 与 session terminal。
- `closed`：所有 stream 只读，可 replay/export/resume-as-new。
- `incomplete`：崩溃或 I/O 失败留下半场；先 verify/recover，不能冒充 closed success。
- `corrupt`：hash/schema/引用验不过；保留现场，只准 inspect，不准 clean export。
- `archived`：closed 的目录管理标记；正文与 hash 不变。

状态迁移由 SessionManager 独占。`session.json` 更新失败时，以 Journal 可证事实为准，
恢复器重建索引。任何进程打开 running session 写入前，须持有 session 独占锁；inspect
可只读。resume source 通常须 closed/archived；无活 writer 的 incomplete 也可按已验证前缀
resume-as-new，但 unknown/gap 必须带进新 session，clean export 仍拒绝。

closed 还有一道硬门：session 内 active stream count 必须为 0。subagent、workflow、node、
goal、loop、后台任务都须 completed/failed/cancelled/unknown。关闭时先 cancel + bounded join；
收不回来便把相关执行记 unknown，session 标 incomplete，不许写 clean closed。P0 不支持
“session 已关，后台仍往旧 JSONL 补结果”。真要做跨 session 的 detached job，日后另立
workspace 级 run 合同，不能偷借 main.jsonl。

### 3.4 main.jsonl

`main.jsonl` 只记 main agent 与 session 控制事实：

```text
main input
main model request/output
main tool call/result
main context injection
main verification/outcome
title/cwd/mode/queue/compact/resume
subagent 派发与结果接收边界
workflow 启动与结果接收边界
```

它不记 subagent 内部模型消息，不记 subagent 内部工具流，也不记 workflow node 内部
流水。

### 3.5 subagents/<agent_run_id>.jsonl

每开一只 subagent，立刻创建一份独立 JSONL：

```text
subagents/agent-0001.jsonl
subagents/agent-0002.jsonl
```

第一行 `run.started` 须带：

```json
{
  "run_kind": "subagent",
  "agent_run_id": "agent-0002",
  "owner_run_id": "main-0001",
  "parent_agent_run_id": null,
  "spawn_event_id": "evt-...",
  "profile_ref": "...",
  "task_ref": "...",
  "isolation_ref": "..."
}
```

这份 JSONL 收完整子轨迹：子输入、模型请求、模型输出、工具调用、工具结果、验证、
终态。main 只留：

```text
subagent.dispatched(agent_run_id, task_ref)
subagent.result.accepted(agent_run_id, child_final_hash, result_ref, outcome)
```

Agent Dock 要看详情，按 `agent_run_id` 打开对应文件。不能回 main 里捞副本。

subagent 再派 subagent，仍在同一 `subagents/` 目录新开文件。新文件的
`parent_agent_run_id` 指直接父代理。无需套出多层目录，免得 Windows 路径越长。

### 3.6 workflow 目录

每启动一次 workflow，开一间 workflow run 目录：

```text
workflows/wf-0001/
  workflow.jsonl
  definition.json
  checkpoints/
  nodes/
```

现有 `workflow::RunJournal` 已有 `manifest + definition + events + checkpoints` 的目录
骨架。保留这条路，只把命名、信封、hash、artifact 与 session 归属统一起来。

现有 RunJournal 写坏后只置 broken、继续跑的策略须改。关键编排事件、node 派发、
checkpoint、node terminal 写不住时，workflow 必须停在明确失败态，不能一边断账一边
继续调度。非关键 UI 投影才许降级。

`workflow.jsonl` 只记编排：

```text
run.started(run_kind=workflow)
workflow.definition.loaded
workflow.node.ready
workflow.node.dispatched
workflow.node.completed|failed|skipped
workflow.branch.started
workflow.join.completed
workflow.loop.iteration.started|completed
workflow.store.patched
workflow.checkpoint.saved
run.completed|failed|cancelled
```

会调 Agent、Tool、Command、MCP 或自定义执行器的 node，每次 attempt 单开：

```text
workflows/wf-0001/nodes/node-shangshu-a1.jsonl
workflows/wf-0001/nodes/node-shangshu-a2.jsonl
```

node JSONL 的第一行带：

```text
workflow_run_id
workflow_id
node_run_id
node_id
node_kind
attempt
branch_path
loop_iteration
map_item_index
reduce_step
```

纯控制节点，如 branch、join、loop gate，只记在 `workflow.jsonl`，不另开 node 文件。

workflow node 若是一只 Agent，它的完整 Agent 轨迹就在 node JSONL，不能再抄进
`subagents/`。目录位置已经说明“它归 workflow 所有”。

重试必须新开 node JSONL。attempt 1 与 attempt 2 不共写一份。

### 3.7 goal 与 loop

goal、scheduled loop 也遵循同一规矩：控制器有自己的 JSONL；它派出的 main turn、
subagent 或 workflow 只留引用，不复制细节。

首版若不迁 goal/loop，可先保留目录占位。main/subagent/workflow 三条主路不得因此
拖延。

### 3.8 Session 级共享内容仓

大正文、图片、音频、工具输出、请求快照与 diff 统一放：

```text
<session>/artifacts/sha256/
```

各 JSONL 只写 BlobRef。这样 subagent 与 main 引同一内容时不复制大文件，又不会把
两边的事件混进一只日志。

Recorder 构造时钉死两处：

```text
stream_path         自己唯一可写的 JSONL
session_artifact_root 只可写内容寻址 blob
```

它不能打开兄弟 JSONL。

### 3.9 跨文件关系

不另造万能关系数据库。关系分两头记：

- 子 JSONL 第一行说“我归谁所有、由哪枚派发事件生出”。
- 父 JSONL 说“我何时派出、何时接受结果”。

两边是两桩事实，不是正文副本。Validator 交叉核：

- child owner 与 parent run 对得上；
- spawn_event_id 真存在；
- 父接受结果时，child 已有终态；
- child final hash 对得上；
- 同一 child 结果至多接受一次。

`indexes/relations.json` 只是查询缓存，删了可扫目录重建。

### 3.10 创建次序

开 subagent 或 workflow node：

1. 父发 child/node id。
2. 用 create-new 原子占住目标 JSONL，此时文件为空，子执行尚未启动。
3. 父先 durable 落 dispatched，写清 child id 与相对路径。
4. dispatched 成功后，子 Recorder 才落 `run.started`。
5. 子写完整轨迹与终态。
6. 父核 child final hash，落 result accepted/completed。

若第 3 步前死，只剩无父引用的空文件，可回收。若第 3 步后、第 4 步前死，父账
明确显示 `dispatched_not_started`，恢复时可判定工具/模型都没跑。父 dispatched 写失败，
只许删除仍为空的占位文件；一旦子已 started，任何一边都不得删账。

首版不加 SQLite，不加新运行时依赖。现有工程已有 JSON、SHA-256、append+flush 与
artifact store。JSONL 便于流式写、肉眼查、造 fixture，也方便训练工具读取。

日后数据量大，离线 exporter 可产 Parquet。Parquet 仍是派生物，不进主运行时。

## 四、事件总合同

### 4.1 EventEnvelope

每行一枚事件。所有事件共用这只信封：

```json
{
  "schema": "lubancode.trajectory.event",
  "schema_version": 1,
  "workspace_key": "lubancode-7d4c9a18f205",
  "session_id": "20260830-031522-7K4M2P",
  "run_id": "agent-0002",
  "run_kind": "subagent",
  "event_id": "agent-0002:evt-00000042",
  "seq": 42,
  "turn_id": "turn-0007",
  "request_id": "req-0019",
  "call_id": "call-0012",
  "kind": "tool.execution.finished",
  "plane": "execution",
  "actor": "tool",
  "origin": "builtin_tool",
  "visibility": ["host_only"],
  "training_policy": "metadata",
  "causation_id": "evt-00000039",
  "correlation_id": "call-0012",
  "wall_time_ms": 0,
  "monotonic_ns": 0,
  "payload": {},
  "prev_hash": "...",
  "event_hash": "..."
}
```

| 字段 | 规矩 |
|---|---|
| `schema` | 固定字符串，防把别的 JSONL 当轨迹 |
| `schema_version` | 整数，只认已实现版本 |
| `workspace_key` | 归哪一层工作目录 |
| `session_id` | 归哪次启动 |
| `run_id` | 本 JSONL 的执行流身份；session 内唯一 |
| `run_kind` | main_session / subagent / workflow / workflow_node / goal / loop |
| `event_id` | session 内唯一，固定为 `run_id + local seq` |
| `seq` | run 内连续递增，不回收 |
| `kind` | 强类型事件名 |
| `plane` | conversation / execution / control / evidence |
| `actor` | user / model / tool / host / verifier |
| `origin` | 更细来路 |
| `visibility` | 这件事实当时给谁看 |
| `training_policy` | 默认训练处理方式 |
| `causation_id` | 直接因由 |
| `correlation_id` | 同一 request/call/verification 的聚合键 |
| `payload` | 由 kind 决定，过强校验 |
| `prev_hash/event_hash` | run 内 hash chain |

`turn_id`、`request_id`、`call_id` 按事件种类选填。该有而没有，提交器拒绝。

### 4.2 plane

```text
conversation
  真用户输入、宿主输入、模型输出、工具结果

execution
  provider 请求、工具执行、取消、错误、重试

control
  title、queue、mode、compact、checkpoint、resume、scheduler

evidence
  测试、浏览器验收、文件 hash、外部验证、最终裁决
```

训练 exporter 默认只取 conversation，按需并入 execution 与 evidence。control 永不
冒充对话。

### 4.3 actor 与 origin

`actor` 粗分主体，`origin` 钉细来源。

```text
external_user
queued_user
peer_agent
scheduled_host
goal_continuation
provider_model
builtin_tool
mcp_tool
plugin_tool
lsp_tool
subagent_tool
hook
memory_recall
background_completion
agent_roster
budget_guard
compact_runtime
recovery_runtime
verifier_host
```

| 现场 | actor | origin |
|---|---|---|
| 人在终端敲一问 | user | external_user |
| 排队消息送达 | user | queued_user |
| 定时 tick 开一轮 | host | scheduled_host |
| 模型输出 | model | provider_model |
| MCP 回包 | tool | mcp_tool |
| 记忆召回 | host | memory_recall |
| 测试结果采证 | verifier | verifier_host |

`queued_user` 可投影成训练 user，但元数据要留。`scheduled_host` 不可投影成真人。

### 4.4 visibility

```text
model_input
model_output
tool_input
tool_output
host_only
user_visible
```

一枚事件可有多个可见面，故字段用数组。不可拿一只枚举丢掉多重关系。

### 4.5 training_policy

```text
include       默认入训练正文
metadata      只进样本 metadata
exclude       默认剔除
review        未经规则或人工审核不得出集
```

这是默认策略，不是最终裁决。Exporter 还要过质量门、隐私门与目标格式门。

## 五、事件种类

### 5.1 生命周期

```text
run.started
run.environment.captured
run.completed | run.failed | run.cancelled

session.clear_requested
session.ended

turn.started
turn.completed | turn.failed | turn.cancelled
```

`run.started` 先于一切业务事件。run 与 turn 的终态各自三选一，至多一枚。
`session.clear_requested/session.ended` 只许出现在 main stream，均属 control plane，
`training_policy=exclude`。`session.ended` 之后本 session 各 Recorder 一概拒写。
main session 正常封口时，须先收齐活动 turn 与 child，再落 run terminal，最后才落
`session.ended`。故 `session.ended` 必是 main stream 最后一枚事件。

外层 turn 可由真人、queue、peer、scheduler 或 goal continuation 发起。起因写在
`turn.started.payload.trigger`，不能拿第一条 user-role message 猜。

### 5.2 输入与上下文

```text
input.received
context.attached
context.detached
```

`input.received` 存本轮主输入：

```json
{
  "input_id": "input-0007",
  "content": [{"type": "text", "text": "..."}],
  "attachments": [],
  "channel": "terminal",
  "sender": {"kind": "local_user"}
}
```

`context.attached` 存请求级宿主注入：

```json
{
  "context_id": "ctx-0014",
  "context_kind": "memory_recall",
  "scope": "request",
  "content_ref": {"sha256": "...", "size": 1234},
  "source_refs": ["memory:fact-19"]
}
```

记忆、hook 附注、agent roster、budget nudge、后台回流都走这类事件。它们不塞进
`input.received`，训练时也不冒充用户。

### 5.3 模型请求与输出

```text
model.request.prepared
model.request.sent
model.output.completed | model.output.failed | model.output.cancelled
```

`model.request.prepared` 是可重放核心：

```json
{
  "model": "gpt-5.6-sol",
  "provider": "ccmoon",
  "wire": "responses",
  "parameters": {
    "max_output_tokens": 32768,
    "reasoning_effort": "high",
    "temperature": null,
    "seed": null
  },
  "system_ref": {"sha256": "...", "size": 0},
  "toolset_ref": {"sha256": "...", "size": 0},
  "message_refs": ["evt-...", "evt-..."],
  "context_refs": ["ctx-0014"],
  "request_snapshot_ref": {"sha256": "...", "size": 0},
  "request_snapshot_sha256": "...",
  "cache_epoch": 2
}
```

`request_snapshot_ref` 指 provider-neutral 规范请求，不是某家 wire 字节。若开诊断，
可另存 `wire_snapshot_ref`。它默认关闭，且须先洗密钥。

`model.output.completed` 存一份规范输出：

```json
{
  "output_id": "output-0019",
  "blocks": [
    {"type": "text", "text": "我先查文件。"},
    {
      "type": "tool_call",
      "call_id": "call-0012",
      "provider_call_id": "toolu_...",
      "name": "read_file",
      "arguments": {"path": "src/a.cpp"}
    }
  ],
  "stop_reason": "tool_use",
  "usage": {},
  "provider_response_id": "resp_..."
}
```

同一份输出不可再另落 assistant message。会话历史由这枚事件投影。

### 5.4 工具调用与结果

```text
tool.execution.planned
tool.input.effective
tool.execution.started
tool.execution.finished | tool.execution.failed | tool.execution.cancelled | tool.execution.unknown
tool.result.committed
```

`tool.execution.planned` 引用 model output 里的 `call_id`，不复制原始 arguments。

hook、权限层可能改入参。真执行前落 `tool.input.effective`：

```json
{
  "call_id": "call-0012",
  "tool_name": "read_file",
  "source_kind": "builtin",
  "source_instance": "",
  "effect_class": "read_only_local",
  "effective_arguments": {"path": "src/a.cpp"},
  "effective_arguments_sha256": "...",
  "rewritten_by": ["hook:pre_tool_use"]
}
```

`tool.execution.started` durable 后，副作用工具才许执行。

`tool.execution.finished` 存执行事实，不存给模型看的 ToolResult 形状：

```json
{
  "outcome": "succeeded",
  "duration_ms": 18,
  "exit_code": null,
  "stdout_ref": null,
  "stderr_ref": null,
  "result_ref": {"sha256": "...", "size": 921},
  "side_effects": [],
  "undo_ref": null
}
```

`tool.result.committed` 存真正回喂模型的规范 ToolResult：

```json
{
  "call_id": "call-0012",
  "content": [{"type": "text", "text_ref": {"sha256": "..."}}],
  "is_error": false,
  "derived_from_event": "evt-00000045"
}
```

执行结果与回喂结果分开，正好说明：工具跑完没有、结果存住没有、模型收到什么、
中间有无截断、脱敏或 artifact 化。

### 5.5 控制与证据

```text
control.command.requested
control.command.completed | control.command.failed | control.command.cancelled | control.command.rejected

control.title.changed
control.cwd.changed
control.mode.changed
control.context_window.changed
control.checkpoint.created

control.queue.item.enqueued
control.queue.item.dequeued | control.queue.item.cancelled | control.queue.item.expired
control.queue.snapshot

control.approval.requested
control.approval.resolved | control.approval.expired
control.cancellation.requested
control.cancellation.applied

compact.requested
compact.request.prepared
compact.output.generated
compact.validation.completed
compact.applied | compact.failed | compact.cancelled | compact.rejected

record.selection.started
record.selection.paused
record.selection.resumed
record.selection.note_added
record.selection.completed | record.selection.cancelled | record.selection.interrupted

resume.source.attached

verification.started
verification.recorded
verification.invalidated
outcome.assessed
```

control 一律 `plane=control`、`training_policy=exclude`。actor 要照实：真人敲 slash 时，
`control.command.requested` 是 `actor=user, origin=external_user`；宿主落下状态变更时，
domain event 才是 `actor=host`。不能为了省枚举，把真人指令伪成宿主自发动作。

审批答案、ESC/stop 取消、排队输入都照这条规矩。`approval.resolved(decision=allow|deny)`
要引 requested 与授权 policy hash；真人点允许，actor=user。queue item 自 enqueued 后只能
dequeued/cancelled/expired 三选一；dequeued 后以同一 input id 触发 turn，不能再抄一条
“新用户消息”。

compact 不重写 Journal，也不另造 session。它只生成一份有来源的上下文投影，再改变
后续 `model.request.prepared` 的 message refs 与 context refs。原事件仍在，回放器能
看出模型当时看了哪一版。

`verification.recorded`：

```json
{
  "verification_id": "verify-0004",
  "kind": "test_suite",
  "subject": "build/release/lubancode_tests.exe",
  "command_ref": {"sha256": "..."},
  "passed": true,
  "facts": {"passed": 104, "failed": 0, "skipped": 0},
  "artifact_refs": [],
  "observed_after_seq": 281,
  "producer": "run_command",
  "fresh": true
}
```

后面若又改相关文件，宿主落 `verification.invalidated`。训练集不能拿旧测试给新代码
作证。

## 六、因果与状态机

### 6.1 一轮标准轨迹

```text
run.started
turn.started
input.received
context.attached*

model.request.prepared
model.request.sent
model.output.completed

tool.execution.planned*
tool.input.effective*
tool.execution.started*
tool.execution.finished | failed | cancelled | unknown
tool.result.committed*

model.request.prepared
model.request.sent
model.output.completed

verification.recorded*
outcome.assessed
turn.completed
run.completed
```

### 6.2 硬约束

提交时即时检查：

1. `run.started` 只能一枚，且 seq=1。
2. turn 终态前必须有 `turn.started`。
3. `input.received` 先于本 turn 首次 `model.request.sent`。
4. `model.request.sent` 必须引用已提交的 prepared event。
5. `model.output.completed` 必须引用已发送 request。
6. ToolCall 的 `call_id` 在 run 内唯一。
7. tool started 前必须有 effective input。
8. 副作用工具 started 未 durable，不许执行。
9. tool finished/failed/cancelled/unknown 四选一，至多一枚。
10. tool result 必须引用已知 ToolCall 与执行终态。
11. 下一次 model request 前，本批每枚 ToolCall 都有 ToolResult。
12. turn completed 前不能留悬空 request 或 tool call。
13. run terminal 至多一枚。
14. `seq` 连续；`prev_hash` 接上前一枚 `event_hash`。
15. 需要审批的副作用工具，started 前必须有 allow 决议或可追溯的自动授权 policy hash。
16. deny/expired 后不得出现对应 tool started；取消后须落到明确 execution/turn/run 终态。
17. queue item 自 enqueued 后，dequeued/cancelled/expired 三选一，至多一枚。
18. dequeued queue item 与 `turn.started.trigger`/`input.received.input_id` 必须对上。

约束违例时，Recorder 回失败 receipt。不能“先写了再说”。

### 6.3 并行、重试与子代理

并行批次另带：

```text
batch_id
position_in_batch
parallel_group_id
```

`seq` 只表示 Recorder 接纳顺序。逻辑并行看 batch/group，耗时看 monotonic 时间。

通用关系字段：

```text
retry_of
compensates
parent_call_id
parent_run_id
child_run_id
blocked_by
```

子代理有自己的 `run_id`。父轨迹只落 child run 开始、完成与结果引用，并引用子 run
final event hash。不要把整本子代理日志抄进父轨迹。

每份 JSONL 自己发 `seq`，自己接 hash chain。main、两只并行 subagent、workflow node
不抢一把全局序号锁。跨文件引用须用全限定形状：

```json
{
  "session_id": "...",
  "run_id": "agent-0002",
  "event_id": "agent-0002:evt-00000042",
  "event_hash": "..."
}
```

跨文件先后靠父 dispatched、子 started/terminal、父 accepted 这些因果边，不拿不同
文件的墙钟或 local seq 硬排。`timeline.json` 只是折叠后的查询索引。

## 七、单一写入架构

### 7.1 模块

```text
src/trajectory/
  event.hpp/.cpp
  schema.hpp/.cpp
  canonical_json.hpp/.cpp
  blob_store.hpp/.cpp
  directory.hpp/.cpp
  session_lock.hpp/.cpp
  journal.hpp/.cpp
  recorder.hpp/.cpp
  validator.hpp/.cpp
  replay.hpp/.cpp
  training_exporter.hpp/.cpp
  session_index.hpp/.cpp
  usage_gc.hpp/.cpp
  metrics.hpp/.cpp
```

runtime 只认 `runtime::TrajectorySink`。装配层把真正 Recorder 挂上。`trajectory`
不 include `app`、`cli`。

### 7.2 Recorder API

```cpp
struct RecordRequest {
    EventKind kind;
    EventScope scope;
    EventLinks links;
    nlohmann::json payload;
};

struct RecordReceipt {
    enum class Status { Committed, Rejected, IoFailed } status;
    std::string event_id;
    std::uint64_t seq = 0;
    std::string event_hash;
    std::string error_code;
};

class TrajectorySink {
public:
    virtual RecordReceipt Record(RecordRequest request,
                                 Durability durability) = 0;
};
```

调用方不传 `seq/event_id/hash/time`。这些由 Recorder 一处生成。

### 7.3 单写者

一场 run 一只 Recorder，一把 mutex，一只打开的 journal handle。

```text
lock
  -> schema validate
  -> state-machine validate
  -> assign seq/event_id
  -> offload blobs
  -> canonical JSON
  -> compute hash chain
  -> append line
  -> flush/fsync by durability
  -> update in-memory indexes
unlock
```

`ToolTraceHub`、`SessionRuntime`、`interactive_session`、app-server 都不直接摸文件。

### 7.4 Durability 与失败

```text
Buffered       非关键 UI/control 事件
ProcessCrash   append + flush；默认
PowerLoss      FlushFileBuffers/fsync；副作用栅栏、checkpoint、run terminal
```

| 失败点 | Runtime 行为 |
|---|---|
| input 记不住 | 不发模型 |
| request prepared 记不住 | 不发模型 |
| model output 记不住 | 不执行工具 |
| side-effect started 记不住 | 不执行工具 |
| tool finished 记不住 | 标 run failed，不假称结果已知 |
| tool result 记不住 | 不发下一次模型请求 |
| verification 记不住 | 不得判 verified success |
| terminal 记不住 | run 保持 incomplete，resume 时收口 |

事实没写稳，就不能继续跨下一道副作用边界。

### 7.5 并发、背压与锁

进程内可有许多生产者，却不能有许多文件 writer。每只 Recorder 持一条有界 MPSC
提交队列与一条串行 commit lane；同步副作用栅栏可直等 receipt，流式 UI 不得占这条路。

```text
model/tool/hook callbacks
        -> bounded RecordRequest queue
        -> one commit lane
        -> one journal handle
```

- canonical event 永不 drop。队列满时给生产者背压；超时便 fail 当前 run，不能继续跑。
- token streaming delta、spinner、进度动画不是 canonical 事实。UI 自己走可丢 telemetry；
  Journal 只收规范 model output、必要 usage checkpoint 与 terminal。
- 大 payload 先流式写临时 blob，避免在队列里复制几份；RecordRequest 只带 blob receipt。
- main、subagent、workflow/node 各有自己的 commit lane，互不争一把全局锁。
- session 目录持进程级独占 lock 文件与 owner metadata。陈旧锁须核 PID/boot identity，
  不能只看 mtime 便硬抢。
- workspace lifecycle 采用 operation 目录，不设共享 append writer。
- shutdown 先停接新活，再 drain 队列、收 terminal、flush、关柄；超时留下 incomplete，
  不拿强退伪装 clean close。

队列容量、单 event 上限、单 blob 上限与 drain timeout 都要配置并记入环境快照。默认值
须由压力测试定，不把魔数散在各模块。

## 八、规范 JSON、Blob 与完整性

### 8.1 Canonical JSON

hash 输入须稳定：

- object key 递归按 UTF-8 字节序排序；
- 无无意义空白；
- UTF-8 先清洗；
- 数字不许 NaN/Inf；
- 相同语义对象跨 Windows/Linux dump 结果一致。

不得直接依赖 `nlohmann::json` 插入顺序算规范 hash。须写公共
`CanonicalJsonDump()`，单测钉字节。

### 8.2 BlobRef

超过内联上限的正文进 blob：

```json
{
  "sha256": "...",
  "size": 123456,
  "media_type": "application/json",
  "encoding": "utf-8",
  "compression": "none"
}
```

首版内联上限建议 `32 KiB`，可配置。图片、音频、二进制永不以内联 base64 进 Journal。

Blob 先写临时文件，算 hash，flush，原子 rename 到内容地址，再提交引用事件。事件
不可指向尚未落稳的 blob。

### 8.3 Hash chain

```text
event_hash = SHA256(prev_hash || canonical_event_without_event_hash)
```

每份 JSONL 的 terminal event payload 记：

```text
first_event_hash
event_count_before_terminal
schema_version
recorder_version
```

terminal 自己的 `event_hash` 就是 `last_event_hash`。文件关闭后再算 `journal_sha256`，
原子更新进 `session.json` 的 stream 索引。不能把整文件 SHA 写进文件自身，会形成
循环依赖。

`session.json` 在安全点记录各 stream 的 path、run kind、terminal state、event count、
`last_event_hash` 与 `journal_sha256`。这张表是索引；真值仍是各 JSONL 的 terminal
event 与 hash chain。

hash chain 能查误改、截断与乱序。它不是数字签名。能写目录的人仍可整本重算。

### 8.4 Schema 演进

“旧脏账不迁”不等于新格式永远只有 v1。今日写出的干净 v1，明日仍要让新程序认得。

- envelope 与每类 payload 各有 schema version；exporter schema 单独版本化。
- 同一 major 内只许向后兼容的加字段/加可选 event kind；删字段、改语义、改 hash 规则
  必须升 major。
- `run.started` 写 `writer_version/min_reader_version/required_capabilities`。
- Reader 碰见未知 event 时先看 replay impact：纯展示事件可保留并跳过；stateful、execution、
  evidence 事件不认识，便把 replay/export 判 unsupported，不能静默略过。
- Validator 按 source schema 的规则验 hash，不拿新版本重新序列化旧事件再验。
- 不原地升级 Journal。若将来确需转换，写一份新派生产物，附 source journal hash 与
  converter version；原件保留到用户显式删除。
- CI 至少保存 v1 golden fixtures。每次改 reader/writer/exporter，都跑旧读新写、同版
  round-trip、未知关键事件 fail-closed 与确定性 hash。

## 九、环境与重现

### 9.1 run.environment.captured

至少记录：

```text
lubancode version / build commit / build type
OS / arch / locale / timezone
cwd / repository root
git HEAD / branch / dirty flag
dirty patch blob ref / untracked file manifest ref
provider / wire / model / model parameters
tool definition set ref / system prompt ref
project instruction ref / loaded skill refs
plugin/package refs and hashes
relevant config snapshot ref（已脱敏）
```

环境变量只记 allowlist。密钥不落值，也不落可暴力猜测的裸 hash。

### 9.2 工作区重现等级

```text
exact
source_exact_environment_partial
input_only
blocked
```

每条执行流在 `run.started`、terminal event 与 `session.json` 索引里明写等级、依据
与缺口。不要只放一个模糊的
`replayable=true`。

### 9.3 side effects

文件工具至少记 path、preimage hash、postimage hash、diff ref、undo ref。

命令工具至少记 argv/shell mode、cwd、允许递入的 env 名、exit code、stdout/stderr
refs、timeout/cancel。

网络与 MCP 至少记 server identity、tool schema hash、effective arguments、response
ref、latency、remote request id。

外部世界不会随记录冻结。这类调用默认只能 exact replay 已录结果；live rerun 属于
新实验，不能覆盖旧轨迹。

## 十、Replay Projection

### 10.1 ReplayState

Replay 纯读 Journal，折叠出：

```text
run state
turn state
effective conversation
provider request steps
tool ledger
control state
evidence ledger
artifact refs
quality/integrity report
```

Replay 不调用模型，不执行工具，不读当前墙钟，不随机发号。

### 10.2 exact replay

用于 resume 前验账、终端/网页逐步回放、bug 复现、状态机回归与训练编译。

同一 Journal、同一 replay version，规范状态 hash 必须相同。

### 10.3 harness replay

把边界换成录制桩：

```text
ProviderBackend -> RecordedModelBackend
ToolExecutor    -> RecordedToolExecutor
Clock           -> RecordedClock
IdAuthority     -> RecordedIds
```

宿主照常跑 turn 状态机。每一步只消费下一枚匹配事件。名字、参数 hash、因果 id
不合，立即报 divergence，不向后硬找。

这条路用来稳定复现“某次模型输出后，宿主为何走错”。

### 10.4 resume

resume 也从目录内的 JSONL 重建，不再另读 Session messages。但它不回头追加旧
`main.jsonl`。每次进程启动本就该有新 session；交互中执行 `/resume`，也要封住当前
session，再开一间新目录。

两条入口同一口径：

- `lubancode --continue`：启动时直接建立
  `start_reason=resume, resumed_from_session_id=<old>` 的新 session，不先造空 session；
- 交互 `/resume <old>`：当前 session 以 `end_reason=switch_to_resume` 封口，随后建立
  新 session。新旧 id 不同，旧目录不再打开写句柄。

恢复步骤：

1. 锁定并读取 source session，验 schema、逐流 seq、hash chain 与父子边。
2. 找最后一枚完整 checkpoint；没有便从头 replay。
3. 从 checkpoint 后继续折叠，算出 effective conversation、control state 与 state hash。
4. 尾部若有悬空工具，按 started/finished/result 三道账给出明确状态；未知副作用不可重跑。
5. 建新 session 与新 `main.jsonl`，首条写 `run.started(start_reason=resume)`。
6. 写 `resume.source.attached`，带 source session id、main terminal hash、replay version、
   imported state hash、checkpoint ref 与跨 session 的 qualified event refs；若入口是交互
   `/resume`，再写跨 session `control.command.completed`。
7. 新 session 自己的 turn/request/call/local seq 从新命名空间起号，再开新 turn。

恢复 main 时先读 source `main.jsonl`；只有它的边界事件表明某只 subagent/workflow
影响未决状态，才去打开对应子文件。已经完成的 child 只核 terminal hash，不把正文
灌进新 `main.jsonl`。effective history 可以引用 source 事件，不能复制成一批假新消息。

source session 若仍由别的进程持写锁，默认拒绝 resume。除非以后另立只读 fork 命令，
不得一边有人写旧账，一边拿半本账恢复。

checkpoint 只是缓存。payload 带 `source_seq/source_event_hash/state_hash`。对不上便丢，
从 Journal 重算。

## 十一、训练投影

### 11.1 Episode 单位

默认一枚外层 turn 一条 episode。另支持按 task/run 聚合。

```json
{
  "schema": "lubancode.training.episode",
  "schema_version": 1,
  "episode_id": "<run_id>:<turn_id>",
  "source": {
    "workspace_key": "...",
    "session_id": "...",
    "run_id": "...",
    "run_kind": "subagent",
    "turn_id": "...",
    "journal_last_hash": "...",
    "exporter_version": "..."
  },
  "messages": [],
  "steps": [],
  "outcome": {},
  "evidence": [],
  "quality": {},
  "replay": {}
}
```

Exporter 先按目录认执行流，不把整间 session 拍平成一段：

| 来源文件 | 默认训练单位 |
|---|---|
| `main.jsonl` | main turn episode |
| `subagents/<id>.jsonl` | subagent turn/任务 episode |
| `workflows/<id>/workflow.jsonl` | orchestration/DAG 决策样本 |
| `workflows/<id>/nodes/<id>.jsonl` | node attempt episode |
| `goals/<id>.jsonl` | goal/checkpoint/evaluator 样本 |
| `loops/<id>.jsonl` | scheduler/iteration 样本 |

父文件里的 `subagent.result.accepted`、`workflow.node.completed` 默认只进 metadata，
不再把 child 正文展开一遍。这样同一段子轨迹不会在 main 与 child 两处重复入集。

若要训练“main 如何派工、workflow 如何编排”，另出 composite episode。它只带子轨迹
引用、输入输出摘要与 outcome，不内联整本 child JSONL。

### 11.2 Provider-neutral messages

```text
external_user / queued_user
  -> role=user

provider_model text
  -> role=assistant

provider_model tool_call
  -> assistant.tool_calls

tool.result.committed
  -> role=tool，call_id 对应

host context
  -> role=developer 或 episode.context
  -> 绝不冒充 user

control event
  -> 默认不进 messages
```

Exporter 保留 `origin`。目标格式不支持时，放 metadata，不得静默丢。

### 11.3 四路输出

```text
success.jsonl
  已完成、结构完整、验收证据足

failure.jsonl
  已知失败/取消，结构完整，可作失败恢复训练

partial.jsonl
  预算耗尽、人工打断、运行未收口

excluded.jsonl
  结构坏、隐私未过、来源不明、证据不够
```

四路由独立质量轴决定，别只用一只 `clean` 布尔值。

### 11.4 QualityAxes

```json
{
  "structure": "valid",
  "outcome": "succeeded",
  "verification": "verified",
  "privacy": "passed",
  "replayability": "exact_offline",
  "completeness": "complete",
  "training_eligible": true,
  "reasons": []
}
```

独立轴能说明：

- 结构完整的失败轨迹能进 failure。
- 任务成功却没验收，只能进 excluded/unverified。
- 有脱敏内容但结构完整，可按目标策略决定是否进集。
- exact replay 与 live rerun 不会混成一句“可重现”。

### 11.5 成功样本门

进 `success.jsonl` 必须同时满足：

- Journal schema、seq、hash chain 全过。
- turn 与 run 正常收口。
- request/output/tool/result 全部成对。
- 没有 unknown side effect。
- 没有缺失 blob。
- 所有宿主注入均有 origin。
- `outcome.assessed` 为 succeeded。
- 每项 acceptance criterion 至少指一枚 fresh evidence。
- evidence 晚于相关最后修改。
- 隐私与密钥扫描通过。
- exporter 没做猜测或修补。

少一项便不进 success。

### 11.6 thinking/reasoning

Journal 可按策略记录 provider 明确返回的 thinking block 引用。Training Export 默认
剔除。不能把 provider 隐藏推理凭空补出，也不能拿宿主日志冒充模型思考。

若用户明确开启 reasoning 导出，manifest 须写策略、来源与许可状态。

### 11.7 数据集幂等

新轨迹不靠训练期去重治写入 bug。Recorder 已从源头保证 event id 唯一。

数据集只防同一 run 被重复导出：

```text
episode_fingerprint = SHA256(
  run_id + turn_id + journal_last_hash + exporter_config_hash
)
```

不同 run 即便正文相同，也不自动合并。

## 十二、隐私与密钥

记录越全，泄露面越大。脱敏须在落盘前做，不等 exporter 临时补。

```text
record
  普通正文、工具结果、diff、测试证据

redact
  token、cookie、Authorization、私钥、密码、连接串

omit_with_marker
  无法安全保存却影响重现的内容
```

omit 时落：

```json
{
  "redacted": true,
  "reason": "credential",
  "replay_impact": "live_rerun_blocked"
}
```

不可用裸 SHA-256 存低熵密码指纹。需要关联同一秘密时，用 run-local 随机 HMAC
槽位；key 不写进轨迹。若没这项需求，干脆只记“secret-slot-N”。

Exporter 再扫常见密钥、PEM、cookie/header、个人路径、二进制误入与不准训练的
附件。命中后进 excluded，报告 source event id，不改 Journal。

### 12.1 文件权限与路径安全

- trajectory 根、session 目录、临时 blob、导出目录默认只许当前 OS 用户读写；POSIX 用
  0700/0600，Windows 建 user-only DACL。权限设不住，启动须告警并按配置拒绝录敏感内容。
- 所有用户给的导出名、record id、session ref 都先过单段名校验与 canonical-path containment；
  拒绝 `..`、绝对路径逃逸、重解析点/symlink 越界。
- 临时文件与最终文件都用 create-new，rename 前再核目标仍在 session 根内。
- P0 不自造应用层加密。可依赖 BitLocker/FileVault/LUKS，并把“未做应用层 at-rest
  encryption”写进威胁模型。日后若加密，密钥管理须独立设计，不能把 key 塞进同目录。
- inspect/export 默认继续脱敏；只有显式本机管理动作才许看受限原文，且不改变 Journal。

### 12.2 容量、保留与 GC

每间 session 分四笔容量：journal、referenced blobs、rebuildable indexes/checkpoints、
derived exports/record drafts。关闭时把四笔字节数与文件数写进 `session.json` 索引。

GC 次序定死：

```text
过期临时文件
  -> 可重建 indexes
  -> 可重建 checkpoints
  -> derived exports / drafts
  -> 停
```

canonical JSONL 与仍被它引用的 blob 不自动删。删它们只走用户显式的 session delete。
首版没有跨 session 物理 blob，故删除一间 session 不必扫全局引用图。

写盘前检查 workspace 配额、磁盘剩余量与 journal emergency reserve。空间不足时，先拒绝
新的副作用与大模型请求，再报告 `storage_exhausted`；不可跑完工具才悄悄丢结果。已经
发生的外部只读/异步回流若落不下，run 标 incomplete/unknown，明说复现缺口。

提供：

```powershell
lubancode trajectory usage <workspace-key>
lubancode trajectory gc <workspace-key> --dry-run
lubancode trajectory gc <workspace-key> --derived-only
```

默认只报账，不自动清 canonical。保留策略、配额与最后一次 GC 结果不进训练正文。

### 12.3 训练导出授权

事实账留在本机，不等于自动同意拿去训练。`auto_export_training` 默认 false；导出须显式
指定 workspace/session、目标格式与隐私档。manifest 写 exporter config hash、过滤规则、
授权来源与生成时间。上传、发布、同步不属 Exporter 职责，另走外部明确命令。

## 十三、配置

```json
{
  "trajectory": {
    "enabled": true,
    "durability": "process_crash",
    "power_loss_barriers": true,
    "inline_bytes": 32768,
    "capture_request_snapshot": true,
    "capture_wire_snapshot": false,
    "capture_reasoning": "reference_only",
    "capture_workspace_patch": true,
    "auto_checkpoint_events": 500,
    "auto_export_training": false,
    "queue_max_events": 4096,
    "queue_max_bytes": 67108864,
    "journal_emergency_reserve_bytes": 16777216,
    "workspace_soft_quota_bytes": 0,
    "session_blob_max_bytes": 0
  }
}
```

事实账默认开启。训练导出默认手工或后台触发，不阻塞主 turn。

`0` 表示不设产品软上限，仍须服从文件系统剩余空间。上面队列与 reserve 数字只是首版
起始值，须经压力测试校准；有效配置要进 `run.environment.captured`，免得出事后不知道
当时用了哪档。

关闭 trajectory 后，副作用工具的可恢复保证会降级。产品须当场明说，不能静默
装作仍有完整审计。

### 13.1 观测与性能门

轨迹系统至少报这些聚合指标，不把正文、路径、event id 塞进 metric label：

```text
events/bytes committed by kind and durability
record queue depth/bytes/high-water mark
commit/flush/fsync latency
blob write/hash/rename latency and failures
rejected/io_failed/incomplete/corrupt stream counts
replay bytes/sec and state-hash failures
export episode counts and exclusion reasons
```

`/doctor trajectory` 显示 active session、各 stream 健康、队列高水位、最近 I/O 错误、
磁盘余量、配额与最后一次 verify。普通状态查询只读，不为看面板再造一套账。

性能路上守四条：

- 不录逐 token delta；只录规范完成输出与必要 checkpoint。
- 大 blob 流式 hash/write，不在内存里来回复制，也不为算 journal hash 重读正文。
- timeline、relations、全文索引全是异步派生；主 turn 只等必须的 durability receipt。
- 指标、索引、训练导出慢，不得堵住 canonical commit lane。

P0 合入前建立基准：一万枚小事件、百枚 1 MiB blob、main + 四只并行 child、100 MiB
session replay、clear/resume 连切与磁盘低水位。Windows/Linux 各留基线；CI 既看绝对预算，
也看相对回退。预算数先由首轮实测定进 benchmark 配置，不能在文档里拍脑袋冒充结论。

## 十四、命令

```powershell
lubancode trajectory inspect <session-id> [--stream main|agent:<id>|workflow:<id>|node:<id>]
lubancode trajectory verify <session-id>
lubancode trajectory replay <session-id> [--stream main] [--turn <id>]
lubancode trajectory harness-replay <session-id> --stream <selector>
lubancode trajectory live-rerun <session-id> --stream <selector>
lubancode trajectory export <session-id> --format training-v1
lubancode trajectory export-workspace <workspace-key> --format training-v1
```

`inspect` 看 run/turn/request/tool 数、actor/origin/plane 分布、outcome、evidence、
replayability、blob/hash 完整性与 training eligibility。

`verify` 扫 session 内所有 JSONL，逐文件验 hash chain，再验父子边。它不调模型、
不执行工具。`replay` 默认 main 的 exact replay；点名 stream 才进 subagent 或 workflow。
真重跑另叫 `live-rerun`，命令名也分开，免得用户误会。

### 14.1 所有 slash 命令先过统一适配层

当前 `SlashCommandTable()` 登了 51 个名字；`image` 与 `notslash` 是死案，`unknown`
只走拒绝，其余命令还会随版本添减。不能在各 handler 里零敲碎打补日志。注册表须给
每条“解析后的动作”补齐这些元数据：

```text
command_name
action_name
effect_class       read_only | session_state | external_read | external_write |
                   spawn_run | session_boundary | destructive
session_policy     stay | close | open_new | close_then_open_new
durability         buffered | flush | power_loss
replay_policy      fold_state | reference_only | never_reexecute | live_only
argument_schema
redaction_policy
```

分类看动作，不只看命令名。比如 `/context` 裸敲是 read_only，`/context 256k` 是
session_state；`/provider list` 是 read_only，`/provider switch` 会改请求环境，
`/provider add` 还会写配置。

统一执行次序：

```text
parse + validate + redact
  -> control.command.requested(command_id, name, action, effect_class, args_ref)
  -> authorization / confirmation
  -> handler returns CommandOutcome
  -> zero or more typed domain events
  -> control.command.completed | failed | cancelled | rejected
```

`CommandOutcome` 至少带 status、稳定 error_code、effect refs、state hash before/after、
是否产生外部副作用、是否需要切 session。handler 不许只打几行终端字便算办完。

有外部写入、派生执行、session 切换或删除时，`command.requested` 须先 durable，随后才
动手；效果落稳后再记 terminal。只读展示可走 buffered。终端排版不进 Journal，除非
其内容本身是复现所需证据，此时存 artifact ref。

slash 原文不进 `input.received`，也不塞进 model history。它是 control event。
Exporter 默认剔除 command lifecycle；确需保留用户偏好时，只从对应 typed domain
event 取 metadata，不能把 `/think high` 造作一轮 user/assistant 对话。

普通命令的 requested/terminal 必须在同一 main stream。`clear/resume` 是例外：旧 main
写 requested 与 session terminal，新 main 在 `run.started` 之后写 command terminal，
用 qualified ref 指回旧 requested；两边同带 `boundary_operation_id`。`delete` 没有新
session，terminal 只落 `lifecycle/<operation_id>/result.json`，tombstone 再指回旧 requested。
Validator 只给这三类开口，别的命令不得跨 stream 拼生命周期。

### 14.2 命令适配矩阵

| 类别 | 现有命令/动作 | 轨迹合同 |
|---|---|---|
| 纯展示 | `help`、各类 `list/status/show/why`、裸 `config/context/skills/mcp/lsp/todos/plugins/agents/tools/sessions/peers` | 只记 command lifecycle；不改 ReplayState，不保存终端画面 |
| 请求策略 | `model`、`provider switch`、`think`、`context <window>`、`plan`、`soul`、`prompt` | 落 old/new、来源与生效边界；下一枚 request 引用新 policy hash |
| 工作面 | `worktree`、`init`、`language`、`title`、`keymap` | 状态变更与文件副作用分开；cwd、prompt、工具集变化各有 snapshot ref |
| 扩展管理 | `skill`、`plugin`、`package`、`hooks`、`memory`、`provider add/remove/set` | 读动作只记 command；写动作记目标、确认、前后 hash、备份/回滚与刷新结果 |
| 派生执行 | `agent`、`workflow run`、`goal`、`loop`、`background`、`doctor probe`、`evolve` | main 只记 dispatch/terminal refs；真正执行落各自 JSONL 或专属 subsystem run |
| 外部通信 | `send`、`peerperm`、peer 收发 | 记 peer id、message id、方向、授权与投递结果；正文按隐私策略存 ref，不冒充真人输入 |
| 上下文变换 | `compact` | 同 session 内生成并应用可验证投影；原事件不改、不抄 |
| 录制派生 | `record` | 只圈选 canonical event spans，再编 skill 草稿；不另录工具流水 |
| 会话切换 | `clear`、`resume`、`exit`/EOF | 封旧 session；clear/resume 另开新 session，exit 只封口 |
| 会话管理 | `archive`、`delete` | 先停写并核 child；workspace lifecycle 记 intent/result；delete 留 tombstone |
| 导出查看 | `export`、`copy`、`trace` | 从 replay projection 读取；输出是派生物，不回写原事件 |
| 更新检查 | `update` | 记 external_read、目标端点的脱敏标识、版本结果与错误；不得假装本地升级已发生 |
| 特殊输入 | `image`、普通 `@file` | 走 `input.received.attachments` 与 blob ref，不走 command handler |
| 非法输入 | `unknown`、参数校验失败、权限拒绝、用户取消确认 | 落 rejected/cancelled 与稳定 reason；不产生 domain effect |

`workflow`、`goal`、`loop`、`memory`、`record` 这类命令有许多子动作。实现时须给每个
子动作填 effect_class，不能拿顶层名字一刀切。新增命令若缺这些元数据，注册测试直接
失败，不能合入。

### 14.3 `/record`：从“第二只录音笔”改成“轨迹选段器”

新轨迹默认常开。故 `/record start` 不再开启另一只事实 Recorder。它只在当前 session
建立一份 selection：

```json
{
  "record_id": "record-0003",
  "session_id": "...",
  "scope": "causal_tree",
  "goal": "...",
  "variables": [],
  "acceptance": "...",
  "included_spans": [],
  "excluded_spans": [],
  "source_terminal_hashes": []
}
```

具体规矩：

- `start` 落 `record.selection.started`，记起点 event ref；目标、变量、验收口述只作
  annotation，不写成 conversation user；
- `pause/resume` 只切选段区间。canonical Journal 照常全录，暂停不制造事实缺口；
- `note` 落 `record.selection.note_added(actor=user)`，也是 annotation，不冒充提问；
- 默认 `scope=causal_tree`：main 在选段内派出的 subagent/workflow 用 qualified refs
  纳入选择，但不把 child 正文抄进 main；
- `stop` 封住 source spans 与各流 terminal hash，再由 SkillDraftCompiler 读取原轨迹，
  产出 `derived/records/<record_id>/selection.json + draft/SKILL.md`；
- 用户口述“已经验过”只能落 claimed verification。没有 `verification.recorded` 的 fresh
  evidence，起草器不得把它写成已证成功；
- `cancel/discard` 只能撤掉 selection 与派生 draft。canonical 事件既已发生，谁也不许删；
- `install` 是一次独立 external_write，须记目标 skill 目录、安装前后 hash、确认与结果；
- `clear`、`resume`、`exit` 遇见活动 selection，须先写
  `record.selection.interrupted(reason=...)`。首版不许跨 session 暗续。

现有 `WorkflowRecorder::RecordToolCall/RecordToolResult` 与 TurnSink 旁听接线应退役。
它们若继续存在，双写迟早回来。可保留命令外观，把实现换成 `RecordSelectionController`
与 `SkillDraftCompiler`。

### 14.4 `/compact`：记录投影过程，不重写事实

手动 `/compact` 与 80% 自动触发走同一状态机，只差 `trigger=manual|auto|midturn`。
一趟 compact 至少落：

```text
compact.requested
  -> compact.request.prepared
  -> compact.output.generated
  -> compact.validation.completed
  -> compact.applied
```

任一步出错，落 `failed/cancelled/rejected`，effective history 保持原样。

`compact.requested` 记 source range、触发线、用户重点说明、旧 epoch 与输入 state hash。
重点说明虽由真人输入，也只是 command parameter，不进对话。`compact.request.prepared`
记 cheap/compact 路由、规范请求 snapshot、prompt/tool/config refs；该模型调用标
`purpose=compaction`，训练默认剔除，不能冒充 main assistant。

`compact.output.generated` 存 UserContract、WorkState、分区摘要、热区 refs 与 artifact
hash。`compact.validation.completed` 记工具对完整性、活动 todo/goal/loop 守恒、大小增长、
schema 与引用可达性。只有 validation passed，才许写 `compact.applied` 并抬高 epoch。

`compact.applied` 带 old/new state hash、pre/post tokens、source event span、summary ref、
protected hot-zone refs 与 validator version。后续 model request 明写所用 epoch 与 message/
context refs。崩溃发生在 applied 之前，重放视作未生效；applied 已 durable，重放便能
恢复同一有效上下文。

训练导出默认从原始 conversation 事件编样本，不拿 compact 摘要替代原话。若以后专训
压缩器，另出 `task_kind=context_compaction` 数据集，输入、输出、validator 证据一并带上。

### 14.5 几条容易写乱的边界命令

- `/clear`：照 3.3.1 换账；command requested 与 `session.clear_requested` 落旧 main，
  新 main 先写 `run.started(start_reason=clear)`，再写跨 session 的 command completed。
- `/resume`：照 10.4 另开 session；绝不 reopen/append source JSONL。
- `/exit` 与 EOF：收口活动 turn/child/record selection，依次写 run terminal、
  `session.ended`，不另开目录。
- `/archive`：先封 session，再改 lifecycle 状态。目录可留原位，由 catalog 过滤；若坚持搬，
  须整目录原子搬，所有关系仍按 id/hash 解引用，不能存脆弱绝对路径。
- `/delete`：先在 workspace lifecycle durable 记 intent，关掉 session 全部 writer，收好
  id/末 hash/目录清单，再删整间 session 目录；删除成功后原子生成 tombstone 与 lifecycle
  result。若崩在删除与 result 之间，恢复器凭 durable intent 和目录是否存在补终态。不能
  只删 `main.jsonl` 留一地 child。
- `/worktree`：不切 session。落 old/new cwd、worktree id、git ref 与 prompt/tool/memory
  snapshot 变化；session 仍归启动 workspace。
- `/model`、`/provider`、`/think`、`/soul`、`/prompt`：不改旧 request。只改 policy state，
  下一枚 `model.request.prepared` 引用新 hash，重放按生效 seq 折叠。
- `/export`、`/copy`：一律读 ReplayState。不得再从某份旁路 history 或 SessionStore 取数。
- `/agent`、`/workflow`、`/goal`、`/loop`：命令本身留 main control 边界；执行正文照目录制
  分账。命令失败未派生 run 时，不造空 child JSONL。

## 十五、现有代码接线

### 15.1 AgentLoop

在这些边界发事件：

```text
PushUserTurn 前后
每次 provider request 准备/发送
assembler 收成 model output
工具有效入参确定
工具 started/terminal/result committed
预算、取消、异常收口
```

模型输出必须先 `Record(model.output.completed)` 成功，才许跑工具。

### 15.2 ToolTraceHub

```text
ToolTraceHub -> TrajectorySink
             -> Runtime EventSink(UI)
             -> Workflow projection
```

它不再持有 SessionStore，也不写 assistant/tool result。消息与工具事实各有明确事件，
都进同一 Recorder。

### 15.3 SessionRuntime

SessionRuntime 管：

- run 生命周期；
- turn 发号与终态；
- session 目录与 `main.jsonl` Recorder 所有权；
- 子执行 Recorder 注册表，只交句柄，不替子执行写；
- replay/resume；
- checkpoint；
- title/queue/mode 等 control 事件。

进程级 `SessionManager` 管 active SessionRuntime 与 `clear` 换账事务。它先备新目录，
再封旧账，末后原子切 active 指针。SessionRuntime 不得原地清零后复用旧 Recorder。

删除“轮末按 persisted_count_ 扫 history 追加”这条路。轮末只验状态机是否收齐，
不补抄消息。

### 15.4 ContextManager

durable history 改成 replay projection 的内存缓存。每项带 source event id：

```text
HistoryItem
  event_id
  api::Message projected_message
  origin
```

`AppendToLast()` 这类追改已提交消息的接口须收紧：

- 未提交 builder 可原地添；
- 已提交后若要加内容，落 `context.attached` 或新 message event；
- 不许静默改旧 event 对应正文。

### 15.5 其他入口

app-server、peer、scheduler、goal 都走同一 `TrajectorySink`。区别写在 trigger/origin，
不再各写自己的会话消息。

### 15.6 Subagent 与 Workflow

`AgentTool` 派工时先向 `TrajectoryDirectory` 申请 `agent_run_id + scoped sink`。子 Agent
只拿自己的 sink。父 Agent 拿不到子 sink 的写权限，只能发边界事件。

`workflow::RunJournal` 不删除领域语义。它改用公共 `JournalWriter` 与 EventEnvelope：

```text
workflow::RunJournal
  管 node/branch/join/loop/store/checkpoint 领域状态

trajectory::JournalWriter
  管 canonical JSON、local seq、hash chain、flush、receipt
```

Workflow HostExecutor 启动 node 时，从 workflow 目录申请 node JSONL 的 scoped sink。
node 执行器无权写 `workflow.jsonl`；它只回 `node_run_id + terminal hash + output ref`，
由 workflow runtime 写 node completed。

### 15.7 Slash Command 接线

`DispatchSessionSlashCommand()` 外面加一层 `TrajectoryCommandExecutor`。注册表不再只有
name/handler/console/idle 四项，还须带 14.1 的 effect metadata 与参数脱敏器。

```text
terminal/app-server command input
  -> SlashParser
  -> CommandSpec + ParsedAction
  -> TrajectoryCommandExecutor
       durable requested
       authorize/confirm
       handler(CommandExecutionContext) -> CommandOutcome
       record typed effects
       durable terminal
  -> presenter(CommandOutcome)
```

presenter 只管打印。handler 只管业务，不直接拿 `SessionStore`、`ofstream` 或
`TrajectoryRecorder`。跨 session 的 clear/resume 交 `SessionManager`；外部写入经统一
effect recorder；read_only 只返回数据。

现有接线要这样收：

- `RecordSessionWiring + WorkflowRecorder` 改成 `RecordSelectionController`，从 canonical
  refs 编 selection，不再从 TurnSink/ToolTraceHub 旁听；
- `RunCompactCommand()` 不再 `AppendCompactV2Event()`；CompactService 返回 typed result，
  executor 按 14.4 的状态机提交；
- `HandleClearCommand()` 不再 `store.Reset()` 后原地清零，由 SessionManager 完成双目录换账；
- `HandleResumeCommand()` 不再 `ResumeAt(old_file)`，而是 replay source 后创建新 session；
- `HandleExportCommand()`、`HandleCopyCommand()` 改读 ReplayState；
- `/archive`、`/delete` 从“单 JSONL 文件操作”改成整间 session 目录与 workspace lifecycle
  操作。

命令注册测试逐项枚举 `SlashCommandTable()`。除了死案，缺 effect_class、session_policy、
durability、replay_policy 或 redaction_policy 的条目一律 fail。

## 十六、测试设计

### 16.1 Contract tests

- 每种 event kind 都有 schema round-trip。
- 缺必填 id 拒绝。
- 不合法 actor/origin/plane 组合拒绝。
- canonical JSON 跨键序稳定。
- hash chain 固定 fixture 稳定。
- 不同正文同 id 冲突。
- 相同正文不同 id 照常落。
- v1 golden fixture 在新 reader 下 state hash 不变。
- 未知可忽略展示事件可 inspect；未知 stateful event 须 fail-closed。
- 不支持的 min_reader_version 明报 unsupported，不判 corrupt。

### 16.2 State-machine tests

- request 未 prepared 便 sent，拒绝。
- output 没 request，拒绝。
- tool 没 effective input 便 started，拒绝。
- tool 双终态，拒绝。
- ToolResult 指错 call，拒绝。
- 本批工具未收齐便发下一 request，拒绝。
- turn 留悬空 call 却 completed，拒绝。
- run 双终态，拒绝。
- 并行批次 position 重号，拒绝。
- 普通 command terminal 没有本流 requested，拒绝；clear/resume 缺 qualified requested，拒绝。
- stateful command completed 却没有 domain effect ref，拒绝。
- destructive command 的 durable intent 晚于外部写入，拒绝。
- command rejected/cancelled 后出现 domain effect，拒绝。
- session.ended 后又写 command 或 domain event，拒绝。
- 新增 slash command 缺 effect metadata，注册测试失败。
- 未审批副作用直接 started，拒绝；deny 后仍 started，拒绝。
- queue item 双终态，拒绝；dequeued 后另造 input id，拒绝。
- cancellation requested 后没有对应 applied/terminal，run 不能判 clean completed。

### 16.3 Durability tests

在 input、request prepared、model output、tool started、tool finished、tool result、
verification、run terminal 各处注入 I/O 失败。

断言 Runtime 停在正确边界，不偷跑下一步，不写重复事件。

尾行被截时：verify 明报；前面完整事件照常 replay；run 判 incomplete；resume 另落
recovery/control 事件，不伪造旧终态。

再注入 queue full、blob 超限、配额不足、磁盘写满、fsync 失败、rename 失败、shutdown
超时。断言 canonical event 不静默 drop，副作用不越过失败栅栏，session 落在可解释的
incomplete/closed 状态。

### 16.4 Full-turn integration

跑真实接线：

```text
external user
  -> model text + two parallel tool calls
  -> one success + one failure
  -> tool results
  -> model second request
  -> final answer
  -> verification
  -> turn terminal
```

断言：

- Journal 只有一份事实，无重复消息副本。
- user 先于首 request。
- model output 先于 tool start。
- tool result 先于下一 request。
- actor/origin/plane 全对。
- exact replay 状态 hash 与运行末态相同。
- training export 的 tool call/result 配对正确。
- 失败工具不被 exporter 写成成功。

再跑一场 main 派两只并行 subagent：

- session 下恰有 `main.jsonl + 2 个 subagent JSONL`；
- main 没有 child model/tool 正文；
- 每只 child 的 local seq 与 hash chain 独立完整；
- main dispatched/accepted 与 child started/terminal hash 双向对上；
- 导出时 child episode 只出现一次。

再跑一场带 parallel + loop + retry 的 workflow：

- `workflow.jsonl` 只见编排事件；
- 每个可执行 node attempt 各有 JSONL；
- retry 两次便有两个 node 文件；
- 纯 branch/join/loop gate 不造空 node 文件；
- workflow 恢复只打开仍未终态的 node stream。

再从一场已有 main + subagent + workflow 的 session 执行 `clear`：

- workspace 不变，前后 `session_id` 与目录不同；
- clear 命令只在旧 main 留 `session.clear_requested`，不投成 user 训练消息；
- 旧 child 全在旧目录收口，新目录不见旧 child 正文；
- 旧 main 以 `session.ended(reason=clear)` 封链，封链后写入必遭拒绝；
- 新 main 首条为 `run.started(start_reason=clear)`，反指旧 session 终态事件；
- 新 history 为空，turn/request/call/seq 不沿用旧 session 命名空间；
- 分别 verify/export 两个 session，不会自动拼成一条训练 episode。

再跑 `/record start -> turn -> pause -> turn -> resume -> subagent -> stop`：

- canonical main/child 只各有一份事实，不出现第二份 tool call/result；
- selection 只列 event spans 与 qualified refs，暂停区间仍在原 Journal；
- note、goal、acceptance 不冒充 conversation user；
- child 正文不抄进 main 或 selection.json；
- draft 可由同一 selection 确定性重编；无 fresh evidence 不写成已验成功。

再跑 manual/auto/midturn 三种 compact：

- 三路走同一事件状态机，只差 trigger；
- validation 失败时没有 applied，effective state hash 不变；
- applied 后原 conversation 事件字节不变，下一 request 引新 epoch；
- applied 前后各模拟一次崩溃，resume 得出明确且唯一的生效结果；
- 普通训练导出不把 compact 模型输出当 assistant。

最后遍历注册表各命令动作：read_only 不改 state hash；session_state 必有 old/new；
external_write 必有 intent/result；spawn_run 必有 child ref；session_boundary 必按矩阵封口或
换账。命令输出文案怎么换，都不得影响 replay hash。

### 16.5 Reproducibility 与训练门

- 同一 Journal 连 replay 两次，state hash 一致。
- 同一 Journal 连 export 两次，规范输出字节一致。
- blob 枚举排序稳定。
- Windows/Linux 路径规范策略有 fixture。
- RecordedModelBackend 遇错 request hash 立即 divergence。
- RecordedToolExecutor 遇错 call/input hash 立即 divergence。
- 成功 + fresh evidence -> success。
- 成功自述 + 无 evidence -> excluded/unverified。
- 工具失败 + 正常收口 -> failure。
- unknown side effect -> excluded。
- scheduled input、tool result、memory/hook 不冒充 user。
- reasoning 默认不进训练 messages。
- secret fixture 必进 excluded。

### 16.6 真机

至少跑：

1. 纯问答。
2. 一枚只读工具。
3. 并行工具。
4. 文件修改 + 测试验收。
5. ESC 打断。
6. provider 失败。
7. tool finished 后、result committed 前模拟崩溃。
8. 子代理 run。
9. app-server turn。
10. resume 后再开新 turn。

每场都跑 `trajectory verify`、`replay`、`export training-v1`。

### 16.7 并发、安全、容量与性能

- 同一 workspace 同时开四个进程：各有 session 目录，无共享 JSONL 争写。
- 同一 session 第二只 writer：独占锁拒绝；只读 inspect 仍可按策略工作。
- stale lock：核 PID/boot identity 后恢复；活锁绝不按 mtime 强抢。
- main + 四 child 高并发提交：各流 seq/hash 独立，无全局锁瓶颈。
- 路径穿越、symlink/reparse-point 逃逸、非法 record/session id 全拒绝。
- POSIX mode 与 Windows DACL 各有平台测试；权限设不住时行为符合配置。
- secret、PEM、cookie、低熵密码 fixture 不落原文；omission 带 replay impact。
- GC dry-run 与 derived-only 只碰可重建物；canonical 与 referenced blob 字节不变。
- 删除 session 后 tombstone 与 lifecycle result 齐全；崩在删除中途可恢复终态。
- 跑 13.1 基准，留吞吐、P50/P95/P99、峰值内存、队列高水位与磁盘放大率报告。

## 十七、实施批次

依赖次序如下：

```text
P0-1 合同/目录/Writer
       |
       v
P0-2 运行时单写口 --------> P0-4 环境/安全/容量/证据
       |
       v
P0-3 Replay/Resume
       |
       +-------------------> P0-5 Training Exporter
                                  |
                                  v
                              P0-6 真机发布门
```

开发期可用合成 session、fixture 与 test-only feature flag。产品切换却按 session 原子选路：

- 新 session 要么全走 Trajectory v1，要么启动失败；不许一半写新 Journal、一半回退旧 Store。
- 禁止 production dual-write。双写正是这场事故的根，不拿它当“稳妥迁移”。
- mid-session Recorder 失败时，run/session 标 incomplete 并停在耐久边界；不可暗开旧写口续命。
- P0-1 至 P0-5 没全过，只能算内部预览；P0-6 真机门过后，才切默认新会话。
- 回滚程序版本不改写现有 v1 Journal。旧 binary 若不支持 min_reader_version，只准明拒读取。

### P0-1 合同与最小仓

- [ ] 冻结 EventEnvelope、枚举与首批 payload schema。
- [ ] 实现 workspace_key、session 目录与原子 `session.json`。
- [ ] 实现 SessionManager 与 `clear` 换账事务、active session 原子切换。
- [ ] 实现 main/subagent/workflow/node 的 scoped JSONL 创建器。
- [ ] 实现 CanonicalJsonDump。
- [ ] 实现 BlobStore。
- [ ] 实现 Journal append、hash chain、终态封口与 session stream 索引。
- [ ] 实现 Recorder receipt 与稳定错误码。
- [ ] 为 SlashCommandSpec 补 effect/session/durability/replay/redaction 元数据。
- [ ] 实现 control.command lifecycle 与 CommandOutcome schema。
- [ ] 实现 session 独占锁、owner metadata 与 lifecycle 状态机。
- [ ] 固化 v1 golden fixtures、min_reader_version 与 unknown-event fail-closed 规则。

验收：纯合成事件可写、可验、可 replay，字节确定。

### P0-2 单一写口

- [ ] SessionRuntime 持有 Recorder。
- [ ] AgentLoop 接 input/request/output 边界。
- [ ] ToolTraceHub 改接 TrajectorySink。
- [ ] 工具 started/terminal/result 三道栅栏接齐。
- [ ] 删除 `persisted_count_` 轮末补抄消息。
- [ ] app-server 接同一口。
- [ ] AgentTool 每只 subagent 申请独立 JSONL。
- [ ] 父子文件只传边界引用与 terminal hash。
- [ ] TrajectoryCommandExecutor 包住 terminal 与 app-server 命令入口。
- [ ] `/record` 改选段器，删掉 TurnSink/ToolTraceHub 的 WorkflowRecorder 旁听。
- [ ] `/compact` 改 typed 状态机，删掉 AppendCompactV2Event 旁路。

验收：完整 turn 只写一份事实；全仓业务层无 SessionStore 直写。

### P0-3 Replay 与 resume

- [ ] 实现 validator 与 ReplayState。
- [ ] effective conversation 从事件投影。
- [ ] tool/control/evidence ledger 折叠。
- [ ] checkpoint 与高水位恢复。
- [ ] exact replay 命令。
- [ ] harness replay 的 RecordedBackend/RecordedExecutor。
- [ ] session verifier 扫目录并交叉核父子边。
- [ ] main resume 不内联已完成 child 轨迹。
- [ ] `--continue` 与 `/resume` 都创建新 session，source Journal 永不 reopen append。
- [ ] export/copy/session view 全部改读 ReplayState。

验收：运行末态与 replay state hash 相同；resume 不重号、不重跑未知副作用。

### P0-4 环境与证据

- [ ] 环境快照、git/dirty patch、工具集、system/config refs。
- [ ] 文件/命令/MCP side-effect 细账。
- [ ] verification 事件与 stale invalidation。
- [ ] outcome.assessed 引 evidence。
- [ ] user-only 文件权限、路径 containment 与 reparse/symlink 防逃逸。
- [ ] 有界队列、背压、blob/配额/磁盘 reserve 与 storage_exhausted 收口。
- [ ] usage、derived-only GC 与 workspace lifecycle/tombstone。
- [ ] recorder/replay/export 聚合指标与 `/doctor trajectory`。

验收：文件修改任务能说明“用什么环境、改了什么、拿什么验过”。

### P0-5 Training Exporter

- [ ] provider-neutral episode schema。
- [ ] success/failure/partial/excluded 四路。
- [ ] origin-aware message 映射。
- [ ] quality axes。
- [ ] 隐私扫描。
- [ ] 数据集 manifest 与确定性 fingerprint。

验收：同一 Journal 重编字节一致；无证据成功进不了 success。

### P0-6 真机与发布门

- [ ] 十类真机场景。
- [ ] app-server、terminal、peer/scheduler 同源。
- [ ] 并行 subagent 分文件真机。
- [ ] workflow parallel/loop/retry 分目录真机。
- [ ] clear 前后双 session、活动 child 收口与崩溃点恢复真机。
- [ ] record 选段、compact 三触发、resume 新目录与全命令分类矩阵真机。
- [ ] focused tests。
- [ ] full ctest。
- [ ] 故障注入与崩溃恢复报告。
- [ ] 训练导出抽样人工审查。
- [ ] Windows/Linux 权限、锁、路径与 schema compatibility fixtures。
- [ ] 并发/大 blob/大 session/低磁盘性能基准与回退门。

## 十八、代码边界验收

```text
SessionStore
  不再是新会话真账

TrajectoryRecorder
  唯一文件 writer

AgentLoop
  只发领域事实，不碰文件

ToolTraceHub
  只装配工具事实与投影，不碰文件

SessionRuntime
  管 run/turn 生命周期与 recorder 所有权

SessionManager
  管进程启动、clear/resume 换账与 active session 指针

TrajectoryCommandExecutor
  包 command lifecycle、耐久门与结构化 CommandOutcome

RecordSelectionController
  只圈 canonical refs，不旁听、不复制事实

CompactService
  生成、校验并返回上下文投影，不直接写盘

Replay
  纯读，不执行外部动作

TrainingExporter
  纯派生，不回写 Journal
```

静态检查：

```powershell
rg -n "AppendMessage\(|Append[A-Za-z]+Event\(" src
```

新会话接线里不得再有旁路写 SessionStore。

依赖方向：

```text
trajectory schema/core
        ^
runtime adapters
        ^
app / app-server / cli
```

## 十九、最终验收

| 项目 | 必须做到 |
|---|---|
| 目录归档 | workspace -> session 两层稳定；进程启动开一间，clear 再开一间 |
| Clear 边界 | 旧账封口，新账开张；不复用 ID，不继承 history，不跨目录搬 child |
| Resume 边界 | source 只读；恢复另开 session，不 reopen/append 旧 JSONL |
| 生命周期 | preparing/running/closing/closed/incomplete/corrupt/archived 转移唯一 |
| 命令总线 | 所有 slash 动作有 effect/session/durability/replay/redaction 合同 |
| Record | 只选 canonical spans；pause 不造事实缺口，draft 可确定性重编 |
| Compact | 原事件不改；生成、校验、应用分账，失败不换 effective state |
| Main 分账 | main 只写 `main.jsonl`，不内联 child 细账 |
| Subagent 分账 | 每只 subagent 一份独立 JSONL |
| Workflow 分账 | 编排一份 JSONL，可执行 node attempt 各一份 JSONL |
| 单一真账 | 每个执行流只认自己的 Trajectory Journal |
| 单一写者 | 每份 JSONL 只有一只 scoped Recorder |
| 并发 | 每流独立 commit lane；有界背压；同 session 第二 writer 拒绝 |
| 强类型 | user/model/tool/host/verifier/control 分清 |
| 稳定身份 | run/turn/request/call/event 全有 id，resume 不撞 |
| 因果链 | causation/correlation/parent/retry/compensate 可查 |
| 正确次序 | 输入、模型、工具、结果、下一请求有硬约束 |
| 完整性 | canonical JSON、blob hash、event hash chain 可验 |
| Schema | v1 golden 可读；未知关键事件 fail-closed；Journal 不原地升级 |
| 恢复 | 从 Journal replay，不靠轮末补抄 |
| 重放 | exact、harness、input、live 四档分清 |
| 环境 | 版本、git、配置、工具集、prompt、workspace 缺口有账 |
| 证据 | 工具成功与任务验收分账；证据可失效 |
| 训练 | 中立母本，四路导出，origin 不丢 |
| 质量门 | 无证据、unknown side effect、缺 blob、隐私命中不进 success |
| 安全 | user-only 权限、入盘前脱敏、路径不逃逸、训练导出须显式授权 |
| 容量 | canonical 不自动 GC；低磁盘先挡副作用；删除按整间 session 办 |
| 观测 | 队列、commit/fsync、blob、replay/export、错误状态有聚合指标 |
| 性能 | 无逐 token 落账；大 blob 流式；并发与大 session 基准过门 |
| 确定性 | 同一输入 replay/export hash 一致 |
| 真机 | terminal、app-server、工具、打断、崩溃、resume 全跑 |

这套账落成后，运行时写下来的，便是有身份、有来源、有因果、有证据的事实。
回放拿它重现，训练拿它投影。各走各路，不争一本账。
