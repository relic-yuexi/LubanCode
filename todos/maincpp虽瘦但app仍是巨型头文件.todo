# main.cpp 虽瘦，app 仍是一只藏起来的巨型编译单元

状态：待做（重构收尾）

## 结论

`src/main.cpp` 已从 6476 行降到 39 行。入口目标达成了，整场重构却还没做完。

2026-08-15 盘点：

```text
src/main.cpp                              39 行
src/app 总计                           5938 行 / 约 300 KiB
src/app/*.cpp                              0 只
src/app/interactive_session.hpp         1626 行 / 约 88 KiB
src/app/commands/settings_commands.hpp  1105 行 / 约 56 KiB
src/app/turn_runner.hpp                  814 行
src/app/cli_app.hpp                     587 行
src/app/one_shot.hpp                    299 行
```

CMake 仍只把这些终端编排源列进可执行文件：

```cmake
add_executable(lubancode
  src/main.cpp
  src/cli/console_input.cpp
  src/cli/spinner.cpp
  src/cli/record_command.cpp)
```

`main.cpp` include `cli_app.hpp`；`cli_app.hpp` 又 include `interactive_session.hpp`、`one_shot.hpp` 和各组 commands。五千多行实现仍由 `main.cpp` 这一只 translation unit 编译。门牌换了，编译边界没有立。

这便是本单要收的尾巴。不是把代码搬回 main，也不是再造一只 `app.cpp`。

## 已经做成的事

这些不用重做：

- `main.cpp` 只留 UTF-16 -> UTF-8 与 `lubancode::app::RunCli(args)`。
- live transcript、ToolDisplay、BackendStack、ToolRuntime、TurnRunner 与四组命令已有落脚处。
- ToolRuntime 已成不可复制、不可移动的拥有者，并有构造、查询、析构测试。
- Interactive 与 OneShot 已共用 ToolRuntime、BackendStack、TurnRunner 的主要实现。
- 底层 `agent/api/cli/config/memory/mcp/lsp/tools/platform` 没有反向 include `app/`。
- 2026-08-15 的 Windows Release 编译通过；`ctest -C Release` 通过，1/1 测试目标、0 失败。

## 偷懒落在何处

### 1. InteractiveLoop 只是原样塞进头文件

`src/app/interactive_session.hpp` 文件头已经明说“先原样搬家，自由函数，后续再收”。眼下仍是：

- `void InteractiveLoop(...)` 长约 1420 行。
- backend、工具、UI、session、peer、queue、recorder 全是局部变量。
- `rebuild_loop`、`persist_new_messages`、`process_line` 等仍是捕获一把引用的大 lambda。
- slash `switch` 仍有约四百行。`/clear`、`/worktree`、`/peers`、`/send` 等肥 case 亲手改状态。
- 退出时仍在函数尾手工 `SetInbox(nullptr)`、停 peer；不是对象析构守门。

TODO 原定的 `InteractiveSession` 类、`InteractiveSessionOptions`、`RunInteractiveSession()`、窄 command state 都没有出现。

### 2. app 公开头文件驮着实现

`backend_stack.hpp`、`tool_runtime.hpp`、`turn_runner.hpp`、`interactive_session.hpp`、`one_shot.hpp`、`cli_app.hpp` 与四组 command header 都有函数体。

坏处不只“文件看着长”：

- 改一条命令，也会重编整只入口 translation unit。
- 公开头拉进 nlohmann、backend、MCP、LSP、终端 painter、文件系统等大批依赖。
- commands 里有多只非 inline 自由函数。眼下没撞 ODR，只因它们几乎只被一只 translation unit include；一旦测试或别的 executable 直接 include，便容易出重复定义。
- 测试只好绕着内部实现走，无法链接一份稳定 app 编排库或窄接口。

### 3. 命令只搬函数，没有收派发与状态

解析已在 `cli/slash_commands.*`，部分 handler 也已按领域搬走。可执行 switch 还在 `process_line` 里。

- 没有 `DispatchSlashCommand()` 或等价 dispatcher。
- 没有 `CommandFlow`，仍拿裸 `bool` 表示“继续还是退出”。
- 没有 `SessionCommandState`、`WorkspaceCommandState`、`PeerCommandState`、`SettingsCommandState`。
- `/provider` 仍传一串 backend、model、catalog、prompt、rebuild_loop 参数。
- `/clear`、`/resume`、`/send` 等状态变化没有独立 handler 测试。

顶层 enum `switch` 可以留。要删的是肥 case，不是 `switch` 语法。

### 4. RunCli 搬了，参数解析没纯化

`src/app/cli_app.hpp` 仍由 `RunCli()` 一边扫 argv，一边攒局部变量、一边处理早退。

- 没有 `CliOptions`。
- 没有纯函数 `ParseCliArgs(vector<string>)`。
- 没有直接覆盖参数冲突、缺值、位置参数的单测。
- `RunCli`、初次向导、hooks 与启动装配仍同住一只实现头。

### 5. OneShot 搬了，边界仍粘 Interactive

`one_shot.hpp` 仍 include `interactive_session.hpp`，还带着从搬家时留下的 `using InteractiveLoop`。这说明共同材料没抽到共同层，OneShot 只能借整个交互头拿 helper。

OneShot 不该构造半只 InteractiveSession，也不该 include 它的实现。两边只共用：

- system prompt 装配。
- backend stack。
- ToolRuntime options 底稿。
- TurnRunner 接线。

session store、queue、peer、recorder、ask_user 仍须只归 Interactive。

### 6. 测试钉子没补齐

现有 slash 测试主要钉“输入串解析成哪个枚举”，不等于“枚举派给哪个 handler”。还缺：

- 每个 `SlashCommand` 的 dispatcher 路由测试。
- `/clear`、`/provider`、`/resume`、`/send` 状态保留/清除测试。
- InteractiveSession 真构造、真注册回调、异常/EOF/Ctrl+C 退场、真析构测试。
- `ParseCliArgs` 参数矩阵。
- Interactive / OneShot 最终 system prompt 与工具清单共同点、差异点测试。
- app 头文件 include hygiene 与链接测试。

Windows screen drivers 虽会编译，却不在 `ctest` 那一只测试里自动运行。涉及 InteractiveSession、footer、composer 的 commit 仍须手跑并留报告。

## 目标结构

```text
src/main.cpp
  -> app/cli_app.hpp
       RunCli(args)

src/app/
  cli_options.hpp/.cpp
  cli_app.hpp/.cpp
  interactive_session.hpp/.cpp
  one_shot.hpp/.cpp
  session_prompt.hpp/.cpp          # 只在真有共同材料时建
  backend_stack.hpp/.cpp
  tool_runtime.hpp/.cpp
  turn_runner.hpp/.cpp
  commands/
    settings_commands.hpp/.cpp
    prompt_commands.hpp/.cpp
    session_commands.hpp/.cpp
    workspace_commands.hpp/.cpp
    peer_commands.hpp/.cpp          # peer 肥 case 足够多时再建
```

公开头只放 options、结果、窄接口与必要类型。具体 backend、终端、MCP/LSP、nlohmann 与文件 IO 尽量留在 `.cpp`。

`main.cpp` 仍守在 80 行以内。不能为了修 header-only 又把 include 和实现倒回去。

## 怎么修

### 第一步：先立真实编译边界

按模块逐一把实现从 `.hpp` 搬到 `.cpp`。一只 commit 只拆一组：

1. backend_stack。
2. tool_runtime。
3. turn_runner。
4. 四组 commands。
5. one_shot 与 cli_app。

搬家 commit 不改函数签名、不改算法、不改文案。CMake 与测试链接跟同一只 commit 走。

不能机械搬的模板、极短 getter、类内小函数可留 header；其余默认进 `.cpp`。每拆一组，用两只独立 translation unit include 公共头并链接，专查 ODR 与漏符号。

### 第二步：把 InteractiveLoop 收成真对象

先在 `interactive_session.cpp` 里藏具体类，头文件只露：

```cpp
struct InteractiveSessionOptions;
int RunInteractiveSession(InteractiveSessionOptions options);
```

具体类可用私有 `Impl`，按所有权持有：

```text
config/model/provider state
BackendStack
ToolRuntime
AgentLoop
ContextTracker/transcript/UI state
SessionStore/title/persisted count
pending queue/peer runtime/recorder
```

把 lambda 逐只升成方法：

```text
RebuildLoop
EnsureSessionStarted
PersistNewMessages
DispatchSlashCommand
RunUserTurn
DrainPeerInbox
DrainBackgroundResults
RefreshStatus
```

一次只收一簇状态。谁拥有、谁借用、谁先死，写在成员声明旁。回调注册、peer Start、SetInbox 等开门动作交 RAII guard 或析构关门，异常与早退也得走。

### 第三步：收瘦 slash case

给四个领域窄状态，不造万能 `CommandContext`：

```text
SessionCommandState
WorkspaceCommandState
PeerCommandState
SettingsCommandState
```

handler 借引用，不持有整场资源。顶层 `switch` 每个 case 只做转发；`WorktreeAction`、状态枚举等短小内层 switch 留在所属 handler。

返回值改成明白的 `CommandFlow::Continue/Exit`。普通正文、peer 来信、subagent 介入消息不进 slash dispatcher。

### 第四步：纯化 CLI，拆开 OneShot

- `ParseCliArgs()` 不读配置、不探终端、不打印。
- `RunCli()` 只做早退、启动材料加载、Interactive/OneShot 分派。
- OneShot 不 include `interactive_session.hpp`。
- 真正共用的 prompt/tool options 才抽 helper；不可为了消重造一只包揽全场的 RuntimeGodObject。

### 第五步：清大头与旧注释

- `settings_commands` 若拆 cpp 后仍过千行，按“向导/供应商切换/普通设置”真实职责再分，不按一条命令一只文件乱切。
- `interactive_session.cpp` 目标 700-1000 行；超过 1200 行必须再查 command、peer、session store 是否仍粘在一起。
- 删搬家时期的 using、重复 include 与“main.cpp 某行”旧注释，改成现模块名。
- `PathToUtf8`、`SameFilesystemPath` 归 platform/tools 路径层，不在 workspace commands 留转发壳。

## 验收

- `src/main.cpp` 不超过 80 行；只 include `app/cli_app.hpp` 与平台入口所需小头。
- `src/app` 有真实 `.cpp`，CMake 明列；不再靠 main 那一只 translation unit 吞完 app 实现。
- `interactive_session.hpp` 不超过 200 行，不 include MCP、LSP、具体 backend、终端 painter、nlohmann。
- `interactive_session.cpp` 没有千行自由函数；主循环 60 行内能看完。
- slash 顶层 switch 只剩路由，肥 case 全退场。
- `cli_app.hpp`、`one_shot.hpp`、四组 command 头只露接口；任何两只测试 translation unit 同时 include 都能链接。
- OneShot 不 include InteractiveSession；底层各层仍不 include app。
- 改一条 settings command，不重编无关的 InteractiveSession、OneShot 与整只入口。
- Release 编译、全套 ctest、CLI smoke、Windows composer/stream/footer/markdown/fold-clear drivers 全过。
- `todos/重构maincpp.todo` 更新为已完成，旧行数与旧文件表改成最终实账。

## 每只 commit 的规矩

- 搬家与行为改变分开。
- 一只 commit 只动一层边界或一簇状态所有权。
- 不顺手改 UI、排队、subagent、跨会话协议。
- 不批量格式化，不带用户别处 TODO 的增删。
- 每只 commit 至少过 Release 编译与 ctest；碰终端接线再跑 screen driver。

## 不做

- 不把 1626 行 `interactive_session.hpp` 改名成 1626 行 `interactive_session.cpp` 便宣布完工；函数与状态也要收。
- 不造一只两三千行 `app.cpp`。
- 不上动态命令注册表。内置枚举 switch 足够用。
- 不为“面向对象”给每个纯函数套类。
- 不趁重构改外部 CLI、slash 命令、JSONL、配置格式、skill/plugin ABI。
- 不把既有大文件 `config.cpp`、`i18n.cpp`、`console_input.cpp` 混进本单。它们可另审；本单先收 main 搬家留下的 app 债。
