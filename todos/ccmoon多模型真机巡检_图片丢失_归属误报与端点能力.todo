# ccmoon 多模型真机巡检：图片丢失、归属误报与端点能力

状态：已修（2026-08-27 真机复跑全过。图片全链路落地：ImageOutput 事件 →
agent 落盘口解码验身 → <会话>/images/ 内容寻址文件 → model_image 引用
块入史（base64 不进 JSONL、不回传）；归属判定次序倒转（先落痕再判，
活列表首选零误报）；端点能力 ClassifyModelEndpoint + [Realtime] 挂标与
确认前提示；错误行带 HTTP 状态与上游 type/code。P2 查证结论见文末。）

## 一句话

`/provider switch ccmoon` 后，文本模型能答，工具也能跑；图像模型则把图片
送到了线缆上，LubanCode 却没接住。另有两桩病：当前 provider 刚列出的
模型，切换时反被说成“属别家”；Realtime 模型也混进 Responses 菜单，点了
才报一团 `upstream_error`。

HTTP 200 只说明门开了。图片能不能落盘、终端能不能看、下一轮能不能续，
还须另算。

## 巡检现场

环境：Windows PowerShell，LubanCode `0.26.66`，`ccmoon`，wire 为
`openai-responses`。先跑 `/provider refresh`，目录缓存由旧 schema 自愈到
schema 2；这项已恢复，不列 bug。密钥、完整响应、request id 均未落盘。

### 模型菜单

`/model` 真机取回 21 项。可见 `gpt-5.6-{luna,sol,terra}`、`gpt-5.4-mini`、
`gpt-4o-realtime-preview`、`gpt-4o-audio-preview`，以及
`gpt-image-1`、`gpt-image-1.5`、`gpt-image-2`。目录能列出，不等于当前
wire 能调用。

### 已跑样本

| 模型 | 场景 | 结果 | 判定 |
|---|---|---|---|
| `gpt-5.6-sol` | 只读 `CMakeLists.txt` 并找版本行 | 工具调用成功，答出第 15 行 | 通过 |
| `gpt-5.6-luna` | 回 `LUNA_OK` | 约 2.4 秒返回 | 通过；伴随错误归属提示 |
| `gpt-5.6-terra` | 回 `TERRA_OK` | 约 3 秒返回 | 通过；伴随错误归属提示 |
| `gpt-5.4-mini` | 回 `MINI_OK` | 约 2.6 秒返回 | 通过；伴随错误归属提示 |
| `gpt-image-1.5` | 生成齿轮图标 | 只说“已生成”，无图、无路径 | 失败 |
| `gpt-image-2` | 生成齿轮图标 | 只说“返回了图片；路径未提供” | 失败 |
| `gpt-image-1` | 生成锤子图标 | 只回“是”，无图、无路径 | 失败 |
| `gpt-4o-realtime-preview` | 普通文本问答 | 约 1.3 秒报通用 `upstream_error` | 当前 Responses 端点不通 |

没有实跑 `gpt-4o-audio-preview`。不可借 Realtime 的结果顺手判它死刑。

## P0：Responses 图片结果被静默吞掉

### 真机证据

先用非流式 `/responses` 探一回：HTTP 200，`output[0].type` 为
`image_generation_call`，`result` 有 935032 字符。

又按 LubanCode 同路发 SSE：HTTP 200，`Content-Type` 为
`text/event-stream`。关键次序如下：

```text
response.output_item.added          image_generation_call
response.image_generation_call.*    in_progress -> generating -> completed
response.output_item.done           result_chars=251596
response.output_item.added          message
response.output_text.delta           三段短说明
response.output_item.done           message
response.completed                  result_chars=251596
```

这不是模型没出图。图已过网。`src/api/responses/events.cpp` 原先在
`HandleOutputItemDone` 略过陌生 item；`HandleCompleted` 也只认
`function_call`。两处都不翻图片事件。会话里于是只有模型自述，找不到
`ImageBlock`、图片路径或 artifact。

本次已补止损与脱敏 fixture：

- `response.completed` 见 `image_generation_call` 便发 `StreamError`；
- 错误只说尚未接入、结果未保存，不夹 base64；
- fixture 保留真机事件次序与字段形状，图片正文缩成占位串；
- 四种 SSE 切法均须得到 `TextDelta -> ContentBlockDone -> StreamError`。

Release 重编后又跑一趟 `gpt-image-2`。约 24 秒收到终帧，终端先露出模型短句
“已生成”，随即判失败并明说结果未保存。进程退回输入态，未崩，未把 base64
打上屏。止损链路已过真机。

这层只管“不装成功”，并不算图片功能已成。

### 完整修复

- [x] 给统一输出协议添图片结果。须分清用户输入 `ImageBlock` 与模型输出，
  不要拿一只类型混过去。（ImageOutput 事件 + ModelImageBlock 引用块）
- [x] base64 边收边解或限额解码，校验 MIME、魔数、尺寸与最大字节数。
  （model_image_store：严格解码 20MiB 顶，PNG/JPEG/GIF/WebP 魔数验身，
  宽高直读字节）
- [x] 原子落到会话 artifact 目录，失败便清临时文件；文件名不得信模型正文。
  （tmp+rename，img-<sha8>.<ext> 内容寻址，同图幂等）
- [x] 终端显示缩略信息与可打开路径。无图形终端也得给清楚路径。
  （image_generation 内置工具卡收尾："已保存 <绝对路径> 宽x高"）
- [x] session/export/resume 留 artifact 引用，不把 base64 塞进 JSONL。
  （model_image 引用块；导出 markdown 给 "[模型图片] 路径 (宽x高, 字节)"）
- [x] 多图、图片加文字、只有图片、生成中断、重复终帧，各钉一册测试。
  （test_loop 五册 + test_responses_events 多图册 + 落盘模块六册）
- [x] 图片帧超限定名：responses client 见图片在途 + 单帧超限时报
  “图片帧超过 SSE 单帧上限(8 MiB)”并指路；决定：8MiB 通用帧上限
  不动，不做分段收图（HTTP chunk 无可靠图片帧边界约定，分段等于
  自造协议），非流式下载另立单。
- [x] 真机验收 `gpt-image-1/1.5/2`：文件存在、可解码、尺寸可读，且续聊
  不重放 base64。（三只各一张 PNG 全可解；续聊输入 8.2k tokens——
  若重放 base64 会是数十万 tokens；JSONL 无 base64）

## P1：当前 provider 的真机目录被静态归属压过

### 复现

1. 切到 `ccmoon`。
2. 裸跑 `/model`，菜单由 `ccmoon /models` 拉出 21 项。
3. 从这张菜单选 `gpt-5.6-luna`、`gpt-5.6-terra` 或 `gpt-5.4-mini`。
4. 切换已成功，随后却提示：该名“属 openai 家（未配置）”。
5. 首次选 `gpt-image-1.5` 又被说成“属 dmxapi 家（未配置）”。

提示与现场打架：模型正由当前 provider 列出，紧接着也由当前 provider 调通。

### 病根

`model_commands.cpp` 先以静态/用户 catalog 调 `ModelProviderHopFor`，再把这回
真机选择交给 `RememberModelChoiceInCatalog`。也就是说，最硬的证据来晚了
一步。静态目录又把公开模型名映到某家 provider，于是中转站第一次选择时
容易挨一声假警告。

### 验收

- [x] 裸 `/model` 的成功列表头一行“以下 N 项由 X 真机列出”；选出的项
  先落痕再判定，本轮零跨家提示（真机：luna/terra/mini 首选全零误报）。
- [x] 直输只说“X 家目录也收录”（真机 gpt-image-1.5 实见）；跳家只吃
  唯一已配映射，多家已配（ambiguous）留在本家提示（单测钉）。
- [x] 真机列表失败退静态清单，头一行明写“静态缓存，可能与真机不一致”。
- [x] 单测钉住（test_app_commands：静态指向 openai 未配置 + 先落痕的
  ccmoon 用户条目 → 判定 nullopt，首选即愈）。

## P1：模型可见，却未标明端点能力

`gpt-4o-realtime-preview` 出现在 `openai-responses` 菜单，选择也成功；真正
发请求才得到：

```text
{"error":{"message":"Upstream request failed","type":"upstream_error"}}
```

这只能证明本次 ccmoon Responses 路由不通。它不能证明模型本身坏，也不能
证明 Realtime API 不通。

- [x] ModelCatalogEntry 添 capabilities（provider catalog 抄来/用户自写），
  catalog/providers.json 给 openai 家 gpt-realtime-* 挂 realtime 键。
- [x] 菜单挂 [Realtime] 标（真机实见 gpt-4o-realtime-preview），确认前
  一行提醒并注明不判模型死刑、不判中转 Realtime 路由。
- [x] 错误行带 HTTP 状态与 type/code（真机：HTTP 503: ... type=api_error）；
  密钥打码、240 字节截短；本例中转未回 request id，若在错误体里会随
  摘要带出。
- [x] ClassifyModelEndpoint 三册（名字兜底/能力键/音频不误伤）+
  capabilities 解析一册 + 落盘模块与 loop 册。
- [ ] 另跑 Audio 真机后再立结论。（未跑，保持悬账；分类器对音频模型
  刻意不判 Realtime）

## P2：推理档位随模型切换的边界未说明

巡检时 provider 的 `high` 档位一路带到 Image 与 Realtime。图像请求仍被
接受，眼下不知是 ccmoon 忽略、转译，还是上游真认。此项只算疑点，不算
bug。

- [x] 查证：ccmoon 是用户自建 provider（不在内置 provider catalog），其
  模型 reasoning 档案为空 → legacy 路径 reasoning.effort 照发（单测
  test_responses_request 既有档位用例钉着形状）；真机带 high 档打
  gpt-image-1.5 仍 HTTP 200——服务端容忍，非档位生效。
- [x] ReasoningConfig.declined：内置条目按 image-generation 且无
  reasoning 能力自动推导（或 JSON 直写），四家 wire 停发；回来恢复，
  current_think 不动（builder 单测钉）。边界：落痕的用户条目会遮蔽
  内置档案（0.26.64 既有语义，方言/档位表同理），被落痕过的模型
  需在 models.json 手写 capabilities 才吃 declined——记档后续单。
- [x] 测试钉的是请求体形状（declined → reasoning/thinking 键整个缺席）；
  图像模型无 usage 拆账可观察，行为差异不可远程区分，如实记档。

## 证据与复跑

脱敏截图留在 `build/test-evidence/ccmoon-smoke/`，不进仓。完整 session 与
原始响应也不进仓。可沉淀的只有协议缩样、断言、复现步骤与统计数字。

复跑时请依次截：provider/config、真机模型单、四只文本结果、三只图片结果、
Realtime 错误、直连 SSE 事件账。截图先查密钥、路径、业务正文，遮净再留。

## 完成定义

- [x] 三只各一张可打开 PNG（1254x1254 三张 + 一张 1536x1024）；终端卡、
  JSONL 引用块、磁盘文件三账相合。
- [x] 真机 luna/terra/mini 首选零误报。
- [x] Realtime 挂标 + 确认前提醒；失败行带 HTTP 状态与 type。
- [x] fixture 只更新事件账（占位 base64 短串，非合法解码输入）。
- [x] ctest 201 册全绿（基线 200 + 新册 1）；真机回归见上。
