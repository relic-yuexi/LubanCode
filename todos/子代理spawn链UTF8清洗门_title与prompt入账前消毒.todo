# 子代理 spawn 链 UTF-8 清洗门：title 与 prompt 入账前消毒

- 状态：已收官（2026-09-04 修毕：两道门先红后绿；全量 ctest 374/377,3 败皆与本单无涉——两只 process 集成测单跑即绿(全量并行时机抖动),memory_global_gate 是主线测试/码漂移(现行 CheckGlobalMemoryHealth 已无 projects/ 报点,测试还盯着旧账),要做另立小单）
- 级别：P1（模型输出污染直接杀死子代理派工，fail closed 虽接住但活没干成）
- 类型：LubanCode 产品缺陷（外部文本编码边界）
- 现场版本：`lubancode 0.26.193`（minimax-m3 主会话）
- 现场实录（session 20260903-153004-K2725J）：同 turn 并发派 4 只子代理，"P0 键贴场景"两只（#1/#4）双双死在 spawn：
  `subagent.run.start_failed | stage=run_started | error_code: trajectory.subagent_run_started: io.canonical_failed: canonical_json.invalid_utf8: 字符串包含非法 UTF-8`
  不同名的 #2/#3 正常存活。fail closed 与字段级留证（0.26.177）全对——治本缺口在清洗。
- 不在本单：CanonicalJsonDump 的拒写行为（对，不动）；loop 已有的模型消息清洗门（SanitizeMessage，不重做）；fail closed 防线

## 一、病根

模型回的 agent 工具调用参数（title/prompt）可携带非法 UTF-8 字节（provider 流式坏字节或模型复制了脏内容）。参数直灌 spawn 链：`AgentTool::ExecuteDispatch` → `SpawnSubagent` → run.started payload → `CanonicalJsonDump` 才发现坏字节、整场拒绝——子代理死在起跑线。loop.cpp 的"编码关口"（SanitizeMessage）只洗模型消息正文，**工具调用参数这条路没有门**。

## 二、修法（两道门，入口为主、账前兜底）

1. **入口消毒（主刀）**：`AgentTool::ExecuteDispatch` 解析 title/prompt（及一切将进 spec/台账的字符串参数）后，立即过 `platform::SanitizeExternalText()`（保合法 UTF-8 片段、只替换坏字节——与 web/MCP/search 同一合同）。清洗后字节即可入账，canonical 不再拒。
2. **账前兜底（保险）**：`SpawnSubagent` 写 run.started 前对 payload 字符串再做一次同款清洗（防其他调用方绕过工具入口——workflow/peer 派工路）。兜底清洗与入口清洗幂等，重复过无副作用。
3. 诊断：入口清洗**替换了字节**时落一行 warning 日志（"子代理参数含 N 处非法 UTF-8 已消毒"，task_label 截断版）——用户可感知模型吐了脏东西，不静默。

## 三、验收

- 单测：title/prompt 带 `0x80` 孤立续字节/截断多字节/GBK 段——spawn 成功、run.started 落账合法 UTF-8、warning 计数正确；干净参数零 warning 零改动（逐字节不变）。
- 复现景：构造与现场同形状（minimax 式坏字节 title）——修前 fail closed（红）、修后子代理正常起跑（绿）。
- 全量 ctest 零回归；0.26.177 的 fail closed 册全部不退（防线仍在，只是好数据不再被误杀）。

## 四、不做什么

- 不放松 CanonicalJsonDump 校验
- 不在 CanonicalJsonDump 里做清洗（账层只认不洗——脏数据该在进账前治）
- 不改 fail closed 语义

## 五、验收记录（2026-09-04，先红后绿）

- 落点：入口 `AgentTool::ExecuteDispatch`（title/prompt 解析即过 `platform::SanitizeExternalText`，替换时落 warning：处数+字段+清洗后首行截断版）；账前兜底 `TrajectorySessionLedger::SpawnSubagent`（run.started 前 payload 字符串同款再洗，幂等，出手不静默）；顺手把前台/后台两处 task_label 的 `substr(0,120)` 裸砍换 `Utf8PrefixBoundary`（砍进多字节腰本身就是坏字节源）。诊断计数口 `platform::CountInvalidUtf8Sites` 新增。
- 测试册：`tests/unit/trajectory/test_subagent_spawn_utf8_gate.cpp`（真 ledger+LogSink 截获，与整合册同构接线）。
- 先红（修前同册跑）：复现景 fail closed（`REQUIRE_FALSE(result.is_error)` 红，即现场形状）；坏字节 title 三形态册 `json.exception.type_error.316` 直穿（TaskSpecHash 的 dump 抛，正是 title 无门的账）；兜底册 `SpawnSubagent` 直灌坏 label 被 canonical 拒。2 过 3 红。
- 后绿（修毕同册跑）：5/5 册、107 断言全过——三形态 spawn 成功、run.started 合法落账（VerifySession 过）、warning 计数对（title/prompt 各一行、处数如实、兜底不重复告警）、干净参数零 warning 零改动（task_ref 与子代理任务书逐字节不变）。
- fail closed 册不退：`test_subagent_spawn_integrity.cpp` 8/8、`test_subagent_trajectory_integration.cpp` 3/3（修前修后各跑一遍皆绿）。
- 全量 ctest：374/377。3 败与本单无涉（见状态行）。
- 留白：GBK 段在 Windows 走 ACP 整段转回原文，断言按合同（出口合法+坏字节换 U+FFFD+合法片段保留）不钉具体字形；入口门只洗 title/prompt——其余字符串参数（agent_type/isolation/template）全在枚举校验后头，坏值本来就拒，没门可钻。
