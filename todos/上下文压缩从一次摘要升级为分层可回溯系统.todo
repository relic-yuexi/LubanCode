# 上下文压缩从一次摘要升级为分层、可回溯系统

状态：待做（架构升级，2026-08-15 按 Codex 源码与 Claude Code 官方资料核过）

## 一句话

现版 compact 是“一次把整份 history 交给模型，换回一段摘要”。短会话够用，长会话便靠运气。

下一版要拆成两层：先用确定规则收掉重复工具账，再按任务阶段分块归纳。磁盘全账一字不丢，模型眼前只摆工作摘要、证据索引与最近热区。摘要若漏了目标、约束、未完事项或关键证据，当场拒收，旧 history 不动。

## 先答那个根本问题

不论这里说的是一百万 token，还是上千轮历史，都不可能无条件“一口吞完再总结”。

一轮摘要请求也得占上下文：

```text
压缩输入 = 旧 history + 压缩指令 + 固定上下文
压缩输出 = 摘要正文
```

旧 history 若已贴满模型窗口，压缩指令与输出便无处落脚。压缩模型窗口若比主模型还小，更是压根送不进去。

能一次压，只因同时满足三条：

- 压缩在窗口吃满前触发，预先留了输出与协议余量。
- 压缩模型装得下整段旧 history。
- 服务端另有专门 compact 能力，接收并返回替换历史。

有一条不满足，就得分块。先收工具冗余，再按任务阶段切段，各段各写一份带证据号的小结，末了归并。不能把一百万 token 塞给十万窗口模型，还盼它凭空知道没见过的八九成内容。

## 查到的 Codex 做法

本机版本为 `codex-cli 0.147.0`。npm 包只带启动壳与原生程序，细节须看 OpenAI 的开源仓库。

Codex 眼下有两条路：

### OpenAI provider 走服务端 compact

开源代码会把 OpenAI provider 送往 Responses compact 路径。服务端回一份 replacement history，内部压缩算法没有公开，不能假装知道它用了分块、向量或 LSH。

客户端仍会再收一遍账：

- 丢掉服务端结果里可能陈旧、重复的 developer 指令。
- 保留真正的用户消息、摘要与可用 assistant 消息。
- 丢掉旧 tool call、tool output、reasoning 等过程项。
- 从当前会话重新注入最新初始上下文，免得压缩后继续背旧权限、旧人格或旧环境。
- compact 请求仍放不下时，把工具输出换成固定截断说明，直到请求能进窗口；调用 id 与成败信息仍在。
- 记录压缩前后 token、摘要 token、缓存 token、耗时、触发原因与成败。

这说明一件事：服务端 compact 也不是“只写一段漂亮话”。替换历史、指令刷新、工具清账、指标记账，各有一道工序。

### 本地摘要路保留用户原话

本地 compact prompt 写的是“给下一只模型的交接摘要”。模型答完后，Codex 不只留摘要，还从新往旧收集真实用户消息，最多留约 20K token，再把摘要放在末尾。旧摘要不会当普通用户消息重复收。

若 compact 请求撞上窗口上限，本地路径会逐项移走最老历史再试。这能救急，却也说明：等历史吃满再总结，必然先丢东西。

Codex 还会提示：长线程与多次 compact 会降准确率。开源 issue 里也有人报告多轮“摘要再摘要”会渐渐失忆。此事不能只靠改 prompt 遮过去。

## 查到的 Claude Code 做法

本机版本为 Claude Code `2.1.220`。官方文档把外层流程说得更直白：

- `/compact` 用结构化摘要替换对话历史；自动 compact 临近窗口时做同一件事。
- `/compact focus on ...` 可指定本次重点。
- `/autocompact <tokens>` 可改触发点；默认点位随模型与配置走，不写死一枚百分比。
- system prompt 与 output style 不在 message history 里，压后不变。
- 项目根 `CLAUDE.md`、无路径限制规则与 auto memory 从磁盘重新注入。
- 带路径条件的规则、子目录 `CLAUDE.md` 会先丢，等再读匹配文件时重载。
- 已调用 skill 正文会重注入，但有每只 5K、合计 25K token 上限，旧 skill 先掉。
- 摘要请求沿用当前 extended-thinking 设置。
- fallback model 的窗口若小于主模型，Claude Code 不拿它做 compact，免得总结前先截历史。
- 大读文件交给子代理，主会话只收摘要与少量元数据。

Anthropic 的工程文章又补了一句关键细节：Claude Code 在 compact 后会带上最近访问的五个文件；旧工具原文可丢，结构化笔记与子代理隔离另管长程状态。官方把“清掉深处 tool call/result”称作最轻、最稳的一种压缩。

Claude Developer Platform 另有自动 compaction 示例：运行器在下一次 API 请求前暂停，插入摘要请求，取回 `<summary>` 后用它替换整段历史，再继续工具循环。示例能自定阈值、摘要模型与摘要 prompt。这个 SDK 示例不能硬说成 Claude Code 内部实现，却可证明分工：触发、摘要、替换、续跑由运行器掌握，模型只负责写摘要。

## LubanCode 眼下的实情

现有链路已经打通，并非空壳：

```text
ContextTracker 最近一次 usage >= context_window 的 80%
-> 下一条外层用户消息发送前触发 Compact()
-> 整份 loop->History() 一次送给 compact_model
-> 模型按六栏写摘要，最多输出 4096 token
-> 摘要不少于 40 个 UTF-8 码点便收下
-> archive 并入最后一轮 user 消息
-> 最后一轮原文照留
-> 写 compact 事件，ReplaceHistory()
```

现版做对了这些事：

- compact 失败不改活 history。
- session JSONL 仍是追加式全量事件账；compact 只改后续模型要看的形状。
- `/resume` 能按 compact 事件重建活状态，`/export` 仍能看到完整流水与压缩点。
- 摘要有任务目标、事实、决策、文件、命令、未完事项六栏。
- 压缩前后 system prompt 不动。
- archive 与最后一轮合并，避开 Anthropic 相邻 user 角色报错。
- 最后一轮 tool use/result 成对保留。
- 子代理已有独立上下文，主会话只接最终回流。这本就是一层天然分片。
- 项目记忆每轮重算 system suffix，不混进 history。这也合乎“稳定资料重注入”的路数。

可短板也很实：

### 1. compact 自己可能装不下

`Compact()` 直接发送整份 history，没有分块，没有先清大工具结果，也没有按 compact 模型窗口算输入预算。主模型是一百万窗口、`compact_model` 只有二十万窗口时，必败。

触发点只看上一回请求 usage。长工具循环中间不会重算自动 compact；须等下一条外层用户消息才动手。若本轮一连读了许多大文件，下一次模型请求可能先撞墙。

### 2. “不少于 40 字”不是质量校验

模型写够四十字，哪怕漏了用户最新要求、当前 diff、失败测试与未回答问题，现版照收。六枚栏目头也没逐栏解析，没有证据回指，没有未完事项守恒。

### 3. 工具账只会截，不会收

各工具虽有输出上限，`TrimHistory()` 也会整轮丢中段、截长 tool result，可它不懂：

- 同一文件同一版本读了三回，其实只须留一份。
- 同一搜索换了两种写法，却命中同一批行。
- 十次失败测试里，前九次日志大同小异；真正该留的是故障如何变化与最终结果。
- 某文件后来改过，旧读取不能同新读取混作一份。

### 4. 字符账有两把尺

`context.cpp` 里所谓 chars 实际累加 `std::string::size()`，对中文算的是 UTF-8 字节。那里估 token 用 `/3`；`session_commands.cpp` 报 compact 前后又用 `/2`。状态触发靠 provider usage，手工报告靠另一套粗估。眼下不致命，做分块预算时却会切歪。

### 5. 多轮 compact 会反复蒸馏

第二次 compact 会把第一次 archive 当普通历史再总结。原始证据虽还在 JSONL，摘要器却不回去读，只会“摘要上一份摘要”。轮数一多，早期缘由、失败路线与用户口气便越磨越薄。

### 6. 硬裁剪会静默降级

自动 compact 失败后，主请求仍可靠 `TrimHistory()` 救场。它保第一轮与最近三轮，中间换成“早前对话已裁剪”；长工具结果则截尾。这道保险该留，可屏上须明说发生了有损硬裁，不能让用户以为语义 compact 已成功。

## 目标架构

不要把 compact 当一枚函数。把它当一条上下文编译流水线：

```text
L0  全量事件账与工具原件          永久、可回放，不送整份给模型
L1  规范化事件账                  用户意图、工具对、文件版本、测试证据
L2  无损结构压缩                  哈希去重、覆盖关系、结果外置、热区选取
L3  探索阶段摘要                  每段带 evidence_refs，可从原账核对
L4  当前工作状态                  目标、约束、决定、改动、测试、待办
L5  最近热区                      最近若干 token 原文，按协议完整保留

发给模型 = 最新稳定指令 + 当前工作状态 + 阶段摘要 + 最近热区 + 可取回证据引用
```

原则定死：

1. 全量 session 是事实源。compact 不删除真账。
2. 先做可证明的结构压缩，再让模型做语义摘要。
3. 相似只能帮忙找候选，不能直接判等。
4. 最新用户要求、活动计划、未完副作用与关键失败证据不得只靠摘要记忆。
5. 每份摘要都带来源事件号。没来源的“事实”按模型猜测处理。
6. tool use 与 tool result 是原子组。要留一块留一对，要换语义事件便整对换。
7. compact 提交须原子化。校验不过，旧工作 history 原样在。

## 第一层：先建规范化事件账

现有 `api::Message` 适合上 wire，不适合判断哪些内容重复。应从 session 事件派生一层中立账，不替换原 JSONL：

```text
UserIntent       用户原话、约束、纠正、验收
AssistantClaim   决定、解释、结论
ToolExchange     tool use + tool result 原子对
FileObservation  path + revision + range/symbol + content hash
EditEffect       patch、目标文件、前后 hash、成败
CommandEvidence  command + cwd + repo state + exit + 关键输出
TestEvidence     suite/case + 状态 + 失败签名 + 日志引用
TaskState        plan/todo 项与状态变化
CompactEpoch     来源范围、摘要、热区、证据引用、指标
```

每项须有稳定 `event_id`。文件类再带 `workspace_id`、规范相对路径与内容版本。Git 内优先用 blob id；未提交文件用内容 hash。没有版本，便不许把两次读取当同一事实。

## 第二层：工具结果怎么无损收拾

### 精确去重先走

不用模型，也不用向量。先靠规范键与内容 hash：

```text
read_file = canonical_path + file_fingerprint + range_or_symbol + output_hash
search    = normalized_query + scope + options + repo_fingerprint + result_hash
web_fetch = final_url + validator/etag + content_hash
test      = normalized_command + cwd + repo_fingerprint + exit + failure_signature
```

同键同 hash，只留一份正文。后来的事件写：

```text
same_as=e123
seen_count=3
last_seen=e187
```

模型仍知道“查过三回”，却不用再看三份相同文件。

### 覆盖关系另算

- 后一次读同一文件、同一版本，范围完全盖住前一次，前件正文可退成引用。
- 文件版本变了，前后两份都不得判重。可把旧观察标成 superseded，但依赖旧版作出的决定仍须留。
- 搜索结果集合相同，查询词不同，可合成一枚探索事件，保住两条 query 与一份结果。
- 连续测试只留“首次关键失败、失败签名变化、最后一次失败、最终通过”的全文。中间相同日志退成计数。
- 编辑失败、回滚、再编辑，可把机械 patch 收成一条尝试史；最终 diff、失败原因与任何未回滚副作用须钉住。

### 大结果外置

长 tool result 不该在每轮请求里来回搬。把正文放进按内容寻址的 artifact：

```text
artifact_ref
content_hash
tool_call_id
mime/type
bytes/tokens
head/tail preview
retrieval_hint
```

工作上下文只留摘要与引用。需要核查时，再从 session 原账或 artifact 取回。第一期只改“发给模型的视图”，不急着改 JSONL 存法，免得伤 `/resume` 与 `/export`。

### 副作用工具绝不跳执行

命令文本一样，不代表本次可以不跑。cwd、环境、文件树、时间与外部系统都可能变。

去重只收历史展示，不准仅凭“上次跑过”跳过本次工具调用。写文件、删文件、发网络请求、启动进程这类有副作用动作，更不能拿相似度作缓存。

## LSH、MinHash、SimHash 放在哪里

这路子有用，却只能坐第二排。

- 精确 hash：判完全相同，首选。
- MinHash：找集合型近重复，如搜索结果路径集、诊断项集合。
- SimHash：找大段近重复日志、网页正文。
- embedding：按语义找相关证据，适合召回，不适合删除。

LSH 只产一张“疑似重复”名单。随后还须核：

- 关键行是否不同。
- exit code、错误码、文件 hash 是否相同。
- 首尾、数字、路径、堆栈顶与失败签名是否一致。
- 两件结果是否来自同一 workspace/revision。

两份日志九成九相同，只差一行 `expected 2, got 3`，那一行往往最值钱。故而相似阈值再高，也不准直接删。第一期甚至不必上 LSH；先把规范键、精确 hash 与覆盖关系做扎实，收益已经很大。

## 第三层：把反复探索收成“阶段”

模型多次翻同一处，通常不是一堆孤立工具调用，而是一段探索：

```text
episode: 查明 compact 为何失效
  e101 搜入口
  e104 读 compact.cpp
  e109 读 context_tracker.cpp
  e115 跑 test_compact
  e118 得出病根与证据
```

阶段边界优先从显式信号取：

- plan/todo 项开始、完成、改向。
- 用户插入新要求或纠正。
- 工作目标从“定位”转为“修改”、从“修改”转为“验证”。
- 文件集合与主要符号明显换域。
- 最终答话或阶段性结论落下。

分不出时，再用时间邻近、路径集合重叠与语义相似作辅佐。切块不得劈开 tool 对，不得把一枚未结束副作用拦腰斩断。

关闭的 episode 可写局部摘要。活动 episode 留在热区，免得边做边总结，把下一步所需细节提前磨掉。

## 第四层：真正的分块 compact

先算压缩模型的实用输入预算：

```text
compact_input_budget =
  compact_model_context_window
  - compact_system_and_protocol
  - requested_summary_output
  - safety_headroom
```

不能拿主模型 `context_window` 冒充。compact 模型窗口未知或小于所需输入时，须分块或换回主模型；不得默默截旧史。

流程如下：

1. 对冷历史跑结构压缩，得到事件账与 artifact 引用。
2. 钉住必须原样保留项，划出最近热区。
3. 按 episode 与 tool 原子边界切块。每块不超过 compact 输入预算。
4. 各块独立产出 `EpisodeSummary`，每条事实带 `evidence_refs`。
5. 将局部摘要归并成 `WorkingState`。归并输入若仍过大，继续分层，不硬塞。
6. 用确定规则校验目标、约束、待办、文件状态与测试证据。
7. 生成 `CompactEpoch`，原子替换工作 history，追加 session 事件。

这才是能吃上千轮历史的路。它不是盲切固定 50K token；应先守任务段与工具对，再用 token 预算落刀。

### 防止“摘要复印摘要”

每次 compact 都从规范化事件账与最近原件做增量，不把上一份 prose summary 当唯一来源。

可保一份滚动工作状态，却须定期 rebase：重新从本次任务相关 event 与 artifact 构建，不让第三次摘要只读第二次、第二次又只读第一次。何时 rebase 可按 compact 次数、摘要漂移或任务阶段结束触发。

## 哪些内容必须钉住

下列内容优先原样或结构化保留，不交给自由摘要随手取舍：

- 最新一条外层用户消息。
- 用户尚未撤回的明确约束、偏好与验收句。
- 当前 plan/todo 及每项状态。
- 未完成 tool call、后台进程、确认、ask_user 与外部副作用。
- 当前未提交 diff 的文件清单、前后 hash 与关键 hunks 引用。
- 最新失败测试全文引用与最后一次通过证据。
- 当前工作目录、分支/worktree、模型与权限边界。
- 用户指出“前面答错了”的纠正。
- 下一步要执行的准确动作。

“最近”按 token 预算算，不按固定一轮或三轮算。一轮可能只有十字，也可能拖着五万 token 工具结果。

## 工作摘要须有机器骨架

不要只存一段 Markdown。至少同时产一份机器 manifest：

```json
{
  "schema": 2,
  "epoch": 3,
  "goal": {"text": "...", "evidence_refs": ["e12"]},
  "constraints": [{"text": "...", "evidence_refs": ["e12", "e44"]}],
  "decisions": [{"text": "...", "why": "...", "evidence_refs": ["e71"]}],
  "files": [{"path": "src/agent/compact.cpp", "fingerprint": "...", "state": "modified"}],
  "tests": [{"command": "...", "status": "failed", "evidence_refs": ["e155"]}],
  "open_items": [{"text": "...", "status": "in_progress", "evidence_refs": ["e160"]}],
  "failed_approaches": [{"text": "...", "reason": "...", "evidence_refs": ["e93"]}],
  "next_action": {"text": "...", "evidence_refs": ["e160"]},
  "hot_from": "e151",
  "source_digest": "..."
}
```

Markdown 供模型读，manifest 供程序验。二者从同一结构渲染，别让模型各写一份互相打架。

## 摘要怎么验

四十字门槛可留作最外层防呆，不能再当验收。

提交前逐项检查：

- 必填栏目齐不齐，JSON/schema 能不能解析。
- 最新用户目标与明示约束是否都在。
- 压缩前 `in_progress/pending/blocked` 项是否仍有去处。
- 每条文件、命令、测试、决定是否能找到 evidence ref。
- 引用的文件 fingerprint 是否仍是当前版本。
- tool use/result 是否仍成对，或已整对换成语义事件。
- 有没有把失败写成通过，把猜测写成已证实。
- 压后 token 是否真下降；省下的空间若小于摘要本身，便不值得替换。

可再开一次模型审阅，却只能作加分项。硬约束仍由程序核，不能叫第二个模型替第一个模型盖章便算完。

## 自动触发不能只看 80%

80% 可作默认参考，真正判定应看“下一步会不会放不下”：

```text
projected = measured_context
          + recent_turn_growth_p95
          + pending_tool_result_budget
          + required_output_reserve
```

当 `projected >= effective_window`，在下一次模型请求前收历史。工具循环里每次回填结果后都须重算，不只等下一条外层用户消息。

另设软触发：某个 episode 已关闭、重复工具账很多、预计可省一大截时，可提前做结构压缩或预计算局部摘要。软触发不得打断用户正在看的流，也不得卡在 tool use/result 中间。

compact 后设目标水位，而不是“刚好低于上限”便算完。例如压到窗口的某个安全区，给后续几轮留下真余量。具体比例交给模型目录与实测，不在代码里到处散常数。

## 可借鉴的预计算

Claude Code 的 telemetry 有 compact precompute reuse 命中/未命中口径。这说明摘要可在后台先算，正式触发时复用；自定义 focus、hook 或新历史会使缓存失效。

LubanCode 可晚些再做：

```text
episode 关闭
-> 后台预算局部摘要
-> 记 source_digest
-> 真 compact 时 digest 未变便复用
-> 用户加 focus 或来源变了便重算
```

第一期不做后台预算。先把同步链做准，免得又添一套并发账。

## 与现有 session、memory 怎么接

### session 继续当全量真账

现有 append-only JSONL 是极好的地基，不推倒。新增 `compact_v2` 事件即可：

```text
schema / epoch
source event range + source digest
working summary + manifest
hot_from / pinned event ids
artifact refs
pre/post tokens
structural_saved / semantic_saved
model / trigger / focus / passes / duration
validation result
```

旧事件一条不删。`/resume` 读到 v2 便重建工作视图；`/export` 仍按原账导出，并在压缩点写出摘要版本与范围。老 compact 事件照读，向后兼容。

### memory 不替 compact 背锅

项目记忆管跨会话稳定知识，compact 管本任务工作状态。不要把临时 plan、当前 diff、失败测试塞进 memory，也不要拿 memory 当丢失 history 的回收站。

项目根规则、稳定 memory 与当前环境应在 compact 后重新注入。路径规则若靠读文件触发，须像 Claude Code 那样明定何时失效、何时重载。

## 对 provider 的要求

LubanCode 同时走多套 wire，须用中立 IR 先 compact，再渲染 provider 消息。

- Anthropic：严格守 user/assistant 交替与 tool use/result 配对。
- Responses：若 provider 有原生 compact 能力，可增 capability 后走远端；返回物仍须过滤陈旧指令并重注入当前上下文。
- Chat Completions：没有远端 compact 便走本地分块摘要。
- 任一路都不准把 provider 私有 opaque item 硬转成假文本后再丢语义。
- compact fallback model 窗口不得小于本次 chunk 所需窗口。Claude Code 这条值得照抄。

远端 compact 若是黑盒，session 中须记 implementation、provider 与返回摘要/替换历史的可展示部分。不能把服务端 opaque state 冒充可审计事实。

## 用户该看见什么

保留现有 `/compact [重点]`，再补：

```text
/compact --dry-run       只看能省多少、哪些会钉住，不改 history
/compact --full          从原始事件账重建，不沿用旧摘要
/autocompact auto|off|<tokens>
/context                 增加 raw/working/hot/cold/reclaimable/next-trigger
```

压缩完成只报几项实数：

```text
compact #3 · 184k -> 46k
结构去重 61k · 语义归纳 77k · 热区 28k · 固定上下文 18k
保留 14 条约束 / 6 项待办 / 3 份失败证据
```

若走硬裁剪，另打一条醒目警告：丢了哪段、截了哪些工具结果、该用什么命令重建。用户须知道模型眼下究竟还看得见什么。

## 观测账

至少记这些：

- trigger：manual / pre-turn / mid-turn / projected-overflow / recovery。
- implementation：local-single / local-hierarchical / remote。
- pre/post token 与各层占用。
- exact duplicate groups、superseded observations、offloaded artifacts。
- chunk 数、reduce 层数、compact 模型、摘要 token、耗时。
- validation failure 原因。
- precompute hit/miss 与失效缘由。
- compact 次数、距上次 compact 新增 token、重压缩旧摘要所占比例。
- 后续模型重新读取相同文件的次数，作为“摘要没保住工作集”的反向指标。

最后一项很要紧。压后模型若立刻把刚读过的五个文件全读一遍，压缩率再漂亮，也未必算赢。

## 分期落刀

### 第一期：先堵会丢账的洞

- 为 compact 单独算模型窗口与输出余量。
- 在每次模型请求前检查 projected overflow，接上 mid-turn 安全触发。
- 统一 token 估算口径；名字叫 chars 的字节账改名或改准。
- 摘要改成可解析 manifest，补目标、约束、待办与证据守恒校验。
- 保留最新用户消息、活动 plan 与最近热区，不再只留“最后一轮”。
- compact 失败仍原 history 不动；硬裁剪须显式告警。

这期先不做 LSH，不做 artifact store，不做后台预计算。

### 第二期：无损工具压缩

- 建规范化事件与稳定 event id。
- tool use/result 原子分组。
- 文件 observation 加 fingerprint。
- read/search/test 做精确 hash 去重与 supersession。
- 长结果在工作视图中换 artifact ref。
- `/compact --dry-run` 展示可回收量与钉住项。

### 第三期：分阶段、分层摘要

- 建 episode 边界。
- 单块装不下时 map/reduce。
- 局部摘要与最终 WorkingState 都带 evidence refs。
- 新增 `compact_v2` 事件与完整回放。
- 定期从原始 event rebase，阻断摘要递归失真。

### 第四期：近重复与预计算

- 用 MinHash/SimHash 只找候选，再做关键差异核验。
- 评估 embedding 召回证据，不拿 embedding 判删除。
- episode 关闭后后台预算摘要，靠 source digest 复用。
- 用真实长任务评估阈值与收益，达不到再关掉，不为算法名留功能。

## 主要落点

- `src/agent/compact.cpp/.hpp`：从单次摘要拆成计划、结构压缩、分块摘要与校验。
- `src/agent/context.cpp/.hpp`：统一 token/预算，硬裁剪改成显式 recovery path。
- `src/agent/session_store.cpp/.hpp`：稳定 event id、`compact_v2`、manifest 与回放。
- `src/agent/loop.cpp/.hpp`：每次模型请求前检查预算，接 mid-turn compact 安全点。
- `src/app/interactive_session.cpp`：自动触发、失败提示、指标落盘。
- `src/app/commands/session_commands.cpp/.hpp`：`--dry-run`、`--full`、真实前后账。
- `src/cli/context_tracker.cpp/.hpp`：projected usage、有效模型窗口、触发来源。
- `src/tools/agent_tool.cpp`：子代理回流加证据/文件元数据，继续隔离大原文。
- `tests/test_compact.cpp`、`tests/test_context.cpp`、`tests/test_session_store.cpp`：补分层与回放守护。
- `docs/sessions-and-context.md`、`docs/query-data-flow.md`、`docs/configuration.md`：写清有损边界与模型窗口。

新增类不必一口气铺满。可先在 `compact.*` 内建纯函数与 manifest；等职责真长开，再拆 `context_events.*`、`compact_planner.*`、`compact_validator.*`。别为了图好看先造七层空壳。

## 验收题

### 超长与窗口

- 造一份一百万 token 或等价体量历史，compact 模型只有较小窗口；系统须分块完成，不得把整份一次发送。
- 主模型窗口大、compact 模型窗口小；须改走分块或明确拒绝，不能静默截断。
- 工具循环中途突然回填大结果；下一次模型请求前能触发，不等下一条外层用户消息。
- compact prompt、输出 reserve 与固定上下文都算进预算。

### 去重与版本

- 同文件、同 fingerprint、同范围读三次，只留一份正文与三次引用。
- 文件改一字再读，不得同旧版合并。
- 两份日志只差一行错误，LSH 可判候选，最终必须保住差异行。
- 相同命令在不同 cwd、不同 repo state 下不得判作同一证据。
- 副作用命令即使完全相同，也不能因历史去重而跳执行。

### 语义守恒

- 最新用户要求、明确禁止项、活动 todo、当前 diff、最后失败测试与未回答问题，压后全在。
- 摘要漏一项 pending，validator 拒收，旧 history 不变。
- 摘要声称测试通过，却找不到 exit=0 证据，拒收。
- 连续 compact 五次后，早期关键决定仍能从原始 event rebase 回来，不靠摘要套摘要。
- 用户纠正旧决定后，工作状态只认新决定，同时保留纠正来路。

### 协议与回放

- 三种 wire 下 tool use/result 都不拆对，不生相邻非法角色。
- compact 取消、超时、流错误、坏 JSON、校验失败，均不半替换 history。
- `/resume` 重建的工作视图与压前提交结果一致。
- `/export` 仍含原始事件与每次 compact 边界。
- 老 session、老 compact 事件照常读。

### 是否真有收益

- 重复读文件与日志占大头的任务，结构压缩率、总压缩率都单列。
- 压后继续完成任务的成功率不得只看摘要相似度；要跑真实续作题。
- 统计压后一到三轮的重复读取量、错误回滚量与遗忘约束数。
- 与现版“一次摘要 + 最后一轮”做同轨 A/B。若压得更多却更常返工，便不算升级。

## 这期不做什么

- 不上向量数据库来替代全量 session。
- 不用 LSH 判语义等价。
- 不删原始工具结果来换磁盘体积。
- 不把当前任务状态塞进项目 memory。
- 不承诺还原模型隐藏推理。只保用户消息、可见回答、工具证据、决定与任务状态。
- 不假称已知 OpenAI 服务端 `responses/compact` 内部算法。公开源码只露客户端边界。
- 不拿“压缩率高”当唯一目标。能接着把活干对，才算成。

## 资料

核查日期：2026-08-15。

### Codex

- OpenAI Codex compact prompt：<https://github.com/openai/codex/blob/main/codex-rs/prompts/templates/compact/prompt.md>
- 本地摘要与 replacement history：<https://github.com/openai/codex/blob/main/codex-rs/core/src/compact.rs>
- Responses 远端 compact、旧指令过滤与工具结果收缩：<https://github.com/openai/codex/blob/main/codex-rs/core/src/compact_remote.rs>
- compact history 原子分组：<https://github.com/openai/codex/blob/main/codex-rs/core/src/compact_remote_history.rs>
- 刷新 compact 后 developer 指令的合并记录：<https://github.com/openai/codex/pull/10574>

OpenAI 面向用户的 Codex 文档没有公开服务端 compact 的切块、摘要 prompt、压缩编码或复原算法。上文只把开源客户端能证实的行为写成事实，服务端黑盒不作猜补。

### Claude Code 与 Anthropic

- Claude Code context window 与 compact 后重注入：<https://code.claude.com/docs/en/context-window>
- `/compact`、`/autocompact` 命令：<https://code.claude.com/docs/en/commands>
- 模型窗口、自动阈值与 compact fallback 限制：<https://code.claude.com/docs/en/model-config>
- `/compact` 请求形状与 prompt cache 失效边界：<https://code.claude.com/docs/en/prompt-caching>
- compact 前后 token、耗时与后台预摘要复用 telemetry：<https://code.claude.com/docs/en/monitoring-usage>
- Anthropic context engineering：<https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents>
- Claude Developer Platform compaction：<https://platform.claude.com/docs/en/build-with-claude/compaction>
- 自动 compaction cookbook：<https://platform.claude.com/cookbook/tool-use-automatic-context-compaction>

## 收尾一句

compact 不是把旧话说短。它要把一地脚印收成地图：哪条路走过，哪处碰壁，哪扇门还没开，都得有记号。原地只剩一段顺口文章，念着虽好，活却接不上。
