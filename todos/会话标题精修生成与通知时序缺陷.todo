# 会话标题精修生成与通知时序缺陷

- 状态：待修
- 现场源码：`0.26.172`，HEAD `f4e35996`
- 日期：2026-09-02
- 类型：LubanCode 产品缺陷（交互时序——标题通知压到下一条用户输入后）
- 回归来源：`ec8b22df` 将标题精修从首问回合内挪到主回合收口后，避开 Trajectory 同 stream 的 turn 重叠；发起时序修了，空闲收货唤醒没接
- 范围：`src/app/interactive_session.cpp`、`interactive_session_assembly.cpp`、`interactive_session_controller.hpp`、`session_title_refiner.*`、`src/cli/console_input*` 及相应测试
- 不在本单：改标题提示词、改 cheap 路由、放宽一条 stream 同时只能有一只 open turn、重做会话标题账

## 一、现场

第一轮回答已经收口，终端画出耗时线和下一枚提示符。用户输入第二问：

```text
— Worked for 4m 25s —
> 没有看懂你说的这些？
标题已设为: 检查Token账本与Prompt审计解决状态 · cheap:gpt-5.6-sol
```

这行标题看着像第二问触发，实则仍是首问的精修结果。标题内容没串，出现的位置串了。

影响：

1. 用户会把标题提示误认成第二问的反馈。
2. 第二问尚未开跑，屏上先横插一行旁路结果，打断问答节奏。
3. 精修即使早已完成，也会一直躺在 worker 槽里，非得等下一次输入、EOF 或别的主循环机会才落账、改名、记 usage。
4. 代码注释仍说“不等整轮收尾”“结果通常在主回合跑着的时候就备好了”，与现状相反，往后排障容易看走眼。

## 二、真实时序

现状不是“模型偶尔慢”，而是控制流写死了：

1. `RunSessionTurn()` 在 `interactive_session.cpp:710` 调 `BeginSessionTitle(content)`。
2. `BeginSessionTitle()` 先落本地标题；第二层精修不启动，只把首问存进 `pending_title_refinement_query_`（`:255-258`）。
3. normal 主回合完整跑完，主循环下一圈在 `:1007` 调 `PollSessionTitleRefinement()`。
4. `PollSessionTitleRefinement()` 发现 pending，才调 `StartTitleRefinement()`；随后立即 `return`（`:333-337`）。这一圈只发货，不收货。
5. 主线程走到 `ReadLine()`（`:1172-1174`），停住等键。后台标题线程即便已经完成，也不会主动叫醒会话主循环。
6. 用户提交下一句话，`ReadLine()` 返回；应用层先在 `:1178` 再调一遍 `PollSessionTitleRefinement()`，后在 `:1213` 才处理用户输入。
7. 第二次 Poll 取走完成结果，在 `:362` 打出“标题已设为”。于是标题行恰好压在刚提交的第二问下面。

两层错位须分开看：

- **生成晚**：为守 Trajectory 状态机，精修从“首问到达”推迟到“首个主回合收口”。这是 `ec8b22df` 的有意取舍，不能贸然挪回去。
- **通知更晚**：精修完成后没有空闲唤醒，只能借下一次用户输入收货。这是本缺陷的直接根因。

## 三、已有能力

不用另造终端轮询器。库里已有 `IdleWakeCoordinator`：

- `interactive_session_assembly.cpp:713-723` 已把子代理完成、后台命令完成挂成 wake source。
- `console_input_composer.cpp:1307-1324` 每 100ms 问一次 `IdleWakeHook`；composer 为空且有系统事件时，收掉旧底栏，让 `ReadLine()` 以空串返回。
- 主循环醒来后，先排干系统事件，再重画提示符。后台任务已经靠这条路做到“不等用户再敲一行”。

标题精修没有注册 wake source；`SessionTitleRefiner` 也没有只读的 `Finished()/Ready()` 查询口。它只有 `Busy()`，而完成待取时仍算 busy，不能拿来做 wake 条件，否则会提前唤醒或空转。

## 四、修法

### P0：把标题完成接入空闲唤醒

1. 给 `SessionTitleRefiner` 加线程安全、无副作用的完成查询口，例如 `Ready()`：只在 shared 槽存在且 `done=true` 时返回 true；不 join，不移动 outcome，不清状态。
2. 在会话装配层把标题完成注册进 `idle_wakes_`，与 subagent、background_tasks 并列；subscription 由控制器持有，析构次序照现有 token 规矩走。
3. 标题精修仍在首个主 turn 收口后发，守住一 stream 一 open turn；本单不以“看起来更快”为由破坏 `model.request.prepared -> sent -> output -> turn.terminal` 账序。
4. composer 为空时，标题完成须让 `ReadLine()` 在 100ms 拍里让位。主线程调用现有 `PollSessionTitleRefinement()`，记 usage、对 generation、落 `control.title.changed`、同步 peer 名称，再打印通知。
5. 精修失败、迟到、人工 `/title` 抢先、`/clear`、`/resume` 等路仍安静丢弃；wake 只负责叫醒，不改采纳合同。

### P0：收紧通知边界

1. 用户尚未输入草稿：标题通知可单独成行，随后重画提示符。
2. 用户已经敲了半句：不得抢走 composer，不得清草稿，不得移动光标后丢字。沿用 IdleWakeHook 的“草稿非空不让位”规矩。
3. 用户恰在标题完成时提交：允许先在主线程采纳标题，**不许**再把裸通知插成“第二问的下一行”。可选做法须在实现前定死一种：
   - 把自动精修改名做成非打断式 panel toast/status 事件；或
   - 本次只静默落账与改名，等下一个清楚的空闲边界再报。
4. 管道/重定向不走逐键 composer，不能宣称 100ms 唤醒。它仍靠正常泵点收货；EOF 前尽力排干，绝不为标题阻塞退出。

### P1：把编排从一只 Poll 里拆明白

目前 `PollSessionTitleRefinement()` 同时管“pending 发货”和“finished 收货”，首圈命中 pending 便 return。可拆成：

- `StartPendingTitleRefinementAfterTurn()`：只在主 turn 已封口的边界调用；
- `DrainFinishedTitleRefinement()`：只管主线程收货、记账、采纳与通知；
- `HasFinishedTitleRefinement()`：给 idle wake 做只读判断。

名字写清，注释也跟着改。删掉“不等整轮收尾”“主回合跑着时就备好”这类旧话，改写成真实合同：本地标题首问即成；模型精修在首个主 turn 收口后起飞；完成后由空闲唤醒收货。

## 五、测试

### 单元测试

- [ ] `SessionTitleRefiner::Ready()`：未启动=false、运行中=false、完成待取=true、`TakeFinished()` 后=false。
- [ ] 并发可见性：worker 写完 outcome 后才立 done；主线程见 Ready 后 Take，标题、usage、generation 全齐。
- [ ] wake source：标题 running 不醒，finished 醒；失败 outcome 也要醒来收账，收完不再醒。
- [ ] generation 翻号、人工标题抢先、clear/resume 后的迟到结果仍不采纳。

### 交互时序测试

用可控假标题后端和可控 `ReadLine`/composer 驱动器钉四幕：

- [ ] 第一轮收口后才启动精修；主 turn 尚开时不得出现 `state.turn_overlap`。
- [ ] composer 为空，精修完成后不喂任何用户按键；250ms 内主循环醒来，标题事件落账，提示符重画。
- [ ] composer 里已有半句，精修完成不抢输入；继续打字、退格、提交，原文逐字不丢。
- [ ] 第二问提交与精修完成竞态重复跑，屏上绝不出现“第二问\n标题已设为”这副误导排版。

### 账与回归

- [ ] 一次成功精修只记一套旁路 request/usage/output/title 事件；wake 不得造成重复 Take、重复 usage 或重复 `control.title.changed`。
- [ ] 精修失败仍记真实 usage；不改本地标题，不刷错误车轮。
- [ ] `test_session_title_refiner`、`test_session_title_account`、console input composer、IdleWakeCoordinator、i18n 相关册全绿。

## 六、真机验收

1. 新开会话，首问让 normal 跑 30 秒以上；确认本地标题仍在首问开头落下，精修只在主回合封口后起飞。
2. 第一轮答完后双手离键。标题模型完成时，不输入第二问；标题须自行落地并重画提示符。
3. 第一轮答完立刻输入一段多行草稿，标题中途完成；草稿、光标、历史、查看态一字不乱。
4. 在标题完成临界点按 Enter，重复二十次；不再出现本单现场排版。
5. 分别跑人工 `/title`、`/clear`、`/resume`、EOF、cheap 超时、cheap 报错；代数、取消、usage、退出时长照旧。
6. 查 Trajectory：主 turn 与 title_refine 旁路 turn 不重叠，事件次序可验证，无 `state.turn_overlap`。

## 七、实施批次

- [ ] P0：补 refiner Ready 查询；标题完成挂 IdleWakeCoordinator；空 composer 自动收货。
- [ ] P0：定死草稿非空与提交竞态的通知策略；补交互时序回归册。
- [ ] P1：拆开发货/收货职责；校正过时注释与命名。
- [ ] 验收：focused 单测、全量 CTest、Windows 真终端六幕；逐项留下命令、退出码与原始失败。
