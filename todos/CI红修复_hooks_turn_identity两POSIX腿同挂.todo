# CI 红修复：hooks_turn_identity 两 POSIX 腿同挂

- 状态：已修（2026-09-04 两笔全落——78f9389b 修 hooks_turn_identity、6b7db9cd 修 app_server_browser，均夹具病非共性层；WSL 红→修→绿全过。剩"下轮 CI 三腿全绿"待 push 后 CI 实录销，**验证过再删档**）
- 级别：P0（CI 三平台门，两条腿一起红）
- 现场实录：macOS-clang 与 ubuntu-gcc 两腿同红 `integration.hooks.hooks_turn_identity`（0.26.188 落的 Hook 身份集成册，Windows 腿绿）；ubuntu-gcc 另单红 `unit.app_server.app_server_browser`（单腿，需复现定性——app_server 系历史上有 -j8 抖动前科，CI 串行也红则是真病）。
- 线索：册内 POSIX 分支（WriteHookScript .sh + cat）本身看着无害；两腿同挂指向共性——嫌疑：hooks/dispatcher 在 POSIX 的进程起法/回读路径（0.26.188 只在 Windows 验过）、钩子超时在 CI 冷机上的余量、或 loader 对 `sh "path"` 命令形态的解析差异。
- 修法：
  - WSL 复现 Linux 腿（build/wsl_repro.sh 复现场，复用 Windows 侧 _deps 源），拿到失败输出定位根因后修——别瞎猜瞎改。
  - app_server_browser：WSL 同场复现定性——抖动则标记 CI 串行复跑观察，真病则修。
- 验收：WSL 复现红→修→绿；下轮 CI 三腿全绿。
- 销账实录（2026-09-04）：
  - hooks_turn_identity（78f9389b）：两处夹具病——①记账粘行（stdin payload 不带尾换行，POSIX cat 字节直通粘成一行，json::parse 抛错），cat 后补一枚换行；②表态 JSON 被 sh 剥引号（echo 未加引号双引号遭 quote removal），照 test_hooks_dispatcher 章法 POSIX 单引号裹整串。WSL 修前 3/4 案红、修后 4 案 64 断言全绿；merge 后复验仍绿。
  - app_server_browser（6b7db9cd）：真病非抖动（CI ubuntu 腿 SegFault 64.75s 同症复现，修前 10 轮 9.3~66.5s）。两处夹具病——①轮询循环 `lock_guard` 作用圈吞掉 `sleep_for`，主线程抱锁睡，glibc 非公平 mutex 下 sidecar 工作线程 `note()` 饿出 12~14.4s，慢机过 60s 收齐门即红；②CHECK 红后早退拆栈，handler 账本钉在测试栈上，在飞工作线程摸之即 SIGSEGV。修：取值一瞬持锁、睡在锁外；账本挪 `shared_ptr` 按值捕获。修后 16 轮全绿钉死 0.75s，-j8 三轮 12/12，Windows 全量 382/382（本树口径，合并 main 后 387）。两腿同治：macOS 腿 43s 通过那跑其实同病灶（变慢未过线）。
- 不在本单：不看不改 CI 工作流配置（除非根因在 yml）。
