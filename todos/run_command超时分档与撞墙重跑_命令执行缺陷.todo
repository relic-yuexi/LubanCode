# run_command 超时分档与撞墙重跑：命令执行缺陷

- 状态：待修（2026-09-02 主人在自家交互会话派工实测发现）
- 现场版本：`lubancode 0.26.169`
- 日期：2026-09-02
- 类型：LubanCode 产品缺陷（工具层）
- 范围：`run_command` 的默认超时、超时报错文案、（备选）工具层重复超时拦截
- 关联：《Agent派工任务书模板与环境附录注入_含隔离房基线查单.todo》（同场发现的派工层毛病，另行立单）
- 不在本单：模型推理能力本身（只修工具能给的结构性辅助）

## 一、现场

子代理跑 `cmake --preset debug`：

```text
run_command(cmake --preset debug)
  ⎿ Error: 命令执行超时(超过 120000 毫秒),已强制终止。

run_command(cmake --preset debug)
  ⎿ Running...
```

两个病：

1. **默认 120 秒不配构建类命令。** 首次 configure 要几分钟起步（FetchContent 若卡网更是死局）；120000ms 一刀切，grep 够用、cmake 必死。
2. **撞墙原样重跑。** 超时被强杀后，模型不加时限、不换后台、不查卡网，原样再跑同一命令——注定再死一遍，纯烧 turn 烧 token。

## 二、修法

### P1：超时默认按命令分档

- 识别构建/configure/测试类命令（cmake、msbuild、ninja、ctest、gradle、cargo、npx 等前缀），默认放宽至 10~15 分钟；其余维持 120s。
- 工具描述明教：长命令传 `timeout_ms` 或走 `run_in_background` + 轮询。

### P1：超时报错带引路

- 现文案只有"命令执行超时，已强制终止"。加三条引子：超时——考虑：① 加大 `timeout_ms` 重跑 ② `run_in_background` 后台跑 ③ 首次构建检查依赖下载是否卡网（离线环境先备 _deps）。
- 目标：模型撞墙后拐弯，不原样重跑。

### P2（备选，实测文案引导不够再做）：工具层重复超时拦截

- 同命令同参数连续两次超时，第二次直接拒绝并要求先改策略（换参数/换方式/说明理由）。

## 三、验收

- 首次 configure 类命令默认不再 120s 挨杀（真机跑 `cmake --preset` 一把过）。
- 人为构造超时（短 timeout 跑长命令）：报错含三条引路；模型下一轮改策略而非原样重跑。
- 既有 run_command 用例零回归（显式传 timeout 的行为不变）。
