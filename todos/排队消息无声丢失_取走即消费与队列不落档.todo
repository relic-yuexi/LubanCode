# 排队消息无声丢失:取走即消费、队列不落档、退场清账只留淡字

状态:待做(2026-08-22 用户报障:316 砖死那场会话里排队的指令"不见了")

## 现场

用户在一场每回合 316(请求体非法 UTF-8)的会话里,趁回合跑着排了条指令进
queue。之后指令没了下文——不见 `[用户排队消息]` 注入,不见自动发送回显,
resume 载回来队列也是空的。具体走的哪条岔路待用户对号(见"待确认"),
但四条丢失路径在代码里都是实打实的。

## 账本(2026-08-22 翻码实况)

- 队列本体:`SessionSteeringQueue`(src/cli/queue_model.hpp:99),会话级、
  线程安全、**只活内存**。会话存档(session_store)不记队列,resume 载回
  的队列恒空。
- 进队:回合跑着时用户键入,目标可指 main 或某只子代理。
- 出路一(轮次边界注入):`reapply_peer_inbox` 里 `loop->SetInbox` →
  `TakeDeliverable(Main())`(interactive_session.cpp:973),工具结果攒完、
  下一请求未发的安全点合成一条 user 消息注入。
- 出路二(收尾自动发送):会话泵 `TakeFirstDeliverable(Main())`
  (interactive_session.cpp:3747),回合自然收尾后取队头当下一轮用户消息,
  带 `> 原文` 回显。
- 清账口:`/clear` 与会话退场 `TakeAllForDisposal`(interactive_session.cpp:
  3663),全数倒掉,只打一行淡字"未送达"。

## 四条丢失路径

1. **取走即消费,回合失败不退还**:出路二把消息从队列拿走,若随后那轮
   请求失败(316/网络错),消息已出队。落没落进 history 取决于 loop 的
   追加时机——用户视角就是"发过一次石沉大海",没有重试没有退还。
2. **队列不落档**:排了没送走就 /exit 或崩掉,存档里无痕,resume 天然的
   空队列。退场那行"未送达"一晃而过。
3. **退场清账**:/clear 与析构直接倒掉,告知只有一行淡字。
4. **子代理目标死了**:条目标失败留原位"等用户处置"——不丢但也不送,
   容易被当成丢了。

## 待确认(用户对号)

- 排队后见过 `> 原文` 自动回显吗?见过 = 路径 1;没见过 = 路径 2/3。
- 排给 main 还是子代理?子代理 = 路径 4。
- 丢之前 /exit 过吗?退出瞬间有没有一瞥"未送达"?

## 候选修法(定药前先对号)

- 路径 1:取走后若回合以请求失败收场,消息还回队首(带"已试过一次"
   标记防死循环);或至少在错误文案旁明写"你排队的消息没送达,已回队"。
- 路径 2:队列落进会话存档(新事件行类型 queue,与 compact/title 同列),
   resume 重建队列;脏关机(无退场清账)也天然自愈。
- 路径 3:清账告知升级——列出未送达条数与首条预览,淡字换醒目样式。
- 路径 4:失败条目给重投/改投 main 的处置键(面板已有条目区,补个动作)。

## 验收草案

- 回合失败后队列不空、消息可追(回队或明示未送达)。
- 排队 → /exit → resume,队列还在,自动发送照走。
- /clear 与退场的未送达告知看得见(条数+预览)。
- 既有 queue 单测(test_queue_model.cpp 等)全绿,另补上述三景回归。
