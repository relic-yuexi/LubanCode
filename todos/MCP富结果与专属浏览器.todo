# MCP 富结果与 LubanCode 专属浏览器

状态：待开工（2026-08-28）

## 一句话

先把 MCP 的 `text/image/audio/resource_link/resource/structuredContent` 完整接进
LubanCode，再用一只常驻 Browser MCP 驾驭 Playwright。浏览器保有独立 profile、
标签页、Cookie 与下载账。WebKit 只是可选引擎，不等于浏览器架构；Windows 内嵌
窗口另列 WebView2 后续项，不拦 Browser MCP 主链交付。

## 这张单要解什么病

当前 MCP 工具能发现、能调用，结果却只剩一根文本：

```text
MCP tools/call
  -> result.content[]
  -> 只拼 type=text
  -> Tool::Result.content:string
  -> api::ToolResultBlock.content:string
  -> 下一轮模型
```

`src/mcp/client.cpp` 遇见非 `text` 块，只追加：

```text
[不支持的内容类型: image]
```

浏览器即便截出一张 PNG，LubanCode 也只会把这句占位话递给模型。图像字节、MIME、
尺寸、资源 URI 与结构化结果都在 MCP 边界丢了。

另一头，现有 process plugin 每次调用起一只短命进程。它没有握手，没有常驻 server，
也没有 JSON-RPC。浏览器却要留住标签页、登录态、DOM、下载与弹窗。硬塞 process
plugin，点一下便重启一次浏览器，路子不通。

## 已确认的源码事实

- `src/mcp/client.cpp` 当前只拼 `type=text`。
- `src/tools/tool.hpp` 的 `Tool::Result` 只有 `std::string content` 可回模型。
- `src/api/types.hpp` 的 `ToolResultBlock` 也只有文本与 `is_error`。
- `src/agent/loop.cpp` 将工具结果按字符串估 token、裁剪、追踪并回填历史。
- `src/runtime/plugin_process.hpp` 明定 process plugin 一调用一进程，无常驻服务。
- `src/mcp/transport.hpp` 的 MCP 子进程活到会话结束，适合浏览器状态。
- `src/app/tool_runtime.cpp` 已有 `StartMcpServers -> RegisterMcpTools` 装配链。
- 当前 MCP protocol version 固定为 `2024-11-05`，没有版本协商账、资源客户端、
  `tools/list_changed` 或多模态结果投影。
- 当前 MCP server 返回图片并不等于模型真能看图。还须过中立类型、provider wire、
  模型能力与上下文预算四关。

## 目标

- MCP 工具结果不丢块、不乱序、不拿占位文本冒充原数据。
- 文本、图片、音频、资源链接、内嵌资源与 `structuredContent` 各有中立表示。
- 不认多模态的 provider/model 仍能收到清楚的文本降级，不发送必败请求。
- 浏览器在整场会话中常驻；一次打开的标签页，下一次工具调用还能接着操作。
- 浏览器使用 LubanCode 专属 profile，不偷拿用户日常 Chrome/Edge/Safari profile。
- `webkit` 与 `chromium` 可配置。Windows 首发不把 WebKit 写死成唯一引擎。
- 浏览器结果同时照顾模型与人：模型拿语义快照；人可看截图、下载和当前 URL。
- 取消、超时、进程退出、页面关闭、导航换页各有稳定终态，不吊死会话。
- 图片、资源、Cookie、密钥、下载文件各守自己的安全边界。

## 非目标

- 首期不造通用桌面浏览器，不补书签同步、扩展商店、密码管理器、打印、历史中心。
- 首期不把浏览器画布嵌进终端 UI。
- 首期不自动绕验证码、替用户输密码、确认付款、发帖、删数据或上传任意文件。
- 首期不把 Playwright WebKit 说成 Safari。它是 Playwright 维护的 WebKit 构建。
- 首期不让 Browser MCP 自由读取整台机器。上传与本地资源须走明确授权路径。
- 首期不把所有插件迁成 MCP。Lua/C ABI 小函数仍走短路。
- 首期不同时造第二套常驻 process-plugin 协议。常驻服务统一走 MCP。

## 架构定案

### 1. Plugin 与 MCP 各管什么

```text
Plugin 包
  ├─ Skill：教模型何时、怎样用
  ├─ MCP server：提供有状态工具
  └─ 可选 UI/资源：供宿主展示
```

LubanCode 现有 Lua/C ABI/process plugin 继续叫“本地工具插件”也可，但文档须说明：
它们是运行时扩展，不等于标准 MCP。将来若做统一安装包，plugin 应当管发现、版本、
依赖、Skill 与 MCP 声明，不再发明一套浏览器 RPC。

### 2. 浏览器是常驻 actor

一份浏览器状态只能有一位主人。Browser MCP 内设 `BrowserSession` actor，所有页面
动作排进同一事件队列。不得让两只 `tools/call` 同时抢一个 page，也不得把 Playwright
对象跨线程乱递。

```text
Agent Loop
  -> McpTool
  -> mcp::Client
  -> Browser MCP stdio
  -> BrowserSession actor
  -> BrowserContext
  -> Page / Locator
```

### 3. 富结果只留一份真账

不能同时养一根“真文本”和一组“真内容块”，日后两边必会走散。目标形状：

```cpp
struct ToolResultPayload {
    std::vector<ToolContentBlock> content;
    std::optional<nlohmann::json> structured_content;
};

struct Tool::Result {
    ToolResultPayload payload;
    bool is_error = false;
    // outcome/error_code/details/effect_summary/... 诊断字段照旧
};
```

`TextProjection()`、终端摘要、旧 provider 降级文本都由 `payload` 现算。不得反过来
拿投影文本还原图片或结构化数据。

为压住改动面，可先给 `Tool::Result` 加显式文本构造器，让现有
`Tool::Result{"text", false}` 自动生成一枚 `TextContent`。迁完调用点后，删掉以
`std::string content` 为权威字段的旧路。

### 4. 不复用 `ModelImageBlock`

`ModelImageBlock` 记“模型生成图片”。MCP 工具返回截图，来源、生命周期、审计与重放
都不同。另建 `ToolImageContent`/`ArtifactRef`，不可借旧类型省几行代码，日后混账。

### 5. 专属来自 profile，不来自 WebKit 三个字

专属浏览器的身份由这些东西组成：

- 独立 user-data/profile 目录；
- 独立 Cookie、localStorage、IndexedDB；
- 独立下载与 artifact 目录；
- 一场会话独占的 browser process/context；
- 明确的域名、上传、下载与凭据策略。

引擎可取 `webkit`、`chromium`。Windows 默认先用 `chromium`，兼容面和排错工具更全；
用户显式选 `webkit` 时真起 WebKit。此默认值须经本机站点矩阵后再冻结，不凭喜好定。

## P0：先补 MCP 富结果主链

### P0.1 中立内容类型

- [ ] 在 `src/tools/` 定义 provider-neutral 的 `ToolContentBlock`。至少覆盖：
  `TextContent`、`ImageContent`、`AudioContent`、`ResourceLinkContent`、
  `EmbeddedTextResourceContent`、`EmbeddedBlobResourceContent`。
- [ ] 公共字段收 `annotations` 与 `_meta` 时要分账。`annotations` 可供 UI/投影参考；
  `_meta` 默认只透传/审计，不直接塞进模型正文。
- [ ] `ImageContent` 保存 MIME、已校验字节引用、宽高、SHA-256；不要长期把大段 base64
  挂在会话 history 对象里。
- [ ] `AudioContent` 先能无损接住与落 artifact。模型投影可后开，不能先丢字节。
- [ ] `ResourceLinkContent` 保存 `uri/name/title/description/mimeType/size`。
- [ ] 内嵌资源分 text/blob 两类；blob 必须解码、验帽、落 artifact 后只留引用。
- [ ] `structuredContent` 用 `optional<json>` 保存。空对象 `{}` 与字段缺失须分得清。
- [ ] 保留 content block 原顺序。`text -> image -> text` 不得归并成两堆。
- [ ] 给所有内容类型写相等比较、序列化、反序列化与稳定摘要函数。

### P0.2 改造 `Tool::Result`

- [ ] 给 `Tool::Result` 添显式构造器与工厂：`TextResult`、`ErrorResult`、
  `PayloadResult`。停止依赖聚合初始化的字段次序。
- [ ] 将 `content` 字符串迁为 `payload.content`。旧内置工具先经文本构造器过桥。
- [ ] `details` 仍只放诊断附注，不拿它偷运图片或 `structuredContent`。
- [ ] `result_artifact` 继续表示“大结果卸载”；须说明它与内容块内 artifact 引用的区别。
- [ ] 提供唯一 `TextProjection(policy)`：
  - text 原样拼接；
  - image 投成带文件名、尺寸、MIME、artifact id 的短句；
  - resource link 投成标题 + URI；
  - embedded text 可按字节帽内联；
  - blob/audio 只投元数据与 artifact；
  - structuredContent 只在没有兼容 text 时按稳定 JSON 投影一次。
- [ ] `TextProjection` 不得静默吞未知块。未知类型返回稳定错误或明确占位，并在 trace 留码。

### P0.3 改造 API 消息

- [ ] 将 `api::ToolResultBlock` 改成富结果，不再只认 `std::string content`。
- [ ] 更新 `EstimateMessageTokensForPreflight`：文本按文本估；图片按 provider 模型预算；
  artifact 引用按短文本估；不可拿 base64 长度粗除四后直接当视觉 token。
- [ ] 更新 context trim。裁文本不能顺手删图片与结构化结果；超预算时按明确策略卸载
  artifact、缩快照或拒绝发送。
- [ ] 更新 prefix hash/equality。图片引用须混入 MIME、SHA、尺寸；结构化 JSON 用稳定序列化。
- [ ] 更新会话保存、恢复、导出、memory extract。恢复后不得把图片冒充普通用户上传图，
  也不得丢掉工具调用配对。
- [ ] 更新 trace：正文只存小文本投影与摘要；base64、Cookie、页面全文不进 trace。

### P0.4 MCP client 完整解析

- [ ] `ToolInfo` 接住 `title`、`outputSchema`、`annotations` 与版本摘要；模型工具定义仍只
  暴露该暴露的字段。
- [ ] `CallTool` 逐块解析 `text/image/audio/resource_link/resource`。
- [ ] 校验每种块的必填字段与类型。坏一块时整次结果按协议错收口，不拿半真半假的
  内容继续跑。
- [ ] base64 严格解码。拒绝坏字母、坏 padding、声明尺寸与实字节不合。
- [ ] MIME 先过 allowlist，再按魔数复核常见图片。`image/png` 装一份 EXE 字节须拒绝。
- [ ] 解析 `structuredContent`。若工具声明 `outputSchema`，客户端须校验；不合时给稳定
  `mcp.output_schema_mismatch`。
- [ ] 兼容旧 server：只有 text 的 `2024-11-05` 响应照旧工作。
- [ ] 不直接把协议常量从 `2024-11-05` 改成最新便算完。客户端列支持版本，读取
  initialize 响应，钉协商结果与不兼容错误。
- [ ] 为未知 content type 留原 type 与安全摘要，报 `mcp.unsupported_content_type`。
- [ ] `isError=true` 时也允许富 content；错误截图、诊断资源不得被粗暴丢掉。

### P0.5 Artifact 落盘与安全帽

- [ ] MCP 二进制结果先落会话 artifact store，再进历史。路径按内容寻址，不信 server
  给的 filename。
- [ ] 图片单块、单次调用、单回合与整场会话分别设字节帽。
- [ ] embedded text 设字符帽；超限落 artifact，只回摘要。
- [ ] URI 不自动抓取。`file://`、`http://`、自定义 scheme 只是一根引用，另走授权读取。
- [ ] `file://` 若将来可读，先 canonicalize，再验工作区/下载区 allowlist；禁止 `..` 逃逸。
- [ ] 日志、trace、错误文案不得打印 base64、Cookie、Authorization、表单密码或完整 DOM。
- [ ] 清理策略写明：临时会话删什么，持久会话留什么，用户怎样手工清空。

### P0.6 四家 wire 的投影矩阵

不要假定每家都能把富工具结果原样送回。每家单独定：

| Wire | 文本 | 图片 | structuredContent | 不支持时 |
| --- | --- | --- | --- | --- |
| Anthropic Messages | 原生 tool_result 文本 | 核官方形状后接 | 文本兼容投影 | 明报降级 |
| OpenAI Responses | function_call_output | 核当前接口后接 | 稳定 JSON 文本 | 明报降级 |
| Gemini | function response | 核 inline data/part 形状 | 原生对象或投影 | 明报降级 |
| Chat Completions | tool message 文本 | 不先假定支持 | JSON 文本 | 明报降级 |

- [ ] 每个 serializer 都写“原生支持 / 降级 / 拒绝”三态，不在调用点四下猜。
- [ ] provider/model 明报纯文本时，图片只留 artifact + 文本说明，不发必败请求。
- [ ] 能看图的模型，图片内容要与对应 `tool_use_id` 保持语义相邻。
- [ ] 多张图、图文交错、错误结果带图，各写 fixture。
- [ ] UI 能看图不等于模型能看图；两条能力分别记。

### P0.7 P0 测试

- [ ] 扩 `tests/fixtures/mcp_test_server.py`：分别返回 text、image、audio、resource link、
  embedded text、embedded blob、mixed content、structuredContent。
- [ ] `test_mcp_client.cpp` 钉解析、乱序、缺字段、坏类型、坏 base64、未知 type、大小帽。
- [ ] `test_mcp_tool.cpp` 钉完整 payload 进 `Tool::Result`，MCP 调用 id/transport generation
  仍能关联。
- [ ] 给 `ToolResultBlock` 的保存、恢复、prefix hash、context trim 各补单测。
- [ ] 四家 request serializer 各补富结果 fixture；不支持者断言明确降级文本。
- [ ] 图片落盘后验 SHA、MIME、尺寸、路径 containment；坏图不留下半截文件。
- [ ] Release 全量：`ctest --test-dir build/release -C Release --output-on-failure -j 8`。

### P0 完成定义

- [ ] 假 MCP 返回一张 PNG，LubanCode 不再生成“不支持的内容类型: image”。
- [ ] 多模态模型下一轮能依据截图回答一项可复验的视觉问题。
- [ ] 纯文本模型拿到清楚的 artifact/尺寸说明，不撞 provider 400/500。
- [ ] `structuredContent` 不丢；有 output schema 时真校验。
- [ ] session save/resume 后，工具结果配对、artifact 引用与文本投影不变。
- [ ] 坏 base64、超大 blob、伪 MIME、逃逸路径全部在发送下一轮模型前拦住。

## P1：Browser MCP 最小可用版

### P1.1 先走现有 `mcpServers`

首版不等统一 plugin 包格式。先把 Browser MCP 当普通 stdio server 配进现有
`mcpServers`，证明浏览器闭环：

```json
{
  "mcpServers": {
    "browser": {
      "command": "node",
      "args": ["<browser-mcp>/dist/server.js", "--engine", "webkit"]
    }
  }
}
```

- [ ] Browser MCP 独立目录、独立依赖锁、独立自测命令。不要把 Node 依赖链塞进 C++
  主程序。
- [ ] stdout 只写 MCP JSON-RPC；Playwright 日志只写 stderr。
- [ ] 启动时不急着开浏览器。第一次 browser tool call 才 lazy launch。
- [ ] 会话结束收 page/context/browser；异常退出也要杀净子进程树。
- [ ] `/mcp` 能看 Browser MCP 存活与工具数；doctor 路后续再补。

### P1.2 配置与 profile

- [ ] 增 `browser.engine = chromium|webkit`。是否再收 firefox，另开单，不顺手扩。
- [ ] 增 `browser.headless`。交互模式默认 headed，CI 默认 headless。
- [ ] 增 `browser.profile = persistent|ephemeral`。
- [ ] persistent profile 落 `~/.lubancode/browser/profiles/<name>/`；项目不得用仓库相对
  路径装 Cookie。
- [ ] 禁止指向用户日常 Chrome/Edge profile。发现目录重合便拒绝启动。
- [ ] 同一 profile 加进程锁。第二只 LubanCode 不可同时开同一 user-data dir。
- [ ] 下载落 `~/.lubancode/browser/downloads/<session-id>/` 或会话 artifact 目录。
- [ ] `browser.clear_profile` 需显式确认，并明列将删目录；不做模糊递归删除。

### P1.3 工具面

首版工具名保持少而稳。每件工具都回 `page_id`、URL、标题与页面 generation。

- [ ] `browser_open`
  - 入参：`url`、可选 `new_page`、可选 `wait_until`。
  - 出参：页面 id、最终 URL、标题、HTTP/导航摘要。
  - 只收 `http/https/about:blank`；其它 scheme 默认拒绝。
- [ ] `browser_snapshot`
  - 入参：`page_id`、可选范围与最大字符数。
  - 出参：带稳定 ref 的可访问性/语义树，不把整页 HTML 当默认结果。
  - ref 绑定 page generation；导航后旧 ref 立即失效，明报 stale ref。
- [ ] `browser_click`
  - 首选 snapshot ref；可选语义 locator。首版不鼓励裸 CSS/XPath。
  - 点击前确认页面 generation，点击后等导航/网络/DOM 稳态中的明确一种。
- [ ] `browser_type`
  - 分 `fill` 与逐键输入。密码框默认不回显值，不进日志与 tool result。
  - 不自动读取系统剪贴板。
- [ ] `browser_wait`
  - 可等 URL、文本、元素状态或固定上限；不得开放无限等待。
- [ ] `browser_tabs`
  - 列 page id、标题、URL、active、generation。
- [ ] `browser_select_tab`
  - 明确切 active page，不靠“最后一页”猜。
- [ ] `browser_close_page`
  - 关页后旧 page id 稳定报 closed，不偷偷复用 id。
- [ ] `browser_screenshot`
  - 可选 viewport/full-page/element ref。
  - 回 text 摘要 + `ImageContent` + `structuredContent` 元数据。
  - 图片先过 P0 artifact/MIME/尺寸/字节帽。
- [ ] `browser_downloads`
  - 列状态、建议文件名、实际安全文件名、MIME、字节、SHA、artifact id。
- [ ] `browser_status`
  - 回 engine、headed、profile 名、browser pid、pages、下载目录，不回 Cookie。

### P1.4 快照与 ref 规矩

- [ ] 用 Playwright locator/accessibility 能力，不自造一套脆弱 CSS 抓取器。
- [ ] 每次 snapshot 生成 `snapshot_id`；ref 至少绑定 `page_id + generation + snapshot_id`。
- [ ] DOM 改动但未导航时，动作前重新解析 locator；目标不唯一便拒绝，不点第一个凑数。
- [ ] iframe、shadow DOM、弹窗各有明确路径。首版不支持的形状须明报。
- [ ] 页面正文做去重、裁剪与分区。导航栏重复一百遍，不能灌满上下文。
- [ ] 快照应含控件角色、可见名、状态、值的安全投影；密码值永不出账。
- [ ] 原始 HTML 只作显式调试工具，设小帽，不列主流程。

### P1.5 动作、权限与秘密

- [ ] 浏览、快照、截图可按只读档处理；点击、输入、上传、下载按动作档处理。
- [ ] 不盲信 MCP server 的 `readOnlyHint/destructiveHint`。宿主可参考，不拿它当授权真账。
- [ ] 跨域导航后，旧授权不自动覆盖新域。
- [ ] 上传工具首版不开放；若开，只能从用户明确点名的文件或会话 artifact 中选。
- [ ] 密码、OTP、API key 不进模型历史。若要输入，走用户交互 broker 的秘密通道，另开设计单。
- [ ] 遇验证码只报告，不绕过。
- [ ] 下载默认隔离。可执行文件不自动运行；压缩包不自动解压。
- [ ] 阻止 `file://`、`javascript:`、`data:` 的任意导航；确有本地测试需求时单设开发开关。
- [ ] 浏览器网络与 LubanCode 主进程权限分开记。MCP 子进程隔离不等于网络沙箱。

### P1.6 取消、超时与恢复

- [ ] 每次浏览器动作有墙钟。默认值按动作分，不拿一枚 120 秒包打天下。
- [ ] ESC/父任务取消要发 MCP cancellation；若当前 client 尚未实现，先补协议，再接浏览器。
- [ ] Playwright action 收取消后停止等待，返回 `cancelled`，不把页面硬判 dead。
- [ ] Browser process 崩溃时，所有在飞调用只收一次终态。
- [ ] 是否自动重启须明定：可重启 browser runtime，不可冒充旧 page 仍活着。
- [ ] 重启后 page id 全换 generation；模型收到“浏览器已重启，旧页面句柄失效”。
- [ ] MCP transport 断开不自动重放写动作，免得表单提交两遍。

### P1.7 本地验收站点

在 `tests/fixtures/browser_site/` 放纯本地站点，测试不依赖公网：

- [ ] 首页有链接、按钮、输入框、下拉框、dialog、iframe、动态 DOM。
- [ ] 登录页用本地 Cookie 模拟会话，不放真账号。
- [ ] 下载页提供 text/png/zip 三种小文件。
- [ ] 慢页面、永不结束请求、导航中断、弹窗、页面崩溃各有夹具。
- [ ] 截图页有四角标记与确定色块，便于像素检查“非空、没裁歪”。
- [ ] 启一个 loopback server；端口动态分配，测试结束必收尸。

### P1.8 Browser MCP 测试

- [ ] 单元：工具 schema、page id、generation、stale ref、大小帽、URL scheme。
- [ ] 组件：起真 Browser MCP + 真 Playwright，走 stdio initialize/list/call。
- [ ] E2E：open -> snapshot -> click -> type -> screenshot -> download。
- [ ] profile：第一场写 Cookie，第二场同 profile 能读；ephemeral 场读不到。
- [ ] isolation：两进程抢同 profile，后一只明确失败，不损坏目录。
- [ ] engine：chromium 与 webkit 至少各跑本地核心矩阵；差异单列，不拿一家的通过替另一家。
- [ ] screenshot：桌面、窄视口各验尺寸、非空像素、目标元素入画、不重叠。
- [ ] cancel：导航、wait、download 三处取消，目标 1 秒内回工具终态。
- [ ] crash：杀 browser child，MCP server 不吊死；旧 page id 明报失效。
- [ ] security：`file://`、`javascript:`、越界上传、伪 MIME 下载全部被拦。
- [ ] Release 全量 C++ 测试 + Browser MCP 自家测试都绿，报告分开记，不混成一句“全过”。

### P1 完成定义

- [ ] 用户一句“打开本地验收站，填名字，点提交，截张图”，模型能完整做完。
- [ ] 第二次工具调用仍操作第一次打开的同一页。
- [ ] persistent profile 跨 LubanCode 会话保留测试 Cookie；ephemeral 不留。
- [ ] screenshot 既能给人看，也能在多模态模型下一轮真参与判断。
- [ ] WebKit 与 Chromium 都能跑核心矩阵；默认引擎有实测依据。
- [ ] 浏览器崩溃、MCP 断线、ESC 取消后，LubanCode 都能回输入态继续聊。

## P2：把 Browser MCP 包成真正的 Plugin

P1 跑稳后再统一分发。不要拿包管理阻塞浏览器闭环。

### P2.1 Manifest v2 / bundle 形状

- [ ] 先定术语：`local runtime plugin` 与 `installable plugin bundle` 分开写。
- [ ] 为安装包定义 manifest v2，至少可声明：id、version、skills、MCP server、平台、架构、
  依赖、权限、配置 schema、入口命令与完整性摘要。
- [ ] 可选方案 A：现有 `plugin.json` 增 `runtime.kind=mcp`。
- [ ] 可选方案 B：另设 bundle manifest，里头引用一台或多台 MCP server。
- [ ] 评审后只留一种真账；不可 A/B 两套长期并存。
- [ ] Browser plugin 包带一份 browser skill，教模型先 snapshot、再按 ref 动作、导航后刷新 ref。
- [ ] plugin 安装只写自己的目录与配置片段，不暗改用户全局 `mcpServers`。

### P2.2 生命周期与管理面

- [ ] `/plugin inspect browser` 能列 Skill、MCP 入口、版本、引擎依赖与 profile 位置。
- [ ] `/plugin doctor browser` 检 Node、Playwright、浏览器 binary、profile 锁与本地启动，不访问公网。
- [ ] `/plugins` 与 `/mcp` 不重复报两份互相打架的状态。plugin 报包，MCP 报运行实例。
- [ ] enable/disable/reload 的权威落在 plugin manager；重启 MCP 后刷新工具表与 generation。
- [ ] 版本升级不删 profile；manifest 若要迁 profile，须有显式、可回滚步骤。

### P2.3 分发与供应链

- [ ] 锁 Browser MCP 依赖版本与 Playwright browser revision。
- [ ] 安装时列下载体积、browser binary 来源与 SHA；离线安装另给路径。
- [ ] 第三方包验签/哈希；项目级包继续过信任账。
- [ ] 卸载只删 plugin 程序。profile/download/artifact 是否删除，另问用户。
- [ ] 依赖更新须跑 chromium/webkit 双引擎矩阵，不只跑 TypeScript 单测。

### P2 完成定义

- [ ] 一只 Browser plugin 包能在干净环境完成 install -> doctor -> enable -> call。
- [ ] 同一包同时带 Skill 与 MCP 声明；用户不再手抄两份配置。
- [ ] 插件状态、MCP 实例状态、浏览器 profile 状态三账可查，互不冒充。

## P3：可选 GUI Shell 与 WebView2 嵌入

这期只在用户明确要“浏览器画面长在 LubanCode 窗口里”时开工。Browser MCP 独立窗口
已经够用，便不急着造 GUI。

### P3.1 Windows 方案

- [ ] Windows 嵌入选 WebView2，不拿 Playwright WebKit 当可嵌控件。
- [ ] 新建 GUI shell，复用 app-server/session/runtime；终端控制器不兼任窗口消息泵。
- [ ] WebView2 UI thread、Agent turn thread、Browser MCP actor 三者画清所有权。
- [ ] 决定“嵌入页由 WebView2 自己驾驭”还是“只镜像 Browser MCP 页面”。两套引擎并跑
  会生双份 Cookie/DOM，未经设计不得混用。
- [ ] 地址栏、前进、后退、刷新、标签页、下载提示只做浏览器必需面，不顺手造全套产品。
- [ ] host object/web message 设 origin allowlist；任意网页不得碰 LubanCode 原生工具。

### P3.2 跨平台口径

- [ ] 若要跨平台嵌入，先单独比较 WebView2、WKWebView、WebKitGTK、Qt WebEngine/CEF 的
  体积、许可证、输入法、无障碍与打包成本。
- [ ] 不为“同一引擎”牺牲各平台可维护性。统一的是 BrowserSession/Tool contract，
  不一定是渲染内核。
- [ ] P3 不得反向污染 P0/P1：无 GUI 环境、SSH、CI 仍能跑 headless Browser MCP。

### P3 完成定义

- [ ] Windows 窗口内页面可导航、输入、截图；Agent 与人操作同一份页面状态。
- [ ] 页面内容不能越权调用宿主能力。
- [ ] 桌面与窄窗口均无控件重叠，浏览区非空，缩放与 DPI 正常。
- [ ] 关闭 GUI 后 browser/MCP/agent 子进程全部收净。

## 横切设计：效果、权限与审计

- [ ] 为浏览器工具补效果分类，至少分 `read`、`page_mutation`、`external_commit`。
- [ ] `click` 本身不总是无害。按钮可能付款、发信、删数据。首版遇提交/购买/删除语义，
  动作前必须确认。
- [ ] 工具结果 `effect_summary` 写“打开了哪个 URL、点了哪个控件、下载了什么”，不记密码。
- [ ] trace 关联外层 execution、MCP request id、transport generation、browser generation、page id。
- [ ] 不自动重放 `external_commit`。网络失败后状态不明，回用户查证。
- [ ] 浏览器控制与页面内容分信任域。网页文字可诱导模型，不得改系统提示、权限表或本地配置。

## 横切设计：资源与上下文预算

- [ ] snapshot 先做语义裁剪，再算 token；不可先灌完整 DOM 再让 context trim 砍半截。
- [ ] screenshot 不把 base64 留在长历史。落 artifact，下一轮按 provider 能力临时装载。
- [ ] 同一截图按 SHA 去重；重复调用只复用 artifact 引用。
- [ ] 页面 generation 变后，可复用旧截图作历史证据，但不可冒充当前页面。
- [ ] structuredContent 大对象设 JSON 深度、键数、字符串长度与总字节帽。
- [ ] resource link 默认不展开；模型真要读，另发受控 resource read。

## 文档收账

- [ ] `docs/architecture/extensions/tool-extension.md` 补富结果数据流、版本协商与降级矩阵。
- [ ] `docs/architecture/extensions/plugin-runtime.md` 明写 Browser 属 MCP，不属短命 process plugin。
- [ ] `docs/features/extensions/README.md` 补 Browser MCP 配置、profile 与安全边界。
- [ ] `docs/reference/configuration.md` 补 browser 配置与默认值。
- [ ] `docs/reference/feature-index.md` 分列 MCP rich content、Browser MCP、embedded browser。
- [ ] `docs/getting-started/troubleshooting.md` 补 browser binary、profile lock、空白截图、旧 ref、
  MCP stdout 混日志、模型不支持图片。
- [ ] docs catalog/index 一并登记；不许正文落了，索引仍找不到。

## 兼容与迁移

- [ ] 旧 MCP text-only server 无须改动。
- [ ] 旧 session 的 string `ToolResultBlock` 读取时转一枚 `TextContent`。
- [ ] 新 session 降级给旧版本 LubanCode 时，明报版本不支持，不把富结果读成空。
- [ ] 现有内置工具、Lua、native、process plugin 全走文本构造器，行为不变。
- [ ] `Tool::Result` 迁移期间加编译期/测试守卫，禁止新代码继续直接写旧 `content` 权威字段。
- [ ] app-server schema 若升版，版本号、兼容投影与前端能力握手一并落。

## 风险与停手线

- 若 P0 需要一次改完四家 wire，先落中立类型 + 文本兼容投影，逐家开原生富结果；不可
  为赶工让某一家静默丢图。
- 若模型不支持图片，Browser MCP 仍可凭 snapshot 完成大量任务。不要把视觉模型当浏览器
  MVP 的硬依赖。
- 若 WebKit 在目标站点兼容性明显差，保留可选档，不拿“专属”二字逼它做默认。
- 若 Browser MCP 必须拿密码/OTP 才能跑，停手另设计秘密交互通道，不把值塞 tool args。
- 若 profile 清理目标解析不清，宁可不删，也不递归扫用户目录。
- 若页面动作并发会打乱状态，先全串行；没有证据，不开并发。
- 若 GUI shell 需要大改终端组合根，另立 P3 实施单，不把 P0/P1 一并拖住。

## 建议提交切片

每片须独立编译、独立测，不攒一只巨型提交：

1. `ToolResultPayload` 与 text compatibility constructor。
2. MCP content block/parser + 假 server fixtures。
3. Artifact 落盘、大小帽、MIME 校验。
4. `api::ToolResultBlock`、history/session/trace 迁移。
5. 四家 wire 文本降级矩阵。
6. 至少一家多模态 wire 真送截图。
7. Browser MCP server 骨架 + status/open/tabs。
8. snapshot/ref/click/type。
9. screenshot/download + P0 rich result 闭环。
10. profile/cancel/crash/security 矩阵。
11. plugin bundle/runtime.kind=mcp。
12. 可选 WebView2 GUI shell。

## 总完成定义

- [ ] MCP 标准富结果从 server 到模型/UI/session 全链不丢、不乱、不冒充。
- [ ] 本地验收站的浏览、表单、标签页、截图、下载能由 Agent 连续完成。
- [ ] Chromium/WebKit 至少各有一册真浏览器结果；默认选择有数字与失败样本。
- [ ] persistent/ephemeral profile 各守语义；用户日常浏览器 profile 不受碰触。
- [ ] 纯文本模型可凭 snapshot 操作；多模态模型可再看截图校验。
- [ ] 取消、超时、断线、崩溃、旧 ref、坏资源各有稳定错误码与可续会话。
- [ ] 密码、Cookie、base64、完整 DOM、下载正文不漏进日志与 trace。
- [ ] Browser MCP 可独立安装、doctor、启停；plugin 与 MCP 状态账分明。
- [ ] P0/P1 不依赖 GUI；SSH、CI、终端模式照样可用。
- [ ] 定向测试、Browser E2E 与 Release 全量各自留报告，不能拿静态检查冒充浏览器验收。

## 参考

- MCP Tools（2025-06-18）：
  `https://modelcontextprotocol.io/specification/2025-06-18/server/tools`
- MCP Schema（2025-06-18）：
  `https://modelcontextprotocol.io/specification/2025-06-18/schema`
- Playwright Browsers：`https://playwright.dev/docs/browsers`
- Playwright persistent context：
  `https://playwright.dev/docs/api/class-browsertype#browser-type-launch-persistent-context`
- WebView2：`https://learn.microsoft.com/en-us/microsoft-edge/webview2/landing/`

