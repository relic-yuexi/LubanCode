# 重试白名单漏 500 与真实错误形态采样

- 状态：已收口（2026-09-02 P0 白名单先落,采样账本+矩阵喂入随后;361/361 全绿）
- 现场版本：`lubancode 0.26.169`
- 日期：2026-09-02
- 类型：LubanCode 产品缺陷（可靠性——该重试的错没重试）
- 范围：`src/api/model_request_recovery.cpp` 的 `IsRetryableError`、Api 错误的分型、测试矩阵的假后端错误形态
- 关联：《请求重试预算加厚_五次重试与指数退避拉长.todo》（已落 0.26.167——预算加厚了，门却没开全）
- 不在本单：退避表与预算（已落）；429 的 Retry-After 尊重（另议）

## 一、现场

子代理 #3 修子代理落账单，633 秒后死于：

```text
Error: 子代理执行失败: 模型返回错误: Upstream request failed (code=upstream_error)
```

0.26.167 刚落的五次重试+长退避**一发都没触发**。看白名单（`model_request_recovery.cpp:69`）：

```cpp
case ErrorKind::HttpStatus:
    return status == 408 || status == 429 || status == 502 || status == 503 || status == 504;
case ErrorKind::Parse:
case ErrorKind::Api:
case ErrorKind::Cancelled:
    return false;   // ← 一刀切不重试
```

两个洞：

1. **HttpStatus 漏 500。** 中转站上游崩了常见回 500——白名单没有它，一次就死。
2. **Api kind 一刀切。** 中转站 200 + 错误 body（openai 线 error 结构）归 Api kind，`code=upstream_error`/`server_error`/`overloaded` 一类**本质是上游瞬时病**的，也被判死刑；而 401/400 这类确定性错误同样在 Api kind 里——不能全放，要按 code 分。

## 二、修法

1. **HttpStatus 补 500**（5xx 全家：500/502/503/504 + 408/429，一发不漏）。
2. **Api kind 按 code 分型**：建"可重试 code 白名单"（upstream_error、server_error、overloaded_error、model_overloaded、internal_error 一类——**以真实采样为准**，见下），命中则重试；其余（authentication_error、invalid_request、not_found、permission…）照旧零重试。code 表落成稳定常量并注释来源。
3. **真实错误形态采样（本单的根上功夫）**：
   - 拉近期实战/交互会话的失败日志与轨迹（主目录 workspaces + 会话 stderr 记录），汇总中转站（ccmoon 及其他 provider）实际回过的状态码与 error code 分布；
   - 把采样到的形态逐一喂进假后端测试矩阵——从"教科书式错误"升级为"实战见过的错误"；
   - 采样脚本/夹具入库，往后每撞见新错误形态就回填一条（错误形态账本）。
4. **重试痕迹可查**：子代理死于此因时，失败信息里带"已重试 N 次"（0.26.167 已有的文案路径确认覆盖 Api 分型重试路）。

## 三、验收

- 假后端回 500 与 200+`{"error":{"code":"upstream_error"}}`：各造一路，重试满预算后才报死；任一回成功即恢复。
- 401/400/invalid_request：零重试（既有用例零回归）。
- 采样账本入库且至少含本次 upstream_error 实录；假后端矩阵含采样形态。
- 真机：交互会话再撞 upstream_error 时，Dock 显示"重连 N/5"而非直接失败。

## 四、实施批次

- [x] P0：HttpStatus 补 500；Api kind 按 code 白名单分型（500/502/503/504+408/429 全认;upstream_error/server_error/overloaded_error/model_overloaded/internal_error/internal_server_error 可重试;200+错误body折Api进恢复环——四条wire的error code保真传出）
- [x] P0：错误形态采样账本（脚本+夹具+首批实录）——scripts/collect_error_shapes.py 采轨迹(model.output.failed/turn.failed,按 wire/http_status/api_code 去重幂等,内置 IsRetryableError 镜像对账,--check 可当 CI 门);账本 tests/fixtures/api/error_shapes.json 首批实录两形态:openai-responses 200+code=upstream_error(0JNHLK 轨迹实录,0.26.169 零重试阵亡,0.26.171 修)、HttpStatus 401(0456 一带,案卷见 0.26.169"401 无头案",会话已不在盘以 changelog 佐证);两形态现行判定均与现场预期相合,零 mismatch
- [x] P1：假后端矩阵喂入采样形态；重试文案确认覆盖——upstream_error/401 既有用例(恢复环用满预算、零重试、responses 错误体解析)在账本记名复用;新增 agent 层用例"断流恢复:采样实录形态用满预算,失败文案如实带重试次数"钉 Api(200+错误体)与 HttpStatus 500 两路用尽后的"已自动重试 5 次仍失败,总时长…含退避"文案(此前只 Network 路验过;文案在 loop.cpp 两 kind 同段拼装,i18n 词条 error.request.recovery_exhausted 中英成对)
