# 本地兼容端 Effort 与前缀缓存诊断

状态：待做（2026-08-16，Qwen3.8-27B / vLLM 实测）

## 现场

- 地址：`http://127.0.0.1:8000/v1`
- 模型：`/data2/shared/models/Qwen/Qwen3.8-27B`
- wire：`chat_completions`
- 服务端支持四种调用：不发 `reasoning_effort`、`low`、`medium`、`xhigh`

同一道三盒错标签题，LubanCode 单发实测：

| 档位 | 耗时 | 输出 tokens | 结果 |
|---|---:|---:|---|
| 不填 | 5.7s | 246 | 成功 |
| low | 10.3s | 523 | 成功 |
| medium | 13.9s | 754 | 成功 |
| xhigh | 5.5s | 231 | 成功（此前另有一次空输出、exit 1，未稳定复现） |

档位确实能发，表现却不单调。`xhigh` 与“不填”很像，未必真有四档。LubanCode 现下只说“已切到 xhigh”，没有证据说明服务端采纳了它。

## 要补的 Effort 诊断

1. 把“不填”当正式状态，显示“未发送参数”，别偷偷映射成 medium。
2. provider 可声明 `supported_think_levels`、默认档、参数名、是否原样透传。自定义端没声明时，UI 写“未经能力验证”。
3. `/think` 裸敲要列当前模型真实声明。未知模型至少列本 provider 配置，不只甩一句“以服务商为准”。
4. `/doctor effort` 发一只极小探针，报告 HTTP 状态、服务端错误、请求中实际档位、usage 中 reasoning/output 拆账。密钥与正文照旧打码。
5. 遇到输出预算全被 reasoning 吃掉、正文为空、finish_reason=length 时，须明报；不能只留 exit 1 或一片空白。
6. xhigh 那次瞬时 exit 1 要留 transport 错误与 response body。错误不可被 spinner/重画吞掉。

## 前缀缓存现场

本次 vLLM `/metrics` 明报：

```text
enable_prefix_caching="False"
prefix_cache_queries_total 0
prefix_cache_hits_total 0
prompt_tokens_cached_total 0
```

故而“缓存利用率 0%”不是 LubanCode 提示词没守住前缀，而是服务端压根没开。当前界面分不清这三种现场：

- 服务端禁用缓存；
- 服务端不报告 usage/metrics；
- 服务端已开，确实零命中。

## 要补的缓存诊断

1. usage 账分成 `not_reported / disabled / enabled_no_hit / hit`，别拿同一个 0 糊过去。
2. 本地兼容端可选配 `metrics_url`。`/doctor cache` 只在用户明配后读取，不擅自探公网 provider。
3. 若服务端回 `prompt_tokens_details.cached_tokens`，状态栏显示命中 tokens 与本场命中率；没回就写“未报告”。
4. custom provider 若没声明 `stream_usage`，启动诊断提醒“token/缓存统计可能恒为 0”；可做一次能力探针，结果写入本地 provider 配置。
5. 做两轮固定前缀测试：同 system/tool schema，同历史前缀，只换最后一句。报告前缀字节是否稳定、服务端 query/hit 增量与 cached tokens。
6. 不把 LSH 混进 KV 前缀缓存。LSH/语义去重可瘦 tool 历史；KV cache 只认字节/token 前缀。两张账分开画。

## 完成标准

- 四种 effort 请求均可在诊断里看见“实际发送值”；不填时字段确实缺席。
- 服务端拒绝某档时，原始错误经清洗后可见。
- 8000 这类缓存关闭现场显示“服务端未启用”，不再显示含糊的“0%”。
- 换到启用 prefix caching 的 vLLM 后，固定前缀第二轮能看到 query/hit/cached tokens 对上。

