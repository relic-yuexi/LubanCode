# conhost 原生几何分叉与宽表机器生成

- 状态：已实施（分支 worktree-agent-native-geometry;验收对账见文末实施记录）。
- 日期：2026-09-09
- 级别：P2（正确性分叉 + 数据可信度；非阻塞，但几何分叉在 emoji 常驻 composer 时有整帧退化的性能面）。
- 范围：`src/cli/grapheme.{hpp,cpp}`、`src/cli/terminal_frame.{hpp,cpp}`、`src/platform/console_win.cpp` 原生输出路、`tests/unit/cli/test_grapheme.cpp`、`tests/unit/cli/test_terminal_frame.cpp`。
- 不在本单：VT 路行为、编辑器语义（P1 已收）、性能的重画缓存大改（本单只治几何分叉与表来源）。

## 一、两笔账

### 1.1 conhost 原生路几何分叉

现状：CHAR_INFO 一格一 UTF-16 码元，量宽一列的非 BMP（孤立指示符、U+1D11E 类）在原生路占两格，与 VT 路差一列；含多码点簇的 footer 行整帧退 legacy 重画（每帧全量，比行 diff 慢）。P1 的处置是 utf16_lossy 出参退 legacy——正确性保住了，分叉与性能账留本单。

方向：核 native cell 数据模型能否带"逻辑列宽 vs 物理格数"分离账（编码格数照 CHAR_INFO 硬约束，几何对齐 VT 口径）；整帧退 legacy 的路径补帧级缓存或脏行判定，emoji 常驻 composer 时不再每帧全量。以册钉死：同输入在 VT/原生两路的最终光标位与折行一致。

### 1.2 宽表机器生成

现状：两张区间表（Grapheme_Extend 约 190 条、宽字约 90 条）人工摘录，个别生僻端点可能差一码位。

方向：从 UCD 官方数据（Unicode 15.1，与现表同版本）机器生成两张表替换之——**接口与回退口径零变化**；生成脚本进 `scripts/`（一次性可重跑，来源 URL 与版本写死在脚本头）；生成表头注明 UCD 版本与生成日期；static_assert 递增把关沿用。差异清单（人工表 vs 生成表）逐条核过写进提交说明——有差异先判谁对，不许闷头替换。

## 二、验收

- 生成表替换后 test_grapheme 既有册零改全绿（口径未变的证明）；新增端点差异册钉生成表的修正。
- 几何分叉册：两路同输入同几何；legacy 退化有帧级判定，emoji 常驻时不整帧全量（计数型断言）。
- windows-msvc 腿全绿（原生路代码只在 Windows 编，本单命门腿）。

## 三、边界

不动 VT 路、不动编辑器、不引第三方依赖（UCD 数据经公开 URL 取文本，不进依赖清单）。

## 四、实施记录（2026-09-09，分支 worktree-agent-native-geometry）

- 几何分叉（§1.1）：`BuildNativeRowCells` 改"逻辑列宽 vs 物理格数"分离账——`cell_count` 是逻辑预算（VT 口径），整簇截断按逻辑账判，编码开销（量宽一列的非 BMP 恒双格打代理对）不再劈行尾，尾部空格至少铺满逻辑预算；`NativeColumnForLogical` 把逻辑光标列折算成原生物理列（差值恰等于光标前窄非 BMP 个数），两路末态光标钉在同一文字位置；`WriteNativeRow` 补右缘裁断保护——代理对被缓冲区右缘劈半时前半格改铺空格，不留孤立高代理。
- 整帧退 legacy（§1.1）：`PaintInlineFrameNativeRows` 改帧级判定 + 按行分路——落笔前先算清哪行直写、哪行字节回退（多码点簇/孤立零宽/坏代理行），直写行全程不挪光标（合同不变），字节回退行按 legacy 语义清行落字（光标挪到该行末的有界例外，帧末仍由调用方一笔钉回）；非真 console 一字节不写整帧交 legacy（字节不落两遍）。emoji 常驻 composer 而该行不脏时 `PlanInlineFrameNativePaint` 计 byte_fallback_rows=0（计数型断言钉死）。
- 宽表机器生成（§1.2）：`scripts/gen_unicode_tables.py`（来源 URL 与 UCD 15.1.0 写死在头，可重跑幂等）从 auxiliary/GraphemeBreakProperty.txt 的 Extend 与 EastAsianWidth.txt W/F（含规范默认 W 段）∪ emoji-data.txt Emoji_Presentation 生成两张表替换人工表（201→364 条 / 94→120 条）；表头注明 UCD 版本与生成日期；static_assert 递增把关沿用；接口与回退口径零变化，test_grapheme 既有册零改全绿，端点差异另立新册钉生成表的修正。人工表 vs 生成表差异清单逐条核实，见该分支提交说明。
- 验收：CI（分支快车道 macOS 腿）绿；windows-msvc 腿（console_win.cpp 原生段只在 Windows 编）待 PR 全腿。
- 已知边角（记档不扩圈）：①旧行窄非 BMP 更多时，其超出新行物理末端的代理半格可能留残影，下一拍重画自愈；②`NativeColumnForLogical` 已出口，但忙路 footer 调用方（console_input_stream_footer.cpp，圈外）的光标钉回仍用逻辑列——采用映射属圈外一笔后续单；③空闲路 RedrawEditArea 在 Windows 恒走 PaintInlineFrameLegacy（不经直写路），不在本单范围。
