# Linux 默认兼容发布包：旧 glibc 基线与自包含依赖

- 状态：待实现；本单只记录方案，尚未构建兼容包。
- 日期：2026-09-05
- 级别：P0（Linux 发布包可运行）
- 范围：Linux x86-64 工具链、依赖链接、打包、安装与升级、旧系统和 Harbor 验收。
- 关联：`AgentHarness性能SLO_稳定性与发布安全门设计.todo`；本单实现 Linux 二进制兼容专项，证据交给总发布门。
- 不在本单：Windows/macOS 发布策略重做、完整 musl 移植、模型质量评测、Harbor adapter 完整开发。

## 一、先下断语

用户下载、解压，就该能跑。不能要求用户先升级操作系统、安装编译器，再给 CLI 补运行库。

把兼容包设为默认 Linux 发布包。首批明确支持 glibc 2.31+、通用 x86-64 指令集。
采用旧目标环境配新版工具链，静态链接 libstdc++、libgcc、OpenSSL，保留 glibc 动态链接。
保持现有压缩包形式，带齐主程序、`libexec/rg`、许可证与必需资源，不强求单文件。

2.31 是拟定支持下限，不是已验收结论。低于此基线、musl 系统和其他 CPU 架构另列支持范围。
不承诺所有旧电脑、所有 Terminal-Bench 题目都能跑；通过哪些环境，就声明支持哪些环境。

## 二、现状证据

2026-09-05 会话内已核：

- `.github/workflows/release.yml` 的 Linux 构建直接使用 `ubuntu-latest`，未固定旧 glibc 目标环境。
- `CMakeLists.txt` 要求 C++23；`BUILD_SHARED_LIBS=OFF` 控制部分依赖，不能据此宣称 Linux 全静态。
- 官方 `v0.26.209` Linux x64 包 SHA-256：
  `e5162d9b93c2f2d97d471ebd66556e85e87016c161eb74a79f670d8d92af6fdf`。
- 用 `readelf`、`objdump` 检查该包，发现 `__isoc23_strtol`、`__isoc23_sscanf`、`fmod` 引用 `GLIBC_2.38`。
- 同包 `current_zone` 引用 `GLIBCXX_3.4.31`，`ios_base_library_init` 引用 `GLIBCXX_3.4.32`。
- `DT_NEEDED` 包含 `libssl.so.3`、`libcrypto.so.3`、`libstdc++.so.6`、`libgcc_s.so.1`、`libm.so.6`、`libc.so.6`。
- 同包在本机 WSL（glibc 2.43）成功执行真实 `--help`；尚未在 Debian 11/12 或代表性题目容器中复现。

旧 glibc 缺少新版本符号，动态加载器会在主程序启动前报错。这是发布包运行依赖，不是 Harbor 主动限制。
Debian 12 提供 glibc 2.36，Debian 11 提供 glibc 2.31；本包不能原样依靠这些系统库运行。
“41 个题目基于 Debian 12”及数据集最低基线尚未独立核实，不能写成覆盖率证据。

另一个已澄清事项：远端 HTTP 不是本单阻点。
`src/config/provider_catalog.cpp` 限制预设目录地址；普通用户配置在
`src/config/config.cpp::ValidateProviderConfig` 接受 HTTP，未限制为回环地址。
同一 release 已向非回环 `172.20.*` HTTP 探针发出 `/v1/chat/completions` 请求并收到错误响应。
这证明请求可发出，不等于真实 vLLM 推理通过；无需为绕过预设目录规则强制增加 HTTPS 或本地转发。

## 三、构建与交付合同

### 3.1 固定旧目标环境

- 首选 Debian 11 构建容器，固定镜像 digest；目标 glibc 不高于 2.31。
- 固定满足项目 C++23 用法的 GCC、binutils、libstdc++ 版本，先做工具链试编再定版本。
- 编译器、标准库、OpenSSL 等依赖都须面向同一旧目标环境构建。不得混装新发行版系统库来凑编译。
- 新主机配旧 sysroot 也可实现同一合同；首批只保留一条生产构建路径。
- 禁止从新系统搬入预编译 `libstdc++.a` 后直接宣称兼容；静态库仍可能引入更高 glibc 符号需求。
- 固定通用 x86-64 指令集，禁止 `-march=native`；检查主程序及依赖，保留第三方已有安全运行时分派。
- 新版 GitHub Actions 所需宿主环境与旧目标构建环境分开。必要时在宿主 checkout，再显式启动构建容器。

### 3.2 链接与资源

- 仅在 Linux 兼容构建配置启用 `-static-libstdc++ -static-libgcc`，不得污染 Windows/macOS。
- 显式构建并选择静态 OpenSSL，检查 curl 最终链接结果，不只检查 CMake 开关。
- glibc 保持动态链接，不向用户系统安装或替换 libc、动态加载器。
- 主程序不得依赖目标机 `libstdc++.so.6`、`libgcc_s.so.1`、`libssl.so.3`、`libcrypto.so.3`。
- 对剩余系统动态库维护小范围允许清单；新增依赖须解释用途并补旧环境验证。
- 核查 HTTPS CA 来源、时区数据来源及缺失行为。需要随包带数据时，固定来源、许可证和更新办法。
- CA 缺失须给出可操作错误，或使用明确配置的随包证书；不得通过关闭 TLS 校验凑通过。
- 核查静态 OpenSSL 是否仍需外部 provider 模块、配置文件；默认 HTTPS 路径不得暗中借用构建机资源。
- 继续随包携带 `libexec/rg`，沿用其 manifest、哈希、执行位与许可证检查。
- 记录静态依赖版本；安全更新需重建并重新发布主程序，不能只指望用户升级系统包。

### 3.3 默认安装体验

- 沿用默认 Linux x64 资产命名及安装入口，兼容包通过验收后替换现有默认产物。
- 安装脚本、自动更新必须选择同一兼容产物，不得升级后重新抬高运行基线。
- 解压到普通用户可写目录即可运行，无需 root、编译器、开发头文件或手工配置 `LD_LIBRARY_PATH`。
- 文档明确最低 glibc、架构及系统资源前提；安装入口遇到不支持环境应在替换旧版本前清楚报错。
- 运行所需外部工具另列：自包含发布包不代表任意题目语言、编译器和第三方服务都随包提供。

## 四、分批落地

### P0-1：盘点目标并固定工具链

- [ ] 固定 Terminal-Bench 数据集版本或 commit、Harbor 版本，记录题目基础镜像及 digest。
- [ ] 盘清 glibc/musl、架构、镜像内安装步骤；核实 Debian 11/12 分布，不沿用未核实数字。
- [ ] 固定构建镜像、工具链及依赖来源，保留版本、校验和、构建参数。
- [ ] 在 glibc 2.31 目标环境编过实际 C++23 用法和完整 LubanCode，核对标准库与时区支持。

验收：完整程序能编出，工具链可重建；目标支持表与未支持项均有出处。

### P0-2：生成默认兼容包

- [ ] 增加 Linux 专用兼容构建配置，静态链接 C++ 运行库和 OpenSSL。
- [ ] 补齐 CA、时区及 OpenSSL 运行资源策略，验证缺资源时的行为。
- [ ] 接入现有 release 打包和安装、升级流程，保留现有目录结构及 bundled rg。
- [ ] 生成构建与依赖清单，记录包体大小；注明这仍是依赖系统 glibc 的发布包。

验收：普通用户解压可用，安装与升级选中兼容产物，无需手工补运行库。

### P0-3：把兼容检查接入发布硬门

- [ ] 扫描最终压缩包内全部 ELF，包括 bundled rg 和新增辅助程序。
- [ ] 用 `readelf --version-info` 检查实际导入符号，按版本数字比较，拒绝高于 `GLIBC_2.31` 的需求。
- [ ] 用 `readelf -d` 检查 `DT_NEEDED`，拒绝系统 C++/OpenSSL 动态依赖，递归核查允许依赖。
- [ ] 核查架构和指令集配置，在不依赖新 CPU 指令的受控环境执行；单看 ELF 标注不算完整证明。
- [ ] 在干净 Debian 11、Debian 12 和固定新版 glibc 镜像中测试同一份最终发布包。
- [ ] 在固定代表性题目镜像中验证 installed-agent 运行路径，记录 adapter 来源或测试夹具。
- [ ] 保存版本、镜像 digest、包哈希、完整命令、退出码与脱敏日志；任何硬门失败都禁止上传该包。

验收：下面这张矩阵逐项有结果，不能用“构建成功”替代运行验证。

| 场景 | 验证内容 |
| --- | --- |
| 启动 | `--version`、`--help`、加载隔离测试配置，不读取开发机配置 |
| 网络 | 非回环 HTTP 模型协议夹具；HTTPS 证书校验；DNS、超时和错误响应 |
| Agent | 受控模型夹具驱动读写文件、执行命令和搜索，检查结果及退出码 |
| 真实模型 | 至少一条真实 vLLM 请求与工具闭环；记录模型、服务版本及连接方式 |
| 轨迹 | 实际产生的规范会话轨迹可读取，工具记录与结果对应；不假定尚未实现的导出参数 |
| 资源 | bundled rg、时区、CA；缺失资源时有明确结果，HTTPS 不得降级跳过验证 |
| 安装升级 | 非 root 解压、安装、旧版升级、目录搬迁后仍可运行 |

真实服务或 Harbor 环境缺失时标“未验”，不得计入已通过；发布前补齐约定硬门。
题目镜像测试不安装新版 libc、C++ 运行库或 OpenSSL 来掩盖产物缺件，也不挂载宿主库目录。
仅允许题目原有安装步骤和明示安装前提；测试报告须保留这些差异。

### P1：文档与维护

- [ ] 更新构建文档、安装说明和 CHANGELOG，说明 Linux 默认兼容范围及依赖更新办法。
- [ ] 修正“静态链接所以没有系统依赖”等过宽措辞。
- [ ] 将包哈希、符号检查与镜像矩阵交给总发布门，后续每版复跑。
- [ ] 有真实需求时再评估低于 2.31 的基线、ARM64 或 musl；另列范围，不暗改当前支持合同。

## 五、预计改动落点

- `.github/workflows/release.yml`：固定目标构建环境，先验证最终包再上传。
- `CMakeLists.txt`：Linux 专用链接配置及依赖选择。
- `scripts/`：固定工具链/容器构建入口、ELF 审查、干净镜像冒烟脚本。
- `scripts/install.sh` 及现有升级资产选择路径：核查默认兼容产物和安装前提。
- `tests/`：复用现有网络及工具夹具，补兼容环境场景。
- `docs/development/build-and-release.md`、安装相关文档、`CHANGELOG.md`：写清支持与验证范围。
- `THIRD_PARTY_NOTICES.md` 及随包许可证：核查新静态依赖和资源。

## 六、收官条件

所有 P0 项逐条勾选，并附证据落点。旧系统启动、模型连接、工具闭环与轨迹均须实测。
运行受影响测试，确认 Windows/macOS 构建未受 Linux 链接选项影响。
最终验收对象必须是安装/升级入口实际交付的压缩包，不能只验 build 目录里的另一份程序。
更换 GCC、OpenSSL、构建镜像或依赖版本时重跑硬门，不允许发布基线随 `latest` 漂移。

## 七、核查入口

- Release：https://github.com/relic-yuexi/LubanCode/releases/tag/v0.26.209
- Debian 11 libc：https://packages.debian.org/bullseye/libc6
- Debian 12 libc：https://packages.debian.org/bookworm/libc6
- GCC 链接选项：https://gcc.gnu.org/onlinedocs/gcc/Link-Options.html
- libstdc++ 时区：https://gcc.gnu.org/onlinedocs/libstdc++/manual/time.html

以上链接供实现时复核。依赖精确版本、工具链取得方式及矩阵结果待 P0 阶段落定。
