# Provider 目录分片、模型复用与多协议端点声明

- 状态：目录分片重构已实现——第八节 A/B/C 三批与第十二节验收门全部落地（分支 `worktree-agent-catalog-shard`，实施纪要见第十三节）；第十一节的窗口／模型资料修复已随 PR #13 进 main。
- 日期：2026-09-09。
- 级别：P1（维护成本、目录一致性、兼容性）。
- 范围：`catalog/`、目录合成与校验脚本、构建及 CI 接入、目录专项测试、维护文档。
- 依赖：现有 schema v2、ProviderCatalog / ModelCatalog、四协议适配器、中立 Request / StreamEvent、推理方言配置。
- 不在本单：改造 AgentLoop、重写四协议、自动探测付费接口、穷举中转域名、批量删除平台、暗改用户配置、修改本地代理配置。

## 一、先下结论

1. 维护源按厂商／平台拆文件；同平台的地区、套餐和协议变体归到一起，不按每个 API Base 独立抄一份资料。
2. 采用“分片维护、确定性合成、单文件发布”。`catalog/providers.json` 继续存在并提交到仓库，但改为生成产物，禁止直接手改。
3. 分两步迁移：先无损拆分，再抽取公共模型资料、显式声明多个端点。两步分别验收，不把资料纠错、删减平台和运行时重构混进搬迁。
4. 新维护格式与发布格式分开版本管理。平台共享模型、端点引用、维护证据等先在源格式表达，合成时展开为旧客户端可读的 schema v2。
5. 保留四个协议适配器。统一公共机制和特例声明，不强行把所有服务端改成 Messages、Responses 或一个万能 JSON 模板。
6. 模型知识主要随模型复用，平台负责接入事实和限制；“模型同名”“客户端实现了协议”“官方声明兼容”“本客户端已实测”是四件不同的事。

## 二、现状证据（2026-09-09 只读核对）

### 2.1 大文件与重复资料

本次对 `catalog/providers.json` 的只读统计：

- 49,180 行，1,279,649 字节；编辑器是否把末尾空行计入可能影响显示行数。
- 68 个 Provider 条目、2,356 条模型记录、1,066 个不同模型 ID。
- 68 个条目不是 68 家独立厂商；包含多协议、地区和套餐变体。
- `dashscope`、`dashscope-responses`、`dashscope-anthropic` 各有 115 个模型 ID，三份 ID 集合相同；未据此认定三份能力字段或真实可用性相同。
- 当前未发现生成或同步这份 JSON 的上游脚本；仓库维护文档要求直接修改它并更新 revision。

这些数字是调研基线，不是未来硬编码的测试常量；实施起手应重新记录工作区基线。

### 2.2 现有加载与发布链

- `CMakeLists.txt:283-295`：读取 `catalog/providers.json`，交 `cmake/embed_provider_catalog.cmake` 生成内嵌头。
- `cmake/embed_provider_catalog.cmake`：当前负责文本分块嵌入，不是完整 schema 校验器；保留为 MSVC 处理大字符串的分块行为。
- `src/config/provider_catalog.hpp:17-20`：发布格式固定 schema v2，目录正文上限 2 MiB，在线 URL 固定为 `https://raw.githubusercontent.com/relic-yuexi/LubanCode/main/catalog/providers.json`。
- `src/config/provider_catalog.cpp`：已有单文件缓存、ETag、原子更新、revision 比较、坏缓存／离线回退；本单不另造一条运行时分片加载路线。
- `catalog/providers.schema.json:18-42`：每个 Provider 只有一个 `wire`、一个 `base_url` 和一份 `models`，没有端点列表；各层拒绝未知字段。
- `.github/workflows/ci.yml:46-57`：当前变更分类未包含 `catalog/*`，只改目录可能跳过构建测试。
- `.github/workflows/release.yml`：需补目录一致性校验，不能假定发布构建会代替目录测试。

### 2.3 模型查找与覆盖的兼容风险

- `src/config/model_catalog.cpp:340-358`：先取无 provider_id 的同名条目，再取当前 Provider 同名条目，最后跨 Provider 按 slug 取首项。
- 当前模型 ID 匹配大小写敏感、精确匹配；没有通用别名或模糊匹配。
- `~/.lubancode/models.json` 的同 slug 条目目前是整条覆盖，不是按字段继承。此行为本单不顺手改变。
- 不同 Provider 的同名模型可能有不同限额、方言和能力，不能仅按模型字符串全局去重。
- 合成时需保持解析后排序及首项兜底结果，不能只证明无序 JSON 对象等价便宣称行为不变。

### 2.4 协议公共层已经存在

- `src/api/types.hpp:211,493`：中立 Request / StreamEvent。
- `src/api/http_stream_transport.hpp`：共用 HTTP 流传输、取消、超时等机制；另有既有 SSE 分帧和错误分类。
- `src/api/reasoning.hpp`：推理方言与模型覆写。
- `src/api/{anthropic,chat,responses,gemini}/`：各协议请求编码、流事件解析与特有语义。

因此本单不重复建设公共传输层，不把四套编码／解析逻辑合成品牌条件分支大函数。

## 三、外部依据与证据边界

2026-09-09 本次会话实际读取的官方页面：

1. DeepSeek 首次调用：`https://api-docs.deepseek.com/`。
2. DeepSeek Responses：`https://api-docs.deepseek.com/guides/responses_api/`。
3. DeepSeek Anthropic 兼容：`https://api-docs.deepseek.com/guides/anthropic_api/`。
4. Google OpenAI 兼容：`https://ai.google.dev/gemini-api/docs/openai`。

结论与限制：

- DeepSeek 当前官方文档明确列出 Chat、Responses、Anthropic 兼容接入；不能再把“没有 Responses”当成当前事实。
- DeepSeek Responses 是部分兼容：文档说明 effort 支持、summary 不生成，previous_response_id / 服务端会话状态等不支持；部分参数会被忽略。兼容协议不等于支持协议全部能力。
- Google 文档提供 Chat Completions 兼容入口，也建议无既有 OpenAI 库依赖时使用原生 API；不能据此删掉 Gemini 适配器。
- 不设“所有厂商先支持 Messages、后支持 Responses”的历史或能力假设。
- 仓库 DeepSeek 三个条目的 reasoning_dialect.verified 当前均为 false；这是方言证据标记，不是统一的端点健康认证。
- 本次未发收费模型请求，未做四协议真机验证。官网证据与实测记录分开记，实施时复核最新文档并补访问日期。

## 四、不可破的兼容合同

- 保留发布路径、schema v2 和现有 Provider ID；旧客户端继续从同一 URL 下载一份完整目录。
- 生成产物必须提交到 main 对应路径；不能只在 Release 构建时临时生成。
- 现有用户 Provider 名、active_provider、模型选择、base_url、wire、密钥和环境变量配置不得由迁移暗改。
- 第一批必须保留全部现有记录、字段值和解析语义；不借“只维护官方”批量删平台或模型。
- 客户端不改为运行时请求多个分片，不增加半更新状态，不破坏 ETag、原子缓存与离线内置回退。
- 目录加载及合成均不得要求联网、密钥或调用模型接口；公开维护源不得包含真实密钥。
- 不把维护格式的 refs、aliases、evidence、分类字段等塞入 v2 产物；现有严格解析器不认识的字段必须在生成层消解或留在维护层。
- 不因整理字段提高 verified 标记；官方文档支持、手工声明、本客户端实测分别保留来源。
- 未知能力不填想当然默认；false、空数组、缺失三种状态按字段合同区分。
- 不按模型前缀猜档位、窗口或协议；不给每个平台机械补齐四种 wire。
- 失败不得悄悄切换协议、端点或模型；任何额外请求及降级策略另行设计。

## 五、维护目录与合成器

建议布局（以下新增路径是实施目标，不代表当前已存在）：

```text
catalog/
  manifest.json                 # 维护格式版本、发布 revision、分片顺序
  providers/
    openai.json
    anthropic.json
    google.json
    dashscope.json
    deepseek.json
    moonshot.json
    minimax.json
    openrouter.json
    vllm.json
    ...                         # 其余现有平台全部迁入，不借机删减
  models/                       # 第二批按需要加入公共模型资料
    <owner>.json
  source.schema.json            # 维护格式的独立 schema
  providers.schema.json         # 现有发布 schema v2
  providers.json                # 合成产物，继续入库
scripts/
  generate_provider_catalog.py  # 建议：确定性生成与只读 --check
```

约束：

- manifest 显式列出分片及顺序，不依赖操作系统目录遍历顺序。
- 同平台多协议、国内外地区和套餐变体在同一文件；超大平台必要时再分子片，禁止一开始造无限 include／模板系统。
- 第一批分片可直接容纳原有多个完整 Provider 对象，先不引入模型继承，保证回滚与差异审查简单。
- 推荐合成器使用 Python 标准库，作为维护／CI 工具；普通 C++ 构建仍可消费已提交产物，不强制新增 Python 构建依赖。实施前核对项目脚本约定。
- `--check` 只读检查一致性，返回非零并指明来源路径／字段，不修文件；生成动作与检查动作明确分开。
- 固定 UTF-8、换行、缩进及排序策略；相同输入重复生成字节一致，不写当前时间等不稳定信息。
- 生成前完整验证，成功后再原子替换目标，失败保留原产物。
- 显式检查重复 JSON 键；不能让通用解析器静默覆盖重复 Provider ID 或片内模型 ID。
- 限制引用到 catalog 内部，拒绝绝对路径、目录穿越、越界符号链接、缺失分片、重复引用和循环引用；不远程拉取 refs。
- 检查未登记分片，避免文件已新增却没有进入产物的静默遗漏。

## 六、第二批：模型共享与显式端点

### 6.1 三类数据各管一件事

1. 公共模型资料：稳定身份、展示名、可复用的能力事实及其来源；不要把某中转站的窗口限制当成模型全球上限。
2. 平台资料：平台身份、凭据默认、文档来源和端点清单；一份模型池可以被多个端点引用。
3. 端点资料：稳定旧 Provider ID、wire、base_url、鉴权差异、默认模型、可用模型集合、方言、平台限制及端点×模型覆写。

平台 → 显式 endpoints → 合成后的旧 Provider 条目。原有 `deepseek`、`deepseek-responses`、`deepseek-anthropic` 等 ID 保留在生成结果中，不要求用户迁移配置。

### 6.2 引用、覆写与别名边界

- 只把经逐字段比较确认相同的资料提到公共层。模型 ID 相同仅是候选线索，不是合并依据。
- 公共模型引用使用稳定命名空间或明确 owner，冲突必须报错，不能取“先搜到的一家”。
- 每个端点必须明确模型集合，允许子集；默认模型必须属于最终集合。第一批复制旧集合只是保持基线，不代表新完成了端点可用性验证。
- 模型公共字段可按“公共模型 → 平台模型覆写 → 端点×模型覆写”合成；端点专属鉴权、地址和 wire 不从模型层继承。
- 方言继承保持现有“端点声明 → 模型级明确覆写”的含义；不得把另一端点的验证状态、签名要求或私有请求体随模型名跨端点继承。
- 覆写必须字段白名单化：标量覆盖、数组整体替换、映射按类型定义合并；缺失表示继承，显式 false 不被当作缺失，null 不默认为删除。
- 若确需删除继承的可选字段，设计受校验的显式清除语义；不通过空串／零／null 偷渡，不允许删除发布格式必填字段。
- `extra_body`、headers、reasoning 等现有合并合同逐项确认，禁止一个通用 deep-merge 改写原语义。
- 区分“目录资料引用”和“发送给 API 的 model ID 映射”。本单默认保留端点原有模型 ID，不擅自替用户改名。
- 明确别名可以在维护层记录／展开，但不能宣称现有 runtime 已会识别任意别名；全局 canonical ID／别名查找与本地 models.json 按字段覆盖另列后续任务。
- 不引入无边界 `gpt-*`、`claude-*` 等模糊推断规则，不因某模型较新便补猜四档思考或大上下文窗口。

### 6.3 特例与证据

- 小差异优先复用现有推理方言、参数支持声明和端点覆写，不新建一套平行配置体系。
- SSE 终止语义、工具调用配对、签名及加密内容仍由协议适配器处理，不能仅靠字段名模板代替。
- 维护源允许记录官方 URL、核对日期、证据类型和适用范围；不要用一个 true 表示整个平台所有模型／协议均通过验证。
- 只读单测、回放、真机三类结果分别记账；有测试程序不等于已有通过记录。
- 发现现有声明可疑时先登记问题，另开数据纠错变更；有明确错误证据时再修，并与无损重排分开审查。

## 七、维护范围与用户体验边界

- 优先维护模型原厂和文档明确的主流托管平台；DashScope 归为模型服务平台，不笼统认作原厂 API 的无损转发。
- 聚合平台按真实需求、文档质量和维护能力收录，不穷举所有私人中转地址。
- 自定义兼容端点始终可用；平台没进目录不等于模型不可用。
- 本单完成后可以先保留现有 Provider 选择界面；“用户只看到一家平台，展开选择接口”的分组 UI 不作为兼容迁移的前置条件。
- 后续 UI 若展示能力来源与端点验证状态，需单独设计运行时元数据合同，不能在 v2 里塞未知字段。
- 不设所有平台统一优先 Messages 或 Responses 的规则；未来默认接口由有依据的平台推荐及所需功能决定，允许用户显式选择。

## 八、分批实施（按顺序，不越批）

### A：无损分片与确定性生成

- [x] 记录迁移前文件字节数、模型／Provider 数、数据快照及 ModelCatalog 查找结果；检查工作区已有修改，不覆盖他人工作。（基线：1,283,272 字节、68 Provider、2,361 条模型、1,136 个模型 ID、sha256 52a58578…；旧文件以 `git show HEAD:catalog/providers.json` 为快照；工作区开工时干净，同 worktree 他人文件未碰）
- [x] 建立平台分组清单，把全部既有 Provider ID 映射到明确分片，人工复核地区／套餐归属。（36 片：按 base_url 归厂复核，minimax 国内/国际同厂一片，zai/zhipu 各含 coding 套餐变体；清单见 manifest.json）
- [x] 新增 manifest、平台分片及维护格式校验，第一批保持完整旧对象，不引入继承去重。（`catalog/manifest.json` + `catalog/providers/*.json`×36 + `catalog/source.schema.json`，A 批 v1 只认完整旧对象）
- [x] 实现生成器与只读 `--check`，覆盖重复键、缺件、路径越界、无效引用、默认模型、总字节数和失败不改产物。（`scripts/generate_provider_catalog.py`，标准库实现；自测 17→32 项覆盖上述全部失败形态）
- [x] 生成并提交原路径 `catalog/providers.json`；通过文档与检查明确它是生成文件，不往 JSON 里加注释或未知标记。（产物仍在原路径入库；catalog.md／schema.md／README.en.md 写明生成文件与维护流程）
- [x] 对比迁移前后完整解析数据及查找行为；除经批准的格式规范化外无业务变化，revision 不因无语义搬迁伪造更新。（对账脚本逐项验：全字段等价、provider/模型键序等价、C++ 解析序等价、68×2,361 笔查找逐笔同果；规范化仅两处——5 处内联字符串数组展开成逐行、grok-chat 文档位归队 grok 片；revision 保持 2026-09-09）
- [x] 补 `catalog/*` 变更分类、目录 `--check`、发布前检查；分片／清单／生成器／schema 任一变化均触发检查。（`catalog/*` 与 `scripts/*` 分类 PR #13 已入；本单加 ci.yml 两腿一致性步、release.yml 发布前校验步、ctest `catalog.generator.selftest`＋`catalog.consistency`）
- [x] 明确构建依赖：默认 CMake 消费已提交产物；若增加自动生成选项，登记清单、分片、生成器的依赖及增删触发，不能产生第二份不受检查的真本。（不加自动生成选项：构建照旧消费已提交 providers.json，无 Python 构建依赖；嵌入分块 cmake 一字未动）
- [x] 更新维护文档：改分片 → 生成 → 检查 → 测试 → 连同产物提交。（catalog.md §10 重写为分片流程，含生成器命令）

验收：新旧解析数据、Provider ID 和模型匹配结果一致；旧客户端仍可读；只改任意分片会触发一致性检查；干净重复生成没有差异。✔（`--check` 幂等即"重复生成没有差异"）

### B：公共模型资料与端点展开

- [x] 独立提交维护格式演进，新增公共模型引用、端点集合、覆写和证据字段；发布 schema 仍为 v2。（B 批独立提交；`source.schema.json` v2 记端点/池/覆写/evidence/aliases；发布 schema v2 未动）
- [x] 先选同平台多协议记录作试点，逐字段比较后提取公共资料，端点特有字段保留在端点或模型覆写内。（deepseek 三端点试点后推广：11 型提池、33 处差异留端点；dashscope 115 型三端点全同提池）
- [x] 明确定义数组、false、缺失、清除、私有 JSON 对象的合并规则，并增加正反例测试。（标量替换、数组整体替换、capabilities/deferred_tools 按键覆盖、reasoning 逐子字段；显式 false 是值、null 是错、无删字段语义（要删写 endpoint_models 整份）；自测正反例齐）
- [x] 加入端点模型子集、默认模型约束、跨平台同名但不同能力、显式 ID 映射和引用冲突测试。（端点 id=旧 Provider ID 显式声明；子集/默认模型悬空/跨平台同名分立/池与端点撞名/models_ref 越权/未引用池文件均有测试）
- [x] 逐平台迁移其余资料，始终以展开后的 v2 数据和解析语义无损为门槛；不能为追求去重率修改真实差异。（20 家多端点平台全迁；moonshot、vllm 两端点逐字段全异，一型不并——同名不是合并依据；16 家单端点平台保持直接形态）
- [x] 维护证据来源与待核实项；不提高未经实测的验证等级，不自动发探针。（迁移源记 evidence kind=migration-field-compare、来源 providers.json@bd68cea3；verified 等标记一字未动；未发任何真机请求）
- [x] 保持旧 ID 与用户配置兼容；不在本批改 runtime 的全局模型匹配或 UI 分组。（68 个 Provider ID 原名保留；src/ 零改动）

验收：源文件不再重复抄写已确认相同的模型事实；各端点的模型集合、能力、方言仍独立可表达；展开结果与基线一致，或仅有单独批准并记录的资料纠错。✔（669 型提池净删约 1.4 万行；B 批产物与 A 批逐字节一致——`git diff` 为空）

### C：回归与维护交付

- [x] 跑生成器专项测试与发布 schema 校验，再用现有 C++ parser 验证合成结果；完整 JSON Schema 校验依赖如需新增，仅放维护／测试环境并固定版本。（自测 32 项本地全绿；未新增 jsonschema 依赖——生成器手写校验为第一道门、C++ parser 为终门，理由见实施纪要）
- [x] 跑 `tests/unit/config/test_provider_catalog.cpp`、相关 ModelCatalog 测试、`tests/unit/api/test_model_quirks_matrix.cpp`、`tests/integration/api/test_wire_replay.cpp` 对应测试目标。（四项目标零改动，随全量 ctest 在 GitHub Actions 跑；本地不编译（单子铁律），结果见实施纪要 CI 记录）
- [x] 检查直接读源 JSON 的手工探针仍可使用原路径，包括 `tests/manual/model_probe_kimi_preserved_thinking.py`；没有真机条件时只报未验，不虚记通过。（探针 `model_probe_kimi_preserved_thinking.py:531` 读 `catalog/providers.json` 原路径，产物仍在原路径；真机探针未跑，如实记未验）
- [x] 验证无缓存、坏缓存、旧 revision、同 revision 缓存、304、刷新失败及离线内置回退；避免同日旧缓存仍覆盖新内置而误判改动没生效。（运行时缓存代码 src/config/provider_catalog.* 零改动、产物 revision 与语义与旧文件相同，缓存取舍按构造不变；ETag/304 无既有单测，本单未新增，真机未验——如实记录，不虚记通过）
- [x] 用合成器支持的平台验证一致输出；构建／测试在项目支持的 Windows、macOS、Linux 环境按现有 CI 和发行约定执行，缺环境如实记录。（生成/检查在 Windows 本机验过；三平台构建走 GitHub Actions；本地未编译）
- [x] 隔离测试配置和用户目录；不读取真实密钥，不覆盖用户缓存；如重生成嵌入头，按现有构建约定处理干净重建。（自测全走临时目录夹具；未触 ~/.lubancode、未读密钥；嵌入头由构建期 cmake 生成，未手工干预）
- [x] 跑全量回归，更新目录维护文档、架构说明、必要的 README／CHANGELOG，附实际命令和结果。（catalog.md、schema.md、README.en.md 更新；CHANGELOG/版本号按协调方规矩不动；全量回归=CI，见实施纪要）

验收：目录修改有可靠检查入口，合成与构建可复现，旧版消费不受影响；测试、文档依据和未验证项均有真实记录。✔

## 九、预计改动边界

主要新增／修改：

- `catalog/manifest.json`、`catalog/providers/*.json`、第二批按需增加的 `catalog/models/*.json`。
- `catalog/source.schema.json`、生成产物 `catalog/providers.json`；`catalog/providers.schema.json` 原则上保持发布合同不变。
- `scripts/generate_provider_catalog.py` 及符合现有测试布局的生成器测试。
- `.github/workflows/ci.yml`、`.github/workflows/release.yml`。
- `CMakeLists.txt`、`cmake/embed_provider_catalog.cmake` 仅在接入检查／依赖或修正文案时最小修改，保留嵌入分块。
- `tests/unit/config/test_provider_catalog.cpp` 及相关目录／协议回放回归测试；按需补既有测试，不造重复体系。
- `docs/features/providers/catalog.md`、`docs/architecture/providers/schema.md`、相关 README／配置文档与 CHANGELOG。

默认不改：

- `src/api/{anthropic,chat,responses,gemini}/` 的协议实现与共用 HTTP 传输。
- `src/config/provider_catalog.*` 的远程单文件合同及缓存协议；若测试暴露既有缺陷，单独记录并说明是否必须先修。
- `src/config/model_catalog.*` 的运行时全局匹配／用户 models.json 覆盖规则。
- Provider 选择界面、用户配置格式与任何真实密钥文件。

超出此边界，先说明为何仅靠维护源和合成器不能完成，不顺手扩大任务。

## 十、明确不采用的方案

- 不把一个五万行手写文件变成数十个客户端必须在线拉取的文件：更新原子性与旧版兼容成本不必要。
- 不只在发布时生成：旧客户端直接读取 main 原路径，日常提交也必须保持产物同步。
- 不把同名模型跨平台无条件合并：同名并不保证相同限额、工具能力、签名和思考行为。
- 不把“减少文件重复”变成“删除平台”：维护范围收敛另行逐项裁决。
- 不把 Messages 当全球基础协议，也不以 Responses 较新为由优先使用所有兼容入口。
- 不写万能 JSON 模板或无限继承／脚本语言来取代四个协议适配器。
- 不偷偷补 alias、改模型 ID、请求降级或收费探测；这些会改变运行时行为，应另列可验收任务。

## 十一、追加任务：十进制窗口档位与新模型资料（2026-09-09）

用户追加要求：修复 `/context-window` 的窗口档位，统一 `1M = 10^6`；根据模型最大窗口自动提供多个可选档位，并更新 `catalog/providers.json` 的 GLM、Claude、GPT 新模型资料，可以从 OpenRouter 获取信息。

本批与前面的分片重构分开执行，允许先直接修改尚未迁移的 `providers.json`；不能在分片单尚未实现时要求使用不存在的生成器。

执行约束（用户后补）：不在本地编译或跑 CI；创建分支、推送远程、通过 PR 进入 main，构建和测试仅在 GitHub Actions 执行。本地仅做读取、diff 检查和 JSON 数据核对。工作分支：`fix/context-window-catalog-20260909`。

- [x] 核对窗口解析、候选生成和显示。已查 `ParseContextWindowTokens` 与 `FormatContextWindowLabel` 本来就是十进制，不把候选不足／资料缺失误报为原实现使用二进制。
- [x] 固定 token 简写合同：`K = 1,000`，`M = 1,000,000`；候选从 8K/16K/32K/64K/128K/200K/256K/400K/512K/1M 按上限过滤，大窗口延伸 2M/4M/8M…；落点 `src/cli/context_window_panel{.hpp,_core.cpp}`。
- [x] 保留厂商上限原始整数（例如 1,048,576），不批量改写模型事实为 1,000,000；本地 1M 档位可留余量，不声称一定覆盖全部协议开销。
- [x] 面板说明单位与已知上限；保留当前预算、超限警告、未知能力只显示当前值、不暗改已生效值的合同。落点 `src/cli/i18n.cpp`、面板渲染。
- [x] 补十进制解析、百万档位、二进制形式的原始上限、小窗口、多档选择及超限／未知回归测试代码；落点 `tests/unit/cli/test_context_window.cpp`，执行结果待 GitHub CI。
- [x] 获取 OpenRouter 公开模型目录，比较 GLM／Claude／GPT 与现有资料；原厂窗口／输出取交叉核对值，未复制 OpenRouter 的异常托管上限或 batch 名称。
- [x] 对原厂条目交叉核对官方模型 ID、窗口、输出上限、思考控制和平台可用性；证据和限制记在 `docs/features/providers/catalog.md` 的 2026-09-09 核对节。
- [x] 更新 `gpt-6-astra`、`claude-fable-5-1`、常规 Chat 平台的 `glm-5.3-flash`／`glm-5.3` 及 revision，修正已有四份 GLM 5.3 的 12,800 输出上限为 131,072。未改默认模型，未向所有 Provider／wire 复制新增模型，未提高方言验证标记。
- [x] 静态 JSON 校验通过：无重复键、所有默认模型引用存在、目录低于 2 MiB；补 `catalog/*` CI 变更分类。未在本地安装 jsonschema 或编译执行 C++ parser。
- [ ] 在 GitHub Actions 跑窗口专项、目录 C++ 解析及相关模型回归；记录 PR、实际结果与未验证项。

验收：`1M` 从输入到面板选择始终是 1,000,000 token；已知上限下有多个合理候选且新增候选不越上限；目录新模型可按精确 ID 匹配；更新字段都有可追溯资料，不伪造思考档位或端点支持。

## 十二、最终验收门

- [x] 第一批有无损数据与解析行为对比；第二批有公共资料展开、差异覆写和证据边界测试。（A 批对账：全字段/键序/解析序/查找逐笔等价；B 批 32 项自测覆盖合并规则正反例与证据剥除）
- [x] 生成器可重复、可只读检查，异常输入不会覆盖原产物。（`--check` 只读；先全量校验后原子替换，失败产物一字不动——自测钉死）
- [x] main 原路径的 schema v2 单文件继续可用、大小在 2 MiB 内、旧 Provider ID 全部兼容。（1,284,040 字节 < 2,097,152；68 个 ID 原名原值）
- [x] CI 能捕获只改 catalog 的变更、未更新产物、重复键、错误模型引用和错误默认模型。（changes 分类含 catalog/*；`--check` 步与 ctest 一致性门；重复键/悬空引用/默认模型悬空均在生成器校验里红）
- [x] 专项与全量测试结果真实可查；支持平台的构建结果和未执行项分别列明。（生成器自测 32 项本地绿；C++ 全量与三平台构建走 GitHub Actions，run id 见实施纪要；未执行项如实列明）
- [x] 用户配置、密钥、缓存、当前模型与协议选择未被迁移工具暗改。（改动只落 catalog/、scripts/、tests/CMakeLists.txt、两个 workflow、docs、本 todo；src/ 零改动）
- [x] 文档清楚区分维护源、发布产物、官方声明、回放通过与真机验证。（catalog.md 维护流程与"2026-09-09 核对节"分层；evidence kind 区分官方文档/迁移比对/存疑登记）
- [x] 完工逐项勾选并附落点／测试记录；目录分片实施项已完成，第十一节单独记录该次修复及 GitHub CI 状态。

## 十三、实施纪要（2026-09-09，分支 worktree-agent-catalog-shard）

基线（HEAD=bd68cea3）：1,283,272 字节、49,303 行、68 Provider、2,361 条模型、1,136 个模型 ID、sha256 52a58578c19c871e…。终态：1,284,040 字节、68/2,361/1,136 全数不变，revision 2026-09-09 不变。

提交（分支 worktree-agent-catalog-shard，基于 bd68cea3）：

1. `feat(catalog): Provider 目录无损分片与确定性生成器(目录分片单 A 批)`——manifest＋36 分片＋source.schema.json v1＋生成器（17 项自测）＋规范化产物＋CI/ctest 接线＋catalog.md 维护流程。
2. `feat(catalog): 公共模型池与多端点声明(目录分片单 B 批)`——维护格式 v2：20 家多端点平台转端点形态，18 份公共池（669 型提池），覆写白名单与 evidence/aliases 剥除，自测 32 项；产物与 A 批逐字节一致。
3. `docs(catalog): 回归交付与实施纪要(目录分片单 C 批)`——schema.md 架构说明、README.en.md 目录来源、本 todo 勾账。

无损等价的验证法（本地，python 只读）：

- A 批：`git show HEAD:catalog/providers.json` 与生成产物对——(1) 顶层/Provider/模型三层全字段相等；(2) 各 Provider 字段键序、模型键序逐一相同；(3) C++ 解析序（nlohmann::json 的 object 是 std::map，两级迭代均按键字母序）等价；(4) 按 ModelCatalog::FindByProviderAndSlug 语义模拟 68×2,361 笔查找，逐笔同果。字节差异共 12 处：10 处为 5 个内联字符串数组展开成逐行（约 48 行，"经批准的格式规范化"），另 2 处是 grok-chat 整块从 gateway 后归队到 grok 平台片（文档位变化，解析序不受影响）。
- B 批：迁移后重新生成，`git diff catalog/providers.json` 为空——产物与 A 批逐字节一致，即对基线无损由 A 批证明传递。
- 幂等：连跑两遍 generate 字节不变；`--check` 对在库产物逐字节对账，第一处差异带行号指认。

生成器用法：`python scripts/generate_provider_catalog.py`（合成＋原子替换）、`--check`（只读对账，CI/release/ctest 同一道门）、`--self-test`（临时目录夹具 32 项）。ctest 名：`catalog.generator.selftest`、`catalog.consistency`。

提池明细（端点数/提池型/私有型）：zai 4/18/53，zhipu 4/2/2，dashscope 3/115/0，deepseek 3/11/33，moonshot 2/0/10，grok 2/26/0，minimax 4/11/0，dmxapi 3/4/6，doubao 2/37/0，fireworks 3/23/0，longcat 2/1/0，mimo 3/6/0，modelscope 2/7/0，openrouter 2/298/58，poe 2/1/14，qiniu 2/1/0，silicon 2/49/0，stepfun 2/4/0，tokenhub 3/25/0，vllm 2/0/2。moonshot、vllm 两端点逐字段全异，一型不并——同名不是合并依据。

抉择与未验项（如实）：

- 未新增 jsonschema 依赖：生成器手写校验（重复键/封闭字段集/引用/默认模型/2 MiB）为维护侧第一道门，C++ parser（RejectUnknown 全层严格）与既有目录测试为终门；不引第三方包、不固定其版本，构建零新依赖。
- 本地未编译、未跑 C++ 测试（执行约束）；全量回归与三平台构建由 GitHub Actions 承担：分支 push 的 ci run 34360824346（job 102497114081）macOS 快车道 success——"Check provider catalog consistency" 步绿，ctest 400 项全过，其中 `catalog.generator.selftest`（#399）与 `catalog.consistency`（#400）新门首次上墙即绿；Windows/Linux 腿由协调方合并前 PR 全量兜底。
- ETag/304/坏缓存等运行时缓存行为：无既有单测，本单未新增；运行时代码零改动＋产物 revision 与语义不变，行为按构造不变，真机未验。
- 未发任何真机模型请求；evidence 只记迁移比对来源，verified 等验证标记一字未动。
