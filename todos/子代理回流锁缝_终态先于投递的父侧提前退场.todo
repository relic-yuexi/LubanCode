# 子代理回流锁缝：终态先于投递，父侧提前退场

- 状态：已实施（2026-09-09，分支 `worktree-agent-reflow-seam`；定案走甲——终态翻页与投父邮箱并入 FinalizeFromToolResult 同一持锁段，见文末实施纪要）。
- 日期：2026-09-09
- 级别：P1（后台子代理再派后台孩子时，父可能拿阶段小结收口、孩子结果落进未送达清单——真丢结果，不只是 CI 抖动）。
- 升格来源：`todos/subagent_recursion时序抖动_回流等待常数界收敛.todo` 三犯并案侦查的结论（该单 §一"唤醒没在时限内送达"一句与实情不符：唤醒送达且被消费了，是父在"无活孩子"门上提前退场——立本单时已带正）。
- 前科指纹：run 33988795509（09-06）、run 34270604648（09-08）、run 34281593474（09-09）三案同一指纹，windows-msvc 两犯。
- 范围：`src/tools/task_ledger.cpp`、`src/tools/agent_tool.cpp`;测试 `tests/unit/agent/test_subagent_recursion.cpp` 补竞态回归。
- 不在本单：测试等待合同（已核实无单可销——`WaitForSettled` 早已是 10ms 步长/15-20s 期限的轮询条件等待）；唤醒识死闸与预留封顶（PR #5 在途，文件不相交）。

## 一、竞态链条（三案日志核对所得）

1. 孩子线程收尾：`FinalizeFromToolResult`（task_ledger.cpp:1668，agent_tool.cpp:1847 调）在台账锁内把孩子翻终态，锁内即调 `NotifyStateChangeLocked()`（:1713，注释明写"等孩子的父与收柄口当拍醒"），松锁。
2. **缝**：孩子已终态、结果尚未进父邮箱——`DeliverChildCompletion`（task_ledger.cpp:1236，agent_tool.cpp:1851 调）是下一把锁的事。
3. 父在 `WaitForKeyChange`（:1649）被那记 notify 叫醒，谓词末门 `AliveChildCountLocked(...) == 0` 已真（孩子刚翻终态），出等。
4. 父回圈查 `SealOrContinueInbox`（:810）：邮箱空、活孩子零 → `inbox_closed` 封账 → 续投返 nullopt → 父以阶段文本收口，Done。
5. 孩子线程这才跑 `DeliverChildCompletion`：父已不活，按"父已不活保持未送达"退回 false——`delivered` 永假，父的事件账里没有 ChildCompletion。

快机上孩子线程几乎必然先回锁（投递先落，父醒时邮箱已有一口）；慢腿调度抖，父抢赢一回即定死。生产同病：任何"后台子代理再派后台孩子"的组合都可能踩。

## 二、修法方向（二选一，择低者落）

- **甲：投递并入收尾同锁**。给 `DeliverChildCompletion` 出锁内变体，在 `FinalizeFromToolResult` 持锁段内、`NotifyStateChangeLocked()` **之前**投父（锁序 ledger→inbox 与既有写口同向）。notify 落地时父邮箱已喂饱，缝焊死。
- **乙：补谓词与封账门**。`WaitForKeyChange` 谓词与 `SealOrContinueInbox` 封账判据里，"已终态但 `ParentTaskInbox` 未送达"的孩子按"还欠我结果"计——父在 WaitingChildren 多等一拍（毫秒级）自愈。改动面更小，但把"欠账"语义引进两处判据，注释要写透。

定案口径：先核两处的锁序与既有不变量，甲若不破锁序优先甲（根治）；乙作甲的兜底或过渡。

## 三、验收

- 三犯指纹场景（父等孩子、孩子收尾notify、父抢锁胜出）稳定复现转绿：用可控后端把两步间的人为延迟做进测试（如投递口注入一拍），断言父必收 ChildCompletion、`delivered` 真、root result 是最终结论。
- 竞态回归册带看门狗（沿既有 WaitForSettled 模式），测试自身不挂死。
- 取消/超时/父先死（真"父已不活"）路径行为不变——未送达清单语义只在"父提前退场"这一假死场景被治，真死照旧。
- 既有 subagent 册全绿；连续重跑（如本地 --repeat 或 CI 多次）零复犯。

## 四、边界

只动 ledger 的收尾/投递/等待三角与对应测试；不碰 agent_tool 的派发合同、不碰 PR #5 的预留/闸文件、不顺带改唤醒识死。

## 五、实施纪要（2026-09-09，甲落地）——附:取消流投递闸(同日补)

- **取消树册 SIGSEGV 后续(PR #7 run 34307354040,同 commit 双 run 一绿一红,仅 macos-clang,windows 过)**:取消树册断言全过、崩在收尾段,doctest 只认当前册。侦查逐路径清了三笔——锁内投递的锁序/寿命全数站得住;PR #5 的 loop.cpp 六 hunk 全是 token 预检数学加 trajectory 专用口,该册零接线;PR #7 只动 write_file,零接线。段错误的直接机制未能钉死,但坐实一枚每-run 时序硬币:缝焊死后,取消流里"孩子的 Cancelled 完成件投进正在取消的父的邮箱"从旧缝的几乎必然落空变成五五开——落进的那一半正是崩溃 run 唯一新增状态。定案按合同拔硬币:挂停止信号(cancel 或 wall_stop)的父数学上不可能再吸收投递(泊等路醒来先查 cancel 即收;续投路 harness 领批后的 cancel/墙钟闸必退信),投它只余 RestoreDrainedInbox 反翻 delivered 的空转与一笔假事件。`DeliverChildCompletionLocked` 加第二道不投闸,孩子保持未送达、收场报告照列,与父真死同语义;正常回流(干净的父)一丝不减。回归册三臂钉死(取消级联父/墙钟软停父/干净父对照),取消树册从此与该缝的时序脱钩。分支 `worktree-agent-reflow-seam-fix`。

- **定案**：甲。锁序核实——全仓嵌套取锁只有"台账锁 -> inbox_mutex"一个方向（SendMessage/SealOrContinueInbox/RestoreDrainedInbox/PendingMessages/HasUndeliveredInboxLocked 皆然，无一例反向），投递并入收尾锁内只嵌父 inbox_mutex，同向不成环，不破不变量，甲可根治。
- **改法**：`FinalizeFromToolResult` 加尾参 `deliver_to_parent`（缺省 false）：置真时在持锁段内、`NotifyStateChangeLocked()` 之前调新拆出的 `DeliverChildCompletionLocked`——notify 落地时父邮箱已喂饱。公开口 `DeliverChildCompletion` 改为锁内体的壳（自带 notify），语义不变。只有 agent_tool.cpp 后台线程收尾一处传 true；前台（delivered 注册即真）与派工即败（失败文本经启动回执同步回调用者，走邮箱会父见两遍）保持缺省。
- **顺带删了旧显式投递**（agent_tool.cpp:1851 的 `DeliverChildCompletion` 跟投）：锁内投递的失败态除"delivered 被 RestoreDrainedInbox 翻回"外全是稳态（父真死/封账/去处不符，皆单向），跟投保护价值为零；退信微秒窗里重投反会造成父见两遍。公开投递口保留（测试钉其"父不活不改投 main"的合同）。
- **测试**（tests/unit/agent/test_subagent_recursion.cpp 新增两册）：①竞态回归——白盒镜像生产续投环（取件/封账/WaitingChildren 条件变量等待），父线程先泊进等待，孩子 `FinalizeFromToolResult(deliver_to_parent=true)` 后线程多活 25ms（把三犯那拍调度抖放大成确定次序），断言父恰收一批 ChildCompletion（带"外来资料"声明）、delivered 真、root result 是最终结论；轮询看门狗（5s 不走完 CancelTask 叫醒）保测试不挂死；20 轮连跑。②父真死语义——父先收口孩子才收尾，锁内投递自返 false，保持未送达、不 reparent、不改投 main（main 三查询口摸不到嵌套账）。
- **边界遵守**：看门狗强收（ForceFinalizeWallClock/NoProgress）路径未动——那条道上孩子终态仍先于（晚到的）投递，属"真死照旧"的既有语义，与唤醒识死同留后续；PR #5 文件集不相交。
