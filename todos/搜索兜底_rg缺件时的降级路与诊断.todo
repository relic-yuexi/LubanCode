# 搜索兜底：rg 缺件时的降级路与诊断

- 状态：已修（2026-09-04 接管工头落地，分支 agent-a45ccf712cce6002e；三层发现/缺件自愈/全缺指引单测全绿，真机裸 exe 三幕（rg-stage 命中 131 hits/PATH 命中/三处全删 24 场景全报错带指引）+ 真机 /doctor search 两形状（随包命中、rg-stage 命中）全过；全量 ctest 379 册 378 绿，唯一红 unit.memory.memory_global_gate 为 main 既有漂移（a069d06e 摘迁移器留下的旧断言，另有单子在册），与本单无关——见文末落地记录）
- 级别：P1（搜索是模型主力工具，rg 缺一件就全瘫）
- 现场实录：用户用独立路径的 exe（面试保命拷贝），`search` 报 `search_backend_missing: 随包 ripgrep 缺件: D:\lubancode\build\libexec\rg.exe`——**rg 按"exe 旁 libexec"单一位置找，拷到哪就得跟到哪，缺件即全瘫无兜底**。
- 病根：rg 发现只有一条路（exe 旁 libexec），缺件直接报错；没有 PATH 兜底、没有用户级安装位兜底（`~/.lubancode/bin` 或 rg-stage）、没有自愈（顺手 stage 一份）。
- 修法（三层兜底 + 自愈）：
  1. 发现顺序：exe 旁 libexec → `~/.lubancode/rg-stage/libexec/`（已有的 staging 位）→ 系统 PATH 的 `rg` → 全缺才报错。
  2. 自愈：exe 旁缺件但 rg-stage 有货时，顺手拷一份到 exe 旁（或直接用 rg-stage 路径，不拷贝），日志一行说明来源。
  3. 报错升级：全缺时的错误文案带自愈指引（"跑 `lubancode doctor` 修复"或给出 stage 命令），不再裸抛路径。
  4. `/doctor` 加 rg 健康检查项（当前位置、版本、兜底命中哪层）。
- 验收：
  - 单测：三层发现顺序、缺件自愈、全缺报错带指引。
  - 真机形状：临时目录放裸 exe（无 libexec）跑 search——命中 rg-stage 或 PATH 兜底成功返回；三处全删才报错且文案含修复指引。
- 不在本单：搜索算法本体、rg 版本升级、远程下载（离线环境不指望网络自愈）。

## 落地记录（2026-09-04）

- **发现层**（`src/tools/search_ripgrep.{hpp,cpp}`）：`BundledRipgrepLocator` 保持随包层生成器（仍只拼 exe 旁
  libexec、不读点名环境变量）；新增 `RipgrepSource/RipgrepCandidate/RipgrepTierStatus/RipgrepDiscovery` 与
  `CollectRipgrepCandidates()`（随包 → `<home>/.lubancode/rg-stage/libexec` → PATH 逐项展开）、
  `DiscoverRipgrep()`（按序过 CheckRipgrepFile，第一枚形态可执行件命中，账走全量三层）、
  `FormatRipgrepAllMissingGuidance()`（全缺文案：逐层聚账 + `/doctor search` +
  `fetch_ripgrep.py --target <home>/.lubancode/rg-stage` 指引）。
- **runner**（`search_ripgrep_run.cpp`）：`EnsureSmoke` 改走三层发现；兜底命中（非随包）时 LogSink warn 一行
  说明来源与路径（不拷贝），三层全缺报 `search_backend_missing` 带上述指引；`RipgrepSmokeResult` 增 `source`
  落账。新增 `BundledRipgrepRunner::Overrides` 整批判注入口（测三层顺序/自愈/全缺），旧的单枚 `exe_override`
  注入口与合同不动。
- **doctor**（`doctor_commands.{hpp,cpp}` + `cli/i18n.cpp` 中英成对 13 键）：`/doctor search` 重写为三层健康
  检查——钉死版本、逐层位置与状态（缺/在不可执行/命中带实测版本）、当前用第几层、全缺时修复指引；排版纯函数
  `FormatRipgrepDoctorSection` 导出供单测钉。
- **边界说明**：兜底件照过 `--version` 精确版本门（PATH 冒牌件得先长得像 15.2.0 才被采用）；命中层 smoke 不过
  （件坏/版本错）报对应错、不悄悄下探下一层——自愈只治缺件，坏件的账在 doctor。
- **验证**：单测 41 册全绿（三层顺序四景/候选清单展开/自愈来源落账/全缺指引/doctor 排版两形状）；真机
  `search_golden_driver` 裸 exe 三幕（rg-stage 131 hits + stderr 兜底行、PATH 131 hits、三处全删 24 场景
  `search_backend_missing` + error_head 带指引）；真机 `/doctor search` 随包命中与 rg-stage 命中两形状。
  driver 的 golden JSON 增 `error_head` 字段（400 字证据窗，不进比对集）。
- **留白**：命中层版本不合时不逐层下探（报 `search_backend_version_mismatch`，文案指 doctor）；
  `docs/development/search-ripgrep-migration.md` 里旧"只认一条路"的表述是历史记录，未回写。
