# Plan 模式：只读研究硬闸、计划审阅与执行交接

状态：待做（2026-08-23 查阅 Codex、Claude Code 官方文档与 OpenAI Codex
开源实现，并巡检 LubanCode 当前命令、提示词、权限、工具、Runtime、session）

## 一句话

给 LubanCode 添一档真正的 Plan 模式。模型先读、先查、先问，再交一份能直接
开工的计划。用户没有明确批准前，宿主不准它改源码、改配置、启动有副作用的
工具。这个“不准”须有 Runtime 硬闸，不能只写进 system prompt，盼模型自觉。

## 先答面试官

面试官问：“Plan 模式不就是提示模型先列计划吗？”

答：不是。至少有六层。

1. **模式状态**：当前 turn 究竟是 Default 还是 Plan，宿主自己记，模型不能靠
   一句“现在开始写”暗中换档。
2. **提示合同**：Plan 模式另注一段不可被项目提示覆盖的 developer/system
   指令，规定先探索、再问选择、最后交 decision-complete plan。
3. **能力硬闸**：工具到了 `RunOneTool` 前再判一遍 effect。写文件、写 memory、
   未知 MCP、原生插件、危险 shell 当场拒绝，Yolo 也压不过去。
4. **交互工具**：允许模型用 `ask_user` 问真正会改变方案的选择；能从仓库查明
   的事不拿来烦用户。
5. **计划成品**：最终计划不是 `todo_write` 那张进度清单。它有独立 item、版本、
   审阅状态与批准记录。
6. **执行交接**：用户批准后才退出 Plan。宿主明确切回 Default，并另起执行 turn；
   不在旧请求半腰换工具表、换 system prompt。

一句短答可说：

> Plan 模式是一层协作协议，也是一层能力约束。提示词管“怎样想”，Runtime
> gate 管“能做什么”，session event 管“重启后还记不记得”，approval 管“何时
> 从研究切到执行”。四者缺一，便只是“请先写计划”的软提示。

## 外部资料核对

资料查于 2026-08-23。以下只记能从官方文档或官方开源仓库核实的事。

### Claude Code

- `plan` 是 permission mode。可用 `/plan [description]`、`Shift+Tab` 或启动参数
  `--permission-mode plan` 进入。
- 模式里可读文件、搜索、跑探索命令、写计划；不该改源码。
- 计划完成后会弹审批。用户可继续规划，或批准后切到不同 permission mode。
- `Ctrl+G` 可在编辑器里改计划；也可选择接受计划前清理规划 context。
- SDK 暴露 `permissionMode: "plan"` 与 `planModeInstructions`。自定义正文之外，
  CLI 仍包只读前言与 ExitPlanMode 协议尾巴。
- 工具表有 `EnterPlanMode`、`ExitPlanMode`。`ExitPlanMode` 本身要审批。
- 其“只读”不等于“一条命令都不能跑”。官方说探索 shell 可跑，权限提示仍按
  default mode 处理。

### Codex

- 官方命令表有 `/plan`。可带一段 inline prompt；任务正在跑时暂不可切。
- 当前 TUI 可见 collaboration mode 为 `Default` 与 `Plan`。这与审批/沙箱并非
  同一个枚举。
- Plan preset 不强换模型，默认把 reasoning effort 设为 medium，并注入独立
  developer instructions。
- Plan 提示分三段：先摸环境，再聊意图，后聊实现；只问仓库里查不出的取舍。
- 正式计划包在 `<proposed_plan>` 中。app-server 把它解析成独立 `plan` item，
  可流式发 `item/plan/delta`。
- `request_user_input` 主要在 Plan 模式开放。`update_plan` 反而在 Plan 模式拒绝，
  因为它是执行期间的 TODO/checklist，不是最终设计稿。
- Codex 公开协议把 `collaborationMode` 放进 turn context；`turn/started` 也带
  mode kind。默认模式模板会明说旧 Plan 指令已经结束，防止切档后模型仍守着
  上一档规矩。
- Codex Plan 允许能增进计划、又不改 repo tracked state 的测试或构建，即使
  它们写 cache/build artifact。LubanCode 首版不照抄这条宽口径；见后文。

### 两边共同点

- 模式由宿主/UI 切，不由普通用户话自行切。
- 规划期间能查资料，也能多轮问答。
- 计划完成不等于已经执行。
- 用户批准是一道显式边界。
- 模式、权限、工具可用性、计划展示各有自己的状态，不该揉成一枚 bool。

### 资料入口

- Codex developer commands：
  `https://learn.chatgpt.com/docs/developer-commands?surface=cli`
- Codex 官方 Plan template：
  `https://github.com/openai/codex/blob/main/codex-rs/collaboration-mode-templates/templates/plan.md`
- Codex collaboration mode schema：
  `https://github.com/openai/codex/blob/main/codex-rs/protocol/src/config_types.rs`
- Codex app-server protocol：
  `https://github.com/openai/codex/blob/main/codex-rs/app-server/README.md`
- Claude Code permission modes：
  `https://code.claude.com/docs/en/permission-modes`
- Claude Code commands：
  `https://code.claude.com/docs/en/commands`
- Claude Code tools reference：
  `https://code.claude.com/docs/en/tools-reference`
- Claude Agent SDK loop/permission mode：
  `https://code.claude.com/docs/en/agent-sdk/agent-loop`

## 名字先分清

LubanCode 已有 `todo_write`，UI 也会画“计划已创建/已更新”。它是施工清单。
Plan 模式交付的是施工图。两者不能再共用“计划”一词糊过去。

| 名称 | 用处 | 生命周期 |
| --- | --- | --- |
| CollaborationMode::Plan | 研究与设计，不实施 | 可跨多轮，用户明确退出 |
| PlanDocument | Plan 模式最终成品 | 可修订、批准、拒绝 |
| TodoListState | 执行期间跟进步骤 | 随任务推进而改状态 |
| PermissionMode | Confirm / Auto / Yolo | 决定执行模式下怎样审批 |
| ImplementationBrief | 批准计划后喂给执行 turn 的短合同 | 一次交接 |

不得把 `/plan` 做成 `/todos` 的别名，也不得让 `todo_write` 调用自动进入 Plan。

## LubanCode 当前底子

### 已经有的

| 能力 | 现状 |
| --- | --- |
| slash 入口 | `ParseSlashCommand` + `DispatchSlashCommand` |
| 模式 UI | `ConfirmMode::{Confirm,Auto,Yolo}`，Shift+Tab 循环 |
| 提示拼装 | `PromptOptions` + `AssembleSystemPrompt`，模块可按能力注入 |
| 运行时换提示 | `AgentLoop::SetSystemPrompt` |
| 工具过滤 | `AgentLoop::SetToolFilter` 与 `RunOneTool` 二次检查 |
| 工具审批 | needs_confirm + settings allow/deny + Hook + Broker |
| 只读 shell 粗判 | `ClassifyCommand` 能认一批只读 PowerShell/cmd/git 命令 |
| 选择题 | `ask_user`，一至四题，UI 自动添自填项 |
| 施工清单 | `todo_write` + `/todos` |
| 事件合同 | thread / turn / item、seq、QuestionRequested 等 |
| session | append-only JSONL，compact/title/cwd/queue 事件可回放 |
| 空闲会话循环 | `InteractiveSession::Run()` 在主线程逐轮消费 |

### 欠的

- 没有 CollaborationMode。现有 Confirm/Auto/Yolo 只讲审批，不讲工作阶段。
- `/plan` 不存在，status line 也无 plan 标记。
- 工具只有 `needs_confirm()` 与 `deferred()`，没有 effect/capability metadata。
- `tool_filter` 眼下主要服务 tool_search。若拿它硬塞 Plan 逻辑，容易把“没加载”
  与“模式禁止”说成同一种错。
- `ClassifyCommand` 只分 Safe/NeedsConfirm。Safe 表示 auto 档可免问，不等于零副作用。
- MCP、Lua、native plugin 没有可信只读声明。未知来源不能在 Plan 中默认放行。
- Hook 可执行外部程序。即使主工具只读，Hook 自己也可能写盘。
- session 不记 mode 与计划成品。resume 后无法知道上一轮仍在规划。
- Runtime 的 ClientCommand 还没有 SetCollaborationMode/ApprovePlan。
- ItemKind 没有 Plan。若硬拿 Text/Todo 顶替，Web/Tauri 无法开计划审阅器。

## 总设计：两根轴，不揉成一根

```text
CollaborationMode: Default | Plan
PermissionMode:    Confirm | Auto | Yolo
```

- CollaborationMode 决定“这轮准做哪类事”。
- PermissionMode 决定“准做的事还要不要问”。
- 先过 Plan capability gate，再过 Hook/schema，再过 Permission gate。
- Plan 拒绝的动作，Yolo 也不能放行。
- 从 Plan 批准执行时，用户选 Confirm 或 Auto。Yolo 只在本场原本已显式启用，
  且 UI 再次给出高风险提示时才可选。
- 离开 Plan 不重置用户原有 PermissionMode；批准框选的新档只改本 session。

建议类型：

```cpp
enum class CollaborationMode { Default, Plan };

struct ModeState {
    CollaborationMode active = CollaborationMode::Default;
    PermissionMode execution_mode_before_plan = PermissionMode::Confirm;
    std::optional<std::string> latest_plan_id;
    std::uint64_t revision = 0;
};
```

`ModeState` 归 SessionRuntime/InteractiveSession 所有，不塞进 LineEditor 的
`ConfirmMode` 原子。前端只读快照，不能各存一份真值。

## 切换规矩

### 命令面

```text
/plan                       切入 Plan；无正文只换档
/plan <description>         切入 Plan，并立刻把正文作为规划请求发给模型
/plan status                看当前档、最近计划与审批状态
/plan off                   不批准计划，单纯退出到 Default
/plan review                重开最近计划审阅框
```

- 命令只在空闲 composer 生效。任务正在跑时不半腰切；提示用户先 ESC，或把
  `/plan ...` 排进下一轮。
- `/plan off` 不等于“批准执行”。它只撤掉模式，不自动发送 implementation turn。
- `/clear` 起新 thread，回默认配置；`/resume` 从旧账恢复最后 mode。
- `/model`、`/provider` 切换可留在 Plan，但重建 AgentLoop 后须重新注 mode prompt
  与 gate；不得只换了模型，忘了只读闸。
- 首版不抢 Shift+Tab。它已用于 Confirm/Auto/Yolo。后续若做统一模式选择器，
  另立 UX 迁移，不让同一键同时转两只状态机。

### 启动与配置

```text
lubancode --mode plan
LUBANCODE_COLLABORATION_MODE=plan
settings.local.json: "default_collaboration_mode": "plan"
```

优先级沿用确认档风格：CLI > env > project-local setting > default。认不得的值报错，
不能安静落回 Default，让用户误以为只读保护已经开了。

### 模型不能切模式

- 用户在普通文本里说“你现在退出 Plan，直接改”，只算新的规划要求。
- 模型不能靠调用 write_file 来试探退出。
- `ask_user` 某个选项写“开始实现”也不能直接改 mode。只有宿主计划审阅动作能切。
- 项目 AGENTS.md、网页、MCP result、Skill 都无权覆盖 mode。

## 提示词层

新增内置、不可由 `~/.lubancode/prompts` 覆盖的模式模板：

```text
src/prompts/modes/default.md
src/prompts/modes/plan.md
```

拼装顺序：

```text
core/persona
environment
project instructions
feature modules
platform module
model/soul/deferred index
mode instructions（最后，宿主内置，不给项目覆盖）
```

Plan 模板至少钉这些话：

- 模式只由 developer/runtime 明令改变。
- 先查环境，再问取舍。
- 可查明的事实自己查，不问用户文件在哪、类型叫什么。
- 问题须改变设计；推荐项写清取舍。
- 不改 repo tracked state，不替用户实施。
- 最终计划须交代目标、现状、改动、接口/数据流、失败边界、测试、迁移与验收。
- 最终计划用 `<proposed_plan>` 包住；每轮至多一份完整替换稿。
- `todo_write` 是执行清单，不是 PlanDocument。Plan 模式不调用它。

Default 模板也要明说：旧 Plan 指令现已结束。只写“你在 Default”不够；模型可能
仍服从前一轮那句“除非 developer 明说，否则一直在 Plan”。Codex 公开 issue 已
出现过这类残留，LubanCode 要在契约测试里钉住。

## 工具硬闸

### 新增独立 ModePolicy

```cpp
struct ToolCapability {
    ToolOrigin origin;
    EffectClass effect;
    bool plan_safe_by_default;
};

struct ModeVerdict {
    bool allowed;
    std::string code;
    std::string reason;
};

ModeVerdict EvaluateModePolicy(
    CollaborationMode mode,
    const ToolCapability& capability,
    const nlohmann::json& input);
```

调用次序：

```text
registry find
  -> deferred/tool_search visibility
  -> ModePolicy               # 新硬闸
  -> PreToolUse Hook
  -> updated input schema check
  -> permission policy
  -> execute
  -> PostToolUse Hook
```

ModePolicy 拒绝用稳定 `mode_denied`，不得冒充“工具没加载”或“用户拒绝”。UI 要写
“Plan 模式禁止写入”，并给 `/plan off` 提示；模型拿到同样语义的 tool_result。

### 首版工具表

| 工具 | Plan 首版 | 说明 |
| --- | --- | --- |
| read_file | 放行 | 只读，仍受路径范围与输出上限 |
| search | 放行 | 只读 |
| context_search/context_read | 放行 | 只读会话 artifact |
| lsp | 按 method | definition/reference/diagnostic 放；rename/apply 拒 |
| web_search/web_fetch | 放行但守网络权限 | 外部读，仍有费用、注入与隐私风险 |
| ask_user | 放行 | Plan 核心交互 |
| tool_search | 放行 | 只返回目录，不等于被发现工具可执行 |
| agent | 只准 Explore | 强制 read-only role；general-purpose/worktree 拒 |
| todo_write | 拒绝 | 施工清单，不是设计成品 |
| write_file/edit_file | 拒绝 | 明确写盘 |
| memory 写入/忘记/重建 | 拒绝 | 会改项目/用户记忆；只读查询可拆开放行 |
| run_command | 仅极窄只读子集 | 见下一节 |
| background command | 拒绝 | 生命周期越过规划 turn，且可写盘 |
| MCP | 默认拒绝 | annotation 只是 hint，不是信任根 |
| Lua/native plugin | 默认拒绝 | 无 capability metadata 不猜 |
| workflow/record/install | 拒绝 | 会写定义、状态或插件 |
| submit/approve plan | 宿主动作 | 不作为普通可扩展工具注册 |

### shell 不能沿用 Auto 的 Safe

现有 `ClassifyCommand` 的 Safe 只表示“auto 档可少问一句”。它允许 `cd`、`echo`、
一批探版命令，未证明命令不会写工作树。Plan 须另开分类：

```text
PlanReadOnly / PlanUnknown / PlanMutating
```

首版只放：

- `rg`、`findstr`、`Get-Content`、`Get-ChildItem`、`Select-String`；
- `git status/log/diff/show/ls-files` 的只读参数组合；
- `pwd/Get-Location`、`Get-Item/Test-Path`；
- 版本探针，且无重定向、管道到写命令、子表达式、环境赋值。

首版不放 build/test。Codex 允许写 build artifact 的检查，但 LubanCode 尚无“只写
ignored build dir、绝不改 tracked file”的沙箱。等 workspace-write 监测与 tracked
pre/post snapshot 落地，再单独开放 `PlanCheck`：

1. 执行前记 Git tracked/untracked 指纹。
2. 只给已知 build root 写权限。
3. 执行后复核 tracked state。
4. 发现改动立即报 violation；不要擅自 reset 用户旧改动。

### MCP 与插件

- MCP `readOnlyHint` 可作展示和候选筛选，不能单独充当安全承诺。
- 放行须同时满足：可信 server、项目/用户配置显式 allow、tool annotation read-only、
  无未知外部写权限。缺一项便拒。
- Lua/native plugin manifest 后续加 effect metadata 与 capability version。旧插件按
  unknown/mutating 处理。
- DeferredTool 要透传真实 capability。不能包装后变成“看不出来源，先放了”。
- Plan mode 不允许插件通过返回“请执行这条命令”绕过；返回只当数据。

### Hook

Hook 是最容易漏的一扇后门。只读工具触发的 Pre/Post hook 仍能起进程写盘。

首版取舍：

- UserPromptSubmit、SessionStart 等非工具 Hook 照跑，但 UI 明示 Plan 只保证
  agent 工具路径，不保证用户自己配置的可信 Hook 毫无副作用。
- PreToolUse、PostToolUse 仅运行声明 `plan_safe=true` 且已信任的 handler。
- 没 metadata 的旧 handler 在 Plan 跳过，记 `hook_skipped_in_plan`；安全 deny 规则
  应迁到宿主静态 policy，不能指望一个被跳过的 hook 才拦危险动作。
- PermissionRequest 不得把 mode_denied 改成 allow。Hook 只可收紧，不能放宽。
- 后续若有只读进程沙箱，再允许普通 Hook 在沙箱中跑。

## `ask_user` 在 Plan 中怎样用

现有工具大体可复用，但合同要收紧：

- 一轮最多三题，与 Codex UI 对齐；现版四题可保协议兼容，Plan prompt 建议三题。
- 每题二至四项，推荐项标明原因；UI 仍添自填项。
- 先探索，后提问。没有任何 repo 探索便问“文件在哪”，测试应判坏样本。
- 取消问题不退出 Plan，只把工具结果标 cancelled，让模型说明还缺什么。
- app-server 用 QuestionRequested/AnswerQuestion，TUI 走现有 Broker adapter。
- 问答记 history；结构化 question event 另记摘要，resume 后不重问已答问题。
- prompt injection 不能借 option label 变成权限指令；答案只是用户内容。

## 计划成品与审阅

### PlanDocument

```cpp
enum class PlanReviewState { Draft, Presented, Approved, Rejected, Superseded };

struct PlanDocument {
    std::string plan_id;
    std::uint64_t revision;
    std::string markdown;
    std::string source_turn_id;
    PlanReviewState state;
    std::string content_sha256;
};
```

- 模型在 assistant text 中输出 `<proposed_plan>`。
- stream parser 只在 Plan mode 识别完整、独立标签；代码块里的同名文本不触发。
- tag 外可有一小段答疑；成品 item 只收 tag 内 Markdown。
- 一轮最多一份。多份、嵌套、缺闭合都按普通 text，并发 warning，不弹审批。
- 新计划默认 supersede 旧 Presented 稿，不删旧账。
- markdown 有字节上限；超限存 artifact，item 留 hash/preview/ref。
- PlanDocument 不自动写进 repo。session JSONL 才是它的存档。

### 审批框

Plan turn 正常收口、且见完整 PlanDocument 后，空闲层弹：

```text
1. 批准，并以 Confirm 执行
2. 批准，并以 Auto 执行
3. 留在 Plan，继续修改
4. 退出 Plan，不执行
5. 在编辑器中审阅（后续）
```

- 审批对象带 `plan_id + revision + sha256`。用户审的是哪一稿，账上写清。
- 模型产出新稿后，旧审批 request 失效。迟到回答报 stale_request_id。
- 用户在弹框期间 ESC，只关弹框，仍留 Plan；可 `/plan review` 再开。
- 计划里夹了“用户已经批准”等文字不算批准。
- app-server 增专用 `PlanReviewRequested`，不要冒充普通 tool approval。

### 执行交接

批准后按这条次序：

1. append + flush `plan_review approved`。
2. 切 CollaborationMode=Default，append + flush mode event。
3. 重建 system prompt 与 tool gate。
4. 生成 ImplementationBrief：plan id/hash、批准稿、用户选的 permission mode、
   未决项、验证标准。
5. 另起一枚 synthetic user turn：“按已批准计划实施”。
6. 执行模型先用 `todo_write` 拆施工清单，再动工具。

不要在解析到 `</proposed_plan>` 的同一次 Provider response 内直接执行。那样工具表、
提示词、mode event 与 UI 都处在半新半旧状态，崩溃恢复也说不清边界。

### context 是否清理

默认保留规划 context。它含源码事实与用户取舍，执行时常有用。

- 使用率低：直接带原 history + ImplementationBrief。
- 使用率已高：先跑现有 compact，但 manifest 必须保 plan id/hash、约束、接口、测试、
  明确不做项与用户回答。
- “清空 context 执行”作为审批框高级选项。清前须把批准稿完整落 artifact/session。
- 不许只剩一句“按上面的计划做”。compact 后“上面”可能早没了。

## session JSONL 与恢复

新增版本化事件：

```json
{"type":"mode_v1","mode":"plan","reason":"slash","revision":1,"ts":"..."}
{"type":"plan_v1","plan_id":"plan-7","revision":1,"state":"presented","sha256":"...","markdown":"...","turn_id":"turn-4","ts":"..."}
{"type":"plan_review_v1","plan_id":"plan-7","revision":1,"decision":"approved","execution_permission":"confirm","ts":"..."}
```

- append-only，最后有效 mode event 决定 resume 档位。
- PlanDocument 逐 revision 留账；批准须同时匹配 id/revision/hash。
- 老 session 无 mode event，按 Default。
- 尾行截断跳过；若 mode 已写 Plan、计划行坏了，恢复仍在 Plan，只提示成品损坏。
- Approved 已落、Default mode 未落：恢复时按事务恢复规则完成 mode 切换，但不自动
  重跑 implementation turn；先查该 turn 是否已存在。
- Default 已落、synthetic turn 未落：显示“批准已记，执行尚未启动”，让用户一键续。
- synthetic turn 已落：按正常 AgentLoop/tool trace 恢复，不能再发第二遍。
- `/clear` 新 session 不继承旧 Plan；`/fork` 复制批准稿，但新 fork 审批状态独立。
- compact 只改有效 history，不删 mode/plan/review 事件。

## Runtime 与多前端合同

扩展中立合同：

```text
ClientCommandKind:
  SetCollaborationMode
  ReviewPlan
  ReopenPlanReview

ServerEventKind:
  CollaborationModeChanged
  PlanReviewRequested
  PlanReviewResolved

ItemKind:
  Plan
```

payload 至少有：

```text
mode / previous_mode / reason / revision
plan_id / plan_revision / sha256 / artifact_ref
available_decisions / selected_permission_mode
```

- TUI slash 只翻译成 typed command；业务不永远困在 `DispatchSlashCommand`。
- TUI 可先画 Markdown + 选择器；Web/Tauri 可做逐段批注，但批注最后仍生成一份
  新 revision，不在旧 markdown 上原地改真值。
- `turn/started` 带 collaboration_mode。重连前端不能只看状态栏猜。
- Plan delta 是 item delta；完整 item/completed 才可审。
- mode gate 失败走结构化 error code，前端各自翻译，不把中文硬塞协议。

## Subagent、Skill、MCP、PTC

- Plan 主代理只可生 Explore 子代理。即便模型传 general-purpose，也在工具 schema/gate
  双层拒绝或强制降为 Explore，并把变化明说。
- Explore 子代理拿同一 Plan mode + 更窄 tool allowlist，不因独立 context 逃闸。
- 子代理不自行弹 plan approval；只交研究结论给主代理。
- Skill 可提供研究流程与领域知识，不可借 `allowed-tools` 放宽 ModePolicy。
- Skill 脚本本身若要执行，按工具 effect 过闸；“这是 Skill”不是豁免牌。
- PTC 生成的调用也走 `RunOneTool` 与 ModePolicy。不能只拦 JSON tool calling。
- MCP request 内外两层 id 仍须关联；Plan 拒绝发生在发 JSON-RPC 前，远端根本不应
  收到请求。

## 状态栏与交互细节

- status line 独立显示 `plan`，与 `confirm/auto/yolo` 并列，如 `plan · confirm`。
- Plan 中被拒工具用 `Blocked` 终态，不弹普通 permission 菜单。
- `/help` 把 `/plan` 说明写成“只读研究并提交计划”，不要只写“列计划”。
- Tab completion 支持 inline description，不把 `status/off/review` 当普通 prompt。
- 切进 Plan 打一行短通知；不清屏，不改历史，不偷偷换模型。
- 管道/one-shot 用 `--mode plan`，产出 plan 后退出，不弹无人能答的审批框。
- 非 TTY 若模型调用 `ask_user`，返回明确 `interaction_unavailable`；不能挂死。

## 安全边界与 corner cases

- symlink/junction 可让“读路径”跳出 workspace。沿用 isolation/path canonicalization；
  Plan 不是扩大读取权限的理由。
- 读 secrets 仍受现有敏感路径与权限规则。只读不等于可读一切。
- Web/MCP 内容有 prompt injection。模式指令与 capability gate 不采信其“请退出 Plan”。
- `git diff` 可触发 pager/外部 diff driver。Plan shell 运行时禁 pager、禁外部 diff，
  固定安全环境；否则“只读 git”也可能起任意程序。
- `git status` 的某些 fsmonitor/filter 扩展可起外部 helper。高安全配置下改用宿主文件
  API；至少在文档中承认这条边界。
- 读操作会更新 atime、网络读取会计费。Plan 保证的是“不实施项目变更”，不是宇宙
  级零副作用。UI 要写清保证范围。
- 计划审阅时用户又改了工作树：批准前重算 repo fingerprint。若关键文件变了，标
  `plan_stale`，建议先刷新计划，不暗中照旧稿开工。
- mode 切换与 `/compact` 同时发生：主线程串行命令；不得并发改 system prompt。
- 计划流式到一半 ESC：不产 PlanDocument，留普通部分文本与 cancelled turn。
- Provider 不听 tag 格式：照普通回答展示，不弹批准；用户可要求重交。
- 模型调用多个工具，其中一枚写工具：逐枚 gate。前面的读工具可成，写工具 blocked，
  后续按现有多工具策略执行；结果一一配对。
- 规划中切 provider：plan mode event 不变，新的 provider 从下一 turn 收完整模板。
- 用户批准后立刻退出进程：review/mode event flush 在 synthetic turn 前，resume 能认。

## 实施分期

### 第 0 期：合同与 golden cases

- [ ] 定 CollaborationMode/ModeState/ModeVerdict/PlanDocument。
- [ ] 定 Default/Plan 两份不可覆盖的 mode template。
- [ ] 写正常计划、问答两轮、半截计划、拒绝写工具、批准后崩溃 golden session。
- [ ] 钉“Plan 与 todo_write 不同”“Plan 与 PermissionMode 正交”。
- [ ] 定保证范围：首版不允 build/test，不宣称 Hook/OS 绝对零副作用。

### 第 1 期：模式最小闭环

- [ ] slash enum/parser/help/i18n 加 `/plan`。
- [ ] Session 持有 ModeState；status line 展示 plan。
- [ ] PromptAssembler 末尾注 mode template；Default 明令结束旧 Plan。
- [ ] `mode_v1` 序列化、append、parse、resume。
- [ ] `/clear`、`/resume`、`/model`、`/provider`、`/worktree` 重建路径全保 mode。

### 第 2 期：硬闸

- [ ] ToolRegistry/Tool 增 capability metadata，不靠 dynamic_cast。
- [ ] `RunOneTool` 接 ModePolicy，放在 Hook/permission 之前。
- [ ] 给 builtin/LSP/web/agent/PTC 标 effect。
- [ ] 另写 Plan shell classifier 与测试，不复用 Auto Safe 结论。
- [ ] MCP/plugin/deferred 未知来源默认拒。
- [ ] Hook 增 plan_safe metadata 与 skipped event。

### 第 3 期：Plan item 与审阅

- [ ] 流式 parser 识别 `<proposed_plan>`，防代码块、嵌套、半截。
- [ ] Runtime 加 ItemKind::Plan 与 delta/completed。
- [ ] session 加 `plan_v1`，大稿接 artifact。
- [ ] TUI 计划审阅框；id/revision/hash 防迟到批准。
- [ ] `plan_review_v1` 与 stale plan 检测。

### 第 4 期：执行交接

- [ ] 批准后先落 review，再落 Default mode，再排 implementation turn。
- [ ] ImplementationBrief builder 与 compact manifest 守恒。
- [ ] Confirm/Auto 选择；Yolo 只在已显式启用时出现。
- [ ] 执行首步才建 todo_write 清单。
- [ ] 崩在每道栅栏的恢复与幂等测试。

### 第 5 期：Runtime/app-server/SDK

- [ ] typed SetCollaborationMode/ReviewPlan/ReopenPlanReview。
- [ ] mode/plan/review 事件接 EventSink 与冷回放。
- [ ] app-server schema 与协议测试。
- [ ] one-shot `--mode plan`、配置/env 优先级。
- [ ] Web/Tauri 计划审阅器只消费中立事件。

### 第 6 期：受控检查与计划编辑

- [ ] 只写 build root 的 PlanCheck 沙箱与 Git tracked pre/post guard。
- [ ] `Ctrl+G`/外部编辑器改计划，保存成新 revision。
- [ ] 可选“接受前 compact/清 context”。
- [ ] repo fingerprint 精确到计划引用文件，减少无关变更误报 stale。

## 测试矩阵

### 命令与状态

- [ ] `/plan`、`/plan 正文`、status/off/review 大小写与空白。
- [ ] 任务运行中 `/plan` 不半腰切。
- [ ] Shift+Tab 仍只转 PermissionMode，不误退 Plan。
- [ ] CLI/env/settings/default 优先级。
- [ ] 非法 mode 明报，不静默回 Default。

### 提示与切档

- [ ] Plan template 永远在用户/项目模块之后。
- [ ] 用户覆盖 prompts 不能删 Plan 硬指令。
- [ ] Default template 明说旧 Plan 已结束。
- [ ] model/provider/clear/resume/worktree 后 mode prompt 不丢、不重复叠加。
- [ ] compact 后 mode 不靠摘要存活。

### 工具 gate

- [ ] read/search/context/LSP read 放行。
- [ ] write/edit/memory write/todo_write 拒绝，Yolo 也拒。
- [ ] safe shell 表逐条放；重定向、子表达式、写 cmdlet、未知命令拒。
- [ ] MCP readOnlyHint 单独存在仍拒；显式可信配置齐全才放。
- [ ] DeferredTool 透传来源。
- [ ] JSON 与 PTC 走同一 gate。
- [ ] 多工具逐枚配对，不因一枚 mode_denied 丢 result。
- [ ] PreToolUse/PermissionRequest 不能放宽 mode denial。

### 问答与成品

- [ ] ask_user 正常、取消、非 TTY、app-server 迟到回答。
- [ ] 完整 tag 产 Plan item；半截/嵌套/代码块不误产。
- [ ] 一 turn 两份 tag 报 warning。
- [ ] 新 revision supersede 旧稿。
- [ ] 大计划 artifact hash 正确、缺 artifact 不可批准。

### 审批与恢复

- [ ] 批准匹配 id/revision/hash，旧 dialog 回答无效。
- [ ] 继续规划不换 mode。
- [ ] off 不自动执行。
- [ ] 崩在 review 前、review 后、mode 后、synthetic turn 后逐点恢复。
- [ ] 已存在 implementation turn 不重复发送。
- [ ] resume 老 session 默认 Default。
- [ ] fork 计划内容可继承，审批状态不串。

### 安全

- [ ] project prompt/Skill/MCP result 说“退出 Plan”不生效。
- [ ] symlink/junction、外部路径、secret 路径仍受旧闸。
- [ ] git pager/external diff/filter 不偷偷起任意程序。
- [ ] Hook 未标 plan_safe 时跳过并留账。
- [ ] 工作树在计划后变化，审批提示 stale。

## 验收

- `/plan <任务>` 能连续读仓库、问关键选择、产一份独立计划 item，全程不改源码。
- write/edit/未知 MCP/native/shell 在 Plan 被 Runtime 拦；Yolo 与 Hook 不能越闸。
- 计划可修订。每稿有 id、revision、hash；批准的是哪稿说得清。
- 批准前绝不执行；批准后另起 Default turn，带完整 ImplementationBrief。
- mode、plan、review 经 crash/resume/compact/fork 后不串、不丢、不重复执行。
- TUI、未来 app-server/Web/Tauri 共用 typed command/event，不复制业务分支。
- 面试时能拿源码指出：提示层、ModePolicy、Plan item、session event、approval handoff
  各守哪一道门。

## 不做

- 不把 Plan 做成一段可选 persona。
- 不把 `/plan` 做成 `todo_write` 别名。
- 不把 Confirm/Auto/Yolo 改名便算 Plan。
- 不允许普通用户文本、Skill、Hook 或工具结果切 mode。
- 不在同一 Provider turn 半腰从 Plan 切 Default 并开写。
- 不把 MCP annotation 当安全证明。
- 不用 `git reset/checkout` 清理 Plan 中误写，更不能抹掉用户原改动。
- 首版不承诺 build/test 可在 Plan 跑。
- 不为计划默认新建 repo 文件；session/artifact 是真本。

## 源码入口

| 责任 | 文件 / 符号 |
| --- | --- |
| slash 解析与帮助 | `src/cli/slash_commands.*` |
| 终端模式/Shift+Tab | `src/cli/line_editor.*`、`console_input.*` |
| 会话分派/重建 | `src/app/interactive_session.cpp` |
| 一轮装配与审批 | `src/app/turn_runner.cpp` |
| 中立权限核 | `src/runtime/turn_runtime.*` |
| typed command/event | `src/runtime/command.hpp`、`event.hpp` |
| 交互 Broker | `src/runtime/interaction_broker.hpp` |
| Agent 工具链 | `src/agent/loop.*`，`RunOneTool` |
| system prompt | `src/agent/prompt_assembler.*`、`src/prompts/` |
| 工具元数据 | `src/tools/tool.hpp`、`registry.*` |
| shell 粗分类 | `src/tools/command_safety.*` |
| 选择题 | `src/tools/ask_user.*` |
| 施工清单 | `src/tools/todo_tool.*` |
| session JSONL | `src/agent/session_store.*` |
| 统一 Runtime 大单 | `todos/显示系统彻底剥离_统一SessionRuntime接TUI_Web_Tauri.todo` |
| 工具逐枚追踪 | `todos/工具调用逐枚追踪_五工具故障归因崩溃恢复与副作用补偿.todo` |
