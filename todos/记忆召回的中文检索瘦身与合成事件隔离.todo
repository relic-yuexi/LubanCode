# 记忆召回的中文检索瘦身与合成事件隔离

状态：待做（2026-08-16，召回主路已通过）

## 先记通过项

在临时项目执行：

```text
/memory remember fact 发布代号 :: 本项目发布代号是青瓷-47。
```

退出，另起新进程，再问“这个项目的发布代号是什么？只回复代号，不要调用工具。”模型直接答“青瓷-47”。`trace-last.json` 显示：

```text
hard_hits = 1
score = 13
injected = true
injected_bytes = 52
```

这说明跨会话落盘、硬命中、上下文注入、模型采用四环都通。别为优化把主路拆坏。

## 已见的脏账

同一份 trace 的检索词里有：

```text
么？  具。  个项  号，  复代  用工  这个  项目
```

中文按二元片段硬切，标点与句尾也进词表。短问尚能命中；历史一长，这类碎片会抬高假阳性，挤掉真正项目词，也白耗 index 与 trace 字节。

## 要做

1. 先归一化：NFKC、大小写、全半角、标点边界、路径分隔符、代码标识符。标点不得黏进中文二元词。
2. 中文检索用“词 + 字 n-gram”双路。项目名、代号、文件名、命令、错误码保留整词；常见虚词与单纯句式片段降权。
3. hard hit 只给稳定实体：显式标题、键值、路径、symbol、用户 `/memory remember` 的 key。普通二元片段不能凑成 hard hit。
4. trace 报每个 term 的来源与权重。用户要看得出为何命中，不只见一把碎字。
5. 检索预算按“去重后有效字节”算。同一事实多次命中只注一份；相同证据只留最新、最可信一条。

## 两项旧包现象，须在当前包复核

旧 release 曾见：

- 零命中也给每轮塞整段 memory 使用说明；
- 后台完成唤醒这类宿主合成 prompt 也跑记忆检索，trace 留下一串无意义词。

当前真机只确认了正常用户提问的成功召回，尚未把这两项钉死。实现前先加 trace：`query_origin=user/background_completion/hook/compact/system`。只有 user 与明确需要事实的 main 回流才检索；合成控制消息默认跳过。

## 完成标准

- “发布代号”跨会话测试仍一次答中。
- 中文标点不进 term；常见虚词不拿高分。
- 同一事实反复保存、同一路径反复探索，不重复注入。
- 零命中不塞空脚手架；后台完成唤醒不污染 trace。
- 用 100 条中文项目记忆跑一组固定问句，给出召回率、误命中率、注入字节 P50/P95；优化前后可比。

