# 记忆写入侧改进：时间线锚点与摘要质量

- 状态：待实施（2026-09-03 立）
- 级别：P1（检索层已吃饱，写入侧成了新天花板）
- 类型：LubanCode 产品改进（记忆写入与抽取）
- 现场基线：LoCoMo E1 改后 R@1=0.612 / R@3=0.791 / R@5=0.865；E2 可答四桶配对 +0.20~0.40、adversarial 幻觉率 35%。**temporal 桶平手（B 0.40 vs A 0.40）**——R@5 已 0.923（检索把货送到了），败在跨条目时间推理。
- 证据：`eval/locomo/retrieval_improved.md`（改进后）、`eval/locomo/report.md`（E2 首批）
- 关联：《记忆检索与注入改进_content进索引与幻觉护栏.todo》（已完工，检索侧）、《智能体记忆效果_个性化适配续接快照与在线评测.todo》（§五"summary 质量"的总方向）
- 不在本单：检索算法再调（检索侧刚收口）；向量库；记忆分层重构

## 一、两刀

### 1.1 时间线锚点（temporal 桶的病根）

temporal 题问"5月8日之前那周""6月9日前几天"——答案散在多条 fact 里，注入的条目各自有日期但**模型拼不出先后关系**（正文里日期被叙述淹没）。

- [ ] 写入侧：fact 类 topic 的 frontmatter 加 `occurred_at`/`event_date` 字段（抽取时从材料里提；提不出留空不造假），正文头部落一行人类可读的时间锚（如 `【2023-05-08】`，样式自定但全库一致）。
- [ ] 注入侧：召回多条含时间字段的条目时，按 `occurred_at` 排序注入（同分稳定序：时间并列按 topic id），时间锚保持可见——模型拿到的是一条时间线，不是一把散卡。
- [ ] schema：frontmatter 加字段走 schema 3 的既有演进路（向后兼容：旧条目无字段不炸、不参与时间排序）。
- [ ] 单测：时间字段读写往返、无字段旧条目混排稳定、注入排序与时间锚可见性。

验收：LoCoMo temporal 桶 E2 复跑（96 题全量双态）——temporal 配对增益从 0（平手）到 **≥+0.15**；可答四桶与 adversarial 不退。

### 1.2 摘要质量（检索天花板抬高）

E1 灌库用的官方摘要；真路是 memory worker 自己抽的 summary——抽取质量决定 title/summary 加权词的成色，孬 summary 直接压 R@1。

- [ ] 抽取 prompt 改进：summary 要求结构化要点（谁、做了什么、何时、何地——实体与日期必须进摘要），长度上限与现状持平（不加预算）；title 要求含核心实体。
- [ ] 对照验证：同一批会话材料，旧/新抽取 prompt 各灌一套库（走真 EnqueueJob→worker 路），E1 同题集跑双库对比——R@1 提升记表；若新 prompt 反而降（要点式摘要丢了叙事词），如实回退并记教训。
- [ ] 抽取成本对账：cheap 路由的请求次数与 token 前后对照（不许为摘要质量翻倍烧钱）。

验收：E1 双库对比表进 `eval/locomo/`（summary_quality.md）；R@1 提升 **≥0.03** 或如实记录"叙事词不可丢"的负结论（负结论也算数，不硬刷）。

## 二、E2 放量（顺路钉死结论）

- [ ] E2 从 2 场放量到 **5 场**（含 temporal 全量），双态配对；逐题账落盘可复算
- [ ] 报告：五场合计四桶增益与幻觉率的区间（bootstrap 或逐场分布），不只报平均

## 三、实施批次

- [ ] P1：时间线锚点（写入+注入+schema）+ temporal 桶 E2 复跑
- [ ] P1：摘要质量双库对照 + 成本对账
- [ ] P2：E2 放量五场 + 报告

## 四、不做什么

- 不造假时间（材料里没有就留空，不许模型猜日期填字段）
- 不为 temporal 单桶牺牲四桶（复跑同报，退步即回退）
- 不动检索打分与注入预算（刚收口，别翻烧饼）
- 负结论如实记（1.2 若要点式摘要降检索，回退并写教训）
