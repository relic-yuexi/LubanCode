# /skill list 漏层与计数口径对齐

- 状态：已修（2026-09-04 接管工头落地，worktree-agent-a694d2ae205781a61 分支；单测钉四层枚举与遮蔽标注 4 册新绿，沙盒真机四层布景 `/skill list` 形状全对；全量 377 册中 376 绿，另 1 册 `unit.memory.memory_global_gate` 为 main 既有漂移、另有单子在册，与本单无关——见文末落地记录）
- 级别：P2（功能可用但显示不全，用户困惑"数量不对"）
- 现场实录：右下角计数 `5 skills`，`/skill list` 只列 2 个（anysearch 项目级/monid 主目录级）——用户问"没识别对吗"。
- 病根（已查明）：**计数与列表两处口径**——计数 `SetSessionSkillCount(skills.size())` 数的是 LoadSkills 全部实体（官方 2 + 项目 anysearch + `.agents` 层 humanizer + 主目录 monid = 5，遮蔽已扣）；`/skill list`（settings_commands.cpp:296-297）只扫 `global_skills_root`（~/.lubancode/skills）与 `project_skills_root`（.lubancode/skills）两层——**漏 `.agents/skills` 层与官方技能层**。计数是对的，列表漏显。
- 修法：`/skill list` 补全四层来源分段显示——官方（lubancode-config/workflow 等，标"官方"）、项目级、`.agents` 层（标"agents 共享"）、主目录级；被遮蔽的标"被 X 遮蔽"。计数口径不动（实体数正确）。
- 验收：真机 `/skill list` 列出 5 个与计数一致；被遮蔽条目可见有标注；单测钉四层枚举与遮蔽标注。

## 落地记录（2026-09-04）

- **加载层枚举口**（不动 LoadSkills 语义）：`src/tools/skill_loader.{hpp,cpp}` 新增
  `SkillLayerEntry{meta, active, shadowed_by}` 与 `EnumerateSkillLayers(project_dir,
  home_dir, official_skills_dir)`——五处根（官方/主目录 .agents/主目录
  .lubancode/项目 .agents/项目 .lubancode）全数摊开，按与 LoadSkills 逐字
  对齐的合并次序判生效/遮蔽（含"旧版官方维护副本让位"分岔），旧版播种
  副本也亮出来标被谁遮。`meta.source_level` 打四层展示标签（"agents 共享"
  单立一段），与 LoadSkills 的三级标签不是一套。package 挂载根不进这张
  账——/skill list 管散装四层，包内清单归 /package 盘点。
- **命令侧**：`src/app/commands/settings_commands.cpp` 的 `list` 动词改吃
  枚举账，四段分列（项目级 → agents 共享 → 主目录级 → 官方，段内按名
  排序），头部报"N 个生效（另有 M 个被遮蔽）"，被遮条目带"被 X 遮蔽"
  标注；安装来源（远端 URL/装机时间）按目录路径对回 ListStoredSkills 的
  账本，对得上才标、账本坏照旧报错。`HandleSkillCommand` 加 `home_dir`
  形参（分派处从 ctx.home_dir 递入）。
- **计数口径未动**：`SetSessionSkillCount` 仍吃 `RefreshSkills` 的
  `LoadSkills` 结果；枚举账的 active 集合与它同一套法则（单测钉对齐）。
- **i18n**：`src/cli/i18n.cpp` 中英两处——`cmd.skill.list_header` 改带
  生效数，新增 `shadowed_total`/`shadowed_by`/`scope_official`/
  `scope_agents`，`.lubancode` 两层复用旧 scope 键。
- **单测**：`tests/unit/config/test_skills.cpp` 新增第 4 节 4 册——五处根
  摊开与四层标签、五处同名遮蔽链逐级标注且胜者与 LoadSkills 对齐、旧版
  官方副本让位标注（含无新版时照常生效）、主目录/官方缺位兜底。4 册全绿。
- **沙盒真机形状自验**（临时 USERPROFILE 布四层：home 层 monid +
  humanizer、项目层 anysearch + agents 层同名 lubancode-config、exe 旁
  摆官方两技能）：`/skill list` 出"本机技能: 5 个生效，另有 1 个被遮蔽"，
  四段 1/2/1/2 对号，官方 lubancode-config 标"被 agents 共享 遮蔽"，
  与启动时的同名冲突警告同账。
- **留白**：用户真机（anysearch/monid/humanizer 那台）复验由主控侧做；
  package 挂载的技能不进 `/skill list`（计数含包内技能时会大于列表生效
  数，属口径设计——包内清单归 /package）。
- **全量 ctest**：377 册中 376 绿；`unit.memory.memory_global_gate` 红
  为 main 既有漂移（`todos/memory_global_gate测试漂移小修.todo` 在册，
  断言盯 `CheckGlobalMemoryHealth` 已删的 `projects/` 旧路），修前修后
  同在，非本单引入。
