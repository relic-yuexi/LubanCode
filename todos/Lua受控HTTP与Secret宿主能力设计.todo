# Lua 受控 HTTP、Secret、取消与字节帽设计
- 状态：阶段 0-3 已落地（0.26.119 合同与 SecretResolver、0.26.122 受控 HTTP、0.26.123 Lua Host API）；阶段 4（manifest-backed Lua 与 Package 挂载）已落地，5-6 待实现
- 日期：2026-08-30
- 范围：manifest-backed embedded Lua、宿主 HTTP API、插件级 Secret 解析、取消、资源上限、信任、诊断与 AnySearch 参考插件
- 前置：`src/tools/lua_tool.*` 的 Pure 画像、指令/内存预算与取消钩；`src/runtime/plugin_contract.*`；`src/package/code_mounting.*`
- 关联：`todos/统一Package封装与组件挂载系统设计.todo`、`docs/features/extensions/README.md`
- 不在本单：WebSocket/SSE、常驻连接、浏览器下载器、任意 TCP/UDP、自动安装依赖、插件市场、第一期接 OS Keychain、向 Lua 暴露密钥明文

## 一、先下断语

LubanCode 眼下的 Lua 适合纯计算。它已有三道软墙：

- Pure 画像关掉 `io`、`os.execute`、`os.exit` 与 `package.loadlib`。
- 自定义 allocator 限内存。
- instruction hook 限 CPU，并在脚本跑动时检查 ESC 取消。

它还不能稳稳当当地访问 HTTP。Lua 标准库没有 HTTP、TLS 与 JSON 客户端。若切回 Trusted 画像，再借 `curl`、`io.popen` 或外部 Lua 模块联网，跨平台、密钥、取消、超时、重定向和输出上限都会散开。

本单定案：

1. 裸 `.lua` 继续走 Pure 画像，只做本地计算；现有行为一字不动。
2. 需要联网的 Lua 必须有 `plugin.json`，使用 manifest-backed `embedded-lua`。
3. HTTP、TLS、代理、DNS、取消和字节帽都由 C++ 宿主执行，Lua 不碰 socket。
4. Secret 由宿主解析并代填 HTTP 头。Lua只拿不透明引用，看不见原文。
5. 启动扫描与 Lua chunk 顶层执行期间，HTTP 与 Secret 注入一概不可用；只有某次工具 `execute` 的动态作用域里能用。
6. 网络权限按精确 HTTPS host 记账。第一期不收通配符、IP 字面量、私网地址、任意端口。
7. 项目与 Package 内代码仍过内容指纹信任门。权限声明变一个字节，旧信任立即失效。
8. AnySearch 只作首个参考插件，不把厂商逻辑写进宿主。

一句话：Lua 只描述请求，宿主握住水管和钥匙。

## 二、为什么值得做

AnySearch 现有 Skill 带 Python、Node、PowerShell、Bash 四套 CLI。它跨客户端，也能按平台找可用运行时。若只用 Process Plugin 包一层 Python runner，安装物没有变轻，跨平台还少了三条退路。

Lua Host API 做成后，形状才真正收紧：

```text
plugin.json
anysearch.lua
```

用户不用装 Python、Node、PowerShell 7、编译器或第三方 Lua 模块。Lua 脚本随 LubanCode 自带解释器运行。API 仍会变化，改动只落在一份 Lua adapter 与一份 manifest。

它带来的价值有四样：

| 价值 | 具体落点 |
|---|---|
| 零外部运行时 | 三平台共用宿主内嵌 Lua |
| 固定工具合同 | name/description/input schema 全在 manifest，模型不猜命令 |
| 宿主统一治理 | trust、确认、Hook、取消、超时、资源帽、trace 同路 |
| Secret 不进模型 | 宿主代填请求头，Lua 与工具参数都拿不到原文 |

这套能力不替代 Skill。Skill 仍教模型何时搜索、怎样选子域；Lua Plugin 只把确定的 HTTP 动作做稳。

## 三、源码现状与缺口

### 3.1 已有底子

| 现有件 | 已有本事 | 本单怎样复用 |
|---|---|---|
| `src/tools/lua_tool.*` | JSON/Lua table 互转、Pure/Trusted 画像、内存帽、指令预算、ESC hook、per-state mutex | 保留软墙；给 state 注入受控 `luban` 模块和本次调用上下文 |
| `src/runtime/plugin_lua.*` | 扫裸 `.lua`、登记工具、给 registry 造 adapter、分发 cancel | 扩成 manifest-backed Lua runtime owner；裸 Lua 兼容路不动 |
| `src/runtime/plugin_contract.*` | `plugin.json` 严格解析、Schema 校验、process runtime、权限展示字段 | 增加 manifest v2、Lua entry、network/secrets/limits 合同 |
| `src/runtime/plugin_tool.*` | 统一工具 Schema、取消指针、结果与错误回填 | 复用模型侧 Tool；Lua 分支改由专属 adapter 执行 |
| `src/api/http_stream_transport.*` | cpr/libcurl、连接超时、硬墙钟、Progress cancel、网络错误分型 | 抽出共用传输底座；Lua 用非流式受控 HTTP，不直接调用 SSE 专用函数 |
| `src/package/*` | code-bearing 信任、内容指纹、原子挂载、Package data 路径、会话快照 | 放行 manifest-backed embedded Lua，纳入整包事务 |
| `platform::GetEnvVar` | 跨平台读宿主环境变量 | SecretResolver 的环境来源 |

### 3.2 眼下缺口

1. `LuaTool` 只收“一份文件、一件工具”的返回表，没有 manifest-backed 多工具分派。
2. `plugin.json` 虽能解析 `runtime.kind=embedded-lua`，`PluginToolAdapter` 会明拒非 process；Package 挂载也只收 process。
3. Lua state 没有宿主 HTTP 函数。
4. instruction hook 只能在 Lua VM 执行指令时检查取消。进入阻塞 C 函数后，必须由 HTTP transport 自己查 cancel。
5. 现有 `PostSseStream` 只为模型流式 POST 服务，不适合直接冒充通用插件 HTTP。
6. `permissions.network` 眼下是布尔记账，不限制 host、scheme、port，也不形成实际网络墙。
7. Provider 有 `key_env/api_key` 规矩，Plugin 还没有通用 SecretResolver。
8. 源码树里的 `.env` 会进 Plugin/Package 内容指纹；改 Key 会撞掉信任，也容易被打包带走。
9. `docs/architecture/extensions/plugin-runtime.md` 的部分 Lua 风险描述落后于现源码；实现时须一并校正。

## 四、边界与所有权

```text
plugin.json
  -> 声明工具、精确网络目的地、Secret 名、可下调资源帽
  -> PluginManifestV2
      -> trust / approval / inspect / doctor
      -> ManifestLuaRuntime
          -> Lua state(Pure)
          -> LuaCallContext(只活一次 execute)
              -> SecretResolver(宿主拥有)
              -> BoundedHttpClient(宿主拥有)
              -> cancel pointer(调用方拥有)
```

所有权定死：

| 事实 | 唯一 owner |
|---|---|
| 哪些 host 可以访问 | Plugin manifest |
| Secret 从哪儿找、何时可用 | `SecretResolver` |
| Secret 明文寿命 | 宿主侧 `SecretValue` RAII |
| HTTP socket/TLS/DNS/代理 | `BoundedHttpClient` |
| 本次取消旗 | `ToolExecutionContext` |
| 请求与响应字节上限 | 宿主硬上限 + manifest 下调值 |
| 模型可见工具 Schema | Plugin manifest |
| Lua 业务路由与响应解释 | 插件 Lua 脚本 |
| 安装、版本、信任与会话快照 | Package/runtime mount |

Lua 不持全局 HTTP client，不持跨调用连接池，不缓存 Secret 明文。若日后要长连接、订阅或 WebSocket，另走 MCP/Channel/专属 runtime。

## 五、manifest v2

### 5.1 兼容策略

- `manifest_version: 1` 的 process 插件照旧解析、照旧执行。
- 旧裸 `.lua` 照旧扫描，最终名字与 Pure 画像不变。
- capability-bearing embedded Lua 必须写 `manifest_version: 2`。
- v1 写 `embedded-lua` 继续明报“manifest-backed Lua 需 v2”，不拿半份合同猜。
- v2 未声明 `permissions.network` 时，`luban.http.request` 永远报 `network_not_declared`。
- v2 未声明 Secret 时，Lua 不得按任意环境变量名取值。

### 5.2 示例

```json
{
  "manifest_version": 2,
  "id": "anysearch",
  "version": "0.1.0",
  "language": "lua",
  "runtime": {
    "kind": "embedded-lua",
    "entry": "anysearch.lua"
  },
  "permissions": {
    "network": [
      {
        "scheme": "https",
        "host": "api.anysearch.com",
        "port": 443,
        "methods": ["GET", "POST"]
      }
    ],
    "secrets": [
      {
        "id": "api_key",
        "env": "ANYSEARCH_API_KEY",
        "required": false
      }
    ]
  },
  "limits": {
    "http_timeout_ms": 20000,
    "http_request_bytes": 1048576,
    "http_response_bytes": 4194304
  },
  "tools": [
    {
      "name": "search",
      "entry": "search",
      "description": "Search the web or a declared AnySearch vertical domain.",
      "input_schema": {
        "type": "object",
        "properties": {
          "query": {"type": "string"},
          "tag": {"type": "string"},
          "params": {"type": "object"},
          "max_results": {"type": "integer", "minimum": 1, "maximum": 10}
        },
        "required": ["query"],
        "additionalProperties": false
      }
    }
  ]
}
```

### 5.3 字段规矩

`runtime.entry`：

- 必填，只收插件目录内普通 `.lua` 文件。
- canonical 后不得逃出插件根。
- symlink 第一版明拒，免得加载后才越界。
- Package 内路径仍受 Package inventory 与快照钉住。

`permissions.network[]`：

- 第一版只收 `https`。
- `host` 必须是精确 DNS 名；转小写、去末尾点、做 IDNA 规范化后记账。
- 不收 `*`、`*.example.com`、IP 字面量与用户信息段。
- `port` 第一版只收 443。
- `methods` 只收 `GET`、`POST`；大小写规范化后去重。
- URL path 与 query 由 Lua 决定，host/scheme/port 必须命中声明。

`permissions.secrets[]`：

- `id` 是 Lua 可见的逻辑名，不得等于真实值。
- `env` 是允许解析的环境变量名。Lua 不可另传名字。
- `required=false` 可退到匿名模式；`required=true` 缺失时工具执行前即报 `secret_missing`。
- manifest 不收 `value`、`default`、`${env:...}` 明文展开。

`limits`：

- 只许下调，不许突破宿主硬上限。
- 缺省：URL 8 KiB、请求头总计 32 KiB、请求体 1 MiB、响应头 64 KiB、响应体 4 MiB、墙钟 30 秒。
- 宿主硬上限：URL 16 KiB、请求头 64 KiB、请求体 8 MiB、响应头 128 KiB、响应体 16 MiB、墙钟 120 秒。
- 0 不表示无限；v2 里 0 直接判非法。

### 5.4 为什么不把 Secret 写进 headers

下面这种清单禁止：

```json
{"headers": {"Authorization": "Bearer ${env:ANYSEARCH_API_KEY}"}}
```

原因有三：

1. 展开后的 header 容易混进 inspect、日志与错误上下文。
2. Lua 可读可改 header 时，便能把 Secret 发往另一个已获准 host。
3. 字符串模板难管编码、重复头与重定向后的去留。

Secret 只以逻辑 id 进入宿主 HTTP 调用，最终头由宿主在发包前一刻添上。

## 六、Lua Host API

### 6.1 模块形状

manifest-backed Lua 脚本只返回 handler 表。工具定义只认 manifest，不在 Lua 里抄第二份 Schema。

```lua
return {
  search = function(input)
    local response, err = luban.http.request({
      method = "POST",
      url = "https://api.anysearch.com/v1/search",
      headers = {
        ["Content-Type"] = "application/json",
        ["X-Anysearch-Client"] = "lubancode-lua/0.1.0",
      },
      json = input,
      auth = {
        type = "bearer",
        secret = "api_key",
        optional = true,
      },
      timeout_ms = 20000,
    })

    if err ~= nil then
      error(err.code .. ": " .. err.message)
    end

    return {
      status = response.status,
      body = response.json,
    }
  end,
}
```

加载后须验：

- 返回值必须是 table。
- manifest 每枚 `tools[].entry` 都须在表里找到 function。
- 多一枚未声明 function 可留着，但不挂成工具。
- 缺 handler、重复 entry、返回非 function，整件插件拒绝挂载。

### 6.2 `luban.http.request`

首版入参：

| 字段 | 必填 | 规矩 |
|---|---|---|
| `method` | 是 | `GET` 或 `POST`，还要命中 manifest methods |
| `url` | 是 | 绝对 HTTPS URL，规范化后命中精确 host/port |
| `headers` | 否 | string -> string；禁 `Authorization`、`Proxy-Authorization`、`Cookie`、`Host`、`Content-Length` |
| `json` | 条件 | 与 `body` 二选一；宿主用 nlohmann/json 序列化 |
| `body` | 条件 | 与 `json` 二选一；原始 UTF-8 string |
| `auth` | 否 | `type=bearer|header`、`secret=<逻辑 id>`、`optional`；宿主代填 |
| `timeout_ms` | 否 | 只能下调 manifest 与宿主上限 |

`auth.type=header` 另收 `name` 与可选 `prefix`。header name 不许落进禁写表；第一期只许 `Authorization`、`X-Api-Key`、`Api-Key` 三类规范名，避免拿 Secret 拼任意业务字段。

成功返回：

```lua
{
  status = 200,
  headers = { ["content-type"] = "application/json" },
  body = "{...}",
  json = { ... },  -- Content-Type 为 JSON 且解析成功时才有
  url = "https://api.anysearch.com/v1/search",
  bytes = 1234,
}
```

响应头须过滤 `set-cookie`、`proxy-authenticate` 等敏感或无用字段。重复头按明确数组形状保留，不能悄悄拼错。

失败不抛 C++ 异常穿过 Lua 边界。返回第二值：

```lua
nil, {
  code = "response_too_large",
  message = "响应体超过 4194304 字节，已中止",
  status = 0,
  retryable = false,
}
```

Lua 作者可选择 `error(err.message)`，也可把业务错误整理后返回模型。

### 6.3 Secret API

Lua 只开放：

```lua
luban.secrets.available("api_key")  -- bool
luban.secrets.ref("api_key")        -- opaque userdata
```

SecretRef 规矩：

- `tostring(ref)` 只得 `<secret:api_key>`。
- 不支持拼接、索引、比较原文、转 JSON。
- 只可交还宿主认可的 sink；第一期唯一 sink 是 `luban.http.request.auth`。
- Secret id 未在 manifest 声明，报 `secret_not_declared`。
- 顶层 chunk 调用 `available/ref` 报 `no_active_tool_call`，也不触发解析。

为省一层 userdata，`auth.secret="api_key"` 可作为语法糖。内部仍先解析成 SecretRef，再走同一条注入链。

## 七、SecretResolver 与 `.env`

### 7.1 查找顺序

同一逻辑 Secret 按下列顺序取第一份非空值：

```text
宿主进程环境中 manifest 声明的 env 名
-> 插件专属 .env
-> 未找到
```

第一期不接 OS Keychain。接口要留 provider seam，第二期可加 Windows Credential Manager、macOS Keychain、Linux Secret Service，Lua 合同不变。

### 7.2 `.env` 放哪儿

禁止自动读取插件源码树里的 `.env`。源码与 Secret 分家：

```text
standalone plugin:
  ~/.lubancode/plugin-data/<plugin-id>/.env

packaged plugin:
  ~/.lubancode/package-data/<package-id>/plugins/<local-id>/.env
```

Windows 的 `~` 取 `%USERPROFILE%`，POSIX 取 `$HOME`。路径由宿主算，不交 Lua 猜。

理由：

- 源码树内 `.env` 会进内容指纹，改 Key 便撞掉信任。
- Package 更新可能覆盖包根文件。
- 打包、提交与分享时容易捎走 Secret。
- 运行数据目录已是 Package 的既有边界。

### 7.3 `.env` 子集语法

第一期自带一个窄 parser，只认：

```dotenv
# comment
ANYSEARCH_API_KEY=value
QUOTED="value with spaces"
```

规矩：

- UTF-8 或 UTF-8 BOM。
- 空行与 `#` 注释略过。
- key 只收 `[A-Za-z_][A-Za-z0-9_]*`。
- 单双引号可包整值；不做 shell 展开、命令替换、变量插值、多行与 `export`。
- 同名后写覆盖前写，记一条不带值的 warning。
- 文件过 1 MiB 直接拒读。
- 只解析 manifest 声明过的 env 名；别的行不进内存结果表。
- 文件权限异常、编码坏、语法坏，`doctor` 明报；工具调用按 required/optional 分流。

### 7.4 明文寿命与打码

新增 `SecretValue`：

- 禁复制，可移动。
- 析构时 best-effort 覆写 buffer。
- 不实现 `operator<<`。
- 错误、日志、trace 只记 secret id、来源种类与 available/missing。
- HTTP 层拿值拼 header 后，不把完整 header 交给公共日志。
- 若上游错误体原样回显 Secret，落日志与模型结果前用本次已解析 Secret 集合再扫一遍并替换 `[REDACTED]`。

best-effort zeroize 不是绝对保密证明。优化器、TLS 库与系统缓冲仍可能留副本。文档要如实写。

## 八、受控 HTTP 传输

### 8.1 不直接复用 `PostSseStream`

`PostSseStream` 带着模型 provider 的专门语义：POST、SSE sink、流空闲超时、非 2xx 错误分型。Lua 第一版需要普通 GET/POST、完整响应与严格收发帽。

应抽一层中立底座：

```text
src/net/http_transport.*
  -> URL/headers/body 中立请求
  -> cpr/libcurl callbacks
  -> cancel / connect timeout / hard timeout
  -> request/response byte accounting
  -> status / headers / body / network error

src/api/http_stream_transport.*
  -> 在中立底座上保留 provider SSE 语义

src/runtime/plugin_http.*
  -> 权限、Secret 注入、JSON、Lua 错误码
```

首批实现可先让 `plugin_http` 直接用同一套底层 helper，待回归钉稳再迁 provider，免得一次改四家模型 wire。

### 8.2 URL 与网络边界

发请求前依次查：

1. URL 能完整解析；禁 fragment 与 userinfo。
2. scheme/host/port/method 命中 manifest 精确声明。
3. host 不是 IP 字面量、`localhost` 或 `.local`。
4. DNS 解析出的每个候选地址都不落 loopback、link-local、RFC1918、CGNAT、组播、保留段与云 metadata 段。
5. 连接时钉住已验地址，防 DNS rebinding；若 cpr 暴露不足，降到 libcurl `CURLOPT_RESOLVE` 或单独 transport，不拿“解析前查过”冒充连接期保证。

第一期重定向一概不跟。3xx 原样作为 HTTP 响应交 Lua。日后放开时，每一跳都须重走上面五关；跨 origin 必须剥掉 Secret header。

### 8.3 字节帽

计数点要靠近水管：

| 帽 | 落锤处 | 超限行为 |
|---|---|---|
| URL | 解析前 | 不发请求 |
| 请求头 | Secret 注入后、cpr 前 | 不发请求 |
| 请求体 | JSON dump/body 接收后、cpr 前 | 不发请求 |
| 响应头 | HeaderCallback | 立即中止 |
| 响应体 | WriteCallback | 达帽即中止，不再多攒一块 |
| 墙钟 | ProgressCallback | 立即中止，分型 timeout |

不能先让 cpr 把完整响应收进 `response.text`，回来再看大小。那时内存已经吃下，帽成了摆设。

### 8.4 取消

调用链：

```text
ToolExecutionContext.cancel
-> LuaCallContext.cancel
-> luban.http.request
-> cpr/libcurl ProgressCallback + WriteCallback
-> abort transfer
-> error.code = cancelled
```

取消须覆盖：

- 调用前已置位。
- DNS/连接/TLS 阶段。
- 正在上传请求体。
- 等首字节。
- 正在读取响应体。

Lua instruction hook 继续管 Lua VM。HTTP 回调管阻塞 C 边界。两条取消链共用同一枚 flag，不另造第二个终止真值。

### 8.5 同步与并发

第一版 `luban.http.request` 是同步函数。一个 Lua tool call 同时只跑一笔 HTTP。同一 lua_State 仍由现有 mutex 串行；不同插件、主会话与子代理可并发。

宿主另设全局与每插件在途上限：

- 每插件最多 4 笔。
- 全局最多 32 笔。
- 超限即回 `concurrency_limit`，不排无限队。

这些值进配置常量与诊断，不让插件自行抬高。

## 九、加载期零副作用

Lua chunk 在启动时会执行顶层代码。故而仅有 trust 仍不够，Host API 还要看动态调用上下文。

实现规矩：

1. 建 state、开 Pure 库、注册 `luban` 模块。
2. 加载并执行 chunk；此时 `LuaCallContext` 为空。
3. 顶层若调 HTTP、Secret 或未来文件 API，返回 `no_active_tool_call`，零网络、零 Secret 解析。
4. 验 handler 表与 manifest entry。
5. 真正工具调用前，用 RAII 把 `LuaCallContext*` 写进 registry。
6. `lua_pcall` 返回后立刻清空；异常与取消路径也须清空。

验收须有假 resolver 计数器：恶意顶层脚本尝试联网与取 Secret，计数仍为零。

## 十、信任、Package 与诊断

### 10.1 信任材料

`/plugin trust`、`/package trust` 须亮出：

- plugin/package id 与版本。
- Lua entry 相对路径。
- 工具名。
- 精确 scheme/host/port/method。
- Secret 逻辑 id 与 env 名，只亮名字。
- 资源帽。
- 内容指纹。

`.env` 在数据目录外，不参与代码指纹。它改变凭据，不改变插件代码。Secret 声明或 host 权限一变，manifest 哈希会变，必须重批。

### 10.2 Package 原子挂载

扩 `src/package/code_mounting.*`：

```text
parse manifest v2
-> validate Lua entry + permissions + limits
-> trust gate
-> 暂存 Lua state
-> 顶层零副作用加载
-> handlers 与 tools 对账
-> 暂存 registry
-> 同包其它 Plugin/MCP 全成
-> 原子发布
```

任一步失败，关掉暂存 state，整包不挂。不能让同包一半工具留下。

会话钉住：

- manifest、Lua 字节、权限与 limits 进入 PackageSnapshot。
- 运行中改源码不换在跑 state。
- `.env` 是否也钉值：第一版每次工具调用重新解析来源，但只解析声明键；这样轮换 Key 无需重启。若要绝对会话复现，轨迹只记 credential fingerprint 与来源，不记值。
- 同一调用开始后拿到的 SecretValue 用到调用结束，不在半包途中换 Key。

### 10.3 `/plugin inspect`

展示：

```text
runtime: embedded-lua
entry: anysearch.lua
profile: pure + host-http
network: POST https://api.anysearch.com:443, GET ...
secrets: api_key <- ANYSEARCH_API_KEY (optional, available via user dotenv)
limits: request 1 MiB, response 4 MiB, timeout 20 s
```

只写 available/missing 与来源类别，不写路径以外的敏感内容，不写值长、前缀与 fingerprint。

### 10.4 `/plugin doctor`

默认只读，不带 Secret 发网：

- manifest/schema/path。
- Lua 编译与 handler 对账。
- Pure 画像是否生效。
- network 目的地规范化与 DNS 安全检查。
- Secret 声明、环境/.env 是否找到，仅报名字与来源。
- limits 是否越宿主硬帽。
- 顶层零副作用探针。

真网自测由插件显式工具或单独 `self_test` 完成，须经确认。`doctor` 不拿用户 Key 偷打一枪。

## 十一、错误合同

宿主错误码稳定，Lua 与测试都按码判断：

| code | 含义 | retryable |
|---|---|---|
| `no_active_tool_call` | 顶层加载期或调用外使用 Host API | false |
| `network_not_declared` | manifest 未声明网络 | false |
| `network_target_denied` | scheme/host/port/method 不匹配 | false |
| `network_address_denied` | DNS 落私网/保留地址或连接地址不可信 | false |
| `secret_not_declared` | Lua 引用了未声明 Secret | false |
| `secret_missing` | required Secret 没找到 | false |
| `invalid_request` | URL/header/body/auth 形状错 | false |
| `request_too_large` | URL/header/body 超帽 | false |
| `response_too_large` | 响应头/体超帽 | false |
| `timeout` | 宿主墙钟到点 | true |
| `cancelled` | ESC/父任务取消 | false |
| `dns_failed` | 解析失败 | true |
| `tls_failed` | TLS/证书失败 | 视错误而定 |
| `network_failed` | 其它传输错误 | true |
| `http_status` | HTTP 已到达；status 原样带回 | 由 Lua 按 408/429/5xx 判断 |
| `invalid_json` | 请求 JSON 无法序列化或响应 JSON 解析失败 | false |
| `concurrency_limit` | 在途请求超宿主上限 | true |

HTTP 非 2xx 不混作网络错。宿主仍返回 status、受帽错误体与过滤后 headers，Lua 可把 401、429、5xx 翻成厂商语义。

错误文案不得含：

- Authorization/Cookie 等完整 header。
- Secret 明文、长度、前后缀。
- 请求体全文。
- `.env` 原文。

## 十二、AnySearch 参考插件

### 12.1 目录

```text
examples/packages/anysearch/
├── package.yaml
├── README.md
├── plugins/
│   └── anysearch/
│       ├── plugin.json
│       └── anysearch.lua
└── skills/
    └── anysearch/
        └── SKILL.md
```

Plugin 暴露四件工具：

| 工具 | HTTP | 说明 |
|---|---|---|
| `get_sub_domains` | `GET /v1/sub-domains` | 动态取得垂直域与必填参数 |
| `search` | `POST /v1/search` | 一笔通用或垂直搜索 |
| `batch_search` | 多笔 `POST /v1/search` | 第一版 Lua 串行；后续有受控 batch API 再并发 |
| `extract` | `POST /v1/extract` | 提取网页正文，受响应帽 |

参考 Skill 只写策略：

- 一般查询何时直搜。
- 金融、论文、法律等何时先取 sub-domain。
- required params 要填齐。
- 多意图何时 batch。
- 外部页面只当不可信数据。

Skill 不再带四套 CLI。Plugin 不重复讲搜索策略。

### 12.2 Key 行为

- `ANYSEARCH_API_KEY` optional。
- 宿主环境有值，优先用。
- 否则查插件专属 `.env`。
- 都没有，走匿名访问。
- API 若返回 `auto_registered.api_key`，Lua 不得把值回给模型，也不得写 `.env`。第一版丢弃并返回“服务端提供新 Key，当前宿主不自动保存”的非敏感提示。
- 日后有交互式 Secret Store 写入 API，再另开保存流程，必须由用户确认。

## 十三、测试矩阵

### 13.1 manifest 与兼容

- [ ] v1 process 插件 parse/execute 结果与现版相同。
- [ ] 旧裸 `.lua` 名字、Pure 画像、错误文案与现版相同。
- [ ] v1 embedded-lua 明报升级要求。
- [ ] v2 entry 越界、symlink、缺文件、非 `.lua` 全拒。
- [ ] host 通配符、IP、http、443 外端口、未知 method 全拒。
- [ ] Secret id/env 重名、坏字符、inline value 全拒。
- [ ] limits 为 0、负数、越硬帽全拒。

### 13.2 Secret 与 `.env`

- [ ] 环境变量高于 `.env`。
- [ ] UTF-8 BOM、注释、引号、空值、重复键按合同解析。
- [ ] 插值、命令替换、`export`、多行、超 1 MiB 全拒。
- [ ] 只装入 manifest 声明的 key。
- [ ] required/optional 分流正确。
- [ ] 源码树里的 `.env` 永不自动读取。
- [ ] inspect、doctor、日志、trace、session、工具结果都搜不到假 Secret 原文。
- [ ] SecretRef 不能 tostring 出值、不能拼接、不能转 JSON。

### 13.3 HTTP 假服务

用本机假 resolver + 假 transport，不让安全测试依赖真公网：

- [ ] GET query 与 POST JSON 字节正确。
- [ ] Bearer/header Secret 只在最终发包头出现。
- [ ] Lua 自写 Authorization/Cookie/Host/Content-Length 被拒。
- [ ] URL、请求头、请求体分别在帽前/帽后一字节测试。
- [ ] 响应头、响应体分别在帽前/帽后一字节测试。
- [ ] 非 2xx 保留 status 与受帽错误体。
- [ ] 坏 JSON 按 `invalid_json` 分型。
- [ ] 3xx 不自动跟随。
- [ ] 私网、loopback、metadata、DNS rebinding 夹具全拦。
- [ ] 取消覆盖连接前、等首字节、读到半截三档。
- [ ] timeout 与 cancelled 不串码。
- [ ] 每插件/全局并发帽不排死队。

### 13.4 Lua 生命周期

- [ ] 顶层 chunk 调 HTTP，假 transport 调用数仍为 0。
- [ ] 顶层 chunk 取 Secret，假 resolver 调用数仍为 0。
- [ ] handler 缺失/非函数，整件拒挂。
- [ ] Lua 普通 error、内存帽、指令帽、HTTP 错误各有唯一终态。
- [ ] 调用结束后 `LuaCallContext` 清空；第二次调用不见上次 Secret/取消旗。
- [ ] 同 state 串行、不同插件可并行。

### 13.5 Package 与真机

- [ ] 未信任项目 Package：Lua chunk 一字不跑。
- [ ] 批准后整包原子挂载；同包另一件坏，Lua state 一并回滚。
- [ ] manifest 权限变更后旧信任失效。
- [ ] `.env` 轮换不改代码指纹，下一次调用读到新值。
- [ ] `/plugin inspect/doctor` 与 `/package show/doctor` 展示同一份权限真账。
- [ ] Windows/Linux/macOS 构建与受控 HTTP 单测过。
- [ ] AnySearch 匿名真实搜索一笔，结果含官方目标；网络不可用时明确 SKIP。
- [ ] AnySearch keyed 真测只在显式环境开关下跑，报告不得打印 Key。
- [ ] focused tests 全绿后再跑完整 Release CTest，分别报数。

## 十四、实施分期

### 阶段 0：冻结合同与可替换 seam

- [ ] 定 `PluginManifestV2`、NetworkPermission、SecretDeclaration、HttpLimits。
- [ ] 定 `SecretResolver`、`BoundedHttpTransport` 接口与 fake 实现。
- [ ] 定 Lua Host API 表形、错误码与顶层零副作用规矩。
- [ ] 先写 manifest/URL/limit/SecretRef 单测，不接真网。

验收：Reviewer 只看头文件与测试，已能说清谁持 Secret、谁能联网、何时取消、哪里落帽。

### 阶段 1：SecretResolver 与插件数据 `.env`

- [ ] 实现 env provider。
- [ ] 实现窄 dotenv parser 与 standalone/package 两类数据路径。
- [ ] 实现 move-only `SecretValue` 与 redaction helper。
- [ ] 接 `inspect/doctor` 的 available/missing/source 展示。
- [ ] 跑泄露扫描回归。

验收：假 Key 能被指定插件解析，Lua 尚拿不到；全仓输出搜不到原文。

### 阶段 2：受控非流式 HTTP

- [ ] 抽/建中立 HTTP transport。
- [ ] 实现 URL 规范化、精确 host/method 权限与安全 DNS seam。
- [ ] 实现 Secret header 注入与禁写 header 表。
- [ ] 实现请求/响应/墙钟/并发帽。
- [ ] 实现连接期、等首字节、收体期取消。
- [ ] 用假 transport 与本机 socket 钉错误分型。

验收：不碰 Lua，也能用纯 C++ 测试证明越权不发包、超帽立刻断、ESC 与 timeout 分账。

### 阶段 3：Lua Host API 与动态调用上下文

- [ ] 给 Lua state 注册 `luban.http`、`luban.secrets`。
- [ ] 实现 `LuaCallContext` RAII 与 registry 绑定。
- [ ] 接 ToolExecutionContext.cancel。
- [ ] 实现 Lua table <-> HttpRequest/HttpResponse/Error。
- [ ] 实现 SecretRef userdata 与不可泄露元方法。
- [ ] 顶层零副作用测试先红后绿。

验收：一份内存 Lua 脚本能调假 HTTPS、拿结构化 JSON；取消当场收口，Secret 原文从未进入 Lua。

### 阶段 4：manifest-backed Lua 与 Package 挂载

- [ ] parser 同时收 v1/v2，旧件回归不动。
- [ ] `runtime.entry` 安全解析与 handler 对账。
- [ ] 新建 ManifestLuaRuntime owner 与 adapter，不把它硬塞进 process 分支。
- [ ] standalone/project Plugin 扫描、信任与挂载接通。
- [ ] Package staged mount/rollback/ToolOrigin/快照接通。
- [ ] `/plugins`、`/plugin inspect/doctor`、`/package` 命令补展示。

验收：项目 Lua Plugin 未信任零执行；信任后挂载；同包坏件令整包回滚；旧 Lua/process/native 行为不变。

### 阶段 5：AnySearch 参考 Package

- [ ] 写四件工具的 manifest Schema。
- [ ] 写 `anysearch.lua`，只用 Host API。
- [ ] 写短 Skill，策略与执行分账。
- [ ] 写假 AnySearch server 回归：auth、401、429、5xx、坏 JSON、超帽、取消。
- [ ] 写 `.env.example` 到文档/数据初始化模板，不把真 `.env` 放包根。
- [ ] 跑一笔匿名真网；Key 真测显式 opt-in。

验收：新机器只装 LubanCode 与 Package，不装 Python/Node/PowerShell 7，也能匿名搜索；配置 Secret 后只多一枚 Authorization 头。

### 阶段 6：文档、兼容与全量验证

- [ ] 更新 `docs/features/extensions/README.md`。
- [ ] 校正 `docs/architecture/extensions/plugin-runtime.md` 的旧 Lua 风险描述。
- [ ] 文档讲清裸 Lua、manifest Lua、process、MCP 的选择边界。
- [ ] 补安全文档：Secret 来源、`.env` 路径、DNS/redirect、日志打码。
- [ ] 跑格式检查、focused suites、完整 Release CTest。
- [ ] 真机核对 Windows/Linux/macOS；未跑的平台明报未验证。

验收：文档命令能照抄；测试报告分清单测、假服务、真网与未测平台，不把设计证据冒充产品实测。

## 十五、建议改动文件

新增：

```text
src/net/http_transport.hpp
src/net/http_transport.cpp
src/runtime/plugin_http.hpp
src/runtime/plugin_http.cpp
src/runtime/secret_resolver.hpp
src/runtime/secret_resolver.cpp
src/runtime/plugin_lua_manifest.hpp
src/runtime/plugin_lua_manifest.cpp
tests/unit/runtime/test_secret_resolver.cpp
tests/unit/runtime/test_plugin_http.cpp
tests/integration/plugins/test_plugin_lua_http.cpp
examples/packages/anysearch/...
```

修改：

```text
src/tools/lua_tool.hpp
src/tools/lua_tool.cpp
src/runtime/plugin_contract.hpp
src/runtime/plugin_contract.cpp
src/runtime/plugin_lua.hpp
src/runtime/plugin_lua.cpp
src/runtime/plugin_tool.hpp
src/runtime/plugin_tool.cpp
src/app/tool_runtime.hpp
src/app/tool_runtime.cpp
src/package/component.*
src/package/code_mounting.*
src/package/trust.*
src/app/commands/workspace_commands.cpp
docs/features/extensions/README.md
docs/architecture/extensions/plugin-runtime.md
CMakeLists.txt
tests/CMakeLists.txt
```

文件名不是铁律。边界才是铁律：通用 HTTP 不住 provider client；SecretResolver 不住 AnySearch；manifest Lua 不借 process runner 冒名。

## 十六、明确不做

- 不给裸 Lua 悄悄打开 Trusted 画像。
- 不开放 `luban.env.get("任意名字")`。
- 不把 Secret 明文送进 Lua、工具入参、模型上下文、session、trace 或 Hook payload。
- 不自动读取插件/Skill 源码目录的 `.env`。
- 不支持 HTTP 明文、通配 host、任意端口、私网与 metadata endpoint。
- 不在第一期跟随重定向。
- 不做流式 body、WebSocket、文件下载、multipart 与 cookie jar。
- 不让插件抬高宿主硬帽。
- 不把 Plugin HTTP 做成另一套 provider client。
- 不因 AnySearch 需要便把厂商路径、字段或错误码写进 C++ 内核。

## 十七、完成定义

本单只有同时满足以下条件，才算落地：

- [ ] 旧裸 Lua、v1 process、native Plugin 回归不变。
- [ ] manifest-backed Lua 只有在受信任且声明权限后才能联网。
- [ ] 启动加载期零 HTTP、零 Secret 解析。
- [ ] Secret 原文从未进入 Lua、模型、日志、trace、session 与 Hook。
- [ ] host/scheme/port/method/DNS/redirect 五道网络边界都有拒绝测试。
- [ ] 请求、响应、墙钟、并发四类上限在数据入口处落锤。
- [ ] ESC 在连接、等首字节、收体三段都能中止，且不冒充 timeout。
- [ ] `.env` 住数据目录，改 Key 不改代码指纹。
- [ ] 项目/Package 信任与原子挂载有端到端测试。
- [ ] AnySearch 参考插件在零外部运行时机器上能匿名搜索。
- [ ] Key 真测 opt-in，任何报告不露 Key。
- [ ] focused tests 与完整 Release CTest 分别报数；未跑项明说。

## 十八、当前实现状态

截至 2026-08-30：

- 阶段 0-3 已落地：合同与 SecretResolver、受控 HTTP 五道边界与字节帽、Lua Host API（`luban.http`/`luban.secrets`、LuaCallContext、SecretRef）。
- 阶段 4 已落地：ManifestLuaRuntime owner（v2 embedded-lua 挂载）、standalone/project 扫描信任挂载、Package 事务第四类 code 组件（整包原子挂载与回滚）、`/plugin trust`、`/plugin inspect/doctor`、`/package` 的 v2 材料与展示。
- AnySearch Lua 参考 Package（阶段 5）还未创建，也未做真网验证。

开工顺序不可倒：先权限与失败合同，再接真 HTTP；先证明 Secret 不泄露，再写 AnySearch。
