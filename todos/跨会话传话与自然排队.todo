# 跨会话传话与自然排队

状态：待做

## 这几样东西别混了

Claude Code 如今有四层代理形态：

| 名目 | 活在哪里 | 谁开它 | 怎么传话 |
| --- | --- | --- | --- |
| Explore / general-purpose | 当前会话里的 subagent | 主代理 | 做完回报主代理；新版也可用 `SendMessage` |
| Agent Teams | 一队受领队管束的 Claude Code 实例 | 领队 | 队友互传，共用任务表 |
| Agent view / background sessions | 一排独立后台会话 | 用户 | 用户从总览里查看、回复、attach |
| Cross-session messaging | 两场本来互不相干的活会话 | 用户分别启动 | `ListAgents` 找人，`SendMessage` 送文字 |

所以，界面里列着 `main / general-purpose / Explore`，仍是当前会话树里的 agent。`Explore` 确是内置 subagent，专管只读搜索。它不是另一个用户会话。

真正“不同 session 互相说话”，是 Claude Code 2.1.224 新添的 cross-session messaging。

官方资料：

- <https://code.claude.com/docs/en/cross-session-messaging>
- <https://code.claude.com/docs/en/sub-agents>
- <https://code.claude.com/docs/en/agent-teams>
- <https://code.claude.com/docs/en/agent-view>

## Claude Code 怎么办

官文给出的规矩如下：

- 2.1.224 起可用。macOS、Linux、WSL2 支持；原生 Windows 暂不支持。
- 同机传话走每场会话自己的 Unix socket，不经过 Anthropic 服务器。
- 跨机器与 web 会话要借 Remote Control，才会走 Anthropic 服务器。
- `ListAgents` 列出够得着的会话，`SendMessage` 按名字送信。
- 信里只有一段纯文本。没有聊天记录，没有文件，没有权限。
- 对方正忙，消息便夹在两次工具调用之间递进去，不硬掐正在跑的工具。
- 对方正闲，消息会另起一轮。
- 收件方可 `accept / hold / refuse`。权限模式不相称时，默认先扣住，等用户点头。
- 来信不能代用户批准工具，不能改配置，不能把 `/compact` 之类当命令执行。
- 重复消息会去重，来回死循环会限速，待读队列也有上限。

这套东西窄，却稳。它不搬上下文，只递一张字条。

## Lubancode 要做成什么

先做同机跨会话。Windows 是 Lubancode 的主场，不能照抄 Unix socket 后便收摊。

目标只有一句：一场会话得了消息、结论或状态，可以递给同一用户开的另一场会话；收件方挑安全时机读，不打断手头工具。

首版不做跨机器。先把同机寻址、权限、排队、重启清理做牢。

## 会话名册

每场交互会话启动后登记一张小名片：

```text
peer_id
session_id
name
cwd
pid
started_at
status        idle | busy | waiting | closing
endpoint
protocol_version
last_seen
```

名字可由 `/title` 派生，也可另添 `/rename`。重名时仍用短 `peer_id` 定人，界面把 cwd 带出来。

名册只准同一系统用户读写：

- Windows：Named Pipe，给当前用户 SID 配 ACL。
- POSIX：Unix domain socket，文件权限 `0600`。
- 注册文件用原子替换。心跳过期、PID 已死，便清掉旧名片。
- 容器、不同用户、不同 home 默认互相看不见。

## 工具与命令

给模型两件窄工具：

```text
list_sessions
send_session_message
```

不要复用 subagent 的 `agent` 工具，也别把跨会话和父子代理绑在一张表里。

给用户这些入口：

```text
/peers
/send <名字或短 id> <话>
```

`/peers` 用现成方向键菜单。每行只摆名字、状态、cwd。Enter 看详情，Esc 收起。以后再加 `@名字` 补全，不拿它卡首版。

模型也可自行递话，不过系统提示要勒住：只有手头结论会影响另一场活时才送；不许闲聊，不许催问成环。

## 信封

线上的信封用版本化 JSON。正文仍只认纯文本：

```json
{
  "version": 1,
  "message_id": "...",
  "sender_id": "...",
  "sender_name": "backend",
  "target_id": "...",
  "sent_at": "...",
  "reply_to": null,
  "text": "接口字段已经改成 tenant_id，前端可以跟进。"
}
```

传输层要回一份结果：`delivered / held / refused / expired / unavailable`。发送方看得见送没送到，别只打一声“成功”便不管。

## 收件时机

这处最要紧。

- 工具正在跑：不打断。等工具回结果、下一次请求尚未发出时再收信。
- 模型正在吐正文：先排住。当前轮收口后再起一轮。
- 会话空闲：直接把信作为一轮“外来消息”交给模型。
- 卡在权限确认：来信不能作答，也不能替用户确认。
- 会话退出：首版回 `unavailable`，不做离线邮箱。

`AgentLoop` 要添一个安全收件点。别让 pipe 线程直接改 history，也别让它碰终端输出。传输线程只入线程安全队列；主线程在轮次边界取走。

来信要带来源。API history 里若暂时没有 origin 字段，便在内部消息类型上补，最终组包时再转成明确的用户块。不能装成用户亲手敲的字。

## 权限

首版可设三档：

```text
accept   同一用户的来信直接送达
hold     弹出一行确认，用户点头才送
refuse   丢掉并回绝
```

默认值跟本场权限模式走：

- 两边都要确认工具，可直接收。
- 任一边处在 yolo / bypass 一类模式，默认 hold。
- 项目不同、cwd 相隔甚远，也默认 hold。

不论哪档，都守这几条：

- 来信不能批准工具。
- 来信不能改权限、配置、AGENTS.md、skill。
- 来信里的 slash command 只算文字。
- 同一 `message_id` 只收一次。
- 同一对会话限速；相同正文短时去重；队列设硬上限。

## 排队输入要改得自然

眼下这行：

```text
> ⦸ 打断 · 排队中：什么叫排队中啊
```

把操作说明、状态、用户正文拧成一股，字一多便挤，光标也像钉在提示语后头。Codex、Claude Code 都把输入当输入，队列另摆。这才顺眼。

改成：

```text
  › 什么叫排队中啊
>
```

规矩如下：

- 输入行永远只画 `> ` 和用户正在键入的字。
- 空输入时可淡淡写占位；一旦打字，占位整行退去。
- `Esc 打断` 放进状态行，不许挤进正文。
- Enter 落队后，消息挪到输入框上方。不要再写“排队中：”。
- 只有一条时不写“待发送消息 1 条”。三条以内逐条摆。
- 超过可见上限，只添一行 `另有 N 条`。
- 输入为空时按上键，取回最后一条待发消息。改好按 Enter，原位替换。
- 再按上、下，可在待发消息间走。Delete 删当前项，Esc 放回队列。
- 当前轮结束，照次序发送。用户打断当前轮，不清队列。

现有 `StreamQueueEchoText()` 不该再拼 `stream.queueing` 前缀。它只回用户正文，或干脆删掉，直接交 composer 画。

测试也别再靠“排队下一条”这句中文找输入行。文案一改便全倒。给 screen driver 添一个找 footer 边框、状态行或光标位置的 helper，按结构认框。

## 分三步做

### 第一步：先收拾排队界面

- 改 `src/cli/format_utils.cpp`、`src/cli/console_input.cpp`、`src/cli/i18n.cpp`。
- 给 `TurnInputListener` 添待发消息浏览、编辑、删除。
- 保留 `src/main.cpp` 现有 `pending_queue` 顺序发送语义。
- 改纯逻辑测试与 Win32 screen drivers。

这一步不碰跨会话协议，改动窄，用户马上看得见。

### 第二步：同机传话

- `src/agent/peer_registry.*`：名册、心跳、清陈条。
- `src/agent/peer_mailbox.*`：信封、去重、限速、队列。
- `src/platform/peer_transport_win.*`：Named Pipe + ACL。
- `src/platform/peer_transport_posix.*`：Unix socket + `0600`。
- `src/tools/list_sessions_tool.*`、`send_session_message_tool.*`：模型工具。
- `src/cli/slash_commands.*`、`i18n.cpp`：`/peers`、`/send`。
- `src/agent/loop.*`：工具轮次之间的安全收件点。

### 第三步：会话总览

待传话跑稳，再做类似 Claude agent view 的总览：

- 一屏看 idle、busy、waiting、done。
- 方向键选中，Enter 查看最近输出。
- 可从总览发一条话，不必 attach。
- 以后若添后台独立会话，再从这里启动和接回。

subagent 面板仍管当前会话树。总览管独立 session。两处别合成一锅。

## 验收

### 排队界面

- 流式时键入“什么叫排队中啊”，屏上只见 `> 什么叫排队中啊`，不见“排队中：”。
- Enter 后，正文挪到上方待发区，输入行清空，框高与光标不乱跳。
- 上键取回、改写、删除都能走；中文、emoji、软换行不截坏。
- 当前轮成功、报错、Esc 打断，队列都按原规矩续发。
- plain 模式不夹 ANSI，也不靠特殊字形才能看懂。

### 跨会话

- 同一用户开两场 Lubancode，`/peers` 能互相看见，重名也能分清。
- A 正闲时收 B 的信，另起一轮；A 正跑工具时，工具不受打断，轮次边界再收。
- 来信里的 `/clear`、`/yolo` 只作文字，不执行。
- 一边处在免确认模式，另一边默认 hold；用户拒绝后，发送方收到 refused。
- 重发同一 `message_id` 不会起两轮；回环消息撞上限后停住。
- 崩掉一场会话，旧名片会清；Named Pipe / Unix socket 不留给别的系统用户。
- 原生 Windows 与 Linux 各跑一套传输集成测试。

## 首版不做

- 不跨机器，不借公共中继。
- 不传文件、图片、聊天记录。
- 不拿来接管另一场会话。
- 不做离线邮箱。
- 不让跨会话消息和 subagent 共享生命周期。
