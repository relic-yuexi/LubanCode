# 失败与恢复：补齐写账闸门、工具结果与重试收口

- 状态：待实现；测试已另行安排，本单列覆盖要求，供实现与测试双方对账。
- 日期：2026-09-11。
- 级别：P1（FA-01～FA-03）；P2（FA-04～FA-05）。
- 来源：[失败之后怎么办](../interview/failure-and-recovery.md)，重点见第 5、6、8、9 节。
- 范围：AgentLoop、TrajectoryTurnBridge、V3 writer/reader、工具结果提交、主路显示与转录。
- 关联：`session轨迹v3_消息主轴树链与四角色壳收敛设计.todo`、`AppServer接入SessionV3_统一入口与事件恢复设计.todo`。
- 本轮交付：只立待办与测试清单；不改生产代码，不补测试实现，不据此勾销缺陷。

## 一、先钉住边界

模型请求重发，不能变成工具重做。工具已经改过外部状态，结果没存住，也不能倒写成“工具没执行”。

正式输入要有可恢复的提交链。写账失败须传回主循环；不能只记诊断，转头拿内存里独有的结果再发请求。恢复时拆开看：执行是否结束、结果是否保存、消息是否接纳。

每次模型尝试各自收口。失败片段可以留作审计，不能混进成功正文。账本能解析、hash 连续，也不等于恢复语义无损。

## 二、证据底稿

本单沿用来源文档所记审计，未另跑探针。数字限定在那次假后端、真实 V3 桥及临时文件系统故障环境；不冒充真实断网、断电或磁盘写满结果。

| 编号 | 原审计观察 | 修复方向 |
| --- | --- | --- |
| FA-01 · P1 | 结果仓打不开：工具执行 1 次，模型调用 2 次；第二次请求有真实结果，盘上无 tool 消息；resume 未列缺口 | 结果提交回执挡住后续发送，恢复列出结果缺口 |
| FA-02 · P1 | 当场请求含 1 份错误结果，回放后错误结果数为 0 | 保存并还原最终回喂结果的 `is_error` |
| FA-03 · P1 | prepared 成功、sent 写失败，writer 已坏，backend 仍调用 1 次 | 发送前提交失败，backend 调用归零 |
| FA-04 · P2 | 两次尝试有 2 条 started、1 条 completed、0 条 failed | 每次尝试有且仅有一个终态 |
| FA-05 · P2 | history 仅含 7 字节 SUCCESS，显示却把 5007 字节放进同一 item | 标明失败片段作废，成功回复另起 item |

证据入口：

- `tests/integration/runtime/test_failure_boundary_audit.cpp`：原探针 6 项测试、43 条断言，含 7 组观察。
- `interview/audits/2026-09-11-failure-boundaries/observations.json`：观察值、源码散列和账本索引。
- 同目录 `probe-output.txt`、`related-tests.txt`：原探针输出与相关测试结果。

原探针通过，只说明复现成立。修复后须换成目标断言，不能保留“错误行为如期发生”便宣布收官。

## 三、分批待办与测试覆盖

### P1-A：FA-01，工具结果提交与恢复

- [x] 让 `V3ToolResultsCommitted` 及上层提交接口返回明确回执，主循环消费回执；失败后停止后续模型发送，或先完成约定的持久化降级路径。
- [x] 将内存 history 推进与持久接纳对齐；结果未提交，不得当作下一次请求的已提交输入。
- [x] 恢复投影分别保留执行终态、结果保存状态和消息接纳状态；执行 done 但结果缺失，也要列出恢复工作。
- [x] 定清结果仓降级合同：仓打不开、metadata 保存失败、主账正文已保住三者分别处理；既查模型上下文是否完整，也查 selected/溯源链是否完整。
- [x] 已有结果只补保存或接纳；缺结果则查现场或交用户处理。不得默认重新执行工具，也不得将保存失败改写成执行失败。

测试覆盖：

- [x] 仓打不开：工具副作用计数为 1；未完成持久提交时 backend 不得进入第二次调用；重启投影须列结果缺口，并保留执行终态。
- [x] 仓已打开、第二份 metadata 保存失败：保留两份实际结果及各自身份；按选定合同断言停止或降级成功，另查 `persist_failed` 与 selected 链，不能只数正文。
- [x] 结果已保存且 selected、尚无 tool 消息：恢复为 `selected_no_message`；补交后身份、正文不变，工具执行次数不增加。
- [x] tool 消息已写、接纳尚未写：不得冒充有效上下文；按提交链补交或显式阻断。
- [x] 保存修复后再次恢复：不得重复执行、重复接纳或重复添加 tool 消息；原 action/call 配对不变。

验收：当前请求与恢复后请求的工具输入一致；不一致就明确停下、列出缺口，不静默放行。

### P1-B：FA-02，错误结果往返不变

- [x] 在 V3 最终 tool 消息中保存回喂语义，并在 `ProjectHistoryFromReplay` 中还原。
- [x] 以 Hook 处理后真正交给模型的结果为准；不从原始执行终态猜 `is_error`。

测试覆盖：

- [x] 成功与失败结果分别写盘、回放：`is_error`、正文、call ID 与配对均不变。
- [x] Hook 将成功改为错误、将错误改为成功、改写正文：当场输入与恢复输入均取最终结果。
- [x] 同一 assistant 含多个工具调用，成功失败混排：回放不串号、不换序、不丢错误标记。
- [x] 对会编码错误标记的 provider adapter，比对恢复前后的对应 wire 字段；JSONL 校验成功不能替代这条断言。

验收：同一份最终结果写盘再读回，模型收到的成功/失败语义不变。

### P1-C：FA-03，发送前写账硬闸

- [x] 让发送前最后一笔提交返回成功或失败，并贯通到 `run_one_attempt`；失败不得进入 `backend_.send_stream`。
- [x] 分清准备发送、本地交给 transport、服务端确认；事件名称与字段不得暗示已拿到远端收据。
- [x] 本地存储失败退出恢复环，不按 Network 错误继续重试。

测试覆盖：

- [x] prepared 写失败：backend 调用 0 次。
- [x] prepared 成功、sent 写失败：backend 调用 0 次，无工具执行，无下一次重试发送。
- [x] 首次网络失败后，下一次尝试的发送前记录失败：调用总数停在此前已发送次数。
- [x] 发送前记录成功、transport 随即失败：保留本地尝试记录，按网络策略收口；不得推出服务端已处理或未计费。

验收：本地已知账写不动，就不再发本次模型请求。

### P2-A：FA-04，每次尝试各自收口

- [ ] 在每次失败尝试结束时提交该尝试终态，再开始下一次；若终态写失败，停止重试并显式报存储故障。
- [ ] 记录逻辑请求与 attempt 关联，保留各次独立身份。
- [ ] 防止恢复环内与最终退出分支重复写同一终态；未起流的尝试按请求层收口，不凭空造 response。

测试覆盖：

- [ ] 首字节前失败、半截流失败后成功：失败尝试收口，成功尝试收口，正式 assistant 仅有成功回复。
- [ ] 多次失败后取消、退避中取消：无取消后的发送；已起流响应各有一个终态。
- [ ] 用尽最多 6 次尝试：次数守界，终态不漏、不重，最后一次不重复记 failed。
- [ ] 尝试终态提交失败：不进入下一次发送；回放显式暴露未收口状态，不补造成功。

验收：按 request/attempt 身份逐项核对生命周期，不能只比较全局事件总数。

### P2-B：FA-05，显示与转录遵守 attempt 边界

- [ ] 主路 wiring 接入 attempt 边界或撤回事件；失败文本标作失效，成功文本另起 item。
- [ ] 同步修改事件适配器、终端正文重绘与最终 transcript；核对子代理及其他事件消费者。

测试覆盖：

- [ ] 5000 字节失败片段后返回 SUCCESS：有效 history 只有 SUCCESS；显示 item 分开，失败片段明确作废；最终 transcript 不将两者当成一份成功答案。
- [ ] 连续重试、未输出即失败、半截工具 JSON、UTF-8 字符中途断流：边界正确，无残留片段混入成功项，无失败调用执行。
- [ ] 重试中取消与成功后结束：显示终态、有效 history、transcript 分别符合取消或成功合同。
- [ ] 主终端做实际显示验收；其他受影响界面逐一记结果。事件层测试不能代替界面验收。

验收：用户不会把作废片段与成功正文读成同一份回复。

## 四、跨层故障窗口：单列验证，不预先定罪

这些窗口在来源文档中尚未完成专项注入，不能算作已复现缺陷。

- [ ] 逐行切断 completed / assistant message / admitted：分别核对磁盘前缀、内存接纳与重启投影；接纳未成，不执行新工具。
- [ ] 工具 started 后、终态写入前强杀进程：恢复为 unknown，明确要求核验现场；不自动重做。
- [ ] artifact 尚未完成约定刷盘、持久引用即将写入时注入失败：不得接纳悬空引用；启动校验能指出缺失对象。
- [ ] 注入半行写入、整行写完但 flush 失败：writer 标坏，不继续提交；读侧区分完整前缀与坏尾，不把追加失败当成“什么都没写”。
- [ ] 用户输入写入/接纳失败、流批次写失败、取消定稿失败：分别断言下一步边界与恢复状态；不把屏上可见当成保存成功。
- [ ] 保留截断尾行与坏链拒开回归；不在本单偷偷引入裁尾续写合同。
- [ ] 安排受控代理断流、隔离环境磁盘写满/刷盘失败、进程强杀实测，保留账本与现场结果；按 ProcessCrash / PowerLoss 分档报告，未测真实断电就写未测。

## 五、测试交接与落点

测试已有人在补。先将新增用例与下表对齐，再补空白；不重复起一套只验证相同控制流的测试。

| 测试入口 | 应覆盖 |
| --- | --- |
| `tests/unit/api/test_model_request_recovery.cpp` | 白名单、非重试错误、最多 6 次、五档退避、取消边界 |
| `tests/unit/agent/test_agent_recovery.cpp` | 半截正文/工具 JSON 不进成功 history；前序工具不重跑；本地提交失败阻断发送 |
| `tests/unit/trajectory_v3/test_v3_stream_wiring.cpp` | 各次尝试收口、取消路径、流失败与正式消息分界 |
| `tests/unit/trajectory_v3/test_v3_writer.cpp` | 逐行失败、writer broken、引用耐久顺序、坏尾拒开 |
| `tests/unit/trajectory_v3/test_v3_tool_action.cpp` | 执行终态与保存终态分开、保存失败降级合同 |
| `tests/unit/trajectory_v3/test_v3_reader_resume.cpp` | done 缺结果、selected_no_message、消息未接纳、unknown、错误语义往返 |
| `tests/integration/runtime/test_failure_boundary_audit.cpp` | FA-01～FA-05 观察断言改成修复断言；真实桥接下比较请求、账本、恢复与显示 |
| 显示/转录测试（实现时确定具体文件） | attempt item 分界、失败标识、主路接线、最终 transcript |

预计生产落点：`src/agent/loop.cpp`、`src/api/model_request_recovery.*`、`src/runtime/trajectory_session.*`、`src/trajectory/v3/`、`src/trajectory/journal.cpp`，以及 `turn_event_adapter.hpp`、`terminal_turn_sink.cpp`、`turn_runner.cpp` 与相关回调声明。实现前按符号重新定位，旧审计行号不作当前行号。

## 六、收官门槛

- [ ] 逐条核对 FA 编号、修复代码、目标断言与实际结果；原审计快照保留，新结果另记，不能覆盖旧证据。
- [ ] 跑新增跨层回归及上表受影响测试；按仓库要求完成构建与相关平台检查，写清失败项和未验项。
- [ ] 每项结果写明后端调用数、工具执行数、关键提交链、恢复状态；显示项补 item/transcript 证据。
- [ ] 更新 `interview/failure-and-recovery.md`：将已修复项改写为有日期、有验证边界的现状；未完成项继续留账。
- [ ] 只有实际验过的条目才勾选；假后端探针、文件故障注入、真实现场实验分开记。
