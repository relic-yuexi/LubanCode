# 终端 Composer 合流：空闲与忙时共用组件及单一绘制循环

状态：待做（2026-08-25，用户查出：开场输入框与回合执行期间的排队输入框
并非同一对象；两边虽都用了 `LineEditorCore`，却各养状态、各算布局、各自
重画。输入框高度与多行光标改中一边，另一边照旧，维护已露出双倍成本）

## 一句话

不要把两枚 `LineEditorCore*` 生硬焊成一枚。要做的是：主草稿从开场到忙时
始终活着，空闲、排队只切策略；队列条目编辑另存草稿上下文，却复用同一
Composer 控制器、同一布局、同一 painter。再收拢终端所有权，只准一条
长驻 UI 线程读键、排帧、挪光标，模型流与工具线程只投事件，不直接下笔。

## 本单定案

“同一个东西”分三层说，免得后来各执一词：

1. **同一组件**：空闲、忙时、队列取回编辑，都走同一 `ComposerController`
   与同一 `BuildBottomChromeLayout`。
2. **同一主草稿**：回合从 Idle 变 Busy，主草稿对象不销毁、不 `BeginLine()`
   重置；忙时未按 Enter 的半截文字，回合结束后仍原样留在输入框。
3. **一个终端 owner**：TTY 交互期间，只有 UI runtime 能读 stdin、写 stdout、
   查屏幕坐标、保存 previous frame、设置物理光标。

不要求所有输入场景只分配一枚可变 editor。工具确认、选择菜单、队列条目
编辑，各自代表不同文档。它们可以有独立草稿槽。可它们不能再另写一套输入
组件和绘制器。

## 用户眼下会撞见什么

- 开场 Composer 有多行实排、上下留白与最小正文高度；忙时框仍按单行画。
- 忙时输入两行，只见第一行加“另有 N 行”，看不见真实插入点。
- 开场修了续行缩进、CJK 行尾光标，忙时仍按首行回显宽度猜光标。
- 改输入框高度，要同时找空闲常量、footer 固定行数、旧帧清理、真机测试。
- 空闲与忙时按键虽都喂 `LineEditorCore`，Up/Down、Esc、Enter、Tab 的分派
  散在两个循环，日后很容易一边添了能力，一边忘了。
- 回合结束时，监听线程析构。本轮尚未提交的新草稿随局部 editor 一同消失。
- 工具确认或 `ask_user` 开屏，要靠 `ConsoleReadMutex`、footer suspend、
  repaint suspend 三处互相躲。画面能保住，多半靠时序与注释守约。

这不是一只显示 bug。数据、交互、布局、终端所有权都分了家。

## 已坐实的代码现状

| 责任 | 空闲路径 | 忙时路径 | 分家之处 |
|---|---|---|---|
| editor 生命周期 | `SharedEditor()` 静态会话对象 | `TurnInputListener::ThreadMain` 栈上局部对象 | 回合一结束，忙时草稿消失 |
| 起读 | `ReadLineKeyByKey` 调 `BeginLine(true)` | listener 每轮构造后调 `BeginLine(true)` | 两次初始化，两本短命状态 |
| 按键循环 | `ReadLineKeyByKey` | `TurnInputListener::ThreadMain` | Enter/Esc/方向键/面板动作各分派一次 |
| editor 输出 | 完整 `RenderState` | 先取 `RenderState`，再压成 `FooterSlot().echo/hints` | 忙时丢掉真实多行布局 |
| Composer 布局 | `LayoutComposerRows` | 单行截断 `echo` | 高度、软换行、光标不共用 |
| 整帧拼装 | `RedrawEditArea` 构造 `InlineFrame` | `RedrawStreamFooterLocked` 构造 `rendered_rows` | 两套行序、两套 cursor plan |
| 帧账 | `previous_frame/prev_frame_origin` | `StreamFooterState.row/rows/...` | resize、擦旧帧各算一次 |
| 高度 | 按物理行 + padding + 最小正文高度 | `frame.composer_rows = 1` | 样式改动天然不同步 |
| 物理光标 | `layout.cursor_row/cursor_col` | `2 + DisplayWidthUtf8(echo)` | 多行与行中光标无法准确显示 |
| 终端读权 | 主线程持 `ConsoleReadMutex` | listener `try_lock` 抢读 | 靠抢锁切 owner |
| 终端写权 | 调用线程拿 `StdoutWriteMutex` 后直接画 | listener/stream/ticker 拿同锁直接画 | 多线程仍各知坐标、各持帧账 |

主要证据：

- `src/cli/console_input.cpp::SharedEditor`
- `src/cli/console_input.cpp::ReadLineKeyByKey`
- `src/cli/console_input.cpp::RedrawEditArea`
- `src/cli/console_input.cpp::StreamFooterState`
- `src/cli/console_input.cpp::RedrawStreamFooterLocked`
- `src/cli/console_input.cpp::TurnInputListener::ThreadMain`
- `src/cli/bottom_chrome.hpp::BottomChromeFrame`
- `tests/manual/stream_footer_driver.cpp` 中“上横线、单输入行、下横线、状态行”
  的四行硬编码断言

### `BottomChromeFrame` 还不算真正合流

眼下两条路都提到了 `BottomChromeFrame`，看着像已共用。其实它只记：

```text
queue_rows / composer_rows / status_rows / rule_rows
agent_dock_rows / transient_rows / revision
```

它不带编辑器的 `RenderState`，不产 Composer 物理行，不算 cursor，不拥有
previous frame，也不落笔。空闲与忙时仍各自把它解释成另一副画面。

“共用一只行数结构”不等于“共用 Composer”。

## 参考产品查得的边界

### Codex CLI

OpenAI 把 Codex CLI 主仓库公开。当前本机 `codex-cli 0.147.0` 的原生程序里
可以查到：

```text
tui/src/bottom_pane/chat_composer.rs
tui/src/bottom_pane/textarea.rs
tui/src/app/event_dispatch.rs
tui/src/app_event_sender.rs
edit_queued_message
```

能坐实的结论：普通输入与排队编辑落在同一套 `bottom_pane/chat_composer`
组件，外部变化经 app event 汇入 TUI。不能仅凭模块名断言从启动到退出只
分配过一枚对象；本单也不靠这条未经证实的假设立设计。

OpenAI 官方开源说明：
`https://learn.chatgpt.com/docs/open-source`

### Claude Code

Claude Code CLI 的实现源码没有随安装包公开，准确类名、线程数无从坐实。
官方交互合同却写得很清楚：Claude 工作时，用户仍在原输入框打字；Enter
以后消息列到输入框上方；Up 又把排队消息取回原输入框编辑。

官方说明：
`https://code.claude.com/docs/en/interactive-mode#queue-messages-while-claude-works`

Claude Code 另有 inline/fullscreen 两种整屏 renderer。这只能说明终端画法
可换，不能推出 Composer 也另写了一套。

### 从参考产品可取的，不是类名

- 输入框在 agent 工作时仍可响应，靠并发事件，不靠第二只仿制输入框。
- 后台任务可以多线程、多进程；终端最后一笔须集中落下。
- Composer 的模型与交互语义应稳定；Idle/Busy 只是 mode。
- 整屏 renderer 可以有多种 backend；它们应吃同一份 view model。

## 目标结构

### 一、会话里长驻一只 Composer

建议由 `InteractiveSession` 或新的 `TerminalUiRuntime` 持有：

```cpp
class ComposerController {
public:
    ComposerSnapshot Snapshot() const;
    ComposerOutcome HandleKey(const KeyEvent& key);
    void SetMode(ComposerMode mode);
    void SetTarget(MessageTarget target);
    void OpenQueueEdit(SteeringQueue::EditHandle handle);
    void CloseQueueEdit(QueueEditCloseReason reason);
};
```

生命周期：

```text
进程进入交互会话
  -> 构造 ComposerController
  -> Idle
  -> BusyQueue
  -> Idle
  -> BusyQueue
  -> ...
  -> 会话退出才析构
```

`TurnInputListener` 不准再每轮新造 `LineEditorCore`。

### 二、主草稿与临时草稿分槽

建议至少认三种文档上下文：

```cpp
enum class ComposerDraftKind {
    MainPrompt,   // 开场与忙时共用，跨回合活着
    QueueEdit,    // 取回某条队列消息，绑 EditHandle
    ModalInput,   // 工具确认/ask_user/选择器需要的临时输入
};
```

规矩：

- Idle 与 BusyQueue 永远指向同一份 `MainPrompt` 草稿。
- Busy 时按 Enter，只把当前主草稿入队，再清出一份新的主草稿。
- Busy 时没按 Enter，回合收场只切回 Idle，正文、光标、补全态都保留。
- 取回队列条目时，保存主草稿，激活 `QueueEdit` 草稿。
- 队列编辑提交、取消、冲突或删除后，恢复先前主草稿。
- modal 开屏只切输入路由，不许调用主草稿的 `BeginLine()` 清内容。
- 每种草稿都用同一 editor engine；不为 QueueEdit 再造简化 buffer。

### 三、拆开 `LineEditorCore` 里混住的状态

眼下 `LineEditorCore` 同时装着：

- 文本、光标、多行；
- paste、补全、候选菜单；
- 历史列表与历史游标；
- confirm mode；
- transcript focus mode；
- submitted/eof 等一次按键结果。

若整只长期共享，确认框一次 `BeginLine(false)` 便会冲掉主草稿。建议渐进拆成：

```text
ComposerDocument
  lines / cursor / paste / completion / menu

ComposerSessionState
  prompt history / confirm mode / shared preferences

ComposerInteractionState
  active draft / focus mode / queue edit transaction / key policy
```

首批不必立刻改完所有字段。可先给 `LineEditorCore` 增加可移动的
`DocumentSnapshot`，由 `ComposerController` 管 active slot；待行为测稳，再把
history、confirm mode 正式移出。

### 四、mode 只决定策略，不决定组件

```cpp
enum class ComposerMode {
    Idle,            // Enter 开主回合
    BusyQueue,       // Enter 入 SteeringQueue
    QueueEdit,       // Enter 原位替换
    ModalSuspended,  // 主 Composer 保稿，但键盘暂归 modal
};
```

每档由一张策略表决定：

| 按键/能力 | Idle | BusyQueue | QueueEdit | ModalSuspended |
|---|---|---|---|---|
| Enter | 提交主回合 | 新消息入队 | 提交原位替换 | 交给 modal |
| Esc | 清稿/退焦点 | 打断回合 | 取消编辑、恢复原文 | 交给 modal |
| Up/Down | 光标/历史/面板 | 光标/队列/面板 | 队列条目间移动 | 交给 modal |
| Tab | slash/焦点 | slash，按 busy policy 限菜单 | 同 busy | 交给 modal |
| Shift+Tab | 确认档/焦点 | 同一确认档 | 同一确认档 | 按 modal 合同 |
| Ctrl+O/E/L | transcript 动作 | 同一 dispatcher | 同一 dispatcher | modal 决定 |

策略函数只返回语义动作，不写 stdout：

```cpp
ComposerOutcome DispatchComposerKey(ComposerMode mode,
                                      const ComposerSnapshot& state,
                                      const KeyEvent& key);
```

空闲与忙时不再各抄一段 `if (key.kind == ...)`。

## 统一 view model 与布局

### 一、让 `BottomChromeFrame` 带得动真实 Composer

建议把“输入区有几行”从外面塞整数，改成由完整输入模型算出来：

```cpp
struct ComposerViewModel {
    RenderState editor;
    std::string prompt;
    std::string placeholder;
    ComposerMode mode;
    ConfirmMode confirm_mode;
    int min_body_rows = 3;
    int top_padding_rows = 1;
};

struct BottomChromeModel {
    std::vector<std::string> activity_rows;
    std::vector<std::string> queue_rows;
    ComposerViewModel composer;
    std::vector<std::string> status_rows;
    std::vector<std::string> agent_dock_rows;
    std::vector<std::string> transient_rows;
    std::string rule_tag;
};
```

名称可依代码风格调整。合同不可退：painter 必须收到完整 `RenderState`，不能
只收 `echo` 与 `composer_rows`。

### 二、全程序只留一个 Composer 布局入口

```cpp
struct BottomChromeLayout {
    InlineFrame frame;
    int composer_first_row = 0;
    int composer_row_count = 0;
    int cursor_x = 0;
    int cursor_row = 0;
    std::vector<int> painted_row_widths;
    std::uint64_t revision = 0;
};

BottomChromeLayout BuildBottomChromeLayout(const BottomChromeModel& model,
                                            const Theme& theme,
                                            int terminal_width);
```

这只纯函数统一负责：

- `LayoutComposerRows` 软换行；
- 首行 `> ` 与续行两格缩进；
- CJK/emoji 显示宽；
- 顶部 padding、最小正文高度、底部补空；
- 上下横线与 rule tag；
- queue、Working、status、dock、slash hints 的固定行序；
- placeholder 只在主草稿真空时显示；
- 真实物理 cursor；
- frame revision 与每行显示宽。

空闲、忙时给同一模型，只在 `mode`、activity、queue title 等数据上不同。

### 三、全程序只留一个 Bottom Chrome painter

建议新立：

```cpp
class BottomChromePainter {
public:
    PaintResult Paint(const BottomChromeLayout& layout,
                      const ChromeAnchor& anchor);
    void Retire();
    void Invalidate(TerminalGeneration generation);
};
```

它独占：

```text
previous_frame / frame_origin / row_count
body continuation cursor
terminal width / viewport generation
resize recovery data
physical cursor known/unknown
```

`RedrawEditArea` 与 `RedrawStreamFooterLocked` 可暂留作 adapter，却只能：

1. 组 `BottomChromeModel`；
2. 调同一 `BuildBottomChromeLayout`；
3. 调同一 `BottomChromePainter`。

adapter 里不许再拼输入行、不许再算 cursor、不许再写 `composer_rows = 1`。

## 终端线程与事件循环

### 为什么不是另起一个输入框进程

单独进程要另办终端句柄、控制序列、resize、退出、IPC 与崩溃恢复。输入框与
正文仍争同一枚终端光标，隔成进程并不会少争，反倒难以原子换帧。本单不走
这条路。

### 最终目标：一条长驻 UI 线程

建议 `TerminalUiRuntime` 从交互会话开始活到退出：

```text
agent/main worker ─┐
model stream ──────┤
tool callbacks ────┤  Post(UiEvent)
queue/session ─────┤
resize/ticker ─────┘
                         ↓
                 TerminalUiRuntime
                 - 唯一 KeyReader
                 - 唯一 screen query
                 - 唯一 frame cache
                 - 唯一 stdout writer
```

事件至少认：

```cpp
struct KeyPressed;
struct ComposerModeChanged;
struct ComposerStateChanged;
struct TranscriptAppended;
struct ToolBlockChanged;
struct QueueChanged;
struct AgentPanelChanged;
struct StatusChanged;
struct ActivityTick;
struct TerminalResized;
struct ModalOpened;
struct ModalClosed;
struct ShutdownRequested;
```

### 等键与 UI 事件如何同时醒

- Windows：console input handle 与一枚 wake event 一同交
  `WaitForMultipleObjects`；事件队列入项后 set wake event。
- POSIX：stdin 与 self-pipe/eventfd 一同 `poll`；后台投事件时写 wake fd。
- plain/pipe：不启 TUI runtime，仍走顺序文本输出。

UI 线程不能阻塞在模型请求、工具执行、文件 I/O 或 modal future 上。

### stdout 所有权

迁完以后：

- agent、stream、spinner、tool painter 不直接 `std::cout`；
- 它们发结构事件或 `TerminalOperation`；
- UI 线程合并同拍变化，算一帧，再批量 flush；
- debug 构建记录 UI thread id，非 owner 写终端立刻断言；
- `StdoutWriteMutex` 在迁移期保命，最终不再充当“多个 painter 都能写”的许可。

锁只能防字节交错，不能让多条线程共享同一本坐标账。故而仅加大锁不算完成。

### modal 与工具确认

最终不该让 `ReadChoiceMenu` 另抢 stdin。应改成：

```text
worker 发 ModalOpened(model, promise)
UI thread 切 active input route
Composer 主草稿原地保留，画面按 modal policy 挂起或缩起
用户作答
UI thread 发 ModalClosed(result)，恢复 Composer
worker future 醒来
```

迁移未到这里前，`ConsoleReadMutex` 与 `StreamFooterSuspendScope` 仍可留作
兼容层。不可一边删旧锁，一边仍让菜单和 UI thread 同时读键。

## 明确的行为合同

### Idle -> BusyQueue

- 同一 `MainPrompt` 草稿对象继续显示。
- 若 Enter 提交已清稿，BusyQueue 显示新空草稿。
- Working、待发队列只在框外增行，不改 Composer 自己的高度算法。
- 模式切换不得调用 `BeginLine()`，除非上一份主草稿已真正消费。

### BusyQueue -> Idle

- 未提交草稿原文、逻辑行、光标、补全状态、paste attachment 全保留。
- 终端宽度变过便按新宽重排，不把旧物理坐标写回 editor。
- 正在 QueueEdit 时回合结束，按现产品合同取消编辑事务、恢复队列原文；
  随后切回保存着的 MainPrompt 草稿。

### Busy Enter

- `ComposerOutcome::Enqueue(text, target)` 交会话层处理。
- Enqueue 成功后才清主草稿；失败则正文与光标原位保留，并显示错误。
- 入队不等于进入历史。真正送进主会话后，只记一次 prompt history。
- 空白正文不入队；多行正文原样存，不缩成首行摘要。

### QueueEdit

- `EditHandle` 与 QueueEdit 草稿绑在一起。
- 原主草稿藏起但不清空。
- Commit 冲突时，编辑正文不能丢；显示冲突，等用户另存为新消息或放弃。
- Cancel/Delete/Commit 都经同一 outcome dispatcher，不由 key loop 直接打印。

### modal

- modal 只接管输入路由，不接管 Composer 数据。
- modal 退场后，原草稿、光标、目标 task、queue edit 状态准确恢复。
- Esc 只归当前 active route。不能既关 modal，又打断 agent。

### resize

- Idle、Busy、QueueEdit 全走同一布局纯函数。
- 一次 resize 只使 painter 的 previous frame 失效一次。
- 真实多行全部画出，不再以“首行 + N 行”代替 Composer。
- cursor 始终来自 `WrappedComposerLayout`，不能再从 echo 字符串猜。

## 迁移次序

### P0：先钉现状与差异

- [ ] 给 Idle 与 Busy 各造同一份 `RenderState`，截取纯布局结果作 golden。
- [ ] 增一条失败测试：两边 Composer 物理行、padding、cursor 必须相同。
- [ ] 增一条真机测试：Busy 输入两行中文，第二行全文可见，光标落末字后。
- [ ] 增一条生命周期测试：Busy 打半句话不提交，回合结束后 Idle 仍见原稿。
- [ ] 记录当前 Enter/Esc/Up/Down/Tab/Shift+Tab 在四种 mode 下的行为表。
- [ ] 给 `StreamFooterState.echo/hints`、`frame.composer_rows = 1` 加迁移注释，
  不再往这两处添新能力。

### P1：先合布局，不动线程

- [ ] 引入 `ComposerViewModel`、`BottomChromeModel`、`BottomChromeLayout`。
- [ ] 写唯一 `BuildBottomChromeLayout` 纯函数。
- [ ] 从 `RedrawEditArea` 抽走输入行拼装、padding、cursor 计算。
- [ ] 让 `RedrawStreamFooterLocked` 传完整 `RenderState`，不再传 `echo`。
- [ ] Busy 真实绘制所有逻辑行与软换行物理行。
- [ ] 删除 `frame.composer_rows = 1` 与 `queue.echo_more_lines` 显示捷径。
- [ ] `BottomChromeRevision` 纳入 Composer 内容、cursor、mode、placeholder。
- [ ] 两条旧入口暂时都调同一布局，并过原有真机 driver。

### P2：合 painter 与帧账

- [ ] 引入 `BottomChromePainter`，独占 previous frame 与 origin。
- [ ] 空闲与忙时都走 `QueueInlineFrameDiff`/同一 legacy backend。
- [ ] resize recovery、viewport reveal、旧尾行清理只留一套。
- [ ] body continuation cursor 与 visible composer cursor 在 painter 内分账。
- [ ] `StreamFooterState` 只留业务显示态；删 row/rows/input_row/宽度等画布字段。
- [ ] `RedrawEditArea`、`RedrawStreamFooterLocked` 收成薄 adapter。
- [ ] 更新 `stream_footer_driver`，不再假定 Composer 永远一行。

### P3：主草稿长驻，合按键策略

- [ ] 引入 `ComposerController` 与 `ComposerMode`。
- [ ] `MainPrompt` 草稿挂到交互会话，不挂 `TurnInputListener` 栈上。
- [ ] Idle/Busy 切换不再构造 editor，不再无条件 `BeginLine()`。
- [ ] 引入 QueueEdit 草稿槽，保存/恢复 MainPrompt。
- [ ] 将 Enter/Esc/方向键/Tab/面板动作收进 mode policy 与 outcome dispatcher。
- [ ] history 与 confirm mode 逐步移出 `LineEditorCore`，至少不再借
  `SharedEditor()` 充全局状态仓库。
- [ ] 删除 `TurnInputListener::ThreadMain` 里的局部 `LineEditorCore editor(...)`。
- [ ] Busy 未提交草稿跨回合保留。

### P4：建单一 UI runtime

- [ ] 交互会话起头启动 `TerminalUiRuntime`，退场有界 join。
- [ ] 建线程安全 `UiEvent` 队列与 wake primitive。
- [ ] 将 key reader 搬进 UI runtime；`TurnInputListener` 先变 adapter，后删除。
- [ ] stream delta、spinner、queue、status、agent panel 改投事件。
- [ ] 所有 interactive TTY stdout 写经 UI runtime。
- [ ] UI thread 合并连续 tick/status 事件，用户按键与 modal 事件不得饿死。
- [ ] debug 下断言终端 owner；补退出、异常、Ctrl+C 后恢复 console mode。

### P5：迁 modal，删旧分叉

- [ ] 工具确认、`ask_user`、choice menu 改走 `ModalOpened/Closed`。
- [ ] modal future 支持 cancel/session shutdown，不留永等线程。
- [ ] 删除 `ConsoleReadMutex` 的双 reader 协调用途。
- [ ] 删除 `StreamFooterState.echo/hints` 的 editor 镜像。
- [ ] 删除两套 Composer 行拼装与 cursor 计算。
- [ ] 删除 `kStreamFooterBoxRows = 4` 这类固定高度假设。
- [ ] 只保留 plain/pipe 与 interactive TTY 两种输出 backend，不保留 Idle/Busy
  两套 TTY renderer。

## 测试矩阵

### 同源布局

- 空串、单行 ASCII、单行中文；
- 两个显式逻辑行、末行空、三行以上；
- 软换行恰满、差一格、宽字符前只剩一格；
- 光标在首行、续行、行首、行中、行尾；
- top padding、min body rows 改值后，Idle 与 Busy 同拍变化；
- placeholder、slash hints、queue rows、dock rows 单独增减；
- rule tag 有/无、dark/light/plain；
- 20/40/80/120/200 列。

### mode 与草稿

- Idle 空稿 -> Busy -> Idle；
- Idle 半截草稿 -> Busy，原文不变；
- Busy 半截草稿未 Enter -> Idle，原文与光标不变；
- Busy 多行 Enter -> 入队一次，主草稿清空；
- Enqueue 失败 -> 草稿不清；
- Busy 取回 QueueEdit，同时 MainPrompt 已有半截文字；
- QueueEdit Commit/Cancel/Delete/Conflict 后恢复 MainPrompt；
- QueueEdit 当口 turn 结束；
- modal 开/关嵌套两层，主草稿不动；
- `/clear`、session exit、resume 的草稿与队列处置写清并测试。

### 按键一致

- 字符、退格、Delete、左右、Home/End；
- Shift+Enter/Ctrl+J 多行；
- Tab 补全、候选轮转、slash hints；
- Up/Down 在多行内先走光标，越边界再走 history/queue/panel；
- Shift+Tab 的 confirm mode 只存一份；
- Ctrl+O/E/L、焦点查看、代理面板在 Idle/Busy 调同一 dispatcher；
- Esc 在 Idle、Busy、QueueEdit、Modal 各只触发一条语义；
- Ctrl+C 单击打断、双击退场，不和 modal/草稿清空串线；
- IME、paste burst、附件占位、外部编辑器读回。

### painter 与并发

- key、stream delta、status tick 同拍到达，只出一帧终态；
- resize 落在 layout 前、flush 前、flush 后；
- footer 变高、变矮，旧尾行擦净；
- transcript 追加时 Composer 不闪、不丢 cursor；
- modal 打开时后台 tick 不盖住菜单；
- UI event flood 下按键延迟有上界；
- 非 UI thread 尝试写 TTY，debug 测试能抓住；
- shutdown 时队列 drain/丢弃策略明确，不在 join 上挂死；
- plain/pipe 不启定位绘制，不夹 ANSI。

### 真机

- Windows Terminal + PowerShell；
- conhost/legacy backend；
- Linux xterm-compatible；
- tmux/screen；
- 窗口改宽时正停在第二行中间；
- agent 连续流字、Working 每拍动、用户同时快速输入；
- 工具确认弹起、作答、退场，草稿仍在；
- 连跑 100 个回合，每回合 Busy 留半截再回 Idle，不涨残帧、不丢稿；
- 录屏逐帧检查，无半帧空框、无光标跳到正文末尾。

## 埋点与诊断

加 `LUBANCODE_DEBUG_TUI=1`，默认只记结构，不记用户正文：

```text
event_seq / event_kind / source_thread
ui_thread_id / terminal_owner_ok
composer_mode / active_draft_kind / draft_revision
layout_revision / frame_revision / terminal_generation
origin / row_count / composer_row_count
planned_cursor / physical_cursor_known
paint_reason / changed_rows / coalesced_event_count
queue_edit_id / modal_depth
```

用户正文只记字节数、grapheme 数、逻辑行数。不可把 prompt 原文落进 debug
日志。模式切换时记 draft revision；若 Idle -> Busy -> Idle 后 revision 无故
变化，立刻能看出谁清了稿。

## 验收口径

- `TurnInputListener::ThreadMain` 不再构造本地 `LineEditorCore`。
- Idle 与 Busy 使用同一 `MainPrompt` 草稿；忙时未提交内容跨回合不丢。
- Idle、Busy、QueueEdit 的输入行都由同一纯布局函数生成。
- 代码里不再出现忙时 `composer_rows = 1` 与“首行 + N 行”替代真实 Composer。
- 改一次 padding、最小高度、续行缩进或 cursor 算法，三种 mode 同拍生效。
- 交互 TTY 存活期间，只准 UI runtime 一条线程写终端、保存物理坐标账。
- 后台 stream、spinner、tool、status 不直接挪终端光标。
- modal 打开前后的草稿、光标、目标、队列编辑事务完整恢复。
- resize、流式输出、动画与快速输入并发跑 500 次，无残行、重影、压字、丢稿。
- plain/pipe 保持顺序文本语义，不输出定位转义。

## 不能拿来冒充完成的做法

- 只把两只局部变量改成同一个全局裸指针。
- 只让两边都调用 `LayoutComposerRows`，painter 与 cursor 仍各算各的。
- 只加一把更大的 mutex，仍让多条线程各自写终端。
- 让 Busy 继续只画首行，却说“editor 已共用”。
- 把 QueueEdit 正文塞进 MainPrompt，取消编辑后找不回原草稿。
- modal 开屏前先清 MainPrompt，退场再凭字符串拼回。
- 为求统一，削掉 Idle 已有的多行、补全、历史或焦点能力。
- 先删 `ConsoleReadMutex`，却没把全部键盘读取迁进唯一 owner。

## 与其他 TODO 的分界

- `终端Composer刷新闪失_Enter所有权交接与多行行尾光标.todo`：管 Enter
  提交帧交接、物理 cursor 与 grapheme/cell 合同。本单先给它一只统一
  Composer 与 painter，彼单不再分别修 Idle/Busy。
- `终端用户输入背景块_工具条目留白与转录信息层级.todo`：管已提交用户块
  与 transcript block 间距，不管活 Composer 的草稿/按键。
- `子代理x停止失效_取消令牌贯通工具进程与可验证回执.todo`：管取消链。
  其中“空闲、流式按键共用 dispatcher”可借本单的 mode policy 落地。
- 本单不顺手改 provider、模型路由、队列投递时机、子代理调度或 transcript
  视觉配色。

## 主要落点

| 责任 | 文件 |
|---|---|
| editor 状态拆分与 snapshot | `src/cli/line_editor.*` |
| Composer controller/mode/policy | 建议新增 `src/cli/composer.*` |
| Bottom Chrome model/layout/painter | `src/cli/bottom_chrome.*`、`src/cli/terminal_frame.*` |
| 旧空闲/忙时 adapter | `src/cli/console_input.*` |
| 队列编辑事务 | `src/cli/queue_model.*` |
| UI runtime 与事件队列 | 建议新增 `src/cli/terminal_ui.*`、platform wake primitive |
| turn 生命周期接线 | `src/app/interactive_session.cpp`、`src/app/turn_runner.*` |
| stream/tool 事件化 | `src/cli/live_transcript.hpp`、`src/cli/tool_display.hpp`、`src/cli/spinner.*` |
| 单元测试 | `tests/unit/cli/test_line_editor.cpp`、`test_terminal_frame.cpp`、`test_bottom_chrome.cpp`、`test_repaint_coord.cpp`、`test_status_refresh.cpp` |
| 真屏驱动 | `tests/manual/stream_footer_driver.cpp`、`busy_tab_driver.cpp`、`screen_driver.cpp`、`agent_stream_driver.cpp` |

## 收尾删除清单

迁完逐项搜索，以下旧貌应当消失，或只剩薄兼容壳：

```text
SharedEditor() 兼任“编辑器 + 历史 + confirm mode 全局仓库”
TurnInputListener::ThreadMain 里的局部 LineEditorCore
FooterSlot().echo
FooterSlot().hints 作为 editor 镜像
frame.composer_rows = 1
queue.echo_more_lines
kStreamFooterBoxRows = 4
RedrawEditArea 自己拼 Composer 行
RedrawStreamFooterLocked 自己拼 Composer 行
多线程拿 StdoutWriteMutex 后各自 SetCursorPos/std::cout
```

删完再跑一次全仓 `rg`。若还留两处 cursor plan、两处 Composer padding、两处
输入行拼装，这单便没有收口。
