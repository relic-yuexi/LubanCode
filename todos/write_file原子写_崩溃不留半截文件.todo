# write_file 原子写：崩溃不留半截文件

- 状态：已实施（2026-09-09；write_file 落盘改接既有基建 `platform::AtomicWriteFile`（临时文件写全+原子换名，未另起炉灶）；错误文案口径沿用，换名失败照同款人话补一句；测试册 `test_write_edit.cpp` 增原子四景——写全无残尸/超长名明确错误/陈年孤儿不碍写不被收走/Windows 换名不成旧文完好。待 CI 验收）。
- 日期：2026-09-09
- 级别：P2（用户文件完整性：写一半崩了，原文件已被截，新内容没落全——两头空）。
- 范围：`src/tools/write_file.cpp`、既有原子写基建 `src/platform/atomic_write.hpp`（统一原子写收口审计 P1 的产物，先核能否直接复用）、`tests/unit/tools/test_write_edit.cpp`。
- 不在本单：edit_file 的替换路（另核另单）、跨卷 rename 的兜底矩阵（POSIX rename 原子、Windows MoveFileEx 语义由 atomic_write 基建背书，不重造）。

## 一、合同

- 写文件走"临时文件写全 → 原子换名"：任何时点崩溃/异常/取消，目标路径要么是旧原文、要么是完整新文，不存在半截。
- 临时文件落目标同目录（同卷才原子），命名带稳定前缀（如 `.lubancode-tmp-`），异常退出要清理，孤儿要能被后续操作识别收走或至少无害。
- 新建文件（原不存在）同享受此合同：不留零字节残尸（事故现场那只空 todo 文件正是此形状）。
- 保留既有行为面：编码关口（SanitizeExternalText 一类既有链）不动、错误文案口径不动、权限/属性沿用现写法或 atomic_write 基建口径。

## 二、修法方向

1. 先读 `src/platform/atomic_write.hpp` 与其现有调用方：若基建即"临时+rename"，write_file 改接基建，不另起炉灶；若基建形状不合（如缺同目录保证），补基建缺口。
2. 测试：故障注入在"写一半"处（内容超限/磁盘满模拟/异常路径，按既有测试手段选最低成本的）；断言目标文件内容非旧即新、无半截、无临时残尸。取消语义沿预留封顶单钉的"取消盲"现状——取消不中断写（协作式），本单只治崩溃级。

## 三、验收

- 故障注入册：三态（旧文完好/新文完整/明确错误）任一时点均不出现半截。
- 既有 test_write_edit 全绿；write_file 行为面（成功路径、目录创建、编码）零变化。
- 临时文件命名与清理规矩写进注释。

## 四、边界

不碰 edit_file、不碰平台 console、不顺带改取消语义。
