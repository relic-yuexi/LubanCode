# 思考流中预览：逐帧露尾与完毕自折叠

状态：待做（2026-08-27，用户对照 Claude Code 现场：思考时能瞥见内容，
完毕后收成 `Thought for 10s (ctrl+o to expand)`。已核现行 LubanCode：
`ThinkingDelta` 能收、能存、能在 Ctrl+O 快照里展开，却不在默认画面流中
露出；运行中展开也不会随新 delta 连续刷新）

## 一句话

思考来时，暗色露出末尾三行；内容往前走，画面跟着换。思考完了，立刻收成
一行 `思考 10.2s（Ctrl+O 展开）`。用户若主动展开，便尊重这只选择，继续
活刷，也不强行收回。只画 provider 真传回来的 thinking/reasoning summary，
没有便不编。

## 现状

```text
ThinkingDelta
  -> TerminalTurnSink::Emit
  -> ToolDisplay::OnThinkingDelta
  -> thinking_buffer / transcript_snapshot
```

- 第一枚 delta 建一条 `Thinking` transcript item，紧凑标题是 `思考中…`。
- 后续 delta 只攒进 `thinking_buffer`，不逐条刷屏。
- 按 `Ctrl+O` 能看当时已经收到的内容；这是一张快照，后续 delta 不追画。
- 完毕后标题改成 `思考 10.2s`，紧凑态藏正文，展开态留全文。
- 紧凑标题没写 `Ctrl+O` 提示。
- 当前只有一只全局 `transcript_expanded`，分不清“自动预览”与“用户主动展开”。

这不是 parser 缺思考。数据线已有。欠的是显示状态与按帧刷新。

## 依赖与边界

本单依赖 `todos/终端画面隔网_网络回调只投事件_一帧一落屏.todo` 的 UI
队列与单画笔。不能在网络回调里为每枚 `ThinkingDelta` 抢 stdout 锁、擦输入
框、重画终端；那会把“能看见思考”换成“输入区又闪没”。

可先把 transcript 的纯状态机与格式测试写好。真机活刷要等事件队列接通后
再开，不与旧逐 delta painter 另养一条旁路。

## 目标画面

### 默认自动预览

```text
思考中…  7.4s
  正在核对请求字段……
  Anthropic 与 Chat 的开关并不是一回事。
  还要查工具调用后的回传。
```

- 最多露三条视觉行，取末尾，不取开头。
- 新内容到来时，旧行上滚，新行露出。
- 用弱色、斜体或主题里的 secondary style；不套 Markdown，不抢正文层级。
- 长行按终端宽度折，预算按“视觉行”算，不按换行符算。
- 一枚大 delta 与许多小 delta 得到同一幅画。

### 思考完毕

```text
思考 10.2s（Ctrl+O 展开）
```

- 收折发生在 thinking block 收口时；若 wire 没有明确 block done，则在首枚
  `TextDelta`、`ToolUseStart` 或 `MessageDone` 到来时补收口。
- 收折只改这一条 thinking item，不带着整个 transcript 跳动。
- 无正文、只有签名的 thinking 不露空框；可只留时长，或与产品文案统一后
  显成 `思考完成`。

### 用户主动展开

```text
思考中…  7.4s · 286 字
  ……完整内容随流继续增长……
```

- 运行中按 `Ctrl+O`：进入用户展开态，后续 delta 按帧续画，不再是快照。
- 完毕后仍保持展开。用户既伸手打开，程序便不擅自关门。
- 再按 `Ctrl+O`：回到折叠态。
- 若 `Ctrl+O` 仍定义为全局 transcript 详略开关，thinking item 也须单独记
  `expanded_by_user`，免得自动收折误伤全局状态；最终可再决定是否把全局
  开关拆成视图策略。

## 状态机

每条 thinking item 至少认四态：

```text
Hidden
  -> AutoPreviewRunning
      -> CollapsedDone
      -> ExplicitExpandedRunning
          -> ExplicitExpandedDone

ExplicitExpandedRunning --Ctrl+O--> CollapsedRunning
CollapsedRunning --Ctrl+O--> ExplicitExpandedRunning
CollapsedDone --Ctrl+O--> ExplicitExpandedDone
ExplicitExpandedDone --Ctrl+O--> CollapsedDone
```

另记数据状态，别与画法混成一只枚举：

```text
received_text_bytes
received_signature_bytes
started_at / finished_at
provider_content_kind = thinking | reasoning_summary | redacted | unavailable
```

- `redacted/unavailable` 没正文可露，仍可报时长与“未提供摘要”；不造内容。
- signature 只为协议回传服务，不进预览，也不计“可见字数”。
- 自动预览只是视图，完整正文仍由 transcript item 持有；收折不丢数据。

## 刷新规矩

- 网络线程只投 `ThinkingDelta`。UI 线程每 16~33ms 合并一批再画。
- 同一帧里先更新 transcript model，再算预览尾行，再与 composer 一笔落屏。
- 时长不必每毫秒跳；100ms 或一帧一次足够。
- 预览最大三行，另设字节/字符安全上限；超长单词也要折，不许撑破终端。
- ANSI、控制字符、非法 UTF-8 先走现有清洗；provider 传来的转义不能改色、
  挪光标、清屏。
- resize 后按新宽重算三条视觉行；不拿旧物理坐标硬擦。
- 多枚 thinking block 依各自 item 排列；只更新当前 running item。
- 思考与工具调用交错时，先收当前 preview，再画工具；后头又来 thinking，
  新开一条 item，不把两段跨工具硬拼。

## plain 与非交互输出

pipe、重定向、无重绘能力的终端不做瞬时预览。只顺序输出稳定结果，免得日志
里落下反复改写的半截思考：

- 默认只留 `思考 10.2s`；
- 若用户显式开 verbose/transcript export，再输出完整 provider summary；
- 不发光标定位、不靠退格伪刷新。

## 测试

### 状态机与格式单测

- [ ] 第一枚 delta：`Hidden -> AutoPreviewRunning`，标题与首段可见。
- [ ] 连续小 delta：末尾三条视觉行随内容推进。
- [ ] 一枚 5000 字大 delta：布局稳定，只露三行，完整 buffer 不丢。
- [ ] 多字节 UTF-8 被劈开：预览不吐坏字节，收尾能补齐。
- [ ] thinking done：默认态自动收成一行，并带 `Ctrl+O` 提示。
- [ ] 运行中主动展开：后续 delta 可见；done 后不自动折叠。
- [ ] 主动折叠：后续 delta 仍收存，画面只留标题。
- [ ] 空 thinking、仅 signature、redacted/unavailable 各有稳定文案，不露空框。
- [ ] thinking -> text、thinking -> tool、thinking -> done 三种隐式收口。
- [ ] 两段 thinking 被工具隔开：生成两条 item，不串 buffer 与时长。

### 布局与安全

- [ ] 40/80/120/200 列；CJK、emoji、组合字符、超长无空格串。
- [ ] 控制字符与 ANSI 注入只当文字或被剥掉，不能操纵终端。
- [ ] resize 时三行预算不变，旧尾行擦净，无重影。
- [ ] compact/expanded 往返 100 次，全文、字数、时长不变。
- [ ] 预览里不跑 Markdown 表格、代码块高亮等重活。

### 按帧与真屏

- [ ] 假时钟 + UI scheduler：1000 枚小 delta 只按帧刷新，不按 1000 次刷新。
- [ ] 10 枚超大 delta：composer 常驻，心跳与键入不饿死。
- [ ] driver 逐帧抓屏：运行中确有预览，完成后下一帧只剩摘要行。
- [ ] 在预览期间输入、粘贴、resize、Ctrl+O，输入区不闪、不丢键。
- [ ] screen repaint 不可用时明走 plain 降级，不画半只框。

### 协议入口

用统一 `ThinkingDelta` 喂同一组显示测试，再各取一只真实 wire fixture 接入：

- Anthropic `thinking_delta + signature_delta`；
- Chat `reasoning_content` 与 vLLM `reasoning`；
- Responses `reasoning_summary_text.delta`；
- Gemini `part.thought=true`。

四家画面须一致；signature、索引、协议差异不得漏进 UI 文案。

## 验收口径

- 交互 TTY 默认在首枚有效 thinking 后一个 UI 帧内露出预览。
- 自动预览始终不超过三条视觉行，composer 始终可见、可输入。
- thinking 收口后的下一帧，只留 `思考 <时长>（Ctrl+O 展开）`。
- 运行中按 `Ctrl+O` 后，新增内容持续出现；完毕不强折。
- 折叠、展开、resize 都不丢全文，不改 session/history 里的 thinking block。
- 1000 枚 delta 压测时，落屏次数受帧率约束，不随 delta 数线性增长。
- provider 没传正文时绝不生成“思考内容”；文案明说 unavailable/redacted。
- Windows Terminal 与一只 POSIX TTY 真屏通过；plain 输出无 ANSI、无反复行。

## 不顺手做

- 本单不改模型思考开关与协议方言；见
  `todos/模型协议兼容实录矩阵_四类wire思考方言与真机回放.todo`。
- 本单不宣称展示模型内部原始思维链。只展示 API 明交出来的 thinking 或
  reasoning summary。
- 本单不另起网络线程直写终端的捷径。
- 本单不重做整套 transcript 导航；只补 thinking item 的视图状态与刷新合同。

## 主要落点

| 责任 | 文件 |
|---|---|
| thinking item 状态与缓冲 | `src/cli/tool_display.hpp`、`src/cli/transcript.*` |
| 事件接入 | `src/app/terminal_turn_sink.cpp`、`src/runtime/turn_event_adapter.*` |
| UI 队列与按帧刷新 | 随 `终端画面隔网_网络回调只投事件_一帧一落屏.todo` 落点 |
| 文案 | `src/cli/i18n.cpp` |
| 状态/格式测试 | `tests/unit/cli/test_transcript.cpp`、新增 thinking presentation 分册 |
| 真屏驱动 | `tests/manual/viewport_driver.cpp`、`tests/manual/stream_footer_driver.cpp` |
