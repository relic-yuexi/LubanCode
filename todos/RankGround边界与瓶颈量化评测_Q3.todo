# RankGround：9 区域 / pointwise-to-listwise / 145ms 的边界与瓶颈（Q3）

- 状态：待实施（2026-09-04 立案）
- 级别：P1（算法边界验证——再往前走就触顶，先把触顶点钉死）
- 类型：RankGround 方案评测（候选区域覆盖率 / 训练顺序有效性 / 筛选延迟瓶颈）
- 范围：RankGround 工程文件 / 评测脚本 / 报告
- 依赖：RankGround 主仓库（外部）、候选区域生成器、VLM 接口适配层
- 关联：与 LubanCode 主线工具检索/上下文治理无直接耦合（独立小项目）；不在本单：VLM 训练本身的损失/数据（已有，单独跑）
- 不在本单：reranker 模型本身的精度（不是这单的范围，这单钉方案边界）

## 一、先下断语

"9 区域覆盖 99.75%"是分布敏感的数字。换分布就会塌。先把分布钉死，再说"覆盖率"。

"pointwise-to-listwise"的训练顺序，**反过来训**不会直接失败，但会丢掉 listwise 的稳定性收益——这账要落到对照实验上。

"145ms 筛选延迟"在桌面前端够，在高频多轮交互场景里是真实瓶颈。先量出 P95 与 P99，再看是 VLM 推理还是 IO 卡。

## 二、现状证据（先把 RankGround 的真实现摸清）

### 2.1 候选区域生成

- 9 区域切分规则（ranker 选区域后给单次 VLM）。
- 切分规则对哪类页面适用，对哪类页面退化——这是分布钉死之前就要摆的。
- 现有真机数据：哪几类页面（截图/表单/代码块/表格）上 9 区域选区失败过。

### 2.2 pointwise-to-listwise 课程

- 训练阶段：先点点评分（pointwise）训稳，再喂 pairwise/listwise 排序损失。
- 现有数字：课程切换点（epoch / step / 验集指标），以及各阶段的对照。

### 2.3 VLM 单次筛选延迟

- 145ms 是中位数还是 P95？测试时硬件是什么？是否含 IO 与预处理？
- 筛选模型大小、量化档（fp16/int8/int4）、是否走 batch。

## 三、外部依据与版本裁决

- VLM baseline：Qwen2-VL / InternVL2 / GPT-4o 三档对照；钉死一档作主档，另外两档作敏感性测试。
- 候选区域标注：固定 200 张真实页面 + 100 张对抗页面（带长滚动、表格嵌套、动态加载）作基准集。
- 不引别家 reranker 横比——钉 RankGround 自身的改进前后对照。

## 四、不可破的合同

- 不动 9 区域切分规则的算法核心——本单只量边，不改边。
- 不动课程训练的 loss 组合——只调 checkpoint 切换点。
- 不动筛选模型的骨干（换骨干独立单）。
- 数据脱敏后入仓，不带原始业务内容。

## 五、实验 A：9 区域覆盖率与漏检代价

### A.1 目标

"9 区域覆盖 99.75%"——是哪份分布上的？换分布塌不塌？

### A.2 实验装置

三组数据集：

1. **桌面网页**：100 张主流站点首页与表单页。
2. **移动 H5**：100 张小程序/H5 表单与列表。
3. **对抗集**：100 张长滚动 + 动态加载 + 多 iframe 嵌套。

每张图跑候选区域生成器，记：

- 命中数（最终落入 VLM 调用的区域数，9 区域全空为"未命中"）
- 与 GT（人工标的目标区域）的 IoU 中位数
- 漏命中场景的页面特征（长宽比 / 滚动高度 / DOM 节点数）

### A.3 验收门槛

- 三组分别报"覆盖率"——桌面、移动、对抗，**各自一份**，不要混。
- 漏检 0.25% 的成本：把漏掉那部分页面在下游的命中率单独算账（VLM 全图重跑 vs 默认跳过）。
- 落点：`docs/evaluation/rankground_coverage.md`，数据 `tests/eval/rankground_coverage/`。

## 六、实验 B：pointwise-to-listwise 反向训练

### B.1 目标

"先 pointwise 再 listwise"为什么有效？反过来行不行？

### B.2 实验装置

三组训练顺序对照：

1. **pointwise → listwise**（现版）
2. **listwise → pointwise**（反向）
3. **joint**（两路 loss 同训）

每组同数据集、同 epoch、同验集。记录：

- 训练 loss 收敛曲线
- 验证集 NDCG@5、MRR、Recall@1
- 训练时长
- 选 checkpoint 后的真机命中率

### B.3 验收门槛

- 三组均出对照表。
- 反向训若是塌，给出塌点（listwise 阶段是否让 pointwise 退化）。
- 落点：`docs/evaluation/rankground_curriculum.md`。

## 七、实验 C：145ms 筛选延迟的瓶颈

### C.1 目标

145ms 是真账还是理想账？卡在哪？高频交互撑不撑得住？

### C.2 实验装置

三档硬件：

1. RTX 4090 / A100（基线）
2. RTX 3060（消费级）
3. CPU-only（兜底）

三档量化：

- fp16 / int8 / int4

每档测：

- 首字节延迟（warm-up 后冷启动）
- 单次 VLM 推理延迟（forward pass）
- IO + 预处理耗时（图片归一化 / tokenize）
- P50、P95、P99、最大值

对照：

- 桌面前端交互阈值：≤ 50ms 才不抖
- 高频多轮交互阈值：≤ 150ms 还能撑

### C.3 验收门槛

- 给出"真实可用延迟分布"——硬件档 × 量化档的笛卡尔积矩阵。
- 圈出真实瓶颈（forward / IO / scheduler / 后处理）。
- 给出至少一项可落地的优化方向（量化档切换 / 批大小 / 流式切片）。
- 落点：`docs/evaluation/rankground_latency.md`。

## 八、分期落地

### P0：基准集与对照装置

- [ ] 100+100+100 基准集脱敏入库（`tests/eval/rankground/datasets/`）。
- [ ] VLM 推理 hook 抽到 `tests/eval/rankground/vlm_runner.{hpp,cpp}`，三档硬件适配。
- [ ] 候选区域生成器自带覆盖率记录。

验收：三组各跑 5 张样本，命中数与漏检特征落盘。

### P1：实验 A（覆盖率）

- [ ] 三组全跑完，对照表与漏检成本账入库。

验收：`docs/evaluation/rankground_coverage.md` 出炉。

### P2：实验 B（课程训练对照）

- [ ] 三组训练顺序跑完，checkpoint 钉死。

验收：`docs/evaluation/rankground_curriculum.md` 出炉。

### P3：实验 C（延迟瓶颈）

- [ ] 三档硬件 × 三档量化的延迟矩阵出。

验收：`docs/evaluation/rankground_latency.md` 出炉。

## 九、改动边界（预计）

- 基准集与评测脚本走 `tests/eval/rankground/`。
- 报告走 `docs/evaluation/`。
- 若实验 A 给出"切 12 区域更稳"的结论，单走候选区域生成器（独立单）。
- 若实验 B 给出"joint 更稳"，单走训练脚本的 loss 组合（独立单）。
- 若实验 C 给出"int4 落 50ms 内"，单走筛选模型的量化档切换（独立单）。

## 十、不做什么

- 不动 RankGround 模型骨干
- 不改训练数据的标注口径
- 不动筛选接口协议
- 不引别家 reranker 作横比
- 不报"提升 N% "这种相对数（没有 baseline 兜底）