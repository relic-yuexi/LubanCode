# One-shot 轨迹指定输出：Harbor Harness 派生 JSONL

- 状态：待实施（2026-09-04 用户提案立案）
- 级别：P1（外部 Harness 接入缺口；不改 Agent 执行语义）
- 类型：LubanCode CLI / Trajectory Export
- 用户入口：`lubancode --yes --output /logs/trajectory.jsonl "<任务 instruction>"`
- 主要落点：`src/app/cli_options.*`、`src/app/cli_app.cpp`、`src/app/one_shot.*`、`src/trajectory/`

## 一、问题

Harbor adapter 能把配置与任务正文送进题目容器，也能捕获 stdout、stderr 与
退出码。LubanCode one-shot 眼下却只把 canonical trajectory 写进自己的
workspace/session 目录；调用方不能在启动时指定一份便携 JSONL 的落点。

adapter 只好从 stderr 抠 session id，再查 workspace 索引、找 session 目录、
拼内部路径。这把 LubanCode 的存储布局泄漏给外部 Harness，也让容器收尾、失败
恢复与日志归档多绕一圈。

## 二、先定语义：这是导出，不是改写 canonical 落点

`--output /logs/trajectory.jsonl` 不得把 `main.jsonl` 的 writer 改道。现有
canonical Journal 仍在 LubanCode workspace 下 append + flush，照旧承担恢复、
验链、resume、usage 与证据真值。

指定文件是一份 **harness export**：one-shot 收口后，从 canonical trajectory
做纯函数投影，再原子写到用户点名的路径。导出失败不得损坏或删除原账；进程被
强杀、来不及导出时，原账仍能拿 session id 恢复补导。

它可以看作“结构化 export history”，但不等于现有 Markdown 会话导出：

| 产物 | 用途 | 内容 |
| --- | --- | --- |
| `main.jsonl` 等 canonical streams | 恢复、验链、审计 | 细粒度事件、hash 链、引用与宿主控制事实 |
| Markdown history export | 给人阅读 | 用户/助手正文为主，省去多数执行细账 |
| `training-v1` | 训练数据集 | success/failure/partial/excluded 四路 + manifest |
| 本单 harness JSONL | Harbor/外部评测采集 | 对话 + thinking + 工具闭环 + usage + 错误 + outcome + 来源锚 |

## 三、CLI 合同

- [x] one-shot 新增 `--output <path>`；只在带任务正文的单发模式生效。
- [x] 交互模式、`app-server`、session 管理与其他子命令带 `--output` 时明报
  `cli.output_mode_mismatch`，不得把参数悄悄拼进问题正文。
- [x] `--output` 缺值、空值、目标是目录、父目录不可建或不可写，均在发模型前
  尽早明报；路径按容器内当前工作目录解析相对路径。
- [x] stdout 仍只承载现有模型输出，stderr 仍承载诊断；导出文件不混进终端
  转义、spinner、工具卡排版与 `[tokens]` 人眼摘要。
- [x] 用户显式点名目标，允许原子替换该一份文件；临时文件须与目标同目录，
  flush 成功后 rename，失败不留下冒充成品的半截 JSONL。
- [x] 成功时 stderr 打一行稳定、可解析的收据：schema、session_id、绝对输出路径、
  record 数与文件 SHA-256。失败返回稳定非零退出码与 error_code，同时打印
  session_id，方便 adapter 回原账补救。

## 四、harness-v1 JSONL 合同

首版格式名暂定 `lubancode.harness.trajectory`，`schema_version=1`。一行一枚
run/episode，不把一条 conversation 拆成靠文件顺序猜关系的散片。one-shot 主
run 至少一行；若有 subagent/workflow 子流，逐流各一行，用稳定 parent/run id
相连。

每行至少带：

- `schema`、`schema_version`、`exporter_version`；
- `session_id`、`workspace_key`、`run_id`、`parent_run_id`、`run_kind`；
- LubanCode 版本、provider、wire、model；
- 生效参数快照：采样参数、thinking 开关、`reasoning_effort`、输出/轮数/超时
  预算；密钥与认证头一律不出；
- 有序 messages：user、assistant text、thinking、tool call、tool result；宿主注入
  保留 actor/origin，不能冒充 user；
- 每次模型请求的 usage、stop reason、request attempt/retry 摘要；
- 工具名、有效入参、退出码、timeout/cancel/error code、结果正文或 artifact ref；
- 最终 outcome：success/failure/partial/cancelled/budget_exhausted/unknown，外加
  LubanCode 进程退出码；
- `source`：canonical stream id、末事件 hash、replay level、导出时间与 exporter
  config hash。

thinking 按现有 trajectory 隐私策略投影：没有授权保存正文时只留 ref/省略缘由，
不得为了 Harbor 临时绕开记录策略。大工具结果沿用 artifact 引用与摘要，不在
单文件里无上限膨胀。

## 五、复用现有 Exporter，不另造第二本历史

- [x] 从 `FoldStreamReplay` / `BuildSessionTrainingEpisodes` 抽可复用的中立 episode
  投影；harness exporter 与 training exporter 共用消息、工具、usage、outcome
  折叠逻辑。
- [x] training-v1 的四路筛选、训练授权与正文排除语义不得硬套给评测导出；
  harness-v1 即便失败或 partial 也要如实产出，靠 outcome 分型。
- [x] exporter 只读已封口或可验证前缀。正常 one-shot 路径先 `CloseSession`，再
  导出；close 失败或 stream 验链失败时不得产出 clean success。
- [x] 不直接复制 `main.jsonl`。内部事件 schema、blob 相对路径和多 stream 布局
  都不该成为 Harbor adapter 的协议。

## 六、Harbor adapter 接法

adapter 负责把生成好的 `.lubancode/config.json` 上传到题目容器，并以 argv
传入本题 instruction：

```text
lubancode --yes --output /logs/trajectory.jsonl "<task instruction>"
```

模型地址与密钥仍可走 `LUBAN_BASE_URL` / `LUBAN_API_KEY` / `LUBAN_MODEL`；
temperature、top_p、top_k、min_p、penalty、thinking 与 effort 写配置文件
`extra_body`。adapter 收到导出收据后，把 `/logs/trajectory.jsonl`、stdout、
stderr、进程退出码与 SHA-256 一并写回 Harbor `AgentContext`。

## 七、失败与恢复

- [x] 模型/API/工具失败但 canonical session 正常封口：仍导出一份 outcome=failed
  或 partial 的 JSONL，LubanCode 退出码照原执行结果返回。
- [x] 导出自身失败：不得把成功执行冒充完整评测记录；返回导出失败码，stderr
  同时给 session_id 与补导命令。
- [x] 进程被 Harbor timeout 强杀：允许没有目标文件；canonical WAL 留下
  incomplete，adapter 可在容器销毁前按 session id 补导 verified prefix。
- [x] 同一路径重试：只在新成品完整且 hash 已算出后原子替换；旧成品在此之前
  保持可读。

## 八、验收

- [x] CLI 单测：one-shot 解析 `--output`，缺值/错模式不落进 positional；Windows
  与 POSIX 路径各一册。
- [x] exporter 单测：纯文本、单工具、多工具、多 step、非零命令退出、工具超时、
  API 重试、thinking ref、usage 缺席/存在、partial 与子流关联。
- [x] 原子性单测：写失败保旧文件；成功替换无 `.tmp` 残件；目标路径只改用户
  点名文件。
- [x] 隐私单测：API key、Authorization、provider secret 与命中红线的 thinking
  正文不出 JSONL。
- [x] 回归：现有 canonical hash、resume、harness-replay、training-v1 字节与路由
  不变。
- [ ] Linux 容器 E2E：上传配置，跑一题至少两轮工具调用，验证目标文件存在、
  每行 JSON 可 parse、tool call/result 成对、usage/outcome/exit code 齐，SHA-256
  与 stderr 收据一致。
- [ ] Harbor 单题 E2E：adapter 不扫描 LubanCode 内部 workspace 路径，只凭指定
  输出与收据便能填回 `AgentContext`；随后并发 1 跑一小批，再谈全量。

## 九、不做什么

- 不把 canonical Journal 直接写到任意外部路径。
- 不拿 Markdown `/export` 冒充机器轨迹。
- 不让 Harbor 格式反向污染 AgentLoop 与 provider wire。
- 不在本单新增网络上传；只写题目容器内本地文件。
- 不声称 `reasoning_effort=xhigh` 已被 vLLM 采用；只记录“实际发送值”与能力验证
  状态，生效与否另靠探针或对照实验判。
