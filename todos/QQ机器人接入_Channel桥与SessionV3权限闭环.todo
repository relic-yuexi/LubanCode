# QQ 机器人接入：Channel 桥、Session V3 与权限闭环

日期：2026-09-13。源码基线：`1e3b460e`，以本次工作区读取为准。
现场修复补单（2026-09-14）：[Windows 连接诊断与交互配置](QQBot_Windows连接诊断与交互配置修复.todo)。覆盖 beta.1 的连接静默、Windows TLS 信任根、密钥文件权限与交互向导；待实现，归入 Q1b/Q3 验收。
补充调研：2026-09-13，工作区 HEAD 已到 `5fcfae9e`；复核文件投影、任务账、异步审批，并读取 QQ 在线官方文档。新增第十至十四节，覆盖文件、定时任务、按钮审批和用户给出的菜单面板入口。
状态：源码审查与实施设计；下列复选框均待实现，不能当作 QQ 已可用。
进度：Q0（权限与激活合同）已落 PR #64；Q1（传输定案 + 协议核心 + 进程内适配器 + 激活接线 + mock 测试）已落 **PR #66**（CI：头提交活跃 suite macos-clang 绿，run [34793234435](https://github.com/relic-yuexi/LubanCode/actions/runs/34793234435)；windows/linux 腿按 PR 快车道设计不跑，合并后全量复验），配对入口与进程外壳拆 Q1b；Q2（V3 与 outbox 总装：§六八项/§七七项全落）已落 **PR #75**（CI：头提交活跃 suite rollup=success——macos-clang 绿，run [34803613704](https://github.com/relic-yuexi/LubanCode/actions/runs/34803613704)；windows/linux 腿按 PR 快车道设计不跑，合并后全量复验）；Q1b（配对闭环：提示限频/批准控制面/一次性码）已落 PR #90；Q5（聊天侧建单次/周期任务：工具/归属/交付绑定/认领执行/补投分型，§十一记账）已落 PR #94；Q4（手机发文件闭环：§十收/发两面，mock 链全通）已落 **PR #93**（CI：见 PR checks；真平台媒体行为归 Q3）；Q3（真机联调）/Q6-Q7 待实现。

官方文档：https://bot.q.qq.com/wiki/develop/api-v2/dev-prepare/event-emit/payload.html

## 一、目标与用法

在本仓库新增 QQ 渠道包，按官方 API 接入；腾讯 SDK 只作可选封装和参考。宿主仍由 C++ Gateway 管会话、工具、权限、执行账和投递账。无需另建项目。直连 API 与可选 SDK 的取舍见第十五节。

首个可用版本：单机器人、已批准用户私聊、WebSocket、纯文本、多轮对话、重启恢复。不先开群聊、媒体、主动推送、流式输出和按钮审批。

启动与多渠道说明已补进 [渠道 README](../docs/architecture/channels/README.md#启动与多渠道)，根 README 和文档首页均有入口。QQ 是首只适配器；宿主按多渠道、多账号设计，飞书等后续平台不另造 Gateway。第一版改账号配置后显式重启，不承诺热更新。

用户只配一次 AppID/密钥来源，再在本机批准 QQ 身份。模型沿用现有配置；工作目录取 `gateway run` 启动位置。启动时冻结 workspace 身份；远端消息不能靠正文换目录、模型、凭据或权限。当前入口尚未装配 QQ，以下只是完成后的使用合同。

```powershell
cd D:\lubancode
lubancode gateway run
```

工作目录不是文件沙箱。批准聊天不等于授予写入、命令、私有记忆或 owner 身份。

## 二、当前代码：哪些可复用，哪些尚未接通

| 源码锚点 | 当前事实 | 改动落点 |
| --- | --- | --- |
| [cli_app.cpp](../src/app/cli_app.cpp)，`CliAction::RunGateway` | 已创建 backend、工具表、GatewayAutomationPump | 在此装配渠道业务；继续保留现有自动任务路径 |
| [automation_pump.hpp](../src/runtime/automation_pump.hpp)，`GatewayAutomationPump` | 现行候选为 AutomationDue，接本地回复投递 | 提供渠道候选与本地任务公平调度，避免两只泵争会话或重复执行 |
| [manager.hpp](../src/channel/manager.hpp)，`ChannelBridgeTransport` | 只有字节传输抽象，文件头明确未 spawn 真进程 | 增加常驻子进程 transport |
| [manager.cpp](../src/channel/manager.cpp)，`StartAccount` | 发 initialize/start；不负责完整激活判定与密钥解析 | 激活、凭据、进程装配后才可进入该入口 |
| [manager.cpp](../src/channel/manager.cpp)，`OnInboundLocked` | 先 Ingest 再 ACK，随后准入、入队；部分状态写入返回值未检查 | 所有失败须传播；ACK 之后的待办靠宿主账恢复 |
| [ingress_store.cpp](../src/channel/ingress_store.cpp)，`AppendLine` | 已保留规范化 event 原文和去重键；写入仅 ofstream + flush | 升为现有 PowerLoss 提交原语，不能重写一套正文仓库 |
| [manager.hpp](../src/channel/manager.hpp)，`TakeNextWork` | 取出即 queued → running；尚非 work/session/turn 完整绑定 | 补持久 claim/bind/settle 与恢复裁决 |
| [channel_router.cpp](../src/channel/channel_router.cpp)，`RouteChannelEvent` | DM pairing/allowlist、群准入、会话键、记忆策略已有；工具只取 best binding | 路由选择与权限合并分开处理 |
| [channel_session_host.cpp](../src/runtime/channel_session_host.cpp)，`Submit`、`PumpOne` | 内存排队、按 session_key 缓存引擎 | 内存只作索引，不能承担可靠受理与跨重启会话身份 |
| [agent_channel_engine.cpp](../src/runtime/agent_channel_engine.cpp)，`RunTurn` | 有 Agent 执行与确认策略，仍直接跑 agent；注释明示工具账遗留 | 提炼并复用完整执行装配，避免复制旧链 |
| [headless_executor.hpp](../src/runtime/headless_executor.hpp)，`HeadlessExecutor` | 已走 SessionService、工具账、预算、取消、reply selection；每次 fresh V3 | 增加渠道多轮所需的持有/恢复会话入口；不能每条 QQ 来信另开空场 |
| [session_service.hpp](../src/runtime/session_service.hpp)，`SubmitInput` | 当前已先存原件、PowerLoss 写操作账；支持 duplicate/conflict | 复用当前修复，不照旧 TODO 再修一次“受理成功却丢正文” |
| [reply_outbox.hpp](../src/gateway/reply_outbox.hpp)，`DurableReplyOutbox` | 已冻结回复、生成 delivery ID、发布本地文件 | 扩展 QQ 投递目标、尝试与回执；保持本地投递兼容 |
| [channel_config.hpp](../src/channel/channel_config.hpp)，`ChannelAccountUserConfig` | 已有 app_id、secret_env/file/inline、pairing、binding；未有完整分层工具上限字段 | 显式扩展 schema 和测试；不写解析器不接受的配置样例 |
| [config.cpp](../src/config/config.cpp)，项目渠道段合并 | 项目 channels 段当前整体拒绝 | 第一版凭据、启用、权限仍归全局；不能把“项目可收窄”文档愿景说成已实现 |

现有冻结件：[Bridge 协议](../docs/architecture/channels/bridge-protocol.md)、[配置](../docs/architecture/channels/configuration.md)、[权限](../docs/architecture/channels/security.md)。与本单提案冲突之处，须在实现前同步修改冻结件和解析测试。
常驻执行沿用 [Session V3 总装计划](持久Agent_Gateway接入SessionV3_常驻助理与可靠自动任务总装计划.todo)；该单 V0/V1 已有实现标记，本单补渠道纵向链。

## 三、目录与凭据：修正此前口头示例

源码布局示例（Node/SDK 备选方案；采用原生适配器时 runtime 下改放 C++ 源码和构建产物，桥协议与账号目录相同）：

```text
channels/qqbot/
  channel.yaml
  runtime/package.json
  runtime/package-lock.json
  runtime/src/index.ts
  runtime/src/bridge.ts
  runtime/src/qq-client.ts
  runtime/src/spool.ts
  runtime/src/delivery.ts
  runtime/tests/
```

包发现/挂载沿现有 Package Channel 组件接入；仅创建这个目录不算已安装。必须补实际安装布局、依赖检查、信任、挂载与真实启动测试。

账号状态沿现有约定：`~/.lubancode/channels/qqbot/main/`，不另插 `accounts/` 层。AppID 与密钥引用放全局 `~/.lubancode/config.json`；密钥不放代码包和项目。

第一版选 `secret_file`，内容为 AppSecret 原值，路径由用户明确指定；也兼容现有 `secret_env`。`secret_file` 并非任意 JSON 凭据文件，读取规则要写清，最多去掉末尾换行，拒绝空值、超限与不安全文件。

- [x] 新增运行时 credential resolver：按现有来源优先级处理；高优先级来源存在却无效时明报，不静默降级。诊断只报来源与状态。（Q0 落 `src/channel/credentials.hpp/cpp`：优先级 secret_file > secret_env > inline；稳定码清单见头文件；`DescribeCredentialSource` 同步同口径。测试 `tests/unit/channel/test_channel_credentials.cpp`——优先级/降级拒绝/稳定码逐项。PR #64）
- [x] 明确绝对路径解析、符号链接/重解析点、文件归属和 Windows DACL/POSIX 权限检查。账号 ID、渠道 ID 也须验证，不能拼出状态根外路径。（Q0 落 `ResolveChannelCredential`：绝对路径 + `weakly_canonical` + 归属/权限检查（POSIX mode 位、Windows owner SID + DACL 逐 ACE 白名单）；`IsValidChannelId/IsValidChannelAccountId` 守门，`ChannelManager::AddAccount` 拒非法 id。测试同上册 + `test_channel_manager.cpp` "非法 channel/account id"。Windows DACL 拒收案在 credentials 册内联；真实跨用户场景 CI 单账户 runner 覆盖不到，写"未验"。）
- [x] 密钥不进入 argv、模型输入、会话、trace、错误或普通日志。（Q0 落宿主侧：错误文案结构上只装稳定码/路径，`RedactSecret` 防御性脱敏，`ResolvedChannelCredential` 无流出口；测试"泄露禁令"案。交付通道措辞传输无关——凭据只进入获准的渠道执行载体（进程内直连则不出宿主进程；受管子进程则走不落日志的专用管道），实装归 Q1；`SidecarEnvAllowlist` 已定形环境白名单合同（定案子进程才消费），规矩写进 configuration.md §4。）
- [x] 适配器经不落日志的专用启动管道接收密钥；与固定 Bridge 握手字段分开，或先正式升级协议，不能悄悄加未知字段。（Q1 定案进程内直连——**无此件**：凭据不出宿主进程，无启动管道；见 §十五 定案与 configuration.md §4 已同步的定案措辞。）
- [x] 子进程只继承必要环境；不把宿主模型 API key、其他 QQ 账号凭据交给适配器。SDK 默认日志也需接脱敏 logger。（Q1 定案进程内直连——**不消费 `SidecarEnvAllowlist`**：无子进程无环境继承，无 SDK 日志面；该表白名单合同保留给未来受管子进程渠道。）
- [x] 暂不生成 `credentials.json.enc`。（Q0 未生成、文档明写"文件名带 enc 不代表加密"；OS 密钥库/DPAPI 后置。）

同一 OS 账号的文件 ACL 不能挡住该账号内拥有任意 shell 的 Agent。首版须限制工具能力并拦凭据路径；要承诺强隔离，另接系统沙箱/隔离账号，不能靠隐藏路径或提示词。

## 四、权限先落在宿主

### 4.1 谁能进门

- [x] QQ 首次配置模板明确 `dm_policy=pairing`、`group_policy=disabled`、`allow_bots=false`、`reply.mode=final`。不修改所有渠道默认值来迁就 QQ。（Q0 落 `MakeQqTemplateAccount()`（channel_config.cpp），configuration.md §7 有 JSON 样例；测试 `test_channel_config.cpp` "QQ 模板" + `test_channel_router.cpp` "QQ 模板路由出来"。PR #64）
- [x] 未批准用户只收到宿主生成的配对提示，不进模型。代码已有 PendingPairing 裁决，但当前 manager 只标 rejected；须补限频的配对提示发送。（**Q1b 已落**：`ChannelManager::OnInboundLocked` 的 PendingPairing 分支接提示出站——router 已申请的一次性 code 经 `MakePairingNoticeText` 生成提示（含 approve 命令指引与"须重发"提醒，零敏感信息），`MarkNoticeSent` 限频（同 sender 同账号 5 分钟冷却窗，PairingStore 持久 notices 账，重启不重发刷屏；被拒 sender `sender_rejected` 守门不再发 code 不再提示），`DrainPendingPairingNotices` 交 `ChannelWorkPump::PumpPairingNotices` 走 Q2 既有链路入 outbox 渠道段（EnqueueChannel 幂等 deliveryId + DriveChannelDeliveries 投递回执结算，不旁路）。崩溃窗口如实记：MarkNoticeSent 落账后、outbox 入箱前进程死，该枚提示丢失且冷却窗内不重发——下一条来信重触发，自愈。测试：`tests/unit/channel/test_channel_manager_pairing_notice.cpp`（入队/限频/重启不重发/被拒/批准放行五案）+ `tests/unit/runtime/test_channel_work_pump.cpp` Q1b 三案（配对闭环 e2e：未配对来信→提示带码→批准→重发→进模型，模型计数先 0 后 1；限频+重启；被拒安静）。真 QQ 平台提示触达归 Q3。PR #90，CI 头提交活跃套件 rollup=success，run [34928579947](https://github.com/relic-yuexi/LubanCode/actions/runs/34928579947)。）
- [x] 本地批准复用 PairingStore。补 Gateway 控制入口，让另一个终端能向持锁 Gateway 提交 approve/reject；普通交互进程不能直接拿一只空 manager 冒充批准成功。（**Q1b 已落**：CLI `lubancode channel pairing approve|reject <渠道> <账号> <配对码或身份> [--profile] [--timeout]`（`src/cli/channel_pairing_command.cpp`，cli_options 解析 + cli_app 分发）；投递门 `JudgePairingGate`——锁不在/读不懂/持有者死透三路明确报错指引（退码 2），不拿空 manager 冒充批准。命令经 Gateway 既有文件控制面（`src/gateway/pairing_command.*`：`pairing-<id>.json` 命令 + `pairing-<id>.result.json` 回执，stop.json 同款通道纪律：原子写、boot_id 复核防陈旧命令追新实例、消费即删）送达 `ChannelGatewayWiring::ConsumePairingCommands`（先按配对码认，not_found 落到 sender 身份路 `ApprovePairingBySender/RejectPairingBySender`）。测试：`tests/unit/cli/test_channel_pairing_command.cpp`（投递门四态）+ `tests/unit/gateway/test_gateway_pairing_command.cpp`（往返/陈旧/卫生/守门）+ `tests/unit/channelqq/test_qq_wiring.cpp` "Q1b 配对控制面"案（真 wiring 消费命令→账上真批准→回执）。PR #90。）
- [x] 配对码须一次性、有期限，批准后原消息不自动执行；用户重新发指令。不能把“第一个给机器人发话的人”自动设成 owner。（**Q1b 已落**：一次性/期限（expired 明报）PairingStore 既有合同保持，`tests/unit/channel/test_channel_pairing.cpp` Q1b 六案复钉（按身份批准/过期/一次性/被拒守门/限频账/v2 账面兼容旧裸数组）；批准后原消息不自动执行——ingress 落 rejected:pairing_pending 不回捞，提示正文明写"已发的这条不会自动补跑"，泵册 e2e 断言批准后模型计数仍 0、重发才 1。"配对不升 owner"Q0 已钉。真账号全链路归 Q3。）
- [x] 配对身份与 owner 分开。owner/私有记忆继续要求本机配置明确指定，不采信 SDK 上报的 is_owner。（Q0 测试钉住：`test_channel_router.cpp` "配对不升 owner"（批准账不进 allow_from）与 "provenance 不信 sidecar 的 is_owner" 既有案。）
- [x] 远端事件中的账号身份取实际连接上下文；宿主复核 channel/account。拒绝跨账号伪造；不要从正文提取 sender。（Q0 落 `ChannelManager::OnInboundLocked` 头一道身份复核——不符按 `invalid_frame` 进 Degraded，不 ingest 不 ack；sender 只取事件信封。测试 `test_channel_manager.cpp` "跨账号伪造" + router 册 "不从正文提 sender"。）

### 4.2 能用什么工具

当前 `best binding` 负责选 Agent，不能独自代表权限五层交集。新增政策字段前先定合并规则：

```text
有效工具 = Agent 已有工具
         ∩ 渠道上限 ∩ 账号上限 ∩ 所有命中 binding 上限
         - 各层 deny 并集
```

- [x] Agent 选择保持具体优先、同档冲突拒绝；权限收集所有适用上限，具体 binding 不能抹掉宽层 deny。（Q0 落 `ChannelRouter`：权限与 Agent 选择分两本账——`matched` 收集所有命中 binding，交集合并渠道/账号/各 binding，deny 全并。测试 `test_channel_router.cpp` "所有命中 binding 都算，具体 binding 抹不掉宽层 deny"。）
- [x] 区分 allow 未设置与 allow=[]：前者不添上限，后者禁用所有工具。现有 vector 空值表示“不设上限”，不能直接改义；须保留显式 presence、迁移旧配置并补兼容测试。（Q0 落 `std::optional<std::vector<std::string>> allow`（channel/binding/账号/渠道四处同口径）；解析层保留 presence；旧配置（不写 tools 段）解析为 nullopt 行为零变化。测试 `test_channel_binding_config.cpp` "allow 未设置与 allow=[] 分开"、`test_channel_config.cpp` "tools 上限字段"、router 册 "allow=[] 禁全部工具"。）
- [x] QQ 模板使用显式最小读取工具名单，逐项核实现有注册名。缺省不等于“所有免确认工具均为只读”。动态 tool_search、插件、MCP、子 Agent 也不能扩出这份上限。（Q0 落 `MakeQqTemplateAccount()`：read_file（`ReadFileTool::name()`）+ search（`SearchTool::name()`）两枚注册名逐项核实；名字 allowlist 天然拦 tool_search/tool_invoke/agent/mcp__*——router 册 "QQ 模板路由出来" 逐名断言。）
- [x] 复用 `ApplyChannelToolPolicy` 与 `ChannelConfirmAllows`，把有效策略及来源带到执行时；须确认工具未获明确授权就拒绝，不能在 Gateway 全局改成 auto。（Q0：`ToolRoutePolicy.source` 来源账随路由冻结；`TurnIngress.tools` 每轮带进执行——引擎建档为会话级暴露面，逐轮收窄由 `on_pre_tool_use_hook` 执行口闸拦（不折前缀缓存）；`ChannelConfirmAllows` 走 `ExplicitlyAllows`——零显式 allow 即无明确授权即拒。测试 e2e "逐轮策略收窄"、session_host 册 confirm 案。）
- [x] 读文件、搜索等工具统一拦账号凭据、全局密钥配置等受保护路径；检查 canonical 路径、链接绕过和实际工具执行入口。只过滤工具名不够。（Q0 落 `channel/tool_guard` 纯谓词（`platform::PathComparisonKey` canonical 比对）+ `AgentChannelEngine` 的 `GuardedChannelTool` 在 `Tool::execute` 入口拦截（注册元数据原样拷贝）；默认护全局 config.json、渠道状态根、信任账、rg-stage。测试 `test_channel_tool_guard.cpp`（含 ../ 回绕、search 缺省根）+ e2e "读文件工具碰受保护路径"（真引擎回路零执行）。符号链接案在无特权平台（Windows CI）可能建链失败——该案如实跳过，POSIX 腿覆盖，Windows 符号链接绕过路径写"未验"。）
- [x] 首版不开放任意 shell。后续开放时，若还要求目录强隔离，必须同时接系统执行隔离；不能用 shell 字符串黑名单冒充沙箱。（Q0：QQ 模板与文档都只放只读件，run_command 不进任何模板 allow；security.md §3 写明后置条件。）
- [x] 权限撤销影响后续待执行输入；已排队输入执行前重验准入并冻结新一轮策略版本。已开始的工具遵循现有取消边界，不假称副作用能撤回。（Q0 落 `ChannelManager::TakeNextWork` 取件重验——不过的输入就地落 rejected（ingress 状态机加 queued→rejected 退场边），过账决策即本轮冻结版本随 `WorkItem.route` 带出。测试 `test_channel_manager.cpp` "执行前重验准入"。）

参考：[路由](../src/channel/channel_router.cpp)、[工具过滤](../src/runtime/agent_channel_engine.cpp)、[确认裁决](../src/runtime/channel_session_host.cpp)。

## 五、QQ 适配器与真实 Bridge

> **2026-09-13 用户定案**：可以不用腾讯 SDK，也可自研薄适配层。方案取舍、传输底座现状与实施要求见 §十五；Q1 开工先按 §十五 定案并同步冻结件。

腾讯来源：`tencent-connect/openclaw-qqbot` 提交 `a730701d36aa7a070f98d4cba0f340f91f15e5f5`，依赖 `@tencent-connect/qqbot-nodejs`；本次会话已拆阅 SDK 1.0.4 源码。实施时固定版本和 lockfile，升级再验接口。仓库链接：[依赖清单](https://github.com/tencent-connect/openclaw-qqbot/blob/a730701d36aa7a070f98d4cba0f340f91f15e5f5/package.json)。

最小映射：

| QQ / Bridge | 适配动作 |
| --- | --- |
| 平台鉴权 | 适配器用 AppID/Secret 换令牌并刷新，可直接 HTTP 调用 |
| `channel.start` | 连接 WebSocket；凭据或 intent 拒绝须明报 |
| `C2C_MESSAGE_CREATE` | 从平台事件映射 sender/conversation；保留平台消息 ID、时间、正文，生成稳定 delivery_id |
| `channel.inbound` | 先落 sidecar spool，再上报规范化事件；等宿主 `channel.inbound.ack` 后清理 |
| `channel.send` | 取 direct OpenID、冻结正文、reply_to_message_id，调用 `POST /v2/users/{openid}/messages` |
| `channel.stop` | 停收、保留未 ACK spool、收连接与进程；超时杀进程树 |

- [x] 实现统一激活决策：Gateway 模式、包信任、双层 enabled、凭据可读、账号锁齐才装配。普通 CLI/one-shot/App Server 零渠道进程、零监听。（Q0 落决策函数 `src/channel/activation.hpp/cpp`；**Q1 接上真实调用方**：`src/app/channel_gateway_wiring.*` 的 `ChannelGatewayWiring::Create` 在 `gateway run` 装配段对每只启用账号跑该函数——trust 快照按定案恒"内置受信"，凭据走 Q0 resolver，锁权威在 `ChannelManager::AddAccount`；skipped 账号带稳定码打给 stderr，Gateway 照常起。cli_app.cpp RunGateway 段挂 `CompositeGatewayPump`（automation 主泵 + 渠道泵，不动 engine 的 GatewayWorkPump 冻结面）。测试 `tests/unit/channelqq/test_qq_wiring.cpp`：零渠道/分支裁决/五闸全过真装配三案。）
- [x] 按现有 manifest/握手能力交集声明；首版只宣称 websocket/direct/text/send/credentials，不借 SDK 全功能表虚报媒体或 streaming 已接通。（Q1 定案进程内直连——无渠道包/manifest 文件，能力声明内置：`QqBotCapabilities()` 只报 `websocket/direct/send/credentials`，media 显式 null、streaming 显式 false；测试 `test_qq_adapter.cpp` "initialize 握手帧"案逐字段断言。）
- [x] Package 安装/构建按实际 runtime 分发：原生适配器随平台编译，无 Node。（Q1 定案：`src/channel/qq/` 编进 `lubancode_engine`，mbedTLS FetchContent 静态链，零外部运行时；安装要求写进 channels/README §6。）
- [ ] ~~新增 `src/runtime/channel_bridge_process.*`，实现 ChannelBridgeTransport（子进程）~~（Q1 定案进程内直连：**不落子进程 transport**。`QqBotAdapter` 直接实现 `ChannelBridgeTransport` 字节面（进程内对象调用替代子进程 stdio），manager/ingress/pairing/路由零改动。stdio 帧、stderr 排水、进程树收尾三面随定案消失；Bridge 四字节帧协议照旧复用（`channel/frame.hpp`），粘帧/半帧在 `ws_frame` 层与 Bridge 帧层各有测试。）
- [x] sidecar spool、session/resume 状态与宿主 ingress 各归各账。（Q1 落 `src/channel/qq/qq_spool.*`：先落 sidecar spool 再上报、宿主 `inbound.ack` 后清理、重启 `ListPending` 全量重投（宿主 ingress 去重键兜底）；gateway session_id/seq 记账在 `qq_gateway`（宿主 ingress 不混账）。测试 `test_qq_spool.cpp` + `test_qq_adapter.cpp` "spool 重启重投"/"C2C 事件 spool 先落再报;ACK 后清理"案。）
- [ ] Gateway 遍历已安装渠道与启用账号，通过统一激活检查后逐一装配，不硬编码 qqbot/main。用两类假适配器验账号凭据隔离、会话隔离、资源公平与单账号故障收尾；损坏共用耐久账仍须停泵，不伪称平台之间绝无影响。（Q1：装配路径已落（`ChannelGatewayWiring` 遍历 channels 段，非 qqbot 渠道如实记 skipped 不起线程）；多渠道并发公平调度与故障收尾验收随 Q2 的 work pump 批次。）

## 六、接进 Session V3，保住多轮与恢复

- [x] ChannelIngressStore 改用既有 JournalWriter PowerLoss；规范化原文、稳定去重键一并落稳后才 ACK。状态追加失败停止推进，不继续排队或执行。（Q2 落 `src/channel/ingress_store.*`：`Open` 即持 `trajectory::JournalWriter`（Append 档），`AppendLine` 全部走 `Durability::PowerLoss`，失败一次性 `write_blocked`；dead-letter 旁路账同原语（惰性开柄）。行格式不变（evt/tr/dup），既有册 `test_channel_ingress_store.cpp`/`test_channel_manager.cpp` 零改动照跑。掉电不丢受理属耐久档差异，CI 只能验行为不能验掉电——写"未验"。）
- [x] 用现有 session_key 再结合 workspace 身份保存会话映射，明确账号锁阻止同一机器人被多个工作目录同时占用。重启换 cwd 不得误续别的项目上下文。（Q2 落 `src/channel/session_map.*`（`ChannelSessionMap`：键 (session_key, workspace_key) → V3 场 id，追加式 JSONL PowerLoss，重放取最后一笔，同键同场幂等）；同时占用由既有 `AccountLock`（AddAccount 即取）挡；映射查不到本 workspace 的键 → 开新场。测试 `test_channel_session_map.cpp` + 泵册 "不同 workspace 不串场"。）
- [x] 提炼 HeadlessExecutor 的执行装配为“已有渠道会话的一轮”，复用 SessionService、工具 Action 账、hook、预算、取消与回复选择。保留自动任务 fresh-session 行为。（Q2 落 `src/runtime/headless_executor.*`：受理→绑定→hook 四点→执行→收口→reply selection 提炼为私有 `RunTurnOnService`（不 Close——生命周期归调用方）；automation `Execute` 照旧 fresh 场跑完即 Close（V1 行为零变化，`test_gateway_automation_v1.cpp` 八案不动）；新 `ExecuteChannelTurn`：进程内同 session_key 复用同场+同 Agent（上下文随场存活），逐轮冻结策略经 `per_turn_tools` 进确认口与 `on_pre_tool_use_hook` 执行闸；活场超帽（缺省 8）LRU 淘汰，Close 后由映射账续。测试 `test_channel_work_pump.cpp` "连续两条消息共享上下文"/"工具轮走完整链"。）
- [x] 重启时通过 V3 resolver 恢复映射场，遵循当前 resume-as-new 合同并更新映射。不能只恢复磁盘账而把 Agent 历史置空。（Q2 落 `HeadlessExecutor::GetOrOpenChannelSession`：缓存 miss 而映射有场 → `SessionLaunchRequest{resume_at_launch, resume_source_session_id}` 开新场，`LaunchResumeHistory` 折叠投影灌回常驻 Agent；开场后 `on_session_mapped` 落映射账（幂等）。恢复裁决侧经 `workspace::index::ResolveDirByWorkspaceKey` + `FindV3SessionStream` 定位原场。测试泵册 "停机恢复仍接上上文"（新场 id ≠ 旧场、模型请求含旧对话、映射已更新）。）
- [x] 输入 client_operation_id 由渠道域、账号、ingress 身份稳定生成。相同来信重发返回原受理；同键异正文 conflict，不重复跑模型。（Q2 落 `ChannelWorkPump::MakeChannelOperationId` = `chan:<ch>:<acct>:in:<sid>`；同信重放经 ingress 去重命中原 sid → 同键 → SessionService 台账（#59/#68 底座）原受理返回。测试泵册 "来信重放"。异正文同键的 operation_conflict 由 SessionService 既有面承担（`test_session_service_durability.cpp` 已钉），渠道路上不可达（正文冻结在 ingress 账）——如实记。）
- [x] 提交顺序：ingress durable → 准入/路由冻结 → work claim → SessionService 接纳 → work/session/turn 绑定 → 执行 → reply selection → outbox。每个跨账窗口写恢复判据。（Q2 落 `src/runtime/channel_work_pump.*`（`ChannelWorkPump`，GatewayWorkPump 合同口）与 `src/channel/work_ledger.*`（`ChannelWorkLedger`：sid→场/轮绑定行，领域绑定先于 V3 gateway.work.bound，PowerLoss）。次序即 `TickOnce` 流程；恢复判据 = ingress Running + 绑定行 + V3 流三账对账（`RecoverOne`）。）
- [x] ACK 后内存满、进程退出、running 标记后未开轮等场景，靠领域账和 V3 查回；有不明工具副作用时标 needs_review，不盲重跑。（Q2 恢复裁决表：绑定行在+V3 有 assistant+无 selection → 补 selection（`V3Writer::Continue` 续原卷，不调模型）；selection 在 → 补 outbox 投影（同 deliveryId 幂等）；无 assistant（生成中断）/绑定行不在（claim 后崩）→ dead letter needs_review，绝不盲重跑。队列满走既有 RateLimited 旁路（OnInboundLocked）。测试泵册三窗注入 + "claim 后崩" + "队列满"。）
- [x] 新增渠道 work pump，与 AutomationDue 通过现有调度器公平取件；同 session 单飞，全局有并发/队列帽。配对、心跳和停止仍须响应。（Q2 落 `ChannelWorkPump`：与 automation 主泵经 `CompositeGatewayPump` 同 tick 各推进至多一轮（公平）+ 账号间轮转；同 session 单飞 = claim 与执行在泵内原子 + `in_flight_sids_`/`active_turns_` 门（多线程泵的闸）；全局帽 `max_active_channel_turns`（缺省 4，取件前查——claim 了不跑会把件搁死在 Running）。配对提示仍 Q1b；桥心跳/停止经 wiring `PumpAll`/`Close` 照旧。测试泵册 "关机次序"。）

## 七、回复只投递一次执行结果

- [x] 为 DurableReplyOutbox 加结构化 QQ target：channel/account/conversation/reply message ID；持久化冻结正文、拆段结果、ordinal、稳定 msg_seq 与 QQ 回执。（Q2 落 `src/gateway/reply_outbox.*`：`ReplyOutboxItem` 增 target 五字段 + `provider_message_id`/`delivery_error`/`attempts`/`source_ref`；`EnqueueChannel` 按段帽 2000 字节 UTF-8 边界拆段（`SplitReplySegments`，换行偏好），每段一枚 item（`MakeDeliveryId(selection, "channel:<ch>:<acct>:<conv>", ordinal)`，msg_seq=ordinal，段原件 replies/&lt;deliveryId&gt;.txt 冻结）。真平台长度上限/每消息回复计数归 Q3 实测校准——写"未验"。测试 `test_reply_outbox_channel.cpp` + 泵册 "长文拆段"。）
- [x] 相同 delivery_id 的网络重试复用同一发送载荷。不能每试一次就让 SDK 另造 msg_seq；须查 SDK 调用口是否保留显式序号，不满足就封装底层方法。（Q2：定案进程内直连无 SDK——`QqMessageSender`（Q1）按 `outbound_delivery_id` 冻结 msg_seq；宿主侧 `manager.SendReply` 恒以 outbox delivery_id 作桥协议 `client_id`，重试同 id 同载荷。测试泵册 "限频"（两次 channel.send 的 params 逐字节同）+ Q1 既有 `test_qq_auth_messages.cpp` "msg_seq 稳定"。）
- [x] channel.send 返回 accepted 与 QQ 已接受/最终失败要区分；补 manager 尚未消费的 delivery.receipt 与发送请求关联、超时、代次隔离。（Q2 落 `src/channel/manager.*`：`SendReply`（受理即编码发 sidecar，记 PendingChannelSend{request_id, delivery_id, generation}）；send 应答按 matched_request_id 结算（Accepted 带 provider_message_id / domain 错误分型）；`OnDeliveryReceiptLocked` 消费 `channel.delivery.receipt` 通知（按 outbound_delivery_id 关联在途 send）；重复回执只结一次（settled 账只记终态，限频不进——否则合法重试会被误挡）；超时 `ExpireStaleSendsLocked`（Pump 内）；代次隔离（受理代次 ≠ 结算代次 → 陈旧回执丢弃留诊断）。测试 `test_channel_manager_send.cpp` 六案 + 泵册 "重复回执"。注：send 应答路径的代次隔离在同步假 sidecar 下结构不可达（应答总在重启前送达），receipt 路径已钉——写"未验"。）
- [x] 发出前记尝试，返回后记 provider_message_id。超时可能是 QQ 已收到、回执丢失；若平台没有足够去重/查询保证，记 outcome_unknown 并停自动重发，不能宣称 exactly-once。（Q2：`RecordAttempt`（item.attempt 先账后网络）→ `MarkSent`（provider_message_id）/`MarkOutcomeUnknown`（`channel.delivery_unknown`，终态停自动重发）；超时缺省 30s（`ChannelManagerOptions::send_timeout_ms`）。测试泵册 "超时未知"（恰发一次、超时后不再发、模型不重跑）。真平台回执延迟分布归 Q3——写"未验"。）
- [x] 平台明确拒绝、限频、令牌失效、回复窗口过期分别处理。过期不擅自转主动消息；限额以真实账号联调核对。（Q2 落 `ClassifySendErrorLocked`：RateLimited/TransportFailed → rate_limited（退避同载荷重试，帽尽 failed）；LoginRequired → auth_failed（账号同步转 NeedsLogin）；detail 含 "expired" → reply_window_expired（不转主动消息，终态）；其余 → platform_reject。测试 `test_channel_manager_send.cpp` "限频分型" 四 subcase。真限频真值/主动消息额度归 Q3/Q5——写"未验"。）
- [x] 恢复只补 selection/outbox 或重投允许重试的已冻结回复，绝不重新执行 Agent。发送错误不得把已成功的业务执行改成执行失败。（Q2：恢复路只走 `PlanReplySelection`（纯函数）+ `CommitReplySelection`（幂等）+ `EnqueueChannel`（幂等）+ 段重投（同 delivery_id）；ingress 侧执行事实（Running→Replied）与投递态（→Delivered/DeliveryFailed）分开结算。测试泵册三窗 + 全部投递族案（模型计数恒 1）。）
- [x] `lubancode gateway status` 继续分 process/work/execution/delivery；输出渠道连接与投递错误，但不带原始密钥和整份平台事件。（Q2 落 `src/gateway/status.*`：`GatewayStatusSections` 增 channel 栏（账号连接态（manager 状态迁移时落的 `account-status.json` 原子快照）/ingress 待处理水位/死信/投递错误稳定码），`SectionsToJson`/`FormatSectionLines` 同步；零建目录零写盘。测试 `test_gateway_status_sections.cpp` "channel 栏"。**Q1b 该笔（status 渠道栏）就此销账**。跨进程连接态是最近一笔迁移的盘上快照（非活探针）——如实记。）

## 八、实施批次与验收

顺序：Q0 → Q1 → Q2 → Q3。Q3 只验纯文本接入；文件、定时任务、审批与菜单分别按第十至十四节的 Q4—Q7 验收。

### Q0：权限与激活合同

- [x] 配置 schema、allow 空值迁移、策略交集、凭据 resolver、真实激活决策；同步冻结文档。（PR #64，基线 c899c0fb。schema/五层交集/模板：`src/channel/channel_config.*`、`channel_router.*`；凭据：`src/channel/credentials.*`；激活：`src/channel/activation.*`；路径闸：`src/channel/tool_guard.*` + `src/runtime/agent_channel_engine.*`；宿主复核/执行前重验：`src/channel/manager.*`、`ingress_store.cpp`。冻结件先改后码：configuration.md §3/§4/§7/§8、security.md §3。）
- [x] 扩展现有 channel_config/binding/router/session_host 测试。证明具体 binding 不能扩权、未批准用户零模型调用、项目不能启用账号、配对不升 owner、凭据不可出日志。（五项逐一：① router 册"所有命中 binding 都算，具体 binding 抹不掉宽层 deny"+ e2e"逐轮策略收窄"；② e2e"准入与 pairing"断言 `backend.captured.empty()`；③ `test_channel_config.cpp` "config 合并"既有案维持（项目 channels 整体拒绝）；④ router 册"配对不升 owner"+"provenance 不信 is_owner"；⑤ credentials 册"泄露禁令"（错误文案/RedactSecret 不带值）+ activation 册 detail 只装稳定码。另新增 activation/credentials/tool_guard 三册、manager 册 Q0 四案、turn_ingress 册逐轮策略案。CI 结论见 PR checks。）

### Q1：真实进程与平台协议桥

（本批已落 PR #66；测试名/符号清单见下方两行记账。）

- [x] ~~新增 QQ 包、安装挂载、stdio transport；真实适配子进程 fixture~~ → **按 §十五 定案落进程内适配器装配**（原生 C++ 直连，无 Node、无子进程、无渠道包；Bridge 隔离语义按"保持一致的部分"执行：manifest 能力声明内置、Bridge 四字节帧协议与宿主权限/任务/会话/outbox 零改动）。握手/半帧/粘帧/超限/坏版本/停止与崩溃收尾逐项测试：`src/channel/qq/`（ws_frame/qq_socket/qq_tls/qq_ws_client/qq_proto/qq_spool/qq_auth/qq_messages/qq_gateway/qq_adapter）；测试册 `tests/unit/channelqq/test_qq_ws_frame.cpp`（半帧/粘帧/分片/超限/方向性 mask/RSV/控制帧）、`test_qq_ws_client.cpp`（真 socket 握手/SendText/ping 自动回 pong/close 传播/TLS 自签两路）、`test_qq_adapter.cpp`（Bridge initialize/start/stop 握手、坏 protocol_version 明败、manager 状态机推到 Running、stop 收线程）。"stderr 堵塞"案随子进程形态一并消失（无 stderr 面）。
- [x] mock HTTP/事件服务测 C2C 映射、spool 重启重投、ACK 清理、稳定发送载荷；未用 SDK（定案直连），无 SDK 适配测试。此处不是 QQ 联调。（测试册 `tests/unit/channelqq/`：`test_qq_proto.cpp`——官方 C2C 事件三例固化为 fixture + intents/opcode 表 + 发送载荷/响应 + 错误码全表分型；`test_qq_spool.cpp`——落盘/重投/ACK 清理/坏账跳过/路径注入拒绝；`test_qq_auth_messages.cpp`——token 单飞/到期/Invalidate/错误分型、msg_seq 稳定（同 delivery 重试同序号、同 msg_id 不同回复递增）、Deduped/过期/401 刷 token 重试；`test_qq_gateway.cpp`——假传输状态机：Hello→Identify→READY→Dispatch seq 记账→心跳饥饿断线、Resume 携带 session_id+seq、Invalid Session 不可恢复清 session 重 Identify、Reconnect 保 session、退避阶梯。mock 面：`tests/support/mock_ws_server.cpp`（回环明文/mbedTLS 自签 WS 服务端）+ 册内 ScriptedHttp/ScriptGatewayTransport。）
- **Q1 遗留（Q1b，规模阀门拆出，如实记账）**：~~配对提示限频发送（§4.1 第 2 行——manager PendingPairing 分支现状只落 rejected，提示发送未接）；Gateway 批准控制入口（§4.1 第 3/4 行，配对码一次性/期限/批准后不自动执行）~~（**Q1b 批已销**，§4.1 记账）；`gateway status` 的渠道连接/投递错误栏（§七 末行，**Q2 已销**）。
- **未验边界（如实记）**：Ready/Resume 的 d 字段形状官方 payload 页未逐字列全，按 opcode 表语义与官方 SDK 交叉核，真平台核验归 Q3；TLS 信任锚生产路只落了平台 PEM 路径探测（`DetectSystemCaPemPath`），真锚对真网关的验证归 Q3 联调；`/gateway` 接口的 URL 获取（GET 返回 `{"url":...}`）与 Access Token 接口字段（`appId/clientSecret`→`access_token/expires_in`）按官方 SDK 家族（botgo/botpy）交叉核，真账号行为归 Q3。心跳节拍按官方表"携带最新 s"实现。

### Q2：V3 与 outbox 总装

（本批已落 **PR #75**；CI 头提交活跃 suite rollup=success，run [34803613704](https://github.com/relic-yuexi/LubanCode/actions/runs/34803613704)。测试名/符号清单见 §六/§七 记账。）

- [x] 假 provider + 真桥 fixture：连续两条消息共享上下文；不同账号/用户/workspace 不串场；停机恢复仍接上上文。（册 `tests/unit/runtime/test_channel_work_pump.cpp`：假 provider（`CaptureBackend`，逐请求落对话 dump——上下文断言看真请求不靠猜）+ 真桥（`FakeChannelSidecar` 走真 Bridge 帧，support 件扩发送脚本/回执口）。四案："连续两条消息共享上下文"（同场+第二轮请求含第一轮对话）/"不同用户不串场"（dm-b 请求无 dm-a 对话、场 id 不同）/"不同 workspace 不串场"（换 ws 目录重启 → 新场，模型看不见旧对话）/"停机恢复仍接上上文"（干净停机 → 重建 → resume-as-new 新场带旧上下文、映射更新）。不同**账号**不串场由账号目录+ingress 账+session_key 含账号三重隔离承担，经理册/映射册已钉——泵册不另起一案。）
- [x] 写盘失败、队列满、来信重放、权限撤销、三个执行/回复窗口崩溃注入；模型和工具调用计数不多一次。（泵册：`HardKillWindowCase` 窗 1（生成后 selection 前）/窗 2（selection 后 outbox 前）共用骨架——工具轮真执行（工具计数 1）+ 恢复补齐全程模型计数不多一次、回复恰投一份、ingress delivered；窗 3（入箱后发送前，`fault_after_enqueue`）；"claim 后崩"（无绑定行 → dead letter needs_review，零模型）；"来信重放"（同 delivery 重投模型恰 1 次）；"权限撤销"（排队后两条同档 binding 冲突 → TakeNextWork 重验 rejected 零模型）；"队列满"（inbox 帽 1 → 第二件 rate_limited 旁路）；"写盘失败"（outbox 账被目录占住 → 入箱失败停泵、模型恰 1 次）。计数走盘上 calls.log，装配销毁重建后仍有效——"不多一次"是对账不是内存断言。）
- [x] QQ 超时未知结果、明确拒绝、重复回执、旧代次回执、限频；发送重试不重跑 Agent。现有自动任务本地投递测试仍通过。（泵册："超时未知"（Silent 脚本+短超时 → delivery_unknown 终态、恰发一次、不再自动重发、模型 1 次）/"明确拒绝"（platform_reject 终态不重试）/"重复回执"（send 应答已结，迟到的 delivery.receipt 不翻账）/"限频"（RateLimitedFirst：退避后同 client_id 同载荷重试成功，两次 params 逐字节同、模型 1 次）。**旧代次回执**：`test_channel_manager_send.cpp` "重复回执只结一次;旧代次回执隔离"（RestartAccount 后 receipt 不结算；send 应答路径的代次隔离同步假件下结构不可达，写"未验"）。既有册零改动：`test_gateway_automation_v1.cpp`（本地投递八案）与 `test_reply_outbox.cpp` 照跑——回归交 CI。）

**Q2 未验边界（如实记，归 Q3）**：真平台 msg_id/msg_seq 行为、回复窗口真值、限频真值、回执延迟分布、2000 字节段帽是否贴平台上限、Deduped（40054005）在真链路的语义——本地 mock 不能代替这些结论；send 应答路径的代次隔离与进程内多线程泵的单飞闸（同步泵下结构不可达/未跑）。~~Q1b 两笔（配对提示限频发送、Gateway 批准控制入口）未随本批带走，仍立账~~（**Q1b 批已销**，§4.1 记账；未验边界同款归 Q3：真 QQ 平台提示触达与真实配对流）；**status 渠道栏一笔已随本批销**（§七 末行记账）。

### Q3：真实账号联调

- [ ] 用户在本机配置凭据；明确授权测试消息接收者后，用真实机器人验证本人配对、陌生人拦截、两轮聊天、重连与停止。
- [ ] 校验 QQ 端实际 msg_id/msg_seq 行为、回复窗口、失败码和平台权限；本地 mock 不能代替这些结论。
- [ ] 另做真实进程硬杀重启；故障注入测过不等于系统硬杀或断电测过。
- [ ] 文档列出已验证范围、Node 安装与账号迁移步骤。群聊/媒体/主动推送/按钮审批另批验收，不提前开启。

## 九、本次交付边界

本单及后续补充只记录源码审查与实施设计。没有修改运行时代码，没有运行 C++/Node 功能测试，也没有 QQ 真实账号调用。文档路径、锚点和 diff 检查另记交付说明。

## 十、Q4：手机发文件，处理后把产物发回去

### 10.1 已核实的接口与当前缺口

QQ 单聊事件的 `attachments[]` 提供 url、filename、size、content_type；引用消息还可在 msg_elements 中带附件。来源：[单聊消息事件](https://bot.q.qq.com/wiki/develop/api-v2/autogen/event/c2c_message_create.html)。下载 URL 是文件入口，不是可永久依赖的存储，也不应原样进入模型与日志。

LubanCode 的 [ChannelPart / MediaAttachment / ReplyAction](../src/channel/types.hpp) 已有媒体字段，但 [MakeChannelTurnIngress](../src/runtime/turn_ingress.cpp) 目前只生成“收到文件，未解析”一类文本；[SessionService 输入](../src/runtime/session_service.hpp) 当前只收 text/images；[ReplyOutboxItem](../src/gateway/reply_outbox.hpp) 仍是冻结文本。只有附件类型声明，尚未完成下载、文档读取和文件投递。

QQ 出站分两步，基址仍为 `https://api.sgroup.qq.com`：

1. `POST /v2/users/{openid}/files` 上传/合并，`srv_send_msg=false`，取得 file_info。
2. `POST /v2/users/{openid}/messages`，传 `msg_type=7`、`media.file_info`，以及本次合法回复的 msg_id/msg_seq。

file_type 为图片 1、视频 2、语音 3、普通文件 4。`file_info` 透传，不自己解码；ttl 到期重传原件。上传成功不等于消息已发出。`srv_send_msg=true` 会直接发信并占主动消息频次，可靠 outbox 路径不走这个捷径。[单聊富媒体上传](https://bot.q.qq.com/wiki/develop/api-v2/autogen/api/v2_users_user_openid_files.post.html)

本机产物优先走分片上传，避免为了发文件临时开放公网文件服务器：

```text
POST /v2/users/{openid}/upload_prepare
  → 文件大小、文件名、MD5/SHA1、前 10002432 字节 MD5
  → 得到 upload_id、block_size、parts、upload_config
逐片 PUT 到服务端返回的预签名 URL
  → POST /v2/users/{openid}/upload_part_finish
POST /v2/users/{openid}/files {upload_id, srv_send_msg:false}
  → file_info → 发送消息
```

依据：[预上传接口](https://bot.q.qq.com/wiki/develop/api-v2/autogen/api/v2_users_user_id_upload_prepare.post.html)。COS PUT 不带 QQ Authorization，也不把预签名 URL 写入普通日志。

**接口与 SDK 有漂移，先加合同测试。** 本次拆阅的 SDK 1.0.4 `src/protocol/api/media-chunked.ts` 使用 `(part.index - 1) * block_size`；官方示例却从 index=0 起。SDK 将 file_size 作为 number 发送，并从响应顶层读 concurrency/retry_timeout；官方字段为字符串大小、配置在 upload_config 下。须以当前真实响应判定，做显式适配；不能让负偏移、字符串相加或错误并发进入上传循环。

**Q4 开工复核（2026-09-15，官方四页逐字重核）**：事件页 attachments 字段表为 url/filename/width/height/size/content_type（枚举含非 MIME 的 `voice`/`file`；三例官方示例均无 attachments）；上传页请求体只列 file_type/url/srv_send_msg/file_name/upload_id（**无 file_data**，Base64 直传确认非官方路径），响应 file_uuid/file_info/ttl（0=长期）/id/raw_url；upload_prepare 页请求六字段全必填且 `file_size` 为字符串、字段名 `md5_10m`，响应 parts[].`presigned_url`（非 upload_url）、官方明写 index"从 0 开始"、upload_config 嵌套 concurrency/retry_timeout/retry_delay；upload_part_finish 请求 {upload_id,part_index,block_size,md5} 响应空对象；发送页 msg_type=7 官方示例只传 media+msg_id+msg_seq（不带 content）。媒体错误码：850019（格式）/850031（超大小）/850026（转存失败）/850027（超时）/40093001（BDH 通道异常，官方建议重试）/40093002（当日容量上限）。全部落进 qq_proto 纯函数与分型表，fixture 钉死。

官方现页普通文件上限写 200MB，腾讯插件 README 写 100MB；当前 manifest 示例还写 20MiB。三者不是同一口径。产品上限按宿主配置、适配器能力和平台实际许可取最小值。SDK 小文件路径发送 file_data，但当前官方页面只列 URL 和 upload_id；Base64 直传列为待核能力，不作为首批必需路径。

### 10.2 具体改动

- [x] 新增宿主附件接纳/解析服务，拟放 `src/runtime/channel_media_service.*`。规范化附件引用先入 ingress；准入通过才下载大文件，不让陌生用户耗下载/解析资源。已 ACK 的附件元数据不等于二进制已持久，另记 downloading/ready/failed。（Q4 落 `src/runtime/channel_media_service.*`：`Open`（建仓+账重放幂等缓存）/`Ingest`（白名单→净化→幂等查账→下载→sha256→原子发布→耐久账行）；接纳时机=`ChannelWorkPump::ProcessWorkItem` 执行前（准入已过，陌生用户耗不了下载资源）；downloading/ready/failed 以 receipt+media.jsonl 逐行追加承担（不另养内存状态机）。媒体仓=`<workspace 身份根>/channel-media/`（inbound/<att-*.bin>+media.jsonl）——刻意不在渠道状态根（Q0 工具护单盖整棵渠道状态树，模型 read_file 读不到）。测试 `tests/unit/runtime/test_channel_media.cpp` 六案。）
- [x] QQ 适配器只取平台附件、执行受控传输；宿主核账号/会话归属、文件名、大小、MIME、hash。下载临时件成功后原子发布到会话可访问的受控 artifact 目录，再进入 Agent 一轮。失败明确报附件不可用，不能假装读过文件。（`qq_proto` 的 `MapC2cMessageCreate` 附件段升级：不再降级 Unsupported，按官方 content_type 枚举落真类型 part（image/*→Image、video/*→Video、voice|audio→Audio、其余含 "file"→File），url 冻结进 remote_ref、size 走 ParseLooseInt64（真机教训两态）；受控传输=`ChannelMediaDownloadFn` seam（装配层 gateway_launch 绑 `qq::DownloadQqAttachment`，url 校验/帽/脱敏都在其内）；原子发布=AtomicWriteFile(ProcessCrashDurability)；失败进 prompt 一行稳定说明（mime_not_allowed/download_failed:码/too_many_attachments），不假装读过。测试 `test_qq_proto.cpp` "附件按官方 content_type 映射真类型"+泵册 Q4 收件闭环。）
- [x] 限制字节总量、重定向、超时和解压膨胀；校验目标地址，拒绝私网与非预期协议，防 DNS/链接绕过。签名 URL、message_scene.ext 中的 auth_token 不进入模型。引用附件只按用户请求显式展开，有递归深度和数量帽。（字节总量：收发同帽 20 MiB（官方 200MB/插件 100MB/示例 20MiB 三口径取最小），下载在传输层（MakeMediaHttpFunc 响应帽=同值，回调入口掐流）+到手复核双重落锤，上传在上传器前置帽；重定向：net 层既有 SetRedirect(false) 不跟；超时：媒体 seam 独立硬墙 60s；目标地址：https 强制+域族后缀白名单（qq.com/qq.com.cn/qpic.cn/myqcloud.com/tencent-cloud.com）+字面 IP 一律拒（私网/回环/公网裸 IP）+userinfo 拒；签名 URL：账面记 StripUrlQuery（host+path），模型 prompt 与错误文案无 token（测试断言钉死）。解压膨胀：本批无压缩/解压面，如实记无此险；DNS rebinding 动态半边（解析后钉地址）未接——写"未验"；引用附件（msg_elements）仍未展开——归后续批。）
- [x] 通过稳定 artifact_id 记录原名、hash、MIME、原始来信和可读范围。补 SessionService 持久输入引用及其 replay 合同；不能只在首次 prompt 塞临时绝对路径，重启就丢。（前半落：artifact_id=att-<sha256 前 16>，media.jsonl 账行含原名（净化前后）/MIME/size/sha256/来源 sid+provider_event_id/脱敏 url/时间。SessionService replay 合同如实未扩：渠道路恢复裁决（RecoverOne）只补 selection/outbox，不重跑不重拼 prompt——执行事实保留，无需 replay；媒体原件跨重启在仓（Open 账重放幂等）。边界：prompt 里的存档路径是绝对路径，跨 workspace 迁移不保证。）
- [x] 文本/源码走受限读工具；图片走现有 ImageBlock 与模型能力检查；PDF/DOCX/XLSX 走可配置提取器，结果带页/表/段出处、大小帽和解析状态。传输支持某格式不等于模型能理解它。此批不运行文档宏、不自动执行上传脚本。（文本/源码：受限读取口=既有 read_file 工具（绝对路径+offset/limit 有界+1MB 帽；媒体仓在 workspace 侧不在工具护单，天然可达）——prompt 投影给路径，大文件模型自取，正文不整塞（预览 2 KiB UTF-8 边界，含 NUL 视为二进制不预览）。图片 ImageBlock 未接（投影只描述，模型能力检查归后续）；PDF/DOCX/XLSX 可配置提取器未做——白名单收纳但只落盘，模型 read_file 读到的是原始字节，"传输支持某格式不等于模型能理解它"如实保持；文档宏不运行、上传脚本不自动执行。）
- [ ] 新增平台中立的产物交付口（拟 `channel_deliver_artifact`），只接 artifact_id 与说明；接收账号和会话由宿主绑定，模型不能把文件改发第三人。用户明确要求跨会话发送时另经目标授权。（Q4 交付口径明示：聊天内点名单文件归后续批——本批产物附件走最保守路，见下一行记账。）
- [x] reply selection 扩展为有序正文/文件引用清单；输出原件冻结后再入 outbox。没有文字、只有一个文件也算有效回复，不被当前纯文本选择器误判为空。（`DurableReplyOutbox::EnqueueChannel` 增附件参数：入箱读原件算 sha256（冻结校验，读不了明败 outbox.attachment_unreadable），账行带 attachment 五字段（旧账行无新键=空，兼容）；附件独占一枚空文本末段（ordinal=文本段数+1）——QQ msg_type=7 不带 content（官方示例口径），正文全在前面自己的段里，谁也不吃掉谁；纯附件回复合法（空正文+附件=单附件段；空正文无附件照旧 outbox.segment_invalid）。产物来源最保守路（10.2 无明确触发口径时的定案）：**长回复（拆段>1）末段自动附带任务结果文件**=reply selection 冻结正文原件 replies/<selectionId>.txt（本地族 out/<id>.txt 的渠道对应物），短回复即正文不带文件；`ChannelManager::SendReply` 放开"正文空拒绝"（text 与附件至少其一）。selection 选择器本身仍纯文本（空正文轮不可选）——纯附件回复的来源本批只有泵投影路（模型无法自发产出纯附件轮），如实记。测试 `test_reply_outbox_channel.cpp` Q4 三案+泵册 Q4 发件闭环。）
- [x] QQ 适配器新增 media-upload 模块；保持原件 hash、upload_id、已完成分片和 file_info/到期状态。过期重传同一原件，投递失败不重做文件生成。失去平台续传条件时可重新上传，但消息发送仍守 delivery_unknown 规则。（落 `src/channel/qq/qq_media.*`：`QqMediaUploader::UploadFile`（upload_prepare→逐片 PUT（预签名 URL 自带鉴权，不带 QQ Authorization）→upload_part_finish（part_index 回显平台原值）→files 合并（srv_send_msg 恒 false）拿 file_info）；分片偏移按 parts 数组序（0 起官方与 1 起 SDK 两案同稳——负偏移进不了上传循环）；file_info 按 sha256 缓存（有界 512），ttl 窗内复用零网络、过期重传同一原件；上传恒串行（upload_config.concurrency 只记账不消费，防错误并发）。**分片续传不保**：每次上传全量分片，"已完成分片"不跨调用保持——大文件重启续传归后续+真机验证，如实记。发送链：`QqBotAdapter` Send 分支解析 file part→SenderLoop 上传前置（file_info 到手才 SendC2c，msg_type=7；Permanent 不自动重试，DeferredRetry 退避整装重试=缓存命中零网络）；`QqBotCapabilities` media 报真。MD5/SHA-1 内核（RFC1321/FIPS180-1 公有域写法）经 prepare 请求体钉标准向量。测试 `tests/unit/channelqq/test_qq_media.cpp` 八案。）
- [x] 回执必须区分 generated/uploaded/sent；上传到平台不等于对方已读。附件原件在未完成任务、审批或投递引用释放前不清理；下载、提取与发送均计入账号资源帽。（上传成功≠消息已发：两步分离（file_info 是上传产物，消息走 outbox 段照旧 sent/failed/delivery_unknown 合同，上传失败不重做文件生成=缓存+冻结原件）。三态账（generated/uploaded/sent）未单列——上传状态在上传器内部，回执族未扩键，如实记。附件原件不清理：媒体仓持久保留（保守；清理策略归后续）。资源帽：收发字节帽+附件数帽（4/信）已计；QPM/频次帽未计——归 Q3 真机/Q5 主动消息批。）

### 10.3 Q4 验收

- [ ] 手机发 TXT、图片、PDF/表格各一份：确认二进制落盘、解析出处、模型实际获得内容；单文件无正文、多个附件、引用附件均覆盖。（mock 面已落：泵册 "Q4 收件闭环"（文本附件→落仓→模型请求带净化名+2KiB 预览+存档路径，url token 不进 prompt）+媒体册（文本/二进制预览分界、多附件、数帽、白名单外拒、下载失败如实、幂等/重启复用）。图片/PDF 真机收件、引用附件（msg_elements）展开——归 Q3/后续批，如实未验。）
- [ ] 执行后收到生成文件，核 SHA-256；无正文文件回复可出站；超限、URL 失效、解析器缺失、伪 MIME、路径穿越均给准确状态。（mock 面已落：outbox 册（附件入箱冻结 sha256、纯附件段合法、原件丢失明败、重开投影）；泵册 "Q4 发件闭环"（超长回复→拆段→末段附件=冻结正文原件，盘上核内容与全文逐字节一致）；超限（下载帽/上传帽/附件数帽）、URL 失效（not_found/host 拒）、伪 MIME（白名单外 mime_not_allowed）、路径穿越（净化钉）均给稳定码。真平台收件核验归 Q3。）
- [ ] 0/1 起始分片、字符串大小、upload_config 嵌套、最后不足一片、大文件重启续传、file_info 过期全测。发送超时不重复生成产物，重复来信不重复跑模型。（媒体册+proto 册已钉：0 起与 1 起两案、file_size 字符串、upload_config 嵌套（顶层不认）、末片不足一块、file_info ttl 缓存与过期重传、PUT 无 QQ 头/签名不进日志；发送超时与产物解耦（delivery_unknown 合同既有）、重复来信模型恰 1 次（Q2 既有案）。大文件重启续传（跨调用保已完成分片）未做——本批全量分片重传，归后续+真机。）

## 十一、Q5：从 QQ 建单次提醒和周期任务

### 11.1 调度属于 LubanCode，QQ 负责投递

腾讯插件 [remind.ts](https://github.com/tencent-connect/openclaw-qqbot/blob/a730701d36aa7a070f98d4cba0f340f91f15e5f5/src/tools/remind.ts) 将提醒转为 OpenClaw cron 参数，包含时间/时区、isolated session、agentTurn、channel/to/accountId；工具还会返回调用 cron 的指引。它不能作为 LubanCode 调度实现。另一个 `cron-scheduler.ts` 是基于内存计时器的轻量路径，不作常驻耐久任务底座。

当前 [AutomationStore](../src/gateway/automation_store.hpp) 已有 once、run-now、幂等键、occurrence claim/bind/settle；周期表达式、时区、补跑、暂停/取消仍待补。[GatewayAutomationPump](../src/runtime/automation_pump.cpp) 可复用；不在 Node sidecar 另养定时器，不让模型写 jobs.jsonl。

### 11.2 从一句话落成一笔任务

- [x] 新增平台中立的 automation 创建/查询/修改/暂停/取消服务及工具。模型提供结构化意图，宿主验证并落 AutomationStore；QQ 菜单与其他渠道也调用此服务，不绕 shell 执行 CLI。（Q5 落 `src/runtime/channel_automation.*`：`ChannelAutomationBridge`（渠道上下文冻结 TurnScope + 归属闸 + 幂等键派生）与三枚工具 `create_reminder`/`list_reminders`/`cancel_reminder`（`RegisterChannelAutomationTools`，Gateway 装配注册，QQ 模板 allow 已列名——未配对 sender 进不了模型即拿不到工具；终端路/自动任务路无 TurnScope 一律 fail closed）。落账走 V2 store 合同（CreateJob/CancelJob，CAS+幂等）。**修改/暂停工具未落**（创建/查询/取消三面先齐，update/pause 沿既有域命令合同后续接）；QQ 菜单入口归 Q7。测试 `tests/unit/runtime/test_channel_job_pump.cpp` 全链+fail closed 案。）
- [x] job 新增 schedule(kind/at 或 cron/interval/tz)、动作类型、workspace、创建者、交付目标、预算、policy ceiling、revision；创建者与渠道目标取真实入站上下文。所有查询/修改核 job 归属，跨用户不泄露任务。（Q5 落 `AutomationJob`/`JobSpec` 增 owner 三元组（channel/account/sender 配对身份）与 delivery 三元组（到点结果投回创建会话）：`job.created` 行新增六键，旧账缺键落空=本地任务，行为不变；幂等比较含六字段（同键异归属=异规格 conflict）。归属闸在桥上（`OwnedBy`）：list 只看自己的、cancel 越权回 `channel_job.not_your_job` 且不泄露他人任务存在。schedule/revision/workspace 沿 V2 既有面。**动作类型（notify/agent_task 分型）、预算、policy ceiling 快照未落**——本批任务轮统一走 agent 执行链（见 11.2 第五行遗留）。）
- [x] “今晚八点”按冻结的接收时间和账号时区解析为具体日期。回复列完整日期、时间、时区和 jobId；若当晚时间已过或“每天整理一次”缺时间/范围，明确询问，不能静默改成明天或猜一个目录。（Q5 定案：自然语言时间解析归模型（at_ms UTC 毫秒或五字段 cron + 显式 IANA 时区）；宿主只验证——`time_in_past` 过期明拒（对照冻结的 received_at，不静默顺延）、坏 cron/坏时区/缺规格明拒、超十年视为算错要求核对。工具回执带 jobId/kind/timezone/nextFireMs（完整计划），模型据此向用户复述。“账号时区”暂无配置面，时区由模型显式递，缺省 UTC。）
- [x] 原始来信 ID + 稳定操作身份生成创建幂等键。写盘成功后才说“已设置”；同键异规格报 conflict。创建确认没发到，只补确认投递，不再创建一个 job。（Q5 落幂等键 `"chanjob-create:<ch>:<acct>:msg:<消息id>"`：同信重发经 ingress 去重+SessionService 台账不重跑（Q2 既有），同轮重调/桥面重调同键同载荷回原回执（duplicate），同键异规格 automation.revision_conflict 如实回给模型。CreateJob PowerLoss 落账成功工具才回 ok——“写盘成功后才说已设置”；确认回复的投递失败走 Q2 聊天回复链（outbox 冻结正文重投），job 已落账不重建。测试：同轮两次 create 只一笔+同信重放模型计数不多。）
- [ ] 区分 notify（固定提醒正文）与 agent_task（到点读资料、整理、生成结果）。纯提醒可直接将持久 payload 投递，不必再调一次模型；它须有独立 committed notification 来源，不能伪造 V3 assistant/reply selection。扩展 outbox 来源合同并保持旧回复兼容。（**Q5 本批未落**：任务轮统一走渠道会话执行链（到点执行→reply selection→outbox），纯提醒也过一次模型。notify 零模型直投+独立 notification 来源留待后续批次——须扩 outbox 来源合同，且 Q3 真机未验前主动投递面本身还是未验边界。）
- [x] 周期 task 默认每次新开隔离 V3 场，固定工作目录与显式输入；不自动带上 QQ 全部聊天史。所需附件用保留的 artifact 引用，不依赖数天后失效的 URL。（Q5 落 `ChannelWorkPump::MakeChannelJobSessionKey` = `channel:<ch>:<acct>:job:<jobId>`：任务自己的隔离渠道场（同 job 跨拍共享自己的上下文，重启经映射账 resume-as-new），不进聊天会话、不带聊天史；prompt 为建单时冻结的自包含规格（描述+会话/创建者上下文+“输出直接发给用户的正文”）。附件 artifact 引用归 Q4 媒件批次联动——本批任务 prompt 不引用附件 URL。）
- [x] 每次 slot 用计划时间发 occurrenceId=hash(jobId+revision+slot)，时区转换由宿主做。定义 DST 重复/不存在时间、时钟回拨、服务离线、重叠执行政策。建议离线后最多合并补一次，再排未来 slot；每种任务可显式配置 skip/coalesce，不默认补跑几百次。（V2 周期调度已全落（#83：`automation_schedule.*` 受限 cron/内置时区表/DST 两裁/misfire coalesce|skip/游标只前进/同 job 不重叠/队列帽），渠道任务直接复用同一只 store 与调度引擎，零第二套语义；渠道侧新增的只有认领分流 `ClaimDue(ClaimScope)`——渠道泵只认 `ChannelBackedOnly`，automation 泵只认 `LocalOnly`，各恢复面同理跳过对方的 occurrence。）
- [x] 取消/修改追加新 revision，未 claim 的旧 occurrence 作废；运行中取消走合作取消，不把已发生副作用说成撤回。执行前重验创建者授权及当前工具政策，只可收窄创建时上限。（Q5 落 cancel 工具（`CancelJob` CAS+幂等键 `chanjob-cancel:...`）：scheduled 就地结算 cancelled、claimed 走 cancel_requested 合作取消边界（V2 既有）；执行前重验落 `ChannelManager::ProbeRoute`（只读路由探针，同一只 ChannelRouter 纯函数）：配对/allowlist/绑定冲突现核，不过→occurrence 结算 `cancelled:creator_not_admitted:*`，任务保留（准入恢复后未来拍照跑）。**“只可收窄创建时上限”半面未落**——无创建时策略快照，任务轮 per_turn_tools 吃现时路由策略（可宽可窄），放宽不回卷留待后续；修改/暂停工具同第一行遗留。）

### 11.3 QQ 主动投递与回复窗口

当前 [单聊发送文档](https://bot.q.qq.com/wiki/develop/api-v2/autogen/api/v2_users_user_openid_messages.post.html) 区分被动回复、主动消息和 `is_wakeup` 互动召回；定时任务不能一直复用建单时 msg_id，也不能把 is_wakeup 当无限推送开关。

文档现列：主动发送方认证后 10 QPS，未认证 5 QPS 且 30 QPM；单关系 20 QPM、每好友每天 1000 条。互动召回另有 30 天内四个区间各一条。接口总频率帽又与主动频控分开。这些是本次文档值，不是本账号已获权限或长期保证，须配置化限速并记录平台返回。

**文档自身有矛盾：页首被动有效期写 60 分钟，msg_id 字段写 5 分钟。** 先以保守窗口安排回复，并将真实账号验证列为放行项；不对用户承诺“60 分钟一定能回”。回复次数现写每条 4 次，文件、说明和审批卡片须统一占用计数。

- [x] 主动投递使用已绑定的 QQ 账号/目标，不附陈旧 msg_id/event_id；建单时检查可投递条件，到点再验。`INTERACTION_CREATE` 的授权事件含 c2c_push/group_push，记录平台授权线索；平台授权与 LubanCode 配对、工具授权仍各管一层。（Q5 落：交付目标三元组建单时冻结（投回创建会话，不投当前别的会话）；outbox 段入箱时 `reply_to_message_id` 恒空——发送锚由泵按“该会话最近一来信是否在窗内”现取（`FreshInboundAnchor`，窗 `passive_reply_window_ms` 缺省 4 分钟=5 分钟口径下再留余量），窗外不带锚走主动消息，不拿陈旧 msg_id 冒充被动回复。“到点再验”=ProbeRoute 准入核+发送分型。**建单时可投递条件检查未落**（主动消息真权限归 Q3 实测，本批只如实分型不预判）；`INTERACTION_CREATE` 授权线索未落（归 Q3/Q6 互动面）。）
- [x] 识别 40034100（主动频控）、40034105（无权限）、40054004（无好友）、40054013（拒收）、40034122（召回区间额度）、304103/40034005（过期回复）等文档错误。限频延后、明确拒收暂停此目标，执行结果保留供用户查询。（Q5 落渠道任务段的分型政策（`ApplyDeliveryOutcomes`/`DriveChannelDeliveries`）：限频族（主动额度受限）与 reply_window_expired → **挂起待互动**（`await_interaction_`，不硬发不谎报：账上留 sending 非终态，下一封来信进窗后锚定新 msg_id 补投，同 delivery 同载荷）；明确拒收/令牌失效维持终态失败留账（不换账号不重跑）。具体平台码→域名的映射沿 Q1 qq_messages/qq_proto 既有域名表（本批不碰）。**结果可查**：outbox 账+`list_reminders`（jobId/状态/下次触发）；投递态与执行态分账显示，list 工具尚未 join outbox 投递态（留待 Q7“查看任务”面板一并做）。真限频真值/主动额度归 Q3——本地 mock 不能代替。）
- [x] 不因 QQ 拒收重跑 Agent，也不自行换账号、群或平台。状态分别显示 schedule/execution/delivery；菜单“查看任务”可查结果，不能把投递失败伪装为提醒完成。（Q5 落：投递失败/挂起不改 occurrence 结算（执行事实 succeeded=“执行完成且已交投递账”，投递态在 outbox item——state/delivery_error/attempts 全留账）；补投重试同 delivery_id 同载荷（msg_seq 稳定），零模型重跑（测试钉：模型计数恒定）。状态分账：schedule=store（游标/occurrence）、execution=occurrence outcome、delivery=outbox item state。菜单入口归 Q7；`gateway status` 渠道栏（Q2）已带投递错误稳定码。）
- [x] 将原第七节“过期不转主动消息”限定为普通被动回复：Q5 中用户明确授权的定时交付有独立主动目标，沿专门投递政策；两条路径不可互相偷换。（Q5 落：普通聊天回复（source=ingress:*）维持 Q2 规矩——reply_window_expired 终态失败不转主动；渠道任务段（source=chanjob:*）走独立政策——窗外默认主动、受限/过期挂起待互动补投。两条路径按 source_ref 前缀分型写死，互不偷换；测试各钉一案。）

### 11.4 Q5 验收

- [ ] 今晚指定时间提醒、每天指定时间整理目录；确认时间和 jobId，重发建单只一笔，修改/暂停/取消可查询。（e2e 假渠道+假模型已钉：`tests/unit/runtime/test_channel_job_pump.cpp` “QQ 建周期提醒全链”（cron+Asia/Shanghai+jobId+归属+隔离场+投回原会话）/“同信重发/同轮重调不双建”/“越权取消拒”。**修改/暂停未落**（工具面遗留）；真机验收归 Q3。）
- [ ] 重启跨过 due、跨午夜、DST、时钟回拨、任务重叠时 slot 不重复；附件数天后仍可读；纯提醒零模型调用。（slot/DST/回拨/重叠由 V2 调度引擎与 store 测试钉（#83，`test_automation_store_v2.cpp`/`test_automation_schedule.cpp`）；渠道面钉了 claim 后崩重派不双跑（`test_channel_job_pump.cpp` “claim 后崩”案）。附件归 Q4；**纯提醒零模型未落**（notify 分型遗留）。真机验收归 Q3。）
- [ ] 平台拒收/限频时任务不重做，结果可查；撤销配对/工具权限后不能继续越权执行。真实 QQ 接收延迟与主动消息权限单独实测。（e2e 钉：限频挂起→互动补投（模型计数不多）/窗口过期挂起不硬发/撤销准入→occurrence cancelled 零模型（ProbeRoute 重验）。**真平台主动发送、真实回复窗口、主动消息权限、接收延迟全部归 Q3 真机未验**——本地 mock 不能代替这些结论。）

### 11.5 Q5 批次记账（2026-09-15）

**落地符号**：`src/runtime/channel_automation.hpp/cpp`（`ChannelAutomationBridge`/`TurnScope`/`RegisterChannelAutomationTools`/`kChannel*ReminderTool`）；`src/runtime/channel_work_pump.*`（`MakeChannelJobSessionKey`/`SweepChannelJobRecovery`/`RecoverChannelJobOccurrence`/`RunOneChannelJob`/`FreshInboundAnchor`，Options 增 `automation_store`/`automation_bridge`/`passive_reply_window_ms`，`await_interaction_`/`recent_inbound_` 补投账）；`src/gateway/automation_store.*`（`AutomationJob`/`JobSpec` owner/delivery 六字段、`ClaimScope` 认领分流、幂等比较含归属）；`src/channel/manager.*`（`ProbeRoute` 只读路由探针）；`src/runtime/automation_pump.cpp`（候选/恢复面跳过 ChannelBacked，`ClaimDue(LocalOnly)`）；`src/app/gateway_launch.cpp`（桥装配+工具注册+store 借用）；`src/channel/channel_config.cpp`（QQ 模板 allow + 三枚任务工具名）；冻结件同步：configuration.md §7 模板样例与名单说明、security.md 受保护路径闸段。

**测试**：新册 `tests/unit/runtime/test_channel_job_pump.cpp`（八案：周期全链/同信幂等/越权归属/额度受限挂起补投/窗口过期分型/claim 后崩重派/工具面 fail closed/撤销准入重验）；`tests/unit/gateway/test_automation_store_v2.cpp` 增三案（归属落账重放/认领分流/Any 默认面）；`tests/unit/channel/test_channel_config.cpp`+`test_channel_router.cpp` 模板名单更新。

**未验边界（如实记，归 Q3 真机）**：真平台主动消息权限与额度（40034100 族真值）、被动回复窗口真值（文档 60min/5min 矛盾，代码按保守 4min 窗）、主动消息触达率与延迟、`INTERACTION_CREATE` 授权事件形状。挂起补投的“互动解锁”在进程重启后丢失（`await_interaction_` 进程内）——重启后主动重试一次、再拒再挂，有界不刷屏，如实记。

**遗留（后续批次）**：notify/agent_task 分型与纯提醒零模型直投（须扩 outbox 来源合同）；修改/暂停工具；创建时策略快照（“只可收窄”半面）；list 工具 join 投递态；QQ 菜单“查看任务”入口（Q7）；群聊建单（交付面 Direct-only，工具已明拒）。

**PR/CI**：[PR #94](https://github.com/relic-yuexi/LubanCode/pull/94)（基线 19143af0）；CI 头提交活跃 suite rollup=success——macos-clang 绿，run [34958181654](https://github.com/relic-yuexi/LubanCode/actions/runs/34958181654)（windows/linux 腿按 PR 快车道设计不跑，合并后全量复验）。

## 十二、Q6：QQ 按钮批准一次工具调用

### 12.1 可复用的两端

QQ 在 Markdown 消息的 keyboard 中放 `action.type=1` 回调按钮，点击触发 `INTERACTION_CREATE`。从 d.id 取互动 ID，调用 `PUT /interactions/{interaction_id}` 结束客户端等待；外层事件 ID、互动 ID、原消息 ID 不可混用。[消息按钮](https://bot.q.qq.com/wiki/develop/api-v2/server-inter/message/trans/msg-btn.html)、[互动事件](https://bot.q.qq.com/wiki/develop/api-v2/autogen/event/interaction_create.html)、[回应接口](https://bot.q.qq.com/wiki/develop/api-v2/autogen/api/interactions_interaction_id.put.html)

官方按钮页注明模板需申请、自定义按钮需开通；是否对当前账号开放须实测。`permission.type=0` 为指定用户，1 为管理员，2 为所有人。SDK/插件类型注释可能不同，不能照注释猜权限；平台 UI 限制也不能替代宿主身份复核。click_limit 已标废弃，按钮变灰也不是防重复执行凭证。

腾讯 [approval-handler.ts](https://github.com/tencent-connect/openclaw-qqbot/blob/a730701d36aa7a070f98d4cba0f340f91f15e5f5/src/features/approval-handler.ts) 展示了 keyboard 与 OpenClaw 审批 RPC 接线；其短 ID、allow-always 和账号 allowFrom 策略不直接移植。

LubanCode 已有 [ApprovalRequest / InteractionDecision](../src/runtime/interaction.hpp)、[InteractionBroker](../src/runtime/interaction_broker.hpp) 和 [on_tool_confirm_async](../src/agent/loop.hpp)。future 等待阻塞执行工作线程，不该堵事件泵；[App Server 交互实现](../src/app_server/interaction.cpp) 可供生命周期参考，渠道宿主不反向依赖 App Server。

### 12.2 宿主裁决，平台只传按钮和身份

- [ ] 新增平台中立 ChannelInteractionBroker（拟 `src/runtime/channel_interaction_broker.*`），复用现有四态和 future。QQ/飞书映射各自卡片，不各造审批引擎。
- [ ] 新增显式 interaction capability 和 Bridge 交互请求/响应合同。当前严格 schema 没有通用审批回调口与 keyboard 类型，须版本化/协商并给旧适配器降级；不要塞普通聊天文本让模型解释“同意”。
- [ ] 扩展为“可申请审批”的工具上限：工具须在本机允许询问的范围内，hard deny 不可被按钮覆盖。Q0 的最小工具表不能先隐藏工具，再期待模型申请它；新审批政策也不能把所有 shell 默认加回。
- [ ] request_id 由宿主分配；pending 表绑定 channel/account/operator/conversation/workspace/session/turn/tool_use_id、规范参数 hash、policy revision、期限和 ownerEpoch。button_data 只放随机 opaque token 与决定码，服务端映射完整身份；不使用前八位 ID，不把命令或密钥塞按钮数据。
- [ ] 卡片展示经脱敏的操作摘要、影响路径和参数差异。参数/文件内容/工作目录发生变化须重新请求批准；对写文件按待写内容 hash，对命令按 argv/cwd 与执行政策核对。审批通过仍过沙箱和权限上限。
- [ ] 第一批只显示“允许这次”“拒绝”。未来若开放 AcceptForSession，文案写“本会话允许”，按现有内存会话语义处理，不能叫“永久允许”或修改全局配置。
- [ ] 校验 QQ application_id、实际连接账号、场景及操作者 OpenID。单聊取 user_openid；以后群聊取 group_member_openid，不拿群 OpenID 当人。批准者须属于该请求授权集合，并在处理时仍有权限。
- [ ] 回调经短路径验证、去重、落决策，再及时回应 QQ；code=0 表示回调处理成功，不表示工具执行成功。重复互动/重复 token 只返回已处理；未知、过期、跨账号或篡改请求不唤醒 future。回应接口每个互动 ID 只能一次，响应重试也要记状态。
- [ ] Broker 等待期间 Bridge、QQ 心跳、控制命令与超时仍运转。Gateway 当前同步 tick 执行路径要拆开工作线程/事件推进，不能等按钮的线程恰好也是唯一收按钮线程。
- [ ] 超时、取消、权限撤销、Gateway 关闭走 Cancel，文案写未获答复；不冒充用户点拒绝。重启后旧 future 不复活：原请求作废并保留审计；只有核实工具未执行且安全恢复后，才能为新执行尝试重新申请。
- [ ] 审批控制消息走有期限的交互 outbox，和已完成 Agent 回复分开；卡片发送失败就取消等待，不因重试卡片突破审批 TTL。卡片无权限时保持 fail closed，用户可回本机处理。
- [ ] 决策与真实工具 Action 关联进 V3 审计。已批准/未执行、执行中崩溃、执行完回执未发三窗分别恢复，未知副作用停审，不借重新点按钮盲重跑。

### 12.3 Q6 验收

- [ ] 手机允许一次只执行一次；拒绝/超时/取消不执行。重复点、他人点、转发卡片、跨账号 token、参数变化、过期和重启后点旧卡片均拒绝。
- [ ] 等待审批时还能收新消息、响应 stop、保持连接。无 keyboard 权限时给准确失败，不能自动改成允许。
- [ ] 终端同步确认、App Server 四态、子代理/PTC 权限原有测试回归；hard deny 不能经远端批准突破。

## 十三、Q7：自定义菜单与指令面板

用户提供的 [菜单面板入口](https://bot.q.qq.com/wiki/develop/api-v2/server-inter/menu-panel/) 已读取。它适合做“帮助、文件处理说明、查看任务、待审批”入口，不承担定时器、文件解析或权限判断。

| 能力 | 官方接口与适用范围 | LubanCode 用法 |
| --- | --- | --- |
| 全局自定义菜单 | GET/PUT `/v2/menu`；仅 C2C，全用户共用；PUT 覆盖整份菜单 | 只放不含私有信息的帮助与常用入口 |
| 指令面板 | GET/POST `/v2/panels`，GET/PUT/DELETE `/v2/panels/{panel_id}` | 创建 LubanCode 面板，保存返回 panel_id，随后更新原件 |
| 面板目标 | PUT `/v2/panels/{panel_id}/target`，op=add/del | c2c/group 可按用户/群关联；channel/dm 只支持 all |

依据：[菜单更新](https://bot.q.qq.com/wiki/develop/api-v2/autogen/api/v2_menu.put.html)、[面板创建](https://bot.q.qq.com/wiki/develop/api-v2/autogen/api/v2_panels.post.html)、[目标更新](https://bot.q.qq.com/wiki/develop/api-v2/autogen/api/v2_panels_panel_id_target.put.html)。文档现列菜单最多 10 项、子菜单最多 5 项且不再嵌套；每 bot 最多 20 面板、每面板最多 20 元素，目标单批最多 20 个。名称长度按平台字符规则校验，不能按 JS 字符串长度猜中文限额。

**两类动作必须区分：** menu.send_message 与 panel.command 按文档是把文字填入输入框；用户发送后才成为聊天指令。旧式快捷菜单还可产生 type=12、feature_id 互动；消息内审批按钮是 type=11、button_data。分别解码，不能假定所有菜单点击都会立即执行请求。

- [ ] 新增 QQ menu-panel 模块与本地配置发布入口；安装或普通启动不自动覆盖用户在开放平台配好的菜单。显式启用后先读远端配置、展示差异、保存版本/摘要，再 PUT；不反复创建面板耗尽额度。
- [ ] 全局菜单建议“帮助”“文件”“任务”“审批”。“文件”提示从 QQ 原生附件入口发送；当前菜单接口未提供通用文件选择器。按钮 anchor=1 只记作特定手机单聊选图能力，不能宣传可任意选 PDF/ZIP。
- [ ] c2c specific 面板可关联已配对用户，撤销时移除；面板可见和 only_admin 都不是宿主授权。用户直接手敲同名命令仍走相同身份/任务归属检查。
- [ ] “查看任务”“暂停任务”“待审批”等控制命令在宿主识别后调用中立服务；只查当前主体有权查看的项目。菜单不展示私有路径、任务名或凭据，不在全局菜单塞个人审批 token。
- [ ] 菜单 switch 的 ext 标记只用于用户偏好，不可改变工具授权/owner/全局模型。状态缺失或客户端不支持时用明确默认，不从一次 ext 消失推断权限撤销或授予。
- [ ] 面板发布独立限速与重试：菜单修改文档为 5 QPM，创建面板 10 QPM，目标更新 60 QPM。GET/PUT 版本冲突不覆盖后台人工修改，给本地可见提示。
- [ ] 把菜单/面板作为可选平台能力；飞书日后用自己的菜单/卡片调用同一任务和审批服务，不向 C++ 通用 schema 塞 QQ 原始 panel JSON。

Q7 验收：手机/桌面分别查填入与发送行为；未配对用户虽见全局菜单仍不能执行；指定面板增删目标、全局覆盖冲突、重启不重复创建、配额错误都测。Q7 帮助入口可提前做，任务/审批入口只有 Q5/Q6 可用后才发布。

## 十四、补充调研证据与交付顺序

本次证据分三档：QQ 在线官方页给协议，腾讯插件/SDK 给参考实现，LubanCode 工作区给已有接线。网页正文通过 HTTPS 读取并提取 main；旧 send-receive 链接已返回“启动接入”页，故改用当前 autogen 接口链接，没有拿跳转页冒充 API 文档。官方页内部及 SDK 的差异均已记入上文，不据此宣称线上某一行为必然成功。

- SDK 来源：[qqbot-nodejs 1.0.4](https://www.npmjs.com/package/@tencent-connect/qqbot-nodejs/v/1.0.4)，核读包内 `src/protocol/api/media.ts`、`media-chunked.ts`、`messages.ts`、`types.ts`。不把本机 Temp 路径作为团队唯一证据入口。
- 文件参考：[腾讯附件中间件](https://github.com/tencent-connect/openclaw-qqbot/blob/a730701d36aa7a070f98d4cba0f340f91f15e5f5/src/middleware/attachment.ts)；下载、语音转录、图片路径与普通附件处理分支均存在，具体能力不直接等同 LubanCode 能力。
- 调度与审批来源分别见第十一、十二节；源码符号、接口路径、已知差异与可执行验收已列明。

建议顺序：Q0—Q3 文字链 → Q4 文件闭环 → Q5 单次提醒再周期任务 → Q6 远端审批。Q7 菜单壳按依赖插入，已实现哪些功能才发布哪些入口。每批保留旧功能回归；发布说明分“文档/fixture 通过”和“真实 QQ 账号通过”。

- [ ] README 后续补手机操作示例、文件格式表、时间/时区确认、拒收后的结果查询与审批超时说明；Q4—Q7 未验收前继续标待实现。
- [ ] 真实账号测试须使用明确授权的收件人；本次只调研公开文档与源码，没有配置菜单、创建定时任务、发文件或发送审批消息。

## 十五、SDK 可选，允许直接实现官方协议

> **2026-09-14 Q1 定案（本节三选一，随 Q1 PR 落地）**：取第一案——**原生 C++ 直调官方 API，进程内直连**。QQ 适配器编进宿主 `lubancode` 可执行（`src/channel/qq/`），无 Node、无 sidecar 子进程、无渠道包。
>
> 协议事实依据（官方 bot.q.qq.com v2 文档，本次逐页核读）：[通用 payload/opcode/intents 表](https://bot.q.qq.com/wiki/develop/api-v2/dev-prepare/event-emit/payload.html)（op 0/1/2/6/7/9/10/11 与 intents 位表）、[单聊消息事件](https://bot.q.qq.com/wiki/develop/api-v2/autogen/event/c2c_message_create.html)（事件体/User/MessageScene/附件三例官方示例）、[发送单聊消息](https://bot.q.qq.com/wiki/develop/api-v2/autogen/api/v2_users_user_openid_messages.post.html)（msg_type/msg_id/msg_seq 合同与全量错误码表）。Q1 协议面（token、网关事件、v2 发送）官方文档全覆盖；腾讯 SDK 只作交叉参考，不进依赖。
>
> 代价对表：
>
> | 维度 | 原生 C++ 进程内（定案） | Node 直连 | Node + 腾讯 SDK |
> | --- | --- | --- | --- |
> | 新增依赖 | mbedTLS 3.6.x（FetchContent 自建，三平台同源码同编译） | Node 运行时 + npm 依赖树 | 同左 + SDK 锁版本（1.0.4 已见与官方文档冲突） |
> | 进程面 | 零新进程：无 spawn、无进程树收尾、无 stderr 排水、无 stdio 帧传输 | 每账号一只受管子进程 | 同左 |
> | 断线/重放可控性 | Identify/Resume/心跳/退避全显式状态机自测 | 同等工作量自行实现 | SDK 隐式行为，resume 语义需拆 SDK 源码核对 |
> | 维护面 | 与宿主同构建同测试同 CI；协议 fixture 进 tests/ | 两套构建、两套测试路 | 同左 + SDK 版本漂移 |
>
> Bridge 隔离语义照"保持一致的部分"执行：QQ 适配器实现 `ChannelBridgeTransport` 字节面（进程内对象调用替代子进程 stdio，`ChannelManager`/ingress/pairing/路由零改动）；能力声明内置（websocket/direct/text/send/credentials），不虚报媒体与 streaming；宿主权限/任务/会话/outbox 不变。凭据交付：进程内直连——**凭据不出宿主进程**（configuration.md §4 中性措辞兑现），无专用启动管道，`SidecarEnvAllowlist` 不消费。包信任闸退化为"渠道实现内置受信"（activation.hpp 注脚兑现：装配侧恒信任，不造假包概念）。
>
> 传输库选型：**TLS WebSocket 客户端 = 自实现 RFC 6455 客户端子集（帧编解码/握手/心跳/close）+ mbedTLS（TLS 底座）**。否掉 ixwebsocket：维护者明言投入减少；上游 CI 的 Windows/UWP 腿直接禁用 TLS（自述因 vcpkg 时长），mbedTLS 后端仅 macOS 有 CI——Windows+TLS 恰是零上游保障组合，而本仓合并后必跑 Windows 腿；OpenSSL 系统供给在 Windows FetchContent 回退路无解。否掉 libwebsockets（lws）：事件循环线程模型集成面更大，mbedTLS 后端组合同样无上游 Windows CI 保障。否掉 libcurl WS（官方仍标实验性，本单明令不赌）与 Boost.Beast/µWebSockets（拉进大依赖）。mbedTLS：ARM 维护、纯 C 自包含、CMake 原生、无 perl/无系统 OpenSSL 供给要求，三平台一份源码，符合"跨平台血统硬、集成面最小"。HTTP 面（token、发消息）Q1 复用现有 cpr 底座（`src/net/http_transport.hpp`，GET/POST 即足，不动 Provider 请求路径）；受控 PUT/DELETE/流式上传属 Q4 媒体批次，不在 Q1 提前扩底盘。CI 风险如实记：PR 腿只跑 macos，Windows/Linux 编译验证靠合并后全量复验——选型以"三平台同源码、零系统依赖"压这个险。

| 方案 | 依赖与代价 | 保持一致的部分 |
| --- | --- | --- |
| 原生 C++ 适配进程直调 API | 无 Node；自行实现事件连接、令牌刷新、上传与重试，按平台发布二进制 | manifest、Bridge、宿主权限/任务/会话/outbox |
| Node 适配进程直调 API | 仍需 Node；不用腾讯 SDK，但自行处理协议 | 同上 |
| Node + 腾讯 SDK | 少写连接代码；需固定版本、核对协议差异 | 同上 |

不用 SDK 不等于不用成熟 HTTP/TLS/WebSocket 库。当前 [HTTP 传输底座](../src/net/http_transport.hpp) 是完整响应的 GET/POST 路径，可供鉴权与小 JSON 请求复用评估；文件流、PUT/DELETE 和 WebSocket 不能假定已支持。此轮在 platform/net 与构建依赖中未找到现成 QQ WebSocket 客户端，实施前需选定传输库并验证平台构建选项。

- [x] 原生适配器分 auth、gateway-events、messages、spool 模块（media-upload/interactions/menu-panel 分归 Q4/Q6/Q7 批次）。令牌刷新单飞、到期重取、错误码分型、限速、心跳和重连状态显式测试，不依赖 SDK 隐式行为。（Q1 落 `src/channel/qq/`：qq_auth（单飞=锁内刷新后来者等结果，到期=expires_in-300s 余量，`test_qq_auth_messages.cpp` "未到期不重取;到期现刷;Invalidate 强制现刷"）、qq_gateway（状态机 state_name 显式观测：idle/connecting/authenticating/running/backoff/stopped；心跳锚定绝对时刻不受事件流重置；退避阶梯 1s..60s+10% jitter，稳定归零）、qq_messages（限频/5xx DeferredRetry 退避三次、Deduped 收账、401 刷 token 重试一次、稳定 msg_seq）——显式测试见 `test_qq_auth_messages.cpp`/`test_qq_gateway.cpp`。）
- [x] WebSocket 实现 Identify/Intents、Heartbeat/ACK、seq/session_id、Resume/失效重建、断线退避和取消（Q1：`qq_gateway` + `ws_frame`/`qq_ws_client`。Resume 携带 session_id+seq；op9 d=false 清 session 重 Identify；op7 保 session 重连；心跳连续 missed_ack_limit 次 ACK 断线；`CancelInFlight` 从停止方打断在途连接。当前账号是否获准 websocket 接收——归 Q3 真账号实测；Webhook 路线未选，不涉）。测试 `test_qq_gateway.cpp` 六案 + `test_qq_ws_frame.cpp`/`test_qq_ws_client.cpp` 帧级全案。
- [ ] HTTP 边界增加受控 PUT/DELETE、流式文件读写、大小帽和取消；避免为了大文件把整件读入内存。复用 libcurl 等底座时不改现有 Provider 请求路径，单独跑原有 HTTP/Provider 回归。（Q1 只需 GET/POST（token、gateway、v2 发送），复用 `src/net/http_transport` 现有合同面零改动、零新回归面；Provider 请求路径未动。**Q4 扩了受控 PUT**：`http_transport.cpp` 的 method 分派加 "PUT"（GET/POST 原路径零变化；分片 PUT 到 COS 预签名 URL 用）+`qq_http.cpp` 增 `MakeMediaHttpFunc`（响应帽可配，媒体下载/上传走它，信令路 MakeDefaultHttpFunc 不动）。DELETE/流式读写仍缺——本批产物帽 20 MiB 整件入内存可承受，流式归真需要时再扩。原 HTTP/Provider 回归交 CI。）
- [x] 将官方 API 示例固化为协议 fixture；第十、十一节记录的 SDK/文档冲突保留为测试案例。SDK 即使不进入依赖，也可用于交叉检查；测试通过不等于真实平台联调通过。（Q1 fixture：官方 payload/opcode/intents 表、C2C 事件三例（文本/ARK 卡片/引用）、发送请求响应两例、错误码表逐枚（`test_qq_proto.cpp`）。文档与 SDK 冲突案（§十 富媒体分片起点等）属 Q4 范围的媒体路径，届时落测试；本批 C2C/发送/网关面无已知冲突。）
- [x] README 按最终 runtime 写安装要求，原生实现不得继续要求 Node。（channels/README §6 新增"QQ 安装要求"：零额外安装，适配器随宿主编译，mbedTLS 静态链，用户只配 config.json；飞书等后续渠道独立选型，宿主不绑定。）
