# 宽窄转换异常掐死会话:"No mapping for the Unicode character exists in the target multi-byte code page."

状态：待做（2026-08-17 用户真机,D:\books\CLRS 问 LIS nlogn,glm-5.2 anthropic wire)

## 现场

模型答到一半(正文含中文注释的代码块已正常上屏),忽然:

```text
[错误] 未预料的异常: No mapping for the Unicode character exists in the target multi-byte code page.
```

顶层 catch(cli_app.cpp:541)接住即整场退出。这条消息是 Win32 **ERROR_NO_UNICODE_TRANSLATION(1113)** 的官方文案——某处 WideCharToMultiByte 转换失败,异常带着 GetLastError 文本一路穿透到顶层。

## 已排除的嫌疑人(静态排查过)

- `platform::WideToUtf8`(console_win.cpp:76、paths_win.cpp:88):失败返回空串,**不抛**。
- 终端标题:窄 OSC 转义,不走宽字符。
- 清行/光标:FillConsoleOutputCharacterW(L' ')/SetConsoleCursorPosition,无文本转换。
- 工具链未跑(纯文本问答),工具侧清洗不涉。

## 剩余嫌疑(按可能性排,须逐一证实/排除)

1. **模型流里混进非法 UTF-8 或孤立代理对**——glm 中转分块把多字节序列劈两半、或上游真送了坏字节;下游某处先 `Utf8ToWide` 再回宽→窄(或 std::filesystem/STL 内部走 ACP 转换)时炸。查:SSE 分帧与 delta 拼装是否保证多字节序列不被拦腰斩;`api/assembler` 拼正文时有没有逐块过 `IsValidUtf8`。
2. **std::filesystem 的 ACP 转换**——MSVC STL 的 `path::string()`(窄、走当前代码页)在 GBK 机器上遇 emoji/生僻字符会抛 system_error 且文案正是这条。grep 全仓 `\.string\(\)` 用在 filesystem::path 上的点,逐个换 `PathToUtf8`(u8string)。
3. **STL 内部 cout/locale**——控制台 65001 + 坏字节写屏失败被某层包装成异常。复现实验:向真 conhost 喂一段被劈半的 UTF-8,看 std::cout 状态与异常路径。

## 要做

### 一、先复现

- 最小 repro:起真控制台会话,glm/中转端流式喂一段含四字节 emoji 且在块边界劈开的正文,看是否命中同一异常;不中再按嫌疑 2 做 path::string() 的 GBK 复现(emoji 路径)。
- 复现不了就加诊断:顶层 catch 打异常 type 与 `type_info::name`,再跑真机会话抓现场。

### 二、修法(按证实的结果落,可多管齐下)

1. **wire 边界清洗**:SSE 拼装的 text/thinking delta 在进显示与 history 前,劈半的多字节序列留在这块缓冲、拼齐再放行(流式分帧器天然该做的账);彻底非法字节过 `SanitizeUtf8`(已有,别再造一份)。
2. **path::string() 全面清剿**:全仓 grep,凡 `std::filesystem::path` 要落 UTF-8 文本/日志/请求的一律 `PathToUtf8`;确需 ACP(调 Win32 API 的窄接口)处写明注释并只限 Win32 边界。
3. **宽窄转换不许抛**:`platform::WideToUtf8`/`Utf8ToWide` 遇坏序列替换 U+FFFD 不失败(目标本就是 UTF-8,宽串里孤立代理对替换掉即可)。
4. **顶层兜底分级**:交互会话的顶层 catch 不再"打印即退"——正文已流了一半的回合,异常转成该回合的 error 收口(历史保住、prompt 回来),只有 truly fatal(内存/启动期)才退进程;退出前把已流正文与 history 落盘。

### 三、验收

- repro 用例(劈半 emoji / emoji 路径 / 坏字节)全部不再炸:要么正确显示,要么 U+FFFD 替换,回合活着,prompt 回来。
- `grep -rn '\.string()' src | grep path` 清零(或仅剩带注释的 Win32 边界)。
- 交互会话中任何 std::exception:回合收口不退进程,session JSONL 完整,`/resume` 能接回。
- 既有 UTF-8 清洗/边界测试全绿,新增劈半流/emoji 路径/顶层兜底三组回归。

## 不做

- 不把锅甩给"模型输出怪"就完事——客户端见了坏输入必须活着。
- 不借机重写 SSE 分帧之外的东西。
- 不加全局 try/catch 每层包一遍——边界四处(wire 边界/path 边界/转换函数/顶层)各一道,够了。
