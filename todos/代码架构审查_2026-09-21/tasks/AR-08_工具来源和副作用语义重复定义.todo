# AR-08 工具来源和副作用语义重复定义

- 状态：修复中；共同合同已下沉(tool_semantics.hpp),loop 三段同义映射已删,待 PR/CI 验收。
- 审查基线：`693d3ddb5607fc67f37c262b8b8873c9f7dc07eb`。
- 主批次：第1批（1=无行为变更合并；2=接口/合同收敛；3=职责拆分）。修复已证错误可在机械合并就绪后优先，不必等所有低风险清理。

## 问题表

| 编号 | 类别(A/B/C) | 严重度 | 位置(文件:行) | 现象（最短代码证据） | 为什么是问题 | 建议方案 | 影响面与风险 |
|---|---|---|---|---|---|---|---|
| AR-08 | A | 中 | src/tools/registry.hpp:23；src/tools/tool.hpp:22；src/agent/tool_trace.hpp:40；src/agent/tool_trace.hpp:47；src/agent/loop.cpp:411；src/agent/loop.cpp:424；src/agent/loop.cpp:435 | tools与agent各有同值域ToolSourceKind/EffectClass；loop再写来源映射和两份EffectClass映射。 | 新增来源或恢复等级须改两份enum、序列化表及多份映射。业务语义明写一一对应，却用运行分支维持同步；漏映射会停在默认Builtin/InProcessUnknown，诊断与恢复建议失真。当前未发现已有枚举值错配，报的是已证重复结构。 | 把来源及副作用等级移到无agent/tools依赖的共同领域合同头；两命名空间短期using兼容，trace保留现有字符串编解码及落盘标签。loop按值传递，删逐项同义转换。PlanToolOrigin含Unknown且缺Deferred，须另保显式投影。 | 影响工具注册、trace、恢复判词与插件来源显示；线上字符串不能改，不能按enum数值序号迁移历史。 |

## 证据与边界

tools/tool.hpp:19-21说明复制是为避免tools依赖agent。正确收口方向是下沉共同合同，不能令tools include更重的agent/tool_trace.hpp。对于原意不同的PlanToolOrigin只统一映射入口，不强转。

代码证据来自当前源码静态核对；没有运行复现、编译或测试。上文后果是对应路径的推导，未声称观察到生产事故。

## 交接范围

- 负责文件：`src/tools/tool.hpp`；`src/tools/registry.hpp`；`src/agent/tool_trace.hpp`；`src/agent/tool_trace.cpp`；`src/agent/loop.cpp`。
- 前置：无硬前置；共改文件须协调。（以全局排期复核为准）。
- 不得并行共改：本组其余任务无同文件写入冲突。
- 可并行：除上述依赖/共改任务外，其余审查任务可单独worktree推进；不要借本单批量格式化或重写无关代码。

## 迁移与验收

1. 固定本单触及合同与行为样例；对照当前源码重新确认行号。
2. 把来源及副作用等级移到无agent/tools依赖的共同领域合同头；两命名空间短期using兼容，trace保留现有字符串编解码及落盘标签。loop按值传递，删逐项同义转换。PlanToolOrigin含Unknown且缺Deferred，须另保显式投影。
3. 小批提交，按下列门槛验收。

- [ ] 逐个enum值往返现有持久化字符串。
- [ ] 未知旧字符串仍按原失败策略。
- [ ] 注册元数据及工具fallback产生同样trace。
- [ ] 公共合同不得include runtime/app。
- [ ] GitHub CI。

## 删除量与旧账

预计净删40行：重复enum约12行，加loop三段同义映射约30行，减using及直接赋值约4行；编解码主体保留。

当前 todos 未找到直接承接此缺口的专单。
## 全局排期复核

- 前置／文件接手次序：无硬前置；共改文件须协调。
- 总排期：[三批重构与并行交接](../07_三批重构与并行交接.md)。本节含跨组复核结果，优先于早期本组可并行提示。
- 当前清单未见同文件冲突；新增公共头或CMake源登记先通知集成人。
