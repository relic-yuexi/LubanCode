# AR-07 审批与Plan重复实现shell词法

- 状态：已修。PR #196（worktree-agent-a62bd4c167241e4c1），CI run 35657311304 rollup 绿（首轮唯一失败为 unit.app_server.app_server_parallel_read 时序 flaky，与本单无关，--failed 重跑即绿；windows-msvc/macos-clang/linux-asan 全腿 success）。
- 审查基线：`693d3ddb5607fc67f37c262b8b8873c9f7dc07eb`；修复基线：origin/main 含 merge PR #194（3eb7651f）。
- 主批次：第1批（1=无行为变更合并；2=接口/合同收敛；3=职责拆分）。修复已证错误可在机械合并就绪后优先，不必等所有低风险清理。

## 问题表

| 编号 | 类别(A/B/C) | 严重度 | 位置(文件:行) | 现象（最短代码证据） | 为什么是问题 | 建议方案 | 影响面与风险 |
|---|---|---|---|---|---|---|---|
| AR-07 | A | 中 | src/tools/command_safety.cpp:48；src/tools/command_safety.cpp:79；src/tools/command_safety.cpp:101；src/tools/command_safety.cpp:116；src/runtime/plan_mode.cpp:32；src/runtime/plan_mode.cpp:61；src/runtime/plan_mode.cpp:82；src/runtime/plan_mode.cpp:102 | 两处分别实现拆段、引号、重定向、子表达式、拆词及可执行名归一；只有函数名及注释不同。 | 同一种shell引号或转义规则变更须同步两套解析。审批与Plan各自消费重复解析结果，修一边会让同条命令在两道门中得到不同词法事实。这里没有把策略不同误判成漏洞；问题是词法事实复制。 | 以现有command_safety词法为初始共同实现，提取底层ShellFacts/Tokenize接口；两端保留各自命令白名单、ReadOnly/Unknown/NeedsConfirm判词。先用共享向量证明行为不变，再另立任务升级语法支持。 | 词法属于安全边界；不能借合并扩大白名单或改变cmd单引号语义。Plan不等于自动审批，两张策略表必须保留。 |

## 证据与边界

plan_mode.cpp:27明确写“与command_safety的Safe不同表”，这是保留策略表的证据；:96-99的脚本块检测已共用tools实现，说明同层共用词法可行。本TODO不合并ClassifyPlanSegment与ClassifySegment。

代码证据来自当前源码静态核对；没有运行复现、编译或测试。上文后果是对应路径的推导，未声称观察到生产事故。

## 交接范围

- 负责文件：`src/tools/command_safety.cpp`；`src/tools/command_safety.hpp`；`src/runtime/plan_mode.cpp`。
- 前置：无硬前置；共改文件须协调。（以全局排期复核为准）。
- 不得并行共改：本组其余任务无同文件写入冲突。
- 可并行：除上述依赖/共改任务外，其余审查任务可单独worktree推进；不要借本单批量格式化或重写无关代码。

## 迁移与验收

1. 固定本单触及合同与行为样例；对照当前源码重新确认行号。
2. 以现有command_safety词法为初始共同实现，提取底层ShellFacts/Tokenize接口；两端保留各自命令白名单、ReadOnly/Unknown/NeedsConfirm判词。先用共享向量证明行为不变，再另立任务升级语法支持。
3. 小批提交，按下列门槛验收。

- [x] 两套现有向量逐字保持判词。（test_command_safety.cpp 24 批与 test_plan_mode.cpp plan_shell 批一字未动，PR #196 CI 全绿即证）
- [x] 共享引号、空段、重定向、子表达式与带路径exe向量。（新增 tests/unit/tools/test_shell_lexing.cpp：拆段/空段/引号分流/重定向/子表达式/脚本块/拆词剥引号/带路径exe归一）
- [x] Plan/审批白名单差异仍有单独断言。（test_plan_mode.cpp“与 Auto 档 Safe 分道——cd/echo 这些 Plan 不放”批，既有未动）
- [x] 只在GitHub CI运行测试。（本地零 build 零 ctest 零 configure）

## 落地记录（PR #196）

- 新增 `src/tools/shell_lexing.hpp/.cpp`（CMake 登记 lubancode_engine）：`ToLowerWord`/`SplitSegments`/`HasUnquotedRedirection`/`HasSubexpression`/`HasUnquotedScriptBlock`/`Tokenize`/`NormalizeWord`，实现自 command_safety 逐字迁入，零行为变更。
- command_safety.cpp 删本地词法件，调用点 ToLower→ToLowerWord；command_safety.hpp 删 `HasUnquotedScriptBlock` 声明（随迁 shell_lexing.hpp；唯一外部调用方 plan_mode 换 include；turn_runtime 的 `CommandHasUnquotedScriptBlock` 不动）。
- plan_mode.cpp 删六件重复词法（SplitShellSegments/HasUnquotedRedirection/HasSubexpression/TokenizeShell/NormalizeShellWord/ToLower），经文件头 using 吃共享件；判词、白名单、deny 文案一字未改。净删约 130 行。

## 删除量与旧账

预计净删114行：plan_mode.cpp重复词法约126物理行，扣除新include与调用适配约12行；只算删掉的一份，不把另一份搬位置算删除。实际 plan_mode.cpp 净删约 130 行（词法件连注释一并去，含新增 using 块与注释）。

当前 todos 未找到直接承接此缺口的专单。

## 全局排期复核

- 前置／文件接手次序：无硬前置；共改文件须协调。
- 总排期：[三批重构与并行交接](../07_三批重构与并行交接.md)。本节含跨组复核结果，优先于早期本组可并行提示。
- 当前清单未见同文件冲突；新增公共头或CMake源登记先通知集成人。（已登记 CMakeLists.txt lubancode_engine 源列表，PR 即通知。）
