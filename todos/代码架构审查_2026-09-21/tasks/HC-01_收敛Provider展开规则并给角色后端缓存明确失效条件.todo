# HC-01：收敛Provider展开规则并给角色后端缓存明确失效条件

状态：已落码，验收交 CI（证据见文末勾账）。

基线：`693d3ddb5607fc67f37c262b8b8873c9f7dc07eb`；先完成职责表，再据实际调用判定。

| 编号 | 类别(A/B/C) | 严重度 | 位置(文件:行) | 现象(最短代码证据) | 具体后果 | 建议方案 | 影响面与风险 |
|---|---|---|---|---|---|---|---|
| HC-01 | A | 高 | src/app/model_router.cpp:101<br>src/app/model_router.cpp:155<br>src/config/config.cpp:396<br>src/app/commands/settings_commands.cpp:1459<br>src/app/commands/settings_commands.cpp:1602 | ConfigForProvider 复制当前 Config 后逐字段改目标端；只改到 reasoning_replay_field。BackendForProvider 先 provider_backends_.find(provider)，命中便返回。 | cheap/lao 指向另一 Provider 时，stream_usage、think_param、native_web_search 仍可能沿用活跃端。相同目标走 RouteDetached 会重建，走 Route/Sample 会复用按名称缓存；编辑目标 extra_body/headers 或删除目标后，旧缓存仍能继续发请求。 | 保留 config::ApplyProviderToRuntimeConfig 作为显式 Provider 展开口，替换 ConfigForProvider 的手抄字段。用有效连接配置版本标识缓存项；每次命中前确认目标仍存在且版本相同，或从已提交配置变更发失效通知。保留主后端与 detached 后端的不同寿命。 | 涉及跨Provider压缩、记忆抽取、标题精炼；更换缓存时须保证在途请求仍持有原后端。不要把环境来源优先级版 ApplyConfiguredActiveProvider 强行改成无条件覆盖。 |

职责依据：ModelRouter 选角色与后端；配置层已经负责把 Provider 条目展开成连接配置。当前重复承担同一字段映射，目标端参数出现串用。

最短证据：
```cpp
// model_router.cpp:103、112-122
lubancode::config::Config derived = config_result_.config;
derived.wire = entry->wire;
derived.base_url = entry->base_url;
// 手工覆盖 11 项，到 reasoning_replay_field 为止
// config.cpp:404-412 的公共展开还覆盖：
config.native_web_search = provider.native_web_search;
config.stream_usage = provider.stream_usage;
config.think_param = provider.think_param;
// model_router.cpp:155-157
const auto cached = provider_backends_.find(provider);
if (cached != provider_backends_.end()) { return cached->second.get(); }
```

`BuildBackend` 在 `backend_stack.cpp:21-47` 确实读取 native_web_search、stream_usage、think_param，不是未用字段。`settings_commands.cpp:1459-1475` 能改 Provider 条目，但只重建当前活跃后端；`:1602` 删除非活跃条目。`session_stack.cpp:369` 每会话只建一次 ModelRouter，没有相应缓存失效口。

静态可达场景：活跃 A 的 stream_usage=false、think_param=X，cheap 指向 B，B 声明 stream_usage=true、think_param=Y。第一次 Route(B) 仍带 A 的这两项。之后修改 B 的 extra_body，旧 Route(B) 缓存仍生效，RouteDetached(B) 却读到新值。删除 B 后，已命中的缓存还先于 FindProvider 返回。

是否应合并：显式选端的字段展开应合并。`ApplyConfiguredActiveProvider` 还负责按字段来源优先级合并，不能整函数替换。主后端稳定壳、角色缓存、detached 独占后端也不能合成一份裸指针缓存。

删除口径：约 11 行重复字段赋值可删；版本/失效代码可能令总行数增加，不以净减行数验收。

实施步骤：
- [x] 先为现有 Provider 字段逐项列映射，使用公共显式展开口；保留路由角色的 model/effort 覆盖语义。
- [x] 规定配置提交点、缓存版本键、目标删除行为；只在已提交变化后失效。
- [x] 迁移 Route、Sample、RouteDetached，验证同一有效配置得到同一 wire 参数。
- [x] 对跨端能力、编辑非活跃端、删除已缓存端、在途请求重建补行为回归。

验收：B 的字段逐项来自 B；非连接全局项如超时按原规则继承；两种 Route 仅寿命不同；已删除 Provider 不再创建新请求；失败配置提交不使缓存半更新。测试交实现者通过 GitHub CI 跑，本次未运行。

落码纪要（2026-09-21）：
- 展开收口：新增自由函数 `app::DeriveProviderRuntimeConfig(Config base, provider, active_provider)`（src/app/model_router.cpp），跨端分支直接调 `config::ApplyProviderToRuntimeConfig`，删掉原 ConfigForProvider 的 11 行手抄——stream_usage/think_param/native_web_search 等 8 项漏抄字段从此逐项来自目标条目；路由角色的 model/effort 覆盖语义在 ModelRoute 层，未动。
- 缓存版本键：新增 `config::ProviderConnectionFingerprint(ProviderConfig)`（src/config/config.cpp），口径 = ApplyProviderToRuntimeConfig 字段集 + ResolveProviderAuth 解析后鉴权值；model_reasoning_effort 不进指纹。`ModelRouterService::BackendForProvider` 命中前先 FindProvider 再比指纹：条目被删 → 清缓存交空指针；被编辑 → 弃旧重建。缓存值改 shared_ptr（换槽时旧 client 活到在途引用松手；Route 仍只在主线程回合边界同步用，worker/嵌套走 RouteDetached 独占——主后端与 detached 后端寿命合同不变）。
- 配置提交点：settings_commands.cpp 三处 /provider set 分支（native_web_search/extra_body/extra_header）由"先改内存后落盘"倒序为"先落盘成功再改内存"，与既有 auth、Remove 分支同序——失败提交不再半改内存，缓存读不到半更新。
- 合同测试（tests/unit/memory/test_model_router.cpp，先于动刀落笔）："HC-01 Provider 展开合同"钉字段逐项来自目标端 + 非连接全局项继承 + 活跃端/空名/ghost 三口径；"HC-01 缓存失效合同"钉复用/编辑重建/指纹外字段不误伤/删除交空。

相关已有 TODO：`../../AppServer模型连接解耦_协议配置与会话隔离设计.todo` §3 要求“显式选择 Provider 后，地址、wire、鉴权和私有参数必须来自同一套连接”；本单修当前终端角色路由，不等待整套协议重构。

## 交接边界

- 批次：第2批。
- 前置：FD-04（以全局排期复核为准）。
- 并行：可与HC-02、HC-03、HC-04并行。
- 禁止共改：以下文件在本单实施期间只归本单负责人；其他任务先改自己的独立文件，接口变动等本单合入后再接。

- `src/app/model_router.hpp`
- `src/app/model_router.cpp`
- `src/app/commands/settings_commands.cpp`
- `src/config/config.cpp`
- `tests/unit/app/test_app_commands.cpp`

预计可删除旧重复代码：约11行；以正文口径为准。
## 全局排期复核

- 前置／文件接手次序：FD-04
- 总排期：[三批重构与并行交接](../07_三批重构与并行交接.md)。本节含跨组复核结果，优先于早期本组可并行提示。
- 共改任务：FD-04（src/config/config.cpp）；HC-07（src/app/commands/settings_commands.cpp, tests/unit/app/test_app_commands.cpp）；FD-05（src/config/config.cpp）；HC-06（src/app/commands/settings_commands.cpp）。
