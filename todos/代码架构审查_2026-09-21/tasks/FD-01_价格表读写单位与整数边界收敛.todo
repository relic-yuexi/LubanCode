# FD-01 价格表读写单位与整数边界收敛

- 状态：已实现；PR #198 CI rollup 绿，待合并；见文末勾账。
- 日期：2026-09-21。
- 基线：`693d3ddb5607fc67f37c262b8b8873c9f7dc07eb`。
- 类别：B；严重度：中；重构批次：2。
- 前置：无硬前置；共改文件须协调。（以全局排期复核为准）。
- 改动所有权：下列文件由本单负责人持有；不同单命中同一文件时，按总索引串行或先交共享接口。

## 问题表

| 编号 | 类别 | 严重度 | 位置 | 现象（最短证据） | 具体后果 | 建议方案 | 影响面与风险 |
|---|---|---|---|---|---|---|---|
| FD-01 | B | 中 | src/accounting/pricing_table.cpp:11；src/accounting/pricing_table.cpp:99；src/accounting/cost_estimator.cpp:14；tests/unit/accounting/test_pricing_cost.cpp:73 | `ParsePriceMicros`：`return units * 1'000'000;`；`ToJson`：`entry["input_per_million"] = price.input_per_million_micros;`；估价：`hi * price_micros + (lo * price_micros) / 1'000'000`。 | 同一 schema、同名字段，读侧认货币单位，写侧放 micros。1.25 读成 1,250,000，写回再读变成 1,250,000,000,000。现有往返测试只验 has_value，没验数值。解析还允许单位值到 9e12，转换后接近 9e18；估价器按 price_micros ≤9e12 的注释假设直接乘，合法输入即可让中间乘积溢出。未找到价格表 ToJson 在 src 中落盘的生产调用，不把这一项说成用户现有账单已受污染。 | 保留整数 micros 内存合同；钉死 JSON 的货币单位合同，读写使用同一转换件。若另开 micros schema，必须换字段或升版本。乘法与四桶加和检查范围，溢出返回明确状态；禁止静默饱和后仍标 estimated。 | 改价格 schema 会影响旧配置与测试夹具；应先保留现有 JSON 输入格式。数值错误不能降成合法零价。 |

## 修改边界

- `src/accounting/pricing_table.cpp`
- `src/accounting/pricing_table.hpp`
- `src/accounting/cost_estimator.cpp`
- `src/accounting/cost_estimator.hpp`
- `src/accounting/usage_sample.hpp`
- `tests/unit/accounting/test_pricing_cost.cpp`

## 实施顺序

- [x] 先加往返数值与边界验收夹具。（commit f66ddd1c：往返逐项钉数测试先行，修复前为红）
- [x] 集中 JSON↔micros 转换，写明兼容策略。（ParsePriceMicros/PriceMicrosToJsonUnits 同一对转换件公示于 pricing_table.hpp；JSON 恒货币单位、内存恒 micros，v1 输入格式不变）
- [x] 更换安全乘加入口，再迁移估价调用。（MultiplyTokensByMicrosPrice 退役，TryMultiplyTokensByMicrosPrice 返回 optional，超界 nullopt；EstimateCost 四桶检查乘加，全仓旧名零残留）

## 验收门

- [x] 1.25、0.1、0、整数价经历 parse→serialize→parse 后 micros 逐项相等。（test_pricing_cost.cpp"价格表往返"用例，两模型八桶全等）
- [x] 用 tokens=2、units=9e12 验证中间乘法可控：数学结果为 18e12 micros，不能先溢出。（TryMultiplyTokensByMicrosPrice(2, 9e18)==18'000'000'000'000；拆段后逐乘检查）
- [x] 覆盖四桶加和超界与小数舍入，调用方区分未定价、估价成功、超界。（新增 CostStatus::Overflow，序列化名 estimate_overflow；四桶各 4.68e18、加和 1.872e19 超界用例；3.75/99'999.9 下取整钉数）

- CI：实现者提交独立PR后跑仓库远程CI；本审查未运行测试。

## 合并取舍与估量

只处理本单钉住的合同，不借题改邻接模块。

预计可删除行数：0。0 表示本单不以删行为目标；移动行数不算删除。

关联既有单：catalog价格账本_四桶计费与分段地区价.todo；本单只修基础合同，不接新价目源。

核证范围：静态调用链与源码分支核对；未编译、未运行产品测试，未声称线上复现。

## 实现勾账（2026-09-22）

- 读写单位收敛：`ParsePriceMicros`（读侧唯一入口）与 `PriceMicrosToJsonUnits`（写侧唯一入口）公示于 pricing_table.hpp，同一对转换件互逆；`PricingTable::ToJson` 不再直放 micros。v1 JSON 输入格式不变（仍收货币单位整数/小数），旧配置与夹具零迁移。
- 整数边界收敛：`MultiplyTokensByMicrosPrice` 退役（全仓引用迁净），`TryMultiplyTokensByMicrosPrice` 拆两段再拆余段、逐乘逐加显式范围检查，超界返回 `nullopt`，全程无 UB、无静默饱和。
- 新增 `CostStatus::Overflow`（序列化名 `estimate_overflow`）：四桶任一乘积或加和装不下 int64 时 `micros=0`、不降 `not_priced`。usage_sample.cpp 补两行枚举映射（hpp 加枚举值的机械必要改动，边界外一行说明）。
- 边界说明：`PriceMicrosToJsonUnits` 整数价任意大小精确；小数价 ≤1e9 单位、六位小数内逐 micros 互逆（现实价格远在界内），超界受 double 精度所限——v1 schema 只有数值一形，已是上限。
- 不借题改邻接：usage_commands 展示层把超界样本计进"其余 not_priced"计数（标签略宽，属邻接面，本单不动）。
- PR/CI 证据：PR #198（https://github.com/relic-yuexi/LubanCode/pull/198），终判 head bc4de1b4。代码头 a53488c5 的 run 35661628592：首跑 unit.channelfeishu.feishu_adapter 时序 flaky（PumpUntil 等待超时，与本单 accounting 无涉，pricing_cost 52 号测试首跑即过），`gh run rerun --failed` 一次后绿。勾账头 bc4de1b4 的 run 35668147611：changes 门走了保险全跑道，windows-msvc / macos-clang / linux-asan 三构建腿齐跑；windows-msvc 首跑挂 integration.workspace.workspace_index_ledger（记忆在案的 Windows 腿间歇红面，与本单无涉），`gh run rerun --failed` 一次后三腿全绿。两次各有首个被并发组取消的空壳 run 留 CANCELLED 假腿污染 rollup，均删除后复核 rollup.conclusion = SUCCESS。绿后未合并，待点名。
## 全局排期复核

- 前置／文件接手次序：无硬前置；共改文件须协调。
- 总排期：[三批重构与并行交接](../07_三批重构与并行交接.md)。本节含跨组复核结果，优先于早期本组可并行提示。
- 当前清单未见同文件冲突；新增公共头或CMake源登记先通知集成人。
