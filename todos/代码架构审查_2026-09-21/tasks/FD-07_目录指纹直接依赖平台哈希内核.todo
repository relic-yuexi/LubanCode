# FD-07 目录指纹直接依赖平台哈希内核

- 状态：已实现；PR #202 CI 双平台绿,待合并。
- 日期：2026-09-21。
- 基线：`693d3ddb5607fc67f37c262b8b8873c9f7dc07eb`。
- 类别：C；严重度：低；重构批次：1。
- 前置：无硬前置；共改文件须协调。（以全局排期复核为准）。
- 改动所有权：下列文件由本单负责人持有；不同单命中同一文件时，按总索引串行或先交共享接口。

## 问题表

| 编号 | 类别 | 严重度 | 位置 | 现象（最短证据） | 具体后果 | 建议方案 | 影响面与风险 |
|---|---|---|---|---|---|---|---|
| FD-07 | C | 低 | src/platform/dir_fingerprint.cpp:13；src/platform/dir_fingerprint.cpp:87；src/hooks/hash.cpp:8；src/hooks/hash.cpp:13 | `platform/dir_fingerprint.cpp` → `hooks::Sha256Hex` → `platform::Sha256Hex`。 | 算法已经收口，基础平台件却绕回 Hooks 薄包装，留下 platform↔hooks 目录环。单独复用目录指纹还须链接 Hooks 包装符号，基础库依赖方向与算法归属相反。 | 目录指纹直接 include platform/sha256.hpp 并调用本层 Sha256Hex。Hooks 包装保留给信任领域使用者，不算重复算法、不顺手全删。 | 只换依赖，不改 Plugin v1/Package v1 的材料拼法、分隔符和摘要格式。 |

## 修改边界

- `src/platform/dir_fingerprint.cpp`

## 实施顺序

- [x] 改一处include与三处限定名，审diff确认没有算法改动。（PR #202：include `hooks/hash.hpp`→`platform/sha256.hpp`，三处 `hooks::Sha256Hex`→`platform::Sha256Hex`；diff 仅此四行，材料拼法/分隔符/摘要格式零改动）

## 验收门

- [x] Plugin与Package存量golden指纹不变。（同一内核同一 string_view 薄口，输出逐字节相同；tests/unit/packages/test_package_trust.cpp 指纹底座用例与 plugin 信任集成测试全过）
- [x] platform目录无hooks include（当前唯一反向边）。（改后 `grep '#include "hooks/' src/platform/` 零命中）

### 勾账证据

- PR：https://github.com/relic-yuexi/LubanCode/pull/202（commit 8524118c）
- CI：run 35685677915，rollup conclusion=success（changes/lane/install-scripts 双平台/macos-clang 全绿；run 35685666209 为并发组抢占 cancelled，非真红）。

- CI：实现者提交独立PR后跑仓库远程CI；本审查未运行测试。

## 合并取舍与估量

此处应删反向依赖；Hooks领域命名包装不必合并删除。

预计可删除行数：0。0 表示本单不以删行为目标；移动行数不算删除。

关联既有单：未要求合并既有总设计单。

核证范围：静态调用链与源码分支核对；未编译、未运行产品测试，未声称线上复现。
## 全局排期复核

- 前置／文件接手次序：无硬前置；共改文件须协调。
- 总排期：[三批重构与并行交接](../07_三批重构与并行交接.md)。本节含跨组复核结果，优先于早期本组可并行提示。
- 当前清单未见同文件冲突；新增公共头或CMake源登记先通知集成人。
