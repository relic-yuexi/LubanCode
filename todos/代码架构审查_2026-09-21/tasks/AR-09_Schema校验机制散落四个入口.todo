# AR-09 Schema校验机制散落四个入口

- 状态：已修待合（PR #171，CI rollup 绿）；审查时未改源码，修复见勾账节。
- 审查基线：`693d3ddb5607fc67f37c262b8b8873c9f7dc07eb`。
- 主批次：第2批（1=无行为变更合并；2=接口/合同收敛；3=职责拆分）。修复已证错误可在机械合并就绪后优先，不必等所有低风险清理。

## 问题表

| 编号 | 类别(A/B/C) | 严重度 | 位置(文件:行) | 现象（最短代码证据） | 为什么是问题 | 建议方案 | 影响面与风险 |
|---|---|---|---|---|---|---|---|
| AR-09 | A | 中 | src/tools/schema_check.cpp:7；src/tools/schema_check.cpp:34；src/runtime/plugin_contract.cpp:1545；src/runtime/plugin_contract.cpp:1582；src/workflow/runtime.cpp:61；src/workflow/runtime.cpp:114；src/agent/loop.cpp:573；src/runtime/hook_host_services.cpp:620 | JsonTypeMatches两份；workflow输入/输出各写required+type扫描；插件另有递归validator。 | 加入一种JSON类型或修正数值类型判断，至少要同步tools、plugin与workflow两段。workflow把null当缺失，tools/plugin required只看键是否存在；工具浅检查不认嵌套和additionalProperties，插件认。差异已经是实码，不能用一个入口名称掩盖。 | 先保留四个领域入口与错误格式，抽共同类型/required/属性遍历原语；再将插件递归验证器下沉为独立schema核心，显式profile控制顶层object、null-as-missing、递归与关键字集合。最后迁移适配器。禁止直接把所有入口换成最严格档造成行为漂移。 | 影响hook改参、tool_invoke、插件调用和workflow产物采用；同名校验不等于相同合同。未知schema/关键字策略需逐入口保持并留后续升级事项。 |

## 证据与边界

是否合并：应合并机制，不应把四个入口无条件合成同一严格策略。plugin_contract.hpp:413-416已说明插件合同更强。测试必须覆盖required:null：workflow拒，工具/插件只有属性type不认null时才拒。

代码证据来自当前源码静态核对；没有运行复现、编译或测试。上文后果是对应路径的推导，未声称观察到生产事故。

## 交接范围

- 负责文件：`src/tools/schema_check.cpp`；`src/tools/schema_check.hpp`；`src/runtime/plugin_contract.cpp`；`src/runtime/plugin_contract.hpp`；`src/workflow/runtime.cpp`。
- 前置：AR-04（以全局排期复核为准）。
- 不得并行共改：AR-03、AR-04；同文件先后落单，或约定具体函数所有权。
- 可并行：除上述依赖/共改任务外，其余审查任务可单独worktree推进；不要借本单批量格式化或重写无关代码。

## 迁移与验收

1. 固定本单触及合同与行为样例；对照当前源码重新确认行号。
2. 先保留四个领域入口与错误格式，抽共同类型/required/属性遍历原语；再将插件递归验证器下沉为独立schema核心，显式profile控制顶层object、null-as-missing、递归与关键字集合。最后迁移适配器。禁止直接把所有入口换成最严格档造成行为漂移。
3. 小批提交，按下列门槛验收。

- [x] 为每种profile建立差异矩阵：required缺键/显式null、type、enum、const、array/items、additionalProperties。（tests/unit/schema/test_schema_contract.cpp，逐格按现行为断言；workflow 两档经公共面观察——invalid_inputs 文案/编排账拒收件 validation.checks）
- [x] 迁移首阶段错误码/路径/文案稳定。（含插件顶层空 path 的前导空格、workflow checks 全收序 required 先于 type，逐字钉死）
- [x] 插件深度上限仍32。（30 层过/40 层拒，文案含"嵌套超过 32 层"）
- [x] 不新增runtime到agent反向依赖。（src/schema 只依赖 nlohmann，落 lubancode_engine；workflow/runtime→schema 同层向下）
- [x] remote CI。（PR #171，run 35552083756 conclusion=success：macos-clang/workflow-tsan/windows-msvc/linux-asan 全绿；另 9 枚 CANCELLED 是被 pull_request 事件顶掉的 push 旧 run 35552070061，同 head，不算数）

## 勾账（2026-09-21）

- 已修：四入口共吃 src/schema 核心（类型/required/属性遍历原语 + 插件递归验证器下沉），Profile 三档显式化（HookRewrite/PluginContract/WorkflowScan）；四入口只留顶层政策与人话适配层，错误码/路径/文案零漂移。
- 提交：f70d1d79（合同测试先行）+ 04670e2a（核心与适配层）；净账：触及源码删 282 增 112（另新增核心 380 行、合同测试 444 行——审查单"净删90"按纯实现口径大致相当，注释与测试另账）。
- 未合并：CI 绿后停在 PR #171 待点名合并。

## 删除量与旧账

预计净删90行：tools/schema_check.cpp约76行与workflow两段同义结构检查约48行，共124行，扣保留profile适配约34行。递归核心搬家不算删行。

关联 todos/Workflow接入SessionV3_编排账与节点恢复设计.todo 第五节产物合同；本单只收口当前已声明子集，不冒充补齐完整JSON Schema。
## 全局排期复核

- 前置／文件接手次序：AR-04
- 总排期：[三批重构与并行交接](../07_三批重构与并行交接.md)。本节含跨组复核结果，优先于早期本组可并行提示。
- 共改任务：AR-03（src/workflow/runtime.cpp）；AR-04（src/workflow/runtime.cpp）。
