# AR-12 轨迹会话门面聚合了互不共用状态的职责

- 状态：已修（第一步"机械拆文件"落地：桥类按现有类边界拆出独立文件,公共接口与行为一字不动;只读查询/标题/RecordSelection 的接口收窄留后续批次）。验收证据见文末。
- 审查基线：`693d3ddb5607fc67f37c262b8b8873c9f7dc07eb`。
- 主批次：第3批（1=无行为变更合并；2=接口/合同收敛；3=职责拆分）。修复已证错误可在机械合并就绪后优先，不必等所有低风险清理。

## 问题表

| 编号 | 类别(A/B/C) | 严重度 | 位置(文件:行) | 现象（最短代码证据） | 为什么是问题 | 建议方案 | 影响面与风险 |
|---|---|---|---|---|---|---|---|
| AR-12 | C | 中 | src/runtime/trajectory_session.hpp:119；src/runtime/trajectory_session.hpp:444；src/runtime/trajectory_session.hpp:597；src/runtime/trajectory_session.hpp:760；src/runtime/trajectory_session.hpp:976；src/runtime/trajectory_session.hpp:1007；src/runtime/trajectory_session.cpp:3356；src/runtime/trajectory_session.cpp:5663 | 单模块同时公开主轮/旁路/子代理/workflow桥、会话生命周期、查询验账、标题与record选段；Impl另养resume与分页缓存。 | 修改某类旁路或workflow事实需进入同一6千行实现，独立审查与并行修改争用同一文件。会话查询只需目录/只读账，却依赖具有发号和写入权限的整套门面；标题、resume缓存、子账counter、record控制互不共享大部分成员。Pimpl隐藏了布局，没划清业务边界。 | 保留TrajectorySessionLedger为会话owner与窄工厂；先把各桥按现有类边界拆出文件，再把只读HistoryQuery/Verification服务、标题控制、RecordSelection各自接窄目录/提交接口。继续复用同一writer与锁，不复制一份账。v2读写退役仍走旧设计专单，不能借拆分删除旧档支持。 | 影响轨迹全链，拆分分批保持事件字节与身份一致。机械拆文件先行，接口收窄随后；只迁移模块不以删行冒充收益。 |

## 证据与边界

成员分组证据：Impl:3357-3369会话owner/元数据；:3371-3378子代理与workflow专属计数/故障注入；:3380-3390恢复材料；:3393 v3写簿；:3396起旧史分页缓存。TrajectorySessionLedger自身:1197 record_selection_、:1198 command_counter_、:1201 environment_captured_分别由独立方法组使用。桥类自身也有独立recorders/maps，本单不要求把它们合成一个接口。

代码证据来自当前源码静态核对；没有运行复现、编译或测试。上文后果是对应路径的推导，未声称观察到生产事故。

## 交接范围

- 负责文件：`src/runtime/trajectory_session.hpp`；`src/runtime/trajectory_session.cpp`；`src/runtime/session_runtime.hpp`；`src/runtime/trajectory_history_view.hpp`。
- 前置：无硬前置；共改文件须协调。（以全局排期复核为准）。
- 不得并行共改：本组其余任务无同文件写入冲突。
- 可并行：除上述依赖/共改任务外，其余审查任务可单独worktree推进；不要借本单批量格式化或重写无关代码。

## 迁移与验收

1. 固定本单触及合同与行为样例；对照当前源码重新确认行号。
2. 保留TrajectorySessionLedger为会话owner与窄工厂；先把各桥按现有类边界拆出文件，再把只读HistoryQuery/Verification服务、标题控制、RecordSelection各自接窄目录/提交接口。继续复用同一writer与锁，不复制一份账。v2读写退役仍走旧设计专单，不能借拆分删除旧档支持。
3. 小批提交，按下列门槛验收。

- [x] 单类拆出后既有事件黄金向量逐字不变。（PR #191:类体/方法体按行段原样搬迁,对 161 个成员函数定义与全部自由函数/类/常量做字节级 diff 对账零差异;既有测试零改动,含 trajectory 行为/黄金向量的测试套在 CI 全绿。）
- [ ] 查询服务无需取得可变writer。（接口收窄目标,属本单第二步"各自接窄目录/提交接口"——机械拆文件先行、接口收窄随后,按单子节奏留后续批次,不冒领。）
- [x] v2/v3相同行为向量分别通过。（PR #191 CI run 35635103140:windows-msvc 与 macos-clang 两腿 success,rollup conclusion=success;v2 老路与 v3 typed 事件两套既有测试零改动全过。）
- [x] clear/resume换场后桥与缓存仍绑正确session_id。（BindV3Books_/AdoptSourceSystemV3_ 及 clear/resume 换场逻辑按行段原样搬迁一字未动;Impl::v3_books 换场重绑、transcript_cache 绑定源指纹的既有测试在 CI 全绿。）
- [x] 编译边界只用静态include图核对，测试走GitHub CI。（未本地 build/ctest;静态 include 图逐符号核对的传递链经 CI 编译验证:新增 4 个桥头 + 2 个内部头,旧头聚合 include 四桥头,全仓 85 处 trajectory_session.hpp 引用与既有测试零改动。）

## 验收记录（AR-12 第一步：机械拆文件）

- PR：https://github.com/relic-yuexi/LubanCode/pull/191（分支 worktree-agent-a1211979ee21d7349,基线 b955f269 含 merge PR #187）。
- CI：run 35635103140,rollup conclusion=success;关键腿 windows-msvc=success、macos-clang=success(clang 严检腿过——静态 include 图与前向声明无坑)。
- 形态：trajectory_session.hpp/.cpp(1226+6165 行)拆为——主轮桥 trajectory_turn_bridge、旁路桥 trajectory_bypass_bridge、子代理桥 trajectory_subagent_bridge、workflow 桥 trajectory_workflow_bridge 各自成件;桥间共用事实构造进内部头 trajectory_bridge_internal.hpp(单点定义,不复制账);TrajectorySessionLedger::Impl 定义进内部头 trajectory_session_impl.hpp(装配跨件共用,对外仍 Pimpl);trajectory_session.* 留会话 owner 与窄工厂、RecordSelectionController,公共面经旧头聚合,全仓引用零改动。CMakeLists.txt 登记 4 个新 cpp(已按"新增源登记知会集成人"规矩在 PR 描述载明)。
- 行为零变更手段:行段 sed 搬迁 + 字节级 diff 对账;AR-03(#162)给 workflow 侧诊断口加的 io_errors_ 锁随段原样保留。

## 删除量与旧账

本项不以删行计收益；预计净删除 0 行。

关联 todos/SessionV3旧设计清理_消费迁移缺口补齐与旧接口退役.todo；该单管旧接口退役，本单管当前仍需保留的职责边界。
## 全局排期复核

- 前置／文件接手次序：无硬前置；共改文件须协调。
- 总排期：[三批重构与并行交接](../07_三批重构与并行交接.md)。本节含跨组复核结果，优先于早期本组可并行提示。
- 当前清单未见同文件冲突；新增公共头或CMake源登记先通知集成人。
