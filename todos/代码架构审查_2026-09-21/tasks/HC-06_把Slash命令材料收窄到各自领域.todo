# HC-06：把Slash命令材料收窄到各自领域

状态：修复中。第一小批已合入 PR #189(CI run 35621540215 rollup success,三腿全绿);第二小批(Model/Memory/Usage/Package)已落待验收;第三小批(Session/Workflow 宽域)待做。

基线：`693d3ddb5607fc67f37c262b8b8873c9f7dc07eb`；先完成职责表，再据实际调用判定。

| 编号 | 类别(A/B/C) | 严重度 | 位置(文件:行) | 现象(最短代码证据) | 具体后果 | 建议方案 | 影响面与风险 |
|---|---|---|---|---|---|---|---|
| HC-06 | C | 中 | src/app/commands/command_registry.hpp:26<br>src/app/commands/command_registry.hpp:95<br>src/app/commands/command_registry.hpp:173<br>src/app/commands/trace_commands.cpp:179<br>src/app/interactive_session_assembly.cpp:1709 | SlashDispatchContext同时公开Config、Agent、ToolRuntime、LSP、Memory、Peer、Goal、Loop等可变句柄；Trace handler只取trace_hub/theme却也include整头。 | 不同命令域必须带上彼此头依赖；当前直接包含该头26个文件，静态可达25个.cpp。仅增加遥测或Loop材料也把Trace/模型/包命令拉入重编范围；测试轻量命令仍需理解大上下文可空合同。 | 复用已有TraceCommandContext、MemoryCommandContext、ModelCommandContext等窄材料；在组合根或独立绑定单元构造对应闭包，路由表只接命令参数及执行器。逐域删去SlashDispatchContext字段，最后删除整束公共材料。 | 第三批拆分；必须在HC01/HC05/HC07相关接口稳定后进行。只搬职责与依赖，不改命令结果、轨迹封套、权限和会话生命周期。 |

职责依据：注册表应负责匹配命令；各域处理器只需要自己材料。当前注册表头反而充当所有命令域的总依赖容器。

最短证据：
```cpp
// command_registry.hpp:173-190，只截一小组
lubancode::agent::Agent* main_agent = nullptr;
lubancode::runtime::SessionRuntime* session_runtime = nullptr;
lubancode::runtime::ToolTraceHub* trace_hub = nullptr;
lubancode::telemetry::TelemetryService* telemetry_service = nullptr;
lubancode::memory::ProjectMemory* project_memory = nullptr;
// trace_commands.cpp 的handler实际只需要：
trace_ctx.trace_hub = dispatch.trace_hub;
trace_ctx.theme = dispatch.theme;
```

成员确实分组而非仅文件偏大：配置/模型字段在`:97-146`，工具及扩展在`:149-170`，会话运行时在`:173-194`，Peer/Record/Channel在`:197-203`，宿主回调在`:206`以后。Trace 不共享这些状态；Model已有单独ModelCommandContext；Memory也有MemoryCommandContext。

依赖证据：此头直接include `agent/agent.hpp`、`app/tool_runtime.hpp`、`channel/manager.hpp`、`memory/project_memory.hpp`、`lsp/manager.hpp`、`telemetry/service.hpp`、`workflow/host_executors.hpp`。`evidence/header-impact.json`统计直接入边26，传递.cpp 25；这是源代码include图估算，不是实测构建耗时，也未算仓外消费者。

实施步骤：
- [x] 第一小批迁Trace、Hook、Telemetry等窄域，保留现有领域函数；新增命令绑定单元集中构造捕获材料的执行器。（2026-09-21：立 session_command_bindings 绑定单元,表改闭包执行器(SlashCommandHandler 只接命令参数);/trace、/hooks、/telemetry 分派位改吃窄材料,trace_commands.cpp/telemetry_commands.cpp/hook_commands.cpp 不再 include 注册表头;SlashDispatchContext 删 trace_hub、enable_telemetry_session 两字段与 tool_trace_hub.hpp include;路由与 TrajectoryCommandExecutor 改吃表,不再摸材料袋。telemetry_service/trajectory 字段保留——doctor 域仍共用,随 doctor 迁移批次退役。验收:Trace 域编译零会话头;三域 handler 只见窄 context;58 案对账与死案兜底测试照旧绿。证据:PR #189,CI run 35621540215 rollup success。）
- [x] 第二小批迁Model/Memory/Usage/Package等已有窄Context域，禁止反向给窄Context塞回整个SlashDispatchContext。（2026-09-22：Model/Memory/Usage 三域分派位改吃各自窄 context——装包段(/model 的跨家切换/活清单/同步三闭包、/usage 的 sessions_root 派生与内存粗账递手)随材料迁组合根 AssembleDispatchContext,闭包一律捕 this,快照点与旧 SlashDispatchContext 字段同点同源,行为零变;Package 域立 PackageCommandContext(home_lubancode/dev_package_dirs/config/skills/package_mount/reload_packages 六样,不递 InteractiveSessionOptions,域文件不进会话层头);model_commands/memory_commands/usage_commands/package_commands 四域文件不再 include 注册表头,Package 域补 config/config.hpp、package/mounting.hpp、tools/skill_loader.hpp 直递;UsageCommandContext.theme 引用改指针(与 Trace/Memory 窄材料同形态,manual smoke driver 构造处跟进);SlashDispatchContext 删 ensure_memory_tool、reload_packages 两枚域独占字段(全仓查证仅本域引用);model_catalog/project_memory/package_mount 等共用字段保留,随各自消费域迁移批次退役。窄 context 内无一枚 SlashDispatchContext,无反向塞回。）
- [ ] 第三小批迁Session/Workflow等宽域，按查询/运行/生命周期材料拆开；公共表只保留命令ID与执行入口。
- [ ] 删除未再使用的字段与include，更新组装和测试；每批都能单独合入。

验收：改某域材料不会触发无关命令域头依赖；Trace域编译无需包含Agent、ToolRuntime、LSP、Memory完整定义；handler不再拿到整场任意可变句柄；原命令输出与轨迹回执无行为差异。

不报悬空：现有注释明确材料借用、命令期间由Controller/Stack保活，未找到这里的并发越界调用证据。问题是编译和变更耦合，不能把多种指针机械判成所有权错误。

相关已有 TODO：`../../LubanCore与CLI分离_核心库独立成宿主底座设计.todo` 要求领域命令不复制；本单先收窄现有终端边界，避免把整束上下文搬进未来核心接口。

## 交接边界

- 批次：第3批。
- 前置：AR-10、HC-01、HC-05、HC-07（以全局排期复核为准）。
- 并行：可与HC-02、HC-03、HC-04并行。
- 禁止共改：以下文件在本单实施期间只归本单负责人；其他任务先改自己的独立文件，接口变动等本单合入后再接。

- `src/app/commands/command_registry.hpp`
- `src/app/commands/command_registry.cpp`
- `src/app/commands/*_commands.hpp`
- `src/app/commands/*_commands.cpp`
- `src/app/interactive_session_assembly.cpp`
- `src/app/interactive_session_controller.hpp`
- `tests/unit/app/test_command_registry.cpp`

预计可删除旧重复代码：约0行；以正文口径为准。
## 全局排期复核

- 前置／文件接手次序：AR-10、HC-01、HC-05、HC-07
- 总排期：[三批重构与并行交接](../07_三批重构与并行交接.md)。本节含跨组复核结果，优先于早期本组可并行提示。
- 共改任务：HC-01（src/app/commands/settings_commands.cpp）；HC-07（src/app/commands/settings_commands.cpp, src/app/commands/settings_commands.hpp）；AR-10（src/app/commands/session_commands.cpp）；HC-05（src/app/commands/command_registry.cpp, src/app/commands/workflow_commands.cpp, tests/unit/app/test_command_registry.cpp）。
