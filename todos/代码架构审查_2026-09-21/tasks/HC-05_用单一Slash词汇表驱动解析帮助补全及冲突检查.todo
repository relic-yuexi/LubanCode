# HC-05：用单一Slash词汇表驱动解析帮助补全及冲突检查

状态：已修复，待合并（PR #183，CI 绿；行为提交按交接表放第二批验收）。

基线：`693d3ddb5607fc67f37c262b8b8873c9f7dc07eb`；先完成职责表，再据实际调用判定。

| 编号 | 类别(A/B/C) | 严重度 | 位置(文件:行) | 现象(最短代码证据) | 具体后果 | 建议方案 | 影响面与风险 |
|---|---|---|---|---|---|---|---|
| HC-05 | A | 中 | src/cli/slash_commands.cpp:77<br>src/cli/slash_commands.cpp:128<br>src/cli/slash_commands.cpp:941<br>src/app/commands/command_registry.cpp:42<br>src/app/commands/workflow_commands.cpp:951<br>src/workflow/catalog.cpp:192 | ParseSlashCommand识别/hooks；SlashCommandTable登记Hooks处理器；AllSlashCommands整表未含/hooks；BuiltinSlashWords又直接从AllSlashCommands取名。 | /hooks能执行，却不出现在帮助/Tab补全；workflow alias=hooks的内建冲突检查漏掉它，用户建出别名后敲/hooks仍进Hook命令。新增命令须同步解析、描述、分派多张表。 | 保留SlashCommand枚举作为稳定内部标识，以共享描述符统一主名称、别名、描述键与可展示标记；解析、帮助、补全、内建保留名从描述符派生。app层仅绑定处理器，不再重写词汇。 | 保留/quit、/effort等别名与WorkflowAlias分流，显示词汇与所有保留词不能混成一张筛选后列表；handler仍留app层，不能令cli反向include app。 |

职责依据：解析、补全、帮助、别名冲突应共享同一命令身份。当前各自维护名称，已经出现实际漏项。

最短证据：
```cpp
// slash_commands.cpp:128
} else if (lower == "/hooks") { parsed.command = SlashCommand::Hooks; }
// command_registry.cpp:75
{lubancode::cli::SlashCommand::Hooks, "hooks", HandleSlashHooks, false, false},
// workflow_commands.cpp:954-955
for (const auto& info : lubancode::cli::AllSlashCommands()) {
    words.push_back(info.name);
}
```

`AllSlashCommands` 在 `slash_commands.cpp:949-1005` 的完整列表没有 `/hooks`；帮助和补全共用这表。`workflow/catalog.cpp:192` 仅遍历传入builtin名字，故hooks碰撞无从命中。这里不是隐藏别名：主命令Hooks有注册处理器，`/hooks`也不是内部哨兵。

是否应合并：命令词汇应合并。handler注册依赖app域，不能把函数指针移到cli公共词汇头。帮助里是否显示 `/quit` 可以另定展示标记，但冲突检查应使用全部保留词，不能继续复用“展示名单”。Image/NotSlash的特殊分流保留，不编造可调用handler。

删除口径：把手工分支与重复字符串表收为描述符后，保守估约100行重复词汇控制代码可删；新增目录行不以压缩写法换行数。最终以一处新增命令即可让各消费面识别为准。

实施步骤：
- [x] 建命令描述符，先逐项对照现有名称/别名/帮助顺序，枚举值保持。（c35ff6f8：cli::SlashCommandDescriptor，56 主名 + 4 别名共 60 行，表序即帮助序，逐行对照旧 AllSlashCommands 顺序；SlashCommand 枚举值未动）
- [x] 让解析及帮助补全读取目录；app处理器表只按稳定ID绑定。（c35ff6f8：ParseSlashCommand 查表、AllSlashCommands 派生、SlashCommandSpec 删 name 只绑 handler，轨迹账名走 FindSlashCommandDescriptor）
- [x] 给Workflow冲突检查单独提供“全部保留词”，补/hooks及隐藏别名边界。（a2db00ee：cli::AllSlashReservedWords，BuiltinSlashWords 改吃全量——主名+别名+隐藏词；alias=hooks、alias=quit 判内建冲突的单测判例在 tests/unit/app/test_command_registry.cpp）
- [x] 覆盖/hooks可见与冲突、/quit和/effort别名、未知Workflow别名分流。（a2db00ee + 6d53818d：/hooks 帮助面与 i18n 描述、保留词含 quit/lang/bg/effort/hooks/trace、逐词解析往返钉死内建词不走 alias 路；Unknown→Workflow alias 分流语义未动，alias_word 照旧，既有 session 层测试看守）

验收：增加命令词汇不需改三处名称列表；每个可执行主命令都有明确展示规则；alias=hooks被判冲突；既有命令行为及帮助顺序按对照表保留。

勾账证据：PR #183（https://github.com/relic-yuexi/LubanCode/pull/183），CI run 35595600337 rollup conclusion=success（macos-clang 全测试 + install-scripts 双平台绿；本 PR 未触门房敏感面，按 ci.yml lane 规矩普通 PR 只烧 macOS 一腿，windows-msvc 不适用）。提交三笔：
- c35ff6f8 refactor(cli): 行为不变——词汇表收拢解析/帮助/补全/保留词（第 1 批验收）；
- a2db00ee fix(cli): 行为提交——/hooks 进帮助与补全、冲突检查换全量保留词（按交接表放第 2 批验收，未夹进行为不变提交）；
- 6d53818d fix(tests): 对账测试补 Unknown 兜底案 skip（CI macos-clang 首轮唯一红点，生产行为无关）。
/trace 维持显式不展示（desc_key 留空即其展示规则，单测钉死）；其展示修复不在本单，需要时另立行为提交。

验收：增加命令词汇不需改三处名称列表；每个可执行主命令都有明确展示规则；alias=hooks被判冲突；既有命令行为及帮助顺序按对照表保留。

相关已有 TODO：`../../session轨迹v3_消息主轴树链与四角色壳收敛设计.todo` 含命令注册/审计元数据计划；本单只收拢词汇，不顺手重定义effect class或轨迹结构。

## 交接边界

- 批次：第1批。
- 前置：无硬前置；共改文件须协调。（以全局排期复核为准）。
- 并行：可与HC-01、HC-02、HC-03、HC-04、HC-07并行；HC-06须等本单合入。
- 禁止共改：以下文件在本单实施期间只归本单负责人；其他任务先改自己的独立文件，接口变动等本单合入后再接。

- `src/cli/slash_commands.hpp`
- `src/cli/slash_commands.cpp`
- `src/app/commands/command_registry.cpp`
- `src/app/commands/workflow_commands.cpp`
- `tests/unit/app/test_command_registry.cpp`
- `tests/unit/cli/test_slash_commands.cpp`

预计可删除旧重复代码：约100行；以正文口径为准。
## 全局排期复核

- 前置／文件接手次序：无硬前置；共改文件须协调。
- 总排期：[三批重构与并行交接](../07_三批重构与并行交接.md)。本节含跨组复核结果，优先于早期本组可并行提示。
- 共改任务：HC-06（src/app/commands/command_registry.cpp, src/app/commands/workflow_commands.cpp, tests/unit/app/test_command_registry.cpp）。
