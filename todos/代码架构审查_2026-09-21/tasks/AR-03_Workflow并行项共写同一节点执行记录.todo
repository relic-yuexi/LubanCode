# AR-03 Workflow并行项共写同一节点执行记录

- 状态：已修已验（PR #162，CI rollup 绿；证据见文末"完工勾账"）。
- 审查基线：`693d3ddb5607fc67f37c262b8b8873c9f7dc07eb`。
- 主批次：第2批（1=无行为变更合并；2=接口/合同收敛；3=职责拆分）。修复已证错误可在机械合并就绪后优先，不必等所有低风险清理。

## 问题表

| 编号 | 类别(A/B/C) | 严重度 | 位置(文件:行) | 现象（最短代码证据） | 为什么是问题 | 建议方案 | 影响面与风险 |
|---|---|---|---|---|---|---|---|
| AR-03 | B | 高 | src/workflow/runtime.cpp:368；src/workflow/runtime.cpp:379；src/workflow/runtime.cpp:510；src/workflow/runtime.cpp:532；src/workflow/runtime.cpp:1395；src/workflow/runtime.cpp:1400；src/workflow/runtime.cpp:1444 | `NodeRunRecord& record = account.nodes[node.id]`；map复制body但保留id；解锁后事件闭包读取 `record.attempt`。 | map多个item共用body.id，因此共用同一NodeRunRecord。worker在锁内更新attempt/时间后解锁，另一个worker可覆盖这些字段；前者在emit及失败路径又锁外读写。锁没有覆盖全部访问，且同一槽无法表达各item独立attempt，导致数据竞争及事件身份串项。 | 保留每次派发已有node_run_id/NodeExecutionIdentity，把attempt状态收进每个RunNode本地执行记录；只在明确提交口持锁更新按execution/item分开的账。节点汇总另做派生投影，闭包捕获本次attempt，不访问共享汇总槽。 | 影响map/foreach、parallel、重试和恢复。不能只给emit加锁后继续让不同item共享执行状态；磁盘execution身份和输出顺序必须原样对齐。 |

## 证据与边界

RunWithStore:1865-1866确有共享nodes_mutex，问题并非完全没锁。RunNode:510主动解锁，:565-570发started并执行；lambda:379的payload.value默认参数会先求值record.attempt，即使payload已含attempt仍发生读取。:532/:548的失败回写也在重锁:579之前。

代码证据来自当前源码静态核对；没有运行复现、编译或测试。上文后果是对应路径的推导，未声称观察到生产事故。

## 交接范围

- 负责文件：`src/workflow/runtime.cpp`；`src/workflow/runtime.hpp`。
- 前置：无硬前置；共改文件须协调。（以全局排期复核为准）。
- 不得并行共改：AR-04、AR-09；同文件先后落单，或约定具体函数所有权。
- 可并行：除上述依赖/共改任务外，其余审查任务可单独worktree推进；不要借本单批量格式化或重写无关代码。

## 迁移与验收

1. 固定本单触及合同与行为样例；对照当前源码重新确认行号。
2. 保留每次派发已有node_run_id/NodeExecutionIdentity，把attempt状态收进每个RunNode本地执行记录；只在明确提交口持锁更新按execution/item分开的账。节点汇总另做派生投影，闭包捕获本次attempt，不访问共享汇总槽。
3. 小批提交，按下列门槛验收。

- [x] 两个map item用栅栏同时执行并交错重试，事件attempt/node_run_id一一对应。
- [x] 给子账创建故障，确认所有NodeRunRecord读写受保护。
- [x] TSan在支持平台的GitHub CI无竞争。
- [x] 顺序图、map输出保序及resume沿用原合同。

## 完工勾账（2026-09-21）

- PR：[#162](https://github.com/relic-yuexi/LubanCode/pull/162)，分支 worktree-agent-a77514d40c6d3e6c4。
- 提交：2dbce681（runtime 本地账+提交口、合同夹具、TSan 腿）；660e164d（TSan 首战实锤的 trajectory 诊断口加锁）。
- 验收一：tests/unit/workflows/test_workflow_parallel.cpp 新册"map 两项栅栏交错重试"——手搓 Barrier/Latch（不靠 sleep）把两项钉在同一执行窗口，item0 重试推进、item1 收尾；断言每枚事件 attempt 与自己 node_run_id 的 -a 段一一对应、i1 started/completed 同 id。旧实现此册必红（共享槽 attempt 被 item0 推到 2）。
- 验收二：test_workflow_trajectory_runtime.cpp 新册"并发 map 撞子账故障"——workflow_node_start_fault 令两项 node stream 开张齐败，项级 fail-closed 互不串账，投影完整一笔，verify 全过；全仓 NodeRunRecord 共写收进 PublishNodeRecord 提交口（RunNode/RunLoop/RunMap/RunReduce/RunParallel/RunAsync 六处），RunParallel worker 的假锁（本地 node_mutex）删除。
- 验收三：CI 新增 workflow-tsan 腿（.github/workflows/ci.yml，macOS AppleClang TSan，只跑 unit.workflows.\*，changes 门按 workflow_threads 放行）。首跑（run 35532008586）真抓一笔：WorkflowRunBridgeImpl 三处 io_errors_ 裸 push 并发竞争——660e164d 加锁后复跑（run 35533751987）TSan 腿绿。
- 验收四：既有 12 册 workflows 单测全量回归（macos-clang 腿 ctest 全绿），顺序图/保序/resume 合同零改动；新册同场断言 map 输出保序。
- 终判：ci rollup conclusion=success（run 35533751987，2026-09-21）。
- 残留留痕：trajectory 侧 turn bridge/子代理桥共指 ledger io_errors_ 裸 vector 的既有形态不在本单范围（本单只锁 workflow 桥三口）；AR-04 预算闸接缝已留在 RunNode 收账口（runtime.cpp with_nodes_lock 段注释）。

## 删除量与旧账

本项不以删行计收益；预计净删除 0 行。（实收：净增。）

关联 todos/Workflow接入SessionV3_编排账与节点恢复设计.todo 第八节并发隔离；旧修复只让slot.output独立，当前NodeRunRecord仍共用。（本单已收口：NodeRunRecord 不再共用。）
## 全局排期复核

- 前置／文件接手次序：无硬前置；共改文件须协调。
- 总排期：[三批重构与并行交接](../07_三批重构与并行交接.md)。本节含跨组复核结果，优先于早期本组可并行提示。
- 共改任务：AR-04（src/workflow/runtime.cpp, src/workflow/runtime.hpp）；AR-09（src/workflow/runtime.cpp）。
