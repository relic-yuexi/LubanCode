# SV-11 工作区真实 manifest 的读改写缺少跨进程串行边界

- 状态：源码已实施（本地零编译零测试，验证交远端 CI）；验收门槛待 CI 证据回填勾账。
- 审查日期：2026-09-21。实施：2026-09-21（worktree-agent-ab497852aa83be3c9，自 origin/main 分支）。
- 源码基线：`693d3ddb5607fc67f37c262b8b8873c9f7dc07eb`。
- 批次：2。第一批只合并等价实现，第二批收敛合同，第三批拆职责。高优先行为缺陷应先以窄补丁处理，不必等低风险整理。
- 核验：已读源码及真实调用点；未修改源码、未编译、未跑测试。下列验收项交修复者执行。

| 编号 | 类别(A/B/C) | 严重度 | 位置(文件:行) | 现象（最短证据） | 具体后果 | 建议方案 | 影响面与风险 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| SV-11 | B | 中 | src/workspace/manifest.cpp:239<br>src/workspace/manifest.cpp:269<br>src/workspace/manifest.cpp:285<br>src/workspace/manifest.cpp:293<br>src/workspace/identity.hpp:7<br>src/trajectory/directory.cpp:170<br>src/memory/project_memory.cpp:2673 | OpenOrRegisterWorkspace 先 ReadWorkspaceManifest，向本地 manifest.checkouts 追加，再 WriteWorkspaceManifestAtomic；整段没有锁或 revision 比较。两个 linked worktree 按合同共 workspace。 | 两进程同时从同一旧 manifest 注册不同 checkout，各自原子写整份 JSON，后写会覆盖先写新增项；last_opened/last_seen 也可能回退。原子替换只防半份文件，不保护读改写。丢失发生在 workspace.json 真账，不能用 index.json 可重建缓存的容错解释。 | 在同一 workspace 的读→校验→checkout upsert→写阶段加跨进程事务锁，锁后重读；时间戳取单调最大值，或改成带 revision 的重试提交。索引仍作可重建投影，独立保留当前丢写容错，不升级为真源。 | 中：工作区创建是启动路径，应有界等待并返回 workspace.locked。锁粒度只覆盖一个 workspace，不能锁整个 home 导致各项目互相阻塞。兼容既有工作区目录命名与 shared-worktree 身份。 |

### 与索引缓存区分

`workspace/index.cpp:279-280` 明确允许可重建 index 的并发丢写；本条不否定那项权衡。问题在 `workspace.json` 自描述真账：A、B 两个 linked worktree 得到相同 identity_root/workspace_key，现有 manifest 可同时读出相同 checkouts。

交错：A 读 [main]；B 读 [main]；A 写 [main,A]；B 写 [main,B]。四次操作都成功且 JSON 完整，A 登记仍消失。消费者入口来自 trajectory 建仓和 memory 独立建仓，代码未要求只能由一个进程调用。尚未做运行压力复现，本结论是可直接复核的读改写交错。

## 交接边界

- 前置：FD-04（以全局排期复核为准）。
- 并行与禁止共改：只改 workspace manifest 事务入口，与 SV-01 的 memory 锁修复可并行，禁止直接复用其旧年龄锁。index.cpp 不在本条编辑范围；共有 platform 锁工具若需新增由集成者协调。原子写返回结果沿 FD-04 区分未提交与已替换未耐久，不把后者当作没写过而盲目重试。
- 允许修改的现有文件：

- `src/workspace/manifest.cpp`
- `src/workspace/manifest.hpp`
- `tests/unit/workspace/test_workspace_manifest.cpp`
- `tests/integration/workspace/test_worktree_shared_storage.cpp`

新增窄合同/实现文件若需登记 CMake，由修复者只追加自己一项，合并者统一核源清单；不得顺手重排整个 target。相邻功能或测试正在改同文件时，先串行合入，不另开同职责副本。

## 分步落地

1. 用以上位置复核当前 HEAD；若代码已变，更新证据再动手。
2. 冻结本条语义差异及验收夹具，再执行建议迁移。先搬等价机械，后改合同，最后删除旧入口。
3. 调用点全部迁妥后搜旧符号；公共别名若暂留，标明删除条件。
4. 只跑本条相关验证及仓库必需门禁。遵循仓库远端 CI 纪律，回报真实结果，不把计划写成已完成。

## 验收门槛

- [ ] 双进程同 workspace 并发注册不同 checkout，两边成功后 manifest 保留 union，first_seen 不被后写重置。
- [ ] 同 checkout 并发更新时间，last_seen/last_opened 不倒退。
- [ ] 一个进程持锁崩溃后可恢复；超时明确返回 workspace.locked，不能悄悄覆盖旧账。
- [ ] 不同 workspace 可同时登记；index 丢写仍可从各 workspace manifest 重建。
- [ ] 提交只覆盖本条职责；说明行为变化、回滚方式及尚未验证边界。

## 合并/删除口径

本条不是重复代码合并项；预计可删除行数记 0。职责拆分或修正合同带来的搬移，不当成删除收益。

## 既有工作关联

- tests/integration/workspace/test_worktree_shared_storage.cpp；workspace 统一存储合同及现有目录索引账。代码中 index 的已知并发容错不覆盖 manifest。
## 全局排期复核

- 前置／文件接手次序：FD-04
- 总排期：[三批重构与并行交接](../07_三批重构与并行交接.md)。本节含跨组复核结果，优先于早期本组可并行提示。
- 当前清单未见同文件冲突；新增公共头或CMake源登记先通知集成人。
