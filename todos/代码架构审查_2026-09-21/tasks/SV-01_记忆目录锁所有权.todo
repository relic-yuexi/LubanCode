# SV-01 记忆目录锁按年龄夺锁，活 Worker 失去独占权

- 状态：源码已实施，远端 CI 验证中。
- 审查日期：2026-09-21。实施：2026-09-21（worktree-agent-a870d433158aafd84，自 origin/main 分支）。
- 源码基线：`693d3ddb5607fc67f37c262b8b8873c9f7dc07eb`。
- 批次：2。第一批只合并等价实现，第二批收敛合同，第三批拆职责。高优先行为缺陷应先以窄补丁处理，不必等低风险整理。
- 核验：已读源码及真实调用点；未修改源码、未编译、未跑测试。下列验收项交修复者执行。

| 编号 | 类别(A/B/C) | 严重度 | 位置(文件:行) | 现象（最短证据） | 具体后果 | 建议方案 | 影响面与风险 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| SV-01 | B | 高 | src/memory/project_memory.cpp:402<br>src/memory/project_memory.cpp:415<br>src/memory/project_memory.cpp:424<br>src/memory/project_memory.cpp:463<br>src/memory/project_memory.cpp:569<br>src/memory/project_memory.cpp:4464 | DirectoryLock: age > stale_after 后 remove_all(path_)；WorkerLockHeld: return age <= seconds(30)；EnsureRunning 仅在 !alive.empty() && WorkerLockHeld(home) 时合并唤醒。 | 一只仍活着的 worker 连续处理队列超过 30 秒，目录 mtime 不续期。此时再次唤醒会再起 worker，后者删掉旧锁并取得同一路径。旧锁析构又会删除新持有者的锁。两只 worker 可同时枚举同一 pending 任务；主题写入、生命周期回执、failed 归档不再受原定独占约束。 | 保留 MemoryWorkerSupervisor 的句柄与退避机制，替换 DirectoryLock 的年龄判死合同。以 OS 文件锁或 PID+进程启动身份+随机 owner token 建立跨进程所有权；活持有者不得因时间长被夺锁，释放前核 owner。WorkerLockHeld 与获取/释放共用同一裁决。 | 高：涉及跨进程排队、崩溃恢复与旧锁兼容。不得只把 30 秒改大。支持旧无 owner 锁时应隔离并明报，不能猜死后直接删除。 |

### 可复核交错

1. A 取得 `memory-jobs/worker.lock`，在 `while (true)` 队列循环中连续工作。
2. 30 秒后该空目录仍保留最初 mtime；本文件不存在为这把锁续期的写时钟路径。
3. 宿主在 `EnsureRunning` 看见 A 尚存活，却因 `WorkerLockHeld` 返回 false，再起 B。
4. B 的 `DirectoryLock` 删除锁目录、重建；A 仍在处理。
5. A 退出，析构按路径删目录，不核持有者，B 的锁也随之消失。

`memory/.state/memory.lock` 在 2213、4206、4308 行同样使用这只锁。普通任务通常很快；本结论不声称已观察到生产损坏，而是给出了代码允许的长任务交错。项目级锁可能暂时挡住部分同项目写入，但它也采用 30 秒夺锁逻辑，且不能替代 worker 队列独占。

## 交接边界

- 前置：无硬前置；共改文件须协调。（以全局排期复核为准）。
- 并行与禁止共改：可与 SV-02、SV-03、SV-04、SV-05、SV-06、SV-10、SV-11 并行。SV-09 必须等本条合入；不得两人同时重排 project_memory.cpp。
- 允许修改的现有文件：

- `src/memory/project_memory.cpp`
- `src/memory/project_memory.hpp`
- `tests/unit/memory/test_project_memory.cpp`
- `tests/support/memory_worker_fixture.cpp`

新增窄合同/实现文件若需登记 CMake，由修复者只追加自己一项，合并者统一核源清单；不得顺手重排整个 target。相邻功能或测试正在改同文件时，先串行合入，不另开同职责副本。

## 分步落地

1. 用以上位置复核当前 HEAD；若代码已变，更新证据再动手。
2. 冻结本条语义差异及验收夹具，再执行建议迁移。先搬等价机械，后改合同，最后删除旧入口。
3. 调用点全部迁妥后搜旧符号；公共别名若暂留，标明删除条件。
4. 只跑本条相关验证及仓库必需门禁。遵循仓库远端 CI 纪律，回报真实结果，不把计划写成已完成。

## 验收门槛

- [ ] 用可控时钟/慢 worker 夹具跨过 30 秒：A 尚活时 B 不得取得 worker.lock，WorkerSpawnCount 不因锁年龄增加。
- [ ] A 崩溃后能恢复队列；PID 被重用或 owner 不符时，不误删新锁。
- [ ] A 旧 RAII 句柄析构不得删除 B 后来取得的锁。
- [ ] 同一 operation_id 只产生一份终态回执；任务不因两 worker 争读而误进 failed。
- [ ] 保留入队成功与 committed 回执分离，沿现有记忆写入测试及远端 CI 验收。
- [ ] 提交只覆盖本条职责；说明行为变化、回滚方式及尚未验证边界。

  - 本单证据：PR 号与 CI 结论开 PR 后回填。

## 合并/删除口径

本条不是重复代码合并项；预计可删除行数记 0。职责拆分或修正合同带来的搬移，不当成删除收益。

## 既有工作关联

- todos/记忆自动入库误报与Windows后台Worker句柄早释修复.todo:192（已落监督器及空窗补拉；本条补其长持锁边界，不重开句柄早释旧问题）
## 全局排期复核

- 前置／文件接手次序：无硬前置；共改文件须协调。
- 总排期：[三批重构与并行交接](../07_三批重构与并行交接.md)。本节含跨组复核结果，优先于早期本组可并行提示。
- 共改任务：SV-09（src/memory/project_memory.cpp, src/memory/project_memory.hpp）。
