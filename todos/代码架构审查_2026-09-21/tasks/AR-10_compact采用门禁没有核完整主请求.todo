# AR-10 compact采用门禁没有核完整主请求

- 状态：已修（PR #180，CI rollup 绿，勾账见文末）；原审查轮只审未改。
- 审查基线：`693d3ddb5607fc67f37c262b8b8873c9f7dc07eb`。
- 主批次：第2批（1=无行为变更合并；2=接口/合同收敛；3=职责拆分）。修复已证错误可在机械合并就绪后优先，不必等所有低风险清理。

## 问题表

| 编号 | 类别(A/B/C) | 严重度 | 位置(文件:行) | 现象（最短代码证据） | 为什么是问题 | 建议方案 | 影响面与风险 |
|---|---|---|---|---|---|---|---|
| AR-10 | B | 中 | src/runtime/v3_compact_runtime.cpp:1072；src/runtime/v3_compact_runtime.cpp:1227；src/runtime/v3_compact_runtime.hpp:85；src/runtime/v3_compact_runtime.hpp:131；src/app/commands/session_commands.cpp:982；src/agent/loop.cpp:158 | post_compact_budget只检查 `tokens_after + main_output_reserve_tokens`；tokens_after来自bytes/4的system、摘要、保留消息，没有tools。 | 压缩前输入允许宿主estimate槽替换，采用候选时却绕回固定bytes/4；主请求还会带工具schema及真实输出预留。候选可能被记为预算通过并applied，下一主请求又因完整输入超窗被挡，形成额外压缩或反复回退。此处不声称服务端必超窗。 | 保留压缩专用请求估算口；另给采用门禁注入完整主请求预算评估口，使用同一份工具定义、最终system、候选历史、输出预留和估算版本。将benefit用于结构收益，post_compact_budget用于真实下一请求，两种统计标注清楚。 | 影响manual/auto compact、动态工具与用户自定义estimate。不能把原始持久消息序列化字节直接当provider输入，也不能把所有预算都换成主模型窗口。 |

## 证据与边界

V3CompactProfile仅有main_window与main_output_reserve字段；会话接线:982-984也只传两项。input.estimate仅用于压缩输入容量:665-669，:1072-1076与:1227未调用它。主循环EstimateRequestInputTokensForPreflight:161-163明确累加tools，证明两道主请求容量解释不同。

代码证据来自当前源码静态核对；没有运行复现、编译或测试。上文后果是对应路径的推导，未声称观察到生产事故。

## 交接范围

- 负责文件：`src/runtime/v3_compact_runtime.hpp`；`src/runtime/v3_compact_runtime.cpp`；`src/app/commands/session_commands.cpp`。
- 前置：FD-02（以全局排期复核为准）。
- 不得并行共改：本组其余任务无同文件写入冲突。
- 可并行：除上述依赖/共改任务外，其余审查任务可单独worktree推进；不要借本单批量格式化或重写无关代码。

## 迁移与验收

1. 固定本单触及合同与行为样例；对照当前源码重新确认行号。
2. 保留压缩专用请求估算口；另给采用门禁注入完整主请求预算评估口，使用同一份工具定义、最终system、候选历史、输出预留和估算版本。将benefit用于结构收益，post_compact_budget用于真实下一请求，两种统计标注清楚。
3. 小批提交，按下列门槛验收。

- [x] 工具定义占主要窗口的夹具中，摘要本身虽小但完整主请求不合格时不得applied。（"ar10-tools-window"册：结构尺 tokens_after+8192 远在 200000 窗内——旧门禁必放行；口报 estimatedInputTokens=195000 时 validation_failed 不 applied，check 详情带"主请求评估口"口径名）
- [x] 替换estimate后采用门禁同版本。（边界册：口报 window−reserve=91808 恰放行、91809 拒收——门禁逐字消费口的数字；接线端评估口与压缩请求同走 EstimateBypassRequestTokens 同一只 PreRequest/estimate 槽，用户替换估算器对两处同版本生效）
- [x] ASCII/CJK/图片/工具结果均使用真实输入快照。（"ar10-real-snapshot"册：口收到 system=账本根"你是 LubanCode。"、messages[0]=新摘要逐字、保留尾部按链序原样——CJK 用户文本、image 块 base64 逐字、tool 消息 tool_call_id 与正文逐字；签名/加密载荷保真（B 阶段回放的就是这份），不喂 BuildCompactMaterialView 投影）
- [x] 失败保留候选且不改context链。（三形 fail closed 册：槽报错/缺 estimatedInputTokens/负数均落 post_compact_budget failed → validation_failed；applied 事件不存在、内存链原样、候选在 HistoryTimeline 可查）
- [x] GitHub CI。（PR #180，run 35586336332 rollup success：macos-clang 腿 success + install-scripts 双平台 success，其余腿 lane 按路径 skip；同 SHA 的 push 路副本 run 35586332065 是并发组去重 cancelled，非失败）

## 勾账

- 状态：已修。PR #180（https://github.com/relic-yuexi/LubanCode/pull/180），分支 worktree-agent-a1ee97912ef2d6328，提交 2dc29c19（刀+合同册+本单入库）、13271c5e/a89b85de/23395cae（CI 三轮自修，均测试/头文件小病，与刀的合同无关）。
- 修法：`V3CompactRunInput` 新增 `main_request_budget` 完整主请求预算评估口——入参=候选主请求快照（账本根基座 system+新摘要[applied 同形]+保留尾部原样载荷），出参=EST1（estimatedInputTokens 必填；可选 outputReserveTokens 压过 profile 预留，FD-02 最终出站快照只此一份）。7.9 门禁吃口的数字；口失败/形状不合 fail closed 落 failed check（候选保留、链不动）；口缺场旧路一字不变。benefit 仍走结构尺，applied 的 tokenMetric 单列 budgetGate（估算名/输入/预留/窗口）——两种统计分开标注。接线：session_commands `main_request_facts` 铸闭包，估算走 PreRequest/estimate 同一只槽（替换估算器同版本），输出预留只认 `PrepareWireRequest` 最终快照（包装层转发齐后真覆盖自动进账，HC-08 在途接缝）；controller 按 /context 同口径现场收集最终 system 叠层与暴露过滤后的 tools。
- 未动 PR #159 合同：A 阶段材料投影（BuildCompactMaterialView）/B 阶段回放裁决（V3CompactReplaySupport）原样；`estimate` 压缩请求槽（P1-C）原样保留。
- CI 证据：run 35586336332 rollup success（macos-clang success + install-scripts windows/ubuntu success，lane 按路径选腿其余 skip）。三轮自修过程账：首跑 35577964121 Build 首错——controller facts 槽把 std::function 当指针解引用；二跑 35579861291 编译错——测试册三处 VerifyV3File 引用内层块作用域外的 harness；三跑 35582563692 悬垂——EventsOf/FindValidationCheck 返回指进 ReadJsonLines 临时 vector 的指针，macos-clang 读成 null（MSVC 碰巧过的同一类病），落地具名 lines 修复。
- 行号复核：基线 693d3ddb 之后病灶仍在 v3_compact_runtime.cpp 的 7.9 段（现约 :1369 起）；session_commands 接线 :982 的 profile 两项照旧，评估口接线在其后 middleware 块（现约 :1057 起）。

## 删除量与旧账

本项不以删行计收益；预计净删除 0 行。

关联 todos/SessionV3旧设计清理_消费迁移缺口补齐与旧接口退役.todo 与 todos/session轨迹v3_消息主轴树链与四角色壳收敛设计.todo 的compact两道容量门禁。

跨组接口：FD-02调整 backend/types 与协议适配的最终请求事实。本单不再自行解释extra_body或输出上限覆盖，只消费最终快照；避免再生一套容量真值。
## 全局排期复核

- 前置／文件接手次序：FD-02
- 总排期：[三批重构与并行交接](../07_三批重构与并行交接.md)。本节含跨组复核结果，优先于早期本组可并行提示。
- 共改任务：HC-06（src/app/commands/session_commands.cpp）。
