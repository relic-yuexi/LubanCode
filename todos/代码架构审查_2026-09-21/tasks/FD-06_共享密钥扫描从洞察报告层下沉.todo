# FD-06 共享密钥扫描从洞察报告层下沉

- 状态：已实现，PR #192 CI 全绿待合并（windows-msvc/macOS-clang 双平台腿全 success，rollup success，run 35637489811；首跑 macOS 腿挂 integration.updater.updater_lock 一枚真子进程锁时序 flaky，与本单无涉，重跑失败腿后 attempt 2 全绿）。
- 日期：2026-09-21。
- 基线：`693d3ddb5607fc67f37c262b8b8873c9f7dc07eb`。
- 类别：C；严重度：中；重构批次：3。
- 前置：AR-11（以全局排期复核为准）。
- 改动所有权：下列文件由本单负责人持有；不同单命中同一文件时，按总索引串行或先交共享接口。

## 问题表

| 编号 | 类别 | 严重度 | 位置 | 现象（最短证据） | 具体后果 | 建议方案 | 影响面与风险 |
|---|---|---|---|---|---|---|---|
| FD-06 | C | 中 | src/trajectory/export_projection.hpp:30；src/trajectory/export_projection.hpp:208；src/telemetry/redactor.cpp:9；src/insights/redaction.cpp:56；src/insights/redaction.cpp:204；src/insights/friction_classifier.cpp:7 | `trajectory/export_projection.hpp` include `insights/redaction.hpp` 并调用 `insights::ScanSecrets`；同一 redaction.cpp 后半又维护 report/session-summary 的 `WalkAllowlist`。 | 通用凭据识别规则与洞察报告 schema 放在同一实现件。轨迹导出本该给分析层供事实，却反向依赖洞察层；形成 insights↔trajectory 模块环。改报告字段白名单会触碰轨迹导出依赖对象，独立打包轨迹导出也须携带洞察实现。 | 保留当前 ScanSecrets/RedactSecrets 算法，原样搬到独立无业务依赖的 privacy/secret_scan 小库或等价公共层；insights 留报告字段允许表，telemetry 留其URL/路径/数据档规则，trajectory 留导出处置策略。各域旧符号短期委托后退役。 | 迁移必须保留命中范围、类型名与替换字节；检测和处置不要混成万能Redactor。移动约200行不等于删除200行。 |

## 修改边界

- `src/insights/redaction.hpp`
- `src/insights/redaction.cpp`
- `src/trajectory/export_projection.hpp`
- `src/trajectory/harness_exporter.cpp`
- `src/telemetry/redactor.cpp`
- `CMakeLists.txt`

## 实施顺序

- [x] 先抽纯检测类型与算法，保留旧入口委托。（commit 282ff5d0：ScanSecrets/RedactSecrets/SecretKind/SecretHit/SecretKindName 自 insights/redaction.cpp 逐字节原样搬 src/privacy/secret_scan.{hpp,cpp}，namespace lubancode::privacy，零业务依赖，挂 lubancode_engine；insights 旧符号改过渡委托（using + inline 转发），行为逐字节相同。）
- [x] 迁移三个消费者，最后收旧声明。（commit a6677004：trajectory 三件（export_projection.hpp/harness_exporter.cpp/training_exporter.cpp，后者摘多余 include）、telemetry/redactor.cpp、insights/html_renderer.cpp、app 两命令（insights_commands/prompt_audit_commands）、runtime/headless_progress.cpp 全部换 privacy 入口；insights/redaction 只留报告字段允许表，旧扫描声明收口，全仓已无 insights::ScanSecrets/RedactSecrets/SecretKind/SecretHit/SecretKindName 引用（grep 证零残留）。）

## 验收门

- [x] 迁移前后同一secret语料逐字节输出相同。（commit e9608fce 先立合同册 tests/unit/privacy/test_secret_scan_contract.cpp：期望值全部写死——ScanSecrets 钉命中枚数/kind 次序/offset+length 圈回的原文片段，RedactSecrets 钉整串输出；语料盖住 header 整行锚点、环境变量赋值（含"尾巴门槛量整串"现行怪癖）、前缀令牌（尾长门槛/大小写敏感/词边界/sk-ant- 重叠单报）、私钥块头、连接串（行内 @ 才报、https 不在表）、重叠取先到、无命中透传。下沉挪册只翻 include 与 using（git 相似度 98%），期望值一字不动。）
- [x] trajectory不再include insights；insights仍可消费trajectory。（grep 证：src/trajectory 下 #include "insights/ 0 处；insights/friction_classifier.cpp 吃 trajectory/v3/envelope.hpp 的正路未动。新增守门册 tests/unit/trajectory/test_trajectory_dependency_gate.cpp 钉死——src/trajectory 任何源文件再现 insights include 即败，防环回来。）
- [x] 报告字段允许表仍归insights，telemetry路径假名化规则不变。（IsAllowedReportFieldPath/WalkAllowlist 原样留 insights/redaction.{hpp,cpp}；telemetry/redactor.cpp 只换扫描入口为 privacy::，URL sanitizer/路径假名化/UTF-8 长度帽（AR-11 刀口）三道闸一字未动；skills::RedactSecrets（workflow_recorder 一系）词表未碰，统一归 SV-12，边界已写进 secret_scan.hpp 文件头。）

- CI：实现者提交独立PR后跑仓库远程CI；本审查未运行测试。
  - 本单证据：PR #192，run 35637489811——windows-msvc 与 macos-clang 双平台腿 success（src/telemetry/* 在 windows 敏感面名单，敏感腿绿），changes/lane/install-scripts(ubuntu,windows) 全 success，linux 系腿按设计 skipped，attempt 2 rollup conclusion success。首跑 macOS 腿 integration.updater.updater_lock（真子进程持锁时序册，test_updater_lock.cpp:286，与本单 diff 无涉）单枚红，gh run rerun --failed 后全绿；本单三册相关测试（unit.privacy.secret_scan_contract/unit.insights.redaction_canary/unit.telemetry.telemetry_redactor/unit.trajectory.trajectory_dependency_gate）双平台全 Passed。

## 合并取舍与估量

应共享凭据检测；不合并三域各自的拒绝、替换、允许表策略。

预计可删除行数：0。0 表示本单不以删行为目标；移动行数不算删除。

关联既有单：未要求合并既有总设计单。

核证范围：静态调用链与源码分支核对；未编译、未运行产品测试，未声称线上复现。
## 全局排期复核

- 前置／文件接手次序：AR-11
- 总排期：[三批重构与并行交接](../07_三批重构与并行交接.md)。本节含跨组复核结果，优先于早期本组可并行提示。
- 共改任务：AR-11（src/telemetry/redactor.cpp）。
