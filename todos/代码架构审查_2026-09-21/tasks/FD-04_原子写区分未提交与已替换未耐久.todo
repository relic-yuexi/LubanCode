# FD-04 原子写区分未提交与已替换未耐久

- 状态：已实现，PR 待 CI（合同与两口迁移已落，其余调用点迁移与兼容口删除留后继）。
- 日期：2026-09-21。
- 基线：`693d3ddb5607fc67f37c262b8b8873c9f7dc07eb`。
- 类别：B；严重度：高；重构批次：2。
- 前置：无硬前置；共改文件须协调。（以全局排期复核为准）。
- 改动所有权：下列文件由本单负责人持有；不同单命中同一文件时，按总索引串行或先交共享接口。

## 问题表

| 编号 | 类别 | 严重度 | 位置 | 现象（最短证据） | 具体后果 | 建议方案 | 影响面与风险 |
|---|---|---|---|---|---|---|---|
| FD-04 | B | 高 | src/platform/atomic_write.hpp:54；src/platform/atomic_write.cpp:165；src/platform/atomic_write.cpp:176；src/config/config.cpp:3765；src/runtime/session_soul.cpp:107 | 头合同：`失败时 target 保持原样`；实现先 `ReplaceFileAtomically(temp, target)`，目录 flush 失败再 `return fail("atomic.durability_flush_failed", ...)`。 | 同一 unexpected 同时表示“旧文件未动”与“新文件已可见”。底层已有专用durability_flush_failed码，但头合同没列出这一例外；配置写口又把它降成字符串，上层仅看has_value便无法按统一合同决定回滚内存或报告已提交。session_soul仍沿用失败保留旧文件承诺。刷盘返回失败只表示耐久未确认，不证明物理数据一定未落盘。 | 保留单份平台原子写。把结果收成提交阶段与耐久状态，区分not_committed、committed_durability_not_requested、committed_durability_unconfirmed、committed_durable；或在现有结构化错误里增committed及durability字段，并让上层保留它们。调用点按阶段处理，不能把换名后的失败当未写盘；AtomicVisibility成功也不能标为已确认耐久。禁止靠删新文件回滚。 | 影响 config、会话快照、Workflow/更新器事务账。先用可注入平台失败点验证，不凭 Windows/Linux 实测猜测目录 flush 总会成功。 |

## 修改边界

- `src/platform/atomic_write.hpp`
- `src/platform/atomic_write.cpp`
- `src/config/config.cpp`
- `src/runtime/session_soul.cpp`
- `tests/unit/platform/test_atomic_write.cpp`

## 实施顺序

- [x] 先定义并记录提交阶段，不破坏正常写入。（本单 PR：`WriteOutcome` 四态 + `AtomicWriteReceipt` 成功回执 + `AtomicWriteError.outcome/failure_kind` 两字段；全部既有调用点只消费 has_value/布尔，源兼容零改动。）
- [x] 迁移公共配置写口与会话快照，再逐个迁其余耐久调用；分PR但串行交付接口。（本 PR 迁 `WriteConfigObjectAtomicPhased` 与 `WriteSessionSoulSnapshotPhased` 两口并留兼容口；其余 ProcessCrashDurability 调用(reply_outbox/workflow account/updater txn/layout/engine/qq_spool/qq_menu/manager/channel_media/channel_file_delivery/headless_executor/session_service/channel_connection_reporter/im_preferences/trajectory harness_exporter)待后继 PR 逐个迁。）
- [ ] 所有使用者迁完后删歧义返回口。（两个兼容口已标注"FD-04 收尾、调用方迁完 Phased 后删"。）

## 验收门

- [x] 注入写临时件失败、替换失败、替换后目录刷盘失败，分别断言磁盘内容与结果阶段。（test_atomic_write.cpp：`SetFileFlushFailureForTest`/`SetDirectoryFlushFailureForTest` 注入面 + 换名前失败/注入文件刷盘/注入目录刷盘三景，盘面与 outcome 逐格断言。）
- [x] 审查所有 ProcessCrashDurability 调用，按阶段选择内存提交与告警。（审查完成：调用清单见 PR body；各调用点的按阶段处置随各自迁移 PR 落，本单立稳枚举/回执合同作接缝。）
- [x] 相对路径无 parent 的耐久合同也要钉清：当前 FlushParentDirectory 空路径直接成功。（裸文件名测试钉死：无父段时文件数据照刷、目录一步无路径可开按合同视为已确认，回 `CommittedDurable`。）
- [ ] HC-07 的保存失败分支按新合同验收，不能盲目回滚成旧内存。（后继单 HC-07 消费 `WriteConfigObjectAtomicPhased` 的 outcome。）

- CI：实现者提交独立PR后跑仓库远程CI；本审查未运行测试。
  - 本单证据：PR 号与 CI 结论开 PR 后回填。

## 合并取舍与估量

只处理本单钉住的合同，不借题改邻接模块。

预计可删除行数：0。0 表示本单不以删行为目标；移动行数不算删除。

关联既有单：HC-07 须消费本单合同；更新器/Workflow/Channel 调用由各域负责人迁，勿跨任务同时改。

核证范围：静态调用链与源码分支核对；未编译、未运行产品测试，未声称线上复现。
## 全局排期复核

- 前置／文件接手次序：无硬前置；共改文件须协调。
- 总排期：[三批重构与并行交接](../07_三批重构与并行交接.md)。本节含跨组复核结果，优先于早期本组可并行提示。
- 共改任务：HC-01（src/config/config.cpp）；FD-05（src/config/config.cpp）。
