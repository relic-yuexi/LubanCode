# AR-11 UTF8字节截断重复并已出现半字符

- 状态：已修（PR #169，CI 绿，待合并）；两笔提交：0b181f4f 纯合并（无行为变更）、34f430af 半字符行为修复。
- 审查基线：`693d3ddb5607fc67f37c262b8b8873c9f7dc07eb`。
- 主批次：第1批（1=无行为变更合并；2=接口/合同收敛；3=职责拆分）。修复已证错误可在机械合并就绪后优先，不必等所有低风险清理。

## 问题表

| 编号 | 类别(A/B/C) | 严重度 | 位置(文件:行) | 现象（最短代码证据） | 为什么是问题 | 建议方案 | 影响面与风险 |
|---|---|---|---|---|---|---|---|
| AR-11 | A | 中 | src/platform/text_encoding.cpp:162；src/tools/web_fetch.cpp:191；src/tools/search_ripgrep_run.cpp:330；src/runtime/turn_item.cpp:240；src/runtime/channel_media_service.cpp:38；src/telemetry/redactor.cpp:138；src/api/types.cpp:77 | 已有Utf8PrefixBoundary；多个消费者仍手写回退；渠道cut=0后恢复max_bytes，遥测直接 `value.resize(256)`。 | 遥测D1文本在256字节切中多字节字符时产生无效UTF8；随后JSON编码可拒绝或丢内容。渠道预览预算小于首字符字节宽时也会返回半字符。其他正确副本仍要求同一边界修正多处同步。 | 统一调用platform::Utf8PrefixBoundary；按需要补string_view重载或用现有string实例，避免为边界查询复制长文。保留每个消费者自己的长度帽、尾缀和统计；删除渠道end==0回退。码点/显示列截断继续独立。 | 影响搜索行预览、文件diff摘要、渠道附件、provider错误及遥测。不能把字节帽改成码点帽，不能令[TRUNCATED]计数或provider尾缀消失。 |

## 证据与边界

确定性反例：256字节遥测帽对257字节文本且第256字节位于汉字/emoji中间时直接resize，保留了残缺序列。渠道TruncateUtf8("中",1)：回退到0后又end=1，返回首字节。tools/tool_search.cpp的max_chars按码点计数，不在合并范围。

代码证据来自当前源码静态核对；没有运行复现、编译或测试。上文后果是对应路径的推导，未声称观察到生产事故。

## 交接范围

- 负责文件：`src/runtime/turn_item.cpp`；`src/tools/search_ripgrep_run.cpp`；`src/runtime/channel_media_service.cpp`；`src/telemetry/redactor.cpp`；`src/api/types.cpp`；`src/platform/text_encoding.hpp`；`src/platform/text_encoding.cpp`。
- 前置：无硬前置；共改文件须协调。（以全局排期复核为准）。
- 不得并行共改：AR-05；同文件先后落单，或约定具体函数所有权。
- 可并行：除上述依赖/共改任务外，其余审查任务可单独worktree推进；不要借本单批量格式化或重写无关代码。

## 迁移与验收

1. 固定本单触及合同与行为样例；对照当前源码重新确认行号。
2. 统一调用platform::Utf8PrefixBoundary；按需要补string_view重载或用现有string实例，避免为边界查询复制长文。保留每个消费者自己的长度帽、尾缀和统计；删除渠道end==0回退。码点/显示列截断继续独立。
3. 小批提交，按下列门槛验收。

- [x] 每个偏移0..4切汉字/emoji：结果永远有效UTF8且正文不超字节帽。（test_unicode_boundaries：混排全帽扫描 0..size+2，合法/不超帽/必为原串前缀三断言）
- [x] 遥测SanitizeText结果可由JSON严格序列化。（test_telemetry_redactor：86 汉字 258 字节过 256 帽，CHECK_NOTHROW(json(out).dump())）
- [x] 已有尾缀/manifest计数保持。（[TRUNCATED] 尾缀、truncated_fields==1、"...(截短)" 路为纯委托不动）
- [x] 搜索与渠道都覆盖首码点大于预算。（test_search_ripgrep_run：TruncateUtf8Boundary("汉",1/2) 为空；test_channel_media：预览帽 2 切"汉" prompt_line 整体合法 UTF-8）
- [x] remote CI。（PR #169 pull_request 跑 35547083041 completed success：windows-msvc 46m49s pass、macos-clang 22m44s pass、changes/lane/docs/install-scripts 全 pass；push 跛 run 被并发组砍属常态，PR #167 同款）

## 勾账证据（2026-09-21）

- 交付：PR #169（base main，分支 worktree-agent-a189686cea1056b10），提交 0b181f4f + 34f430af。
- 修法与单子一致：platform 两把尺收 string_view、新增 TruncateUtf8Prefix 整刀；turn_item/ripgrep/api(types) 三处纯委托（golden 钉字节不变）；渠道删 end==0 回退、遥测 resize 落在 Utf8PrefixBoundary。
- 未夹带：cli::TruncateUtf8Bytes 与 tool_search 码点计数截断留原样（后批）；redactor 只换刀口不动脱敏规则（ScanSecrets 归 FD-06）。

## 删除量与旧账

预计净删30行：turn_item、ripgrep、channel、api/types四组重复回退循环共约40行，改成公共调用约8-10行；遥测修正不计删行。

当前 todos 未找到直接承接此缺口的专单。

跨组排程：本单先改 telemetry/redactor.cpp 的字节截断，再由 FD-06 下沉 ScanSecrets；两单不得同时改这份文件。脱敏词表、规则与底层归属由 FD-06负责，本单只换截断边界。
## 全局排期复核

- 前置／文件接手次序：无硬前置；共改文件须协调。
- 总排期：[三批重构与并行交接](../07_三批重构与并行交接.md)。本节含跨组复核结果，优先于早期本组可并行提示。
- 共改任务：AR-05（src/runtime/turn_item.cpp）；FD-02（src/api/types.cpp）；FD-06（src/telemetry/redactor.cpp）。
