# HC-07：统一Provider配置的持久提交与内存发布顺序

状态：已修复(PR #170,CI rollup 绿,证据见下)。本次实施完成,未本地 build/ctest,验证交 CI。

基线:`693d3ddb5607fc67f37c262b8b8873c9f7dc07eb`→实施基线 `eefb8589`(含 FD-04 PR #160、HC-01 PR #165)。

| 编号 | 类别(A/B/C) | 严重度 | 位置(文件:行) | 现象(最短代码证据) | 具体后果 | 建议方案 | 影响面与风险 |
|---|---|---|---|---|---|---|---|
| HC-07 | B | 高 | src/app/commands/settings_commands.cpp:1415<br>src/app/commands/settings_commands.cpp:1459<br>src/app/commands/settings_commands.cpp:1485<br>src/app/commands/settings_commands.cpp:1044<br>src/app/commands/settings_commands.cpp:1600 | native_web_search/extra_body/extra_header先SetProvider*(config.providers)，随后保存；保存失败直接return。Auth补救和Remove则先保存成功再改内存。 | 同为/provider写操作，失败语义相反。磁盘写失败时Provider条目已变，活跃顶层Config及backend尚未变，列表/下一次切换读取新值而当前请求仍用旧值；返回“设置失败”却留下会话内变更。 | 统一构造候选后再持久提交；区分尚未替换与已替换但未确认耐久，再按真实盘面发布Provider、有效Config及缓存版本。替换前失败不发布；已替换错误不能盲目回滚内存。 | 和HC01共用settings_commands.cpp，必须串行，并依赖FD-04明确原子写结果。配置源优先级、写回目标保持；ExecuteProviderSwitch本场切换但记忆失败的独立合同保持。 |

职责依据：同一命令族负责持久配置提交，但分支分别安排内存、磁盘、有效连接的更新时间。先后次序不同，错误回执无法表达真实状态。

最短证据：
```cpp
// settings_commands.cpp:1459-1466
if (!config::SetProviderExtraBody(config.providers, command.name, parsed)) { ... }
const auto saved = config::SetProviderExtraBodyInGlobalConfig(command.name, parsed);
if (!saved.has_value()) {
    TermOut() << trf("cmd.provider.set_failed", saved.error()) << "\n";
    return;
}
// :1473-1475 在成功之后才更新活跃镜像
if (active_provider == command.name) {
    config.extra_body = parsed;
    real_backend.Rebuild(config);
}
```

相同顺序见 native_web_search `:1415-1432`、extra_header `:1485-1506`。相反，auth补救 `:1044-1050` 先落盘，失败返回以后才改内存；Remove `:1600-1606` 也先存后改。候选先存后发的流程可以收敛，但不能把当前保存函数的所有unexpected都当成“磁盘没变”。

静态可达场景：全局配置路径导致替换前写入失败；执行 `/provider set A extra_body ...`。命令显示失败，但config.providers中A已更新，config.extra_body及real_backend仍旧。之后查看/切到A可以采纳这份未保存条目。这个后果不依赖网络、线程或模型回应。

公共写入合同另有前置：`config.cpp:3765-3770`用ProcessCrashDurability；`platform/atomic_write.cpp:172-176`在rename成功后仍可能因父目录flush失败回unexpected，此时盘面已经换新。FD-04负责明确结果阶段，本单据阶段发布或重新读取盘面；禁止一律 `!saved => rollback-memory` 再造反向分叉。

是否应合并：持久设置的发布步骤应收敛。同为Provider切换的 `ExecuteProviderSwitch` 则明确区分本场切换与记住默认端失败，会给回执；不能为了本单把它改成“落盘失败就禁止会话切换”。

实施步骤：
- [x] 用候选副本跑字段校验，校验不接触live providers。
- [x] 接FD-04提交阶段回执：替换前失败不发布；替换成功且耐久确认后正常发布；已替换未确认耐久按明确策略对齐盘面并提示，不冒充没变。
- [x] 三条Set分支采用相同流程；检视Auth/Edit/Remove是否能复用，但保留各自交互内容。
- [x] 分别注入替换前失败、替换后目录flush失败：前者各内存版本保持，后者不得与实际盘面分叉；发布时按HC01刷新缓存。

验收：回执明确未提交、已替换未确认耐久、成功三种结果；磁盘、Provider条目及有效连接依同一提交阶段收口；非活跃端编辑不会偷偷改变当前后端；下次角色调用采用已发布版本。删除行数记0，修复价值不依赖代码行数下降。

落码纪要（2026-09-21）：
- config 层立 `ProviderCommitReceipt`（src/config/config.hpp）：成功带 `platform::WriteOutcome` 分档（CommittedDurable / CommittedDurabilityUnconfirmed）+ 落盘路径；失败（unexpected）一律未提交、盘面未动。`UpdateProvidersInConfigFile` 由裸 ofstream 截断写改走 `WriteConfigObjectAtomicPhased`——换名后父目录刷盘失败映射为“已替换未确认耐久”的成功回执，盘面已是新内容，不冒充没变，调用方照常发布、不得当未写盘回滚。Add/Remove/Set×4/Auth×3/Replace 全家族签名统一换回执，收尾统一 `return UpdateProvidersInConfigFile(...)`。
- settings_commands.cpp 立统一发布口 `PublishCommittedProviderConfig`：/provider set 三分支、auth 家族、补救页三处（含原先“内存先改、落盘失败即返回”的 inline key 持久分支——审查表未点到的一处同病）、add（向导+一行式）/edit/remove 全走它。合同：未提交 → publish 不执行、内存一字不动；已提交 → 发布 providers 条目 → 活跃端镜像 → backend Rebuild；未确认耐久另打一行如实提示（i18n 新增 cmd.provider.commit_unconfirmed，zh/en）。
- doctor 探针写回倒正次序：候选副本（MakeStreamUsageCandidate）先改并落盘，提交成了才发布进 live 列表与 config.stream_usage；项目级路径不再拿 live 列表直接写盘。cli_app 初装向导与 assistant_host config/model/set 同步换回执（后者未确认耐久在 JSON 回执带 durability=unconfirmed）。
- 边界（明确不动）：ExecuteProviderSwitch 的“本场切换成功但记忆失败只另报一行”独立合同保持；SetActiveProviderInGlobalConfig / UpdateProviderModelInConfigFile（单键手术式改写）签名与行为不变，留后续单子。发布时按 HC-01 的连接指纹惰性失效刷新角色后端缓存，不点名重建。
- 合同测试先于动刀（tests/unit/app/test_app_commands.cpp，五例）：未提交（注入换名前文件刷盘失败）钉盘面字节级原样+内存三处不动+回执报失败（extra_body/auth/remove 三路）；已替换未确认耐久（注入换名后目录 flush 失败）钉盘面已新+内存照发+回执如实行；耐久确认钉正常一路；非活跃端编辑钉当前后端镜像一毫不动；栅栏全用原子写注入旗（RAII 还原），不靠 sleep。

验收证据（2026-09-21）：
- PR：https://github.com/relic-yuexi/LubanCode/pull/170
- CI：run 35550057693（worktree-agent-adf648b27ced70f9e 分支，pull_request 事件）rollup=success；macos-clang 腿 Test 602 例全过（含 unit.app.app_commands，本单五例在内）。两轮返工：首跑 35547338074 红——Remove case 作用域立裸声明，clang 不许后续 case 跨声明跳转（MSVC 宽松），c463a048 套块大括号收进作用域；二跑 35548392800 红——非活跃端用例断言 config.base_url 却没初始化基线，判据自身毛病，2c38c655 补一行初始化，源码零改动。
- 回执三档落地：未提交=set_failed 行；已替换未确认耐久=cmd.provider.commit_unconfirmed 行+正常收尾；成功=正常收尾。三档断言均在合同测试里钉死。

相关已有 TODO：`../../AppServer模型连接解耦_协议配置与会话隔离设计.todo` 已要求连接与配置更新有明确来源，本单仅修现有CLI持久设置提交顺序。

## 交接边界

- 批次：第2批。
- 前置：FD-04、HC-01（均已合入）。
- 并行：可与HC-02、HC-03、HC-04并行。
- 禁止共改：以下文件在本单实施期间只归本单负责人；其他任务先改自己的独立文件，接口变动等本单合入后再接。

- `src/app/commands/settings_commands.cpp`
- `src/app/commands/settings_commands.hpp`
- `tests/unit/app/test_app_commands.cpp`

本单另动（前置已合、批次表内无在途冲突，FD-05 第三批以本单为前置）：`src/config/config.hpp`、`src/config/config.cpp`（ProviderCommitReceipt 与全家族签名）、`src/app/commands/doctor_commands.cpp`（探针写回次序）、`src/app/cli_app.cpp`、`src/app/assistant_host.cpp`（回执机械适配）、`src/cli/i18n.cpp`（新词条）。

预计可删除旧重复代码：约0行；以正文口径为准。
## 全局排期复核

- 前置／文件接手次序：FD-04、HC-01
- 总排期：[三批重构与并行交接](../07_三批重构与并行交接.md)。本节含跨组复核结果，优先于早期本组可并行提示。
- 共改任务：HC-01（src/app/commands/settings_commands.cpp, tests/unit/app/test_app_commands.cpp）；HC-06（src/app/commands/settings_commands.cpp, src/app/commands/settings_commands.hpp）。
