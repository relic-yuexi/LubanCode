# HC-02：让UI事件出队次序与提交次序一致

状态：已修复。PR #177，CI run 35568060667 rollup 绿（macos-clang 全量 605/605 通过，unit.app.session_ui_dispatcher 与 unit.app.terminal_turn_sink_projection 两册含新题各 Passed）。

基线：`693d3ddb5607fc67f37c262b8b8873c9f7dc07eb`；先完成职责表，再据实际调用判定。

| 编号 | 类别(A/B/C) | 严重度 | 位置(文件:行) | 现象(最短代码证据) | 具体后果 | 建议方案 | 影响面与风险 |
|---|---|---|---|---|---|---|---|
| HC-02 | B | 高 | src/app/session_ui_dispatcher.cpp:130<br>src/app/session_ui_dispatcher.cpp:159<br>src/app/session_ui_dispatcher.cpp:218<br>src/app/interactive_session_assembly.cpp:726<br>src/cli/console_input_turn_listener.cpp:70<br>src/app/terminal_turn_sink.cpp:235 | ConsumerMain 先把 pending_ 搬到私有 batch，再逐条取 commit_mutex_；RunSync 同时 StealPending，再逐条取同一锁。 | 锁只保证不同时画，不能保证先提交先执行。旧批被消费者拿走但未落笔时，输入线程能先处理新批；工具完成事件若抢在开始事件前，会在 open_tools_.end() 分支直接丢弃，视图及录制可能只剩开始没有结束。 | 让所有队列出队与实际提交经过同一顺序闸；保持同步体在调用线程执行的合同，或在明确锁序后改为单消费者屏障。不能仅把逐条锁挪到整批外层，那仍挡不住新批先抢锁。 | 输入监听、切页、工具结束、footer心跳均经过此口。必须保留菜单读键留调用线程、渲染互斥、Quiesce在飞记账及退出排干，避免为FIFO引入锁循环。 |

职责依据：SessionUiDispatcher 宣称统一事件与动作提交次序。它同时允许消费者线程、RunSync 调用线程从同一队列领走不同批次，执行锁却在领走以后才拿。

最短证据：
```cpp
// session_ui_dispatcher.cpp:136-138
RunEntriesOnCaller(StealPending());
std::lock_guard<std::recursive_mutex> commit(commit_mutex_);
body();
// :159-161，每一条才加锁
for (Entry& entry : entries) {
    std::lock_guard<std::recursive_mutex> commit(commit_mutex_);
    ExecuteEntryLocked(entry);
}
// :232-240，消费者先搬完队列，再执行
batch.push_back(std::move(pending_.front()));
pending_.pop_front();
// 释放 queue_mutex_ 后 RunEntriesOnCaller(std::move(batch))
```

真实并发入口：`TurnInputListener` 在 `console_input_turn_listener.cpp:70` 起线程；`ThreadMain` 内多次调 RunUiSync（如 `:234`）。`interactive_session_assembly.cpp:726-727` 把它接到同一 `ui_dispatcher_.RunSync`。另一方面，`SessionUiDispatcher::Start` 自行起消费者，`TerminalTurnSink::Emit` 向它投事件。

无需假设同时画屏即可成立的交错：
1. 消费者领走旧批 `[tool.started]`，尚未拿到提交锁。
2. 模型/工具侧提交新批 `[tool.completed]`。
3. 输入监听调 RunSync，领走新批并先拿到提交锁。
4. `TerminalTurnSink::ApplyEvent` 在 `:235-237` 查不到 open_tools_，跳过终态。
5. 消费者补交开始事件，留下未关闭条目。

这是静态可达交错，不冒充真机已复现。既有 `test_session_ui_dispatcher.cpp:101` FIFO 测试只让消费者排队；`:160` 的 RunSync 测试没有控制“旧批已出队、未提交”窗口；`:183` 只验证同时执行数不超过1，不能证明次序。

实施步骤：
- [x] 在出队/提交边界增加可控测试调度点，用屏障钉住上述窗口，不靠 sleep 碰运气。
- [x] 选择一套执行所有权与锁序；所有消费入口共享它，动作与事件同序。
- [x] 同时覆盖 RunSync body、Flush、Stop、递归同步调用与 Quiesce。
- [x] 用工具开始/结束事件验收完整终态，不只比较渲染并发数。

验收：提交序号单调应用；同步体开始前，其边界前事件全部已应用；开始/结束对不丢；Stop/Quiesce 不提前返回；原有调用线程要求与中断响应保留。源码改动限定调度器及其测试，不顺手拆终端绘制系统。

## 修复纪要(2026-09-21)

- 病根：`StealPending`/消费循环先把 pending_ 搬走，执行锁在搬走之后才拿——消费线程与 RunSync 调用线程各领一批，提交锁只保证不同时画，保证不了先后。
- 修法：加一道顺序闸 `order_mutex_`（递归，恒为最外层锁）。出队与执行捆成一个闸单元：消费线程的每一批、RunSync/Flush 的"排干+body"、Stop 的收尾排干、停表后 PostAction 的就地执行，一律先过闸再领队列，领完在闸内跑完才放闸。谁领走的批谁落笔，后来者在闸上等旧批落定，先提交先执行由此成立。锁序铁律：顺序闸 → 队列锁 → 提交锁 → stdout/登记簿。
- 合同保留：RunSync 仍在调用线程执行（过闸等在飞批落定，与旧款等提交锁同一量级的有界等待）；菜单读键留调用线程；Quiesce 在飞记账判据不动；IsUiThread/世代登记/合并语义不动。顺带修掉停表后 PostAction 在队锁内就地跑的自锁隐患（嵌套投递会死锁），改走闸+提交锁。
- 测试钉子：`SetDebugHook(DebugPoint::ConsumerBatchStolen)`——消费线程"批已出队、第一枚未提交"的病窗上开放屏障，单测钉确定性交错，不靠 sleep；生产恒空。
- 测试三册：
  - `test_session_ui_dispatcher.cpp` 屏障册：钉住病窗，start→done→body 一笔不倒；
  - 同册压力形：3 写者×40 笔（事件/动作混投）+30 次 RunSync body，提交序与执行序逐位相等；
  - `test_terminal_turn_sink_projection.cpp` 终态册：真 sink 配对——开始事件卡在消费者手里时投终态+RunSync，视图账工具条目 Succeeded、result_text 落账、修订号恰两笔（旧款终态被 open_tools_ 空查丢弃，只剩未关闭的开始）。
- 文档：`docs/development/tui-write-paths.md` 锁表补顺序闸一行，锁序铁律与 RunSync 条目同步改口径。
- 改动面：调度器本体两文件 + 两测试册 + 文档，未越 cli/app 其他地盘。

相关已有 TODO：`../../TUI主子代理视图彻底隔离_单写者与按代理状态投影.todo` P2/P3 标为已落，本单是当前实现仍存在的顺序缺口，不重复立“整个单写者尚未实现”的旧账。

## 交接边界

- 批次：第2批。
- 前置：无硬前置；共改文件须协调。（以全局排期复核为准）。
- 并行：可与修改文件不相交的HC任务并行。
- 禁止共改：以下文件在本单实施期间只归本单负责人；其他任务先改自己的独立文件，接口变动等本单合入后再接。

- `src/app/session_ui_dispatcher.hpp`
- `src/app/session_ui_dispatcher.cpp`
- `tests/unit/app/test_session_ui_dispatcher.cpp`
- `tests/unit/app/test_terminal_turn_sink_projection.cpp`

预计可删除旧重复代码：约0行；以正文口径为准。
## 全局排期复核

- 前置／文件接手次序：无硬前置；共改文件须协调。
- 总排期：[三批重构与并行交接](../07_三批重构与并行交接.md)。本节含跨组复核结果，优先于早期本组可并行提示。
- 当前清单未见同文件冲突；新增公共头或CMake源登记先通知集成人。
