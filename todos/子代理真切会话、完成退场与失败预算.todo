# 子代理真切会话、完成退场与失败预算

状态：待做（子代理交互修漏）

## 为何另开一单

《子代理导航贴底并整帧去重》已经写明目标：导航坞放在输入区下方；Enter 切到子代理 transcript；详情不得缀在列表后；每一帧只留一份。

现行实现只做了半截。位置挪了，边界却没立住。用户实跑后又撞见四桩错：

1. Enter 仍在导航坞下展开长详情，没有切换上方会话。
2. 后台任务一完成，旧输入框与代理坞被推入滚屏；新输入框又画一份。
3. 代理行只写“失败”，看不出是轮数耗尽、空结论、接口报错，还是用户中止。
4. 主代理默认不限轮，子代理偏生暗藏 `40` 轮硬闸。模型撞墙后又擅自加到 `120`，烧钱不治病。

故此另开修漏单。旧单管布局总账，本单管会话切换、底栏交接、失败语义与执行预算。两单不可混销。

## 现场一：Enter 仍是假切换

截图中，用户选中 `general-purpose #1` 后，屏上仍是主会话正文。导航坞下面又铺出：

```text
任务标题：提取 docs 事实清单
[后台]
任务说明：...
工具调用流水：...
```

这不是“查看子代理会话”，只是把详情塞进底栏。

代码也照此走：

- `src/cli/agent_panel.hpp` 仍保存 `detail_open`。
- `src/cli/agent_panel.cpp` 仍产出 `DetailHint`、`DetailLine`。
- `src/cli/console_input.cpp` 调 `BuildAgentTaskDetail()`，再把详情行算进 `agent_dock_rows`。
- `src/app/interactive_session.cpp` 只拼 prompt、来源、工具流水，没有把上方 transcript 的数据源换掉。

这条路须拆。导航坞只管导航，不装长正文。

### 目标状态

会话层只留一枚真状态：

```text
viewed_task_id = 0        -> 上方显示 main transcript，输入发给 main
viewed_task_id = task_id  -> 上方显示该任务 transcript，输入发给该任务
```

由它一口气推出三样东西：

```text
visible_transcript
composer_target
viewed_row_marker
```

不可三处各记一份。否则视图看着 #2，消息却送给 main，迟早串话。

按键规矩：

```text
↑/↓    composer 为空时移动选择
Enter  消费本次按键，设置 viewed_task_id；不得顺手提交草稿
Esc    viewed_task_id 清零，回 main；草稿按既定归属保存
x      运行中任务停止；终态任务清除
```

切到子代理后，导航坞仍贴在最下方。所选行常亮。上方正文整块换源。完整 prompt、工具调用、结果与错误都在会话视口里看，不能向导航坞下方生长。

Claude Code 官方文档明说：agent panel 位于 prompt input 下方；上下选择，Enter 打开 teammate transcript，并可直接给该 teammate 发消息。它不是“树后展开详情”。可借它的组件边界，不照抄代码与配色。

参考：

- <https://code.claude.com/docs/en/agent-teams>
- <https://github.com/anthropics/claude-code/issues/24245>
- <https://github.com/anthropics/claude-code/issues/50139>

## 现场二：后台完成后，旧输入框没有退场

截图里先留下一份：

```text
────────────────
> 键入并回车
────────────────
确认模式 ...
代理导航 ...
```

主会话收到后台结果后，下方又画一份。往上一翻，两副底栏都在。中间还夹着：

```text
[后台子代理完成,结果交回主会话继续]
```

根子已经露出：

- `src/cli/console_input.cpp` 的 idle wake 路径只把光标挪到旧 body 尾部，打印一个换行，随即返回。
- 它没有擦掉或正式收束上一帧 `BottomChromeFrame`。
- `src/app/interactive_session.cpp` 随后把完成提示写成永久正文，再跑一轮主代理。
- 流式 footer 接手，回到空闲后 composer 又重画。旧帧便成了滚屏里的“小尾巴”。

### 底栏所有权

把状态转换写成一条路：

```text
IdleComposer
  -> wake
  -> RetireIdleChrome
  -> NoChrome
  -> StreamingFooter
  -> RetireStreamingChrome
  -> IdleComposer
```

每次交接先清旧账，再交所有权。任何时刻只能有一名 owner。不得靠 `\n`、空行、自然滚屏或整屏清空遮丑。

后台完成提示有两条可走：

1. 作为短暂进度行，下一帧覆盖掉；
2. 作为有归属的 transcript 事件，写入 main 会话历史。

不可夹在两副 chrome 中间，变成一行来路不明的永久字。

## 现场三：“失败”把几种病混成一团

本次实跑至少出现两种不同失败：

```text
#2  已达配置的轮数上限(max_turns=40)
#1/#3 子代理没有给出文本结论
```

前者是预算耗尽。后者只是兜底文案：最后一条 assistant 消息没有 text，`AgentTool` 便报“没有给出文本结论”。它没有保住 `stop_reason`、最后工具状态、上游错误与部分结果。用户只看见一枚 `失败`，无从下手。

### 结构化结果

子代理不得只交 `text + is_error`。至少留下：

```text
TaskOutcome
  status: completed | failed | stopped | budget_exhausted
  reason_code: api_error | max_turns | max_context | no_final_text |
               tool_error | user_stop | protocol_error
  message: 给人看的短说明
  partial_result: 已取得的事实或最后检查点
  stop_reason: 模型原始 stop reason
  last_tool: 最后一次工具名与结果摘要
  turns_used / turn_limit
  token_usage / elapsed
```

导航坞只放短因：

```text
失败 · 接口报错
耗尽 · 40/40 轮
停下 · 用户中止
失败 · 未交结论
```

Enter 切进该 transcript 后，再看完整错误、最后几轮、部分结果与可重试动作。

代理已经找到东西，临门撞限，也要把部分结果带回来。不得把四十轮探索一笔勾销。

## 现场四：子代理暗藏 40 轮硬闸

这不是用户配置出来的。

现行两套默认值互相打架：

```text
src/config/config.hpp          kDefaultMaxTurns = 0  // 主代理不限轮
src/app/tool_runtime.cpp       default_max_turns=40  // 子代理写死 40
src/tools/agent_tool.hpp       构造器声明默认 15
```

文档与配置测试都说 `0` 表示不限轮。实际派子代理时，`ToolRuntime` 却绕开配置，硬塞 `40`。所以用户没设上限，也会在第四十轮撞墙。

### 改法

1. 删掉 `ToolRuntime` 里的魔数 `40`。
2. 子代理默认值从明确配置来。首选新增 `subagent.max_turns`；未设时继承 `config.max_turns`。
3. `agent(max_turns=...)` 仍可按单项覆盖；`0` 的语义全路一致，都是不限轮。
4. 派出时便把预算写进任务快照。面板与 transcript 可见，不能等撞墙才揭晓。
5. 剩三轮时要求代理收口：停止漫游，写检查点，交部分结论。
6. 到限后返回 `budget_exhausted`，不是笼统 `failed`。
7. 主代理重试时，不得只把 `40` 改成 `120`。先读失败原因，缩小范围，拆任务，续接检查点。

硬闸可以留，暗闸不可留。`0` 也不是万灵药。真正预算至少要能分开管：

```text
轮数
累计输入/输出 token
墙钟时间
工具调用数
上下文占用
```

首期不必五样齐上。轮数来源、展示、收口、部分结果四件事须先做全。

## 长任务还缺 compact

`src/tools/agent_tool.cpp` 现有注释说，子代理“短命任务用不上”自动 compact。如今一只代理能跑 40 轮，重试又涨到 120 轮，这个前提已经倒了。

截图中 `337.7k tokens` 是多轮累计用量，不等于单次请求装进了 337.7k 上下文。可每轮重送旧历史，账仍会越滚越大。没有 compact，代理读得慢，费用也涨。

长任务须加检查点式压缩：

- 完整保留任务目标、约束、用户后续消息、未完成事项与最近若干轮。
- 文件反复搜索、同一路径多次读取、相同工具结果按稳定键去重。
- 旧探索压成“查过哪里、得出什么、排除什么、证据在哪”。
- 保留工具错误与关键原文，不能只剩一段漂亮话。
- compact 前后写可回溯边界；失败时能把最近检查点交回 main。

这部分可复用上下文压缩专单与现有 compact 代码。本单只要求子代理接上线，不另造第二套摘要协议。

## 队列与当前会话须同账

进入某只子代理后，输入文字有两种去处：

```text
代理空闲       立刻开它下一轮
代理正忙       入该 task inbox，在安全边界送达
```

安全边界指下一次模型请求组装前，或当前工具调用回合收束后；不能把半条消息硬插进正在执行的工具参数。队列行须带目标 task id、真正短标题、排队数与送达状态。

返回 main 后，新输入只归 main。旧的子代理队列不能跟着焦点漂移。

## 实施次序

### 第一刀：拆掉详情行

- 删除导航坞里的 `DetailHint`、`DetailLine` 渲染契约。
- 用 `viewed_task_id` 替代 `detail_open`。
- 让 transcript selector 与 composer target 共用这枚 id。
- 导航坞只留行、状态与提示。

### 第二刀：收紧底栏交接

- 给 idle、streaming 两条路共用 `RetireBottomChrome()`。
- wake 先退旧帧，再交给主循环。
- 完成事件有明确 transcript 归属。
- resize、任务完成、队列变化、视图切换都走同一帧账。

### 第三刀：重做结果与预算

- 引入结构化 `TaskOutcome`。
- 传通真实 stop reason 与部分结果。
- 清掉 `40`、`15` 三处默认值分叉。
- 子代理接入 compact/checkpoint。

## 自动回归

### 会话切换

- Enter 选 #2 后，上方只显示 #2 transcript；导航坞不出现 prompt 与工具流水。
- composer 非空时 Enter 被导航消费，不提交草稿。
- 发一条消息，只进 #2 inbox；main 与 #1 均收不到。
- Esc 回 main 后，视口、目标、标题、选中态一同复位。
- 连续切 main/#1/#2 二十次，屏上不多一份输入框、状态栏或代理坞。

### 后台完成

- idle composer 等待时让 #1 完成。
- 唤醒前后扫描整个可视区及滚屏尾部：旧输入框、旧状态栏、旧代理坞都不能留下副本。
- `CountVisibleComposer() == 1`。
- `CountVisibleMainRows() == 1`。
- 每个 task id 的导航行至多一份。
- 完成提示若进 transcript，只出现一次，且归 main。

### 失败与预算

- `max_turns` 未配置时，子代理继承配置，不得偷偷变 `40`。
- 显式 `max_turns=3` 用满三轮后，状态为 `budget_exhausted`，保留部分结果。
- 显式 `max_turns=0` 不触发轮数硬闸。
- assistant 最后一轮无 text 时，保留 stop reason 与最后工具状态，不只报一句“没有给出文本结论”。
- API 错、用户停止、工具错、轮数耗尽在面板与 transcript 中可分辨。
- 临近上限的收口提示只注入一次；代理交出检查点后不再继续漫游。
- compact 后仍能指出查过的文件、关键证据与未完事项。

## 手工验收

开两只后台代理：一只读 `docs`，一只读 `src`。

1. main 继续打字，同时上下切换两只代理。
2. 分别进入两边 transcript，各排一条消息。
3. 让一只正常完成；画面只能留一副输入区。
4. 让另一只用很小的显式轮数撞限；面板须写“耗尽”，进入后能看部分结果。
5. 从检查点续派，不抬高轮数，确认它不会重读已经查过的全部文件。
6. 回 main，核对两边结果都带来源，队列没有串人。

用户全程须一眼看出：

```text
我正看谁
我正给谁发话
谁还在跑
谁为何停下
旧输入框是否已经退场
```

## 不做

- 不拿 prompt 前 40 字充短标题。
- 不靠清屏、刷空行、隐藏滚屏来掩旧帧。
- 不把所有失败都涂成同一个 `failed`。
- 不把默认上限从 `40` 换成另一个暗藏魔数。
- 不以“设成 0 不限轮”代替 compact、检查点与预算展示。
- 不因查看 transcript 就把子代理前台化；查看谁与它在前台/后台执行，是两回事。
- 不照搬 Claude Code 的源码、颜色或文案；只借它已经验证过的会话边界与导航层级。

## 销单线

以下十二条，一条不差：

1. Enter 真切 transcript，不再在导航坞展开长详情。
2. transcript、composer target、选中标记共用同一 `viewed_task_id`。
3. 导航坞始终贴在输入区下方，只放导航，不放长正文。
4. 后台完成后，旧 composer、状态栏与导航坞先退场；滚屏不留小尾巴。
5. 完成通知有且只有一条，并有明确会话归属。
6. 失败分得出接口错、工具错、空结论、用户停与预算耗尽。
7. 失败仍保留 stop reason、最后工具状态与部分结果。
8. 子代理不再暗藏 `40` 轮；默认值从配置传入，`0` 语义全路一致。
9. 正数上限在派出时可见，临近上限会收口，到限交检查点。
10. 长任务会 compact；重复探索不再整段重送，关键证据仍可回溯。
11. 定向消息队列跟 task id 走，在安全边界送达，不因焦点切换串话。
12. 单元测试、空闲屏幕驱动、流式屏幕驱动与真实终端手测全过。
