# 压缩触发失衡：预估虚算把 midturn 压缩线打穿到真实四分之一水位

- 状态：待修（2026-09-03 用户真机实录立案）
- 级别：P1（频繁误触发自动压缩、白折腾一轮模型旁路请求的风险路）
- 类型：LubanCode 产品缺陷（context 压力判定口径失衡）
- 现场版本：`lubancode 0.26.189`
- 现场实录（用户真机，2026-09-03）：context **24%**（61.5k/256k）时触发 `[compact] 工具循环中途,预计下一次请求将超出窗口`——随后压缩以"没有冷区也没有旧存档:榨不出收益,拒绝空跑"失败收场，历史未动。真实水位四分之一就被喊溢出，压完发现根本没货。
- 不在本单：压缩分区算法本身；拒绝空跑的失败处理（这是对的，不动）；显示层窗口账（256k 显示与判定一致，已核）

## 一、病根（已查明）

`src/agent/loop.cpp:1062-1091`（PreRequest 预估）：

1. `projected` 用**故意虚算的保守口径**：`EstimateHistoryTokensForPreflight(BuildPressureDryRunView())` 把重复工具结果与超长回包全量计入——源码注释自己记着病史：**"真请求 47k 时估出 189k,压缩被误触发又一压再压"**（P1-1 修过 dry-run 口径，但虚算保留）。
2. 判溢出线 `kProjectedOverflowPercent = 80`（context.hpp:116）是按**真实口径**标定的。
3. 两者叠加：虚算≈真实 3 倍 × 80% 阈值 → **触发线实际落在真实水位的 ~25-27%**。大工具循环会话（read_file/search 反复跑、重复结果多）虚算放大更狠，用户 24% 即中。

配套失衡：真实历史 61.5k 明明没多大、没有冷区可榨——压缩注定空跑。触发判定与"压得出东西"完全脱钩。

## 二、修法（两刀，二选一或叠加，工头裁决后写明依据）

### 方案 A（口径归一，治本）

- projected 改用**与真请求同一副视图**估：`BuildWorkingView()` 压缩后的 messages 估 token（与 1095 行真请求同源），不再拿 dry-run 全量虚算视图。保守性由输出预留（`OutputBudget::reserve_for_estimate`）单独保留——那是真会占的空间。
- 重复工具结果的虚算若有意保留（防"下一轮工具结果再涨"），改成显式增量项（如 +k 重复结果预估）并注释系数依据，不许整段历史 3 倍放大。

### 方案 B（双闸，止血快）

- 触发条件改 `projected_overflow && working_view_tokens >= window * kRealOverflowPercent(60)`——真实水位不过线不压，虚算单独不触发。真实水位 token 从 BuildWorkingView 估。

**裁决建议**：A 为主（口径错就该归一），B 的真实水位闸作为第二道保险一并落（防 A 的估算仍偏低撞真墙——宁可双闸都不误触）。

## 三、验收

- 复现场景构造：历史真实 ~60k/256k + 大量重复工具结果（dry-run 虚算 ≥180k）——修前必触发 midturn compact（红），修后不触发（绿）。
- 真实水位 ≥80% 场景：该触发仍触发（不许把该压的放过去）——两道闸各自单测。
- `context.pressure.recorded` 事件的 projected/window 数值如实反映新口径（轨迹可对账）。
- 全量 ctest 零回归；`compact.hysteresis` 既有册不退。

## 四、不做什么

- 不动拒绝空跑的失败路（诚实行为，保留）。
- 不动 ShouldAutoCompact 的 80% 线（那是 turn 间自动压缩，另一条路，本次实录是 midturn 路）。
- 不拿"调高阈值"糊弄——80% 是对的，错的是分子虚算。
