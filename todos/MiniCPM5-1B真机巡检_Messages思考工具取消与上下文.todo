# MiniCPM5-1B 真机巡检：Messages 思考、工具、取消与上下文

状态：三件收尾全落，真机 Esc P95 已过门（2026-08-28，LubanCode `0.26.69`，vLLM 本地兼容端
`http://localhost:8001`，模型 `MiniCPM5-1B`，窗口 32768；Release ctest 202/202）

## 一句话

纯文本、连续对话、工具调用这条协议主链能走通。模型肯不肯真发工具调用，另算
1B 模型能力，本轮不管。客户端已收住三道边界：工具续轮原始 `<think>` 不再混进
正文；必超窗口的新消息在本地止步；乱报 500 的上下文、图片错误会先翻成人话。

取消链路用两种假服务钉住，均在 2 秒门内返回。先前真机一回拖了 8.4 秒；本轮
终端注键又受输出排队干扰，测不准按键落点，故不冒充通过。真机 P50/P95 仍待重测。
（2026-08-28 收账：专用刮屏/注键驱动 `tests/manual/esc_cancel_driver.cpp` 五轮实测，
Esc→断开回输入态 P50 26ms、P95 320ms，1 秒门内；先前 8.4 秒确系终端注键排队的
假账，cpr ProgressCallback 唤醒够勤，无需动 transport。）

图片这条路不通。服务端明说此模型不是多模态，却也报作 HTTP 500。模型不听话、
工具后答错行号，另算 1B 模型能力，不算 LubanCode 的 bug。

## 现场配置

本轮不改用户全局配置，只用进程环境变量隔开：

```text
LUBANCODE_WIRE=anthropic
LUBANCODE_BASE_URL=http://localhost:8001
LUBANCODE_API_KEY=unused
LUBANCODE_MODEL=MiniCPM5-1B
LUBANCODE_CONTEXT_WINDOW=32768
LUBANCODE_THEME=plain
NO_PROXY=localhost,127.0.0.1
```

Anthropic 客户端会自行添 `/v1/messages`，故 `BASE_URL` 只能写站根，不能再带
`/v1`。写成 `http://localhost:8001/v1` 会请求 `/v1/v1/messages`，得 404；
这属配置写错，不列 bug。

`GET /v1/models` 真回 `MiniCPM5-1B`，`owned_by=vllm`，
`max_model_len=32768`。`/docs` 也列出 `POST /v1/messages` 与
`POST /v1/messages/count_tokens`。

## 已跑矩阵

| 场景 | 结果 | 归属 |
|---|---|---|
| 单轮文本，`high` | 正文可见，退出码 0 | 通过 |
| 单轮文本，`none` | 正文可见，仍显示“思考中” | 方言不生效 |
| 连续两轮记暗号 | 第二轮能复述，thinking signature 可回放 | 通过 |
| 强制 `read_file` | 工具真执行，结果也回了模型 | 主链通过；模型末答错属能力 |
| 工具后续正文 | 原样露出 `<think>...</think>` | P0 |
| `/doctor effort unset/none/high` | 三档都收 HTTP 2xx；前两档 64 token 全耗在 thinking | P1 诊断盲区 |
| `Esc` 打断长答 | 约 1 秒按下，8.4 秒才停；下一轮 `CANCEL_RECOVER_OK` 正常 | P1 |
| 3 万枚 `a ` 的单条输入 | 服务端报输入至少 24577 + 输出 8192 超 32768；自动压缩也超窗 | P1 |
| `/image` PNG | 服务端 500：`MiniCPM5-1B is not a multimodal model` | 端点分型 + 客户端能力提示 |
| 超窗 `count_tokens` | 服务端 500 `internal_error`，不回 token 数 | vLLM 端 |

## 本轮客户端收账

- Anthropic Messages 工具续轮才开兼容门。只有首枚正文命中完整
  `<think>...</think>`、闭标签后又隔两行正文，才隔离原始思考。无签名内容不伪造
  `ThinkingBlock`，免得续轮重放再撞 400。
- fixture 按单块、逐字节、随机切片、整流回放；另钉闭合、未闭合、跨 delta、正文
  合法 XML。解析器不吐原始思考，下游存档、复制、导出自然也拿不到。
- 32768 窗口先扣 8192 输出与 512 协议余量。3 万枚 `a ` 真机复跑在 0.0 秒本地
  拒绝，没再发到 8001，也没白跑 compact。CJK、代码、base64、可压旧史都有离线册。
- `internal_error` 里若明写 context overflow 或 non-multimodal，用户先见中文分型，
  原始 detail 留作附账。
- 环境变量换走 wire/base URL/model 后，`/config` 与状态栏显示
  `env override / unbound`；旧 provider 的 metrics、推理回放、搜索与能力声明不再偷渡。
- 假服务“无响应头”与“已有 SSE 头、无后续 body”两形，150ms 置取消旗，均在 2 秒
  内返回 `Cancelled`。传输层已有唤醒能力，本轮无需乱改超时参数。
- Release 全量 `ctest`：202/202 通过。精确环境下，3 万枚短词本地预检耗时 253ms。

## 环境插曲：连续矩阵后短时失联，已确认是网络波动

完整探针刚跑完时，最后两笔图片与 `count_tokens` 仍有响应。紧接着再跑快速自检，
第一步 `GET /v1/models` 等满 30 秒超时。随后分别探 `/health`、`/v1/models`，
两次 8 秒与一次 10 秒都收不到一个字节，HTTP 状态为 000。

Windows 侧 `127.0.0.1:8001` 当时仍在 Listen，持有者是 VS Code Insiders 的
端口转发进程。用户随后说明网络有波动并已恢复。复探 `/health`、`/v1/models`
都在 0.46 秒内回 200；一轮快速真机探针也跑完。

故此不列 LubanCode、vLLM 或 VS Code bug。只保留探针的失联收口：入口超时时
仍落脱敏报告，写 `endpoint_unavailable/models`，退出码 2，不只甩异常栈。

## P0：工具结果之后，原始 `<think>` 当正文上屏

### 真机证据

首轮用 Anthropic `tool_choice` 强制 `read_file`，服务端回
`thinking + tool_use`，thinking block 带 signature。LubanCode 执行工具，把
`tool_result` 送回。第二份 SSE 却变了样：

```text
content_block_start.type = text
content_block_delta.delta.type = text_delta
text 开头 = <think>
thinking_delta 字符数 = 0
```

一次脱敏直抓得：正文 1980 字符，含 `<think>`，没有一枚
`thinking_delta`。终端于是把模型推理逐行画出来。这个错先发生在 vLLM 的
Anthropic 适配层；LubanCode 又没有兼容止损，用户照样看见内情。

已沉淀：

- `tests/fixtures/api/anthropic_messages/live_vllm_minicpm5_post_tool_raw_think.*`
  留住事件形状，正文缩成短样，不留 signature、request id、工具原文；
- `tests/manual/minicpm5_messages_probe.ps1` 能重跑首轮工具与第二轮 SSE，只记
  block/delta 种类、字符数和标签命中。

### 验收

- [ ] 先在 vLLM 端核 chat template 与 reasoning parser（服务端事项，本轮不改）。工具回合前后须同走
  `thinking_delta`，不可忽然退成带标签的 `text_delta`。
- [x] LubanCode 对兼容端给止损。若正文首段命中完整原始思考包裹，日志明报
  provider 协议异常，不可悄悄把思考当普通答话存档、复制、导出。
- [x] 不可见着任意 `<think>` 就剥。用户让模型讲 XML、Markdown 或代码时，
  这串字本可合法出现。判定须吃块位置、回合阶段、首段边界与闭合状态。
- [x] 工具后续有闭合标签、无闭合标签、标签跨 delta、正文合法引用标签，各钉
  一册测试。
- [x] session/export/resume 不得把误判的原始思考泄到正文账里。入口不再产出这段
  `TextDelta`；无签名内容也不写入 thinking 历史。

## P1：`/think none` 看似可选，实则关不掉

本节是模型与 vLLM 方言事项。按本轮边界，只留证据，不改 LubanCode 去猜私参。

直连跑三组 `thinking={"type":"disabled"}`。三组全回非空 thinking，长度分别
136、159、203 字符。`high` 三组为 291、285、142 字符；两组范围交叠，单凭
一回 HTTP 2xx 压根儿不能说档位有效。

又试 vLLM 私有形状 `chat_template_kwargs.enable_thinking=false`。三回全无正文，
只回 2 至 5 个 thinking 字符。也就是说，眼下不是“换个开关便好”：Anthropic
disabled 被忽略，模板开关 false 又把可见回答一并抹了。

`/doctor effort` 没误报“已支持”，这点守住了。可它每档只发一回，
`max_tokens=64`。unset/none 恰好都以 `max_tokens` 收场，正文 0；人还得自己猜。

- [x] provider/model 能声明 `always_think` 或 `off_unsupported`。`/think none`
  切换前后都要明说“此端点未证实可关”，别让状态栏只挂一枚 `none` 便算数。
  （2026-08-28 落地：capabilities 两键经 `ClassifyThinkOffDeclaration` 认定，
  内置目录与用户 models.json 同形状；真机验——临时 USERPROFILE 的 models.json
  写 `off_unsupported:true`，`/think none` 切换与裸敲都打出“此端点未证实可关”。）
- [x] `/doctor effort` 每档至少重复三回；报告 HTTP 接受、thinking 是否产出、
  正文是否产出、终止原因与分布，四账分开。
  （`SummarizeEffortProbeRounds` 纯函数聚账，单测四册；真机 `none` 档三回
  3/3 产出 thinking（388–514 字符）与正文，`high` 档 218–477 字符，范围交叠
  一眼可见——2xx 不当行为支持。）
- [x] 探针给推理优先模型留足正文预算。若预算耗尽，只判 inconclusive，不把
  `text=0` 当支持或不支持。
  （doctor 探针预算 64→1024，`stop=max_tokens 且正文 0` 的回单列“预算耗尽”账，
  全部耗尽只判 inconclusive；ps1 探针 dialect/template 行同改 1024 并添
  `probe_budget_exhausted_*_inconclusive` finding。真机复跑：三档全 `end_turn`、
  正文 12 字符，预算不再被思考吃满。）
- [ ] vLLM 部署侧若修好私有模板开关，再把准确参数写进 provider profile；
  修好前不在 LubanCode 里硬塞 `enable_thinking=false`。（服务端事项，挂账；
  本轮客户端只声明、只明说，请求侧照发标准 disabled 形状。）

## P1：上下文预算没挡住必超窗口的单条输入

把 3 万枚短词经管道交给 LubanCode。客户端仍发请求，并预留 8192 输出 token。
服务端报：输入至少 24577，加输出 8192，合计已过 32768。随后客户端尝试自动
压缩；压缩请求又预留 4096，仍撞同一面墙，最终失败。

这不是旧历史裁不动。用户新来的一条消息本身就太大。字符估算低估了短 ASCII
token 密度，又没在发前用“输入预算 + 最大输出预算”做硬门。

- [x] 上下文门先扣输出预算，再算输入可用量；主答按本轮 8192 预留，另加 512 余量。
- [x] 单条新输入若已装不下，发送前拒绝。文案列出窗口、预留输出与估算输入，
  不要先发一次，再拿 500 当压缩信号。
- [ ] 有 token count 端点时可选精算。眼下该端点会回 500，客户端已直接走保守估算，不得
  把精算失败本身又送进自动压缩循环。
- [x] 测试短 ASCII、CJK、代码、base64。超长单词仍由既有 UTF-8 尺兜底。重点钉住 32768 窗口下
  “单条输入 + 8192 输出”这道线。
- [x] 自动压缩须先判断“可压的是历史，还是不可压的当前用户消息”；后一种不跑
  compact。

## P1：`Esc` 已置位，网络回合仍拖 8.4 秒

长答刚起便按 `Esc`。UI 收到了键，最后也显示 `[已打断]`；可时钟仍从 1 秒走到
8 秒，才退回输入框。下一轮答出 `CANCEL_RECOVER_OK`，会话没砖死。

`PostSseStream` 已挂 `ProgressCallback`，也在 `WriteCallback` 查取消旗。真机
表明 Windows + 本端点无新字节时，回调醒得不够勤，取消时延仍可到数秒。

- [x] 给无响应头、已有响应头但无 body 两种传输形状各钉取消时延测试。模型长算
  但迟迟无 SSE 在线上仍落成“已有响应头、无 body”这副线形，同册覆盖。
- [x] 目标：按下 `Esc` 后 1 秒内断开并回输入态；测试量按 P50/P95 记，不拿一次
  偶然快当通过。（2026-08-28 专用驱动 `tests/manual/esc_cancel_driver.cpp` 真机
  五轮：Esc→“Stopped after” 320/23/28/26/22ms，P50 26ms、P95 320ms；Esc→
  “[已打断]”回执 P95 93ms。键直写子进程控制台输入队列，不沾终端注键排队。）
- [x] 若 cpr easy 接口不能及时唤醒，改可唤醒 transport 或独立连接句柄；不可
  用 1 秒 LowSpeed 粗暴代替，那会误杀正常长思考。（实测唤醒够勤——P95 320ms
  在门内，前件不成立，transport 一根毛不动。）
- [x] 取消仍要守住现有语义：半截正文保留，工具账配对，下一轮可续。（驱动五轮
  钉住锚句“雨从夜里下起”上屏后打断，5/5 打断后仍在屏账；收口后续问正常
  “Worked for”；工具账配对由假服务两册既有覆盖。）

## P2：能力与错误分型太晚

`/image` 能把 PNG 发出去，服务端才说模型非多模态；HTTP 状态还是 500。
`count_tokens` 超窗也报 500 `internal_error`。LubanCode 能把原始 JSON 亮出来，
却没给“模型不支持图片”“上下文超限”两种稳定分型。

- [x] live model/catalog 能记 `text/image/audio` 能力。已知纯文本模型在 `/image`
  发送前拦住；未知才允许试探。
  （2026-08-28 落地：`ClassifyImageInputSupport` 合判 `input_modalities` 与
  `capabilities.image`，用户 models.json 条目即可声明（live 列表落痕条目同形状，
  加capabilities 键就能记 audio）；闸在 `RunTurn` 发送前，交互/单发共用。真机验：
  models.json 声明 `image:false` 后 `@probe.png` 当场拦下、未发请求；空目录的
  对照组照发，服务端 500 被既有分型翻成“当前模型不支持图片输入”。）
- [x] 服务端虽错报 500，客户端仍可按安全字段识别常见 capability/context 错，
  给人话与下一步；原始 detail 只作附账。
- [x] 不能凭这回 MiniCPM 结论断言整个 provider 都不认图片。只认服务端本回原话。

## P2：环境变量已换全套端点，provider 名仍挂旧选择

`LUBANCODE_WIRE/BASE_URL/API_KEY/MODEL/CONTEXT_WINDOW` 都来自环境变量，
`--config` 与状态栏却仍沿用全局 `active_provider=<旧选择>`；全局 `think=high` 也按
逐字段优先级带进新模型。规矩虽写在帮助里，现场仍容易把本地请求误归到旧选择。

- [x] 当运行字段已与 active provider 脱钩，显示 `env override / unbound`，不要只
  挂旧 provider 名。
- [x] provider profile 只在端点、wire、model 归属能对上时套用；不相干的
  metrics/capability/default_think 不可暗带。
- [x] `/config` 加 `provider_binding = env override / unbound`，明报组合来源风险；
  各字段原有来源标签照旧列出。

## 不算 LubanCode bug 的项目

- 模型读到 `CMakeLists.txt` 后仍答错版本行；工具结果确实送达，属模型质量。
- 指令要求只回固定短串，偶有多字、少字；属 1B 模型服从度。
- 模型声明非多模态。不能因 API 收 image 字段便说模型该会看图。
- 端点把 unsupported/context overflow 错报 HTTP 500，根因在 vLLM 适配层；
  LubanCode 只欠更好的客户端分型。

## 证据与复跑

脱敏截图在 `build/test-evidence/minicpm5-smoke/`，不进仓。探针报告默认落
`build/test-evidence/minicpm5-messages-probe/`，也不进仓。

```powershell
pwsh -File tests/manual/minicpm5_messages_probe.ps1 `
  -BaseUrl http://localhost:8001 `
  -Model MiniCPM5-1B `
  -ApiKey unused `
  -Repeats 3
```

报告不记 API key、thinking 正文、signature、request id、图片 base64 或完整工具
结果。它只留状态码、事件种类、字符数、终止原因与标签命中。

Esc 真机 P50/P95 用专用刮屏/注键驱动复测（环境变量照“现场配置”节喂，另设临时
`USERPROFILE` 隔开全局配置；轮数 1–5，默认 5）：

```powershell
$env:USERPROFILE = "<临时家目录>"   # 可选:放声明 MiniCPM5-1B 的 models.json
esc_cancel_driver <lubancode.exe> <空临时工作目录> <报告文件> 5
```

它把 Esc 直写子进程控制台输入队列，量 Esc→“[已打断]”与 Esc→“Stopped after”
两笔时延，轮末验“下一轮可续”；题面锚句上屏过的轮，断言打断后锚句仍在屏账
（半截正文保留）。报告落在 build/test-evidence 或自选路径，不进仓。

## 完成定义

- [x] 工具后续不再把原始思考标签当正文显示、存档、复制或导出。
- [x] `none` 无效时明说 unsupported；doctor 三回对照，不拿 2xx 当行为支持。
- [x] 32768 窗口下，必超请求发送前收住；不可压的单条输入不触发 compact。
- [x] `Esc` 真机 P95 在 1 秒内收口，下一轮仍可续。（P50 26ms / P95 320ms）
- [x] 图片与上下文错误分型清楚，服务端 500 不再只剩一团原始 JSON。
- [x] 脱敏 fixture 四种切法回放、定向测试与 Release 全量 ctest 202/202 全绿；真机取消
  P95 已用专用驱动重跑过门（320ms），不拿终端注键排队的数据充数。
