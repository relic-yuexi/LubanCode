# 持久 Agent：Gateway 接入 Session V3，常驻助理与可靠自动任务总装计划

- 日期：2026-09-13。
- 状态：新版设计，待实施、待验收。现状按 HEAD `c6b755e4` 及本次工作树静态核查；未重跑构建、测试、真实模型、渠道或系统服务。
- 替代：旧总装计划《持久Agent_Gateway自动任务与可靠恢复总装计划》（2026-09-17 删档，G0/G1 历史见 git 史），不将旧勾选搬成新版验收结果。
- 主合同：[Session V3](session轨迹v3_消息主轴树链与四角色壳收敛设计.todo)、[V3 schema](../docs/architecture/trajectory-v3-schema.md)。本文新增接口、字段、状态均为提案，未注册前不能写入现行 V3 envelope。
- 实施范围：`src/gateway`、`src/channel`、`src/runtime`、`src/trajectory/v3`、`src/workspace`、`src/platform`、`src/cli` 及对应测试；自动任务领域目录按本单新增。
- 首版：单机、单用户、显式启动，先本地定时任务，再接一只真渠道。多租户、集群、跨机迁移另立验收。

## 一、这份计划到底要做什么

2026-09-14 产品入口补单：[常驻助理 Web 主界面：C++ 本地服务与 Gateway 总装](常驻助理Web主界面_CPP本地服务与Gateway总装.todo)。HTML/JS/CSS 提供不依赖 IM 的助理主界面，C++ 同宿主接 AppServer 控制入口与 Gateway 工作服务；本单仍管持久任务、调度、恢复与服务托管。新入口待实现，不把参考 Web 前端当完整助理交付。

是为了支撑 OpenClaw、Hermes 那类常驻助理：从聊天入口接事，按时间或事件办事，用户关掉终端也能跑，办完把结果送回去。底座还可供仓库巡检、资料监测、日报、运维助手复用。

Gateway 管常驻、受理、排队、接管和投递。LubanCode 原有 Agent、工具、Session V3 负责执行与记账。具体业务、技能包、人格、知识源和产品页面放上层；本单不包办两家产品所有功能，也不以“能接 IM”冒充完整助理。

首版交付两条用户水路：

1. 建一项“每天九点检查指定仓库”任务，关终端；系统拉起 Gateway，到点执行，结果落本地，稍后可查。
2. 从已授权聊天会话交办任务；先拿受理回执，再收执行结果。网络断了重送回复，进程崩了先查账，不盲目重做工具。

外部参照已于本日查阅官方资料，只取产品形态：

- [OpenClaw Gateway](https://docs.openclaw.ai/gateway)：常驻进程管理路由、控制面与渠道连接。
- [OpenClaw Automations](https://docs.openclaw.ai/automation/cron-jobs)：自动任务产品入口，具体调度语义不照抄旧版本描述。
- [Hermes Messaging Gateway](https://hermes-agent.nousresearch.com/docs/user-guide/messaging/)：聊天入口、后台会话、结果回送及服务管理。
- [Hermes Cron Internals](https://hermes-agent.nousresearch.com/docs/developer-guide/cron-internals)：调度与执行分工；cron 每次开 fresh session，持久记忆可载入，前次临时对话不混进来。

这些资料不证明 LubanCode 已有同等能力，也不替任何平台承诺 exactly-once。

## 二、旧单哪些过时，哪些保留

| 旧说法或安排 | 当前判断 | 新版处理 |
| --- | --- | --- |
| 新 Trajectory 默认关闭 | 已过时；V3 新场默认开启，只有环境变量恰为 `0` 才回 v2 | Gateway 新执行固定要求 V3；遇 v2 新写配置明报拒绝，不偷偷双写 |
| SessionStore、旧 Journal 两套生产账待合并 | 不宜再当新版起点 | Session V3 管执行事实，领域账只存职责内事实及引用 |
| `~/.lubancode/trajectories/workspaces/...` | 已过时 | 走 workspace identity/index/resolver，解析当前 workspaces 下会话目录 |
| Channel 缺 Manager、IngressStore、SessionHost | 已有组件，旧文前后也有冲突 | 复用并补耐久与生产装配，不重新造同名组件 |
| Gateway 尚不存在 | G1 骨架存在 | 保留 run/status/stop、profile、boot history、SafeMode；补真正工作泵 |
| Loop 可从旧存档恢复 | 当前不能沿用此承诺 | 明列事件落盘断口；服务级 Automation 不依赖旧 `/loop` 恢复 |
| Goal 只是旧 goal 事件账 | V3 已有服务、投影接管及迭代收口代码 | 复用领域服务；Gateway 不另造目标调度器 |
| HeadlessSessionHost 从零建 | 已有 ChannelSessionHost/AgentChannelEngine，另有 SessionService | 提炼共同装配，补持久受理、完整工具账与恢复入口 |
| 有模型输出即可投递 | 对 V3 太宽 | 必须显式选定可外送回复，固定原始来源与正文版本后入 outbox |
| 子代理持久化紧跟调度做全套 | 首版范围过大，又与异步工具单重叠 | 先完成普通 turn 闭环，再共用任务执行底座 |

保留旧单六条底线：显式启动；单机先行；同场单飞；未知副作用停住；发送失败只重送；真机证据与假件证据分开。

## 三、源码核查：已有件与真断口

以下行号只定位本次快照，实施时按符号复查。

| 来源锚点 | 核查结果 | 对新版意味着什么 |
| --- | --- | --- |
| `src/trajectory/v3/session_switch.cpp:13`，`NewSessionV3WriteEnabled` | 新场默认 V3 | 不能再用旧 feature-off 分阶段 |
| `src/trajectory/v3/writer.hpp:169`，`V3Writer::Start/Continue` | writer 有创建、续写、提交回执 | 底层 Continue 存在，不等于恢复业务应就地续源账 |
| `src/trajectory/session_manager.cpp:2434`，`ResumeAsNewV3Locked` | 来源账验证后另开新 session 接续 | Gateway 接管也遵循 resume-as-new，保留原 work 身份 |
| `src/trajectory/v3/reader.hpp`，读取侧合同 | 历史、模型上下文、单次请求是不同投影 | 禁止把整段历史回填成模型输入 |
| `src/trajectory/directory.cpp:249`，`ResolveDirByWorkspaceKey` | workspace_key 通过索引找实际目录 | 不拼目录名，不把显示名当身份 |
| `src/gateway/process.cpp:469`，`GatewayProcess::Run` | 主循环只查停止信号/文件命令并等待 | 进程能活，不代表会跑任务或接渠道 |
| `src/cli/gateway_command.cpp`，`RunGatewayCommand` | 仅分发 run/status/stop | install/start/restart/doctor/logs 都须另验，不按旧命令表宣称支持 |
| `src/channel/manager.hpp:1`、`src/channel/ingress_store.cpp:322` | 已有账号生命周期、注入 Transport、入站事件与去重；写盘使用 stream.flush | 仍须接真实进程 transport，并核实 ACK 所需耐久档位 |
| `src/runtime/channel_session_host.cpp:40`，`Submit/PumpOne` | 队列、engine、busy 状态留在进程内 | 增加持久 work 到 session/turn 对照；内存队列只作投影 |
| `src/runtime/agent_channel_engine.cpp:41`、`:127`、`:189` | 直接构造 SessionRuntime；工具栅栏接线有遗留；回复从新增 assistant 文本拼接 | 接共同执行装配，逐项核查工具录账；停止以拼接文本作为可靠最终回复来源 |
| `src/runtime/session_service.cpp:188`、`:236` | 受理账只存键、ID、hash；正文在 pending_inputs_；Append 失败被忽略；来源去重只种直接上一场 | 不能把现有 SubmitInput 当 durable inbox，首批补齐 |
| `src/app/commands/loop_commands.cpp:429`、`:464` | 旧恢复路已删，FlushLoopEvents 只送内存/前端投影 | 当前 `/loop` 不是耐久 AutomationStore |
| `src/app/wirings/loop_session_wiring.cpp:53` | 会话 Ensure 中 StartTimer | Gateway 未启动时禁止的是服务调度器，不能误禁用户显式使用会话 `/loop` |
| `src/app/wirings/goal_session_wiring.cpp:195`，`RestoreFromArchive`；`src/runtime/goal_service.cpp` | 已有 V3 goal 投影接管、状态提交等实现 | 按现有规则恢复，持久托管授权另行校验，不笼统写“Goal 尚未接 V3” |
| `src/tools/task_ledger.hpp:782`、`src/tools/agent_task_coordinator.hpp:183` | 任务对象、线程由进程持有 | 任务快照/子账可查，不等于工作线程可跨进程复活 |
| `src/app_server/outbox.hpp`、`src/hooks/outbox.hpp` | 一个是前端有界事件队列，一个是 hook pending/ack 账 | 都不是本文渠道 DurableReplyOutbox，名字相似也不能直接顶替 |
| `src/trajectory/journal.cpp:26` | PowerLoss 走 FlushFileBuffers/fsync；与 ProcessCrash 分档 | 关键领域提交明确要求耐久档位，不能只看 flush 字样 |

另有首批必须复核之处：`GatewayLock::TryAcquire` 在 `src/gateway/process.cpp:131` 附近先读文件，再写文件，不能仅凭顺序单测断言并发启动互斥。业务认领前须补 OS 排他锁或原子创建合同、实例 fencing 与双进程竞争测试。

已有 `tests/unit/gateway/test_gateway_process.cpp`、`tests/unit/runtime/test_channel_session_host.cpp`、`tests/integration/channel/test_channel_phase3_e2e.cpp`、`tests/unit/trajectory_v3/`。这里只确认测试源码存在；旧报告“16 例全绿”不是本次运行结果。

## 四、新版总水路与分层

```text
OS supervisor
  -> GatewayProcess（显式启动、profile 锁、健康、停止）
       -> AutomationScheduler -> occurrence
       -> ChannelManager      -> ingress
       -> 本地受理入口          -> operation
                    |
                    v
          DurableWorkCoordinator（引用来源，不另抄输入正文）
                    |
          SessionWorkScheduler（内存就绪/公平/单飞）
                    |
          共用 Headless 装配 -> SessionService / SessionRuntime
                    |
          Agent / tools / hooks -> Session V3
                    |
          committed reply selection + artifact
                    |
          DurableReplyOutbox -> local / channel adapter

重启：先拿所有权 -> 验领域账和 V3 来源链 -> 对账裁决
      -> 补待派发/待投递投影 -> 按托管授权恢复执行
```

图中 DurableWorkCoordinator、DurableReplyOutbox、回复选择记录是拟新增职责。实现可收进现有类，不以多建一层为目标。AgentLoop 不复制；平台 SDK 不进入 agent/runtime；OS supervisor 只拉起进程。

AppServer 提供控制入口与观察视图，Gateway 提供常驻生命周期。未来可共宿主，也可用控制连接；首版无需为了跑 cron 再暴露公网 WebSocket。断开 UI、打开历史、订阅事件都不能触发执行。

普通 CLI 不因 enabled job/channel 偷启 Gateway。用户手动开的 `/loop`、普通工具后台命令仍按各自合同运行，别把“Gateway 默认关闭”扩大成禁止现有会话功能。

## 五、领域账、身份与目录

### 5.1 各账只管本职

| 账 | 写者 | 内容 |
| --- | --- | --- |
| Gateway profile/boot | GatewayProcess | 实例身份、配置版本、启动/停止、健康 |
| Automation | AutomationStore | spec revision、时间计划、每次 occurrence、暂停/取消、触发结算 |
| Channel ingress | ChannelIngressStore | 原始来信/附件引用、平台去重键、准入与路由裁决 |
| Work/受理 | 共用持久受理服务 | workId、sourceRef、inputRef、状态、ownerEpoch、session/turn 引用；不抄会话 history |
| Execution | V3 单写者 | 四角色消息、请求、Action、结果、上下文链、终态 |
| Task execution（后批） | 与异步工具单共用服务 | 跨 turn 作业、接管、结果引用、取消；不另建 Gateway 专用 task 真值 |
| Reply delivery | DurableReplyOutbox | 固定正文 artifact、来源引用、目标、发送尝试、回执、未知态 |

Work 是统一执行意图，occurrence/ingress 是触发来源。两者通过 sourceRef 关联，不能各自都拥有一套“是否已经执行”的独立判据。Work 的运行投影最终须核对 V3 开轮/终态；UI 快照随时可重建。

接纳前正文可以保存在 durable input artifact；进入 V3 后用 inputRef/messageRef 对照。这是输入原件与执行事实，不是第二份可改写 history。不得只存 payloadHash 而不留可恢复正文。

### 5.2 身份不能随恢复洗掉

拟定领域身份：

```text
jobId + specRevision + scheduledSlot -> occurrenceId
accountId + providerEventId          -> ingressId（按平台规则补 conversation/thread）
sourceKind + sourceId                -> workId
workId                              -> session lineage + current ownerEpoch
logicalConversationId               -> current sessionRef（恢复后原子换代）
replySelectionId + target + ordinal  -> deliveryId
```

- V3 跨场事实引用沿现有五键 `sessionId/runId/seq/id/hash`；外层再带 workspaceKey，不能只留 event hash 或相对路径。
- sourceId/workId 全局带域，不拿 `op-1` 这种会话局部号跨场判重。
- occurrence 的 slot 用计划内时间，不用实际启动时间；时钟倒拨不能再造同一拍。
- 回复恢复后仍指原已提交选择；不能用新 sessionId 重新散列 deliveryId，否则 resume 一次便多送一次。
- 多次 resume 沿完整来源链查去重和已完成工作；不是只读上一场 operations.jsonl。
- ownerEpoch 拦旧 worker 迟到提交；它不能撤回外部已发生动作，未知副作用仍须外部对账。

### 5.3 目录与持久提交

```text
<home>/.lubancode/gateway/profiles/<profile>/
  gateway.json / gateway.lock / control/ / boot-history.jsonl
  automation/     # spec、occurrence 事实及可重建索引
  work/           # 受理/派发/接管引用、输入原件
  channels/       # 账号状态、ingress、adapter 状态引用
  delivery/       # 回复原件、发送事实、回执、dead letter

<home>/.lubancode/workspaces/<resolved-workspace-dir>/sessions/<sessionId>/
  <sessionId>.jsonl
  artifacts/
  subagents/...
```

Gateway 子目录布局为提案；会话位置必须调用现有 resolver，不按图手拼。显示 slug、workspaceKey、实际目录分别处理。

首版用追加事件与原子快照，可复用 JournalWriter 耐久原语；领域 schema 独立版本化，不假冒 V3 会话事件。关键受理、认领、执行授权、回复选择与发送回执要求 PowerLoss 级提交；文件创建、替换及父目录同步须逐平台核实。进程硬杀测试不能冒充断电测试。

写盘失败就拒绝受理或停在可诊断状态。索引/快照是派生物，损坏可重建；已提交原件丢失或中段断链则隔离，不能跳过继续执行。只有未提交残尾可按备份、修复意图、修复回执流程处理。

活 work、来源链、未送回复持有 artifact 保留引用。压缩、清理、归档不能删这些依赖；敏感凭据存 SecretRef，日志和死信不泄露票据。

## 六、受理与执行：先补跨账窗口

受理次序：

```text
验身份/权限/配额/输入 -> input 原件落稳 -> accepted(sourceRef, inputRef, hash) 落稳
  -> 向调用方或平台确认受理 -> 投内存 wake
```

若平台必须在短时限 ACK，就把 durable ingress 当受理边界；来不及落稳便返回平台允许的重试/拒绝，不先 ACK 再赌写盘。平台 ACK 行为须由 adapter 明确声明。

派发次序：

```text
source.accepted -> 幂等建立 work -> claim(ownerEpoch) 落稳
  -> 绑定预留 session/turn 身份与 workId -> V3 输入/开轮事实提交
  -> 按统一工具授权执行 -> V3 终态/回复选择提交 -> 结算 work
```

无跨文件事务时，靠稳定身份、先后栅栏和恢复对账补齐窗口：

- source 已接纳，work 未建：按 sourceId 补同一 work。
- work 已认领，V3 未开轮：核实没有旧执行，再开轮；不能凭租约过期认定未执行。
- V3 已开轮，work 还没记 turnRef：从预留身份/注册后的 work 关联事实找到原轮，不能再派一个新轮。
- V3 已终结，work 未结算：补状态，复用输出，不调用模型。
- accepted 写稳、队列未入：从账重建；duplicate 回原回执，并保证原 work 仍可观察/推进，不能“去重成功、任务失踪”。

`SessionService` 受理面须统一修补：检查 Append 回值；输入有 durable ref；启动重建 pending；操作计数与作用域不冲突；重复键同载荷返回原操作，异载荷冲突；沿来源链恢复。现有 AppServer/CLI 调用者同步适配，不能只在 Gateway 私下加一个绕行入口。

Headless 装配须与共用执行路核齐：模型路由、工具/Profile、权限确认、ToolTraceHub/Action、结果捕获与选用、Lua hook、预算、取消、V3 close。注册了工具或绑了 boundary_recorder 都不算核齐。

## 七、触发规则：Cron、Heartbeat、Goal、Loop 分清

| 对象 | 负责什么 | 首版安排 |
| --- | --- | --- |
| Automation | 持久计划，何时生成工作 | 新增 once/interval/cron，先验 once 和 interval |
| Heartbeat | 周期检查与变化通知策略 | Automation 的一种用途，不另养 timer；无变化默认安静 |
| 会话 `/loop` | 当前会话周期触发 | 继续原职责；显式导入才转托管 automation，来源停用或明确允许双跑 |
| Goal | 目标、预算、迭代、验收与完成状态 | 复用 GoalService；不因 Gateway 重启无限补跑迭代 |
| Workflow | 图与节点依赖、节点恢复 | 后批调用 WorkflowService 合同，不由 Gateway 重做节点调度 |
| 异步工具/子代理 | 一次长任务执行、结果与取消 | 与异步工具单共用执行账，不与 cron jobId 混用 |

AutomationSpec 至少固定：owner、workspaceKey、prompt/inputRef、model/tool/profile 版本、schedule/timezone、sessionPolicy、misfirePolicy、overlapPolicy、budget、authorizationRef、deliveryTarget、revision。

首版裁决：

- cron 默认 fresh session；continuation 显式绑定 logicalConversationId，进入同一单飞队列。下一周期属于新 occurrence，崩溃恢复属于原 occurrence，不能混成一次“重试”。
- prompt 默认固定内容和 hash；若允许每次读文件，spec 明写 live 模式，每个 occurrence 派发前保存所读版本。
- 时区用 IANA 名称，存 UTC slot；明确 DST 跳过/重复时段规则。测试不得只用 Asia/Shanghai。
- 错过周期默认合并一拍，记录覆盖范围；once 过期按 deadline 执行或记 missed；paused/disabled 不补跑。
- 同 job 默认不重叠，最多留一份合并待办；全局/工作区/会话都有并发和队列上限，不无限 catch up。
- update/pause/cancel 使用 expectedRevision；排队时固定 specRevision；取消先停未来派发，再处理当前 work，不删除历史来假装未发生。
- 模型失败重试、工具重试、整轮重试、delivery 重试分别计数。重启不刷新预算、deadline、失败次数或原授权。
- heartbeat 留 observation/resultRef 与上次已通知版本；只在有变化、完成、失败或需用户处理时投递。检查失败不能记成“无变化”。
- 持久 memory 复用实际可用召回面，明确作用域；不能把旧 run history 当长期记忆，也不将尚未落地新版 Memory 合同写成现有能力。

## 八、V3 恢复裁决

恢复顺序：取 profile/账号/会话所有权，封住旧 epoch；读领域账；验证会话来源链及 artifacts；先补已完成投影，再分类未完工作；最后才开 scheduler 和执行器。

状态读回、执行续跑分别授权。普通 resume/read 仍只读或恢复默认状态；已明确托管且授权仍有效的 work，可由 Gateway 策略续跑。目标暂停、用户取消、权限撤回、deadline 到期都压过自动恢复。

| 最后可信事实 | 恢复动作 |
| --- | --- |
| accepted，无 claim | 排同一 work |
| claim，无开轮事实 | 对账后重派同一 work，另记 attempt |
| model request 已发送、无完整响应 | 记请求中断/未知成本；按模型重试政策另发 requestId，不称为 provider 断点续传 |
| assistant 已落但未接纳上下文 | 按 V3 已提交链及请求状态裁决，不因消息存在就自动入链 |
| Action started，无 terminal | 查进程/外部操作；不能判明便 unknown/needs_review，不默认重跑 shell/MCP/HTTP |
| 工具已返回，结果捕获/选用提交失败 | 保存现有证据并停住，不能通过重跑工具补一份结果 |
| 工具结果完整，模型尚未消费 | 按 actionId/resultSelection/message/context 事实补允许的后续步骤，不再执行工具 |
| compact 候选存在，无 applied | 恢复旧链 |
| compact.applied 已提交 | 恢复新链；投影失败禁止后续发送，不退回旧内存史凑请求 |
| reply selection 已提交，无 outbox | 补同一 deliveryId |
| adapter 已 send，无 receipt | 有幂等键或查询能力则对账；无此能力记 delivery_unknown，不无声重送 |
| 多段 resume 来源链缺失/坏 hash | 隔离该 work，明报缺失来源；不补造历史 |

unknown/needs_review 是工作或执行投影状态，不直接塞入现有 V3 OpStatus。新增事件先注册 schema、校验前驱、校验引用，再接 writer/reader/测试。

只读或幂等工具也不只凭工具名决定能否重试：记录实际调用参数、资源键、授权、输入版本、外部幂等键是否真的送达。epoch 只能拦旧结果落账；没有外部 fencing 时，不因本地换 owner 就断言旧动作已停止。

处置入口至少支持 inspect、reconcile、cancel、明确授权 retry。处置要带 expectedRevision、原 work/action 引用与理由，留下回执；不能直接改 JSON 状态。

## 九、回复与投递

V3 的“assistant 消息”“当前上下文”“对外最终答复”不能混用。中途说明、工具调用伴随文本、compact 内部问答、子代理摘要、隐藏思考都不得靠一次 history 拼接送出去。

拟新增 reply selection 合同：

1. 按完成策略选择明确的对外正文/附件；默认 turn 成功才选，失败另用明确的失败通知。
2. 固定来源 message/event 引用、最终正文 artifact/hash、selectionId、ordinal、target 与格式版本。
3. 原件先落稳，选择事实后提交；选择事实引用对应 V3 终态。不为 Gateway 改成“等 outbox 成功才准 V3 收口”。
4. Projector 从已提交选择生成唯一 outbox item；扫描游标与投影版本落盘，崩溃可重扫并去重。
5. sender 只读 outbox。回复格式和内容在 enqueue 后不随最新 prompt、路由或配置改变；重定向是新的有审计操作。

另补“V3 已终结、选择记录尚未提交”这道缝：派发前冻结 reply policy/version；执行收口时须能从已提交事实唯一定位最终正文引用。恢复器据原终态、正文引用和冻结策略生成同一 selectionId，再补 artifact 与选择记录，全程不调模型。若现行终态无法给出足够引用，V0 先补 schema/投影合同，V1 再接执行器；不能退回“取最后一条 assistant”猜答复。原件缺失或选择仍有歧义就转 needs_review，不凭空补内容。

拟定投递状态：pending → sending → delivered；暂时失败到 retry_wait，永久失败到 dead_letter，发送结果不可判到 delivery_unknown。业务执行成功与投递失败分别显示，发送异常不倒改 work 执行终态。

本地投递也要幂等：固定 deliveryId 文件名，原子发布 artifact/receipt，重启核 hash。渠道发送适配器声明 client-id、receipt-query、消息长度、媒体上传与限流能力；附件上传和正文发送分开留回执。

首版不可靠持久化 token delta，不承诺断线重放流式外送；先保最终答复。若以后加进度推送，另定独立投递 ID、通知频率与可丢规则。

## 十、实施批次

新版使用 V0—V6，避免与旧 G0—G8 混号。各批先过放行门，再勾完成。

### V0：合同与持久受理底线

- [x] 更新 gateway 三篇冻结文档，删去当前权威口径中的旧存储路径、旧 Loop 恢复与组件缺失判断。（2026-09-13，`docs/architecture/gateway/` 三篇：README 批次地图换 V0—V6、目录布局对齐 §5.3 提案并废 `~/.lubancode/trajectories/...` 旧口径；contracts 各账对齐 §5.1、提交次序换 §六/§九、deliveryId 定式换 §5.2、§10 锁裁决升级、新增 §11 V0 裁决；audit-write-map 新增 §6 V0 复核补记。测试：文档型改动无独立测试册，随 CI 编译验真。）
- [x] 固定 work/occurrence/reply 身份、跨账栅栏、schema 版本和 resolver；现行 V3 不支持的关联事实先补注册。（身份定式/栅栏/schema 规矩/resolver 规则冻结于 contracts.md §11.1—§11.4；V3 注册表追加两枚 statusless kind：`gateway.work.bound`（`envelope.hpp/cpp`、`schema3.cpp` 校验器）、`reply.selection.committed`（同），schema 文档同步条目。测试：`unit.trajectory_v3.test_v3_gateway_contract`。）
- [x] 修 SessionService 受理失败仍成功、正文丢失、pending 重建、单跳来源去重问题；三端共用。（`runtime/session_service.*`：账行走 JournalWriter PowerLoss 档、`(void)` 吞返回值拔除（失败回 `operation.append_failed` 不回成功）；正文落 `operations-inputs/` 原件（原子写+fsync）；dispatched 先落稳再出队（`PopPendingInput` 三态）；resume 沿完整来源链种表并把未派发输入重落重排（账 schemaVersion=2 纯追加）；调用方 `app_server/server.cpp`、`app/one_shot.cpp` 同步适配。测试：`unit.runtime.test_session_service_durability` 七案。）
- [x] profile/账号/会话锁做实际双进程竞争测试，补原子互斥与 ownerEpoch。（`GatewayLock`/`AccountLock` 弃"先读后写"改 create-new（wbx）原子占位+身份核+有界重试；锁账加 `owner_epoch`（=boot_id，fencing）；真起 racer 子进程双进程竞争：`tests/support/gateway_lock_racer.cpp` + `unit.gateway.test_gateway_lock_race`（gateway/account 两模式，断言至多一只持锁、真撞 RefusedAliveHolder、坏锁/旧 schema 锁保守拒、epoch 互不相认）；`SessionLock` 本就 wbx 未动、未加新册。）
- [x] 定义 PowerLoss 提交原语及原件保留规则；写盘失败停止受理/执行。（规则冻结于 contracts.md §11.5：受理账行走 `JournalWriter::AppendLine(PowerLoss)`、原件走 `AtomicWriteFile(ProcessCrashDurability)`、索引可重建、原件丢失隔离、孤立原件可回收；受理面 broken 传播持续拒绝、泵侧 dispatched 落不稳停泵。测试：`test_session_service_durability` 磁盘写失败两案（append_failed/artifact_failed、broken 传播、账行不落）。）

放行（V0 口径，按"测试缝隙注入账态"验收；真拔电/真杀进程属后续批次真机验收）：accepted 前后——原件已落/账行未落不重排（孤立原件无害，`test_session_service_durability` 孤立原件案）；accepted 落稳/内存入队前崩——重建不丢输入（同册重建案，正文与键全回）；claim 前后（以 dispatched 行为可注入面）——已派发不重排无双派发（同册 dispatched 案）；开轮前后——合同与 `gateway.work.bound` 注册先行，真开轮裁决属 V1 主泵（**未验**）；磁盘写失败收不到成功回执（同册写失败两案）。重启无丢输入、无双派发已过；断电一致性与真进程硬杀**未验**。CI：PR #59（https://github.com/relic-yuexi/LubanCode/pull/59），头提交 62ea1ae4 活跃 check suite 全绿（macos-clang 20m7s 过；cloudflare 位历史常态 queued，同 PR #58，不属判绿口径）。附带落定：resume-at-launch 显式指名源不再被 LatestResumableSessionId 的"running 不碰"连坐——接管硬杀场是常驻恢复正路（`trajectory_session.cpp`）。

### V1：最短纵向闭环——一次任务到本地结果

- [x] GatewayProcess 接有界主泵，复用现有 SessionWorkScheduler；先暂停接活，再摘 wake，再收执行器和 writer。（`gateway::GatewayWorkPump` 合同口（engine 层，`src/gateway/work_pump.hpp`）+ `runtime::GatewayAutomationPump` 真装配（`src/runtime/automation_pump.*`）；`GatewayProcess::Options.pump` 递进，Run 每 tick 调 `TickOnce`（SafeMode 跳过），Shutdown 按 StopAccepting → 主循环出（摘 wake）→ Close(grace) 次序，泵没收净算 shutdown_timeout 不假写 clean；取件经 `PumpNextWork`（`WorkKind::AutomationDue` 纯追加，`session_work_scheduler.*`）；同步单飞取舍：stop 在 turn 边界生效，contracts §12 如实记。测试：`unit.gateway.test_gateway_automation_v1` 关机次序案（StopAccepting 后不取新活、Close 幂等）。）
- [x] 新增最小 AutomationStore，先支持 once 和手动 run-now；持久任务创建走本地命令/控制服务，不直接改文件。（`src/gateway/automation_store.*`：jobs.jsonl 纯追加事件账（job.created/occurrence.created/claimed/bound/settled），JournalWriter PowerLoss 档、lazy 开写者、broken 传播；occurrenceId=hash(jobId+revision+slot) 计划内时间；幂等键同载荷回原回执、异载荷 conflict。创建入口：`gateway job add|run-now` CLI 落 `control/job-*.json` 命令文件（`src/gateway/work_pump.cpp` 写/消费两侧），泵消费进账即删；`job list`/status 走只读投影 `ReadAutomationProjection` 零建目录零写盘。测试：`unit.gateway.test_automation_store` 七案（定式/建账/幂等/claim-bind-settle/重启重建/坏账容错/写盘失败）。）
- [x] 提炼共用 headless 装配，核齐真实工具 Action、结果捕获、hook、预算和取消链。（`src/runtime/headless_executor.*`：从 AgentChannelEngine/SessionService 提炼（`ApplyChannelToolPolicy` 提为共用导出）；开场/受理/派发走 SessionService 同一账路；补齐渠道引擎遗留——ToolTraceHub 挂桥真实工具 Action 落账、hook 四点（PreUser/PostUser/PreRequest 与渠道路同一派发点）、预算三根硬线（AgentRuntimeProfile 步数/墙钟/token）、取消链经 AgentLoop::Run cancel 旗贯通 wire 与工具；工具确认 fail closed 复用 ChannelConfirmAllows。未做中立 RuntimeAssembly 大框架（归 LubanCore 单阶段 B）。测试：`unit.gateway.test_gateway_automation_v1` 闭环案（含工具轮，模型 2 次工具 1 次）与取消链案。）
- [x] 建 V3 新会话、执行、提交 reply selection，经本地 DurableReplyOutbox 出结果。（执行器强制 V3（v2 场明报 `gateway.requires_v3` 拒绝不双写，册内钉 v2refuse 案）；`gateway.work.bound`（ownerEpoch/attempt/inputRef）落 V3 PowerLoss；回复冻结策略 `selectionId="sel-"+turnId`、正文=turn 内最后 assistant text 全文（`PlanReplySelection` 纯函数执行/恢复共用）；原件先落 `delivery/replies/` → `reply.selection.committed` 落 V3 → 本地 `DurableReplyOutbox`（`src/gateway/reply_outbox.*`：outbox.jsonl+replies/+out/<deliveryId>.txt；deliveryId=hash(selectionId+target+ordinal)；投递幂等——已发布核 hash 补回执、hash 不符/原件缺失 flagged 隔离）。测试：`unit.gateway.test_reply_outbox` 六案 + `unit.gateway.test_gateway_automation_v1` 闭环案（V3 流两枚 kind 断言、发布文件内容）。）
- [x] `status` 分别报 process、work、execution、delivery，不能拿“进程 running”冒充任务成功。（`GatewayStatusSections`/`ProbeStatusSections`/`SectionsToJson`/`FormatSectionLines`（`src/gateway/status.*`）：process 活探针与三栏领域只读投影分开采分开报；`gateway status` 文本面四栏、--json 加 work/execution/delivery 键。测试：`unit.gateway.test_gateway_status_sections` 三案（空 profile 零副作用、有账分报、没跑过不画成功）。）

放行：假 provider + 受控工具，进程退出重启后能查原任务与原结果；生成结束、入 outbox、发布本地文件三处硬杀，都不多调用模型或工具。（已过，CI 可重复册：`unit.gateway.test_gateway_automation_v1`——“进程退出重启”以装配销毁重建模拟（盘上账唯一真源），模型/工具调用计数落盘上 calls.log 作“不多调用”判据；重启可查案（泵销毁后经只读投影查回任务/结果/正文）；三窗注入 `fault_injection`/`fault_after_enqueue`：生成结束（AfterGeneration）/selection 已落（AfterSelectionCommitted）/入 outbox 后发布前（fault_after_enqueue）——重开后计数不多一跳、结果落文件、occurrence=succeeded；发布后回执未落窗在 `unit.gateway.test_reply_outbox` receipt-window 案（补回执不出第二份）。真杀进程/重启冒烟与断电一致性**未验**；恢复面 V1 只做“补齐不重跑”（生成未完成/claim 后崩 → needs_review 停审，重派归 V2）已验；V0 留的“V3 已开轮、work 未记 turnRef”真窗口裁决属 V2 §八全表，**未验**。） PR：#63（https://github.com/relic-yuexi/LubanCode/pull/63），头提交 81f4b0b0。

### V2：周期调度与可靠接管

- [x] 加 interval/cron、时区/DST、misfire、合并、并发与队列上限。（调度引擎 `src/gateway/automation_schedule.*` 纯函数零 IO:五字段受限 cron 子集明拒不猜、interval 锚点等差、内置时区表含 America/New_York/Europe/Berlin 两 DST 区 + Asia/Shanghai + 固定偏移串——libc++ 无 C++20 tzdb,手写规则表三腿可编译;DST 两裁:春跳缺口拍跳过不补、秋拨重复拍只取第一次出现;时区显式存储不拿进程本地当隐含值。`AutomationStore.SweepSchedule`:coalesce(默认,补一拍落最老 slot、missedCount 记覆盖范围、既有待办 occurrence.merged 并入不双建)/skip(迟到拍不补不并游标直进;迟到判定 = now-slot>宽限,interval 宽限 min(周期,60s)、cron 60s;最近一拍在宽限内算当前拍照跑)两政策;生成游标 schedule_cursor 只前进(job.schedule_advanced 行),时钟倒拨不重跑原 slot;同 job 不重叠(claimed 在途不生成不进游标);全局 open occurrence 队列帽默认 256,满则 stalled 游标不动下轮重试。**随批修正 selectionId 定式**:V1 的 sel-<turnId> 在周期任务多场共存下撞名(turnId 场内才唯一,第二轮原件内容对不上即断),V2 改 sel-<sessionId>-<turnId> 并对旧式已提交的沿用旧式(V1 在途恢复不双送;contracts §13.8)。裁决冻结 contracts.md §13。测试:`unit.gateway.test_automation_schedule`(历法/cron 明拒清单/UTC+固定偏移/工作日与 dom-dow 并集/NY-Berlin 春跳秋拨/校验)12 册 + `unit.gateway.test_automation_store_v2` 生成册 7 案。）
- [x] 加 list/read/update/pause/resume/cancel/run-now 领域操作，命令名实施时固定；所有写操作带幂等键和 revision。（命令名定为 `gateway job add|run-now|list|read|update|pause|resume|cancel|import-loop`(`src/cli/gateway_command.cpp`、`src/app/cli_options.cpp` 解析、`GatewayJob*Command` 命令文件族 `src/gateway/work_pump.*` 扩到六型);update/pause/resume/cancel 一律 `--rev <expectedRevision>` CAS(0 拒,照 GoalService 先例)+`--idem` 幂等键(同键同操作回原回执,异操作 conflict);update 改周期即重锚(revision+1,排队 occurrence 固定建账 revision 不挪);pause 停生成停派发、resume 游标直进不补跑、cancel 先停未来派发(scheduled 就地结算 cancelled 历史保留)再标在飞取消边界;run-now 沿 V1;list/read 只读投影零写盘。稳定码新增 automation.job_terminal/automation.import_conflict。测试:`test_automation_store_v2` CAS/重锚/pause-resume/cancel 册 + `test_gateway_automation_v2` 命令文件消费册。）
- [x] 按第八节实现恢复裁决；多次 resume 保持同一 work、预算与投递身份。（§八 V2 落面 `GatewayAutomationPump::RecoverOccurrence`:claim 后无绑定行(领域绑定先于一切模型/工具动作=未开轮)→ 重派同一 occurrence(occurrence.redispatched 行,attempt+1,occurrenceId 不洗),attempt 帽 kMaxAttempts=3 到顶转 needs_review(redispatch_exhausted),任务已取消转 cancelled;已绑定沿 V1:有 assistant 无 selection 补 selection、selection 在补 outbox、无 assistant(模型请求可能已发)needs_review 停住不盲跑、场对不上 needs_review。多次 resume:settled 不重开、deliveryId 定式不变、模型不重调;attempts 不随 resume 重置(帽跨重启累计)。测试:`test_gateway_automation_v2` 重派册(attempt=2、model=1、同 occurrenceId)/attempt 帽到顶册/三次 resume 册(model=1、单 occurrence、单 delivery delivered)/未知副作用册(绑定在场场对不上→needs_review 零模型)。）
- [x] 加 heartbeat 观察结果与通知去重；区分 fresh/continuation；补权限撤回与 deadline。（job 带 notifyOnChange(`--heartbeat`):成功执行记 occurrence.observed 行(结果 sha/是否变化/是否投递),正文与 job.last_observed_sha 相同→不入 outbox 结算 unchanged_notification_suppressed,投递成才更新已通知版本;检查失败不能记无变化——失败路永远投 notice-<occurrenceId> 失败通知(outbox 显式通知项与 reply selection 分账)且不污染已通知版本;恢复路同款去重。fresh/continuation:occurrenceId 定式与 session 策略无关(身份稳定),continuation 需渠道路绑定归 V3 线,本批创建面明拒不假装支持。权限撤回=pause/cancel 语义:撤回影响后续待执行 occurrence(停派/结算 cancelled),已开始的沿取消边界(cancel_requested 置 cancel 旗,turn 边界收场按 cancelled 不判失败)。deadline:job 可带 deadlineMs,过线不生成、待办 claim 面结算 cancelled/deadline_reached 不判 failed;在飞硬掐沿用 V1 墙钟预算(turn 边界,如实分账)。测试:`test_gateway_automation_v2` heartbeat 三拍册(未变静默 outbox=2)/失败必投册(notice- 在箱)/deadline 册(零模型零 occurrence)。）
- [x] 对 `/loop` 提供显式导入方案；没有导入 receipt 不暗搬、不双跑。（`gateway job import-loop <来源会话id> "正文" --task loop-N --every 秒 [--idem 键]`→ control/job-import-loop 命令文件→活 Gateway 消费→`AutomationStore::ImportLoop`:建 interval job(prompt 固定副本,不逐拍现读原 /loop 状态)+ loop.imported receipt 行(receiptId=imp-hash(sessionId+taskId));同来源(sessionId+taskId)幂等回原 receipt 不建第二个 job;结构上无暗搬路——Gateway 不扫会话 loop 状态,导入只走这条显式命令;原 /loop 状态只读留档(Gateway 不写它)。崩在"job 已建 receipt 未落"极窄窗凭 --idem 兜底,如实记窗口(contracts §13.6)。测试:`test_automation_store_v2` 导入册 + `test_gateway_automation_v2` 命令文件导入/重导幂等册。）

放行：停机跨多个周期后按政策补一拍；时钟倒拨不重跑原 slot；未知副作用停住；三次 resume 不重复执行已完成工作。（已过,CI 可重复账态注入册 `unit.gateway.test_gateway_automation_v2`:补一拍=停机跨五拍册(装配销毁重建模拟进程退出,盘上调用计数作判据——coalesce 出一枚 occurrence 落最老 slot、missed_count=4、model=1、outbox=1);时钟倒拨=游标只前进册(倒拨后 TickOnce 不出新拍不重跑,model 不变);未知副作用停住=绑定在场而 V3 场对不上→needs_review 零模型,另 attempt 帽到顶→needs_review;三次 resume=重启三轮已完成工作不重开(model=1、单 occurrence succeeded、单 delivery delivered、deliveryId 定式不变)。真起子进程具名栅栏硬杀冒烟、断电一致性、断网渠道投递**未验**(归后续批次);恢复面"V3 已开轮 work 未记 turnRef"真窗口在 V1/V2 均走领域 bound 行锚定,账态注入已验、真进程硬杀窗未验。) PR:#83(https://github.com/relic-yuexi/LubanCode/pull/83),头提交 0b86f99c 活跃 check suite 全绿(macos-clang 腿,PR 只跑 macOS 是 ci.yml 既定裁决"合入 main 后跑全平台";同提交竞态取消的重复 suite 与 cloudflare 位历史常态 queued 均不属判绿口径)。首批两红教训入账:skip 政策原实现"到点即丢"致 skip 任务永不出活(补迟到宽限判定);selectionId 旧式 sel-<turnId> 多场撞名致第二轮 reply_unavailable(改 sel-<sessionId>-<turnId>,contracts §13.8)。

### V3：渠道总装与真 transport

- [x] 接现有 ChannelManager/IngressStore/ChannelSessionHost，增加 durable work 引用，替换仅内存排队假设。（2026-09-17 审计回账：PR #75 落 `channel_work_pump.hpp`——claim=ingress queued→running 耐久态；`work_ledger.hpp` sid→场/轮绑定账 PowerLoss 先于 V3 `gateway.work.bound`；`ingress_store` 升 JournalWriter PowerLoss；生产装配 `gateway_launch.cpp:236-244`；内存队列降为投影/背压件（`inbox.hpp`）。ChannelSessionHost 旧件退役归后续清理。真进程硬杀/断电窗口归真机批次。）
- [x] 落真实 sidecar Transport、进程树收尾、重连、generation、账号锁；保留 FakeChannelSidecar 测试口。（定案变更：QQ Q1 定案进程内直连，sidecar 子进程与进程树收尾随之消失，不算未做。真实传输 `qq_gateway`（Identify/Resume/退避 1s..60s/durable 游标，PR #66/#112/#113）；底座共享层 `src/channel/transport/`（R0，PR #118）；generation 代次隔离 `manager.cpp:345/1105-1227`；账号锁 `account_lock.hpp` create-new 原子占位；FakeChannelSidecar 测试口保留在泵册全量使用。真机重连行为归 Q3。）
- [x] 路由固定 owner/workspace/profile/conversation/thread 身份，补准入、附件原件和背压。（session_key 定式 `channel_router.hpp:51`；workspace 隔离 `session_map.cpp`；配对不升 owner（PR #64）；准入全链 bot/dm/group/mention/binding_conflict + Q1b 提示限频（#90）；附件原件 `channel_media_service.*`（Q4，PR #93）；背压 `inbox.hpp` 三级帽 + 泵级 4/8 帽。群聊 scope 现直聊-only，真验归 Q3。）
- [x] ACK 严格服从 ingress 耐久回执；重复来信复用原 work；已读/已 ACK 不等于 turn 已成功。（`manager.cpp:626-646` Ingest PowerLoss 失败不 ack、退避重发；spool 先落盘再上报（`qq_spool`）；三级去重键 + `MakeChannelOperationId` 回原受理（泵册"来信重放"案模型恰 1 次）；ingress 状态机 Replied 与 Delivered 分立；执行失败走失败通知+dead letter 不互相改写（PR #114）。真平台回执语义归 Q3。）
- [x] 真渠道明确 allowlist 与工具授权交集；未决审批留 waiting/拒绝，不因无人值守自动放行。（QQ 模板 pairing+allowlist（#64）未配对零模型；ToolRoutePolicy 五层交集+deny 并集（#64）；审批带 Q6（#97）——300s 超时等价拒绝、卡片失败 fail closed、重启旧卡作废；Web 侧 AssistantApprovalBroker 超时默认拒 120s（#91）；headless 无人值守 fail-closed 零变化。真平台按钮触达归 Q3。）
- [x] 先用假渠道验完整收发，再选一只真渠道。（假渠道全链 FakeChannelSidecar 走真 Bridge 帧（泵册三窗硬杀/超时未知/限频/长文拆段，#75/#93）；真渠道三只已选已落：QQ（#66）、企微 wecombot（#121）、飞书（#119 进行中）——经 R0 注册表装配（#118）。QQ Q3 真机联调整批仍欠（配对/两轮聊天/重连/真实错误码真值），企微飞书同理。）

放行：重复、乱序、断线、ACK 丢失、send 成功但 receipt 丢失逐项扫；平台无幂等能力时明确展示 delivery_unknown。（2026-09-17 审计：mock 面逐项有册——重复=去重键/乱序=seq 账/断线=Resume+退避/ACK 丢失=spool 重投/receipt 丢失=重复回执只结一次/delivery_unknown=终态停自动重发；**真平台面全数归 QQ Q3 及企微/飞书真机批次，未验**。）

### V4：服务安装与常驻运维

- [x] 落 install/start/restart/doctor/logs；现有 stop 与系统 supervisor 的停止语义统一。（命令族进 `lubancode gateway install|uninstall|start|restart|doctor|logs [--profile X]`（`src/cli/gateway_command.cpp` 独走；解析 `src/app/cli_options.*` verb 表扩六词+`--wait-ready/--ack-safe-mode/--tail/--gateway-root`，接线 `cli_app.cpp` 映射）。停止语义统一：supervisor 语境下的停止就是既有文件控制面（投 stop 命令→drain→宽限→锁释放，超时 `gateway.shutdown_timeout` 退 4 不代杀）——uninstall/restart 的停止段走同一条路，没停干净不摘服务不再拉起；start 经服务管理器（schtasks /Run、systemctl --user start、launchctl load/kickstart）不裸 spawn，未 install 明错 `service.not_installed`；install 只注册不 start，注册前校验 gateway.json 可装载（坏拒装退 3），凭据/配置体检 Warn 以上当场明列不自动修。服务件 `src/gateway/service.*`：单元生成纯函数+服务管理器操作经可注入 `ServiceRunner` seam（生产 `platform::RunProcess` argv 数组不经 shell）+安装记录 `<profile>/service/install.json` 钉 exe 路径/lubancode 版本/单元落位。测试：`unit.gateway.test_gateway_service`（三平台生成物文本、argv 形状、缺席稳定码、install.json 严格解析、uninstall 摘除）+ `test_gateway_process` V4 解析案。）
- [x] Windows Scheduled Task、systemd、launchd 按支持范围逐个验证；先交付一平台不得宣称三平台完成。（三平台生成物全落并有 CI 文本断言：Windows schtasks XML（Task Schema 1.3，ONLOGON+PT30S 延迟、`ExecutionTimeLimit=PT0S` 不限时、`RestartOnFailure` 间隔 60s 限 3 次、cmd 重定向 stdout/stderr 到 `<profile>/logs/service.log`、路径 XML 转义）；Linux systemd user 单元（`Restart=on-failure`+`RestartPreventExitStatus=3` 坏配置按码豁免、`TimeoutStopSec=grace+15`、journal）；macOS LaunchAgent plist（`RunAtLoad`、`KeepAlive={Crashed}` 仅崩溃拉起、StandardOut/ErrorPath）。防重启风暴分平台如实（Windows 无法按退出码豁免只能限次——schtasks 能力边界，runbook 写明）。**真实验证边界**：CI 只验生成物与命令面（注入 runner），真实服务注册/拉起/登录自启/失败重启三平台真机全未验——`docs/architecture/gateway/runbook.md` §8 分平台列表；Windows 真机 install/start/stop 一轮归用户验收时走（先交付 Windows 实测路径）。）
- [x] 配置/凭据换代、账号失效、SafeMode、健康探针、死信检查、备份与升级回滚。（doctor `src/gateway/doctor.*`：体检清单一项一码零猜测——服务注册/锁活态/配置可装载/安装记录对账（`install.exe_missing` 退 2、`install.version_mismatch` Warn）/凭据可读不显值（逐账号 `ResolveChannelCredential` 全跑，明文来源 Warn；渠道激活闸其余三闸归 V3 报 `activation.deferred_to_v3` 不冒充）/磁盘可写（即写即删探针不建目录）/坏账（`ledger.needs_review` 计数）/死信（`dead_letter.outbox_flagged`+`dead_letter.channel`）/SafeMode 连击/最近关机事实；退出码稳定 0/1/2，`--json` 机器可读；doctor 零副作用合同保持（不带 --ack 零写盘零建目录）。健康探针 `doctor --wait-ready <秒>`：ready=锁活+control running+health ok+非 SafeMode（进程 running≠ready），超时如实退 1 报 `gateway.not_ready`。SafeMode 显式 ack 口（contracts §10.3 留给 V4）：`--ack-safe-mode` 落 boot-history `type=ack` 行清连击，不伪造 shutdown。凭据换代/账号失效处置与备份升级回滚手动步骤写进 runbook §5/§6（升级=stop→换 exe→install 刷记录→start→doctor；回滚手动，不做自动）。测试：`unit.gateway.test_gateway_doctor`（未初始化全 Info 退 0/坏配置退 2/坏账在飞 Warn/死信 Warn/SafeMode ack/关机未收净 Warn/安装记录对账/wait-ready 判定与超时）。）
- [x] 关机宽限到期如实记录未收净 work，不假报取消成功；重启先 reconcile 再接新活。（boot-history shutdown 行新增 `uncollected_work` 字段（occurrence id 数组）：`GatewayWorkPump` 新增虚口 `UncollectedWorkIds()`（缺省空），`GatewayProcess::Shutdown` 在泵 Close 失败时取清单写账行、`clean=false` 退 4——不结算 cancelled 不假报；`GatewayAutomationPump` 实现该口（claim→结算窗记 in_flight RAII 清口，Close 前抓残留；同步泵 V1/V2 下恒空，机制先立异步泵接上后生效）。重启先 reconcile：泵 TickOnce 次序（恢复扫描→outbox→至多一枚新执行）保证恢复裁决先于新派发（V2 §13.4 合同，V4 不改），进程没跑而账有在飞时 doctor 报 `ledger.in_flight` Warn 注明"重启后 reconcile 先裁决再接新活"。测试：`test_gateway_doctor` 假泵注入案（Close 失败→退 4、shutdown 行带清单、JSON 往返）+干净关机不带字段案+boot 行 ack/uncollected 往返案。）

放行：首个平台关闭终端、注销或重启后的行为有实测记录；24 小时常驻，断网/崩溃/坏配置不丢账、不重启风暴。具体系统监督条件写进 runbook。（**未过，如实记**：CI 已验生成物/命令面/码表（`test_gateway_service`+`test_gateway_doctor`+`test_gateway_process` V4 案全绿为门）；真机放行门——Windows 关终端/注销/重启实测、24h 常驻、断网/坏配置不丢账 soak——三平台均**未验**，runbook §8 分平台列表，Windows 真机验收路径先交付归用户走；24h soak/容量扫点归 V6。合同裁决回填 contracts.md §14，运维手册 docs/architecture/gateway/runbook.md（三平台安装形态/防风暴差异/linger/升级回滚/未验边界），audit-write-map §7 增补写盘面。）

### V5：长任务、Goal 与 Workflow

- [ ] 与[异步工具计划](SessionV3异步工具_任务执行协议配对与结果投递.todo)共用 job execution、租约、权限、结果与取消服务。
- [ ] detached task 明确从会话所有转为 Gateway 托管的交接记录；未交接任务不能因 daemon 存在自动长住。
- [ ] 任务执行结果、provider 工具协议配对、聊天渠道投递三者分账；禁止借渠道 outbox 补 tool result。
- [ ] 复用 GoalService 投影接管、预算和验收规则，补 headless 场装配与策略测试。
- [ ] 按[Workflow V3 计划](Workflow接入SessionV3_编排账与节点恢复设计.todo)调用编排服务；工作流终态不等于每个未知副作用均已解决。

放行：主聊天继续交互，后台任务完成能回送；杀 worker 只恢复可判工作；取消后迟到结果不能把任务复活。

### V6：发布验收

- [ ] 跑相关回归、完整 Release CTest、目标平台构建；报告分列假 provider 和 opt-in 真 provider。
- [ ] 72 小时 soak：CPU/RSS/线程/句柄、队列、文件与索引、写盘延迟、模型成本；初值和最终值留原始数据。
- [ ] 容量扫点：重复 ingress、拥塞会话、429/5xx、低磁盘、只读目录、坏尾、坏中段、PID 复用、双实例。
- [ ] 验 secret 脱敏、控制端身份、权限收窄、artifact 保留与垃圾回收；控制面不可跨 workspace 读写别人的任务。
- [ ] 更新能力表与用户文档，按已验收范围写“常驻自动任务/渠道助理”；缺哪个平台、渠道、恢复分支就明列。

## 十一、故障验收清单

| 注入点 | 必须看到的结果 |
| --- | --- |
| input 原件落稳，accepted 未提交 | 无成功回执；孤立原件可回收 |
| accepted 提交，内存入队前硬杀 | 重建原 work，正文和附件都在 |
| operations Append 失败 | 明确失败，不返回 accepted |
| 两个 Gateway 同时启动 | 至多一只进入业务派发；另一只拒绝 |
| occurrence 落稳，next-fire 索引未更新 | 重扫仍只有同一 slot；重建索引不漏下一拍 |
| V3 开轮成功，work turnRef 未回写 | 找回原轮，不双开 |
| shell 已写文件，terminal 未落 | unknown/reconcile，不再写一次 |
| 结果捕获失败或 post-hook 提交失败 | 原工具不重做，后续模型请求停止 |
| compact.applied 后投影失败 | 不拿旧链继续请求 |
| V3 已成功，reply selection 尚未提交 | 依原终态正文引用和冻结策略补同一 selection，不调模型；材料不足则停住 |
| reply selection 已提交，outbox 尚未投影 | 从已提交回复选择补出同一 delivery |
| 本地文件已发布，receipt 未落 | 核 deliveryId/hash 后补回执，不出第二份 |
| 渠道已发送，receipt 未落 | 幂等查询或 delivery_unknown，不当作普通网络失败盲重试 |
| 连续 resume 三次 | work/预算/原回复身份不变，去重穿过完整来源链 |
| pause/cancel 与到点派发竞争 | revision 决胜；未派发受取消约束，已执行事实保留 |
| 旧 worker 在换 owner 后回报成功 | epoch 拒绝旧提交，保留外部对账线索 |
| 归档/GC 碰上待投递 artifact | 引用保留，无法安全删除就拒绝 |
| 只打开历史/断开 AppServer | 零新增执行、零新增平台发送 |

测试驱动应真实起子进程，在具名栅栏硬杀再启动，核副作用计数、V3 引用与领域状态。只改 JSON 尾行可以测读取合同，不能独自证明进程级恢复。

## 十二、与相邻计划如何分工

- [AppServer V3](AppServer接入SessionV3_统一入口与事件恢复设计.todo)：共用 SessionService 与操作身份；本单负责常驻接管，不私造另一套输入接纳语义。
- [多渠道计划](多渠道消息接入与常驻ChannelPlugin设计.todo)：平台协议、账号与 transport；本单负责把 ingress、执行与 delivery 接成一条路。
- [异步工具计划](SessionV3异步工具_任务执行协议配对与结果投递.todo)：执行作业和 provider 配对；本单只提供持久托管与触发，不重定工具协议。
- [Workflow V3](Workflow接入SessionV3_编排账与节点恢复设计.todo)：图账与节点执行；Gateway 只看外层任务及编排引用。
- [工业化多协议接入](工业化多协议接入_能力裁剪与工具扩展统一设计.todo)：共同引擎装配与能力配置。无需等全部工业化目标完成，但 headless 工具/权限链是 V1 硬门。
- [上层应用方向](LubanCode上层应用方向_场景组合与独立项目候选.todo)：决定替谁办哪件事，Gateway 为其提供常驻底座。业务逻辑不塞进调度器。

这次先把 V0、V1 做透：受理落得住，执行查得清，结果送得稳。那条路打通，再往上添周期、渠道和长任务，才不至于处处有零件，重启却没人能把账接上。
