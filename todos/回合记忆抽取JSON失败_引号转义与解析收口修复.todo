# 回合记忆抽取 JSON 失败：引号转义、解析收口与失败诊断

- 状态：P0-A/P0-B/P1-A 已落地（PR 见下方"落地记录"）；P1-B 评估后暂缓，默认不开；事故原始响应仍缺（未核实标记见 P0-A 首条）。
- 日期：2026-09-14
- 级别：P1；字段类型异常越过抽取失败边界，须优先补齐。
- 范围：memory 抽取提示、解析、采样结果收口、错误显示、旁路诊断与测试。
- 关联：`记忆写入调度_显式保存门控批处理与成本账.todo`、`失败与恢复_写账闸门工具结果与重试收口.todo`。
- 边界：本单不改记忆权限、review/auto 闸门、召回算法或默认模型；暂不展开批处理调度。

## 一、结论与证据边界

用户日志：

```text
[memory] 回合总结(code)… · cheap:minimax-m3
[memory] 回合总结失败,本轮跳过: 抽取输出不是合法 JSON:
[json.exception.parse_error.101] parse error at line 1, column 43:
syntax error while parsing object - invalid literal;
last read: '"用户问"�'; expected '}'
```

已确认：回合尾抽取拿到文本，截出花括号片段，JSON 解析失败。此处直接返回，尚未处理本次候选。
提示词要求输出 JSON，宿主却没有给这次请求接结构校验与格式恢复流程。

高疑原因：summary 里混入未转义双引号，字符串提前结束，后续汉字落到字符串外。
也可能是缺逗号或别处语法损坏，必须看原始响应才能定案。

日志里的 `�` 不能单独证明模型输出、网络或终端编码坏了。用项目现有 nlohmann/json
头文件已复现：输入全是合法 UTF-8，仅引号出错，异常文本照样带 `�`。

本轮未取得事故对应的原始响应、实际 wire 请求、stop_reason 与运行二进制版本。
不把当前源码等同于事故当时版本，不据此认定 minimax-m3 本身有缺陷。
日志上方 `Worked for 22s` 也不能直接当作这次抽取耗时。

## 二、源码调用链

```text
ExtractTurnMemory
  → BuildTurnTranscript / BuildExtractionSystemPrompt
  → ModelRouterService::Sample(MemoryExtract)
  → SampleModel → MessageAssembler
  → FinishMemoryExtraction
  → ParseExtractionJson
  → 失败：打印提示、记失败账、return
  → 成功：更新检索词、处理候选、沿既有闸门排写入任务
```

以下行号取自本次工作区；实施时以符号为准。

| 落点 | 当前行为 | 缺口 |
| --- | --- | --- |
| `src/app/commands/memory_commands.cpp:609` | 构造 SampleCall；请求 max_tokens=1500，timeout=45 秒 | 调用未接 schema，也没有格式失败后的重试 |
| `src/app/model_router.hpp:76`、`src/app/model_router.cpp:127` | SampleCall 只传 system/messages/max_tokens，路由补模型与 effort | 不能从这里传 output_schema 或 enforce_output_limit |
| `src/agent/sample_model.hpp` 的 SampleRequest | 已有 output_schema，但仅做本地复检，不上 wire | 不能把这个字段当成 provider 强制 JSON 能力 |
| `src/agent/sample_model.cpp` 的 SampleModel | 拼接 TextBlock，保留 stop_reason；发送无错且无 StreamError 即 ok=true | 采样成功不等于抽取文本完整、合法 |
| `src/app/memory_extract.cpp:229` | 只查 sampled.ok 和正文是否为空，随后解析 | 未按 stop_reason 分清长度截断；未检查 schema_ok |
| `src/app/memory_extract.cpp:129` | 从首个 `{` 截到最后一个 `}`，再严格解析 | 围栏可过；多对象、正文含花括号、未转义引号仍会失败或取错片段 |
| `src/app/memory_extract.cpp:145` | 用 root.value/item.value 取字符串 | 字段类型错会抛 type_error；这些语句在 parse 的 catch 外 |
| `src/app/memory_extract.cpp:645` | 多种抽取错误统一映成 parse_failed | 分不清语法错、编码错、字段错、截断 |
| `src/app/commands/memory_commands.cpp:647` | 把异常文本直接送终端，再记失败账并返回 | 异常夹带片段、半个 UTF-8 字符，既难读也难定位 |
| `src/prompts/features/memory-summary-base.md:7` | 要求只输出 JSON，给了一份格式模板 | 未专门示范正文引号、反斜杠、换行如何转义 |

转写截断用 Utf8PrefixBoundary（memory_extract.cpp 的 ClipBytes/BuildTurnTranscript）。
MessageAssembler 对 TextDelta 直接追加字节（src/api/assembler.cpp:71）。这些代码不能证明
整条 wire 链无误，却足以说明：现在不该见到 `�` 就去改中文编码。

另记一项预算边界：1500 是调用方请求值。本路未启用 enforce_output_limit；事故实际出站
上限须对照 backend 最终请求与配置覆盖，不能只读这一处赋值便当成硬上限。

## 三、最小复现

本轮用 g++ 编译独立小程序，包含项目现有头文件目录：
`build/release/_deps/nlohmann_json-src/include`。未调用 provider，未改生产源码。

输入：

```json
{"task_type":"code","summary":"用户问"问题"","candidates":[]}
```

执行 nlohmann::json::parse 后得到：

```text
[json.exception.parse_error.101] parse error at line 1, column 42:
syntax error while parsing object - invalid literal;
last read: '"用户问"�'; expected '}'
```

与事故日志差一个字节位置，错误类型、上下文和 expected 内容一致。
输入里的“问”是完整 UTF-8；异常末尾只留下首字节 E9，随后便接 ASCII 单引号。
终端显示 `�`，源头在诊断字符串，不必先假定输入乱码。

正确写法：

```json
{"task_type":"code","summary":"用户问\"问题\"","candidates":[]}
```

还复现了两项解析边界：

- `{"summary":42}`：parse 成功，随后 value("summary", std::string()) 抛 type_error.302。
- `{}`：目前接受，summary 落空。缺字段应否接受，需要明确合同，不能靠默认值掩过去。

上述结果只证明故障机制可复现，不证明拿到了事故原文。

## 四、修复合同

1. 模型文本按不可信输入处理；语法、编码、字段错均从抽取接口返回稳定错误，不能越过这层抛出。
2. 任何失败都不得排候选或记“写入成功”；主回合已完成的回答与工具效果保持原状。
3. 不用正则盲补引号、逗号、花括号，不从半截对象中捞候选入库。
4. 围栏兼容限定为明确包装；遇到多对象或歧义文本，拒绝选一个碰运气。
5. 成功解析也要走既有证据、敏感信息与 review/auto 闸门；修格式不授予额外写权限。
6. 失败请求照记 usage；重试若启用，每一发另记请求与费用，同一结果只采用一次。
7. 保留 cheap 路由与现有默认配置，不拿换模型、加 token 当已证实根因。

## 五、分期落地

### P0-A：补事故证据与安全诊断

- [x] 若对应 V3 旁路轨迹仍在，按 purpose=memory_extract 定位 request/output，核对模型、wire、版本、stop_reason、usage 与原文；缺失就如实标注。
  ——**未核实**：本单没有事故现场的 V3 旁路轨迹文件（日志未附会话目录），原始响应、实际 wire 请求、stop_reason、运行版本仍缺。诊断结构（ExtractionError.request_id/stop_reason/body_bytes）已就位，轨迹在场时这些字段可回查。
- [x] 定义结构化抽取错误：syntax_invalid、utf8_invalid、schema_invalid、output_truncated、empty_output、transport_failed；兼容旧 parse_failed 消费方。
  ——`src/app/memory_extract.hpp` 的 `ExtractionErrorCode` 七枚（六类 + route_miss）+ `ExtractionErrorCodeName`；`StableExtractErrorCode(const ExtractionError&)` 出新码，旧 `StableExtractErrorCode(const std::string&)` 保留（parse_failed = syntax/utf8/schema 三类旧统称，注释与测试钉了映射）。
- [x] 诊断记录关联请求 ID、正文总字节数、错误字节偏移、UTF-8 校验结果与结束原因；区分截取片段偏移和原文偏移。
  ——`ExtractionError` 五字段齐；语法错偏移按"截取段偏移 + 段首原文偏移"换算回原文偏移（memory_extract.cpp 的 parse catch，`begin + e.byte`）。
- [x] 终端只显示短错误与定位号；不直接透传含 last read 的库异常。确需查原文时复用受控轨迹，不新建默认明文转储。
  ——memory_commands.cpp 的失败分支只打 `extraction.error().message`（短文案 + 字节偏移/总字节）；测试断言 message 不含 "last read" / "json.exception"。
- [x] 将本单最小复现纳入回归夹具。UTF-8 输出诊断须合法；不得把密钥或整段聊天塞进错误账。
  ——test_memory_extract.cpp 的"本单事故形态"用例（原文复刻 §三输入）；"诊断文案自身是合法 UTF-8"用例跑 `platform::IsValidUtf8(message)`。

验收：能分清“输入字节坏了”和“合法中文落在 JSON 字符串外”；没有原文时给出待核实结论。
——达成：utf8_invalid/syntax_invalid 分类测试两行分开钉死；事故原文待核实已在上方如实标注。

### P0-B：收住解析与字段异常

- [x] 先校验完整响应 UTF-8，再按明确规则处理纯 JSON 或单层围栏；保留已有无歧义前后说明兼容须写明规则并加用例。
  ——ParseExtractionJson 第 1 步 `FirstInvalidUtf8Offset` 整段预检；包装规则（纯 JSON / 单层围栏 / 前后说明）写进函数头注释与"包装规则"测试用例；前后说明的放行规则钉死：前导与尾巴均不得含结构字符 `{`/`[`。
- [x] 替换首 `{` / 末 `}` 的盲截法；字符串转义、嵌套、多个对象都要有确定结果。
  ——字符串感知配对扫描 `MatchBrace`（字符串内 `{}`/`"`/转义全跳过）+ 单层围栏剥离 `StripSingleFence`（嵌套/多围栏/围栏外还有话一律不剥）；多对象、半截对象（外层截断但内层已有 `}`）、字符串内花括号均有测试用例。
- [x] 定义字段合同：顶层必须 object；summary 等字符串字段显式检查；缺必填字段、null、数字、数组须返回字段路径。
  ——task_type/summary 必填 string（summary 非空）；`ContractString` 显式判型，错误带 `field_path`（如 `candidates[0].title`）；`{}` 拒、顶层数组拒。
- [x] 明确候选字段出错策略：结构类型错拒绝整次抽取；无效业务候选可沿既有规则跳过，禁止静默类型转换。
  ——候选条目非 object、kind/title/summary/content/confidence/occurred_at/keywords/paths 类型错 → 拒整次带路径；kind 枚举外/title 空/content 空/超预算 → 跳过该条；confidence 枚举外清洗成 inferred（不再冒充高置信）。
- [x] 覆盖整段 JSON 解码与字段提取的 nlohmann 异常边界，向上返回 expected 错误。
  ——parse 的 parse_error/json::exception 双层 catch（只记 exception id，不带 e.what()）；字段提取整段包 catch 收进 schema_invalid——`value()` 旧路全部换成 `contains + at + is_string`，§三复现的 type_error.302 不再有触发面。
- [x] FinishMemoryExtraction 检查已知截断结束原因；未知/缺失结束原因单列诊断，不仅凭空值判失败或判完整。
  ——`IsTruncationStopReason`（max_tokens/length/max_output_tokens，小写化比对）→ output_truncated；未知值与空 stop_reason 照走解析，诊断字段 `stop_reason` 单列原值（测试两行钉死）。

验收：坏 JSON、错字段和半截输出只让本轮抽取失败；不会越界抛出，也不会新增候选/job。
——达成：test_memory_dispatch_ledger.cpp 的"抽取收口 e2e"三幕（坏 JSON/截断/流内错）断言主回合历史不动、候选区空、零重试。

### P1-A：压低格式出错率

- [x] 给基础提示补一份真正可输出的短样例，展示双引号、Windows 反斜杠、换行转义及 candidates=[]。
  ——`src/prompts/features/memory-summary-base.md` 新增"字符转义"节：样例本身是合法 JSON（`\"`、`D:\\repo\\src\\app`、`\n`、`candidates":[]`）；顺带写明中文引号不必转义、content ≤1500 字。
- [x] 统一两条入口：RunMemoryExtraction 与 ModelRouterService 调用链使用同一字段合同和收口函数。
  ——两条入口都设 `MemoryExtractionOutputSchema()`、都收口在 `FinishMemoryExtraction`（memory_commands.cpp 的 SampleCall.output_schema + RunMemoryExtraction 的 sample.output_schema）。
- [x] 如复用 SampleRequest.output_schema，先补 SampleCall 透传和 schema_ok 消费；检查公共本地校验对 discarded JSON 的处理，不能只用 is_null 判断 parse 失败。
  ——`SampleCall.output_schema` 透传进 `ModelRouterService::Sample`（test_model_router.cpp 有透传断言）；sample_model.cpp 复检改 `is_discarded()` 判 parse 失败，字面 `null` 交给 schema 按 type 拒（test_sample_model.cpp 新用例钉死）；schema_ok=false 时错误诊断并进 `ExtractionError.schema_check_error`。
- [x] 核对请求输出上限与最终 wire 上限。增加候选正文预算，先减冗长输出；不要无证据扩大默认上限。
  ——max_tokens=1500 两处调用点原样保留（不无证据扩大）；本路未启用 enforce_output_limit——事故实际出站上限因原文缺失无法核实，待 P0-A 首条的轨迹回查。候选正文预算 `kMaxCandidateContentBytes`（8 KiB，与写路 kMaxTopicBytes 对齐）+ 提示端 1500 字；超预算候选整条跳过（不截断、不静默改写）。
- [x] provider 结构化输出另按实际 wire 能力接入。能用才用，不支持则保留本地校验；仅设置 output_schema 不算已接通。
  ——**未接通（按单子口径如实标注）**：api::Request 没有 output_schema 字段（四家协议各异，批六 API 合流再议），本单只做本地复检，provider 侧约束未接。

验收：两条入口对同一文本给出同一结果；本地 schema 校验、provider 约束、最终字段验证各自有证据。
——两条入口同一收口函数达成；本地 schema 校验与最终字段验证各有测试；provider 约束这层未验证（见上）。

### P1-B：可选、有预算的格式重试

- [ ] 前两批完成后再评估一次重试；默认不开。仅对明确格式错使用，取消、权限/鉴权错误不得触发。
- [ ] 若启用，最多补一发，限定总耗时与 token 预算，沿原路由重新生成；不重跑主 Agent 或工具。
- [ ] 原始响应若带入重试，只能当待校验数据；生成结果重新过全部字段与写入闸门。
- [ ] 两发请求分别记 usage/耗时，记录最终是否采用；最终失败照常跳过，不称已修复。

**P1-B 评估意见（2026-09-14，P0-A/P0-B/P1-A 落地后）：默认不开，暂缓实施。**
理由：① P0-B 把失败收成六类稳定错误后，格式失败只损失本轮总结，不伤主回合、不丢账，重试的边际收益小；② P1-A 的转义样例 + 字段合同 + schema 复检三道闸先压出错率，效果没有真机数据前加重试是双倍成本拍脑袋；③ 事故原文与真实出错率（§一）未取得，无法估重试命中率，先攒 error_code 账（trajectory 的 memory.extraction.assessed 已带 syntax_invalid/utf8_invalid/schema_invalid 分类）再评估有据；④ 若日后启用，钩子位就留在 memory_commands.cpp 失败分支——ExtractionError 的 code 已能精确筛"仅格式错"（syntax/utf8/schema 三类，transport/cancel 不触发），usage 分笔记账路（BackgroundCallAccounting/ledger）现成，两发各记各账的合同可直接落。等真实 error_code 分布出来再立小单。

## 六、测试与收官证据

扩展现有 `tests/unit/memory/test_memory_extract.cpp`，按需补
`tests/unit/agent/test_sample_model.cpp`、`tests/unit/memory/test_model_router.cpp`
及回合尾宿主集成测试。测试这些行为：

| 场景 | 预期 |
| --- | --- |
| 引号已转义、中文引号、中文/emoji、Windows 路径、转义换行 | 合法内容完整保留 |
| 本单引号错误、漏逗号、全角标点落在语法位置 | syntax_invalid；诊断能正确显示 |
| 真正残缺 UTF-8 与合法 UTF-8 语法错 | 分类不同 |
| `{}`、顶层数组、字段 null/数字/对象、候选字段类型错 | 按合同拒绝，不抛出 |
| 围栏、前后说明、字符串内花括号、多对象、外层截断但内层已有 `}` | 只接受无歧义包装，不误收半截对象 |
| max_tokens/length 结束、缺收尾事件、空正文、StreamError | 分类准确；失败账保留 |
| JSON 错发生于已完成主回合之后 | 主回答保留，候选/job 数不增加 |
| 重试成功与连续失败（仅在实现后） | 请求数有界、费用齐全、只采用一次 |

收官时附三层证据：纯函数回归、fake backend 到回合尾的集成、真实配置的 provider 验证。
真实验证记录模型/wire、有效输出上限、stop_reason、失败类别与样本数；报告本轮观察值，
不把少量成功样本写成“再不会失败”。没有跑真 provider，就保留未验证标记。

## 七、本轮交付记录

- 已查当前源码与现有测试，独立 C++ 探针复现同款 parse_error 和字段 type_error。
- 只新增这份 TODO；未修改生产代码，未跑全仓测试，未调用真实模型。
- 事故原始响应仍缺；具体是哪一个引号或字节出错，待按 P0-A 核实。

## 八、落地记录（P0-A/P0-B/P1-A，2026-09-14）

- PR：https://github.com/relic-yuexi/LubanCode/pull/78（头提交 b0835335，CI run 34845039402 rollup conclusion=success：lane/linux 与 macos-clang 两腿绿；中途两红均为测试夹具笔误——hex 转义贪婪、样例漏收尾 `}`——已修）。
- 分支 `worktree-agent-memory-json`，基线 origin/main @ 7f195c50。
- 改动面：
  - `src/app/memory_extract.hpp/.cpp`：ExtractionError/ExtractionErrorCode（七枚）、MemoryExtractionOutputSchema、kMaxCandidateContentBytes、解析器三步收口（UTF-8 预检 → 包装规则 → 严格 parse + 字段合同）、FinishMemoryExtraction 六类分诊、StableExtractErrorCode 双版。
  - `src/agent/sample_model.cpp`：output_schema 复检改 `is_discarded()` 判 parse 失败（字面 `null` 交 schema 按 type 拒）。
  - `src/app/model_router.hpp/.cpp`：SampleCall.output_schema 透传（本地复检，不上 wire）。
  - `src/app/commands/memory_commands.cpp`：SampleCall 带 schema、RouteMiss 结构化、终端短文案、新稳定码。
  - `src/prompts/features/memory-summary-base.md`："字符转义"节 + 可输出样例 + content 预算。
  - `tests/manual/locomo_retrieval_driver.cpp`：适配新错误类型（.message）。
- 测试：`tests/unit/memory/test_memory_extract.cpp`（§六 八行表的纯函数层）、`tests/unit/memory/test_memory_dispatch_ledger.cpp`（回合尾三幕 e2e：坏 JSON/截断/流内错）、`tests/unit/agent/test_sample_model.cpp`（discarded 判定）、`tests/unit/memory/test_model_router.cpp`（schema 透传）。
- §六 三层证据：①纯函数回归 ✓；②fake backend 到回合尾集成 ✓；③真实 provider 验证 **未跑**（无事故原文、未调真模型）——有效输出上限、真实 stop_reason、失败类别分布均未验证，不冒充。
- P1-B：评估意见见上节，默认不开。
