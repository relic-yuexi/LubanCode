# 多智能体真机回归：可视区、重排与查看态

状态：待做（2026-08-16，接 127.0.0.1:8000 真模型实测）

## 基线

- 仓库 HEAD：`6207837`
- 程序：0.26.0 Debug；含当前多智能体 UI 工作树改动
- 模型：`/data2/shared/models/Qwen/Qwen3.8-27B`
- 终端：Windows Terminal
- 测试目录：一处临时小项目，`alpha/`、`beta/` 各放数篇 Markdown；不用仓库 tests

## 已确认

### 1. 主回合收口后，composer 与代理坞掉出可视区

主模型派出两只后台代理，随即结束本轮。正文、工具卡、token 行都画完了，终端却停在 token 行。输入框和代理坞已画在下方，却不在 viewport 内。

- `Ctrl+End` 只把新到的“后台子代理完成”通知带进来，仍见不到 composer。
- 按一次 `Ctrl+-`，借终端 reflow 重排，输入框才重新露面。
- 这便是用户口中的“main 回不去”：程序未必死，手却够不到 main。

要求：凡回合从 streaming 转 idle，须把 composer 顶边、代理坞、状态栏一并纳入可视区。不能靠改字号、拖窗、滚轮救场。

### 2. 带后台任务时改字号，曾出现一次 code 1 退出

另起两只 45 秒后台任务。主回合尚在派发阶段，按一次 `Ctrl+-`。Windows Terminal 随即显示：

```text
[已退出进程，代码为 1 (0x00000001)]
现在可以使用 Ctrl+D 关闭此终端，或按 Enter 重新启动。
```

这次只撞见一回，先按高危疑案记，不冒充稳定复现。须在 Debug/ASan 下连跑三轮：启动中、运行中、完成回流时各改字号十次。若再出，留异常栈、最后一帧状态、后台线程数与退出路径；若不出，也把退出码来源查清。

### 3. 已完成代理自动退场，这条本轮通过

两只代理完成后，底下代理坞会自动收掉。reflow 后也没有残留已完成行。别把这条又改坏。

## 仍须补齐的交互验收

1. 运行中，`Up/Down` 只挪选择光标，不换上方内容。
2. 只有按 `Enter`，才切到所选 agent。
3. 选中 agent 后，继续按 `Up/Down`，上方视图保持原样；再按 `Enter` 才换。
4. agent 视图要像 main：有用户任务、思考、正文、工具卡、统计与当前状态。不能只剩一张 tool-use 流水单。
5. `Esc` 逐层退回列表，再退回 main。草稿不丢，焦点不串。
6. 某 agent 完成时，若用户正在看另一只，不抢焦点，不把完成通知糊到当前 agent 帧上。
7. 完成结果回流 main 后，main 的 context 增长；用户回 main 才看见主会话整理后的输出。

## 实现落点

- viewport 跟随、reflow、窗口尺寸变化只走一处帧账。别让正文 painter、composer、代理坞各自猜光标。
- idle 交接须是一笔事务：收 streaming footer，写最终正文，画 composer/代理坞，最后保证锚点可见。
- `WM_SIZE`/ConPTY resize 到来时，只重算布局；后台任务表、查看态 id、输入草稿不得重建或清空。
- 给真控制台驱动添截图回归。至少覆盖 80×24、120×35、全屏、改字号、滚动缓冲区非底部五种现场。

## 完成标准

- 上述 7 条交互验收全过。
- 三个时机各改字号十次，进程不退、任务不丢、main 可回。
- 回合收口后无需任何补键，composer 始终可见。
- 完成代理自动退场仍保持通过。

