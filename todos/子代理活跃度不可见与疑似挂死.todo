# 子代理活跃度不可见与疑似挂死

状态：待做（2026-08-16 用户真机:general-purpose #1「编写中国象棋网页游戏」运行 65.7s+,坞行恒显"思考中(0 次工具调用 · 0 tokens)"）

## 现场与核心问题

子代理跑长任务,dock 行只有"运行中(N 次工具调用 · 0 tokens · 时长)"。思考阶段一个工具没调、usage 要流结束才有——于是 65 秒甚至几分钟里,唯一在动的数字是秒表。用户无法区分:

```text
A. 模型真在长思考(xhigh 自适应思考,大任务合法地想几分钟)
B. 流真的挂死了(超时没兜住/回调没走到/连接半死)
```

代码事实(已核):
- `agent_tool.cpp:1016` 思考增量确实写 `task->pending_reasoning`,正文增量写 `pending_text`(1008)——但**没有任何用户可见出口**:dock 行不读它们,无落盘日志,查看态要等事件边界切段。
- 子代理 detached 后端是薄壳包主后端,理论继承 LowSpeed 空闲读超时(`anthropic/client.cpp:355` 一带)——**但未经证实**:若 detached 构造链路丢了超时参数,或超时阈值配得极大,B 就永远挂着。
- 用户问得对:"你怎么知道真入了账?"——不知道。没有证据通道。

## 要做

### 一、dock 行实时活跃信号(治"看不见")

运行中任务的坞行与查看态加一条实时进度:
- `思考中 · 已收 N 字思考`(pending_reasoning 字节,节流 1s 刷新)
- 正文流式时 `正文 N 字`
- 工具执行中 `工具 <名> · M 秒`
- 首字节耗时(从发起请求到首个 delta)
已有 TouchTasks/TaskRevision 机制,回调里加计数即可;dock 行文案按当前阶段换,别堆三段。

### 二、子代理流事件诊断日志(治"无法证明")

环境变量 `LUBANCODE_DEBUG_SUBAGENT=1`(或 =<路径>)时:
- 每个子代理流事件落一行到 `~/.lubancode/logs/subagent-<task_id>.log`:时间戳/事件型(delta.thinking/delta.text/tool_use/usage/done/error)/字节数/累计
- 请求发出与首事件各记一行(首字节耗时可查)
- 错误与超时也落——B 场景当场现形
- 不打正文与思考内容,只打计数与类型(密钥/正文不进日志的既有规矩)
- dock/`/doctor` 提一句这开关存在,便于下次教用户自查

### 三、detached 超时链路核查与兜底(治"理论上")

- 实测:子代理用一台死端口/挂起的服务,确认 stream_idle_timeout 真会掐断并给 TaskOutcome(api_error 带超时原因),不是永远挂着
- 若 detached 构造丢了超时,补上(继承 main 的 connect_timeout/stream_idle_timeout/request_timeout)
- 子代理整轮加墙钟上限兜底(如 30 分钟,可配 `subagent.wall_clock_timeout_secs`,0=不限):哪怕所有超时全失效,任务也不能无限占着坞行

### 四、验收

- 长思考任务:坞行秒级跳动"思考中 · N 字",不再是死秒表
- `LUBANCODE_DEBUG_SUBAGENT=1` 起会话,log 文件逐事件可数,首字节耗时可查
- 死端口子代理:在 stream_idle_timeout 内翻成 api_error 终态,dock 行换错误短因,不再永挂
- 墙钟兜底:构造一个永不返回的假后端,确认到点收杀并留账
- 空闲路回归:正常任务 dock/查看态/退场不变化

## 不做

- 不把思考正文打进日志或 dock
- 不给思考设人为上限(该想多久想多久,只要求看得见)
- 不改思考折叠与消息账的既有切边规矩
