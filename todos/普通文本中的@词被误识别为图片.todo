# 普通文本里的 `@词` 被误识别成图片

状态：待做（bug）

## 现象

在普通中文里写到 `@cache` 一类提及时，输入没有照常发给模型，反倒报错：

```text
[错误] 不认得图片格式: cache(只收 png/jpg/jpeg/gif/webp)。
```

这次原本只想记下一段解题思路：

```text
看到没有，记录一下我的思维模式句漂亮话。它并没有交代答案是怎么长出来的。更容易解释。所谓“逆转一下想法”，多半是解题者
```

正文中夹了一处 `@cache`，便被图片入口截走。普通话还没送出去，先吃了一记图片格式错误。

用户判断这事可能已有人在修；这张单仍留着，日后据此复查，免得修了一头又漏一头。

## 病根

`src/cli/image_input.cpp` 的 `ParseInlineImagePaths` 眼下只看两件事：

- 当前字符是不是 `@`；
- `@` 前头是不是空白，或它正好在行首。

条件一中，后面直到下一处空白的整段文字便算“图片路径”。它不先看扩展名，也不分普通提及、邮箱、标签、程序记号与文件引用。

随后 `PrepareImageInput` 把每个候选都交给 `LoadImage`。`@cache` 没有图片扩展名，于是落到 `UnsupportedType`，整条用户消息一并失败。

现有测试还把 `@notes.txt` 写死成“图片入口应报不支持”。这条旧语义正是冲突所在：内联 `@` 太宽，图片加载又太严，两层合起来便会误伤正文。

## 想要的规矩

### 1. 普通 `@词` 留在正文里

- `@cache`、`@todo`、`@某人`、`foo@bar.com`、代码里的装饰器或注解，都不得自动当图片。
- 没有明确图片模样的 `@词`，原样留作 `TextBlock`，不能报图片格式错误。
- 一条正文里即使有多个普通 `@词`，也须整段照常送出。

### 2. 明确图片引用仍照常工作

- `@shot.png`、`@screens/error.JPEG`、`@"shots/last one.webp"` 仍可附图。
- 后缀像图片、文件却不存在时，仍报“文件不存在”；不能悄悄当正文，叫用户误以为图已送出。
- 图片后缀正确、内容损坏时，仍报“图片无效”。
- `/image <路径>` 是明确命令，继续走严格校验；给了 txt、空路径或坏文件，都应报清楚。

### 3. 别把“文件提及”与“图片附件”永远绑死

若后头要做通用 `@文件`，先由一层 mention/path 解析器认出引用，再按文件类型分派。图片模块只接已经判定为图片的路径，不该吞下所有 `@` token。

首版若只修眼前 bug，可把内联候选收窄为“扩展名属于 png/jpg/jpeg/gif/webp”；`/image` 仍保留严格入口。别用“路径当前存在”作唯一判断，否则 `@cache` 恰逢本地有同名目录时，毛病又回来。

## 代码落点

- `src/cli/image_input.cpp`：收窄 `ParseInlineImagePaths`，或把它改成能区分普通提及与图片候选的解析结果。
- `src/cli/image_input.hpp`：注释写清内联 `@` 的判定规矩。
- `tests/test_image_input.cpp`：改掉 `@notes.txt` 必报图片错这条旧断言，补普通提及与混合正文回归。
- `src/app/turn_runner.cpp`：确认准备图片失败时才拦消息；没有图片候选时，正文须原样过关。

## 验收

- 输入含 `@cache`，不报错，模型收到的正文逐字不变。
- 行首、句中空白后、中文标点旁的 `@cache` 都不触发图片加载。
- `foo@bar.com` 不触发。
- `@cache @todo @某人` 可同句发送。
- `@shot.png`、大写后缀、相对路径、绝对路径、带空格引号路径仍能附图。
- `@missing.png` 报文件不存在；`@broken.png` 报图片无效。
- `/image notes.txt` 仍报格式不支持；`/image shot.png` 行为不变。
- 正文同时含 `@cache` 与 `@shot.png`：普通提及留在文本里，图片只附一次。

## 测试样本

```text
看到没有，记录一下我的思维模式。@cache 里那句漂亮话，并没有交代答案是怎么长出来的。
```

还要补这些纯函数样本：

```text
@cache
提到 @todo 再往下说
邮箱 foo@bar.com
代码 @decorator
看图 @a.png
看图 @"a b.webp"
先提 @cache，再看 @a.png
```

## 不做

- 不靠吞掉所有图片错误来遮住误识别。
- 不把找不到的 `@x.png` 悄悄降成普通正文。
- 不破坏 `/image` 的明确命令语义。
- 不在这张单里顺手设计完整的通用文件附件系统。
