# Package 驱动的自进化闭环设计

- 状态：待实现
- 日期：2026-08-28
- 前置：`todos/统一Package封装与组件挂载系统设计.todo`
- 相关：`todos/自定义Agent与PromptProfile设计.todo`、`/record`、项目记忆、Workflow、`/goal`
- 范围：经验采集、能力提炼、Package Candidate、评测、批准、晋升、灰度、回滚与演化账
- 不在本单：让模型直接改写 LubanCode 核心并自动发布、无人值守执行未知代码、远端 Package 市场、跨用户联邦学习

## 一、这件事究竟是什么

LubanCode 已有记忆、录制生成 Skill、Workflow、Agent、Plugin、MCP，又在设计统一 Package。
这些东西一旦接成闭环，便有了自进化的骨架：

```text
任务运行
  -> 收成败证据
  -> 找出可复用做法
  -> 生成 Package Candidate
  -> 静态校验
  -> 回放与留出评测
  -> 用户批准
  -> 原子安装
  -> 小流量启用
  -> 观察、晋升或回滚
```

这里的“自进化”不是 LubanCode 偷偷改自己。它是从真实工作里提炼新本事，封成
可查、可验、可卸的 Package，再由宿主按规矩挂载。

Package 是产物，不是评判者。它只装能力。候选、评测、批准、晋升、回滚合起来，
才是自进化闭环。

## 二、为什么产物要落成 Package

只写进 Memory，能记住一句事实或偏好，装不下一套完整本事。

只生成 Skill，能记住做法，难以同时带上 Agent、Prompt、Workflow 与工具依赖。

直接改核心，风险又太大。一次偶然成功，不能拿来改整套系统。

Package 恰好夹在两者中间：

```text
Memory                         记一句话
Skill                          记一套做法
Workflow                       固定步骤、分支与收口
Agent + Prompt                 固定谁来做、怎样想、拿什么能力
Plugin / MCP                   添一件新工具
Package                        把上面几样封成一份可安装能力
LubanCode core patch           改宿主本身，另走源码审查与发布
```

一份进化产物可能只有 Skill，也可能是一整套 Agent + Prompt + Skill + Workflow。
只有现有工具办不了事时，才准添 Plugin 或 MCP。

## 三、先分五档，别见什么都封包

### 3.1 事实与偏好：进 Memory

例：用户明确说“这个项目只用 `uv`”。这是一条记忆，不值得起一只 Package。

推断出的偏好仍进候选箱，须由用户审阅。不可把模型猜测写成用户明说。

### 3.2 可复用步骤：进 Skill

例：一套排查 provider 绑定的步骤，换项目仍大体能用。先从 `/record` 录制件起草
Skill，再把它装进 content-only Package Candidate。

### 3.3 稳定编排：进 Workflow

例：每次都要“收集日志 -> 诊断 -> 修复 -> 测试 -> 截图”。步骤、输入、失败路与
验收都说得清，便升成 Workflow。

### 3.4 稳定角色：进 Agent + Prompt

例：多次任务都要同一套模型角色、工具白名单、Skill 与 Prompt Profile。此时才提炼
Agent Definition。不要因一次任务写出一只新 Agent。

### 3.5 新执行能力：进 Plugin 或 MCP

例：现有工具压根儿读不了某种协议，才考虑生成 process Plugin 或 MCP。它们会跑
代码，须过更严的信任、沙箱、依赖、网络与真机评测。

## 四、候选态是一道硬门

进化出来的 Package 不得直接装进 active catalog。先落候选区：

```text
~/.lubancode/package-candidates/
  moontide.provider-auditor/
    cand-20260828-001/
      package/                 候选 Package，只读看待
      evolution.json           演化来源与父版本
      eval-plan.json           评测计划
      eval-results.jsonl       只追加结果账
      approval.json            批准或拒绝记录
```

候选状态机：

```text
observed
  -> drafted
  -> validated
  -> evaluated
  -> awaiting_approval
  -> staged
  -> canary
  -> active

任意非终态 -> rejected
canary/active -> rolled_back
```

状态只许由一枚 `EvolutionCoordinator` 改。CLI、TUI、Workflow、Agent 都不得各写
一套迁移规矩。

候选目录与正式 Package 目录分开。失败候选不污染 PackageCatalog，也不占正式
组件名字。

## 五、EvolutionRecord：把来龙去脉记清

`package.yaml` 仍须薄，只写身份、版本与兼容。进化账另放 `evolution.json`，也可
同步写进宿主账本。

建议字段：

```json
{
  "schema": 1,
  "candidate_id": "cand-20260828-001",
  "package_id": "moontide.provider-auditor",
  "candidate_version": "0.2.0-candidate.1",
  "parent": {
    "version": "0.1.0",
    "content_hash": "sha256:..."
  },
  "objective": "减少 provider 绑定误判，并保留可核验证据",
  "sources": {
    "run_ids": [],
    "goal_ids": [],
    "recording_ids": [],
    "memory_ids": [],
    "user_feedback_ids": []
  },
  "generator": {
    "provider": "...",
    "model": "...",
    "prompt_revision": "..."
  },
  "changes": {
    "components_added": [],
    "components_changed": [],
    "components_removed": [],
    "permissions_added": [],
    "tools_added": []
  },
  "created_at": "..."
}
```

规矩：

- 来源只记稳定 ID 与脱敏摘要，不把整段会话、密钥、Cookie 塞进包。
- 父版本与父内容哈希都要有。无父版本时明写 `null`，不可假装是升级。
- 生成模型、Prompt revision、能力表要留账，日后才复现得出。
- 权限、工具、进程、网络、环境变量依赖须单列差异。
- 候选内容改一字，content hash 便变；旧评测与旧批准随即失效。

## 六、从哪里取经验

首版只收已有、可追根的材料：

- `/record` 的 `events.jsonl`、目标口述、变量、验收与最后验证。
- Workflow run 的 definition、events、checkpoint 与最终产物摘要。
- `/goal` 的 objective、iteration、evidence 与终点判词。
- ToolTrace 中脱敏后的工具名、输入形状、outcome、error_code 与短摘要。
- 项目 Memory 中已接受的事实、偏好与 feedback。
- 用户当场说出的纠正、批准与拒绝。

首版不收：

- 模型长篇思考原文。
- 未脱敏的工具输入与输出。
- 只有 HTTP 2xx、没有产物证据的“成功”。
- 模型自己声称“我学会了”的话。
- 被用户拒绝过、内容未变的同类建议。

采集器只产 `EvolutionObservation`。它不生成 Package，也不决定晋升。

## 七、什么时候才值得提炼

不可每跑一轮便生一只包。须有触发门槛。

首版支持三种触发：

1. 用户明说：`/evolve propose <run|goal|recording>`。
2. 同类任务多次成功，系统只提示“发现可复用做法”，等用户点头再起草。
3. 用户反复纠正同一毛病，系统提出修订既有 Package 的候选。

自动提示至少要满足：

- 有两次以上独立任务证据；或有一次用户明确要求沉淀。
- 输入、产物、验收大体同形。
- 不是偶然重试、临时路径、日期、账号或单机环境造成的成功。
- 没有同 fingerprint 的 pending/rejected 候选。
- 可说明它比现有 Memory、Skill 或 Package 多解决了什么。

触发门槛只是起草门，不是安装门。

## 八、如何从经验长成 Package

建议新增 `EvolutionDrafter`。它先判该落哪一档，再起草相应组件。

### 8.1 先做最小包

能靠一份 Skill 办成，就只放 Skill。能靠 Skill + Workflow 办成，就不添 Agent。
现有工具能用，就不生 Plugin、MCP。

### 8.2 抽掉偶然值

日期、网址、绝对路径、账号、模型名、工作目录等具体值，提成输入或约束。不得把
录制现场原样焊死。

### 8.3 保住失败路

起草器不能只抄最后那次成功。前面若有稳定失败模式，要写成 Workflow 分支、Skill
排错步骤或评测夹具。

### 8.4 验收必须可执行

“看起来不错”“回答专业”不算验收。优先写：

- 文件存在、格式可解析。
- 测试命令通过。
- JSON schema 合法。
- 指定工具真被调用。
- 产物内容命中特定结构。
- 副作用未越出允许目录。
- 预算、耗时、token 不越帽。

## 九、评测：须比旧版，不只看自己能不能跑

候选先过五道门：

```text
Package doctor
  -> 组件原生 validator
  -> 来源任务回放
  -> 留出任务评测
  -> 与父版本或裸 Agent 对照
```

### 9.1 静态门

- Package schema、SemVer、兼容范围。
- 组件 schema、包内引用、路径与 symlink 越界。
- 密钥扫描、绝对路径、硬编码账号与环境依赖。
- 工具、网络、进程、env 与权限差异。
- code-bearing 分类与信任材料。

### 9.2 来源任务回放

重跑它学来的任务，证明候选至少没有忘本。能在 fixture 或隔离工作区回放的，优先
走确定性回放。

### 9.3 留出任务

至少留一份未参与起草的同类任务。没有留出任务，只可标 `experimental`，不可自动
建议晋升稳定版。

### 9.4 基线对照

父 Package 存在时，与父版对照；没有父版时，与未挂载候选的普通 Agent 对照。

至少记：

- 任务成功率与验收通过率。
- 误报、漏报与不可恢复失败。
- tool calls、tokens、墙钟时间。
- 用户确认次数与权限请求次数。
- 工作区写入、外部副作用与回滚结果。

候选不能只因成功率多一分便晋升。若权限陡增、耗时翻倍、token 暴涨，须把代价亮给
用户看。

### 9.5 评判者不能只用生成者

生成模型可自查，却不能单独盖章。最终结果由确定性检查、真实执行证据与独立
Evaluator 合并。Evaluator 的文字判词只算一份证据，不压过测试与产物。

## 十、批准与权限

首版一律由用户批准晋升。批准页须亮出：

- Package id、候选版本、父版本与内容哈希。
- 从哪些任务和纠正里学来。
- 添、改、删了哪些组件。
- 评测样例、基线、结果与未测之处。
- 新增工具、进程、网络、env 与文件权限。
- 安装位置、灰度办法与回滚目标。

分三档：

```text
content-only                可批准后直接进入 canary
process Plugin / MCP        须另过 Package trust 与运行沙箱
native Plugin / core patch  不走自动晋升；交人工审查与发布
```

“批准这只候选”只认当前 content hash。文件变过，须重验、重批。

## 十一、安装、灰度与回滚

正式 Package 不原地改。建议版本化存放，再用 active pointer 或 PackageSnapshot
选中版本：

```text
~/.lubancode/package-store/
  moontide.provider-auditor/
    0.1.0/
    0.2.0/
```

晋升次序：

1. 把候选复制到 staging，复算 hash。
2. 再跑 Package doctor。
3. 原子落入 version store。
4. 新会话或新任务拿到 canary 快照。
5. 老任务继续钉旧快照。
6. 观察到足够样本后，才改 active version。

首版灰度可很朴素：只在用户点名 `/evolve use <candidate>` 时启用。不要一开头便做
复杂流量路由。

回滚只切回旧快照，不删除新版本，不抹评测账。若候选已造成外部副作用，Package
回滚只能撤能力版本，不能假装外部世界也已复原；须另走补偿步骤。

## 十二、与现有系统怎样接

### 12.1 `/record`

保留现有“示范一次 -> 起草 Skill”。新增一条出口：把草稿连同验收夹具封进
content-only Package Candidate。原有直接安装入口先保留兼容，文档推荐走候选评测。

### 12.2 项目 Memory

Memory 提供已接受的事实、偏好与 feedback。它也是去重与触发材料。Memory 不负责
生成组件，不负责安装 Package。

### 12.3 Workflow

Workflow 承担可复现实验、留出评测与灰度任务。它也可成为候选 Package 的组件。
评测 Workflow 与被测 Workflow 须分开，免得自己给自己打分。

### 12.4 `/goal`

`/goal` 可承载一场较长的候选修订任务，GoalEvaluator 可给终点判词。它不替代
Package validator、回放、基线对照与用户批准。

### 12.5 PackageCatalog

PackageCatalog 只管正式包的发现、校验、挂载与快照。候选仍由
`EvolutionCoordinator` 管。只有 staged/active 版本才进入正式 Catalog。

## 十三、命令面

建议新增：

```text
/evolve status
/evolve suggest [on|off]
/evolve propose <run-id|goal-id|recording-id>
/evolve list [pending|active|rejected|all]
/evolve show <candidate-id>
/evolve diff <candidate-id>
/evolve test <candidate-id>
/evolve approve <candidate-id>
/evolve reject <candidate-id> [reason]
/evolve use <candidate-id>
/evolve promote <candidate-id>
/evolve rollback <package-id> [version]
```

另给 CI 一条非交互入口：

```text
luban evolve test <candidate-dir> --baseline <package-dir> --json
```

命令不得把“模型已生成”“validator 已通过”“Evaluator 说完成”混成同一枚绿色状态。
应分别显示 drafted、validated、evaluated、approved、active。

## 十四、核心职责

建议落下这些类型：

```text
EvolutionObservation    一条脱敏、可追根的经验
EvolutionCluster        同类经验簇与去重 fingerprint
EvolutionProposal       为什么值得沉淀、应落哪一档
EvolutionDrafter        经验 -> 最小 Package Candidate
EvolutionRecord         来源、父版、生成器与权限差异
EvolutionEvalPlan       来源回放、留出任务、基线与指标
EvolutionEvalRunner     隔离执行并只追加结果账
EvolutionVerdict        确定性证据 + Evaluator 的合并判词
EvolutionCoordinator    候选状态机唯一写口
EvolutionStore          候选、评测、批准与拒绝账
EvolutionPromoter       staging、版本安装、canary、晋升与回滚
```

依赖方向：

```text
runtime journals / memory / recordings
              -> observation adapters
              -> evolution domain
              -> package compiler + validators
              -> package store/catalog
```

Evolution 层可调用原有 parser、validator 与 runtime，不可复制它们的 schema 和权限
判断。

## 十五、安全铁律

- 缺省不开自动起草，也不开自动安装。
- 项目内容不得因一次 checkout 就启动代码。
- 候选评测用隔离工作区；默认断网或沿用更窄权限。
- 评测不可读取正式密钥。需要真实服务时，须由用户单独批准。
- 候选不得扩大父 Agent 权限；真要扩大，批准页须单列。
- 不把用户私有路径、账号、token、Cookie、环境变量值写进候选。
- 不从模型思考原文提炼规则，只认行为、结果、用户反馈与可核验证据。
- 拒绝账须留 fingerprint，内容未变时不反复劝装。
- active Package 不原地改；会话钉快照。
- 自动生成的 native Plugin 不加载。首版只可产草稿，交人工构建与审查。
- LubanCode core patch 永远不走 `/evolve approve`。它须过 Git diff、构建、测试、
  review 与发布流程。

## 十六、实现次序

### 阶段 0：冻结合同

- [ ] 写 `docs/features/evolution/README.md`。
- [ ] 冻结候选状态机、目录与 `evolution.json` schema 1。
- [ ] 冻结“Memory / Skill / Workflow / Agent / code”五档决策表。
- [ ] 冻结证据、批准与 hash 失效规则。
- [ ] 写一只 content-only 候选夹具与一只带代码的拒绝夹具。

验收：实现者能说清 Package 是产物，自进化闭环才是机制。

### 阶段 1：Observation 与去重

- [ ] 为 `/record`、Workflow run、`/goal`、ToolTrace、Memory 写只读 adapter。
- [ ] 统一脱敏、来源 ID、outcome 与 evidence ref。
- [ ] 实现同类经验 fingerprint 与 rejected 去重。
- [ ] 只做 `/evolve status/list/show`，不生成 Package。

验收：能从一场任务追到原始账，又查不到密钥与模型思考原文。

### 阶段 2：Skill-only Package Candidate

- [ ] 复用现有 Skill drafter。
- [ ] 生成最小 `package.yaml` 与 `skills/<id>/SKILL.md`。
- [ ] 写 `evolution.json`、父版、来源与权限差异。
- [ ] 候选只落 candidate store，不进 PackageCatalog。
- [ ] 接 `/evolve propose/diff/reject`。

验收：一场 `/record` 能变成可查、不可偷装的 content-only Package Candidate。

### 阶段 3：评测与基线

- [ ] 实现静态门、密钥扫描与绝对路径扫描。
- [ ] 支持来源任务回放与留出 fixture。
- [ ] 支持父版或裸 Agent 基线。
- [ ] 记录成功率、验收、tool calls、tokens、耗时与权限请求。
- [ ] 独立 Evaluator 只在确定性证据之后给判词。
- [ ] 接 `/evolve test/show` 与 JSON CI 输出。

验收：能如实说“通过几项、没测什么、比旧版贵多少”，不只吐一句 pass。

### 阶段 4：批准、安装与回滚

- [ ] 批准绑定 candidate content hash。
- [ ] 接 Package staging、version store 与 PackageSnapshot。
- [ ] 实现点名 canary、新任务生效、旧任务持旧版。
- [ ] 实现 promote 与 rollback，保留所有账。
- [ ] 文件变动后让旧评测、批准与信任一并失效。

验收：候选坏了，一条命令切回父版；半场任务不换能力。

### 阶段 5：Workflow 与 Agent 组合包

- [ ] 从多场同类任务提炼结构化 Workflow。
- [ ] 只有稳定工具、Prompt 与 Skill 组合才提炼 Agent。
- [ ] 校验包内短引用与 canonical id。
- [ ] 评测 Workflow 与被测 Workflow 分家。
- [ ] 比较组合包与最小 Skill 包，证明复杂度确有收益。

验收：不是组件越多越容易晋升；最小可行包仍是默认答案。

### 阶段 6：代码型候选

- [ ] 首版只生成 process Plugin/MCP 草稿，不自动启用。
- [ ] 生成依赖清单、命令、args、env 名、网络与文件权限差异。
- [ ] 复用 Package trust、沙箱与整包事务。
- [ ] 加恶意脚本、依赖投毒、路径逃逸、网络越权夹具。
- [ ] native Plugin 仍停在人工审查线外。

验收：用户不批，零进程；包内一件代码组件失败，整包不挂。

### 阶段 7：有限自动建议

- [ ] 缺省关闭 `/evolve suggest`。
- [ ] 只提示候选机会，不自动起草、测试或安装。
- [ ] 同类任务次数、收益与去重门槛可 inspect。
- [ ] 用户拒绝后不死缠。
- [ ] 收集 suggestion 命中率与用户接受率。

验收：系统会提醒，却不自作主张；关掉后不再后台收集新材料。

## 十七、测试账

### 状态与账本

- [ ] 每条合法与非法状态迁移。
- [ ] candidate id、package id、version、parent hash。
- [ ] 内容改动让评测、批准与信任失效。
- [ ] events/results 只追加，半截 JSONL 可恢复。
- [ ] 拒绝 fingerprint 去重。
- [ ] active、candidate、rejected 三类目录互不污染。

### 提炼

- [ ] 单次明确沉淀与多次同类触发。
- [ ] 一次偶然成功不自动建议。
- [ ] 日期、URL、cwd、账号与绝对路径抽象。
- [ ] 失败重试折叠，稳定失败路保留。
- [ ] 能用 Skill 时不生 Agent，能用现有工具时不生 Plugin。
- [ ] 推断偏好不可冒充用户 feedback。

### 评测

- [ ] 来源回放、留出任务、父版基线、裸 Agent 基线。
- [ ] 成功率相同但 token/耗时暴涨时明报退步。
- [ ] 权限扩大时不得静默晋升。
- [ ] Evaluator 与确定性测试冲突时，确定性证据优先。
- [ ] 生成模型与评判模型相同时明示弱证据。
- [ ] 网络、服务、硬件没测到时写 unverified。

### 安全

- [ ] token、Cookie、authorization、private key 全链路脱敏。
- [ ] symlink、junction、`..` 与绝对路径逃逸。
- [ ] 项目候选未 trust 时零进程。
- [ ] 评测环境拿不到正式密钥。
- [ ] process command 不经 Shell 拼串。
- [ ] 自动生成 native Plugin 不加载。
- [ ] core patch 不可经 evolve 晋升。

### 晋升与回滚

- [ ] staging 写到一半失败，正式 store 不变。
- [ ] Package doctor 二次失败，不切 active。
- [ ] canary 只影响点名任务。
- [ ] promote 只影响新任务，旧快照照跑。
- [ ] rollback 切回父版，候选与评测账仍在。
- [ ] 外部副作用不能靠切版本假装撤销。

### 真机验收

- [ ] 跑 Release 全量 CTest，记真实命令与通过数。
- [ ] 用真实 `luban` 跑“录制 -> 候选 -> 评测 -> 批准 -> canary -> 晋升 -> 回滚”。
- [ ] 用 `/provider ccmoon`，至少挑两种模型角色。
- [ ] 准备一份来源任务与一份留出任务，证明不是背答案。
- [ ] 截图留证：来源账、diff、评测、权限差异、批准、active 版本与回滚。
- [ ] 故意改候选一字，证明 hash 变、旧评测和批准失效。
- [ ] 故意让 code-bearing 候选起进程失败，证明正式 ToolRegistry 零残留。
- [ ] HTTP 成功、模型口头说学会、单张截图都不算闭环完成。

## 十八、完成线

- [ ] 能从真实 run、goal 或 recording 生成最小 Package Candidate。
- [ ] 候选与正式 Package 分仓，未经批准绝不挂载。
- [ ] `evolution.json` 能追到父版、来源、生成器、改动与权限差异。
- [ ] 每只候选都过静态校验、来源回放、留出评测与基线对照。
- [ ] 生成者不能独自批准自己；模型判词不压过真实证据。
- [ ] content-only 与 code-bearing 分门把守。
- [ ] 晋升原子，任务钉快照，旧版可一键切回。
- [ ] Memory、Skill、Workflow、Agent、Plugin、MCP 各守本分。
- [ ] LubanCode core patch 不走 Package 自进化旁路。
- [ ] 命令、文档、单测、集成测试、Release CTest、真机与截图证据齐全。

## 十九、留到后头

- 自动合并多只候选。
- 跨 Package 依赖求解与 lockfile。
- 多臂老虎机式流量分配。
- 长期无人值守自动晋升。
- 自动生成并加载 native Plugin。
- 跨用户经验汇总、联邦学习与隐私预算。
- 远端候选交换、签名、透明日志与供应链证明。
- 自动提交 LubanCode core patch。

先让它学会收拾自己的手艺。学出一招，装进匣里；验得真，才挂上身。学歪了，立刻
退回旧版。如此才叫进化，不叫碰运气。
