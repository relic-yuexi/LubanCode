# LubanCode 真实实测问题记录

- 状态：记录中
- 日期：2026-08-29
- 场景：在真实项目中使用 LubanCode 开发，随用随记
- 原则：只记亲眼所见。现象、推测、根因分开写；未经排查，不把猜测当结论。

## 问题 1：流式回复中的 Markdown 粗体没有渲染

- 状态：待排查
- 级别：待定
- 证据：`C:\Users\MOONTI~1\AppData\Local\Temp\codex-clipboard-eda5b10a-3e72-4a21-8937-bf218a5655dc.png`

### 现场

LubanCode 调完 `todo_write` 和数次 `search` 后，继续流式输出正文。正文中这段文字：

```text
**React + Vite 前端、Express 后端、JSON 文件持久化**
```

没有显示成粗体。左右两组 `**` 原样露在终端里。截图时界面仍处于“思考中”。

### 复现

1. 启动 LubanCode，进入真实项目会话。
2. 让模型在流式正文中输出 Markdown 粗体。
3. 在回复尚未结束时观察终端正文。
4. 检查回复结束后，标记是否仍未渲染。

### 实际结果

终端直接显示 Markdown 源标记 `**...**`，没有套用粗体样式。

### 预期结果

普通正文中的 `**...**` 应显示成粗体，不该露出 Markdown 标记。流式阶段与最终定稿的显示应当一致；若流式阶段暂不能确认闭合标记，也应在标记闭合后重绘成最终样式。

### 待查边界

- 只在流式阶段失效，还是回复结束后仍失效。
- `**` 与正文落在不同流式分块时，解析器能否跨块拼合。
- 中文、英文、标点混排是否影响粗体识别。
- 粗体之外，标题、列表、行内代码、链接是否也会露出 Markdown 源标记。

### 验收

- 覆盖单块与跨块两种 `**粗体**` 输入。
- 流式标记闭合后，当前行能正确重绘。
- 回复结束后，不残留本应参与渲染的 `**`。
- 覆盖中英文混排、窄终端与换行场景。
- 代码块和转义后的字面量 `\**` 仍按原文显示，不误渲染。

## 问题 2：忙碌期排队的 `/context` 被当成普通消息送给模型

- 状态：已定位，待修复
- 级别：P1
- 证据一：`C:\Users\MOONTI~1\AppData\Local\Temp\codex-clipboard-bd97a127-b732-4587-bf15-77353510d3b3.png`
- 证据二：`C:\Users\MOONTI~1\AppData\Local\Temp\codex-clipboard-9a808bf8-eb56-48c2-bef9-c0b91eaf1e7e.png`

### 现场

LubanCode 正在执行工具时，用户在忙碌输入框键入并提交：

```text
/context
```

界面先把这行排进队列。到了下一处工具边界，它没有执行 `/context`，却把这行送给模型。模型随后读取 context、compact、session 和 command 文档，又调用 `context_search`、`context_read`，最后自行解释 `/context` 的用途，并继续修改项目文件。

这条命令已经从队列中消费。等当前回合结束，它不会再作为斜杠命令补执行。

### 实际结果

- `/context` 没有打开终端本地的上下文占用面板。
- 模型把 `/context` 当成普通用户需求，自行查文档并作答。
- 无关的读取与工具调用白白吃掉时间和上下文。
- 用户以为排队的是本地命令，实际排队的却是模型消息，界面没有提醒这层语义变化。

### 预期结果

忙碌期提交完整斜杠命令后，队列应保留它的命令身份。到了安全边界，应交给本地 slash 分发器执行，不得注入模型上下文。

若某条斜杠命令不支持忙碌期排队，提交时就应明说并拒绝入队，不能悄悄降成普通消息。

### 已定位的代码岔路

空闲轮末的自动发送路径会调用 `ProcessLine()`：

```text
src/app/interactive_session.cpp:944-948
```

`ProcessLine()` 会先跑 `ParseSlashCommand()`，故这条路径认得 `/context`：

```text
src/app/interactive_session.cpp:501-514
```

工具边界的 inbox 路径却调用 `TakeDeliverable()`，把主队列里的所有条目包成 `[用户排队消息]`，直接注入模型：

```text
src/app/interactive_session_wiring.cpp:730-749
```

这里没有识别 slash，也没有给 slash 留到轮末。工具边界先把 `/context` 抢走，轮末的 `ProcessLine()` 自然再也看不见它。

### 临时绕法

1. 按 `Esc` 打断当前回合，或等它自然结束。
2. 等空闲提示符回来。
3. 重新输入 `/context`。

刚才那条已经送给模型，须重新输入。

### 验收

- 忙碌期排队 `/context`，即使随后还有多个工具边界，也不得进入模型请求。
- 当前回合收尾后，队列泵经 `ProcessLine()` 执行 `/context`，显示本地上下文面板。
- `/help`、`/todos`、`/compact` 等本地命令走同一条命令身份保留规则。
- 普通排队文字仍可在工具边界作为 steering 消息送给模型。
- slash 与普通文字混排时，各走各路，原有顺序不乱。
- 补一条回归测试，专门覆盖“工具边界早于轮末”的场景；现有“轮末队列泵按 slash 分派”手测不足以挡住这条岔路。

## 问题 3：流式分块渲染吃掉标题前的空行

- 状态：已定位，待修复
- 级别：P2
- 证据：`C:\Users\MOONTI~1\AppData\Local\Temp\codex-clipboard-f85e22ac-040c-4777-abe5-e5f6ea2fd02b.png`
- 会话：`C:\Users\moontidef\.lubancode\sessions\20260829-223210-做一个图书管理系统，node前端，nod.jsonl`

### 现场

最终答复显示完“已内置 3 条示例图书数据。”后，下一行立刻贴着标题“运行方式”，中间没有空行。

会话 JSONL 保存的模型原文却是：

````markdown
- 已内置 3 条示例图书数据。

### 运行方式

```bash
npm run dev
```
````

模型原文有空行。空行是在终端流式收束重画时丢的，不是模型少写。

### 实际结果

列表末项与下一节标题紧贴：

```text
  • 已内置 3 条示例图书数据。
运行方式
```

标题本身已经剥掉 `###` 并套用样式，说明 Markdown 渲染确实触发；只有标题前的垂直间距丢了。

### 预期结果

块级标题前后各留一空行。即使流式渲染器按段落分块收束，跨块的最终画面也应保住原文中的段落间距：

```text
  • 已内置 3 条示例图书数据。

运行方式

```

### 已定位的代码岔路

`ScanBodyDelta()` 遇到空行便收束当前块并清空累计正文：

```text
src/cli/live_transcript.hpp:494-527
```

故而上面的回复至少被拆成两块：

```text
块一：- 已内置 3 条示例图书数据。\n\n
块二：### 运行方式\n\n
```

`RenderMarkdown()` 处理标题时调用 `push_blank()`。可 `push_blank()` 只有在当前输出 `out` 非空时才补空行：

```text
src/cli/markdown.cpp:663-667
src/cli/markdown.cpp:728-750
```

标题独占第二块。此时 `out` 为空，标题前空行便不生成。标题后的空行仍会生成。整篇一次渲染时，标题前已有列表内容，故现有标题单测能过；流式分块路径却会漏。

### 验收

- 用原始片段 `- 列表末项\n\n### 标题\n\n` 做流式分块回归，最终画面保留标题前空行。
- 分别覆盖单个 delta、跨 delta、逐字 delta 三种到达方式。
- 段落接标题、列表接标题、代码块接标题，都保留恰好一行间距。
- 连续多个空行仍收成一行，不把画面越撑越松。
- 标题位于回复开头时，不凭空多出首行空白。
- 不只测 `RenderMarkdown()` 整篇输入；须穿过 `ScanBodyDelta()` 与 `StreamBodyTracker` 的真实分块收束路径。

## 问题 4：后台任务缺少用户可见、可查、可停的管理面

- 状态：已确认产品缺口，待设计修复
- 级别：P1
- 证据：`C:\Users\MOONTI~1\AppData\Local\Temp\codex-clipboard-5ac68380-0cc2-4840-90f5-6367f58020ad.png`

### 现场

模型用 `run_command(npm run dev)` 启动后台任务 `#2`，随后用 `background_output` 两次查询，并告诉用户：

```text
后台任务未设置自动超时。如需关闭，告诉我“停止后台服务”即可。
```

回到空闲提示符后，底栏没有后台任务入口，也没有运行数量、状态或停止按钮。用户只从模型答复里看见一枚 `#2`，无法直接展开它、查看完整输出或亲手关闭。

### 当前真实能力

底层并非没有这些能力：

- `BackgroundTaskRegistry` 保存 task id、状态、PID、命令、日志路径、起止时间和退出码。
- `background_output` 不带 id 可列任务；带 id 可读日志尾部。
- `stop_background` 可按 id 停任务。
- 空闲时输入 `/background`，可列状态、PID、命令、日志路径、已跑时长及最近三行非空输出。

可这些能力没有完整接到用户面：

- `/background` 只会列清单，不接任何参数。
- 没有 `/background show 2`、`/background logs 2` 或实时跟随视图。
- 没有 `/background stop 2`。
- 完整日志与停止只作为模型工具暴露。用户须再次请求模型代办。
- 底栏不常驻显示后台任务数量和运行状态。

### 代码现状

`/background` 的 handler 明确忽略参数，只调用列表函数：

```text
src/app/commands/workspace_commands.cpp:605-678
src/app/commands/workspace_commands.cpp:748-750
```

模型侧的查日志与停止已经落在：

```text
src/tools/background_output.cpp
src/tools/background_tasks.hpp
```

后台 PowerShell 以 `-NonInteractive` 启动，stdout/stderr 落进日志文件：

```text
src/tools/run_command.cpp:524-551
```

故而当前只能“读日志”，不能附着到一只可交互 PTY。若要让用户进入后继续输入命令，须另建可附着终端会话；不能把日志查看说成终端接管。

### 实际结果

- 后台服务启动后从主界面消失，只剩模型文字中的任务编号。
- 用户无法从 UI 看出后台究竟还跑着几只任务。
- 用户无法直接打开任务详情或持续查看 stdout/stderr。
- 用户无法直接停止任务，只能请模型调用工具，或离开产品另用系统命令杀 PID。
- 模型若忘了收尾，长命服务可一直留着。

### 预期结果

后台任务既由本场 LubanCode 启动，用户就该握住同一份台账。能看，能查，能停。模型可以代办，用户也能亲手操作；两条路须共用 `BackgroundTaskRegistry`，状态不可各算各的。

### 建议命令面

```text
/background
/background show 2
/background logs 2 [--tail 100] [--follow]
/background stop 2
/background stop all
```

`show/logs` 只读，不应经过模型。`stop` 直接走宿主命令与本地确认，不应先发一轮模型请求。

### 建议终端面

- 底栏常驻一枚后台任务状态，如“后台 1 运行 / 2 完成”。没有任务时收起。
- 进入后台任务焦点态后，可上下选择、Enter 看详情、`x` 停止、Esc 回主会话。
- 详情页显示 task id、状态、PID、命令、cwd、启动时间、已跑时长、退出码与日志路径。
- 日志页可实时跟随，允许暂停滚动、翻看旧行、复制输出；Esc 返回，不影响进程。
- 任务完成或失败时给一次通知，并保留可回看的结果，不能通知一闪便无处再找。

### “进去看”的边界

第一步应交付日志查看：实时读 stdout/stderr，足够看开发服务器到底吐了什么。

若还要在里面输入、回答提示、操作 REPL，那是可附着 PTY。届时 `run_command` 启动模型、进程句柄、输入转发、终端尺寸、Ctrl+C/Ctrl+D 和退出收尸都要另立契约。

### 验收

- 启动两只后台任务后，底栏无需模型提醒便显示准确数量和状态。
- `/background` 与模型 `background_output` 读取同一份任务账，字段一致。
- 用户可打开 `#2`，查看启动后持续增长的日志，并返回主会话。
- 用户可直接停止 `#2`；状态按 `running -> stopping -> stopped` 变化，树死透后才报已停。
- 停止失败须显示 `stop_failed` 与原因，不许假报成功。
- `/background stop all` 列出将停止的任务并确认，已终态任务不重复杀。
- Windows 下验证开发服务器子进程树一起收掉，不能只杀 PowerShell 根进程留下 Node。
- 日志为空、日志被删、非法 UTF-8、超长日志和任务已退出时，详情页都能如实显示。
- 本期若只做日志查看，界面明确写“查看日志”，不得写成“进入终端”。

## 问题 5：缓存面板把“模型请求”写成“轮”，误导成用户发送轮次

- 状态：已定位，待修复
- 级别：P2
- 证据：`C:\Users\MOONTI~1\AppData\Local\Temp\codex-clipboard-083c7067-9682-48ed-8253-c1f8ad81f5af.png`
- 会话：`C:\Users\moontidef\.lubancode\sessions\20260829-223210-做一个图书管理系统，node前端，nod.jsonl`

### 现场

用户刚提交一条请求，随后打开 `/context`，缓存卡片却显示：

```text
逐轮命中（最近 12 轮，最旧在前）
```

下面排出 12 行输入与命中数据。按通常界面语义，“轮”指一轮用户问答。用户自然会以为自己发了 12 次，或产品偷偷造了 12 个用户轮次。

### 真实口径

这里的每一行不是用户轮次，而是一次带有效 usage 的 provider 请求。

一条用户请求可触发多次模型请求：

```text
用户发一条任务
  -> 请求模型
  -> 模型调用工具
  -> 工具结果写回
  -> 再请求模型
  -> 再调工具
  -> ……
  -> 模型最终作答
```

每次请求结束，只要 provider 回了有效 usage，`ContextTracker::ApplyUsage()` 就追加一笔缓存记录。记录上限固定为 12，超出便删掉最旧一笔：

```text
src/cli/context_tracker.hpp:96-120
src/cli/context_tracker.cpp:23-40
```

`/context` 直接把这只请求级环形缓冲显示成“逐轮命中”：

```text
src/app/commands/session_commands.cpp:118-130
```

### 会话实账

对证据会话的 JSONL 按结构解析：

```text
真正的外层用户文字：3 条
协议中以 user role 保存的工具结果：56 条
assistant 消息：62 条
```

三条外层用户文字分别是：

1. 做一个图书管理系统。
2. 为什么后台任务会退出？
3. 那你挂后台启动吧。

截图里的“最近 12 轮”既不等于这 3 条用户消息，也不只属于最后一条。它是整场会话最近 12 次带 usage 的模型请求。`12` 还是环形缓冲上限；请求更多时，画面照旧只显示 12。

### 实际结果

- “轮”同时被拿来指用户问答、历史 turn、Agent step 和 provider request，口径混在一起。
- 用户无法看出 12 行分别属于哪一条外层请求。
- 工具调用多的单次任务会迅速填满 12 行，看起来像用户发了许多轮。
- 环形上限没有写明，用户会把“12”误读成全量计数。

### 预期结果

先把名字写准：

```text
逐请求缓存命中（会话最近 12 次 provider 请求，最旧在前）
```

更好的做法是按外层用户轮次分组，并给每次模型请求编 step：

```text
用户轮次 #3：那你挂后台启动吧
  请求 1  输入 27.2k / 命中 0
  请求 2  输入 27.6k / 命中 25.1k
  请求 3  输入 27.7k / 命中 27.1k
```

“用户轮次”“模型请求”“工具调用”三种计数须各叫各名，不能都叫轮。

### 验收

- 一条用户请求连续调用 5 次工具时，UI 显示“1 个用户轮次 / N 次模型请求”，不显示成 N 轮用户对话。
- 缓存历史每笔带 `turn_id` 与 `step/request_index`，可追到所属外层请求。
- `/context` 明写这是全会话历史，还是仅当前用户轮次；不得靠用户猜。
- 达到 12 条上限时显示“仅保留最近 12 次”，不把 12 冒充总数。
- 本地 slash 命令不发模型请求，不得增加这张请求级缓存表。
- provider 未回 usage 的请求标成“未回报”或另记缺测，不冒充 `0%`。
- 子代理请求若不计入，标题明写“仅主会话”；若日后计入，须分开列账。
- 单测与真机都覆盖：纯文本直答、一次工具、多次工具、请求失败、ESC 打断、compact 请求和续接会话。

## 问题 6：分角色调用账隐藏零调用角色，`lao` 像是凭空消失

- 状态：已定位，待修复
- 级别：P3
- 证据：`C:\Users\MOONTI~1\AppData\Local\Temp\codex-clipboard-eb30b24d-de1c-4bf4-a5bd-4a03b3bcdbf2.png`

### 现场

`/context` 的“模型调用分角色账（本会话累计）”只显示：

```text
cheap  · gpt-5.6-sol · 1 次调用
normal · gpt-5.6-sol · 31 次调用
```

三角色里的 `lao` 没有出现。用户无法判断它是没配置、路由坏了、回落了，还是本场压根没触发。

### 真实口径

`lao` 仍在。当前默认任务映射只有 Plan 走 `lao`：

```text
src/agent/model_router.cpp:46-66
```

本场没有 Plan 调用，故 `lao` 为 0 次。调用账的 `ReportLines()` 遇到从未记账的角色便直接跳过：

```text
src/agent/model_router.cpp:163-185
```

所以它不是丢了，只是零调用角色被隐藏。

### 实际结果

- 标题写着“分角色账”，却没有把三只角色列全。
- 缺行有多种含义，用户无法从画面分辨。
- `cheap`、`normal` 恰好都落到 `gpt-5.6-sol`，画面又不写来源与回落关系，更难看懂角色究竟有没有独立配置。

### 预期结果

分角色账固定列全 `cheap / normal / lao`。没有调用也明写：

```text
lao · gpt-5.6-sol · 0 次调用 · 本场未触发（Plan/规划任务）
```

若角色回落到 normal，应把回落写在同一行，不能只显示相同模型名让用户猜。

### 验收

- 新会话尚未发请求时，三角色都可见，均写 0 次。
- 普通实现任务只调用 normal 时，cheap 与 lao 仍显示 0 次及默认职责。
- 标题生成后 cheap 变 1 次；没有 Plan 时 lao 保持 0 次。
- 进入 Plan 后 lao 增长，normal 不替它冒账；若发生回落，行内写明原因。
- `/model roles` 与 `/context` 共用同一份有效路由、来源和回落口径。

## 问题 7：自动标题等首轮结束才同步生成，来得迟又挡住提示符

- 状态：已定位，待修复
- 级别：P2
- 证据：`C:\Users\MOONTI~1\AppData\Local\Temp\codex-clipboard-eb30b24d-de1c-4bf4-a5bd-4a03b3bcdbf2.png`
- 会话：`C:\Users\moontidef\.lubancode\sessions\20260829-223210-做一个图书管理系统，node前端，nod.jsonl`

### 现场

用户发出首条 query 后，LubanCode 没有马上起标题。它先跑完整个图书管理系统任务，七分多钟后收下主答，再调用一次 `cheap` 生成标题。

证据会话时间：

```text
会话启动：22:32:36
首批工具：22:32:42
首轮最终答复：22:40:05
title 事件落盘：22:40:12
```

分角色账显示这枚标题耗费：

```text
cheap · gpt-5.6-sol · 1 次调用 · 输入 492 tok · 输出 10 tok · 用时 6.3s
```

标题“实现Node图书管理系统”直到首轮结束后才出现。用户等主任务时看不见有意义的会话名；主答已经出来后，又须等约 6.3 秒才能拿回提示符。

### 代码现状

主用户回合收尾后，才调用 `MaybeGenerateSessionTitle()`：

```text
src/app/interactive_session.cpp:765-779
```

该函数还硬性要求历史里已有 assistant 正文，故首问刚提交时绝不会起标题：

```text
src/app/commands/memory_commands.cpp:596-625
```

标题生成在当前线程同步等待，超时上限 30 秒：

```text
src/app/commands/memory_commands.cpp:627-644
```

标题请求不是完整工具历史全塞。它只取 user/assistant 的 `TextBlock`，每块最多 600 字节，总转写约 4KB；可它仍扫描首轮历史，且给标题预留 `max_tokens=100`：

```text
src/app/session_title.cpp:104-155
```

### 额外问题：`cheap` 名不等于真便宜

标题默认映射到 `cheap`。可 cheap 未单配时会整体回落 normal：

```text
src/agent/model_router.cpp:124-136
```

本场 `cheap` 与 `normal` 都是 `gpt-5.6-sol`。也就是说，这枚低价值标题实际调用主模型，不是独立小模型。

492 输入 token 不是本场 728.3k 输入的大头。真正不合算的是：为了十个输出 token，在关键路径上再等 6.3 秒；cheap 未配置时还自动回落昂贵 normal。

### 预期结果

首条真实 query 一经接收并建档，标题工作就该启动，不必等 assistant 回复，更不必等所有工具跑完。

推荐分成两层：

1. 立即用首问在本地生成临时标题。清洗首行、截到合适长度，零模型 token，当场可见。
2. 只有配置了独立 cheap 路由时，才并行发一枚标题精炼请求。只喂首问，不喂首轮回复与工具历史；结果回来后原子替换临时标题。

标题生成不得阻塞 normal 主请求，也不得拖住首轮结束后的提示符。

### 建议预算

```text
输入：首条 query，最多 600 UTF-8 字节
输出：最多 24 tokens
reasoning：关闭或最低档
超时：3-5 秒
工具：无
失败：保留本地临时标题，不重试，不回落 normal
```

若产品决定标题不值得再发模型请求，直接保留本地标题也比当前同步调用稳当。

### 并发与覆盖边界

- 标题任务与 normal 主任务并行，各走独立 backend/request 状态，不能抢流式回调。
- 用户在标题返回前手动执行 `/title`，人工标题优先；迟到的自动结果须丢弃。
- 首问被取消或建档失败，不留下孤儿标题任务。
- resume 已有标题的会话不重复生成；无标题旧会话可用首条用户 query 补本地标题。
- 标题 usage 仍记入 cheap 账，但不得混进主会话 context 占用。

### 验收

- 提交首问后，无须等首个 normal token，短时间内便能看到临时标题。
- 一场耗时十分钟的首轮任务，标题不再等十分钟后才出现。
- cheap 精炼耗时再久，也不阻塞 normal 主任务或空闲提示符。
- cheap 未独立配置时，不为标题回落调用 normal；模型调用账中标题为 0 次。
- 独立 cheap 已配置时，标题请求只含首问截段，输入不随首轮工具数量增长。
- 标题输出上限收紧到足够容纳目标长度，不再给十几个字预留 100 tokens。
- 真机记录 query 接收、临时标题、精炼标题、首个 normal token、主答结束五个时间点，校验时序。

## 问题 8：`Ctrl+R` 把本会话每条提问列了两遍

- 状态：已定位，待修复
- 级别：P2
- 证据：`C:\Users\MOONTI~1\AppData\Local\Temp\codex-clipboard-13858c17-ed98-4748-9627-621ce6460627.png`
- 会话：`C:\Users\moontidef\.lubancode\sessions\20260829-223210-做一个图书管理系统，node前端，nod.jsonl`

### 现场

按下 `Ctrl+R` 后，终端进入“历史搜索 [本会话]”。本场三条真实提问各出现两遍：

```text
做一个图书管理系统，node前端，node后端。要有图书的增删改查。
为什么后台任务会退出？
那你挂后台启动吧
```

上面一批尾巴写 `20260829-223210 · library_sys`，下面一批写具体时间与会话标题。看上去像发了六次，实则只发了三次。

### 根因

`CollectPromptHistory()` 先遍历 session 文件，把本场已落盘提问全收进来：

```text
src/app/interactive_session.cpp:388-415
```

随后它又无条件遍历 `main_agent->History()`，把本场活历史再并一次：

```text
src/app/interactive_session.cpp:416-443
```

代码注释说活历史只作“建档前（或建不了档）的本场提问”兜底，实际没有先查磁盘里是否已有本场，也没有只补尚未落盘的尾巴。

索引层只去掉“同一场、前后相邻、文字相同”的条目：

```text
src/cli/history_search.cpp:100-109
```

磁盘一批、内存一批中间隔着别的提问，故三组重复全漏过去。现有单测只钉连续同文，没有覆盖“当前 session 文件 + 活 history 重叠”：

```text
tests/unit/cli/test_history_search.cpp:50-74
```

### `Ctrl+R` 是什么

它不是缓存面板，而是提问历史反向搜索：

```text
输入文字：筛选历史提问
Ctrl+R：往更早一条走
Ctrl+S：切换本会话 / 本项目 / 全部范围
Tab：取回到输入框
Enter：取回并发送
Ctrl+C：退出
```

### 预期结果

本场已落盘的提问只列一次。活历史只补尚未落盘的尾巴，不能把整场重抄一遍。

去重不能只拿文字硬砍。同一句话若由用户在不同时间真发了两次，应留两条。最好给提问事件保留稳定身份，如 session id 加事件序号或 JSONL 偏移；磁盘与内存认出同一事件后合并，另一次真实提交仍照常保留。

### 验收

- 本场三条提问已落盘，`Ctrl+R` 只显示三条。
- 新提问尚未落盘时，活历史能补出这一条；落盘后不变成两条。
- 用户隔一阵真把同一句发两次，两次都保留，并带各自时间。
- resume 旧会话后继续提问，旧档与新活历史不重不漏。
- 单测覆盖：仅磁盘、仅内存、磁盘与内存完全重叠、尾部部分重叠、同文不同事件。

## 问题 9：同一缓存 epoch 内命中率忽高忽低，现有面板说不清断在何处

- 状态：现象已确认，根因待探针分层
- 级别：P1
- 证据：
  - `C:\Users\MOONTI~1\AppData\Local\Temp\codex-clipboard-083c7067-9682-48ed-8253-c1f8ad81f5af.png`
  - `C:\Users\MOONTI~1\AppData\Local\Temp\codex-clipboard-eb30b24d-de1c-4bf4-a5bd-4a03b3bcdbf2.png`
  - `C:\Users\MOONTI~1\AppData\Local\Temp\codex-clipboard-13858c17-ed98-4748-9627-621ce6460627.png`

### 先把三笔账分开

状态栏这句：

```text
缓存命中 20k(68%)
```

说的是最近一次模型请求：总输入约 `29.5k`，其中 provider 回报 `20k cached_tokens`。

`/context` 里的：

```text
会话累计：命中 208.4k / 总输入 728.3k(29%)
```

说的是本会话主代理全部模型请求累计。它不是当前窗口占用，也不是用户提交次数。

本场用户只提交三条 query。一次 query 里，模型会“请求模型 → 调工具 → 塞回工具结果 → 再请求模型”，来回多次。因此账上有 `31 次 normal 调用`，并不等于用户发了 31 轮。问题 5 已另记这层命名混乱。

### 真正可疑之处

最近十二次 provider 请求都落在 `epoch 1`，输入从 `27.2k` 长到 `29.5k`。命中却这样跳：

```text
0%, 0%, 91%, 98%, 0%, 23%, 0%, 0%, 97%, 23%, 0%, 68%
```

若旧前缀稳定，只在尾巴追加工具调用与结果，已经出现过的长前缀理当反复可用。如今刚命中 `98%`，下一次便掉到 `0%`；再过几次又回到 `97%`。用户多发几条消息只能让请求尾巴变长，解释不了同一 epoch 里的这般抖动。

### 本地已能确认的边界

LubanCode 直接读取 Responses API 回包里的 `input_tokens_details.cached_tokens`，再摊成非缓存输入与缓存命中：

```text
src/api/responses/events.cpp:153-164
```

故 `0% / 23% / 98%` 不是界面凭空估出来的，是 ccmoon / 上游端点报回来的数。

本地 `cache epoch` 只比较模型、system、工具定义与历史消息指纹。旧消息没改，只往尾部添消息，epoch 就不变：

```text
src/agent/prefix.cpp:180-233
src/agent/context_manager.cpp:113-132
```

这说明 LubanCode 眼里的请求对象保持追加律。它还不能单独证明：

- 当前 wire 序列化后的实际缓存前缀逐字节稳定；
- ccmoon 是否把相邻请求送到共享同一缓存的上游节点；
- 上游缓存是否过期、逐出、分片，或按别的字段另算 cache key；
- provider 回报的 `cached_tokens` 是否始终可靠。

眼下不可贸然断言是本地改写前缀，也不可把锅径直推给 ccmoon。须拿探针把两边拆开。

### 现有诊断还差一口气

`/doctor cache probe` 已有两轮固定前缀探针，还会比较当前 wire 两份请求体的公共前缀：

```text
src/app/commands/doctor_commands.cpp:559-623
```

可它为公网 provider 设了安全闸：`base_url` 不是 loopback，且没配 `metrics_url` 时，不发探针：

```text
src/app/commands/doctor_commands.cpp:561-566
```

当前 ccmoon 场景很可能只能看日常请求账，没法在会话内一键做成对复测。日常账又只画 token 数、epoch 与命中率，不展示 wire 前缀指纹、上游节点线索或 miss 分型。

### 预期结果

缓存面板不只报“命中多少”，还要答出“本地前缀有没有变”。本地没变而 provider 报零时，应明写“上游未命中”，不能让用户误以为自己多发了几条便冲掉缓存。

建议每次请求留一笔不含正文、不含密钥的诊断账：

```text
turn_id / request_index
cache_epoch / epoch_break_reason / prefix_append_only
model / wire
system_hash / tools_hash / stable-message-prefix hash 与长度
序列化公共前缀长度（诊断模式）
provider cached_tokens / total_input_tokens
miss 分类：首请求、明确断 epoch、本地前缀稳定但上游未命中、usage 未回报
```

公网探针不可偷偷烧 token。可加一次性确认，明写会发两枚小请求、预计上限与端点；用户答应后再跑。没有 metrics 权限，也应凭两轮 usage 与本地 wire 前缀作有限判断。

### 验收

- 同一 epoch 的每次请求都能看见 `prefix_append_only` 与稳定前缀长度。
- system、工具定义、旧消息或 wire 前缀一变，epoch 当场递增并点名断因。
- 本地前缀稳定、provider 却回 `cached_tokens=0` 时，标成“上游未命中”，不标成“本地前缀失效”。
- provider 没回 usage 时显示“未回报”，不拿 `0%` 顶替。
- 经用户确认后，可对当前公网 provider 发两轮固定前缀探针；输出两轮 usage 与实际公共前缀字节数。
- 连跑多组探针，能区分稳定命中、固定阈值命中、间歇性 miss 与完全不支持缓存。
- 日志不落 prompt 正文、API key、完整 URL 查询参数；只留短 hash、长度与分类。
- 真机复测当前 ccmoon：至少跑五组相同固定前缀，逐组记命中；若仍高低交替，再查代理分流、缓存亲和与上游回报。
