# 看门狗强收路径：晚到投递的终态缝收口

- 状态：已实施（2026-09-09，分支 `worktree-agent-watchdog-deliver`；两强收闸的终态翻页处并投递，与正常收尾同款，见文末实施纪要）。
- 日期：2026-09-09
- 级别：P2（回流锁缝的剩余面：正常收尾已并锁根治，强收路径仍可能让孩子结果落未送达清单）。
- 范围：`src/tools/task_ledger.{hpp,cpp}` 强收两函数及其调用链、`src/tools/agent_tool.cpp` 看门狗线程、`tests/unit/agent/test_subagent_recursion.cpp`（沿竞态回归册的白盒手法）。
- 不在本单：正常收尾路径（甲已落地）、唤醒识死闸（预留封顶单已落）、跨账 reparent。

## 一、合同

- 强收（墙钟/无进展两闸）触发时，孩子的最终状态与投父邮箱照"先喂饱再 notify"的同锁次序，或等价保证——父不会在邮箱空、活孩子零的门上提前退场。
- 强收的本义（超时强终、不许无限拖）不破：收口时限与现在同档，不因投递而放水。
- 父真死语义不变：未送达清单照旧，不改投 main。

## 二、修法方向

1. 先读两强收函数的持锁形状：能复用 `DeliverChildCompletionLocked`（回流锁缝单已拆出）就直接在终态翻页处并投递——与正常收尾同款。
2. 若强收路径有多孩子批量翻页，保持"逐个翻页逐个投递"或"全翻完统一投"择一，锁内不回头拿锁。
3. 竞态册沿既有白盒手法（镜像强收入口、放大翻页与投递间的拍），断言强收场景父仍收得着 ChildCompletion；看门狗保测试不挂死。

## 三、验收

- 墙钟/无进展两闸各一册竞态回归：强收触发、父必收孩子结果、delivered 真。
- 时限册：强收阈值不因本单放宽（既有册的常数断言零改）。
- 既有 subagent 册全绿。

## 四、边界

只动强收两函数及其线程；正常收尾、取消、派工即败路径一行不碰。

## 五、实施纪要（2026-09-09）

- **核实**：两强收函数（`ForceFinalizeWallClock`/`ForceFinalizeNoProgress`）都是"单任务持锁翻页"形状——入口 `lock_guard(mutex)`，翻终态、记账、`EmitSupervisionEventLocked`、`NotifyStateChangeLocked`、`Touch`，无多孩子批量翻页（监督器 `FireWallGrace`/`FireNoProgressGrace` 逐任务单调），"逐个翻页逐个投递/全翻完统一投"的择一题不成立，落点唯一：翻页处并投递。监督器两落点在监督锁外调台账（`ProcessDeadlines` 锁内只收集 due），不新增锁嵌套。
- **改法**：两函数在 `EmitSupervisionEventLocked` 之后、`NotifyStateChangeLocked()` 之前各插一笔 `DeliverChildCompletionLocked(task)`——与 `FinalizeFromToolResult` 的正常收尾同锁同序（翻页 -> 投递 -> notify），notify 落地时父邮箱已喂饱。锁序 ledger->inbox 同向（回流锁缝单已核实全仓无反向）；`RecordChildDeliveredLocked` 至多 notify cv，不回头拿锁。强收的是根任务（MainTurnContext）时投递自返 false，天然空转。
- **幂等**：强收投递成功后 `delivered` 真；任务线程晚到的收尾（`FinalizeFromToolResult` 的 `force_finalized` 分支，`deliver_to_parent=true`）再投时因 `delivered` 已真自返 false，父不见两遍。
- **强收本义零改**：墙钟阈值、宽限期、空转阈值、Failed/WallClockTimeout/NoMeaningfulProgress 分型、部分结果保留（CheckpointFallback）一概未动；投递只多翻一枚 `delivered` 与父邮箱一项。父真死（先被强收/取消/已收口）时投递自返 false，未送达清单语义照旧，不改投 main。
- **测试**（test_subagent_recursion.cpp 新增两册，沿回流锁缝册白盒手法）：白盒建父（MainTurnContext 根）/子（ParentTaskInbox），父线程镜像生产续投环（取件/封账/WaitingChildren 条件变量等待），先泊稳 20ms；孩子线程镜像监督器强收落点直调 `ForceFinalizeWallClock`/`ForceFinalizeNoProgress`，随后 sleep 25ms 放大"任务线程晚到收尾"那拍，再走生产晚到收尾（`FinalizeFromToolResult` 带 `deliver_to_parent=true`）。断言父恰收一批 ChildCompletion（投影头/外来资料声明/强收分型状态行"失败·墙钟超时"/"失败·空转收口"，且不含晚到收尾正文）、`delivered` 真、强收终态不被翻案、父正常收口、drain 后未送达清单干净；轮询看门狗（5s 不走完 CancelTask 叫醒）保测试不挂死；各 20 轮连跑。若把投递从强收锁内退回（修复前形状），父在 25ms 拍里见空邮箱封账，`REQUIRE(absorbed.size() == 1)` 稳定转红。
- **时限册零改**：既有墙钟时限册（test_agent_activity.cpp 的 `SetWallClockTimeout(1,5)`/`(1,1)` 两册）与监督器册（test_agent_supervisor.cpp）一行未碰，`git diff` 只含 task_ledger.{hpp,cpp}、test_subagent_recursion.cpp 与本单。
- **agent_tool.cpp 看门狗线程**：核实后未动——强收的触发与落账全在监督器（src/runtime/agent_supervisor.cpp）与台账两函数里，agent_tool 侧无强收代码路径，续投环/晚到收尾（`deliver_to_parent=true`）沿用回流锁缝单已并锁的形状。
