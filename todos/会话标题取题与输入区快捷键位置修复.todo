# 会话标题取题与输入区快捷键位置修复

- 状态：解决方案已定；实现已在当前工作区验证，源码尚未提交
- 日期：2026-09-03
- 现场版本：`lubancode 0.26.175`
- 类型：LubanCode 产品缺陷（会话标题取题 + 终端底栏布局）
- 现场截图：首问以 `.todo` 路径开头，末尾只说 `commit一下这个吧`；本地标题截成盘符路径，模型精炼又写成“提交终端重画与光标修复待办”；`? for shortcuts` 落在输入框下方，`4 skills` 反在它上面
- 范围：`src/app/session_title.*`、`interactive_session.*`、`src/cli/bottom_chrome.*`、`console_input_composer.cpp`、`console_input_stream_footer.cpp` 及相应测试
- 关联单：`会话标题精修生成与通知时序缺陷.todo`、`ShiftTab五档审批模式与中文状态栏设计.todo`
- 不在本单：改 cheap/normal 路由、取消异步标题精炼、五档权限语义、重做 slash/search/mention 菜单、提交当前源码改动

## 一、要修成什么样

截图里的首问：

```text
D:\lubancode\todos\终端思考活动条重画与输入框光标跳动.todo commit一下这个吧
```

本地标题应取文件主题：

```text
终端思考活动条重画与输入框光标跳动
```

模型若再精炼，可写：

```text
修复终端光标跳动
```

不得再写：

```text
D:\lubancode\todos\终端思考活
提交终端重画与光标修复待办
```

空 composer 的底栏次序钉死如下：

```text
? 键位 · Ctrl+R 搜历史 · Ctrl+O 展开/收起 · Ctrl+G 编辑器
确认 Shift+Tab 切换到 auto 模式                                  4 skills
--------------------------------------------------------------------------
>
--------------------------------------------------------------------------
```

英文同理：`? for shortcuts` 必须独占 `N skills` 上一行。空闲、模型工作、排队输入三种场面都守同一位置，不能一开跑又跳去 `Working` 行右端。

## 二、根因

### 2.1 本地标题把首行当成自然语言硬截

`LocalSessionTitle()` 只取首行，随后直接按 UTF-8 码点截断。首问若以绝对路径开头，盘符与层层目录先吃完长度，真正文件名反被截掉。这里缺的不是更大的字符上限，而是“路径与自然语言不是一类输入”的取题规则。

### 2.2 精炼提示把处置动作当成任务主题

精炼器只喂首问，这条边界本身没错。问题在提示词只要求“说清请求在做什么”。模型看见句尾 `commit一下`，很容易拿“提交”作谓语，再把文件名压成宾语，便生出“提交……待办”这类像 Git 操作记录、又不像会话主题的标题。

### 2.3 异步精炼结果被写进正文区

标题精炼在首个主 turn 收口后起飞。结果晚归时，控制器把“标题已设为……”用裸 `TermOut()` 写在 `Worked for` 与下一枚输入框之间。它虽不是模型正文，视觉上却混进问答时间线。标题只属会话元数据，不该留下永久对话行。

### 2.4 快捷键提示与 skills 共用错了层

当前模式行把 `skills` 钉在右端，又把空 composer 的键位速览塞进 `transient_rows`。`transient_rows` 固定画在输入框、Agent 坞之后，故截图里 `skills` 在上，快捷键反沉到左下角。忙碌态还有第二条旧路：`BuildFooterWorkingLine()` 把 `? for shortcuts` 拼到 `Working` 行最右。两条路各摆一次，位置自然不守恒。

## 三、标题取题合同

### 3.1 本地标题先分输入形状

`LocalSessionTitle(first_query, max_chars)` 仍只看首行，免得多行粘贴把说明正文卷进标题。首行再按下列次序处理：

1. 跳过开头空白。
2. 若开头是 Windows 盘符绝对路径、UNC/反斜杠根路径或 POSIX 根路径，抽出路径段。
3. 路径可用单引号或双引号包住；带空格路径必须靠引号定界。未加引号时，第一枚空白结束路径。
4. 只取 basename，剥最后一层扩展名。例如 `...\修复登录超时.todo` 取 `修复登录超时`。
5. 路径后面的 `commit一下`、`请提交` 等处置文字不参加本地标题。
6. 首行不是路径，照旧把首行交给 `SanitizeTitle()`；不擅自删自然语言中的动词。
7. 最后统一剥围栏、引号、首尾空白，压连续空白，并按 UTF-8 码点限长，绝不从汉字中腰切断。

本地规则只做高置信度裁剪，不上正则词典猜语义。`请修复 D:\...` 仍走普通首行兜底，交给第二层模型精炼；不要为包打天下写一套脆弱中文分词器。

### 3.2 精炼器同时拿“线索”和“原请求”

标题请求正文改成：

```text
本地主题线索: <LocalSessionTitle(first_query, 48)>
原请求: <首问 UTF-8 安全前缀，最多 600 字节>
```

这样既给模型一枚已去盘符、去扩展名的题眼，又保留原请求，免得只看文件名误判。600 字节刀口仍算在原始首问上；附加线索不能破坏旧 UTF-8 边界合同。

system 提示钉死四条：

- 写任务实际主题，不写交付或处置动作。
- 路径出现时，优先从文件名判断主题。
- 忽略 `提交`、`commit`、`继续`、`帮我处理`、`看看图片` 这类尾随指令。
- 给正反例：应写“修终端光标跳动”，不要写“提交终端修复待办”。

模型输出仍走 `SanitizeTitle()`。失败、超时、空输出照旧保留本地标题，不重试，不刷错误车轮。

### 3.3 自动精炼静默改名

精炼结果采纳后仍须：

- 记真实 usage 与耗时；
- 核对 generation，拒掉 `/clear`、`/resume`、人工 `/title` 后的迟到结果；
- 落 `control.title.changed`；
- 同步 peer 名册与终端标题。

但不再把“标题已设为……”写进正文区，也不再保留 `deferred_title_notice_`。自动标题没有非看不可的副作用；用户要查，可看终端标题、`/sessions` 或显式 `/title`。人工 `/title` 的即时确认不在本单，照旧显示。

这里不改精炼发起时序。它仍在首个主 turn 封口后起飞，守住一条 stream 同时只有一只 open turn。静默只改展示，不改账序。

## 四、底栏布局合同

### 4.1 给快捷键速览独立分区

给 `BottomChromeModel` 与 `BottomChromeFrame` 添 `shortcut_rows`。固定行序：

```text
help_rows
activity_rows
queue_rows
shortcut_rows
status_rows
上横线
composer_rows
下横线
agent_dock_rows
transient_rows
```

其中：

- `shortcut_rows` 只放空 composer 的常用键速览，固定在模式/skills 上方。
- `status_rows` 仍放当前审批档、下一档提示与右端 skills 数。
- `transient_rows` 仍放 slash 候选、历史搜索、文件提及等临时菜单，位置留在输入框下，不跟本单搬家。
- 展开的 `?` 全量帮助仍走 `help_rows`，垫在帧顶；本单只挪常驻的一行速览。

### 4.2 空闲与忙碌只认一份位置

`console_input_composer.cpp` 在 composer 为空、且 `hint_lines` 只有常驻速览一行时，把它送进 `shortcut_rows`；搜索、提及、slash 等仍送 `transient_rows`。

`console_input_stream_footer.cpp` 用同一判定。若模型工作时 composer 为空、又没有完整速览，就至少放一行本地化的 `input.shortcuts_hint`。同时从 `BuildFooterWorkingLine()` 删去右端快捷键拼接；活动行只留：

```text
• Working / Stopping + Esc 提示 + 秒钟
```

如此一来，忙闲切换只换活动行，不搬快捷键，不碰光标。

### 4.3 帧账、窄屏与矮屏

`shortcut_rows` 必须进入：

- `TotalRows()`；
- `AgentDockFirstRow()`；
- `BottomChromeFingerprint()` 与 revision；
- resize 旧帧追踪；
- `dropped_optional_rows`。

高度不足时，输入正文仍最保。可选行按下列次序舍：

```text
transient -> dock -> shortcut -> queue -> activity -> help
```

常态有空间时，快捷键须完整占一行；窄屏只按显示宽截断，不软换行，不切坏 UTF-8 或 ANSI。`skills` 仍优先保住右端，不能因快捷键另占一行又改掉既有窄屏合同。

## 五、源码落点

- `src/app/session_title.cpp`：路径 basename/stem 取题；精炼提示与“本地主题线索 + 原请求”正文。
- `src/app/interactive_session.cpp`：精炼采纳后静默更新，不再写裸通知。
- `src/app/interactive_session_controller.hpp`、`interactive_session_assembly.cpp`：删通知参数、压后通知方法与状态槽。
- `src/cli/bottom_chrome.hpp/.cpp`：`shortcut_rows`、固定行序、帧高、指纹、坞偏移与高度裁剪。
- `src/cli/console_input_composer.cpp`：空闲 composer 将常驻速览分流到 `shortcut_rows`。
- `src/cli/console_input_stream_footer.cpp`：忙碌态同源分流；`Working` 行不再夹快捷键。
- `tests/unit/app/test_session_title_account.cpp`：截图同款路径与带空格引号路径。
- `tests/unit/memory/test_model_router.cpp`：新请求正文与 600 字节 UTF-8 刀口。
- `tests/unit/cli/test_bottom_chrome.cpp`：快捷键在 skills 上一行，临时候选仍在框下。

## 六、测试与证据

当前工作区实现已跑：

```powershell
cmake --build build/release --config Release --target lubancode_tests -j 4
ctest --test-dir build/release -C Release -R "unit\.(app\.(session_title_account|session_title_refiner|status_panel_assembly)|cli\.(bottom_chrome|status_refresh))$" --output-on-failure
ctest --test-dir build/release -C Release --output-on-failure -j 4
```

结果：

- 测试目标编译成功，退出码 `0`。
- 聚焦测试 `5/5` 通过，退出码 `0`。
- 第一轮全量查出一条旧格式断言：`unit.memory.model_router` 仍硬认 `用户: ...`；更新成“本地主题线索 + 原请求”合同后，单测通过。
- 最终全量 `362/362` 通过，退出码 `0`，总耗时 `123.66s`。
- 主程序全目标第一次编译已成功；末次重链时，运行中的 `build/release/Release/lubancode.exe` 被 Windows 占用，报 `LNK1104`。这只说明新 exe 尚未覆盖，不可冒充真机验收失败，也不可冒充发布物已生成。

新增/校正断言：

- [x] 截图同款 `.todo commit一下` 输入只取文件 stem。
- [x] 带空格的引号路径只取文件 stem。
- [x] 标题精炼原请求在 600 字节处不留下半枚汉字。
- [x] 新标题请求带本地主题线索，不带助手回复或工具转写。
- [x] `shortcut_rows` 正在 `status_rows` 上一行。
- [x] slash 临时候选仍在输入框下。
- [x] 帧行数与指纹计入快捷键行。
- [x] 全量 CTest 通过。

## 七、真机验收

源码提交、退出旧进程并重建新 exe 后，必须补以下真机证据：

1. 用截图同款首问开新会话。顶部本地标题不得含 `D:\`、`todos`、`.todo` 或 `commit`。
2. 等第一轮收口与精炼完成。正文区不得再出现迟到的“标题已设为……”永久行；`/sessions` 与终端标题须已换成精炼结果。
3. 空闲 composer 截图：键位速览在 `N skills` 正上方，不在左下角。
4. 模型工作至少 10 秒：快捷键仍在同一行位；`Working` 行右端不再冒第二枚 `? for shortcuts`。
5. 忙时键入排队草稿、清空、再输入 `/`、`@`、`Ctrl+R`：slash/search/mention 菜单仍在框下，快捷键只在空 composer 出现。
6. 在 20、40、80、120、200 列宽下各看一遍；文字不越界，skills 右端不丢，光标不跳。
7. 打开 `?` 全量帮助再收起；帮助层与常驻速览各归各位，不重复，不残影。
8. 跑人工 `/title`、`/clear`、`/resume`、cheap 超时与 cheap 报错；标题 generation、usage、退出时长照旧。

真机未跑完前，只可说“实现与自动测试已过”，不可说“终端显示已验收”或“缺陷已发布修复”。

## 八、提交边界

本次按用户要求只提交这份 todo：

```text
todos/会话标题取题与输入区快捷键位置修复.todo
```

当前工作区里的标题、底栏、测试源码，以及用户正在改的 `终端思考活动条重画与输入框光标跳动.todo`、`tests/manual/stream_footer_driver.cpp`，都不随本文档提交。源码应另起提交，提交前重看 diff、退出占用中的旧 exe、重建并补第七节真机证据。
