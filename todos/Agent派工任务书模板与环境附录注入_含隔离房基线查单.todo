# Agent 派工任务书模板与环境附录注入（含隔离房基线查单）

- 状态：2.3 已修（2026-09-02 实战六场两段接力+主控修编译三错,362/362 全绿）;2.1 环境附录与 2.2 模板已修（2026-09-03,单测钉死,362/362 全绿;真机交互会话复测待排）
- 现场版本：`lubancode 0.26.169`
- 日期：2026-09-02
- 类型：LubanCode 产品缺陷（agent 派工辅助与隔离房基线）
- 范围：agent 工具派工的任务书辅助（模板/环境附录注入）、隔离房起树路径的基线冻结、派工前单子状态核对
- 关联：《Agent派工Worktree基线_后台能力与长任务收尾缺陷.todo》（其 P0 修了主派工口基线，本单 2.3 是漏网路径实证）
- 不在本单：run_command 超时与重跑（工具层，另单）；改写用户 prompt 本体（只做注入与引导）

## 一、现场

**现场 A——白干的活（基线+查单双失守）**：主人派 general-purpose 子代理修《Agent派工Worktree基线》单。该单 **0.26.164 已修完合并**；子代理隔离房 `agent-w0f2a4cct9` 基线落在旧 origin/main（2ac86613 一线）——在旧树上修已修完的单，739s / 2.26M tokens 全部沉没。两个失守叠一起：隔离房起树路径没吃到"冻结调用者 HEAD"的修复；派工口也没查"这张单是不是已经修完了"。

**现场 C——任务书裸奔**：派工 prompt 只有背景/任务/报告三段套话（"修复 todos 中编号 1 的任务"这类含糊指代），无环境实情（离线 _deps、preset、ctest 临时 USERPROFILE、--clean-first）、无纪律（commit 署名、不 push、版本号、只勾验过的）、无验收线——子代理全程自己摸环境，烧 turn 烧 token。对照主控实战任务书（单子路径/范围红线/环境实情/纪律/完工标准/回报格式六件套），一次跑成率天差地别。

## 二、修法

### 2.1 环境附录自动注入（P1）

- agent 工具派工时自动在用户 prompt 尾部附"本机环境附录"：仓库 preset、离线构建路（_deps 拷贝 + `FETCHCONTENT_FULLY_DISCONNECTED=ON`）、ctest 规矩（临时 USERPROFILE、rg 全机兜底）、动头文件 `--clean-first` 提醒。
- 数据源：启动时探测一次并缓存（build 树在不在、_deps 齐不齐、preset 名），会话内复用。
- 附录标注来源（"[宿主注入·本机环境附录]"），不与用户正文混淆。

### 2.2 任务书模板（P2）

- `/help` 或文档提供六件套模板：单子路径 / 范围红线 / 环境实情（已由 2.1 自动）/ 纪律 / 完工标准 / 回报格式。
- 模板只引导不强制；派工参数可选 `template: full` 直接套壳。

### 2.3 隔离房基线全路径冻结 + 派工前查单（P0）

- 彻查所有起树路径（含 `.lubancode/worktrees/agent-*` 系的嵌套派工/子代理路径），一律冻结派工瞬间调用者 HEAD；TaskSnapshot 的 base_commit 与调用者 HEAD 不符时阻断——前单 3.3 合同补齐到漏网路径。
- 派工前查单：prompt 指向 todos 单子时，若该单在当前 HEAD 已标"已实现/已销"且无未勾批次，派工口警示或拒绝（"此单已修完于 <commit>，确认要在当前基线重做？"）。
- 回归：本地 main 领先 origin/main 时，任意路径开的隔离房 HEAD = 调用者 HEAD；派已修完的单被拦。

## 三、验收

- 主人在交互会话裸派一张单（三段套话 prompt），子代理收到的 prompt 尾部带环境附录，构建测试一把过——对照现场 C。
- 现场 A 不再复现：隔离房基线对齐；派已修完的单被警示拦截。
- 全量 ctest 零回归。

## 四、实施批次

- [x] P0：隔离房基线全路径冻结 + 派工前查单（2.3）(派工口早段冻结caller_base+SetupIsolationRoom二次核对阻断漂移;查单口解析todos/*.todo路径,已实现/已销且无未勾批次稳定拒绝;12用例钉死嵌套基线/漂移阻断/查单拦截)
- [x] P1：环境附录自动注入（2.1）(新件 tools/subagent_env_appendix:启动探测一次缓存于 AgentTool,probe 由装配层 ToolRuntime 启动时灌入并预热;派工 prompt 尾部附 [宿主注入·本机环境附录],spec 合同存原文;测试册 test_subagent_env_appendix 钉注入/缓存/零注入三面)
- [x] P2：任务书模板（2.2）(agent 工具新参 template: full 六件套套壳,原文逐字节居中;模板落 docs/reference/agents.md §1.2,/help 留指引(i18n 中英成对);顺手修 embed_tool_text.cmake 正则 \t 落字面 t 吃键名的坑,template.full 键不再被嵌成 emplate.full)
- [ ] 全量 ctest 零回归；真机交互会话复测两现场(ctest 362/362 已两轮全绿;真机复测待排)
