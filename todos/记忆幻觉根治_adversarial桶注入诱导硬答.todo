# 记忆幻觉根治：adversarial 桶注入诱导硬答

- 状态：待修（2026-09-04 立单；E2 五场终版负结论入库后的下刀单）
- 级别：P1（记忆注入在"该答不出"的题上诱导硬答——净帮倒忙的桶）
- 类型：LubanCode 产品改进（记忆注入策略）
- 现场基线（E2 五场终版，`eval/locomo/paired_five.md`）：adversarial 桶配对 **-0.250 [-0.417, -0.083]**，幻觉率 A 0.417 → B 0.660——记忆注入让"答不出"的题答得更错。首批（2 场）护栏话术曾修到 35%，五场放量下不稳：**护栏方向对、力度与形态不够**。
- 证据链：逐题账 `eval/locomo/_run/e2_per_question_*.json` 本地可复算；runner 已钉 provider、熔断只数 timeout；A 态基线账可直接复用（不重烧）。
- 关联：《记忆写入侧改进》（已销，temporal 负结论同场）——本单只打 adversarial。
- 不在本单：temporal 推理面（瓶颈在模型不在注入，另议）；检索打分权重（0.26.185 刚收口）；向量库

## 一、三层修法（按侵入度递进，工头先做 A+B，C 视 A/B 效果）

### A. 检索相关性门槛硬化（首刀）

现行低分拦截（kMemoryMinRecallScore 一带）显然拦不住"沾边弱线索"——adversarial 题问的实体常在记忆里**真出现过**（别场别事），分数不低但与问题无实质关联。

- [ ] 检索结果带**相关性分级**注入：top 命中与问题仅有词面重叠、无实体-动作-对象实质匹配的，标注降权或直接不注（判据工头定：如问题实体在条目正文中出现但不在同一句/无动词关联即视为弱关联）。
- [ ] 注入头部信息行升级：不只护栏话术，还带"以上 N 条按相关性排序，末 M 条为弱相关背景"——把不确定性如实传给模型。

### B. 护栏话术位置与强度（第二刀）

首批话术在记忆段头/尾——长上下文里易被稀释。

- [ ] 护栏话术改**包裹式**：每条弱相关条目前缀短标记（如"[弱相关]"），段尾保留总护栏；强相关条目不标（不误伤正常增益）。
- [ ] 总护栏措辞强化：从"不足以确定答案就答不知道"升级为明确指令"若线索与问题只是话题相近而非直接回答，视为无关，答不知道"——措辞 A/B 由工头定形并用 adversarial 子集（20 题）快测择优。

### C.（视 A/B 效果）问题侧预筛

- [ ] 答题前先做一次轻量自检：让模型对"记忆中是否有直接回答该问题的条目"自答 yes/no/unsure——unsure/no 时整段记忆不注入重答。仅在 A+B 不达标时启用（多一轮请求的成本要值回票价）。

## 二、验收

- adversarial 桶 E2 复跑（96 题双态，A 态复用基线账）：幻觉率 **≤0.50**（压回裸底水位）且四桶配对增益**不低于五场终版**（+0.097 全局/三桶 +0.24~0.30 不退）。
- 快测回路：adversarial 子集 20 题 B 态单跑（几分钟级），A/B 措辞与门槛参数用它调，终验才跑全量。
- 单测：相关性分级判据、弱标注注入形状、护栏升级文案（中英成对）。

## 三、不做什么

- 不拿幻觉率单桶改善冒充全局改善（四桶同报）
- 不删 adversarial 题或改评测口径
- 不引入新模型调用除非 C 档启用（成本入账）
