# Kanban 看板功能：任务列视图与状态流

- 状态：待实现（2026-09-01 主人立单；探索性扩展功能——先调研形态再定实现）
- 日期：2026-09-01
- 级别：P2（扩展功能，非核心闭环）
- 范围：待调研定（候选：todo_tool 扩展 / 独立面板 / web-console 视图）
- 依赖：既有 TodoTool（清单式任务）、AgentPanel/Dock（面板渲染先例）、web-console（examples/ 前端底子）
- 关联：`LubanCore与CLI分离_核心库独立成宿主底座设计.todo`（若走 web 形态则吃它）
- 不在本单：项目管理软件的完整功能（甘特/协作/多人）；把 TodoTool 重写

## 一、先下断语

看板是**任务的状态流视图**，不是新的任务系统。数据还是那本账（TodoTool 的清单/模型建的 todo），看板给它一件外衣：按列（如 待办/进行中/已完成/阻塞）摆开，项可在列间流动。**先调研三选一形态，再立实现批次——本单第一批是调研与定案，不是写码。**

## 二、形态调研（第一批的活）

三个候选，各自量优劣后给主人拍板：

1. **终端列视图**（TodoTool 扩展）：`/kanban` 命令渲染 ASCII 列（复用 Dock/AgentPanel 的面板渲染路数）。优点：零新依赖、与终端体验一体；缺点：宽列受限、拖拽没有（用命令移动：`/kanban move <id> <列>`）。
2. **web-console 视图**（前端扩展）：给 examples/web-console 加 kanban 页签，数据走 AppServer 协议新事件/查询口。优点：真拖拽、视觉自由；缺点：要动协议面（core 侧加查询口），依赖 LubanCore 分离单的边界。
3. **两者都要**（终端简版 + web 全版）：账同一本，两个视图。成本最高，但与"界面一份、后端一个"的架构一脉。

调研要量：现有 todo 数据模型够不够（有无"列/状态"字段，缺则怎么加）、模型怎么跟看板交互（模型能否自己 move）、状态流定义（默认几列、可否自定义）。

## 三、清单（第一批：调研与定案）

- [ ] 现状照相：TodoTool 数据模型、渲染、与模型交互的全部现状。
- [ ] 三形态优劣账（含各自改动面估计、协议/核心改动清单）。
- [ ] 状态流定义草案（默认列、移动规则、与既有 todo 状态的映射）。
- [ ] 模型侧交互设计：模型可否通过工具 move 项（复用 TodoTool 还是新枚工具）。
- [ ] 定案文档：选哪形态、批次划分，等主人拍板后立实现批。
验收：三形态账目齐、有推荐项、主人拍板后实现批次的单子条目成文。

## 四、不做什么

第一版不做：多人协作、跨会话共享看板、自动排期、与外部看板工具（Trello/Jira）同步。

## 五、最终验收门（调研批）

定案文档进仓；实现批次清单可派工；不动生产码（调研批零代码或纯文档）。
