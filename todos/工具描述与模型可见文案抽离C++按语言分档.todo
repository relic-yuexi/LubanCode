# 工具描述与模型可见文案抽离 C++,按语言分档

状态：基建与批1-2 已并入(2026-08-22,0.26.14/0.26.19):tool_text 查表回退链、embed 通道含 (POSIX) 分档节,read_file/write_file/edit_file/search/命令族入档,逐字节零差异;批3-5(代理族含 persona/交互/外接)在途

## 一句话

给模型看的文字(工具描述、子代理 persona、schema 参数说明)现在焊死在 C++ 里且只有中文——英语用户拿到一手中文工具表。要抽成数据文件,内置中英两套,其他语言谁要谁自己加。

## 用户定调

- 这些提示词**不该写进 cpp**——不合多语言。
- **至少中英两套**。
- 其他语言:谁需要谁自己修改——机制上留好口子,不内置。

## 现状盘点(已核)

模型可见的中文文案散在三类地方:

1. **工具描述与 schema 参数说明**——`src/tools/*.cpp` 十几个文件里 `return "中文…"`,如 `agent_tool.cpp:30` 的子代理 persona、`run_command.cpp:92` 的参数说明、`edit_file.cpp`/`ask_user.cpp`/`context_tools.cpp` 等的 description。粗数约 **40+ 处**,全部只有中文。
2. **记忆抽取分型提示词**——已在数据文件(`src/prompts/features/memory-summary-*.md`),机制对路,但同样只有中文一套。
3. **系统提示骨架**——`src/prompts/{core,features,platforms}/*.md` 编译期嵌入 + `~/.lubancode/prompts/` 用户覆盖,机制完全对路,也是只有中文。

对照组:终端 UI 文案走 `cli/i18n.cpp` 中英成对 + `~/.lubancode/languages/*.json` 外部语言包回退中文——**人看的字已双语,模型看的字没有**。

既有可复用的地基:
- prompt 模块机制:嵌入 → 播种 → 用户文件压过嵌入(运行时逐模块取值)。
- i18n 语言选择链:`LUBANCODE_LANG`/config.language → 内置 zh-CN/en → 外部语言包。

## 要做成什么样

### 一、存放:数据文件,不进 cpp

- 每个工具一条描述文件,或按领域归档(见"分档"节),放 `src/prompts/tools/` 之下,随构建嵌入,机制对齐 prompt 模块(embed → seed → override)。
- 工具 C++ 代码里只剩键名与拼装:`ToolDescription("agent.persona")` 这类;文案一概查表。
- schema 参数说明同路:每个工具的参数说明打包进同一份文件,键名带参数路径。

### 二、语言分档

```text
src/prompts/tools/
  zh-CN/          内置简体中文(默认,从现行 cpp 文案原样搬)
  en/             内置英文(整份翻译)
```

- 选择链与 i18n 一致:会话语言 → 该语言目录 → 缺档/缺键回退 zh-CN,再回退 cpp 里那份原文案(迁移期兜底)。
- 外部语言包同样能盖工具描述:`~/.lubancode/languages/<lang>/tools/*.md`(或并入现有语言包格式,实现时择一,别造第三套目录规矩)。
- 翻译质量取舍:首版英文目标"准确、完整、不装母语级润色";术语表(工具名、参数名)两种语言一致。

### 三、迁移:一批工具一枚 commit,不许一刀全换

- 先抽基建(查表、回退、嵌入、播种、语言选择接线),一枚 commit。
- 再按领域逐批搬:文件工具(read/write/edit/search)→ 命令与后台(run_command/background)→ 代理族(agent/agent_message/Explore persona)→ 交互(ask_user/todo)→ 外接(lsp/mcp/lua/context_tools)。
- 每批:cpp 删字串、建 zh-CN 与 en 两份、注册进嵌入清单、既有单测改从表读、补"英文档选择生效"与"缺键回退"单测。
- 记忆抽取分型提示词与 core/features/platforms 系统提示的英文版**另立后续单**,本单只管工具表——战线别拉太长。

### 四、给模型的语言跟谁走

- 缺省跟会话界面语言(i18n 选择链):中文用户给中文工具表,英文用户给英文工具表。
- 不做"按模型名猜语言"——模型能不能读中文是它的事,用户界面是什么语言是我们的事;要改语言,`/language` 一处改,人看模型看一起换。

## 验收

- `LUBANCODE_LANG=en` 起会话:所有注册工具的 name/description、schema 参数 description 均为英文;`/tools` 与 wire 请求里的 tools 数组一致为英文。
- `LUBANCODE_LANG` 缺省(中文机器):与现行一字不差——迁移零行为变化。
- 某工具只有 zh-CN 档:英文会话下该工具回退中文,启动打一行"工具 X 无英文档,已用中文"不静默。
- 外部语言包盖工具描述生效;坏文件跳过并警告,不阻断启动(对齐语言包既有规矩)。
- cpp 里 `src/tools/*.cpp` 不再有面向模型的成段中文字面量(键名与拼装代码除外);grep 抽查过关。
- 既有工具单测全绿;每批补"英文档生效/缺键回退"两例。

## 不做

- 不为翻译质量上模型自动翻译;翻译人工管。
- 不把 UI 文案(i18n.cpp 那套)并进来——人看的和模型看的两本账,机制相邻、目录分开。
- 不动工具 schema 的结构(字段名、类型、enum),只动 description 文字。
- 不首期内置中英之外的第三语言;机制留口子,内容谁要谁添。
- 不借机改任何工具的行为与确认语义。
