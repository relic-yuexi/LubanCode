# 查看态下的后台回流与完成退场回归

状态：待做（2026-08-16 用户实测三桩,基线含多智能体视图回归单)

## 用户实测三桩

场景:两只后台代理,用户 Enter 切到 agent #2 查看。此刻 #1 完成。

1. **#1 完成后,底部导航坞里那行没自动退场**——回归单声称 done+delivered 已退场,实测仍在。
2. **#1 的结果应回流 main,main 在后台消化并输出分析**——用户此刻在看 #2,这些应全部后台发生,不打断 #2 视口;回 main 时能看到 main 的新输出。
3. **回流后 context 应该变大**(结果进了 main 历史)——实测没变。

## 根子分析(已核到代码路径)

回流主路在 `src/app/interactive_session.cpp:2447` 一带(空闲循环顶):

```text
HasUndeliveredCompletions()
-> CompletionNoticeLines() 用裸 std::cout 打通知
-> RunPeerTurn("后台子代理有新结果送达...") 原地跑主轮
```

这条路的每一环都没考虑 `viewed_task_id != 0`:

- **通知打在哪**:裸 cout 落在当前光标处——你正看着 #2 的 retained 视口,这几行字就糊在 #2 的帧上,帧账(顶行锚点)随之错位。
- **主轮跑在哪**:RunPeerTurn 的流式正文、工具卡、统计行全部往终端铺——盖掉 #2 视口;#2 的查看帧没有"挂起/让位"的交接,新旧画面互踩。
- **退场为何失灵**:delivered 由 RunTurn 开头的 DrainCompletionNotices 置位。若回流主轮因查看态视口错乱、帧互踩、或输出锁冲突而没能正常收口(或用户在混乱中按了键打断),delivered 未置或置了但坞的重画被查看帧压住——DockEntryRetired 的过滤本身是好的,坏的是它上游这条链在查看态下没走稳。诊断须实测钉死:是回流轮根本没跑,还是跑了没置位,还是置位了坞没重画。
- **context 为何不动**:context 靠回流轮的 on_usage→UpdateStatusLineContext 更新。回流轮没正常收口(或根本没跑),usage 不来,数字自然不动——第三桩是前两桩的下游症状,不是独立的病。

另核一处:空闲唤醒钩子(637 行)在 ReadLineKeyByKey 的 100ms 轮询里问 HasUndeliveredCompletions——composer 等键时会醒,循环能转,回流有机会触发;但唤醒把读取打断返回空串,若用户正在给 #2 敲半句草稿,草稿保存/恢复的账也要一并查(别为回流吃掉用户输入)。

## 需求

### 一、查看态是第一等状态,回流须后台化

`viewed_task_id != 0` 时,后台完成回流照常发生,但一切对终端的直接影响收进后台:

```text
通知:   不打裸 cout;归 main transcript 一条事件(已有 agent_notice 条目,保留数据路,去掉裸打印)
主轮:   照常 RunPeerTurn(main 消化结果、输出分析、usage/context 更新)
输出:   流式正文与工具卡不铺终端——进 main 的 transcript 台账,回 main 时重铺可见
视口:   #2 的查看帧原样保住;流式 footer 不起(或起在不打扰查看帧的方式,诊断后定)
草稿:   唤醒打断读取前,用户敲了一半的草稿必须保全(现有 draft 保存路核一遍)
```

等价于:main 在后台"静默收货",用户看什么不受影响;回到 main 时新内容都在。

### 二、完成退场链钉死

- 回流轮正常收口 → delivered 置位 → 坞行退场 → 查看帧(若在看别的代理)与空闲帧同帧重画,两路都得验。
- 用户正**看着 #1** 时 #1 完成:既有规矩(结果交 main 一次/视口回 main/toast/选择落相邻)不回归。
- 用户看着 #2 时 #1 完成:#1 从坞退场,#2 视口与查看状态纹丝不动,toast 一枚"#1 已完成"(不抢屏)。

### 三、context 更新与可见

- 回流轮 on_usage 照常更新 ContextTracker 与状态行(查看态下状态行属底栏帧,更新走锁内改数据、下一帧带出——现有机制,验通路)。
- 回到 main 后状态行数字与 /context 一致;回流轮被打断时数字标旧值不冒新。

### 四、回归测试

- 状态机:看着 #2 时 #1 完成并回流——viewed 仍 #2、composer 目标仍 #2、agent1 退场、main transcript 新增完成事件与主轮输出、context 增长。
- 刮屏驱动器:#2 查看帧在回流全过程中锚点不漂、无主轮文本侵入、回流完成后坞里无 #1。
- 草稿:给 #2 敲半句,#1 完成回流,草稿原样回来。
- 真终端手测:复刻用户场景两只后台代理,切 #2 等 #1 完成,逐拍核对三桩。

## 不做

- 不为"不打扰"就不回流——main 必须收货,只是安静地收。
- 不把后台轮的输出静默丢弃——进台账,回 main 可见。
- 不在查看态起第二套互踩的帧——要么复用底栏帧账,要么挂起让位,诊断后择一。
- 不拿"用户切回 main 才回流"当修法——那是不修。

## 销单线

1. 看 #2 时 #1 完成:回流后台发生,#2 视口零扰动(锚点、内容、光标)。
2. #1 坞行退场,台账保留。
3. 回 main 见 main 的新输出与完成事件各一次。
4. context 增长且状态行/`/context` 一致。
5. 半句草稿不丢。
6. 看 #1 时 #1 完成的既有三步(回 main/toast/选择落相邻)不回归。
7. 状态机/刮屏/手测三层全过。
