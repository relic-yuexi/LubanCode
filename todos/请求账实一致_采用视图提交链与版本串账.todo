# 请求账实一致：采用视图提交链与版本串账

- 日期：2026-09-16。P1。
- 来源：原《Session V3 真实会话审计》单 V3-REAL-06 的阻塞尾巴，该单随 PR #55 销单删除，完整链路按其 §七"另立单"指令立此单承接。
- 已落基础（随 #55）：prepared 行 inputView 视图指纹账（消息数/链引用数/逐消息指纹/system 指纹/divergent 标记），离线可逐块核对引用还原与实际输入是否一致。
- 状态：待实施。

## 待办

- [ ] 「先提交已采用预览/视图，再以它准备请求」的完整链路：动消息主轴与视图提交链重排——请求准备消费的是已提交视图，不是内存临时裁剪；prepared 有序引用还原的即实际模型输入。
- [ ] estimated/prepared/sent/usage 串到同一次请求与同一输入版本；输出降额逐请求可追溯；不盲目重复落全量 wire，复用不可变正文与引用。
- [ ] 验收：捕获假后端实际发送正文与 prepared 引用重建逐块比对；覆盖巨型工具结果、降档、Hook 改写、compact、resume——不能只断言 refs 数量或 JSONL 可解析（原单验收口径）。

## 关联

- 主合同：session轨迹v3 总设计单 §4.36。
- 触发场景原档已删，现场复算样本见 git 历史原单（PR #55 时代）。
