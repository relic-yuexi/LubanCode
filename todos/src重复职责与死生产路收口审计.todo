# src 重复职责与死生产路收口审计

- 状态：首轮源码审计完成；终端 UI 五组、非显示模块六组问题已坐实，另留两组待证候选
- 日期：2026-09-03
- 类型：架构债与回归防线
- 范围：`src/` 内重复生产路、重复状态源、死调用路径、靠数据形状猜语义的分流，以及对应测试盲区
- 关联单：`ShiftTab五档审批模式与中文状态栏设计.todo`、`会话标题取题与输入区快捷键位置修复.todo`
- 不在本单：当场修改源码、顺手删同字翻译、只凭函数名相似便判重复、把测试夹具或中英文双语表算作冗余

## 一、审计口径

本单所说的“冗余”，不只是两段代码长得一样。满足下列任一条，才入账：

1. 同一用户语义由两处独立生产，改一处便会忙闲分叉、前后台分叉或协议分叉。
2. 同一份状态由两本账各自保存，生命周期、锁、默认值或落盘时机并不共用。
3. 一条旧生产路已无调用，数据采集与测试却仍围着它运转，绿灯掩住真实功能缺失。
4. 上层拿字符串、行数、空值等外形猜语义，缺少明确类型；新场景一来便容易走错槽。
5. 同一合同的格式化、解析或权限裁定散在多处，已有可见漂移，或缺少能阻止漂移的交叉测试。

下列情况不凭“重复”二字硬删：

- 不同业务场景碰巧用了同一句翻译；
- 纯函数与平台适配层各守一层职责；
- 读路径与写路径虽成对出现，生命周期本就不同；
- 测试夹具为隔离边界而保留的小段构造；
- 中文表与英文表中的同名 key。

每项须留四样证据：两条源码路径、实际调用关系、可见或可推导风险、一个明确收口点。证据不够，只列“候选”，不得标成缺陷。

## 二、已坐实：终端 UI 与底栏

### P0：完整状态栏成了死生产路，数据照采却无人绘制

当前有两套状态行组装：

- `src/cli/console_input.cpp::BuildComposerModeLine()` 只画审批档、下一档提示与 `skills`。
- 同文件 `BuildStatusLine()` 会读取 `StatusDataSlot()`，画模型、effort、cwd、Git 分支、context、tokens、cache、REC、worktree、tools、Plan、goal/loop、后台任务与审批档。

生产调用已经一边倒：

- 空闲 composer 在 `src/cli/console_input_composer.cpp` 调 `BuildComposerModeLine()`。
- 流式 footer 在 `src/cli/console_input_stream_footer.cpp` 也调 `BuildComposerModeLine()`。
- `src/` 内已找不到 `BuildStatusLine()` 的调用，只剩声明与定义。

旧账却没有停：

- `src/app/interactive_session.cpp` 每轮仍调用 `BuildStatusPanelData()`，重取 cwd、分支及动态状态。
- `SetStatusLineData()` 仍把 `values/items/separator` 写进 `StatusDataSlot()`。
- usage 到达后，`UpdateStatusLineContext()` 仍更新 context、used tokens、window tokens 与 cache note。
- `tests/unit/cli/test_format_utils.cpp` 仍验证完整状态段的纯函数。

结果是“采集有值、纯函数有测、真底栏不画”。0.26.181 真终端已看见模型、cwd、context、tokens 一齐消失。两组单测可以各自全绿：一组测完整状态数据，一组测精简模式行，却没有一条集成断言要求最终 `BottomChrome` 同时保住两类信息。

收口：

- 不可简单删除 `BuildStatusLine()`；它背着仍需恢复的产品资料。
- 建一条正式状态投影：上方模式行只拿 `permission_mode + skills`，输入框下方资料行继续按 `status_panel.items + separator` 画其余状态段。
- `StatusPanelData` 只保一份活账；两行从同一快照投影，不再各造一套状态模型。
- 收掉无人调用的兼容声明，或把它改成正式资料行构造器并恢复生产调用；不得继续半死不活。

回归：

- 空闲与运行中都断言 mode、skills、model、cwd、branch、context、tokens 同时可见。
- `status_panel.items` 重排与隐藏仍生效；只把 `permission_mode` 从资料行剥出。
- context/usage 新快照到达后，最终帧随下一次安全重画更新。
- 真终端留空闲、Working、排队输入三张图；单测不可冒充发布验收。

### P1：空闲与运行中各拼一遍 `BottomChromeModel`

`src/cli/console_input_composer.cpp` 与 `src/cli/console_input_stream_footer.cpp` 各自填写：

```text
queue_rows
agent_dock_rows / agent_dock_tints
mode_notice_rows
shortcut_rows / transient_rows
rule_tag / selected_task_id
composer.editor / prompt / mode / confirm_mode
status_rows
```

两路眼下只在 activity、placeholder、composer mode 等处本应不同，其余大段职责重叠。`?` 忙闲文案分叉与右对齐丢失，已经证明这不是抽象洁癖：同一底栏语义一旦要改，调用方很容易只搬一半。

收口：

- 抽出共同的 `BuildBottomChromeModel()` 或语义等价的 assembler。
- 调用方只提供场景差值：Idle/Busy、activity、placeholder、队列输入状态。
- mode notice、assist row、status projection、dock、rule tag 的归槽只写一次。
- `BottomChrome` 继续独占行序、高度预算、ANSI 安全截断与物理布局。

回归：

- 用同一语义快照分别造 Idle 与 Busy，除活动条和 composer mode 外，公共行内容、列号与裁剪结果须一致。
- 空闲、Working、排队输入、切档说明显示中四景共用一组参数化断言。

### P1：`RenderState::hint_lines` 一只篮子装四类语义

`src/cli/line_editor.hpp` 仍把 `hint_lines` 注释成 slash 匹配行。实际还装：

- 历史搜索面板；
- `@` 文件提及菜单；
- 空 composer 的常用键速览；
- slash 候选及普通临时提示。

空闲与流式两路随后用下列外形猜身份：

```cpp
composer_empty && hint_lines.size() == 1
```

猜中便送进 `shortcut_rows`，否则送进 `transient_rows`。这条判断不知道内容是什么。一种菜单只剩一行，或以后增加一行空 composer 临时提示，便可能被抬到 `skills` 上方。

收口：

- 给提示加明确种类，如 `ShortcutAssist`、`HistorySearch`、`MentionMenu`、`SlashMenu`、`TransientNotice`。
- 更稳妥的做法是各场景直接写入 `BottomChromeModel` 对应槽，不再借 `RenderState::hint_lines` 中转非编辑器语义。
- `LineEditorCore` 只拥有编辑器自身的候选状态；应用层面板不要反塞进编辑器字段。

回归：

- 各造零行、一行、多行菜单，断言位置只随种类变化，不随行数变化。
- 搜索无命中、提及只剩一项、slash 只剩一项，都不得冒充快捷键行。

### P2：快捷键提示格式化有三条生产路

当前三路如下：

1. `console_input_composer.cpp::apply_search_hints()` 内联 lambda：`ActionId -> ChordFor -> FormatKeyChord -> tr(label)`。
2. `history_search.cpp::BuildHistorySearchLines()` 又写一只近乎相同的 lambda。
3. 流式 footer 拿不到 `hint_lines` 时，绕开 keymap，直接使用 `input.shortcuts_hint = ? 查看快捷键`。

第三路已造成空闲“`? 键位`”、忙碌“`? 查看快捷键`”。它还会在用户改绑 `HelpShow` 后继续写死 `?`。

收口：

- 提供一只 `BuildKeyHints()`：输入一组 `(ActionId, label_key)`，统一处理未绑定动作、和弦格式与分隔符。
- 帮助入口只认 `ActiveKeymap().ChordFor(HelpShow)` 与一枚标签。
- 删除 `input.shortcuts_hint` 兜底生产路；是否显示由场景状态决定，不由另一条翻译硬补。

回归：

- 改绑 `HelpShow`、`ChatSearchHistory` 后，空闲、运行中、帮助层、搜索层提示一齐跟脚。
- 动作无绑定时整段略过，不留下孤单分隔符。
- 中文与英文只换标签，不换动作来源。

### P3：Agent 面板有两对纯重复场景翻译

`src/cli/agent_panel.cpp` 按 `streaming` 选择 idle/stream 两套 key。其中两对中英文完全相同：

- `agent_panel.hint_short` / `agent_panel.stream_hint_short`
- `agent_panel.hint_idle_expanded` / `agent_panel.stream_hint_idle_expanded`

这两对可共用 key，删掉无意义分支。其余 focused 文案含“返回”与“逐层退出”差别，语义确实不同，不随手合并。

回归：

- 参数化覆盖 streaming × focused × wide × idle-summary。
- 只合并文字与行为完全相同的格子；退出层级不同的格子须保留独立断言。

## 三、已坐实：非显示模块

本轮另派一名 subagent 只读扫过 `src/` 共 902 个文件，主线程随后逐项反查定义、调用点与状态流。下列六项已有源码证据；不是把相似名字一股脑算作重复。

### P0：Hook 的 `turn_id` 另造一本账，首个 Hook 还可能吃到上一轮身份

正式回合号有一条清楚的主路：

- `src/app/interactive_session.cpp` 用 `session_runtime_.ids().NextTurnId()` 产生 `trace_turn_id`。
- 这枚号经 `TurnContext::turn_id_for_trace` 传进 `RunTurn()`。
- `src/app/turn_runner.cpp` 再把它交给 `turn_event_stream.Start(turn_id_for_trace)` 与 `turn_trace_hub->Install(...)`。

Hook 却没有复用：

- `RunTurn()` 先在约 773 行调用 `ApplyUserPromptSubmit()`；此时本轮 Hook context 尚未更新，`UserPromptSubmit` 可能仍拿启动时的 `unassigned`，也可能拿上一轮留下的 `turn_id`。
- 到约 822 行，代码才复制 dispatcher context，并调用 `HookDispatcher::NextHookRunId()`，把一枚 `hookrun_*` 填进名为 `turn_id` 的字段。
- 真正发射每次 Hook 时，`src/hooks/dispatcher.cpp::RunEventCore()` 又调用一次 `NextHookRunId()`，生成另一枚 `hook_run_id`。
- `src/hooks/protocol.cpp::BuildStdinPayload()` 会同时把 `hook_run_id` 与 `turn_id` 交给插件。两枚字段眼下都长得像 Hook 执行号，却不是同一枚，也对不上 runtime/trace 的正式回合号。

风险：

- `UserPromptSubmit`、`PreToolUse`、`PostToolUse` 可能无法归到同一用户回合。
- Hook 记录与 trajectory、tool trace、runtime event 按 `turn_id` 联表时会裂账。
- 插件若把 `turn_id` 当稳定父键，首个事件可能挂到 `unassigned` 或上一轮，后续事件又挂到伪回合号。

收口：

- 每轮只 mint 一枚 canonical turn id。它须在 `UserPromptSubmit` 之前确定，并同时交给 Hook context、TurnEventAdapter、trace hub 与 trajectory bridge。
- `NextHookRunId()` 只许生成 `hook_run_id`，不得再给 `turn_id` 代班。
- 若 prompt Hook 阻断后不算正式开轮，须把“预留 turn id、未发 TurnStarted、以 blocked 收口”的生命周期写成合同；不可为抢 ID 先发一个永不收尾的 TurnStarted。
- 子代理、workflow、one-shot 也走同一身份分配口，不各自补号。

回归：

- 一轮内 UserPromptSubmit、PreToolUse、PermissionRequest、PostToolUse、Stop 共用同一 `turn_id`。
- 每次 Hook 发射各有不同 `hook_run_id`，且不得等于 `turn_id`。
- 连跑两轮，第二轮首个 prompt Hook 不得继承第一轮 ID。
- prompt Hook 阻断、one-shot 无宿主 turn id、Stop Hook 续跑三景都要钉住生命周期。

### P1：审批档位有四套枚举，转换与强转散在多处

当前值域共有四份：

- `src/approval_mode.hpp::ApprovalMode`
- `src/cli/line_editor.hpp::ConfirmMode`
- `src/runtime/turn_runtime.hpp::PermissionMode`
- `src/agent/permission_mode.hpp::AgentPermissionMode`

转换也各写各的：

- `src/app/turn_runner.cpp::ToRuntimePermissionMode()` 写一份 Agent -> Runtime switch。
- 同文件 `BuildTurnRuntimeOptions()` 又写一份 CLI -> Runtime switch。
- `src/app/interactive_session_assembly.cpp` 的 workflow 权限口再写一份 CLI -> Runtime switch。
- `src/app/tool_runtime.cpp` 又写一份 CLI -> Agent switch。
- trajectory、peer 与 session resume 尚有枚举间 `static_cast`。

四套枚举的顺序已经不同：`PermissionMode` 是 `Confirm, AcceptEdits, Auto, Yolo, DontAsk`，其余主枚举把 `Yolo` 放在 `Auto` 前。眼下几处强转碰巧没有跨进这套异序枚举，不能算安全合同。往后添档、改名或调序，编译照过，行为却会静默串档。

收口：

- 以公共 `ApprovalMode` 作唯一业务值域。CLI 只持显示状态，Runtime 与 Agent 只持能力投影；能直接吃公共枚举便不另造同构枚举。
- 层间必须隔离时，只留一组具名转换函数，放在中立边界；删除散落 switch。
- 禁止枚举间 `static_cast`。持久化与协议边界一律按 machine name 解析，不按底层整数过桥。
- 权限比较继续按能力集合，不拿枚举大小排强弱。

回归：

- 五档 × 各转换边界做表驱动往返测试。
- 打乱某个测试枚举声明顺序，编译期或测试必须响，不能静默换档。
- session resume、trajectory、peer、Hook、子代理继承逐一核对 stable machine name。

### P1：原子写文件有多套私房实现，失败保证已经分叉

代表路径如下：

- `src/workspace/manifest.cpp` 与 `src/telemetry/service.cpp`：固定 `<target>.tmp`，写完调用 `platform::ReplaceFileAtomically()`。
- `src/gateway/control_server.cpp`：固定 `.tmp` 后直接 `std::filesystem::rename()`；Windows 覆盖失败便先删除正式文件，再次 rename。
- `src/insights/derived_store.cpp`：另造 `.tmp + .old` 搬移与回滚协议。
- 同类写法还散在 agent、memory、mcp、trajectory、workflow、config 等目录。

风险：

- Gateway 的“先删正式文件再换名”留出文件不存在的窗口，不能再叫原子替换。
- 固定 `.tmp` 会让同一目标的并发或重入写互踩。
- 各处对建目录、flush/close、替换、回滚、失败清理与错误码说法不同。
- 上层若宣称 process-crash durability，而底层只做 iostream flush，没有文件与目录持久化合同，便会言过其实。

收口：

- 在 `platform` 建统一 `AtomicWriteFile`，提供唯一临时名、同目录写入、close 检查、平台原子替换、失败清理与结构化错误。
- 明分 `AtomicVisibility` 与 `ProcessCrashDurability`；需要后者时再做文件/目录 flush，不让所有调用点口头共用“原子”二字。
- JSON 序列化留在业务层，字节可靠落盘归平台层。
- 迁移按风险分批：Gateway 控制文件与持久事实账先走，派生缓存后走。

回归：

- 目标已存在、目标不存在、替换失败、临时写失败、两写者并发、进程中断后残留清理。
- Windows 与 POSIX 分别验证；Windows 不许靠删除正式文件换取成功。

### P1：网页抓取自带一套 UTF-8 清洗器，外部文本合同分叉

`src/tools/web_fetch.cpp::SanitizeUtf8()` 手写 UTF-8 解码。遇非法字节便写 ASCII `?`。抓回网页后，正文固定走这只函数。

平台层已有正式入口：

- `src/platform/text_encoding.cpp::SanitizeUtf8()`：本机输出可先按 ACP 尝试转换，再以 `U+FFFD` 收口。
- `SanitizeExternalText()`：专管网络、MCP 等外来文本，保住已有合法 UTF-8 片段，只替换坏字节；仓内多条工具与 API 路径已经使用它。

网页工具绕过公共入口，会让同一份坏字节经 web、MCP、命令工具进入历史时得到不同正文。GBK 网页还会被不可逆地洗成一串问号。

收口：

- 删除 `tools::SanitizeUtf8`，网络正文统一过 `platform::SanitizeExternalText()`。
- 若将来要读 HTTP charset，先按响应头/HTML meta 解码，再过外部文本清洗；本机 ACP 不可无条件套给互联网内容。
- 截断继续用公共 UTF-8 边界函数，不在 web 工具里再养一套字节刀。

回归：

- 合法中文、孤立 continuation byte、截断多字节、UTF-8 中夹少量坏字节、GBK 页面分别留样。
- WebFetch、MCP rich result、SearchTool 对同一非法 UTF-8 样本须遵守同一替换合同。

### P2：SHA-256 算法完整维护两份

`src/hooks/hash.cpp` 与 `src/channel/digest.cpp` 各自带 64 枚常量、消息填充、压缩轮与十六进制输出，也都导出 `Sha256Hex(std::string_view)`。仓内绝大多数模块已经复用 `hooks::Sha256Hex`，只有 Channel 仍养第二本实现。

摘要参与内容寻址、配对、幂等与信任边界。两份眼下结果相同，不代表长期可以各修各的。

收口：

- 把算法搬到中立的 `platform/crypto` 或 `core/digest`。
- 提供字节 span 与 `string_view` 两个薄入口；Hooks、Channel 只做领域命名与输入组装。
- 迁移时保住既有摘要字节序与十六进制大小写，不能让存量 key 集体失配。

回归：

- 空串、`abc`、跨 55/56/63/64 字节边界、大载荷与含 NUL 字节向量。
- 新公共实现须与两份旧实现及公开 SHA-256 向量逐项相等。

### P2：标准 Base64 编码至少维护五份

已查到：

- `src/agent/tool_result_images.cpp`
- `src/app_server/ws_frames.cpp`
- `src/platform/clipboard_posix.cpp`
- `src/tools/run_command.cpp`
- `src/cli/image_input.cpp`

五处都手写标准 Base64。`tool_result_images.cpp` 的注释甚至明说仓里已有三份，仍又添一份。业务输入虽不同，字母表、三字节分组与 padding 合同相同。

风险在边角：空输入、含 NUL 字节、`char` 有无符号、1/2 字节尾巴、大载荷预分配。图片、WebSocket 握手、OSC 52 与 PowerShell `-EncodedCommand` 任一路漂移，表象都不同，很难联想到同一算法。

收口：

- 抽一只无业务依赖的 `Base64Encode(span<const byte>)`，另给 `string_view` 薄适配。
- 各模块只管输入编码。例如 PowerShell 先产 UTF-16LE 字节，公共函数只做 Base64。
- WebSocket 用的 SHA-1 不在本项，不顺手改算法边界。

回归：

- RFC 标准向量、空串、含 NUL、1/2/3 字节尾巴与大图片。
- 五个旧调用场景各留一条集成断言，证明迁移没改载荷。

## 四、待证候选

### P2 候选：安全边界的路径比较键复制三份

下列三处都做 `weakly_canonical`，失败退 `lexically_normal`，再统一斜杠、ASCII 小写并去尾斜杠：

- `src/tools/isolation.cpp::NormalizeKey()`
- `src/runtime/worktree.cpp::NormalizeKey()`
- `src/tools/session_utils.cpp::NormalizePathForCompare()`

前两处参与隔离与 worktree 正身校验，漂移会碰安全边界。不过三处调用域不同，当前尚未找到同一输入得出不同结果的证据，故只列候选。

先做：

- 建一组共享测试向量：不存在路径、盘符大小写、混合斜杠、尾斜杠、UNC、junction/symlink、`..`、根路径。
- 三实现若输出一致且合同确实相同，再抽中立 `PathComparisonKey()`；若安全域需求不同，改成具名策略，不强并。

### P3 候选：`run_command` 私有环境变量读取与平台入口空值语义不同

`src/platform/paths_win.cpp`、`paths_posix.cpp::GetEnvVar()` 把未设置与空字符串都当 `nullopt`。`src/tools/run_command.cpp` 又写一套跨平台读取，空字符串返回 `optional<string>("")`。

眼下私有入口只读 `PATH`，实际风险很低，尚无故障证据。可先加“未设置/空/非空”三格测试，再改用 `platform::GetEnvVar()`；若 PATH 空值须与未设置不同，应把差别写进具名 API，不留隐式分叉。

## 五、测试为何没拦住

现有测试按函数边界各守一摊，没有钉住最终用户合同：

1. `unit.cli.format_utils` 验证完整状态文本能拼出来，却不证明生产帧调用它。
2. `unit.cli.status_refresh` 验证 `BuildComposerModeLine()` 的模式色与 skills，却不问模型等资料是否还在。
3. `unit.cli.bottom_chrome` 只断言快捷键位于 skills 上一行，并用裸字符串相等固定了左起笔；它不比较两行右边缘。
4. 没有一组忙闲同源测试，把同一状态喂给 Idle 与 Busy 后逐行比较公共区域。
5. 没有改绑 keymap 后跨场景检查所有可见提示。

须补一层面向最终帧的合同测试。测试输入用结构化语义，断言可见文字、物理行序、显示列、窄屏裁剪、场景切换与数据更新。只测某只 formatter 返回什么，不足以守住终端产品。

## 六、实施批次

- [ ] P0：恢复完整资料行的正式生产调用；清掉“数据照采、函数无人叫”的半死状态。
- [ ] P0：补最终 `BottomChrome` 集成合同，先用测试复现 0.26.181 的字段丢失。
- [ ] P1：合并 Idle/Busy 的 `BottomChromeModel` 公共 assembler。
- [ ] P1：以有类型的提示模型取代 `hint_lines.size()==1` 分流。
- [ ] P1：以左右槽 `assist_row` 取代 `shortcut_rows`；右槽帮助入口与 skills 右边缘同列。
- [ ] P2：抽出 keymap 驱动的快捷键提示 formatter，删除写死 `?` 的流式兜底。
- [ ] P3：合并 Agent 面板两对完全相同的翻译 key 与选择分支。
- [x] P0：修 Hook 回合身份；先钉 UserPromptSubmit 的更新时序，再让 Hook/事件/trace/trajectory 共用一枚 turn id。
- [x] P1：把四套审批枚举收成公共值域与具名转换，先清全部跨枚举 `static_cast`。
- [ ] P1：建立统一原子写 API；先迁 Gateway 与持久事实账，再迁派生缓存。
- [ ] P1：WebFetch 改走公共外部文本清洗入口，删私有 UTF-8 解码器。
- [ ] P2：合并 SHA-256 与 Base64；算法迁移各自单独提交，不跟业务改造混装。
- [ ] 候选路径比较与环境变量读取先补同向量测试；没有行为同一性证据，不贸然抽象。
- [ ] UI、身份、权限、存储、编码、摘要六条线分提交推进；不得因同属“去重”绑成一只大改。

## 七、验收门槛

- [ ] 每个整改项先有能在旧代码上失败的回归，再改实现。
- [ ] 聚焦单测、全量 CTest 均通过；多配置生成器明确传 `-C Release`。
- [ ] `rg` 证明被淘汰的旧生产函数、翻译 key、重复分支与兼容声明没有孤尾。
- [ ] 真终端核对空闲、Working、排队输入、窄屏、矮屏、resize、切档说明到期七景。
- [ ] model/effort/cwd/branch/context/tokens 与 mode/skills 同时存在，不拿一行换另一行。
- [ ] 帮助入口改绑后，各场景显示真实键位；空闲与忙碌不换称呼、不换列。
- [ ] 非显示模块每项另留行为证据：同一输入只走一条 canonical path，旧路无调用、无双写、无双份落盘。
- [x] Hook payload 的 `turn_id` 可与 runtime event、trace、trajectory 直接等值联表；`hook_run_id` 只标一次 Hook 发射。（integration.hooks.hooks_turn_identity 钉五类 Hook 与事件流等值；trace/trajectory 吃同一枚 canonical 号,代码同源）
- [x] 审批档跨 CLI/Runtime/Agent/Session/Peer/Hook 往返不靠枚举整数。（unit.cli.approval_mode_boundary 表驱动五档乱序往返;resume/peer/hook/子代理各边全走 machine name 或具名桥,rg 无 static_cast 孤尾）
- [ ] 原子写故障注入证明正式文件不会先消失，并写清 crash durability 到底保到哪一层。
- [ ] 外来文本、SHA-256、Base64 各只剩一个算法内核；业务层保留薄适配，不复制循环。

## 八、提交边界

本次审计只提交本文档：

```text
todos/src重复职责与死生产路收口审计.todo
```

不改 `src/`，不改测试，不带入 `Testing/` 或工作区其他改动。本文档落账不等于缺陷已修；实现须另分提交，逐批验证。
