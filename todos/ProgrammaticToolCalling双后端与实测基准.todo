# Programmatic Tool Calling 双后端与实测基准

状态：待做（2026-08-16，依据 *The Bitter Lesson of Tool Calling*）

论文：https://arxiv.org/abs/2608.06370

## 先把名分定准

这里的 PTC 是 **Programmatic Tool Calling**，不是 Parallel Tool Calling。

现下 LubanCode 走原生 JSON Tool Calling：宿主发送 `tools[].function.parameters`，模型返回 `tool_calls[].function.arguments`，主循环逐项执行。即使一轮收到了多枚 `tool_calls`，仍属 JSON 路数。

PTC 要换一副骨架：

1. 宿主把可用工具编成带类型的 Python stubs。
2. 模型写 Python 脚本，调用这些 stubs。
3. 脚本可写变量、条件、循环、函数，也可用 `asyncio.gather` 扇出。
4. 运行时执行脚本；每次 stub 调用回到 LubanCode 工具宿主，取得真实结果。
5. 一段脚本收完，再把精简结果送回模型。长链不必每走一步都多开一轮推理。

只让模型在 `run_command` 里写 Python，不算 PTC。那样没有类型 stub、调用审计、权限桥、结果协议，也绕开现有工具栈。

## 为什么值得做

论文在 BFCL v4 上比较 14 个模型：11 个模型的 PTC 持平或胜过 JSON；长链越长，PTC 的优势越显；大规模 fan-out 下，JSON 会漏调用，PTC 靠循环和并发结构守得更稳。

LubanCode 正有三类合适现场：

- 遍历一批文件，做同构读取、搜索、抽取。
- 先查 A，再把 A 的结果喂给 B、C，形成长链。
- 同时派若干只读工具或子代理，收齐后聚合。

短任务未必划算。Python stub、运行时说明与脚本本身都有固定开销。因此不能一刀切，须保留 JSON 后端，实测后再让 `auto` 选。

## 产品形状

### 1. 三种调用档

```json
{
  "tool_calling": "json | programmatic | auto"
}
```

- `json`：保持现状。
- `programmatic`：强制走 PTC；模型或平台不支持时明报。
- `auto`：按模型能力、工具数、依赖深度、预估 fan-out 选择，并在状态栏写出本轮选择。

provider/模型目录可以给一条厂商提示，但提示不算验明正身：

```json
{
  "programmatic_tool_calling_hint": "supported",
  "ptc_language": "python",
  "ptc_max_script_bytes": 32768,
  "ptc_max_calls": 100,
  "ptc_max_parallel": 8
}
```

未知模型默认仍走 JSON。不能凭“会写代码”便暗开 PTC。

### 2. PTC 支持不是布尔值，须实测分档

PTC 没有统一 API capability。它靠提示词、typed stubs 与代码执行跑起来。许多代码模型都能试写，能否稳定写对却是另一回事。论文也撞见旧模型把多行脚本写成字面量 `\n`，一进子进程便语法错误。因此不能只存一枚 `supports_programmatic_tool_calling=true`，更不能按“Qwen/GPT/Claude”家族名一概放行。

先查五条硬条件：

1. 当前平台有可靠 Python sandbox。
2. 模型能输出自由代码，provider 没把正文锁成 JSON。
3. 上下文装得下当前 stub 集。
4. 入选工具已接 PTC RPC、权限、hooks 与取消链。
5. Python 运行时版本符合宿主声明。

少一条，直接走 JSON。模型会写 Python，而宿主没有 sandbox，也算不支持。

硬条件齐了，再跑无副作用探针：

1. **单调用**：按签名调用一枚 stub，参数与结果都对。
2. **真实依赖链**：拿第一枚工具的真实返回值，传给第二枚；不能拿常识猜中间结果。
3. **八路 fan-out**：用循环或 `asyncio.gather` 发齐八次，不漏、不重。
4. **异常收口**：一枚调用失败，其余调用与最终摘要仍有账。
5. **编码**：中文、emoji、反斜杠、引号、真实换行都能穿过脚本与 RPC。

每项连续跑数轮。偶然跑通一次，只算会试，不算支持。

能力账用画像，不用布尔值：

```json
{
  "ptc": {
    "status": "verified",
    "language": "python",
    "single_call_accuracy": 1.0,
    "chain_accuracy": 0.98,
    "max_verified_chain": 20,
    "fanout_accuracy": 1.0,
    "max_verified_fanout": 32,
    "runtime_error_rate": 0.01,
    "verified_at": "2026-08-16",
    "harness_revision": "ptc-v1"
  }
}
```

状态分四档：

- `unsupported`：硬条件不齐，或探针稳定失败。
- `unknown`：尚未测过；默认 JSON。
- `experimental`：基本能跑，只许用户显式强开。
- `verified`：完整基准过线，`auto` 才可选。

画像须绑定 `provider + endpoint + 精确 model id + 模型版本/量化 + chat template + PTC prompt/stub 版本`。任一项变了，旧画像降回 `unknown`，重新验。不能拿同名模型在另一端点的成绩作保。

运行时再放熔断器：连续出现语法错、漏调用、空脚本、RPC 协议错，便把本场从 PTC 降回 JSON。零调用发生时可自动重试；只读调用发生后应复用已有结果；副作用已经发生，严禁盲目重放。

### 3. 真正的 stub 模块

根据 `ToolDefinition` 生成只读 Python 模块，例如：

```python
from luban_tools import read_file, search

hits = search(mode="grep", path="src", pattern="HookDispatcher")
files = [read_file(path=item["path"]) for item in hits[:8]]
emit({"hits": hits, "files": files})
```

生成规则：

- JSON Schema 的 object/string/integer/boolean/array/enum 转成 Python 类型签名。
- docstring 留参数约束、权限等级、输出摘要；不把几页说明全塞进去。
- 工具名冲突时用稳定命名空间，不能临场改名。
- `tool_search` 延迟挂载仍生效。首屏只给索引；模型选中一组工具后，再生成这一组 stubs，免得 100 份 schema 淹进上下文。

### 4. 脚本不是直接摸宿主

Python 子进程只拿一条窄 RPC：

```text
stub call
  -> framed stdin/stdout RPC
  -> ToolRegistry
  -> PreToolUse
  -> PermissionRequest
  -> 工具执行
  -> PostToolUse
  -> 结构化结果回脚本
```

禁止脚本直接调用 LubanCode C++ 对象。子进程死了，宿主照旧活着；协议坏了，也能留下完整账。

## 安全边界

PTC 最大的甜头是代码，最大的险处也是代码。

1. 默认禁网络、禁任意文件系统、禁起新进程、禁环境变量、禁加载本机模块。脚本只能 import 宿主生成的 stub 模块与一小撮纯计算标准库。
2. Windows 用 Job Object、受限 token、进程树终止；Linux/macOS 另接各自 sandbox。没有可靠 sandbox 的平台，不得默认开启。
3. 每一枚 stub 调用仍走原有 schema 校验、permissions、deny_commands、hooks、超时、取消与审计。PTC 不是越权暗门。
4. 只读工具可并发。写文件、执行命令、插件副作用、项目记忆写入，默认串行；工具须显式声明 `parallel_safe` 才能进并发池。
5. 脚本设墙钟、CPU、内存、输出字节、调用数、并发数五道上限。撞线要指出是哪道墙。
6. `Esc` 先取消未开始调用，再打断在跑工具，最后杀脚本进程树。不能只灭 Python，留下子命令继续跑。

## 副作用与回退

PTC 脚本语法错、import 错、运行时错，可以考虑回退 JSON；可一旦某枚有副作用的工具已经成功，绝不可整轮盲重放。

每轮须留调用账：

```text
ptc_run_id
script_hash
call_id
tool_name
normalized_input_hash
started/finished
side_effect_class
result_hash
```

回退规矩：

- 零调用发生：可自动回 JSON 一次。
- 只读调用发生：可复用已有结果，再让模型补剩余步骤；不得重复烧接口。
- 副作用已发生：停下，列出已完成与未完成，交给模型或用户续办。

## 历史与压缩

别把整段脚本、每次 RPC 原文、每份大结果全塞回主历史。

历史分三层：

1. 语义层：脚本目的、调用 DAG、最终摘要，常驻上下文。
2. 审计层：脚本原文、每次调用入参与结果 hash，落会话存档，可展开。
3. 大结果层：落盘，只把稳定引用、头尾预览、大小与校验值放进历史。

compact 时按 `ptc_run_id` 把同一轮折成一条事件。相同只读调用按工具名、规范化参数、工作区版本去重；不能只靠文本 LSH 猜同一副作用。

## UI

流式区只画一张 PTC 卡：

```text
● programmatic_tool_calling（索引 36 份文件）
  ├─ 12 次 read_file   8 并发   3.2s
  ├─ 4 次 search       4 并发   1.1s
  └─ 完成              16/16
```

`Ctrl+O` 展开脚本与逐次调用。默认不把 30 行同构工具卡刷满屏。

状态栏须能看出：`tools json`、`tools ptc` 或 `tools auto→ptc`。失败时标语法错、sandbox 拒绝、工具拒绝、超时或部分完成，别只写“调用失败”。

## 与多智能体的关系

PTC 先做本地工具编排，不要第一版就让 Python 任意创建 agent team。

第二阶段再给受控 stub：

```python
tasks = await agents.gather([
    AgentTask(title="查 hooks", prompt="..."),
    AgentTask(title="查 memory", prompt="..."),
])
```

每只 agent 仍进统一 TaskRecord、后台队列与导航坞。标题、父子关系、权限来源、完成回流一项不少。PTC 只管“怎么编排”，不另造一套代理台账。

## 基准，先赢了再默认开

搭一套 LubanBench-Tool，JSON 与 PTC 同模型、同题、temperature 0，各跑至少五轮。

场景：

1. 单工具、短参数。
2. 2、5、10、20 步真依赖链。
3. 2、8、16、32、64、100 路独立 fan-out。
4. 128 份无关工具 schema 下找 1 至 3 个目标。
5. 大结果落盘与摘要。
6. 一半调用失败、超时、拒权。
7. 混合只读与副作用工具。
8. hooks、Lua、C ABI、MCP、LSP、Skills、多智能体交叉。

每题记：

- 任务成功率与漏调用率；
- 参数正确率；
- 模型请求轮数；
- 首结果与总耗时；
- 输入、输出、缓存命中 tokens；
- 工具重复调用数；
- sandbox/权限/hooks 覆盖率；
- 失败后的副作用重复数。

`auto` 的首版门槛可守些：预估链长 ≥4 或独立 fan-out ≥8，模型能力画像为 `verified`，当前画像指纹未过期，且所有入选工具都可进 PTC，才选 programmatic。厂商目录只写“支持”而本机没跑过探针，仍不得自动启用。数据出来后再调，不靠拍脑袋。

## 分期

### P0：离线原型

- ToolDefinition → Python stub 生成器。
- 受限 runner，只接假工具。
- 跑论文式 BFCL 子集与仓库自建题。

### P1：只读真工具

- 接 `read_file/search/glob/list`。
- 串上 hooks、取消、超时、审计。
- UI 单卡与展开。

### P2：并发与动态链

- `asyncio.gather`。
- 结果回脚本，支持真实依赖链。
- 大结果落盘、去重与 compact。

### P3：副作用与多智能体

- 写工具的事务账与不可盲回退。
- 受控 agent stubs。
- `auto` 选择器。

## 完成标准

- PTC 不是 prompt 约定，而有 typed stubs、受限 runner、RPC、审计四件实物。
- 每枚调用都过 schema、权限与 hooks；Lua、C ABI、MCP 也不漏。
- 20 步依赖链比 JSON 少至少一半模型轮数，结果正确率不降。
- 32 路 fan-out 无漏调用，且只读工具并发受 `ptc_max_parallel` 约束。
- 脚本失败不会重复副作用；部分完成可恢复。
- JSON 后端始终可选。`auto` 的每次选择都有理由、有指标、可复盘。
- Windows、Linux、macOS 都有 sandbox 与取消测试；缺 sandbox 的平台默认禁用 PTC。
