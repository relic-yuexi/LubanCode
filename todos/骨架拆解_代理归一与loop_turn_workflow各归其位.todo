# 骨架拆解：代理归一与 loop/turn/workflow 各归其位

状态：待做（2026-08-26，用户以架构视角通读 src/ 定调：开发日久，聚合过度、
重复渐多。mainagent 与 subagent 本是一个 agent，只该差 prompt 与 tools；
loop 是 loop，turn 是 turn，workflow 是 workflow；分解推崇 PocketFlow——
workflow 是定好步数的图，loop 是带退出条件的 turn 串。四路探子解剖完毕
（interactive_session、循环三家、API 四后端、主/子装配），本单收账成文）

## 一句话

引擎只有一台，壳有五尊，另有两条绕开正门的暗道。`Agent::Run` 加
`RunOneTool` 这副内骨架全仓库独一份，五个装配点全走它——底盘是好的。
坏在外壳：turn harness、回调装配、续跑环、取消链、预算/退避/账本/回放，
在 turn_runner、agent_tool、loop_scheduler、goal_coordinator、
workflow/runtime 各养各的。拆法借 PocketFlow 钉住概念：消息块是原子，
step 是 Node，turn 是小 Flow，loop 与 workflow 同一具图壳——一个拿退出
条件走路，一个拿定好的图走路。agent 不分种类，只换皮；皮是一份
AgentProfile；壳上的横切设施只写一份。

## 词汇表（本单定案，后人不再各执一词）

| 词 | 定义 | 对应现状 |
|---|---|---|
| 块 block | system / user / assistant / tool_use / tool_result 五种内容块。一切引擎只产块消块，不碰 wire | api::types 的 ContentBlock variant（types.hpp:70），已是 |
| 步 step | 一次模型请求 → 一条 assistant 消息（可带 ToolUseBlock）→ 工具批执行 → tool_result 回填。Node 三段：prep 拼请求，exec 模型流加工具批，post 看 stop_reason 路由 | 活在 AgentLoop::Run 的 for 循环体里（loop.cpp:666 起），有实无名 |
| 回合 turn | 见下数式。一次 `Agent::Run()` | loop.cpp:589；收口判据 RunOutcome（loop.hpp:300） |
| 循环 loop | turn 的重复，外加退出条件。定时拍、goal 判定都是 loop 的策略 | 外壳三份之一 |
| 工作流 workflow | 定好步数的图，节点是 step / turn / 子 agent | workflow/runtime.cpp 图游标（runtime.cpp:963） |
| 代理 agent | 一台引擎加一张皮。皮 = AgentProfile（provider / request / runtime / prompt / tools / sink）。main、Explore、general-purpose、单发、app-server、workflow 节点，全是皮 | AgentProfile 已立（loop.hpp:402），尚缺 tools 与 sink 两字段 |

turn 的数式（钉死，各家实现以此对账）：

```text
一次用户 turn
= 1 条本轮接纳的 user 消息
+ M 个 step（M >= 1）
+ M 次模型请求
+ 0 到若干次本地工具执行
+ M 条 assistant 消息
  └─ 最后一条不含 ToolUseBlock，是交给用户的最终回复
```

退出另有三种非常规收口：ESC 打断（cancelled，不是错误）、输出预算尽
（length 续跑用完仍无正文）、步数满（max_steps_per_turn > 0 才有）。
RunOutcome 三旗已经写对，保持。

## 已翻过的代码（证据账）

### 一、做对了的（不动清单）

| 东西 | 坐标 | 为什么不动 |
|---|---|---|
| api::Backend | backend.hpp，30 行 | 一个 send_stream 了事，干净 |
| 内循环唯一 | AgentLoop::Run（loop.cpp:589）+ RunOneTool（loop.cpp:41） | 五处装配点全走它：interactive_session:2015、agent_tool:1233、one_shot:255、app_server:834、host_executors:103。PTC 单工具链也走 RunOneTool（ptc_tool.cpp:250）。没人另造轮子 |
| AgentProfile / AgentRuntimeProfile | loop.hpp:402、runtime_profile.hpp | "换皮不换核"的种子已下；main→sub 派生已通（interactive_session.cpp:2018-2020），头注释明言"不靠继承分叉" |
| prompts/ 目录 | core/features/modes/platforms/tools 五层 | 模块化、用户覆盖、嵌入兜底，一条路走到底。子代理人格也走 prompts/tools/<lang>/agent.md 的 persona.* 节查表 |
| workflow/definition.hpp | 强类型 AST | 十八种节点、join 策略、幂等键、内容 hash，考究 |
| EventSink 合同 | runtime/event_sink.hpp + TurnEventAdapter（turn_event_adapter.hpp） | "一份事件，多路消费"的图纸已画；接线到第六步停了，本单批二接着干 |
| session_work_scheduler | session_work_scheduler.cpp:18-50 | loop 拍与 goal 续轮公平仲裁，对路 |
| PTC 工具链 | ptc_tool.cpp:250 复用 RunOneTool | 沙箱归沙箱，链路走正门。旁路治理的正面样板 |

### 二、病灶（按疼的程度排）

**病一·turn harness 两份。** 主会话一份（turn_runner.cpp:980-1547），
子代理一份（agent_tool.cpp:1137-2048，920 行）：自带 for(;;) 续投外环
（:1797）、自带 Stop 钩子续跑（:1883-1917，与 turn_runner.cpp:1445-1476
同一模式两份代码）、自带三处自旋等待（:1647-1688）、自带收场分型
TaskOutcome（:1919-2019）。这正是"main 与 sub 是同一个 agent"的病灶
本体：引擎合了，鞍辔各配一副。

**病二·暗道两条，绕开正门。** workflow 的 ToolExecutor 直捅
tool->execute()（host_executors.cpp:45）——PreToolUse/PostToolUse 钩子、
权限确认、trace、schema 复检全没有，只剩自己一个 confirm gate
（:33-44）。LlmExecutor 裸调 send_stream（host_executors.cpp:181-233），
无钩子无账。这不止是重复，是审计与安全的缺口：同仓库同时存在两条工具
执行路，一条全监管，一条裸奔。且执行器层本身就是两条装配路：
AgentExecutor（:103）走 agent::Agent 全装，LlmExecutor（:181）手拼
Request 直发，SkillExecutor（:327）又包着 LlmExecutor——同一个"调一次
模型"，装了两遍。

**病三·外壳三份。** loop_scheduler（930 行）、goal_coordinator（1287 行
+ evaluator）、workflow/runtime（1271 行）互不相认，各自实现同一套横切
设施：

| 外壳 | 主循环 | 预算闸 | 重试/退避 | 事件账+回放 |
|---|---|---|---|---|
| loop_scheduler | PumpDueTick :406 | 间隔上下限 :123、连败五拍自停 :546 | 每拍两次退避 :576 | loop_tick_v1 + ReplayEvent :808 |
| goal_coordinator | 会话泵 PumpGoalContinuation（interactive_session:4664） | iterations/elapsed/tokens :891、防空转 :832/:863 | evaluator 两次 schema 修复（goal_evaluator.cpp:248） | LedgerSink + ReplayEvent :993 |
| workflow/runtime | 图游标 :963 | max_steps :970、tool_calls/tokens :1072、timeout :1079 | 节点级指数退避 :288-389 | RunJournal + checkpoint + Resume :831 |

三家的"要不要再跑一轮、跑多久停、败了怎么办"各写各的，规矩还各不相同。

**病四·裸 send_stream 六处。** goal_evaluator.cpp:248、
host_executors.cpp:181（Llm 节点）、session_title.cpp:166、
compact.cpp:172/833、microcompact.cpp:140、memory_extract.cpp:218——
全是"无历史、无工具、单发采样"这一个需求，各自攒流、各自算 usage、
各自兜错。缺一个 SampleModel 原语。

**病五·Callbacks 肥结构，六处装配。** 25 个以上 std::function 挤一只
结构（loop.hpp:87-265），六处各挑各的子集：turn_runner.cpp:559、
agent_tool.cpp:1336、app_server:836、turn_event_adapter.hpp:71、
host_executors.hpp:76、ptc_tool.cpp:229。漏配一个字段就静默丢账。
loop.hpp:204 自己写了正解："一份事件，两路消费……不许 AgentLoop 同时
手调三份写口"。图纸有了，照着干完。

**病六·Agent 类过载，且仍住 loop.hpp。** 一只类握着：身份、预算策略、
双历史（history_ + request_history_，loop.hpp:609-610）、前缀指纹与
cache epoch（:614-616）、sticky 视图（:624）、结构压缩开关与台账
（:605-607）、artifact 仓（:608）、工具可见性（:627-628）、inbox
（:629）、追踪发号（:634-636）。上下文管理一个概念长了六七只手。该拆
一只 ContextManager 出去，Agent 只留身份、皮、历史句柄。loop.hpp 整本
也是杂货铺：Agent、AgentLoop、Callbacks 25 字段、审批四态
（ApprovalRequest/Decision/Response）、ToolHookDecision、ToolPhase、
OutputBudgetReport、RunOutcome、ContextPressure、ToolTraceContext 挤
一头——Agent 拆出去自立门户时，这些类型各归各家（审批归 runtime/
interaction、trace 归 tool_trace、压力归 context）。

**病七·API 四家两成半纯重复。** anthropic/chat/gemini/responses 共
2891 行，实测约 650-750 行可消（22%-26%）：cpr 传输骨架四份（各家
client.cpp 约 190 行的 flags/Header/Write/Progress 五连）、
ExtractStatusCode ×4、ClassifyNetworkError ×4、dump 兜底 ×4、
RoleToString ×3 加字面量散落一份、extra_body 合并尾 ×4、thinking 开关
块两份逐字节相同。responses/client.cpp:26-27 注释明写"不为共用几行
代码另起公共头"——当年两份小巧，如今四份过临界，这条局部决定该翻篇。
最大收益恰在传输层（约 500 行），不在 JSON 翻译本身。

**病八·大佛 interactive_session。** 6524 行、145 只 include、约百个
数据成员、60 个方法、构造函数 495 行（:788-1282）兼作组合根。十五类
职责：回合驱动、47 分支 slash 分发（:3153-4025 一个 switch 873 行，
/model 一案 152 行含 HTTP 调用、/trace 166 行）、子代理面板渲染
（约 650 行）、transcript 键盘导航（:1688-1962）、goal 子系统（约 600
行，CloseGoalIteration 单函数 202 行）、loop 子系统（约 450 行）、
compact、记忆（HandleMemoryCommand 单函数 387 行）、存档恢复、peer
通信、Plan 模式（约 420 行）、杂项命令、后台小模型活、hooks 发射、
后台代理清理。三家具体 wire client（:53-57）、安装向导（:106-108）、
升级检查（:123）的 include 全进了会话层——hpp:9 宣称的窄门只在 hpp
成立。

**病九·双胞胎函数。** RunUserTurn/RunPeerTurn（:5027/:2202，"发一轮"
的两份近似）；PumpLoopTicks/PumpGoalContinuation（:4567/:4632，"拼
合成提示开 turn"的两条平行路）；子代理接线（SetPromptsDir/
SetRuntimeProfile/SetTurnContextProvider 那一束）在
interactive_session.cpp:948-972 与 one_shot.cpp:195-217 平行维护。

**病十·隐性分叉（比重复更阴）。** 子代理提示词走 BuildSystemPrompt
薄壳（prompts.hpp:78-89），mcp/web/lsp/platforms 四个开关不传——子代理
天生缺这几段，主代理有；无文档说这是设计还是疏漏。app-server 更彻底：
不走 AssembleSystemPrompt、不走 ToolRuntime，system_prompt 一句字面量
"lubancode app-server"，max_steps 硬编 32（server.cpp:832-834）。
皮与皮的差别没写进 profile，散落在装配代码的省略里——省略出来的差别，
迟早变成对不上的行为。

### 三、复核补入（2026-08-26 用户逐条复核后加账）

**病十一·profile 自己先打架。** 三笔账：
其一，model 字段两处档案各存一份——RequestProfile.model
（types.hpp:165）与 AgentRuntimeProfile.model（runtime_profile.hpp:78），
靠 SetRequestProfile 里一句手工 if 同步（loop.hpp:454-459：
`if (!request_profile_.model.empty()) profile_.model = ...`）。请求整形
归 RequestProfile，运行档案只管预算，model 该只留一份。
其二，provider 字符串与 backend 引用分开握，无绑定约束——SetProvider
只改字符串不动 backend；RebuildableBackend 的"切 provider 不换引用"
（backend_stack.hpp:33-40）正是缺绑定的代偿；ThinkOverrideBackend 还另
收 current_provider 指针，第三条旁路。
其三，五层请求改写后端（ModelOverride/ThinkOverride/
ModelInstructions/SoulOverlay/DeferredIndex，backend_stack.hpp:57-158）
与 AgentProfile 职责重叠。根由 backend_stack.hpp:52-55 注释自己招了：
"AgentLoop 的 model 是构造时定死的私有成员，没有 setter（**agent 层
现有文件不让动，详见任务规矩**），这层包装是唯一办法"——五样会话级
请求策略（model、effort、模型指令、魂、延迟索引）当年为不碰 agent
文件，全走 shared_ptr<string> 旁路挂上了传输层。皮收口时这五层整体
退役，归 RequestProfile 管道；SpinnerBackend 是 UI 混进传输层，挪去
sink 侧。

**病十二·Agent 的门太多。** 注释写"profile 构造时定死"，现实是三只
构造函数（profile 正门、runtime+prompt 兼容门、裸参门，loop.hpp:426-449）
加十二个 setter（SetProvider/SetRequestProfile/SetSystemPrompt/
SetToolFilter/SetToolFilterDenial/SetInbox/SetTurnContext/
SetContextWindowTokens/SetOnContextPressure/
SetStructuralCompressionEnabled+Options/SetArtifactStore/
SetExecutionIdIssuer）。装配散在十几扇门，"单一来源"名存实亡。收 profile
要连门一起收：正门留 profile，兼容门逐批封，环境类接线（inbox、压力
钩、发号器）收进一只 AgentWiring。

**病十三·工具策略与工具实例未分家。** main/sub 两张全能力表并行全量
重建（tool_runtime.cpp:273-274），explore 第三张（:271）；MCP/LSP/插件/
todo 各自往两张表各挂一遍。可见性策略又分两层——registry 差异件一层
（agent 只挂主表、子表挂转发壳），loop 级 SetToolFilter 又一层。该走的
路：一份实例仓（工具只建一份），策略层（皮里的 tools 可见性谓词+
拒绝文案）按代理声明。AgentProfile 补 tools 字段正是为此。

**病十四·AgentTool 身兼六职。** 创建（execute 分岔前后台）、调度
（并发槽/深度 RAII，agent_tool.cpp:1144-1163）、线程（三信号合并
:1636-1662 加墙钟看门狗 :1670-1716）、台账（TaskRecord 事件账
:2158 起）、消息（inbox 续投 SealOrContinueInbox :2276）、隔离
（worktree 包装表 :275-318）。RunTask 一只 920 行。harness 合流（批三）
抽走循环与回调后，剩下的按六职拆：SubagentScheduler（槽与深度）、
TaskLedger（台账）、IsolationScope（隔离），AgentTool 只留工具门面。

**病十五·src/agent/ 在长成第二尊佛。** loop/compact/context/prefix/
runtime_profile/prompt_assembler 是引擎件；可 peer_mailbox/
peer_registry/peer_session、goal_session、session_store/session_catalog/
session_lifecycle、skill_drafter、workflow_recorder、model_router 也全
住引擎屋。agent/ 该只装引擎（Agent、Loop、Profile、Context、Prompt），
会话域、peer 域、目标域、技能域的物件搬 runtime/ 或自立门户——模块
边界与类边界一样要守。

**病十六·台账方言五种。** session JSONL（session_store）、tool_trace/
tool_trace_hub、loop 的 loop_task_v1/loop_tick_v1、goal 的 LedgerSink、
workflow 的 RunJournal+checkpoint——五套事件模型、五套回放规矩，id 与
时间戳口径各定各的。批五收外壳底座时连方言一起收：至少 id 同源
（IdAuthority）、回放接口统一，各域的事件载荷可以保留。

## 目标骨架

```text
层四  代理皮   AgentProfile{provider, request, runtime, prompt, tools, sink}
               main / Explore / general / 单发 / app-server / workflow 节点
               一处 AgentFactory 装配，五处装配点收敛
层三  图壳     一具 Flow：while (curr) { curr = next(curr.run(shared)) }
               loop = 图成环，post 回一条退出边；workflow = 定好的图
               横切设施只写一份：预算闸 / 退避 / 事件账 / 回放 / 取消
层二  turn     一条 user 消息进，末条无 ToolUseBlock 的 assistant 出
               （外加打断 / 预算尽 / 步数满三种非常规收口）
层一  step     Node：prep 拼请求 → exec 模型流+工具批 → post 路由
层〇  块       system / user / assistant / tool_use / tool_result
```

PocketFlow 对照：Node 三段即 step；Flow 自己就是 Node，故子代理嵌套
白送——agent 工具的 exec 里跑一整只内层 Flow；循环不设专门构造，post
回一条指向旧节点的边，图成环，loop 就出来了。仓库照抄这层**关系**，
不抄那 100 行 Python——C++ 侧要的是概念归位，不是行数比赛。

落地不要求把 loop_scheduler、goal_coordinator、workflow/runtime 揉成
一个类。三者语义真不同：一个管节拍，一个管判定，一个管图。要求的是
图壳之下横切设施只写一份，三外壳降为三枚策略（CadencePolicy /
GoalPolicy / 图遍历），都调同一台 turn 引擎、同一条 RunOneTool 链、
同一个 SampleModel。

代理归一的路数：AgentProfile 补齐 tools（可见性谓词+拒绝文案）与
sink 两字段；五处装配点收敛到一处 AgentFactory；turn harness 合成一
份，渲染策略三档可换——终端画、静默（查看态回流单那套）、事件流。
app-server 裸奔与子代理缺段，靠"皮上显式声明"一并治好：皮上写不出
的差别，就是还没想清的差别。

## 施工批次（一批一版本，验收各自独立）

| 批 | 干什么 | 动哪里 | 验收 |
|---|---|---|---|
| 一 | 封暗道 | ToolExecutor 改走 RunOneTool；LlmExecutor 与另五处裸调收敛到新原语 SampleModel（一次调用、无历史、可选 output_schema、usage 统一报），与 ModelRouterService 合成一门"小模型活"服务（Route 加 Sample 一站，compact/title/memory/evaluator/Llm 节点全走它） | workflow 工具节点过钩子账、trace 落账；六处采样行为不变，usage 对得上 |
| 二 | 事件流升正房 | Callbacks 退役，TurnEventAdapter 成唯一出水口；六处装配点变六处 sink 配置；BuildCallbacks 删；SpinnerBackend 同批挪出传输层 | 同流验收：一份假 backend 喂终端与 JSON 两 sink，id、次序、终态、领域数据一致 |
| 三 | harness 合流 | turn_runner 与 agent_tool RunTask 合成一份 TurnHarness（RunTurn 二十四参、BuildCallbacks 十五参收成一只 TurnContext）；渲染三档；Stop 续跑环、取消链、收场分型各留一份；AgentTool 剩件按六职拆 SubagentScheduler/TaskLedger/IsolationScope。手头正在修的 agent.md / agent_tool 改动与此同路，直接并入 | 子代理四幕（agent_stream_driver 真机）与主回合不劣化；后台确认策略照旧 |
| 四 | Agent 瘦身与皮收口 | ContextManager 拆出（双历史/压缩/sticky/epoch/artifact/memo）；Agent 只留身份+皮+历史句柄；model 归 RequestProfile 单份；provider/backend 绑定收紧（RebuildableBackend 收编）；五层 override 后端退役，归 RequestProfile 管道；三只构造门收敛、setter 分流（策略进皮、接线进 AgentWiring）；Agent 出 loop.hpp 自立门户，loop.hpp 杂货各归各家 | /context 数字不变；前缀缓存命中不降（cache_epoch 账照走）；/model、/think、/soul、tool_search 即时生效照旧 |
| 五 | 外壳底座与台账归一 | 预算/退避/账本/回放抽一份；三外壳降策略；五种台账方言收 id 同源（IdAuthority）与统一回放接口，事件载荷各域保留 | loop、goal、workflow 三处 ReplayEvent 各自照旧能恢复 |
| 六 | API 传输合流 | cpr 骨架抽一份 HttpStreamTransport；四家剩 url 加 JSON 翻译钩子；ExtractStatusCode 等小件归一；顺手把 responses/client.cpp:26 那条注释翻篇 | 四家 wire 请求逐字节对比不变（既有单测钉着）；gemini 的 x-goog-api-key 特例保留 |
| 七 | 大佛开刀与模块归位 | interactive_session 按子系统外迁：goal/loop/plan/memory/compact/panel 各成文件；47 案肥 case 外派各领域 handler；组合根留 cli_app；死 include 清走；src/agent/ 的会话域、peer 域、目标域、技能域物件外迁，agent/ 只剩引擎 | 斜杠命令 47 条行为逐一照旧；hpp 窄门在 cpp 也成立；agent/ 目录只剩引擎件 |

批序有讲究：一、二是地基，安全洞先堵、事件流先通；三、四是结构大
手术，站前两批肩上；七最重也最后，依赖批二。六独立，可与三、四、五
worktree 并行。按仓库节奏一批一 patch，不攒大版本。

## 风险与留神

- 每动一步先钉测试。ctest 已全 stub 化，裸跑即绿；真机抽验用临时
  USERPROFILE，别碰本机配置。批三动子代理，cpr 驱动四幕必跑。
- 行为不变是硬规矩。迁移期两轨并行（BuildCallbacks 老路与事件流新路）
  正是显示剥离单的既定打法，照旧沿用，新路验收过了再拆旧路。
- 病十下刀前先问一句：子代理缺 web/mcp/lsp/platforms 段，是无意还是
  有人靠它省 token？批三合流时把这段差别写进皮里——留就写明留，补就
  写明补，不许继续悬着。
- 病七别贪。JSON 翻译里各家形状差异（system/instructions/
  systemInstruction、四种工具格式、usage 摊账口径）是协议本性，不是
  重复，不强行归一；归一的只有传输骨架与纯函数小件。
