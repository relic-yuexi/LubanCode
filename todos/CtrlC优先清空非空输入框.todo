# Ctrl+C 优先清空非空输入框

状态：待核验并补齐（小交互）

## 要的手感

输入框里已有草稿时，按一下 `Ctrl+C`：

- 清空整只输入框，包括多行内容。
- 光标回到提示符后头。
- slash 提示、补全候选、软换行续行一并收净。
- 留在当前会话继续输入，不提交、不退出、不打断正在跑的代理。

输入框已经空了，`Ctrl+C` 才沿用所在场景原义：

- 空闲主 composer：按既定退出/EOF 规矩处理。
- 流式 footer：打断当前轮；短时间双击仍可强制退出。
- 阻塞确认菜单：仍按取消菜单处理。

核心规矩只有一句：**有字先清字，没字再中断。**

## 眼下已有半截

`LineEditorCore` 已实现：

- 非空单行 `Ctrl+C`：`ClearBuffer()`，返回 `cleared=true`。
- 非空多行 `Ctrl+C`：整段清空，不只清当前物理行。
- 空行 `Ctrl+C`：返回 `eof_requested=true`。

也已有 doctest 与 Windows screen driver 覆盖基本空闲 composer。若用户实际按下没反应，先查：

1. 运行的是不是安装目录里的旧 `lubancode.exe`，而非当前 Release build。
2. Windows Terminal、VS Code Terminal、conhost 有没有截走 `Ctrl+C` 当复制。
3. 输入焦点是否在 stream footer、排队消息编辑、subagent composer 或菜单；这些路不全走 `LineEditorCore`。
4. 重画是否清了内存，却没擦干净屏上旧物理行。

不能看见已有测试便把单子关掉。真机没吃到，就得沿实际按键链查到断处。

## 需求

### 1. 空闲 composer

- 单行、多行、中文、粘贴文本、光标在中间，`Ctrl+C` 都整段清空。
- 清空不写进历史，不留下可用 Up 翻回的“已提交消息”。
- agent 面板焦点、transcript 焦点、slash 候选先退出相应焦点态，再清草稿；不能误停 subagent。
- 当前目标若是某只 subagent，只清给它写的草稿；右上角目标标签与 subagent 查看态仍保留。

### 2. 流式 footer

眼下 `TurnInputListener` 看见 `Ctrl+C` 便打断，没先问输入 buffer 是否有字。改成：

```text
footer 草稿非空 -> 清空草稿，cancel_flag 不动
footer 草稿为空 -> 打断当前轮
```

- 已落队消息不受影响，只清正在输入、尚未 Enter 的那一条。
- 正在取回编辑排队消息时，`Ctrl+C` 只清编辑框。原消息是否暂存、Esc 是否还原，按排队编辑事务规矩办。
- 清空后 1.2 秒内再按 `Ctrl+C`，此时输入为空，可按既有“打断/双击退出”状态机处理；第一下清草稿本身不可计作强制退出第一击，免得用户清字后再按一下便误退进程。

### 3. 子代理介入 composer

- 给 subagent 写了一半，`Ctrl+C` 只清草稿，不发送、不取消该任务。
- 输入框为空再按，才执行该界面明写的返回/停止语义。
- main 与各 subagent 草稿若分目标保存，只清当前目标草稿，不波及别处。

### 4. 画面

- 软换行从 N 行缩回一行，下横线、状态栏、代理面板与队列区随帧账上移。
- 不打印 `^C`，不新添“已清空”滚动日志。
- 清空后 slash 空输入提示按既定规则恢复；旧候选不得残留。
- plain、ANSI、窄终端都不留半行旧字。

## 验收

- 空闲 composer 输入一行，`Ctrl+C` 后框仍在、内容为空、进程仍活。
- 输入五行中文并把光标挪到第二行，`Ctrl+C` 后整段清空，光标回首行。
- 流式期间敲半句但不 Enter，按 `Ctrl+C`：半句消失，模型继续输出；再在空框按一次，当前轮才打断。
- 已排三条、正在敲第四条，`Ctrl+C` 只清第四条，前三条次序与目标不变。
- subagent 查看态里清草稿，任务仍 running，右上角短标题仍在。
- 空 composer 的退出、流式空框打断、双击强退、菜单取消均不回归。
- Windows Terminal、VS Code Terminal、conhost 与 POSIX 真终端各手测一次。

## 测试

- 扩充 `LineEditorCore`：单行、多行、中文、光标中间、历史浏览态。
- 扩充 `PendingQueueCore`/排队编辑：草稿清空不删已落队消息。
- 给 `TurnInputListener` 做可控按键测试：非空 Ctrl+C 不置 cancel，空 Ctrl+C 才置；清草稿那一下不参与 double-tap 退出计时。
- 扩充 Windows screen driver：断言旧续行、候选、状态框位置全收净，进程仍存活。
- smoke test 分别跑 build 目录与安装目录 executable，防止“代码有了、用户跑的还是旧版”。

## 不做

- 不把 `Ctrl+C` 改成任何时候都退出。
- 不让清草稿顺手清掉已排消息、历史或 subagent 任务。
- 不用 `Esc` 代替；两枚键可有相近处，既有取消与返回语义仍各守本分。
- 不因当前空闲 composer 已有实现便忽略 stream footer 与真机按键链。
