# LubanCode 上层应用方向：场景组合与独立项目候选

- 日期：2026-09-13。
- 状态：应用方向草案，待选择、访谈和验证；不是全部项目的开工清单。
- 用户意图：把 LubanCode 当基建，单列可承载的应用，例如 Agentic RAG、IM 机器人、多租户 Agent、导购机器人，并继续展开其他方向。
- 与基建分开：[工业化多协议接入、能力裁剪与工具扩展](工业化多协议接入_能力裁剪与工具扩展统一设计.todo) 只管协议和引擎接入；本文件只管上层应用、组合方式、最小业务闭环与选题验证。
- 相邻依赖：[多租户隔离](多租户隔离_身份授权与隔离Worker落地设计.todo)、[多渠道接入](多渠道消息接入与常驻ChannelPlugin设计.todo)。它们是基础能力设计，不是现成 SaaS 或已完成的平台接入证明。
- 本轮未创建新项目，未实现应用，未接真实账号/客户数据，未调研市场规模或商业回报。下文价值、优先级与难度均为设计判断，须用真实任务验证。

## 一、先定产品与基建的边界

LubanCode 提供模型循环、工具调用、上下文、运行记录、取消和能力边界。上层应用提供真实数据、领域工具、业务规则、交互界面和验收办法。两边接好，才有可交付产品。

不应每做一个应用就复制 AgentLoop、另建工具权限和执行账；也不应把所有行业字段、订单规则和报表模板塞回 LubanCode。

```text
LubanCode 基建
  ├─执行、工具、会话、预算、恢复、协议
  └─上层应用
      ├─业务对象：文档、商品、工单、客户、任务……
      ├─领域工具：查库存、读条款、查日志、提交工单……
      ├─业务规则：版本、权限、状态流转、审批……
      ├─产品入口：Web、IM、桌面、API……
      └─业务验收：答得对、办得成、成本和耗时合适
```

基建当前缺口沿基建单补齐，尤其生产 app-server 工具/Profile 装配、可靠受理与共同工具授权。应用原型可以使用假数据和测试工具先验交互，但不能因此宣称生产链、恢复或隔离已经完成。

## 二、这些名称不在同一层

| 层次 | 回答什么问题 | 可选项 |
| --- | --- | --- |
| 业务任务 | 用户究竟要办什么事 | 查资料、选商品、处理售后、排障、核对单据、跟进客户 |
| 完成任务的方法 | 如何拿证据、做判断、执行 | 固定检索、Agentic RAG、结构化查询、规则流、受控 Agent 工具循环 |
| 交互入口 | 用户从哪里来 | Web、IM、桌面、业务 API、定时任务 |
| 托管形态 | 谁用，怎么部署和隔离 | 单人本地、企业私有部署、团队共享、多租户 SaaS |

因此：

- Agentic RAG 是检索与推理方式，可以落成独立知识助手，也可藏在导购、客服或排障内部。
- IM 机器人是产品入口形态。只有“能聊天”还不够，要指定它替谁办什么事。
- 多租户 Agent 是托管平台形态。可承载多家客户的客服、知识助手或其他应用。
- 导购机器人有明确业务任务：理解需求、查真实商品、解释取舍、把用户带到下一步。

例子：“多租户 IM 导购助手”可以同时具有上述三种形态。不要分别做三个仓库，只因它有三个名字。

## 三、管理系统哪些相同，哪些不同

“很多管理系统都是那些东西”，这判断有一半成立。账号、列表、表单、检索、导入导出、状态、权限、审批、通知和审计，确实能复用。

但业务真正难的地方，常藏在字段与状态后面：库存能不能扣、退款能不能退、这张单据是否算重复、谁能看这个客户、合同采用哪版规则。界面相似，不代表这些规则可以省略。

| 可以共用 | 应留在具体业务 |
| --- | --- |
| 登录、会话、任务、事件、执行记录 | 商品库存、退款资格、工单责任、客户归属 |
| 工具注册、权限检查、调用和错误 | 业务 API、字段含义、有效状态流转 |
| 文件/消息接入与引用框架 | 文档类型、来源质量、版本生效规则 |
| 人工确认和结果展示组件 | 什么动作须批准，谁有资格批准 |
| 评测运行器、成本/延迟统计 | 正确答案、合格结果、业务损失与收益 |

Agent 适合处理自然语言、跨来源查证、材料不齐时追问、路径不固定的多步任务。已经明确的计算、字段校验、数据库事务和固定审批规则，继续用普通代码。不能让模型凭一句话绕过业务系统本来就有的约束。

衡量产品时问：“少翻了多少页面，少查了几次资料，哪件事终于办完了？”不要只看是否多了一只聊天框。

## 四、候选应用总表

以下按任务划分，不按协议划分。优先级表示验证顺序，不表示市场结论。

| 编号 | 应用方向 | 最小可交付结果 | 适合的首轮边界 |
| --- | --- | --- | --- |
| A01 | Agentic RAG 知识助手 | 带可核验出处的答案 | 单一资料域，只读 |
| A02 | IM 工作助手 | 在会话里完成一次查询或交办 | 一个渠道、一类任务 |
| A03 | 多租户 Agent 托管台 | 两家租户各运行一款受控 Agent | 固定模板，先不开放任意代码 |
| A04 | 导购/选型助手 | 符合约束的候选与差异说明 | 单品类，查真实价格与库存 |
| A05 | 售后与工单助手 | 查明问题、整理材料、生成工单草稿 | 查询和草稿，不自动退款 |
| A06 | 技术排障助手 | 证据链、原因假设和验证步骤 | 只读日志/文档/配置 |
| A07 | 文档核对与材料审阅 | 差异、缺项、规则命中及出处 | 固定文档类型与明确规则 |
| A08 | 数据问答与经营查询 | 可追溯的数据答案和查询条件 | 单数据域、只读语义层 |
| A09 | 客户跟进/业务助理 | 客户摘要、待办与跟进草稿 | 内部准备，人工决定外发 |
| A10 | 研究与信息监测助手 | 带来源的调查简报或变化提醒 | 有边界的主题与来源集合 |
| A11 | 学习与培训助手 | 根据材料和作答证据安排下一步 | 单课程、明确知识与评分规则 |
| A12 | 受控流程办理助手 | 将口语需求转成合法流程草稿并执行获准步骤 | 一个有状态机的业务流程 |
| A13 | 3D 资产制作助手 | 可编辑模型、预览和规格检查报告 | 单个静态道具，固定格式与规格 |
| A14 | 游戏原型制作助手 | 可启动、可游玩的短流程与源码 | 单机小原型，一种核心玩法 |
| A15 | 长篇小说创作工作台 | 连贯章节、设定账与修订记录 | 一个故事弧，先验连续几章 |
| A16 | LubanCode 桌面工作台 | 可安装客户端内完成会话、文件、工具执行与产物查看 | 先做单项目任务、受控工具、文件与改动查看 |

这些候选可以合并：A01＋A02 是 IM 知识助手；A04＋A05 是购前购后服务；A01＋A06 是技术支持助手；A03 可以托管其中已验证的模板。

## 五、候选应用卡片

### A01：Agentic RAG 知识助手

用户交代：“这两个版本有什么区别？这项规定适不适用于我？”系统检索、读原文、核对版本，证据不足时补查或追问，最后带出处作答。

- 目标用户：需要频繁查手册、内部文档、技术资料的团队；具体首批用户待确定。
- 最小闭环：收问题 → 搜索 → 读相关段落 → 必要时补查 → 答案与原文定位。
- 业务资产：文档、版本、生效日期、访问范围、片段定位；切分和索引归应用服务。
- 领域工具：search、read_span；重排可以先藏在检索服务内部。
- Agent 增益假设：跨文档、多跳查证、问题表述模糊时，比固定一次检索更有用。
- 首版不做：任意网络抓取、无限资料域、知识图谱平台、自动修改资料。
- 验收：答案正确、引用支持结论、拒答得当；对照一次检索，连同成本与延迟一起看。
- 停止条件：真实问题一次检索已够，多轮没有补上正确率却明显变慢，则保留简单流程。
- [ ] 取得一组获准资料与真实问题，人工标注可核验出处，再决定检索方案。

### A02：IM 工作助手

用户在群里问“这项流程找谁办”，或私聊交代“帮我整理这条故障信息”。机器人查资料、追问缺项，回一段可用结果。

- 目标用户：工作已经发生在聊天平台里的团队。
- 最小闭环：只接一个平台，固定支持知识查询或工单草稿其中一种任务。
- 业务资产：群/私聊/话题线程映射、用户身份、可用资料、待办与交办归属。
- 领域工具：复用所选任务的工具；IM 本身不决定工具集合。
- 真正难点：何时响应、是否被 @、群内资料能否公开、重复消息、长答案如何分段、何时转人工。
- 首版不做：所有 IM 平台齐上、无人值守随意外发、读到群消息便自动执行命令。
- 验收：请求识别、串话率、重复执行、投递成功、转人工完成；还要验所承载任务本身。
- [ ] 选一个平台与一个具体任务，写清“回复谁、在哪回复、什么情况不回复”。

### A03：多租户 Agent 托管台

这是供团队配置和运行 Agent 的平台：创建租户，选模板，绑定资料与获准工具，查看运行结果、用量和审计。

- 目标用户：需要向多个团队或客户交付同类 Agent 应用的运营者。
- 最小闭环：两家租户、同一模板、各自资料与凭据、分别发任务、分别查结果。
- 产品对象：租户、成员、角色、项目、Agent 模板、发布版本、知识源、工具授权、用量额度。
- Agent 增益不来自“有租户表”，而来自平台所托管的具体应用；租户管理仍用普通后端代码。
- 真正难点：隔离、授权、版本发布、配额、审计、成本归属和故障恢复，不能只做一张 Agent 列表。
- 首版不做：任意用户上传 native 插件、插件市场、跨节点自动迁移、复杂计费套餐。
- 验收：同名资源不串租户，成员撤权生效，任务和凭据隔开，用量可追溯；隔离细节引用基建单。
- 前置条件：至少一种应用已有真实使用需求，否则容易先造一座没人用的平台。
- [ ] 用两份隔离数据跑同一模板，明确模板版本、配置权限和结果归属。

### A04：导购与产品选型助手

用户说“预算这些，主要做这些事，还有这些限制”。助手追问关键条件，查商品，列候选，解释为什么选、哪里不合适。

- 目标用户：有明确商品目录、需要辅助比较与选型的商家或服务团队。
- 最小闭环：一个品类 → 收需求 → 查真实目录/当前价格/库存 → 给少量候选 → 跳转商品或人工咨询。
- 业务资产：SKU、规格、适配关系、价格、库存、促销有效期、配送范围；动态信息取业务系统，不只靠文档索引。
- 领域工具：search_products、get_product、check_availability；知识检索只补解释性材料。
- Agent 增益假设：用户只会讲用途，不会写筛选条件；助手将口语约束转为可验证筛选，再解释取舍。
- 硬规则：价格/库存/优惠不由模型编造；用户硬约束优先；无匹配项就明说，不硬凑推荐。
- 首版不做：自动下单、扣款、改价、跨所有品类推荐；写操作以后单独设计确认与幂等。
- 验收：硬约束满足率、数据时效、推荐理由可核验、无匹配处理；转化指标需真实流量，不能拿离线样例冒充销售增益。
- [ ] 找一份能合法使用的单品类目录和典型需求，先测“筛得对不对”。

### A05：售后与工单助手

用户说“订单没到”或“机器报这个错”。助手查当前状态、读适用规则、补问必要信息，给处理建议或生成工单草稿。

- 最小闭环：核验身份 → 查订单/产品状态 → 读规则 → 收齐信息 → 生成可交接的工单。
- 业务资产：订单、保修/售后规则、工单状态、处理部门、历史沟通；权限须覆盖个人订单数据。
- 领域工具：lookup_order、lookup_policy、create_ticket_draft；政策规则和资格判断由后端验证。
- 真正难点：规则版本、身份核验、资料缺失、不能兑现的承诺、转人工时信息是否完整。
- 首版不做：自动退款、赔付承诺、关闭投诉、直接改订单。
- 验收：问题分类、信息完整、政策引用、人工接手成本；不能只统计机器人回复了多少条。
- [ ] 选一种售后问题，画清状态与人工交接点。

### A06：技术排障助手

用户给出故障现象。助手查版本、日志、配置和运行手册，提出原因假设，再给能区分这些假设的验证步骤。

- 最小闭环：收症状 → 查相关证据 → 列假设 → 只读验证 → 报告结论或未解之处。
- 业务资产：发布记录、日志、监控、配置快照、故障手册与服务拓扑。
- 领域工具：query_logs、read_config_snapshot、lookup_release、query_metrics；每只工具限范围、时间窗和返回量。
- Agent 增益假设：故障路径不固定，需要跨来源追查；这比普通 FAQ 更能检验工具循环。
- 首版不做：自动重启、生产改配置、删除数据、扩大网络权限。
- 验收：是否找到支持根因的证据、是否混淆猜测与事实、排查耗时和工具成本。
- [ ] 用历史故障和冻结证据做回放，避免只挑已知答案且过于简单的样例。

### A07：文档核对与材料审阅

上传两版说明、采购材料或申请材料，按一份明确规则核对缺项、差异与冲突，附原文位置。

- 最小闭环：解析指定类型 → 提取候选字段 → 规则校验 → 必要时回读原文 → 差异清单。
- 业务资产：文档、模板、校验规则、字段定义和原文定位。
- 领域工具：read_document、read_span、compare_versions、validate_fields。
- 真正难点：扫描件/表格抽取、跨页内容、单位和版本；模型解释不能替代确定性字段校验。
- 首版不做：所有文档格式通吃、自动批准材料、将辅助审阅当专业法律结论。
- 验收：缺项召回、误报、原文定位与人工复核耗时；解析出错单独记，不混成推理错误。
- [ ] 选一种文档和一套可判定规则，建立人工核对样本。

### A08：数据问答与经营查询

用户问“这一周哪类问题最多，按渠道拆开看看”。助手识别指标、时间范围和维度，查询后给表格与解释。

- 最小闭环：明确问题 → 选择受支持指标 → 只读查询 → 返回数据、过滤条件和口径。
- 业务资产：指标字典、语义层、数据快照、行列权限。
- 领域工具：list_metrics、query_metric、get_metric_definition；首版优先参数化查询，不开放任意 SQL。
- 真正难点：同名指标口径不同、时间范围、关联重复、缺值与权限；算数和聚合由数据库/代码完成。
- 首版不做：写数据库、任意跨库查询、自动据图表执行经营决策。
- 验收：查询范围、数值正确、口径说明、权限与执行成本；“图画得好看”不是数据正确证明。
- [ ] 选一个数据域和有限指标，给每个问题准备可执行的标准查询。

### A09：客户跟进与业务助理

在获准的客户记录和沟通材料中整理摘要、找缺失信息、列跟进任务，起草下一次沟通内容。

- 最小闭环：选定客户 → 汇总已授权材料 → 标出事实与待核实项 → 生成待办/沟通草稿。
- 业务资产：客户归属、沟通记录、产品信息、业务阶段和待办。
- 领域工具：read_customer、read_interactions、create_task_draft；草稿与发送动作分开。
- 真正难点：把推测写成客户事实、跨销售人员泄露记录、重复提醒、过时承诺。
- 首版不做：自动群发、搜集无关个人信息、擅自修改客户阶段或合同承诺。
- 验收：摘要保真、待办有依据、人工采用率；外发只有用户明确确认后才执行。
- [ ] 用脱敏且获准的记录验证草稿，不先接自动发送。

### A10：研究与信息监测助手

用户限定主题、来源和时间，助手查证、比较、整理简报；监测场景只报告有意义的变化。

- 最小闭环：限定问题 → 收集指定来源 → 保存证据 → 比较/去重 → 简报与未证实项。
- 业务资产：来源清单、快照、时间、主题规则、已读/已通知状态。
- 领域工具：search_sources、fetch_source、read_snapshot、compare_snapshot。
- 真正难点：来源可信、日期混淆、重复转载、引用漂移、无意义通知和无法访问的来源。
- 首版不做：无边界全网爬取、每次无变化也发消息、把猜测当实时事实。
- 验收：来源可核验、变化准确、通知有用；成本按一次简报/一次有效变化计算。
- [ ] 选一个固定主题与获准来源集合，人工核对多轮变化。

### A11：学习与培训助手

围绕指定课程材料答疑、出题、读作答证据，指出薄弱处，再安排下一步练习。

- 最小闭环：材料范围 → 小测 → 有依据的反馈 → 一组针对练习 → 再测。
- 业务资产：知识点、题目、评分依据、材料引用和学习记录。
- 领域工具：search_course、get_exercise、grade_deterministic_parts、save_learning_record。
- 真正难点：题目质量、评分一致、没有测过却断言掌握、重复出题；对确定答案用代码评分。
- 首版不做：全学科导师、仅凭对话热情推断学习效果、自动许诺成绩提升。
- 验收：反馈正确、前后测题目可比、能力迁移；使用时长不能直接当学习增益。
- [ ] 选一个小课程单元和可复核评分标准。

### A12：受控流程办理助手

把“帮我申请某项资源”这样的口语要求，转成字段齐全、规则合法、用户能核对的流程草稿。

- 最小闭环：理解意图 → 查流程与当前状态 → 追问缺项 → 展示草稿 → 获准后提交 → 查回执。
- 业务资产：表单字段、状态机、资格规则、审批人、任务回执。
- 领域工具：get_form_schema、validate_draft、submit_request、read_request；提交由后端事务保护。
- 真正难点：自然语言含糊、重复提交、流程途中规则变化和权限；Agent 不负责发明状态转换。
- 首版不做：万能管理系统生成器、一句话任意改库、模型自行批准。
- 验收：字段完整、非法状态转移零发生、重复提交可识别、提交前后都能追踪。
- [ ] 选一种低影响流程，先实现草稿模式，再设计写操作与确认。

### A13：3D 资产制作助手

可以做，但先分清“生成 3D”指哪件事。LubanCode 负责拆任务、调工具、看结果和修订；几何、贴图、渲染由建模软件或专门生成服务完成。语言模型写出一段描述，并不等于已经交付模型文件。

| 制作路径 | 怎样做 | 首轮验证重点 |
| --- | --- | --- |
| 程序化建模 | Agent 编写或调用受控脚本，创建几何、材质和场景 | 尺寸、结构、可编辑性；适合先试规则形状/低多边形道具 |
| 外接生成服务 | 提交文本/参考图，领取任务 ID，查询并取得模型 | 服务能力、格式、成本、版本与资产质量；具体服务另行选型实测 |
| 资产加工 | 对已有模型检查、改材质、减面、导出、渲染预览 | 修改范围、原件保留、输出能否在目标工具中正确打开 |

- 最小闭环：需求/参考 → 固定规格 → 制作一个静态道具 → 多视角预览 → 几何与导出检查 → 用户选择修订 → 交付源文件与目标资产。
- 业务资产：需求版本、参考图、尺寸/单位、轴向、面数/纹理预算、材质、来源、生成参数和文件版本。
- 领域工具：run_model_script、submit_generation、query_generation、inspect_mesh、render_views、export_asset；这是拟议工具面，不代表 LubanCode 已有这些适配器。
- 可接入依据：Blender 提供后台运行与 Python 脚本入口，存在程序化编排路径；本轮未在本机安装或执行。[Blender 官方测试文档](https://developer.blender.org/docs/handbook/testing/python/)
- 真正难点：参考一致、拓扑、UV/贴图、尺度、法线、材质兼容和目标引擎导入。截图好看与模型能用须分开验。
- 检查按用途选：打印件关注封闭和可制造约束；游戏道具关注面数/材质/碰撞与引擎表现。不能把所有用途硬套同一份检查表。
- 长任务：保存外部 job ID 与参数版本，超时先查询，不盲目重新生成；取消是否成功与是否计费如实记录。
- 首版不做：一句话做完整影视角色、自动完成所有绑定动画、复杂 CAD 工程精度或完整开放世界。
- 验收：文件可打开/编辑/导入、硬规格满足、多视角形状与材质正确、修改可追溯；艺术质量由用户审阅，不只看脚本返回 0。
- [ ] 选一个静态道具类别、一个制作路径、一个目标导出格式，制作少量可人工比较的样例。

### A14：游戏原型制作助手

用户描述玩法，助手拆规则、写代码、组场景、接资产、启动试玩，按错误与用户反馈修订。交付目标先是一段能玩的体验。

- 最小闭环：玩法卡 → 一个关卡/场景 → 输入与核心规则 → 胜负/结束条件 → 启动测试 → 试玩反馈 → 修订与打包。
- 产品形态可先二选一：受模板约束的小游戏生成器，或在现有项目中补功能的制作助手；两者自由度与验收范围不同。
- 业务资产：规则、场景、脚本、角色状态、资产目录、输入映射、构建配置和版本。
- 领域工具：read_project、edit_script、import_asset、run_engine、run_smoke_test、capture_playtest、export_build。
- 可接入依据：Godot 官方文档提供命令行运行、headless 和导出流程；这支持自动化集成的设计判断，不证明当前 LubanCode 已接 Godot。[Godot 命令行文档](https://docs.godotengine.org/en/stable/tutorials/editor/command_line_tutorial.html)
- 真正难点：场景和代码引用一致、资产路径、碰撞/输入、存档状态、性能与玩法体验。构建通过只验了一层，不能等同好玩。
- 自动验收：项目可打开、可启动，规定输入可触发核心行为，能结束/重开，无阻断错误；另做可见试玩，检查界面、动画、声音和操作反馈。
- 用户验收：规则是否清楚、反馈是否及时、节奏和难度是否合适。headless 测试不能替代视觉与手感检查。
- 资产策略：先用占位素材或现有获准素材；A13 可后接，不把 3D 生成完成设为小游戏原型前提。
- 首版不做：大型多人游戏、开放世界、全自动商业发行；打包与发布分开，商店上传另需明确授权与平台验收。
- [ ] 选一种小玩法和固定引擎版本，限定一个短流程，保存可重复的启动/操作测试。

### A15：长篇小说创作工作台

可以做成“构思、写作、校对、修订”连续工作台。重点在跨章节记住已经发生的事、守住人物与叙事线，不能只把一次生成的字数拉长。

- 最小闭环：故事意图 → 人物/世界设定 → 一个故事弧与章纲 → 逐章写作 → 连贯性与文风审阅 → 作者采用修订 → 更新设定账。
- 业务资产：已采用正文、人物卡、时间线、人物知道/不知道的事实、地点、伏笔、章节目标、叙事视角和文风约束。
- 领域工具：read_chapter、search_canon、read_character_state、save_draft、compare_revision、check_timeline、export_manuscript。
- Agentic RAG 可用于检索已写章节与设定；它帮助找材料，不保证检索到全部矛盾，也不替代作者取舍。
- 真值分层：作者确认的设定与已采用正文是依据；计划、未采用草稿、编辑建议分别保存。模型写了一份草稿不能自动把其中新事实写成正式设定。
- 变更规则：章稿与设定更新用同一采用版本关联；修改前文时列出受影响章节/伏笔，按影响范围检查，不偷偷重写整部作品。
- 角色分工可以用策划、写作、校对三个档案串行完成；不强制多 Agent 并行，以免各写一份相互冲突的设定。
- 连贯检查：人物动机与口吻、时间/地点、知识边界、物品状态、伏笔收束、重复情节。规则探针与模型审阅分开，候选矛盾由作者裁定。
- 审美验收：人物有没有动作与选择，场景是否推进冲突，节奏是否顺，语言是否贴作者要求。不能把“无错别字”“无检出矛盾”或字数达标当作好小说。
- 首版不做：承诺全自动写成畅销长篇、无人审阅自动连载；发布、封面等另设交付步骤。
- [ ] 选一个短故事弧，连续写几章并改一次关键设定，检验系统能否找到受影响内容并保持版本清楚。

### 创作应用共性：围绕作品迭代

这三项把应用范围从“回答/办理”扩到“作品生产”。它们共用的是任务拆解、资产版本、工具作业、预览、反馈、局部重做和导出。

```text
创作意图 → 规格/大纲 → 草稿/资产 → 运行或预览 → 审阅反馈
                              ↑                     │
                              └──── 局部修订 ───────┘
                                      ↓
                               采用版本 → 导出交付
```

应用侧保存“哪版被采用、修改影响哪些下游产物”，不能只存聊天摘要。源文件、预览、导出包与版本清单一起交付；未采用版本保留或按明确策略清理。

外部生成/渲染作业与模型请求分开计费、限时和记状态。失败恢复先查旧作业；达到预算就交待审版本或说明未完成，不无限生成碰运气。素材来源与使用范围随作品记录，是否满足具体发布要求在选定工具/平台后核验。

同样的生产闭环还可延伸到关卡设计、互动叙事、分镜/漫画、短片预制作等方向；当前先保留为组合机会，不另开一批候选项目。

### A16：LubanCode 桌面工作台

用户最终指的是眼前这款 Codex 桌面应用，识别文字为“Powered by Codex & OWL”，版本 26.903.71938。这是用户给出的产品参照，不是对其内部架构的核验。LubanCode 可以承载自己的桌面工作台：选项目、开任务、交代工作、查看执行过程、检查文件与改动、预览产物。本单不再把需求缩成普通聊天窗口，也不承诺与参照产品全部功能对等。

它可以独立成为应用，不必先选客服、导购等垂直业务。与 A03 的分工是：A16 管用户每天怎样使用助手，A03 管多个团队怎样配置、授权和运营。两者可组合，但单人版 A16 不需要先建完整多租户后台。

拟议界面：

```text
┌─────────────┬──────────────────────────┬────────────────┐
│ 新建对话    │ 当前会话 / 助手 / 状态    │ 文件与产物预览 │
│ 搜索历史    │                          │                │
│ 会话列表    │ 用户输入                 │ 文档、代码     │
│ 项目/资料夹 │ 助手回复                 │ 图表、图片     │
│             │ 工具进度 / 确认 / 错误   │ 其他文件引用   │
│ 设置        │ 输入框、附件、发送、停止 │ 版本与下载     │
└─────────────┴──────────────────────────┴────────────────┘
```

右侧预览按任务展开，窄窗口可折叠；不要求三栏始终挤在一屏。项目/资料夹归属是应用对象，不能直接当作引擎 cwd 或授权范围。移动端与独立网页产品不属于本项首版。

- 目标用户：希望通过桌面窗口使用模型和本地/远端工具，不想面对终端、协议字段与配置文件的人。
- 最小闭环：安装/启动客户端 → 创建对话 → 发消息/选择或拖入文件 → 看正文与必要工具进度 → 按需确认或补充信息 → 查看产物 → 关闭再打开并查询原会话。
- 首版桌面视图：项目与任务侧栏、会话主区、文件/产物预览、改动审阅、基础设置。终端面板按实际 PTY 与进程能力接入。新建、标题、归档/删除按已有服务能力逐项接通，不用前端按钮冒充后端已实现。
- 首版能力：普通问答、获准文件阅读、有限工具调用、停止任务、历史查询与结果下载；以服务实际能力为准。
- 领域/产品工具：文件解析、来源读取、可选搜索；底层工具由 Profile 裁剪。上传只是保存附件，不等于已解析、已建索引或已授予所有会话访问权。
- 用户选择：可以提供具名助手/模式，以及服务真正支持的模型选项；不要把未开放的 model/list、配置更新或热切换画成可用功能。换模式、模型、工具时说明当前会话还是新会话生效。
- 流式呈现：正文、工具状态、错误、确认请求分别展示。内部 thinking、凭据与原始调试字段不直接投到聊天页；工具进度也不伪装成模型内部思考。
- 前端职责：保存输入草稿、呈现事件、处理交互；运行真值仍查服务。刷新/重连先查原操作和结果，不能自动重复提交任务。
- 编辑与重试：编辑旧消息、重新生成、分支对话牵涉新执行和历史归属，首版可延后；实现时保存旧版本，明确分支起点和新费用，不能覆盖旧记录冒充从未执行。
- 产物管理：文件有所属会话/项目、版本、状态、下载授权；预览和下载单独检查权限。生成的 HTML/脚本等主动内容在隔离环境预览，不能借作品预览执行宿主代码。
- 桌面分层：窗口/UI → 桌面宿主与安全 IPC → LubanCode 协议客户端 → 本机引擎或已授权远端服务。UI 不直接拿密钥、执行任意系统命令或拼接启动参数。
- 本机模式：桌面宿主管理 LubanCode 子进程，通过 stdio 收发协议；用户无需手开终端。引擎来源、版本协商、启动失败、日志与退出由宿主管理，不能捆绑一个 exe 就算交付。
- 远端模式：以后可连接团队部署的引擎服务；桌面安装位置不决定模型和工具执行位置。界面明确显示当前执行环境；本地文件送往远端须有清楚的上传与授权流程。
- 文件交互：支持系统文件选择器、拖放、另存为与打开输出目录；只按明确授权访问选定文件/范围，不因桌面应用已运行就读取整盘或持续监听剪贴板。
- 关闭与后台：最小化、关闭窗口、退出应用、取消任务分别定义。首版不默默留下后台任务；若允许托盘常驻，须有明确设置、运行提示与退出方式。重新开窗先查原任务，不自动重发；重启后能否续跑仍受引擎恢复合同约束。
- 桌面交付：先选一个目标系统，验证安装/卸载、更新与引擎版本匹配、崩溃提示、孤儿进程清理和本地数据保留策略；签名、升级与回退纳入正式发行设计。跨平台逐项验证，不由网页能打开推定客户端可交付。
- 真正难点：前端状态与运行状态一致、附件生命周期、断线续看、取消、人工确认、版本和结果归属。漂亮聊天气泡只完成外壳。
- 首版不做：语音实时通话、任意工具市场、完整团队计费、所有媒体生成和无限后台任务；有具体需求再加。桌面框架与打包技术在独立项目设计时选，不在候选册提前锁定。
- 验收：发问/上传/停止/确认/刷新/重开会话/下载都能完成；重复或乱序事件不串话、不重复执行；用户看得懂进行中、失败和完成，未支持能力不显示可操作按钮。
- [ ] 先画“选项目 → 新任务 → 查文件 → 修改 → 看 diff → 验证结果”的桌面交互链，接真实引擎事件验证关闭/重开、取消和结果查询；执行恢复须另验，不能由历史可见推定。

产品扩展可以沿已有候选做：接 A01 形成带资料库的助手，接 A13–A15 形成带作品预览与修订的创作工作台。通用入口不等于向每个用户开放全部工具，当前助手档案与权限仍决定实际可用范围。

### A16 补充：桌面工作台与可复用组件

桌面工作台是一款成品。组成它的几块功能也可以单独交付，让别人嵌进自己的应用。首个产品就守住模块边界，第二个真实使用方再验证哪些接口值得发布成公共包；不必先造一整套万能框架。

拟议接入路径：

```text
自家桌面工作台 / 第三方桌面、Web、IM 应用
    → LubanCode 客户端库（拟议的薄 SDK）
    → app-server：请求、事件、审批、取消、结果查询
    → LubanCode Runtime：Agent 循环、上下文、工具、运行记录
    → 可选择的模型适配器、工具后端、Lua hook、领域服务
```

这里“直接基于 LubanCode”，指由 LubanCode 承担 Agent 运行。应用不必链接内部 C++ 类，也不必重复写循环。客户端库只封装公开协议、连接和类型；它不应另养一份执行状态机。初期可直接用协议，待真实调用方式稳定，再发布所需语言的 SDK。

用户提到有人基于 Claude SDK、Codex SDK 开发应用，本项目可走 LubanCode 接入路线。这是运行底座的选择，不据此断言其他 SDK 不能扩展。本文未审查这些 SDK 当前能力，也不把用户所说的采用情况当作市场调查。

| 可独立抽出的部分 | 职责 | 不应接管 |
| --- | --- | --- |
| 协议客户端库 | 握手、请求关联、事件订阅、取消与审批接口、查询与错误类型 | Agent 循环、擅自重试执行 |
| 桌面引擎宿主 | 启停子进程、stdio、版本与日志、崩溃提示 | 业务任务规则、在渲染页暴露凭据 |
| 会话与任务组件 | 列表、输入、流式文本、运行状态、历史与结果入口 | 自行判定服务端任务已完成 |
| 审批与工具进度组件 | 展示动作、目标资源、等待/失败/取消与审批回复 | 自行放宽权限、把隐藏按钮当禁用工具 |
| 文件、产物与改动组件 | 文件树、引用定位、diff、预览、下载和版本 | 默认执行作品脚本、代替引擎做文件授权 |
| 终端组件（可选） | PTY 交互、尺寸、退出码与进程状态 | 把模型工具日志伪装成可交互终端 |
| 能力配置组件 | 呈现可用模型、工具、hook、Profile 和生效范围 | 把未接通能力画成开关、前端绕过宿主授权 |
| 领域扩展包 | 注册工具适配、领域流程和相应结果视图 | 修改公共会话真值、把行业字段塞入 Runtime |

装配应允许只取所需部分：知识助手用会话、引用和附件；编程工作台再加文件树、diff 与终端；小说工作台加章节与版本；3D 工具加模型预览。关闭终端面板只改变界面；禁止命令执行须由引擎策略落实。安装扩展、授权扩展、在模型工具表中展示扩展，分别处理。

### A16 补充：自定义 MCP 与本地工具接入

工具扩展不限于 Lua hook。开发者可以接自己的 MCP 服务，也可以直接接本地工具；由 LubanCode 装配、向模型提供工具描述，并处理调用。新增工具不必经过 hook。只有需要拦截、改参、路由或加工结果时，才加对应 hook。

| 接入方式 | 适用场景 | 接入边界 |
| --- | --- | --- |
| 协议注入的应用侧工具 | 桌面或业务服务已有自己的函数/程序 | 注册描述与执行归属，经引擎授权采用；调用由协议下发，应用执行并提交结果，按基建 7.3 管理断线、取消与版本 |
| 自定义 MCP 服务 | 复用已有 MCP 工具，或把企业系统封成服务 | 由引擎侧 MCP 客户端连接、发现并适配工具；服务可在本机，也可在远端，按实际支持的传输接入 |
| 本地进程工具 | Python、Node、命令行程序或已有业务程序 | 通过进程插件/适配器声明名称、schema 和结果合同，管理启动、参数、取消、退出码与输出；不能把任意可执行文件路径当作完整工具协议 |
| 本地 Lua 工具 | 轻量逻辑、参数转换、领域组合 | 注册为工具，按宿主实际开放能力执行；Lua 工具与 Lua hook 分别装配 |
| 本地原生工具 | 宿主内实现、已有 C++ Tool，或受支持的 native 插件 | 走注册/加载接口，遵守版本与对象寿命；需链接或重新构建的接法明确标注，不冒充运行时热装 |

这里“直接接进去”指通过明确扩展入口挂载，不要求上层应用重写 AgentLoop。若工具只存在于桌面客户端，远端引擎不会自动调用它；须另设获准的工具服务或桥接。协议注册与应用侧执行回调已正式列入[基建设计第 7.3 节](工业化多协议接入_能力裁剪与工具扩展统一设计.todo)：应用提交工具描述，引擎发执行请求，应用调用自己的实现并回传结果。不必先包成 MCP，也不必把实现放进引擎；这是待实施合同，当前 SDK 尚不能据此宣称支持。

面向开发者，应统一展示工具名称、来源、版本、输入与结果合同、授权范围、执行位置和启停状态。MCP 与本地插件各自保留生命周期；对模型与应用尽量统一调用体验，不抹掉底层错误和取消差异。现有接入代码不等于生产 app-server 已完整装配，仍按基建单补齐入口并验收。

首个产品可提供：配置 MCP 服务、安装本地工具包、按助手/Profile 选择工具、检查依赖与连接状态、查看实际调用记录。工具出现在列表、获得执行授权、加入当前模型请求，分别处理。新增同名工具不默默覆盖；替换既有工具仍遵守下一节合同。

- [ ] 用一只协议注入的应用侧工具、一只自定义 MCP 工具和一只本地工具跑同一条桌面任务链，核验发现、授权、调用、结果和错误展示。
- [ ] 分别验证 MCP 断连、本地进程崩溃、取消与更新；后台任务保留其所用版本，不能把重新连接当作重新执行。
- [ ] 按 Profile 验证工具增加与禁用，覆盖模型直接调用及 hook 间接调用；注明当前会话或新会话生效，不默认承诺热插拔。

### A16 补充：自定义实现与 grep 替换

希望形成的长处：应用开发者能保留交互与任务合同，换成自己维护的模型接入、检索工具、上下文加工或领域服务。换实现后，权限、取消、预算、记录和结果仍有共同约束。不是每一层都已经能靠配置热换；各扩展点需分别核验。

先纠正工具名：当前模型工具叫 `search`，`mode="grep"` 表示内容搜索；生产默认后端是 ripgrep。用户举出的 tgrep、zgrep 在本单只作候选后端名，未指定项目、版本或参数，不推定它们与 rg 兼容。

当前源码证据（2026-09-13 静态检查，未运行替换链）：

| 源码位置 | 已看到什么 | 不能由此推定什么 |
| --- | --- | --- |
| `src/tools/search.cpp:228` | 工具名为 search，默认构造持 BundledRipgrepRunner | 把可执行路径改成任意程序就能兼容 |
| `src/hooks/middleware.cpp:201` | PreAction 合同允许 input.rewrite、backend.select 等效果；PostAction 允许结果加工 | backend.select 已接入生产工具选择与执行 |
| `src/hooks/middleware.cpp:1212` | 中间件零次 next 可短路剩余链 | 短路输出必然已映射成合法模型工具结果 |
| `src/runtime/plugin_lua_host.cpp:764` | Lua 有 luban.tools.call 宿主桥 | Lua 能无授权调用任意系统程序 |
| `src/runtime/hook_host_services.cpp:601` | 查注册工具、检查 hook 授权和 schema、限制递归，随后调用 execute | 已与模型执行路径共用全部策略和记账 |
| `src/tools/registry.cpp:39` | Find 按注册顺序返回首个同名工具 | 重复注册同名工具就是可靠覆盖机制 |

本次检索 src 中的 `BackendSelect` / `backend.select` / `HookPoint::PreAction`，命中集中在中间件定义与合同层，未找到将后端选择落到生产 Action 执行的消费点。故把完整替换列为待接线能力，不能写成“现在加一段 Lua 就能无缝替换”。旧式 PreToolUse 的 updatedInput 只负责改参，也不能等同于替换工具实现。

推荐的目标合同：

```text
模型调用 search(mode=grep, ...)
    → 检查逻辑工具与资源范围
    → 已授权 Lua hook / 配置选择后端
    → 适配器校验参数、后端授权、实际资源与预算
    → rg / 指定 tgrep / 指定 zgrep 实现
    → 统一搜索结果与错误
    → 原调用收到唯一终态；记录实际后端与版本
```

Lua 可负责路由与参数转换；宿主掌管可执行文件、参数数组、工作目录、取消和输出上限。若走现有 luban.tools.call，先把后端注册为获准工具，再验证子执行与父 Action 如何配对。只调用另一只工具并不会自动取消原 search，也不会自动把子结果交给模型。

新增、替换与结果加工分别定义：

1. **保持 search 合同，换后端。** 应用与模型继续使用相同参数和结果。适配器负责差异；缺少某项搜索能力便拒绝该参数，不能悄悄换成另一种搜索含义。
2. **增加另一只工具。** 若候选工具做语义检索、结构查询或压缩包搜索，且合同不兼容，独立注册工具名与 schema，再按 Profile 开关。是否比现有工具更好，另用任务验证。
3. **用 hook 加工结果。** 过滤、补充、摘要有独立用途；PostAction 时原搜索已经执行，不能把结果加工说成节省了原调用。

路由配置应指向明确实现和版本，拒绝含糊的同名覆盖。默认在新会话生效；在途调用钉住原实现。撤权按引擎合同拦后续调用，不能只卸载 UI。必要后端缺失、授权失败或语义不兼容时明确失败；仅在已声明可降级且确定原执行状态时切回 rg。不能因超时就偷偷再跑一遍，更不能把通用自动重试套到写工具上。

共同结果至少保留匹配文件、可用定位、匹配内容、是否截断与错误；是否还能给普通行号，取决于后端。如果涉及压缩包，需保留容器和成员定位，并约束解压与输出；不能硬造一个本地文件位置。正则、大小写、编码、忽略规则、glob、符号链接、排序、分页等差异逐项声明。这里是待设计合同，不是现有 JSON schema。

验收与推进：

- [ ] 先指定 tgrep/zgrep 的真实项目、版本、许可证与运行方式，列出和 search 的合同差异。
- [ ] 选一个后端接受控适配器，证明模型请求不变时能取回正确结果；记录真实后端，而非只记录 search。
- [ ] 将后端选择接入生产工具路径，检查 hook 子执行、原 Action、调用 id 与结果配对；证明原 rg 没有重复执行。
- [ ] 验拒绝授权、越界路径、缺依赖、参数不支持、超时、取消、大输出和后端崩溃；空结果与执行失败分开。
- [ ] 验同名冲突、旧会话持有旧版本、后台任务仍运行时更新、撤权，以及禁止错误降级。
- [ ] 用同一组获准文件比较结果正确率、定位、耗时与成本；不预设替代工具更快或更省。
- [ ] 桌面与另一种最小协议客户端使用同一配置，核对实际后端、审批和结果；证明扩展不依赖桌面 UI。
- [ ] 把必要引擎改动链接到协议/能力基建单；本单保留产品需求和验收，不在此决定内部类与配置字段。
- [ ] 为桌面工作台另写项目设计，首版选一条编程任务链；公共组件和薄 SDK 各列独立边界，第二使用方再验接口。

## 六、应用如何组合

| 组合 | 可形成的产品 | 真正新增的工作 |
| --- | --- | --- |
| A01 + A02 | 团队 IM 知识助手 | 渠道身份、群聊范围、回复投递 |
| A04 + A01 | 有依据的导购/选型助手 | 商品结构化数据与资料解释交叉核验 |
| A04 + A05 + A02 | IM 购前购后助手 | 身份、商品/订单状态、转人工与阶段边界 |
| A06 + A01 | 技术支持与排障助手 | 日志/版本证据、只读诊断工具 |
| A07 + A12 | 材料预审与办理助手 | 文档结果转合法表单、人工确认、回执 |
| A08 + A10 | 数据与变化简报助手 | 指标口径、数据快照、变化规则 |
| A13 + A14 | 带资产制作的游戏原型助手 | 资产规格/导入、碰撞、场景与版本关联 |
| A15 + A14 | 互动小说/叙事游戏制作助手 | 分支状态、对白触发、存档、不同路线一致性 |
| A15 + A13 | 世界观与场景资产工作台 | 文字设定到视觉规格的映射、作者审阅与资产版本 |
| A16 + A01 | 带文件/知识库的桌面 AI 助手 | 附件生命周期、资料范围、引用与结果展示 |
| A16 + A13/A14/A15 | 桌面创作工作台 | 作品预览、运行/试玩、修订与采用版本 |
| A16 + A03 | 连接团队服务的 AI 桌面客户端 | 用户工作台与租户管理台分工、权限和用量归属 |
| A03 + 任一已验证应用 | 多租户托管产品 | 隔离、模板发布、授权、用量与运营 |

组合时重用领域服务，不把各 Agent 的全权限取并集。售后工具不能因导购 Agent 同属一个产品就自动可用；不同用户/阶段仍各自授权。

IM 与 Web 可以复用业务会话服务，但不能只凭同名用户自动合并身份或历史。跨入口账户绑定须有明确验证。

## 七、先挑哪个：按验证条件选，不按名字热闹选

建议先找“拿得到真实任务、拿得到合法数据、有人能判结果”的方向。下面只是暂定顺序，随资料与用户资源变化调整。

| 顺序 | 候选 | 选择理由 | 开工前必须拿到 |
| --- | --- | --- | --- |
| 第一组 | A01 或 A06 | 靠近现有文档/工具执行能力，只读边界容易限定 | 资料/历史问题、能核验结果的人 |
| 并列替代 | A04 | 用户目标与硬约束明确，适合验证口语转筛选 | 单品类真实目录、价格库存来源、典型需求 |
| 第二步 | 为已选应用加 A02 | 验证日常交互与持续使用，不先增加第二门业务 | 一个可测试平台和明确使用流程 |
| 后续 | A03 | 在应用已验证后复用模板与托管能力 | 至少两组彼此隔离的用户需求 |
| 另择方向 | A05/A07/A08/A09/A10/A11/A12 | 取决于用户手中哪类数据与业务关系最扎实 | 对应业务对象、规则和人工验收 |
| 创作方向 | A13/A14/A15 | 若目标是创作工具，可独立选择，不必先做管理系统 | 固定作品规格、可调用制作工具、能审阅成品的人 |
| 桌面产品方向 | A16 | 若目标是让普通用户直接使用 LubanCode，可先做可安装的桌面助手 | 最小助手能力、可用事件接口、进程管理和桌面交互验收 |

不要同时开全部候选。先选一门业务或一种作品，跑通一条闭环；再换入口；确认有人持续使用后，再做多租户托管。创作应用不必先做 RAG 产品；它可以直接调用制作工具，在确有跨文档/章节需求时再接检索。若用户手里已经有明确客户和部署要求，则按真实约束重排，不机械照表。

几个判退问题：

1. 一个检索框或参数表单已经解决得很好，Agent 多轮究竟补了什么？
2. 没有实时库存、订单或指标数据，是否只做出会编答案的演示？
3. 结果无法人工核验，准备怎样发现错误？
4. 用户愿意把哪一步交给它，哪一步仍要自己确认？
5. 最慢、最贵或最易出错的一段来自模型、数据、外部系统，还是业务规则？

## 八、共用应用底座要等第二次需求来验证

先复用现有基建，不预先建设一座“万能 AI 管理系统”。同一能力在第二个应用真实出现后，再抽公共件。

可能抽出的应用侧公共件：

- 业务会话映射、任务与结果页面、人工交接。
- 工具结果到用户回复的安全投影。
- 数据来源、版本、引用和附件展示。
- 模板发布、工具授权与运行档案配置页面。
- 评测样本管理、运行对照、错误分类与成本汇总。
- 创作资产版本、预览与采用记录、依赖变更、局部重做和导出清单。

不宜过早统一：商品模型、退款规则、课程评分、客户阶段、文档审批条件。它们可共享“校验/状态机/事务”的技术件，业务规则仍各自实现。

如果两个应用只有登录页面与聊天气泡相似，不必因此强制共用全部后端。

## 九、每个候选进入独立设计前，填这张卡

| 项目 | 必须写清 |
| --- | --- |
| 用户 | 谁每天或每周遇到这件事，谁判断交付合格 |
| 原流程 | 现在怎样办，翻哪些系统，花多少时间，哪里容易错 |
| 单次任务 | 明确起点、输入、结果和结束条件 |
| 数据 | 来源、授权、版本、更新删除、无法访问时的处理 |
| 工具 | 只列最小必需集合，读写分开，每只工具有业务边界 |
| Agent 的作用 | 哪些步骤需要自主查证，哪些由固定代码完成 |
| 确认与人工 | 哪些动作先看草稿，失败怎样转交，谁来接 |
| 入口 | 先选桌面/Web/IM/API 中一种，别为了展示把入口全部接上 |
| 托管 | 单人、团队、私有部署或多租户，按实际要求选 |
| 验收 | 真实样例、对照流程、正确性、时效、成本、完成率 |
| 不做什么 | 首版明确排除的业务，不以“大模型都能聊”扩大范围 |
| 继续/停止 | 何种证据值得继续，何种结果应简化或放弃 |

应用 spec 填完，再决定项目目录、技术栈、数据 schema、部署包与业务工具协议。当前文件不预先替所有候选定一套实现。

## 十、推进 TODO

- [ ] 从候选中选出两个最有资料、最有真实使用者的方向；不以名称数量作为进度。
- [ ] 为这两个方向各填一张选题卡，记录可获取的数据、实际用户与验收者。
- [ ] 收集一小组真实任务，保留复杂、无答案、资料冲突和工具失败样例，不只挑演示题。
- [ ] 写出不使用 Agent 的最简单对照：检索、表单、规则流或人工步骤。
- [ ] 选择一个首发方向；其余候选继续留在本单，不同步开工。
- [ ] 为首发应用另建详细设计 TODO，定义业务工具、状态与结果合同。
- [ ] 对照基建缺口，列最小依赖；不把所有协议和热扩展完成设为原型前提。
- [ ] 用获准数据先跑只读/草稿闭环，记录失败原因与人工复核成本。
- [ ] 有证据后再加写操作、IM 入口或第二组用户，一次只扩大一条主要边界。
- [ ] 多租户产品只有在隔离和恢复验收完成后才进入托管试用；产品页面完成不算过关。
- [ ] 第二个应用出现相同需求后，再抽共享应用件；业务规则保留独立。
- [ ] 定期删除没有用户、没有数据或没有可验证收益的候选，避免应用清单变成永久堆积。

## 十一、这份文件怎样维护

- 本单是应用选题册，候选卡上的待办表示“要核验什么”，不是已批准启动全部开发。
- 选定应用后，另写具体项目设计；在本单补链接与阶段，不把协议主单改回业务方案。
- 新增方向先判断是新业务、入口变化还是托管变化；可组合的直接组合，不靠换名称制造项目。
- 应用验证结果分别记录业务效果与基建能力。协议/工具测试通过，不能证明导购有效、客服解决问题或学习成绩提高。
- 本轮只新增此文件，未修改基建设计，未创建应用仓库，未提交 Git。
- 后续补入 A13–A15：3D 资产、游戏原型和小说创作。外部文档仅确认部分自动化入口，未验证本机集成或作品生成效果。
- 后续补入 A16，并按用户澄清限定为LubanCode 桌面工作台。作为独立上层应用，协议/引擎合同仍归基建单；未实施界面或功能。

- 需求再澄清：A16 参照 Codex 桌面应用，补入可复用组件、LubanCode 薄 SDK 路线与 Lua 工具后端替换边界。新增源码检查仍属静态证据，未实现或实测替换。

- 协议注入补充：应用侧工具注册与执行回调正式列入基建 7.3/P4a，本单同步产品入口；尚未实施。
