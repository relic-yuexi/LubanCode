# API 钥匙解析两坑：环境变量撞车与 inline 不兜底

- 状态：已实现（2026-09-02 当日销账。P0 解析链倒转+四情形单测、P1 可观测中英成对、P2 文档向导提示全落；ctest 365/365 全绿，两坑假后端复现验收通过——注入撞车变量带警告跑通、unset 后回落 inline 跑通，实际发送钥匙与 `--config` 展示逐情形核对一致。打码复用 MaskApiKey 前 8 位口径，与单子"前 6 位"字面略异）。**2026-09-02 二次实战又抓出坑三，主人定案通名退役——见"六、坑三与通名退役"，待修。**
- 日期：2026-09-02
- 级别：P1（配置可靠性——钥匙错了用户只会看见 401，不知道错在哪）
- 范围：provider 钥匙解析链、`--config` 展示、缺钥匙报错文案
- 依赖：无
- 关联：无
- 不在本单：改各家 provider 的鉴权协议本身；密钥管理器/钥匙串

## 一、先下断语

钥匙解析现在的规矩说不清、也不给人看。用户在 config 里明明白白配了 `api_key`，只因 `key_env` 也配着，解析就整个交给环境变量——变量里有值也好、没值也罢，inline 那把都不顶用。两坑由此生：

**坑一：撞车。** ccmoon 条目的 `key_env=ANTHROPIC_AUTH_TOKEN` 是个通名。Claude Code 会话、各类代理工具都爱往环境里塞这个名。塞了别家的钥匙进去，LubanCode 拿它发请求——401，用户看着配置里配好的 key 百思不解。实战实录：shell 里有 Claude Code 注入的 `ANTHROPIC_AUTH_TOKEN=1c9cd6c2...`，压过配置里的 `sk-fa1...`，cc.moontidef.work 当场拒收。

**坑二：不兜底。** 把撞车变量 unset 掉再跑——报"缺少配置: api_key"。`key_env` 指的变量不存在时，inline `api_key` 不补位，直接判缺。`auth: inline` 字段形同虚设。用户两条路都走不通：变量在，可能撞车；变量不在，判缺。

## 二、定案（主人裁决前的建议案）

1. **解析优先级倒过来**：inline `api_key` 有值就是底座；`key_env` 变量存在且有值时作为覆盖（显式压过 inline）。变量不存在——回落 inline，不判缺。两把都没有才报缺。
2. **两把钥匙不一致时要叫**：`key_env` 与 inline 都有值且不同——启动给一行警告（变量名 + 两把的前 6 位打码），说明用的是哪把。不静默选边。
3. **`--config` 说人话**：api_key 一行明示——"来自环境变量 ANTHROPIC_AUTH_TOKEN（sk-fa1b…，压过配置里的 inline）"或"来自配置 inline（key_env 指的 X 未设）"。钥匙来路一眼可见。
4. **报错文案带路**：缺钥匙时的三条途径提示里，加一句"配置里 key_env 指向的变量名"——用户知道该去设哪个变量，或改用 inline。
5. **通名黑名单警示（可选，P2）**：`ANTHROPIC_AUTH_TOKEN`/`ANTHROPIC_API_KEY`/`OPENAI_API_KEY` 这类通名被 key_env 引用时，文档与首次配置向导里提示撞车风险。

## 三、实施批次

### P0：解析链修正
- [x] inline 为底座、key_env 存在才覆盖、都无才报缺
- [x] 两把并存且不一致：警告 + 明示用哪把
- [x] 单测钉死四种情形：仅 inline / 仅变量 / 两把一致 / 两把不一致（警告路径）

### P1：可观测
- [x] `--config` 展示钥匙来路（变量名/inline/压过关系，前 6 位打码）
- [x] 缺钥匙报错文案带 key_env 变量名与 inline 状态
- [x] 中英文案成对

### P2：文档与向导
- [x] 配置文档写明优先级与撞车风险
- [x] 首次配置向导配 key_env 时给通名提示

## 四、验收

- 复现实战两坑的原始环境（注入撞车变量 / unset 后跑）：前者带警告跑通，后者回落 inline 跑通，均不再 401/判缺。
- `--config` 在上述四种情形下输出与实际发送的钥匙一致（抓包或假后端核）。
- 现有 provider 配置解析测试全绿，无行为回退（正路配置不受影响）。

## 六、坑三与通名退役（2026-09-02，已完成）

### 6.1 现场（比前两坑都阴）

二次实战（0.26.166，坑一坑二已修）unset 撞车变量后仍判"缺 api_key"。破案链：Claude Code 会话给 shell 注入了 `ANTHROPIC_BASE_URL` 等四个通名变量 → 顶层 wire 未配时内置默认按 Anthropic 线 → `generic_env.anthropic_base_url` 有值，base_url 落 [通用环境变量] 来源 → provider 应用段 `EnvironmentOverridesProviderBinding` 判"环境解绑"（`environment_unbound=true`）→ **374 行 auth_token 段被短路，ccmoon 的 inline 钥匙根本不应用** → 顶层 api_key 链（此刻按 anthropic 挑 `anthropic_auth_token`，已 unset）落 2543 清空 → 缺钥匙。

变量不撞车、也不缺——旁边站着同院子的邻居，钥匙就没了。且全程无警告无提示，`--config` 只显示"(未设置)[内置默认值]"，无头案。

### 6.2 主人定案：通名退役，只认自家命名空间

> 不要读 ANTHROPIC_*/OPENAI_* 这类通名，只读 LUBAN 开头的环境变量——比如 LUBAN_BASE_URL、模型同理。

落地口径：

1. **砍掉 generic env 链**（`ANTHROPIC_BASE_URL/ANTHROPIC_AUTH_TOKEN/ANTHROPIC_API_KEY/ANTHROPIC_MODEL`、`OPENAI_API_KEY/OPENAI_BASE_URL/OPENAI_MODEL` 等一切通名读取）——base_url/model/api_key 的顶层来源链里 [通用环境变量] 一级整体退役。
2. **只认 `LUBAN_*` 命名空间**：`LUBAN_BASE_URL`、`LUBAN_MODEL`、`LUBAN_API_KEY`；既有 `LUBANCODE_*`（如 `LUBANCODE_API_KEY`）作为同族保留（都以 LUBAN 开头，不破坏）。
3. provider 的 `key_env` 字段照旧可用（显式指定变量名），但其值也不该再指通名——指向通名时按 P2 的既有提示警告。
4. 退役是干净砍，不走兼容窗：CHANGELOG 写明"通名环境变量不再读取，改用 LUBAN_*"；`--config` 与文档同步。自家产品自家用户，一刀利落。
5. 砍掉后 `environment_unbound` 的误判源（generic base_url/model）随之消失——provider 绑定在纯文件配置下天然稳定，坑三断根。

### 6.3 验收

- [x] Claude Code 式污染环境（注入四个 ANTHROPIC 通名变量）下跑 one_shot：provider 的 inline 钥匙照常生效，零警告零干扰。
- [x] `LUBAN_BASE_URL`/`LUBAN_MODEL`/`LUBAN_API_KEY` 三变量按优先级正常生效，`--config` 来源标注为 [环境变量(LUBAN_*)] 一类明示。
- [x] 通名注入任何组合都不再改变 wire/base_url/model/api_key 的解析结果。
- [x] 相关单测更新（generic env 链的既有用例改判/退役），全量 ctest 零回归。
