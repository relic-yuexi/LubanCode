# 主会话输出预留占坑：预检误判"上下文将尽"与交接死循环

- 状态：调查已定案，待实现修复；本轮未改生产代码。
- 日期：2026-09-09
- 级别：P0（主会话在 256K 窗只用了一半时被误判"上下文将尽"，禁用工具调用，任务永远无法收尾）。
- 事故现场：`~/.lubancode/workspaces/D--lubancode-4d95553f/sessions/20260908-152500-U3ESGR/`（2026-09-08，LubanCode 自家会话写 SessionTurnStepAction 设计单，三段交接后一事无成；只建了一个空 todo 文件）。
- 范围：`src/agent/loop.cpp` 预检闸、`src/agent/runtime_profile.hpp` 输出预算、`src/runtime/trajectory_session.cpp` 取消记账、后台完成唤醒与应急模式的互搏、相关测试。
- 不在本单：compact 流程本体（另单）、emoji/粘贴半包（另单）、子代理侧已修的同类病（`SubagentOutputReserveCap` 已收口）。

## 一、先下断语

主会话的 token 预检把模型目录声明的 `max_output_tokens`（本例 128K）**整份当作输出预留**去撞窗口。256K 窗的模型，输入一旦过约 124K（256K − 128K 预留 − 协议余量），预检必爆——**此时窗还空着一半**。爆后走应急支（预留收窄到 8K 放行），并注入收尾交代（`BuildContextWrapupNudgeText`，loop.cpp:66 起）："请立即收尾，**不再发起新的工具调用**;用一段简短交接写明……"。

模型照办：只写交接，不碰 write_file。后台子代理完成又把这只"待死"会话摇醒（`interactive_session.cpp:1155` 起的回流路，会话空闲即另起一轮）——新 Run 重注一道收尾交代，再写一份交接。如此三环，文件永不成形。

这病根**已有前科**：`runtime_profile.hpp:88-100` 的 `SubagentOutputReserveCap` 注释原文写着"256k 窗口 × 128k 目录上限的真机事故:输入 13 万 + 预留 12.8 万,预检永远过不去"——当年只给**子代理**戴了帽（预留封顶 clamp(window/8, 8k, 32k)），**主会话这条路没戴**。本次是同一病灶在主会话原样复发。

## 二、证据链（2026-09-09 验尸记录）

事故会话轨迹（`main.jsonl`，run main-0008）逐条对得上：

1. `context.pressure.recorded`：`phase=preflight_exceeded`、`estimated_input_tokens≈135287`、`window_tokens=256000`、`reserve_clamped=true`、`reserved_output_tokens=8192`——135K 输入远小于 256K 窗，预检却爆，唯一解释是**预留大于约 120K**（目录声明 128K 输出上限）。数字与 `SubagentOutputReserveCap` 注释里的前科一字不差。
2. 预检闸（loop.cpp:1180-1252）：`ExceedsContextWindow(input, 常规预留, window)` 过不去 → 应急支放行 + 注入收尾交代（`context_wrapup_nudged` 逐 Run 一道）。收尾交代明令"不再发起新的工具调用"——模型三段交接的措辞（"宿主要求立即收口,未继续工具操作"）即它的转述。
3. 三次 `model.output.cancelled`（payload.reason 硬编码 `user_interrupt`）+ `turn.cancelled`（origin=recovery_runtime）。取消旗的**唯一**置位人是监听线程的 `interrupt_turn`（console_input_turn_listener.cpp:308 起，ESC/单击 Ctrl+C）；`request_interrupt()` 全库零调用。三次应系真按键（用户看它光交接不干活，按 Esc 踢它），**不排除**粘贴半包合成假 ESC（另单覆盖，本单不判）。
4. 每次 `turn.cancelled` 之后紧跟 `turn.started(user)` + `input.received`——后台子代理完成回流唤醒（#4、#5 两只探子各唤醒一次）。
5. 主历史里**零**"用户按 ESC 打断了这条回答"文案：主会话流式走 `sample_model` 单发采样路（loop.cpp:1658 的 ESC 块不是主路），取消错 `ErrorKind::Cancelled` 在 sample_model.cpp:124 记账——ESC 块的入史仪式不在道上。
6. 空 todo 文件的来历：轨迹里**没有任何工具事件**碰过该路径（只读复核），存疑；候选假设是"建文件与写内容两步非原子、取消窗口夹在中间"，列 §五 P3 验证。

## 三、病理拆解

```
目录声明 max_output_tokens=128K(能力上限)
        │ 被当作每次请求的输出预留(占坑,不是请求字段本身)
        ▼
可用输入帽 = 256K − 128K − 协议余量 ≈ 124K   ←窗空一半,输入先"满"
        ▼
历史 > 124K → 预检爆 → 应急支:预留收 8K 放行 + 注入"立即收尾,不再发起新的工具调用"
        ▼
模型只写交接(禁令在前,write_file 不敢碰)
        ▼
后台子代理完成 → 会话空闲回流另起一轮 → 新 Run 再爆再注 → 又一份交接
        ▼
三环之后用户 Esc 踢场/放弃;若无按键,此循环可无限续
```

四宗罪并坐：

| # | 罪 | 位置 |
| --- | --- | --- |
| 1 | 主会话输出预留不封顶，能力上限当占坑预留 | loop.cpp 预检闸 + `ResolveOutputBudget` 消费侧 |
| 2 | 应急支把"输出预留过肥"与"历史真满"混为一谈，一律进收尾禁令 | loop.cpp:1232-1241 |
| 3 | 后台完成唤醒不识"待死"会话，把应急态会话反复摇醒 | interactive_session.cpp 回流路 |
| 4 | 取消记账硬编码 `user_interrupt`，真伪用户打断不分 | trajectory_session.cpp:558 |

## 四、修复合同

### 4.1 主会话预留封顶（主修）

- 预检用的输出预留**封顶**：`clamp(window/8, 8k, 32k)`（与子代理同尺；窗未知给 32k）。`ConfigFile` 显式值不收——用户手笔，尊重原值（子代理侧同款例外）。
- 注意帽子戴在**估算用的预留**上；请求实发的 `max_tokens` 字段另账：当"输入 + 声明输出上限"超窗而"输入 + 封顶预留"不超时，**实发值优雅降级**为 `window − 输入 − 协议余量`（下限 8K，再低走应急），而不是进收尾禁令。能力上限是"最多能给"，不是"每次都要留足"。
- 应急支收紧进闸条件：只在**封顶后仍装不下**（历史真满）时进收尾交代。预留过肥的情形在封顶这一步就化解，不再惊动模型。

### 4.2 取消记账说真话

- `OnOutputCancelled` 增带真实来源（用户按键 / 内部取消 / 流错误），`user_interrupt` 只留给真按键路。轨迹是验尸的账本，不许硬编码冤枉用户。

### 4.3 唤醒识死

- 连续 N 次（建议 2）应急放行的会话，后台完成回流不再自动另起一轮：改为终端一枚明确通知（"上下文将尽,已暂停自动续轮;请 /compact 或开新会话"），把去留交还用户。唤醒复活可以，无限复活不行。

### 4.4 回归测试

- 预检三态各有独立预期：肥预留 + 半窗输入 → 放行且实发 max_tokens 降级、**无**收尾交代；封顶后仍满 → 应急 + 收尾交代恰一道；单消息自爆 → 明确报错文案。
- 唤醒识死：模拟两次应急后回流不另起 Run，通知文案在册。
- 取消记账：三来源各记各的名（合同测试，不真按键）。

## 五、分期

- [ ] P1：主会话预留封顶 + 实发值优雅降级 + 应急进闸收紧（§4.1），回归测试三态。
- [ ] P2：取消记账真实来源（§4.2）。
- [ ] P3：唤醒识死与通知（§4.3）；顺带验证空文件假设（write_file 两步原子性，若证实另立小单修）。
- [ ] 文档：configuration 手册写明 `max_output_tokens` 三级解析与预留封顶口径；CHANGELOG 记用户可见变化（主会长会话不再被误判"上下文将尽"）。

## 六、验收矩阵

- 256K 窗 × 128K 目录输出上限 × 输入 130-200K：请求正常放行，无收尾交代，任务能继续调工具；实发 max_tokens 随余量降级且不低于 8K。
- 输入真到 248K+：应急支照旧工作，收尾交代恰一道，文案不诬窗。
- 子代理既有行为零变化（帽已戴，回归在册）。
- 两次应急后子代理完成：不另起 Run，通知在册；用户显式输入照常开轮。
- 轨迹取消事件：真按键记 user_interrupt，其余记实因。
- 事故现场复放：同形状会话（大目录输出上限 + 长历史 + 后台完成回流）能写完 todo 文件并 commit。

## 七、本轮交付

只此调查与修复设计单。验尸基于事故会话轨迹只读复核（事件序列、压力账、取消账）与源码对读（loop.cpp 预检闸、runtime_profile.hpp 帽、turn_listener 升旗路、interactive_session 回流路、sample_model 记账路），未改生产代码、未建测试、未编译。
