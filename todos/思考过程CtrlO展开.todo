# 思考过程 Ctrl+O 展开

状态：待做

## 一句话

思考块现在收成一行「思考 Xs」，全文藏在 `full_output` 里等展开——要的是 Claude Code 那样按 Ctrl+O 就地把思考正文摊开看，再按一下收拢。

## Claude Code 怎么办的

- Ctrl+O 是「详细/紧凑」翻转开关（verbose transcript toggle）：按一下，整个转录切到详细档——工具条目带全量入参与输出，thinking 全文摊开；再按一下收回紧凑档。
- 思考流式期间就有一个折叠头（「Thinking…」带秒数），展开后能边流边看已到的正文。
- 展开是原地重画，不是另铺一屏。

## LubanCode 眼下有什么

- 思考显示已合入（08b5e8c）：流式期间折叠成「思考 Xs」，正文攒进 `thinking_buffer`，结束时灌进该条目的 `full_output`（`src/main.cpp:2156` 一带注释写明「供 Ctrl+O 展开」）。
- Ctrl+O 机制也在：空闲态 `UiKeyAction::ToggleExpand` 翻 `transcript_expanded`，`FormatSnapshotForToggleLocked(expanded)` 用 `FormatTranscriptItems(..., expanded)` 重排整个转录（`src/main.cpp:2582`）；流式态监听线程也认 Ctrl+O（`ThreadMain` 的 `PK::CtrlO` 分支）。
- 缺口在两头：
  1. **思考进行中**：折叠头亮着，正文边流边攒，但没处看——Ctrl+O 只翻「已完成条目」的快照，正在长的思考块摊不开。
  2. **思考完成后**：`full_output` 有货，展开依赖整个转录的 expanded 档——若 `FormatTranscriptItems` 对 thinking 条目不认 `expanded`（只给工具条目用），全文就永远见不了天日。这里要实查：`src/cli/transcript.cpp` 的 `FormatTranscriptItems` 对 tool_name=="thinking" 的条目在 expanded 档下到底吐不吐 `full_output`。吐，则收尾只差「进行中可看」；不吐，两头都要补。

## 怎么做

1. **实查第一刀**：跑一遍带 thinking 的会话，等思考完，Ctrl+O 看展开里有没有思考全文。有 → 只做第 2 条；没有 → 先给 `FormatTranscriptItems` 补 thinking 条目的展开档（对齐工具条目的摆法：摘要行之下缩进铺全文，行宽截断照旧）。
2. **思考进行中可看**：流式期间按 Ctrl+O，除翻转录档外，正在长的思考块把「已攒到的正文」快照摊开（读 `thinking_buffer`，锁内取副本）；再按收拢。不打断流式，不挪正文锚点（照 Ctrl+O 现有的 `ForgetAnchorsLocked` + 重画事务走）。
3. **快门体验**：展开的思考正文若很长，摘要行保留「思考 Xs · N 字」，展开区超过一屏时截断加「共 N 行，结束后 Ctrl+O 看全文」——别让一次展开刷飞滚动历史。
4. plain 主题（管道）不搞原地展开：正文按现有 plain 路子整段输出，本就全可见，不添花样。

## 代码落点

- `src/cli/transcript.cpp` / `transcript.hpp`：`FormatTranscriptItems` 对 thinking 条目的 expanded 档（若实查确认缺）。
- `src/main.cpp`：Ctrl+O 流式分支带上「进行中思考块」的快照；`thinking_buffer` 取副本的锁规约（现在它在 RunTurn 线程单线程攒，监听线程要读就得有锁或改经 transcript 快照通道——优先把「已到的思考正文」也走 transcript_snapshot_ 更新，别开第二条数据路）。
- `src/cli/i18n.cpp`：新文案中英成对。

## 验收

- 思考完成、转录收拢：条目是一行「思考 Xs」；Ctrl+O 展开后正文缩进铺开，再按收拢。
- 思考进行中按 Ctrl+O：已流到的正文当下可见，继续流则（下一笔重画时）跟随变长；Esc 打断、收轮都正常。
- 展开超长思考不刷飞滚动历史，带「共 N 行」收口。
- 工具条目、子代理条目的 Ctrl+O 行为不回归；plain 路子不夹 ANSI。
- 单测：`FormatTranscriptItems` thinking 条目紧凑/展开两档的纯逻辑（现有 transcript 测试样式）。

## 不做

- 不做思考流的逐字滚动窗（正文快照随重画节律走，不单独起动画）。
- 不做 per-条目选择性展开（维持全局档 + 最近一条 expanded_index 现状）。
- 不改 thinking 的收录与脱敏（那套在录制器/转录里另有账）。
