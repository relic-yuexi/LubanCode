# QQ Bot：Windows 连接诊断与交互配置修复

日期：2026-09-14。
问题版本：`v0.26.263-beta.1`，Windows x64 发布包。
复核基线：工作区 `f4559cb1`；同时核对问题版本源码。
状态：P0-A（§三连接状态）与 P0-B（§四 Windows 信任根）已实现，测试上册，待 CI 与 Windows 真机复测；§五/§六（向导与 IM 入口）另行批次进行中。真实 QQ 联调仍未做（Q3）。
归属：补充 [QQ 接入总单](QQ机器人接入_Channel桥与SessionV3权限闭环.todo) 的 Q1b 配置入口与 Q3 真实账号验收；不重复 Q2 会话与投递实现。

产品主线补充（2026-09-14）：[常驻助理 Web 主界面总装](常驻助理Web主界面_CPP本地服务与Gateway总装.todo)。先有不依赖 IM 的聊天、任务、结果和审批界面，本单成为“渠道设置”子项目。Web 表单与终端 im 向导复用配置/凭据服务；最近平台只是渠道偏好，不决定整位助理是否启动。

## 一、用户现场与证据边界

用户在 PowerShell 运行 `lubancode gateway run`，依次遇到：

1. `channel_disabled (channels.qqbot.enabled != true)`：手填 AppID 和密钥路径，没有渠道与账号开关。
2. `credentials_missing (secret_file_insecure)`：启用后，密钥文件未通过 Windows 归属或 DACL 检查。
3. 手动收紧密钥文件 ACL 后，只剩 `[gateway] profile=default boot=... pid=...`；QQ 端仍显示未连接。

前两项有现场日志；第三项只证明本地 Gateway 启动，不能证明取令牌、TLS、鉴权成功。用户没有提供原始 ACL，不能断言具体是哪条 ACE 导致拒绝。

[QQ 官方 FAQ](https://q.qq.com/qqbot/openclaw/faq.html) 指出，“未连接服务”大多与 WebSocket 异常有关，应查主机日志。该页本次已读取；OpenClaw/Hermes 专属命令不能照搬给 LubanCode。

源码确认存在 Windows 信任根加载缺口与错误原因丢失。它们是应修问题；现场究竟先卡在哪一步，须补日志再复现，不能提前认定全由证书引起。

## 二、源码落点

| 文件 | 本次核对结果 | 修复职责 |
| --- | --- | --- |
| [qq_tls.cpp](../src/channel/qq/qq_tls.cpp) | `DetectSystemCaPemPath` 只探测 Unix 常见路径；TLS 消费传入的 CA | 跨平台信任根加载与可辨认的 TLS 错误 |
| [channel_gateway_wiring.cpp](../src/app/channel_gateway_wiring.cpp) | CA 未传入时只走上述探测 | 装配时检查信任根，将渠道状态交给宿主展示 |
| [qq_gateway.cpp](../src/channel/qq/qq_gateway.cpp) | 取网关、连接、鉴权失败已有 detail；重连另发退避事件 | 保留失败阶段、错误与重试时间 |
| [qq_adapter.cpp](../src/channel/qq/qq_adapter.cpp) | Disconnected 只上报 backoff，丢掉 detail；status 用线程存活代替 connected，last_error 固定为空 | 报告真实连接状态，不把线程启动当在线 |
| [credentials.cpp](../src/channel/credentials.cpp) | 核对 owner 和 DACL；拒绝非白名单 SID 的允许项；只读校验，没有安全创建入口 | 安全写入、校验、旧文件迁移；保留严格读取检查 |
| [channel_config.cpp](../src/channel/channel_config.cpp) | `MakeQqTemplateAccount` 已定义 pairing、禁群、final、只读工具模板，账号默认关闭 | 向导复用模板，提交时显式启用目标渠道与账号 |
| [config.cpp](../src/config/config.cpp) | 已有配置路径解析与写入入口 | 定点更新原始全局 JSON，保留模型、其他账号与未知字段 |
| [cli_app.cpp](../src/app/cli_app.cpp) | `RunGateway` 装配渠道，打印 skipped；启动行不代表 QQ ready | CLI 路由、连接进度、退出语义 |

## 三、P0：连接状态说清楚

- [x] QQ session 独占平台连接状态：取令牌 → 查询地址 → TCP/TLS → WebSocket → Identify/Resume → READY/RESUMED。适配器透传结构化快照，宿主负责输出；不另养一份猜测状态。（P0-A 批：`GatewayEvent::StageChanged`/`kStage*` 六阶段常量 src/channel/qq/qq_gateway.hpp；适配器 `ConnectionSnapshot` src/channel/qq/qq_adapter.hpp + `ConnectionState()`；测试 tests/unit/channelqq/test_qq_gateway.cpp"阶段推进事件"案、test_qq_adapter.cpp"ConnectionState"案）
- [x] `connected=true` 只在 READY/RESUMED 后成立；断线、停止立即变 false。分别保留线程存活、连接阶段、最近失败、失败时间、重试次数与下次重试时间。（`HandleGatewayEvent` 全分派：SessionReady/Resumed→connected=true、ConnectFailed/Disconnected/Stopped→false；快照带 thread_alive/stage/last_failure{stage,error_code,detail,at_ms}/retry_count/next_retry_at_ms；Health 响应修"线程存活冒充 connected"病——test_qq_adapter.cpp"Health 响应带真实 connected"案）
- [x] 失败保留 `stage`、稳定 `error_code`、清洗后的简短说明。退避事件不得覆盖根因；连接成功后把错误移到历史，当前错误清空。（`GatewayConnectError{stage,error_code,detail}` 承载事件流：ConnectFailed/Disconnected 带根因，`BackoffScheduled` 只带 attempt/next_retry_at_ms；适配器 `last_failure` 归档 `failure_history`（留 8 笔）；测试 test_qq_gateway.cpp"READY 前失败发 ConnectFailed"案、test_qq_adapter.cpp"连接失败记根因；成功后错误移入历史"案）
- [x] `gateway run` 默认打印账号开始连接、首次失败、在线、断线和停止。重复失败合并限频；原因变化立即显示，不能刷屏，也不能一直沉默。（`ChannelConnectionReporter` src/app/channel_connection_reporter.{hpp,cpp}：开场行、边沿立即打、同码 30 秒窗合并+超窗补"仍在失败(第 N 次)"、码变立即；挂 ChannelGatewayWiring::TickOnce；测试 tests/unit/channelqq/test_qq_connection_reporter.cpp"边沿都立即打印"/"同码重复失败限频合并"案）
- [x] 不打印 AppSecret、token、Authorization、原始响应体或带敏感参数的 URL。诊断只取允许字段；平台报文不能原样拼日志。（快照字段全来自 GatewayEvent 稳定账（token 层脱敏在 QqTokenManager/QqHttp 既有禁令）；`RedactConnectionDetail` 控制字符折叠+200 字帽双保险；日志只打阶段/码/清洗 detail，不打印 URL；测试 test_qq_connection_reporter.cpp"脱敏清洗"案）
- [x] 提供跨进程只读状态入口，目标命令为 `lubancode channel status qqbot main`。由 Gateway 发布带 boot ID、更新时间的脱敏快照；CLI 校验存活与过期状态，不把旧快照当在线。快照不成为连接状态权威。（`connection-status.json` 由 reporter 每 tick 写：变化即写+5 秒保新鲜刷 updated_at，带 boot_id/pid；CLI `JudgeChannelStatus`：进程死/快照过期(60 秒)/未连接/在线四路裁决；源 src/cli/channel_status_command.{hpp,cpp}、装配 src/app/cli_options.cpp channel 子命令；测试 test_qq_connection_reporter.cpp"快照发布"案、tests/unit/cli/test_channel_status_command.cpp 全册）
- [x] 一个账号失败不拖死其他账号或自动任务；启动日志汇报失败账号。专用连接检查遇到失败/超时须返回非零；不能凭 PID 宣告检查成功。（每账号独立网关线程既有事实不变，wiring skipped 清单照打+新增信任根诊断行；reporter 开场行逐账号报"已装配,开始连接"，首次失败立即打=启动日志可见失败账号；`channel status` 非在线路径全退非零，在线只认 connected=true+进程活+新鲜——test_channel_status_command.cpp"进程已退"/"快照过期"/"未连接"案）

目标输出示例（尚未实现）：

```text
[qqbot/main] 正在获取访问令牌
[qqbot/main] 正在连接 QQ
[qqbot/main] 连接失败：TLS 信任根未加载；2 秒后重试
[qqbot/main] 已连接 QQ，等待消息
```

## 四、P0：Windows 信任根

- [x] 默认走 Windows 系统证书信任，不要求用户下载 PEM、造 `/etc/ssl` 目录或设置未接线的环境变量。（`ResolveChannelTrustRoots` src/channel/qq/qq_tls.{hpp,cpp}：Windows 分支 `ExportWindowsTrustRoots` 从 Root/Ca 店（CurrentUser+LocalMachine）导出喂 mbedTLS；用户零动作。**未验边界**：`#ifdef _WIN32` 块 CI 只编 main 不跑测，真机行为归 Q3 下个发布包复测。）
- [x] TLS 层封装平台信任校验。Windows 优先用系统证书链构建与 SSL 策略校验，接入 mbedTLS 握手验证；覆盖目标主机名、有效期、链信任、用途与系统不信任策略。若实现改为导入 ROOT，须证明与系统策略的差异并处理显式不信任，不能只导出几张证书就声称等同系统校验。（两道都上：mbedTLS 验导入链 + `mbedtls_ssl_conf_verify` 挂 `WindowsPolicyVerify` 回调（leaf 上 `CertGetCertificateChain`+`CertVerifyCertificateChainPolicy(CERT_CHAIN_POLICY_SSL)`，覆盖 CN/有效期/EKU/Disallowed/系统弱算法策略）；差异论证：系统链构建可走 AIA 拉中间证书、Disallowed 显式不信任、系统策略演进，导出子集做不到——故系统裁决为权威（回调拒→握手拒，过→清 flags 放行），且导出时即剔除 Disallowed 命中的锚。**未验边界**同上，Q3 真机。）
- [x] 保留 Linux/macOS 现有路径行为，测试用 CA 注入仍可用。显式 CA 覆盖若对外开放，须接全配置解析、优先级与文档；配置无效时明报错，不静默退回其他来源。（Linux/macOS 探测路径与 PEM 读取行为不变；`TlsTrustMode::ExplicitCa` 下三平台同走纯 mbedTLS 验证（测试自签 CA 注入不受影响，tests/unit/channelqq/test_qq_ws_client.cpp TLS 两案原样）；显式锚未接用户配置（只测试 seam），无效即明报不回退——test_qq_tls_errors.cpp"explicit 优先且不回退"案、test_qq_wiring.cpp"信任根诊断"案；文档 docs/architecture/channels/configuration.md §13.2。）
- [x] 信任根为空、加载失败、证书过期、主机名不符、握手超时分别报稳定错误。禁止关闭证书验证或失败后改走明文。（`TlsError.error_code` 稳定码族 kTlsCode*：trust_store_empty/trust_store_load_failed/cert_expired/cert_hostname_mismatch/cert_not_trusted/cert_policy_rejected/cert_verify_failed/handshake_timeout/handshake_failed；`authmode` 恒 `VERIFY_REQUIRED`，无降级开关、无明文回退；映射测试 test_qq_tls_errors.cpp"验证 flags→稳定码"/"握手错误码映射"案。）
- [x] 检查 HTTP 取令牌/查地址与 WSS 的信任策略差异；两段分别验收，不能拿 HTTP 成功代替 WSS 成功。（两段本就异栈：HTTP 走 cpr/libcurl（Windows 发布包 vcpkg 路线默认 Schannel=系统信任），WSS 走自建 mbedTLS——本批补齐 WSS 的 Windows 信任根；连接快照按阶段分开报错（fetching_token 段 token_*/gateway_url_* 码、connecting 段 tls_* 码），成功各记各的，HTTP 段成功不会写成 WSS 在线（connected 只认 READY/RESUMED）。HTTP 段真机行为未改动未复测——如实记 Q3。）

P0-A/P0-B 落地批：分支 worktree-agent-qqwin-conn（PR/CI 链接见下笔补记）。mock 覆盖：tests/unit/channelqq/{test_qq_gateway,test_qq_adapter,test_qq_connection_reporter,test_qq_tls_errors,test_qq_wiring}.cpp、tests/unit/cli/test_channel_status_command.cpp。未验口径：Windows 证书链/策略校验真机行为、HTTP(Schannel) 与 WSS(mbedTLS) 双段真平台联调、真实账号全链路——全部归 Q3 下个发布包复测，mock 绿不冒充联调过。

## 五、P0：用户输入配置，程序管文件权限

目标命令（待新增，不是当前可用命令）：

```powershell
lubancode channel setup qqbot
lubancode channel setup qqbot --account main
```

### 5.1 向导流程

- [ ] 显示目标账号与实际配置路径，依次询问 AppID、AppSecret。默认账号 main；已有账号允许保留旧密钥或输入新密钥，绝不回显旧值。
- [ ] AppSecret 用本地终端隐藏输入，支持粘贴、退格、取消；退出和异常都恢复回显模式。无交互终端时明确报错，自动化沿用已有 secret_file/secret_env，不通过命令行参数传明文 secret。
- [ ] 输入只留在配置向导内，不进入 Agent、模型提示、会话 JSONL、trace、遥测或 shell 历史。内存中减少副本，用完尽力清理；不承诺 C++ 普通字符串已得到绝对擦除。
- [ ] 校验 AppID 与密钥非空、编码及控制字符；错误说明不引用用户密钥。配置路径由现有参数根解析，不能硬编码当前 Windows 用户目录。向导显示应用根覆盖后的实际位置。
- [ ] 默认在当前配置根下 `secrets/` 保存程序管理的凭据，JSON 只存绝对路径 `secret_file`。账号标识限制为安全文件名，拒绝路径分隔符与越界。
- [ ] 新账号复用 `MakeQqTemplateAccount`：websocket、DM pairing、禁群、final 回复、只读工具上限；成功保存时设置 channels.qqbot.enabled 与目标 account.enabled 为 true，明确 default_account。
- [ ] 已有账号只修改用户选定字段，保留原权限与路由；显示差异，不用模板静默扩大旧权限。其他账号、模型配置和未知 JSON 字段原样保留。
- [ ] 保存后分别显示“配置已保存”“尚未验证连接”，给出当前工作目录下的 `lubancode gateway run`。网络验证须有超时；失败不撤销已保存配置，也不谎称在线。
- [ ] 若提供向导内短时连接检查，复用账号锁，检查后释放连接；已有 Gateway 占用时读取状态，不开第二条竞争连接。配置保存不会热更新正在运行的 Gateway，明确提示重启。
- [ ] pairing 配套完成本地批准入口与首条私聊提示，纳入 Q1b。配置保存、QQ 在线、身份已配对、模型能回复分四步展示；没有模型配置时准确提示。

### 5.2 安全创建与替换

权限校验要保留。修复目标是让正常配置自动得到合格文件，不是取消 secret_file_insecure。

- [ ] 增加统一凭据写入服务，由向导与迁移入口调用；网络适配器只读凭据，不得偷偷改 ACL。
- [ ] Windows 创建凭据目录和文件时就附带安全描述符：owner 为当前用户，DACL 关闭继承，默认只允许当前用户。不要先写明文再收紧权限，避免短暂暴露。
- [ ] 现有读取白名单还允许 SYSTEM、Administrators 与两类应用包 SID。单独审查两类应用包放行依据；向导不主动授予这些组权限，兼容策略调整须补测试与迁移说明，不能顺手放宽白名单。
- [ ] POSIX 目录按 0700、文件按 0600 创建；核对 owner。Windows 与 POSIX 共享同一写入合同，平台实现分别落地。
- [ ] 对路径、重解析点/符号链接、文件句柄与最终目标做核对；拒绝借替换目标绕过检查。既有文件更新不跟随任意链接，不递归改父目录权限。
- [ ] 用同目录安全临时文件写入并落稳，再原子提交。新密钥先存为独立版本，配置原子指向新文件；配置提交失败保留旧配置与旧密钥，并清理本次新件。全部提交后再回收旧受管文件。
- [ ] 配置更新加锁并核对读取版本，避免两个向导互相覆盖；重新解析写入结果，再用生产凭据读取器检查权限及内容，成功后才显示保存完成。
- [ ] 中途取消、磁盘满、ACL 设置失败、进程中断都不能留下“启用但无可读密钥”的配置，也不能留下权限宽松的临时文件。恢复只清理由程序标识的孤儿受管文件。

### 5.3 已有密钥文件不合权限

- [ ] 错误区分 owner 不符、DACL 过宽、无法读取安全描述符；用户说明只显示文件位置与原因，不显示内容。
- [ ] 向导检测已有受管文件权限过宽时，展示“收紧这份密钥文件权限”的选项，调用同一服务修复并复验；无需用户手敲 PowerShell，不要求整套程序以管理员身份运行。
- [ ] 外部自定义路径默认提供“重新输入密钥，保存到受管位置”。确需迁移旧内容时，由用户明确选择，安全读取后写入受管文件，更新路径；不删旧外部文件，不擅自改其父目录。
- [ ] owner 不符或没有改 ACL 权限时，准确报错并提供重新输入到新受管文件的路径；不自动提权、夺取文件所有权或修改整棵用户目录。
- [ ] `gateway run` 只做校验与诊断，发现不合权限时提示 setup 入口；绝不在后台默默修用户文件。保留 secret_file > secret_env > inline 优先级，高优先级无效不得降级。

## 六、统一 IM 选择与配置界面（2026-09-14 用户补充）

用户要求：首次没配置就进入界面，选择 QQ、飞书等平台，再输入配置；下次默认选最近使用的平台和账号，也能显式指定。入口须为后续 IM 留位置，不做 QQ 专属的一次性问答脚本。

首版采用终端交互界面，方向键选择、回车确认，并提供数字选择回退；暂不另造网页服务。普通 `lubancode` 仍进入原有编程交互，自动配置只在 IM 入口触发。

### 6.0 架构定案：入口分工与统一边界

IM 是面向用户的渠道选择、配置和启动入口。它调用 Gateway 运行所选账号，不新增独立 IM 执行器，也不在首版把 Gateway 改造成 AppServer 客户端。

目标回归（用户本轮澄清）：沿用[持久助理总计划](持久Agent_Gateway接入SessionV3_常驻助理与可靠自动任务总装计划.todo)。用户通过 IM 找到同一位常驻助理：来信能处理、到点能办事、结果能送回、重启能按账恢复。选择 IM 只是选沟通渠道，不应关闭助理的自动任务。此前“im 不启动自动任务泵”的设计撤回。

“统一协议”分两层说，不能把下表合称一套总协议：

| 边界 | 合同 | 谁使用 | 本次设计 |
| --- | --- | --- | --- |
| 自建应用 → AppServer | AppServer 请求/事件协议，承载为 stdio 或 WebSocket | 网页、桌面客户端、应用后端 | 维持现有入口；不同客户端共用同一方法与事件格式 |
| 平台适配器 → 渠道宿主 | `lubancode-channel/1` Channel Bridge | QQ 适配器、后续飞书等适配器 | 平台消息统一成渠道事件；回复统一成渠道发送请求 |
| 运行宿主 → 会话执行 | 进程内 SessionService 与既有执行装配 | CLI、AppServer、Gateway | 共用会话受理、执行及记录能力；这是代码/服务合同复用，不是新的网络协议 |
| IM 界面 → 配置及启动服务 | 本地函数/类型合同，待提取 | `lubancode im` 和 setup 别名 | 选择、安全配置、生成启动计划；不承载聊天正文，不调用模型 |

当前路径与拟新增入口：

```text
自建应用 ── AppServer 协议 ── AppServer 宿主 ──────┐
                                                 ├─ SessionService / Agent / 工具
QQ ── QQ 适配器 ── Channel Bridge ── Gateway ──────┘
飞书 ─ 飞书适配器（待实现）─────────────┘
自动任务 ────────────────────────────┘

lubancode im（待实现）
  └─ 平台/账号选择 → 安全配置 → 启动计划 → 复用 Gateway 宿主
```

图中的共用执行层不表示几个进程共用同一运行中会话，也不表示可绕过账号锁、工作目录绑定或权限准入。各宿主保留自己的连接、输入与投递责任。

- [ ] AppServer 不处理 QQ token、平台重连、配对账和回复 outbox；这些仍归适配器/渠道宿主。Gateway 不为本次 IM 向导新增 AppServer RPC 监听端口。
- [ ] IM 发来的只是本机启动选择；QQ 聊天正文必须经过 ChannelManager 的持久入站、去重、准入、路由，再进入 ChannelWorkPump → HeadlessExecutor → SessionService，不能从向导直接调用 `turn/start` 绕过渠道权限。
- [ ] AppServer 与 Gateway 共用执行底座，但各自编排任务。若以后确需远端 Agent Worker，再单列 Gateway → AppServer 的客户端桥设计，明确身份、幂等受理、审批、断线恢复和 outbox 归属；本单不提前改通信拓扑。

#### 本地模块与启动计划

以下类型是待实现设计名，不表示仓库已有接口：

| 模块 | 输入/输出 | 唯一职责 |
| --- | --- | --- |
| `ImEntryController` | CLI 参数、终端能力 → 选择/配置流程 | 调度界面；不直接写 JSON、ACL 或创建适配器 |
| `ChannelSetupRegistry` | 平台 ID → 字段、校验、模板及能力 | 为各平台供应表单；和运行时适配器注册对账，拒绝有表单无实现的假可用平台 |
| `ChannelConfigService` | 目标账号、配置变更 → 提交结果 | 定点写配置，调用凭据服务；内存中的 secret 不放进通用启动计划 |
| `CredentialStore` | 本地密钥输入 → 安全文件引用 | 按第五节创建、替换、修复与复验文件权限 |
| `ImPreferenceStore` | 平台/账号标识 → 最近选择 | 独立保存界面偏好，不能改变运行时开关或渠道策略 |
| `GatewayLaunchPlan` | 工作目录、profile、渠道范围、自动任务开关 | 一次启动的不可变选择；不含 AppSecret、token，不修改全局 enabled |
| 共用 Gateway 启动服务 | 启动计划 → 生命周期/状态/退出结果 | 从现有 RunGateway 装配提取；持锁、装配渠道及工作泵、停止和收口只实现一份 |

- [ ] `gateway run` 生成计划：所有已启用渠道 + 当前 profile 的既有自动任务；`im` 生成计划：所选渠道账号 + 当前 profile 的既有自动任务。二者调用同一启动服务，IM 不以“聊天入口”为由关闭自动任务。启动界面显示实际 profile、workspace 和任务范围。
- [ ] `im` 默认沿用既有 default profile，并可显式指定 `--profile`；它与相同 profile 的 Gateway 互斥。提示用户停止原实例或选择其他 profile；即使 profile 不同，同一渠道账号锁仍互斥。不得自动制造 profile 绕过冲突。
- [ ] 从 `RunGateway` 提取启动服务时，使渠道泵可在无自动任务泵时独立运行；不能让现有“先有 automation pump 才装 composite pump”的装配关系成为 IM 隐藏依赖。
- [ ] 渠道选择只限制渠道入站与渠道投递，不改变自动任务归属；核对恢复扫描与 outbox 扫描的范围。当前 profile 的自动任务照其既定权限执行；若回复目标账号未启用，则保留投递待办并显示原因，不改投当前 QQ、不丢结果。需要全部渠道投递时，明确引导运行完整 Gateway。
- [ ] IM 启动前向导取消：不创建运行宿主。启动后 Ctrl+C：复用 Gateway 停止接收、收口执行、落账、释放连接与锁的次序，不留下 IM 子进程。
- [ ] 第一版不支持向已有 Gateway 动态挂载账号。若账号已在运行，显示实例和状态，不用“连接成功”掩盖本次没有启动；未来远程控制接口另列协议与权限设计。

#### 没有渠道与运行验证

- [ ] `gateway run` 无渠道时明确输出“未启用任何渠道”；有自动任务就处理自动任务，两者都没有则显示“当前空闲，等待任务”。启动时列出工作目录、profile、启用范围与失败账号，不只打印 PID。
- [ ] `im` 无渠道时进入平台向导；取消就退出；非交互时缺配置则报错退出。渠道已配置但连接失败走诊断，不冒充首次配置。
- [ ] 验证 Gateway 不依赖 IM：不配渠道，登记一个现有机制支持的单次自动任务，核对执行与本地结果投递。不能用尚未落地的周期调度或入站 Webhook 冒充现有能力。
- [ ] 验证 AppServer 独立：不启动 Gateway，由现有客户端完成 initialize → thread/start → turn/start → turn/completed，核对事件与结果；不得意外连接 QQ。
- [ ] 验证 IM 共用路径：真实 QQ 消息能在渠道入站账、会话执行与回复 outbox 对上；重复消息不重跑，停止释放账号锁。另验 IM 启动后当前 profile 的自动任务仍能执行，不领取其他 profile 的任务。
- [ ] 常驻助理验收另跑完整场景：QQ 上创建获准的任务 → 耐久受理回执 → 到点执行 → 结果送回原会话 → 重启后按账恢复。聊天侧任务创建工具、身份/任务归属、回复目标绑定和平台主动发送权限必须逐项落地；已有本地 job CLI 与 QQ 被动回复不等于这条路径已接通。周期任务、heartbeat、系统服务托管按持久助理总计划 V2/V4 实施，不能用 IM 配置向导代替。
- [ ] 文档分别使用“AppServer 协议统一应用接入”“Channel Bridge 统一平台适配”“SessionService 复用执行底座”，不再笼统写“所有入口同一协议”。

### 6.1 命令与选择规则

以下均为目标命令，尚未实现：

```powershell
lubancode im
lubancode im --select
lubancode im qqbot
lubancode im qqbot --account main
lubancode im qqbot --account main --profile personal
lubancode im setup
lubancode im setup qqbot --account main
```

- [ ] `lubancode im` 是日常入口：没有账号配置时打开平台选择；只有一个可用账号时选它；已有有效最近选择时默认使用最近选择，显示平台、账号与切换提示，然后启动该账号。
- [ ] `--select` 每次打开选择界面，列出已配账号与“添加新平台/账号”；最近选择排在默认光标位置。`im setup` 始终进入配置管理，不顺带启动连接。
- [ ] 指定平台/账号时，以显式参数为准；目标没配过就进入对应向导。不能因指定目标不存在而悄悄连上最近账号。未知平台、拼错名称先报错，不写空壳配置。
- [ ] 只指定平台时：先取该平台有效的最近账号，再取其 `default_account`，再取唯一可用账号；仍有多个则弹选择。全局最近平台不能覆盖显式平台。
- [ ] 未指定目标且最近记录无效时：唯一可用账号自动选；多个可用账号打开列表；无可用账号进入配置/修复界面，保留已有配置供修改。
- [ ] 缺必填字段、密钥不可读、权限不合时进入对应修复步骤。已配置但停用的账号显示“已停用”，用户选择后明确询问是否启用，不暗改开关。
- [ ] 网络超时、平台拒绝鉴权属于连接诊断，不等同“没配置”。保留选择和配置，展示重试、编辑配置、返回列表，不反复弹出全套首次向导。
- [ ] 选择单账号只形成此次启动过滤器，不把其他账号 enabled 改为 false。过滤器贯穿账号装配、锁、工作泵与投递，不能只筛 UI 标签。
- [ ] IM 入口复用 Gateway 的启动、锁、调度、停止与工作目录合同，不复制一套运行时。遇到既有 Gateway/账号锁先说明归属，不杀旧进程、不暗起第二实例。

### 6.2 记住最近选择

- [ ] CLI 配置界面独占选择偏好；Gateway 独占运行状态；平台适配器独占连接状态。最近选择不写进 Agent 记忆或会话消息。
- [ ] 增加独立、可版本化的用户偏好记录，保存最近的 `{channel_id, account_id}` 和各平台最近账号。只存标识，不存 AppID、密钥、token 或聊天内容。
- [ ] 按现有应用参数根隔离偏好，个人布局与受管应用不串号；同一参数根下换项目仍可沿用选择，但工作目录照本次启动冻结，不迁移旧聊天上下文。
- [ ] 已有账号被用户选定且配置校验通过、开始启动时更新最近选择；新账号在配置提交成功并用于启动时更新。临时网络失败不抹掉偏好；取消向导、保存失败、仅浏览列表不更新。
- [ ] 偏好原子保存，并发更新不损坏文件；启动不写持久偏好的非交互命令，不参与“最近使用”排序。偏好丢失/损坏只回到选择流程，不阻止已有配置使用。
- [ ] 删除账号时清理相关偏好；手工改配置导致记录悬空时按 6.1 回退，不复活已删除账号。

### 6.3 平台接入合同

- [ ] 由渠道能力注册表提供平台 ID、显示名称、实现/可用状态、配置字段、字段校验、敏感输入标记、默认账号模板、连接检查及配对指引。公共向导管导航和提交，平台模块管本平台字段与校验。
- [ ] QQ 首版填写 AppID/AppSecret；飞书及其他 IM 接入时提供自己的字段和事件接入要求，不照抄 QQ WebSocket 假定。
- [ ] 只允许选择已有可运行适配器的平台。飞书尚未实现时可显示“尚未支持”，不可进入假成功配置或宣称能连。
- [ ] 所有平台复用安全凭据写入服务、配置定点更新、取消恢复与脱敏输出。字段扩展不改变原有渠道配置默认行为。
- [ ] `channel setup qqbot` 如保留，作为统一向导别名；本文第五节 QQ 流程是平台表单实现，不另维护第二套交互状态机。

界面示意（待实现）：

```text
选择聊天平台
> QQ · main          上次使用，已配置
  添加 QQ 账号
  飞书               尚未支持

QQ · main
AppID：请输入
AppSecret：隐藏输入

配置已保存，密钥权限已设置。
正在连接 QQ……
已连接 QQ。首次使用请完成身份配对。
```

### 6.4 后台兼容与验收

- [ ] 现有 `gateway run` 保持运行所有已启用渠道及自动任务的语义，不因最近选择默默缩成单账号。交互终端下完全没配置渠道时，显示可进入 IM 配置的提示选项，允许继续只跑自动任务。
- [ ] 非交互、服务、管道输入、AppServer 与一次性执行不弹界面、不等键盘。`im` 缺配置或选择不唯一时返回稳定非零码和 setup 提示；显式目标配置完整时可直接启动，不更新交互偏好。
- [ ] 测试覆盖：首次无配置、最近目标、显式覆盖、只指定平台、多账号选择、停用账号、删除账号、坏偏好、取消、新建失败、连接失败、后台无 TTY、应用根隔离与 Gateway 锁冲突。
- [ ] 回归：选择 QQ 不停用飞书配置；`gateway run` 不漏原有渠道/自动任务；普通编程启动不意外拉起 IM 向导或网络连接。

## 七、验收与交付顺序

1. P0-A：补真实连接状态和脱敏错误，让现场能定位第一处失败。
2. P0-B：补 Windows TLS 信任；用 Windows 打包产物复测。
3. P0-C：统一 IM 选择入口、最近选择、QQ 表单、安全写入与旧配置迁移；同步 Q1b 配对入口。
4. Q3：用户本机填凭据，确认测试对象后验证真实账号；更新使用文档与发布说明。

- [ ] Windows 单测：普通用户新建、继承 Users/Everyone 权限、owner 不符、无权改 ACL、中文/空格路径、重解析点、替换时 ACL、并发配置更新与写入中断；不依赖管理员运行。
- [ ] 终端测试：隐藏输入与取消后回显恢复；密钥不出现在 stdout/stderr、日志、trace、会话或临时配置中。用测试密钥扫描输出，不读取真实用户秘密来做检查。
- [ ] 连接测试：令牌拒绝、TLS 信任根缺失、主机名不符、超时、READY、RESUMED、掉线、重连、停止；断线后 connected 必须为 false，last_error 保留根因，重复错误输出限频。
- [ ] 向导测试：全新配置走完即得到两层 enabled、模板策略和安全凭据；旧配置保留模型/未知字段/其他账号；取消和提交失败不毁旧配置。
- [ ] 用干净 Windows 用户环境解压新发布包，不另装 Node、不手工建证书目录、不手工改 ACL；走 setup → gateway run → QQ 在线 → 本机配对 → 两轮文字对话。
- [ ] 真实测试覆盖重连、停止和硬杀重启；记录版本、系统、通过步骤与未验范围，日志先脱敏。mock 成功、构建成功不能充当平台联调成功。
- [ ] 随包文档给新向导流程和旧配置迁移入口；清理渠道文档里“QQ 未装配”等过时说明，仍未交付的功能照实标注。
- [ ] 发布说明单列“Windows QQ 连接”“交互配置与权限”“真实账号验收结果”。本修复单在这些验收完成前保持未完成。
