# 子代理面板移到输入框上方

状态：待做（交互体验）

## 现象

lubancode 把多智能体面板画在输入框下面。代理一多，面板便往终端底外头长；人正盯着正文和输入框，既扫不清谁在跑，也看不全谁已完成。展开详情后更挤，屏面上下颠倒。

同类 CLI 的顺序更自然：

```text
对话正文

多智能体面板
  main
  Explore ...
  general-purpose ...

──────────────── 输入框上沿
> 用户输入
──────────────── 输入框下沿
状态栏
```

输入框与状态栏钉在底下。代理面板贴在输入框上方，向上长，挤的是旧正文，不是把新状态推出视口。

参考界面还做了两桩小事，不能漏：

```text
Enter to view · x to stop · ctrl+x ctrl+k to stop all agents
```

选中代理按 Enter 后，输入框上沿右端还挂一枚短标题，如“修 ask_user 被子代理状态遮挡”。人往下翻详情、再回来敲字，一眼便知此刻看的是哪只代理。

## 已查到的根子

空闲 composer 这条路写死了上下次序：

1. `ReadLineKeyByKey()` 从 `AgentPanelProvider` 取代理快照。
2. `with_agent_panel()` 调 `FormatAgentPanel()`。
3. 它把结果追加进 `RenderState::hint_lines`。
4. `RedrawEditArea()` 先画输入正文，再画下横线和状态栏，最后才画 `hint_lines`。

所以代理面板不是偶然掉到下面，而是数据结构便把它当成了“状态栏下方提示”。代码在：

- `src/cli/console_input.cpp`：`FormatAgentPanel()`、`with_agent_panel()`、`RedrawEditArea()`。
- `src/cli/console_input.hpp`：`AgentPanelEntry` 与 `AgentPanelProvider`。
- `src/app/interactive_session.hpp`：会话把 `AgentTool::TaskSnapshots()` 转成面板条目。

还有两道截断：

- 会话只取 `TaskSnapshots(8)`，第九只以后压根进不了面板。
- 任务说明只留 34 个码点；详情只列最近 8 次工具调用。

这便解释了“看不出完整情况”：位置不对，数据也不全。

## 需求

### 1. 面板挪到输入框上方

空闲 composer 的固定顺序改成：

```text
agent panel
top rule
composer rows
bottom rule
status line
ordinary hints
```

代理面板不再借 `hint_lines` 落脚。给编辑帧一块明写的 `rows_above`，或设独立的 `AgentPanelFrame`。普通 slash 提示仍可留在状态栏下面，两者不可再混用。

顶部横线眼下在首次进入 composer 时单独打印，不在 `InlineFrame` 的重画账里。面板要动态增减，这根横线也得纳入整帧，或由一只上方区域 painter 统一管。否则任务完成、终端缩放、面板展开时，旧行擦不净，光标也会跳。

### 2. 输入框钉在视口下部

代理新增、结束、展开详情时，面板向上占行。输入框、下横线、状态栏不要被推到屏幕外。

- 算整帧高度时，把面板、上下横线、composer 软换行、状态栏一并算入。
- 空间不够便滚走较旧的 transcript，随后重算所有屏幕锚点。
- 光标始终回到 composer 当前字符，不落在代理行里。
- 终端改宽、改高后下一帧须重新排版，不能沿用旧物理行数。

这事要复用现成 `EnsureRoomForRows`、screen scroll hook 与 synchronized output。不可每条代理各 `flush` 一次。

### 3. 展示完整任务，不让面板无限长

“完整”不是把几百行一次铺满屏。要让每只任务都能找到，同时守住输入区。

- 所有运行中任务都进列表，不受“最近 8 只”截断。
- 完成、失败、取消任务可按时间收拢；面板顶部写清总数与未展示数。
- 任务超过可用高度时，围着当前选择做窗口或分页。上下键能走遍全表。
- 列表行至少显示：代理类型、任务短述、运行/完成/失败、耗时、工具调用数、token。
- Enter 进入当前代理查看态。详情可滚动，能看完整任务说明与工具调用流水；不能只剩最近 8 次又不作说明。
- `main` 固定为第 0 项。切回 main 后，详情区收起或显示主会话摘要。

默认态仍要克制。两三只代理时全显；代理多了再开窗口，别一上来便做重菜单。

### 4. 空闲与流式共用一套上下关系

空闲 composer 走 `AgentPanelProvider`，流式回合又有 `AgentStatusPainter` 与 footer。两条路不能各画一套位置。

- 空闲时：面板在 composer 上方。
- 流式时：面板在 stream footer 上方，正文在更上面。
- 回合从流式转回空闲，不重复打印同一批完成条目，不留下旧状态块。
- 排队消息区若也在 footer 上方，次序定为“代理面板 -> 待发消息 -> 输入框”，或另给明确设计；不可随调用先后互相穿插。

最好抽一份纯布局模型，输入 transcript 锚点、代理行、队列行、composer 行与终端尺寸，吐出各块行号。两个 painter 共用它，不要继续各自猜光标。

### 5. 聚焦、清理与停止要有明白提示

- composer 为空时，上下键进入代理焦点。
- Enter 进入查看态，Esc 先退查看态，再退代理焦点。
- 焦点落在一只运行中代理时，`x` 停止当前代理；落在完成、失败、取消条目时，`x` 把该条从面板清掉。
- 若实现层不肯让同一按键随状态分义，也可把运行态写作“停止”、终态写作“清除”；屏上提示必须跟着当前条目变，不能统称 `clear` 却暗中发 cancel。
- `Ctrl+X`、`Ctrl+K` 做成有时限的两段组合，停止全部运行中代理。第一段按下后要显出“再按 Ctrl+K 确认”；超时、Esc 或别键都撤销，免得误杀。
- `main` 行不接停止或清除。焦点在 main 时，`x` 与停止全部提示须变灰或不出现。
- 正在输入正文时，不抢上下键，不抢 Enter。
- 正在输入正文时，普通字母 `x` 只进 composer，绝不能触发代理操作。组合键也只在面板焦点或明确的全局代理控制态生效。
- 面板首行常驻操作提示。中文、英文各写全：`Enter 查看 · x 停止/清除 · Ctrl+X Ctrl+K 停止全部代理`。不再把提示落到状态栏下面。
- 中英文和 plain 主题都要守住宽度；长单词、宽字符、ANSI 色码不能把边框撑裂。

停止命令须接 `AgentTool` 正式取消接口，等任务线程报出终态后再改灯号。不能只从面板抹掉，留下后台线程照跑。

### 6. 查看态在输入框右上角挂代理短标题

进入某只子代理查看态后，在输入框上横线右端画一枚紧凑标签：

```text
──────────────────────────────────── 修 ask_user 被子代理状态遮挡
>
──────────────────────────────────────────────────────────────────
```

- 标签取任务短述，不取一水相同的 `general-purpose`。代理类型可放 tooltip 不成的终端里，用前缀或颜色辅佐。
- 按终端宽度截断，先保住左侧横线与最小 composer 宽度。窄窗里宁可缩成 `ask_user…`，不可盖住提示符。
- 切换选择时即时换标签；Esc 退查看态、切回 main、任务被清理时摘掉。
- 标签的颜色跟当前代理状态走，或至少与边框分得开；plain 主题只留文字，不夹 ANSI。
- 标签既表明“正在查看这只代理”，也表明 composer 此刻以它为收件人。消息去向必须明写，不能只换颜色却仍把话送给 main。
- 空闲 composer 与流式 footer 若都允许查看代理，两边沿用同一枚标签和截断函数。

### 7. 进入子代理后，composer 能直接介入它

Enter 不是单纯展开一块只读详情。它把交互焦点切进这只子代理。此后用户在带短标题的 composer 里提交消息，话须送进该任务自己的上下文：

```text
main
  └─ subagent #4 正在查调用链
       ↑ 用户补一句：“别改代码，只列证据”
       └─ subagent #4 带着原有 history 继续跑
```

规矩如下：

- 每只运行中任务有独立 inbox。消息按任务 id 投递，不经 main history，不串到别只代理。
- 正常发送先排队，在“当前工具调用收尾、下一次模型请求发出之前”注进该子代理 history。不可打断写文件到一半，也不可丢掉刚产出的 tool result。
- 屏上显示“已排给 subagent #N”，并把尚未送达的介入消息列在这只代理详情里。
- 若要“立刻介入”，另给明确的 interrupt-and-send 操作：停当前流式响应，保住已完成工具结果，再让同一只 `AgentLoop` 接着跑。不能把现有永久 `cancel=true` 直接拿来顶；那会让任务整场退出。
- 任务已进终态时，不得悄悄把消息改投 main。应提示“代理已结束”，让用户清掉、回 main，或另选“续开一只代理”。
- Esc 退出子代理上下文后，composer 右上角标签消失；下一条消息重新归 main。
- 切进另一只代理时，未提交的 composer 草稿要么跟着原目标保存，要么阻止切换并提示。不可把给甲写了一半的话送给乙。
- `/clear`、退出会话、切 worktree 时，逐只处理未送达 inbox。不能无声遗失，也不能跨目录误送。

现有实现没有这条通道：`AgentTool::TaskRecord` 只有 `cancel`；后台线程里的 `sub_loop` 是 `RunTask()` 局部对象；公开接口只有快照、完成回流与 `HasRunningTasks()`。实施时至少要补：

```text
AgentTool::SendTaskMessage(task_id, text, delivery_mode)
TaskRecord::inbox                         // 线程安全队列
AgentLoop::SetInbox(...)                  // 每只 sub_loop 接自己的 poller
AgentTool::CancelTask(task_id)
AgentTool::CancelAllTasks()
AgentTool::ClearFinishedTask(task_id)
```

`AgentLoop` 已有轮次边界收件口，可复用注入规则；但主会话跨会话 inbox 与子代理 inbox 要分清来源、目标和展示文案。别共用一张无类型字符串队列。

## 与现有 bug 单的关系

`todos/ask_user被子代理状态遮挡.todo` 管的是阻塞菜单期间的屏幕所有权。此单管的是平日布局。

两边须合账：

- `ask_user`、工具确认、授权菜单打开时，代理面板暂停重画并让出屏面。
- 菜单结束后，面板在输入框或 footer 上方恢复。
- 单把面板挪上去，不能算遮挡 bug 已修；单暂停 ticker，也不能算此单已完。

## 建议落刀

### 第一步：拆开 above 与 below

给编辑帧加上方行与下方提示两块。把代理面板从 `hint_lines` 搬到上方行。先只支持当前条目数，不改选择语义。

### 第二步：整帧记账

把顶部横线纳入重画，统一算高度、滚屏、擦旧帧、放光标。补终端缩放与条目数增减。

### 第三步：列表窗口

去掉 `TaskSnapshots(8)` 这道无声截断。provider 交出完整轻量快照，renderer 按可用高度裁窗，并明写“另有 N 只”。详情另取，别让每 100ms 刷新都复制全部工具输出。

### 第四步：合流式状态块

让空闲面板与 `AgentStatusPainter` 共用布局规约、条目格式和完成态留存规则。收掉重复 painter 或给两边一只共同 coordinator。

## 验收

- 一只、三只、十六只子代理并行时，面板都在输入框上方；输入框与状态栏始终看得见。
- 十六只任务可逐只选到。屏上没铺开的任务有明确计数，不会无声丢掉第九只。
- 代理从 running 变 done、失败、取消时，旧行擦净，选择不乱跳，composer 光标不动。
- 展开长任务说明与二十次以上工具调用，可滚完详情；退出后回到原选择。
- 列表首行能看见 Enter、`x` 与停止全部的提示。`x` 停当前运行任务或清终态条目；`Ctrl+X Ctrl+K` 经两段确认后停全部，误按第一段不会动任务。
- 进入第二只代理后，输入框右上角显示它的短标题；上下切换、Esc 返回、清掉任务时，标题跟着换或消失。
- 进入运行中的第二只代理，发“只读，不要修改”：消息只进第二只的 history，在下一工具边界生效；main 与其余代理均收不到。
- 子代理正在跑工具时连发两条介入消息，按原顺序送达，不截断工具结果；任务先结束则明确拒收，不改投 main。
- 子代理焦点里留半句草稿再切目标，草稿不串台；Esc 回 main 后新消息只进 main。
- 用户输入长文并软换行时，面板不盖字；输入非空时，上下键仍归 composer。
- 终端从 160 列缩到 80 列、从高窗缩到矮窗，下一帧排版正确，无残影、越界、重复横线。
- 流式输出、排队输入、空闲 composer 三态切换，代理面板一直在输入区上方。
- `ask_user` 和工具确认停十秒，代理 ticker 不插进菜单；退出菜单后在正确位置恢复。
- Windows Terminal、VS Code Terminal、conhost 各手测；plain、管道、重定向不添 ANSI。

## 测试

- 给纯布局函数测 0/1/3/16 只代理、不同终端宽高、不同 composer 高度。
- 给帧 diff 测上方行增减、详情展开收起、终端缩放后旧行清理。
- 给选择窗口测首尾、翻页、任务完成后索引修正、当前任务被清理。
- 给数据 provider 测“运行中全量、完成态收拢、详情按需取”，不再硬截 8 只。
- 给按键状态机测 `x` 在运行态、终态、main、正文输入四种分支；再测停止全部组合键成功、超时、Esc 撤销、错键撤销。
- 给右上角标签测中英文宽字符、超长短述、窄终端、plain/ANSI 截断，断言不盖横线与 composer。
- 给任务 inbox 测并发投递、顺序、工具边界注入、终态拒收、取消竞态与会话退出清场。
- 用两只可阻塞 fake subagent 实测定向介入：给 #2 的消息只出现在 #2 下一次 request，#1 和 main 的 captured requests 都没有。
- 补一只 Windows screen driver，抓屏断言代理行在上横线之上，输入框与状态栏仍在视口内。

## 不做

- 不改子代理调度、并发数、结果回流与 token 口径。
- 不把整个 transcript 塞进面板。
- 不靠增大终端缓冲区掩盖向下生长。
- 不用频繁全屏清屏代替帧 diff。
- 不让每 100ms 面板刷新复制全部工具输出。
- 不照搬别家颜色与文案；只借上下层级和交互规矩。
- 不把“介入子代理”伪装成给 main 发一句“请转告 #N”；消息须直达原任务上下文。
