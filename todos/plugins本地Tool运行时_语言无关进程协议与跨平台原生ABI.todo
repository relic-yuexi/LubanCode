# Plugins 本地 Tool 运行时:语言无关进程协议与跨平台原生 ABI

状态:待做(2026-08-22 用户重新定调:Plugin 用来替本地轻工具绕开 MCP 的
服务、握手与配置负担；保留 Lua，补 Python/任意可执行程序，原生库跨平台)

## 一句话

让一份本地代码直接变成模型工具。插件先交名称、说明与 JSON Schema；模型只
回工具名和 arguments；LubanCode 验参、执行、收结果。无需为一枚加法函数先
搭一台 MCP server，也不把整套 JSON-RPC 生命周期搬进来。

```text
plugin manifest
  name + description + input_schema + runtime + entry
                           │
                           ▼
                    ToolRegistry / prompt
                           │
                  LLM: tool_call(arguments)
                           │
                           ▼
                  PluginRuntime dispatch
              ┌────────────┼──────────────┐
              │            │              │
         embedded Lua   local process   native C ABI
                        Python/Rust/     DLL/SO/dylib
                        C++/Go/Node/...
```

## 用户原意

- Plugin 首先是 Tool，不是另一套聊天协议。
- 一件可信本地函数，拿来便跑。不要为了 `add(a, b)` 另起服务、握手、列工具、
  收通知、管断线。
- 工具 Schema 仍要给模型。模型照普通 function calling 返回参数，宿主把参数
  送到确定的函数或进程入口。
- 语言只是实现细节。Lua、Python、Rust、C、C++ 都能写；模型不必看见
  `language=python` 这类无用信息。
- 本地轻工具可以取代一大批 MCP 用法；真正的远端服务、长活连接与复杂生态
  仍可留 MCP，不能口头宣称一刀全替。

## 先答五个问题

### 一、DLL 在别的平台叫什么

| 平台 | 共享库 | 动态加载 API | 常见构建目标 |
| --- | --- | --- | --- |
| Windows | `.dll` | `LoadLibraryW/GetProcAddress/FreeLibrary` | MSVC/MinGW shared library |
| Linux | `.so` | `dlopen/dlsym/dlclose` | ELF shared object |
| macOS | `.dylib` | `dlopen/dlsym/dlclose` | Mach-O dynamic library |

三者可以共用同一份纯 C ABI 头，二进制不能混用。还要对齐 OS、CPU 架构、调用
约定与 ABI 版本。`x64 Windows DLL` 到 `arm64 macOS` 上压根没有“兼容加载”
一说，插件包须按 target triple 分发各自产物。

### 二、Lua 要不要另装

不用。仓库已经由 CMake 拉 Lua 5.4.7，编成 `lubancode_lua` 静态库，再链进
LubanCode。`.lua` 是解释执行，不要求用户装 Lua，也不要求 Lua 编译器。

插件作者只交脚本。编 LubanCode 本身时才需要 C/C++ 工具链；官方发行包已经
带好内嵌 Lua runtime。

### 三、能不能支持 `.py`

能，但 Python 脚本要有 Python runtime。LubanCode 不该偷偷假定 `python` 一定
存在，也不该启动时擅自 `pip install`。

- manifest 明写 command，如 `python3`、`py -3`、某个 venv 的解释器，或直接
  写带 shebang 的脚本路径。
- `/plugin doctor` 查命令、版本与依赖，缺了便把该插件标成 unavailable。
- 依赖可由插件自带 venv、用户预装，或将来交给受控 managed environment。
  安装依赖会执行第三方代码，必须显式确认并有 lock/hash，不能静默动全局环境。

### 四、C/C++ 源码能不能直接跑

不能凭空跑。`.c/.cpp` 是源码，运行前总要编译、链接。只有三条正路：

1. 插件作者预编成独立可执行文件，走 process runtime。用户机器不需编译器。
2. 预编成 `.dll/.so/.dylib`，走 native-library runtime。仍按平台分别发包。
3. `/plugin build` 在安装时调用 CMake/编译器。用户机器须有工具链；构建脚本
   本身也是代码执行，首版不自动干。

对“一枚本地工具”而言，独立 executable 往往比共享库省心：没有跨 CRT free、
没有宿主 ABI、崩了也不带走主进程。

### 五、Rust 行不行

行。Rust 可编成独立 executable，或 `cdylib` 导出 C ABI。运行预编译 executable
通常不需要安装 Rust；只有构建源码才要 rustc/cargo。故而 Rust 不必在
LubanCode 里另设一种协议，它只是 process/native-library 两种 runtime 的作者
语言。

## Plugin 与 MCP 的边界

### Plugin 能替掉的

- 本地、可信、按调用启动、请求后即可退出的工具。
- 文件转换、格式检查、小型计算、项目脚本、调用现成本机 CLI。
- 只有 tool name/description/schema/call/result，不需资源、prompt 模板、订阅、
  server notification 或跨客户端复用。
- 用户嫌搭 server 太重，只想丢一份脚本或可执行文件进目录。

### MCP 仍有用的

- 已经存在的 MCP 生态与第三方 server。
- 长驻服务、昂贵连接池、数据库 session、浏览器状态、远程服务与认证续期。
- 除 tools 外还要 resources/prompts、能力协商、通知、流式进度或双向请求。
- 一台服务要供多个客户端复用，或必须与宿主地址空间隔开。

故而产品话术定成：“Plugins 是本地 Tool 快路，可替本地轻量 MCP；MCP 留给真
服务。”不能把一套窄协议慢慢添握手、通知、资源、持久连接，末了又造回一份
缩水 MCP。

## 核心定案:三种 runtime，两种为主

### A. `embedded-lua`:最小巧

- 兼容现有一文件一工具 `.lua`。内嵌 Lua，不依赖系统环境，启动快。
- 适合字符串整理、小计算、轻胶水与纯数据变换。
- 同进程，故障域最大：死循环、`os.exit`、内存打爆都能拖死宿主。只给可信
  插件；第三方 Lua 可选走隔离 plugin-host 子进程。
- 现行 `luaL_openlibs` 把 io/os/package 全开，不算沙箱。后续加 profile：
  `pure` 缺省关 `os.execute/io/package.loadlib`，`trusted` 才全开；instruction
  hook 设 CPU/取消检查，自定义 allocator 设内存帽。
- 同一 lua_State 已有 mutex，保住并发子代理不撞栈。此修复须保留。

### B. `process`:默认主路

- 任何能从 stdin 读 JSON、往 stdout 写 JSON 的命令都能挂：Python、Rust、Go、
  Node、Deno、Ruby、PHP、Java、C/C++ executable、shell wrapper 都一样。
- 首版每次 tool call 启一只短命进程。无握手、无常驻 server、无 JSON-RPC。
  进程退出，调用结束。冷启动很贵的插件以后另议 `persistent`，不塞进 v1。
- 进程崩溃只坏本次调用，宿主可看退出码、stderr、超时并杀进程树。它仍有当前
  用户的文件与网络权限，进程隔离不等于安全沙箱。
- language 只作诊断/展示；真正分派只看 `runtime.kind=process + command/args`。

### C. `native-library`:高级兼容路

- Windows 扫 `.dll`，Linux 扫 `.so`，macOS 扫 `.dylib`。底下统一成
  `OpenModule/FindSymbol/CloseModule` 平台抽象。
- 继续用纯 C ABI `luban_plugin_entry`，不导出 C++ 类。C++/Rust/Zig 都可从
  外头导出同一 C ABI。
- 只适合已有原生库、极低延迟或大数据不宜搬进程的场景。不是推荐入门路。
- 同进程加载时，库 constructor/DllMain 在确认工具调用前便可能执行。野指针、
  栈破坏、ABI 错配会掀翻宿主。`trust=native` 必须单独批准并记文件 hash。
- v1 ABI 已有 name/description/schema/execute/free_result。跨平台前先补：
  struct_size、api_min/api_max、plugin id/version、shutdown、capability flags、
  host allocator 或稳定的 buffer/free 契约。

## Schema 的方向不能倒

正确次序：

```text
插件声明 JSON Schema
    -> LubanCode 启动扫描、解析、校验
    -> ToolRegistry 注册
    -> 本轮把 name/description/input_schema 发给模型
    -> 模型回 tool_call {name, arguments}
    -> 宿主再按同一 Schema 验 arguments
    -> 路由到 manifest 中已绑定的 entry
```

- 不能让模型临时送一份 Schema，再据此决定调用哪个本地函数。模型输入不是
  权限来源，也不能指定任意 command/path/entry。
- runtime/language/command/env/timeout/permissions 不进 prompt。模型只看调用
  所需的 name、description 与 input_schema。
- Schema 坏了必须拒绝加载，不能像现有 DLL 路径那样悄悄退成宽 object。宽化
  会让模型乱传参数，也绕过作者本想守的边界。
- 调用前统一跑 JSON Schema 子集验证：required/type/enum/const/min/max/length/
  array/object/additionalProperties。实现层仍须防御性验参，Schema 不是内存安全。

## 为什么不用 `python add.py 1 2` 作主协议

它能演示，却扛不住正经 Schema：

- object、array、null、布尔、带空格字符串与多行文本没法稳映射位置参数。
- optional/default、互斥字段与同名 flag 很快长出第二套 argparse 规矩。
- Windows quoting、POSIX quoting、shell expansion 各有一套坑；若拼 shell 字符串，
  参数里一枚引号便能变命令注入。
- 结构化结果、错误码、图片/文件引用与 stderr 更没处安放。

故而 process v1 走 stdin/stdout JSON。可另做 `argv` convenience adapter，只许
扁平 scalar Schema，并用 argv 数组直传、绝不经 shell；它不是核心合同。

## 本地进程协议 v1

### 请求(stdin，恰好一份 JSON)

```json
{
  "protocol": 1,
  "call_id": "call_01H...",
  "plugin": "local-math",
  "tool": "add",
  "arguments": {"a": 1, "b": 2},
  "context": {
    "cwd": "D:/project"
  }
}
```

- stdin 写完即 close，让脚本可 `json.load(sys.stdin)` 读到 EOF。
- context 首版只给 cwd 与非敏感 call id。会话历史、system prompt、API key、
  全份环境变量一概不送。
- command 与 args 来自已信任 manifest；arguments 永不插进 command string。

### 响应(stdout，恰好一份 JSON)

```json
{
  "protocol": 1,
  "call_id": "call_01H...",
  "ok": true,
  "content": [{"type": "text", "text": "3"}],
  "structured": 3
}
```

失败：

```json
{
  "protocol": 1,
  "call_id": "call_01H...",
  "ok": false,
  "error": {"code": "bad_input", "message": "a must be a number"}
}
```

- stdout 是结果专线。日志只写 stderr。stdout 前后混日志，整次调用判协议错；
  不从一堆字里猜最后一枚 JSON。
- 非零退出码、signal/crash、超时、取消、坏 UTF-8、坏 JSON、call_id 不合、输出
  超限各有稳定宿主错误码。
- `content` 首版认 text；file/image/json 等富结果待 Runtime 的 TurnItem 合同稳定
  后再加。未知 content type 按 capability 拒绝，不静默转字符串。
- structured 可供宿主/前端用，送模型仍须有明确 text 或经过统一序列化，避免
  provider 各自猜对象怎样变 tool_result。

## Manifest v1

推荐一插件一目录：

```text
~/.lubancode/plugins/local-math/
  plugin.json
  add.py
  requirements.lock          # 可选
```

```json
{
  "manifest_version": 1,
  "id": "local-math",
  "version": "1.0.0",
  "language": "python",
  "runtime": {
    "kind": "process",
    "command": "python3",
    "args": ["${plugin_dir}/add.py"],
    "timeout_ms": 30000
  },
  "tools": [
    {
      "name": "add",
      "description": "Add two numbers.",
      "entry": "add",
      "input_schema": {
        "type": "object",
        "properties": {
          "a": {"type": "number"},
          "b": {"type": "number"}
        },
        "required": ["a", "b"],
        "additionalProperties": false
      }
    }
  ],
  "permissions": {
    "cwd": "project",
    "network": false,
    "env": []
  }
}
```

- `id/name/version/language` 有字符集与长度规矩。完整工具名仍为
  `plugin__<id>__<tool>`，重复名在注册前拒绝，绝不让 Registry 留两枚同名。
- 一个 package 可有多件 tools；process 请求带 tool/entry，脚本自行分派。入门
  scaffold 可生成一插件一工具，省得用户手写 router。
- `${plugin_dir}` 由宿主作结构化路径替换，只能出现在 args/env value，不能拼成
  shell。canonical 后须仍位于插件目录，除非 manifest 明确批准外部 executable。
- language 字段只给 `/plugins`、doctor、模板与错误提示。删掉它也能靠 runtime
  执行；绝不把 language 塞进模型 prompt 浪费 token。
- 不靠 import Python 模块、反射 `def __main__()` 来发现 Schema。扫描时 import 会
  执行顶层代码，且注解表达不了完整 JSON Schema。manifest 才是静态真账。

## 零配置与兼容

- 现有 `*.lua` 一文件一工具继续认，视作 legacy embedded-lua manifest。用户
  不用立刻搬家。
- 现有 DLL v1 继续认；跨平台新 ABI 若升版本，loader 明报 legacy/native-v1，
  不静默拿错结构体。
- 给 Python 提供 `lubancode plugin init python`，生成 plugin.json + runner.py +
  单测。便利来自 scaffold，不来自危险反射。
- 可认 executable 旁的 `<name>.tool.json` 作为一文件快捷包；里面仍是完整静态
  manifest，不执行程序作发现。
- 项目级 `<cwd>/.lubancode/plugins/` 将来可开，但首次见到须按 manifest + 文件
  hash 信任。用户目录现有插件也应逐步补 trust 账；“放进目录”就是执行代码，
  不能装作普通配置。

## Python 作者体验

最小 runner 不强迫依赖 SDK：

```python
import json
import sys

def add(args):
    return args["a"] + args["b"]

request = json.load(sys.stdin)
handlers = {"add": add}
try:
    value = handlers[request["tool"]](request["arguments"])
    json.dump({
        "protocol": 1,
        "call_id": request["call_id"],
        "ok": True,
        "content": [{"type": "text", "text": str(value)}],
        "structured": value,
    }, sys.stdout)
except Exception as error:
    json.dump({
        "protocol": 1,
        "call_id": request.get("call_id", ""),
        "ok": False,
        "error": {"code": "execution_failed", "message": str(error)},
    }, sys.stdout)
```

以后可给一个薄 SDK 装饰器减少样板，但 SDK 只能帮收发/分派，不能把 Python
type hint 冒充完整 Schema，也不能成为运行必需依赖。

## 原生插件跨平台布局

一只包可带多平台产物：

```text
native-tools/
  plugin.json
  include/luban_plugin.h
  bin/windows-x64/native_tools.dll
  bin/windows-arm64/native_tools.dll
  bin/linux-x64/libnative_tools.so
  bin/linux-arm64/libnative_tools.so
  bin/macos-x64/libnative_tools.dylib
  bin/macos-arm64/libnative_tools.dylib
```

- 宿主按 OS + arch 精确挑一份。找不到当前 target 就标 unavailable，不能拿相近
  文件试载。
- Linux 处理 RPATH/依赖 `.so`；macOS 处理 install name、codesign/quarantine；
  Windows 处理依赖 DLL 搜索目录。`/plugin doctor` 须把“主库不存在”“依赖缺失”
  “架构不合”“签名/隔离属性拦截”分开说。
- native tool 的函数入参仍是 UTF-8 JSON，结果仍走插件自己 free 的 buffer。
  ABI 不暴露 nlohmann::json、std::string、异常、RTTI 或 C++ allocator。
- plugin module 按 canonical path 幂等加载。现有“main/sub 两张 registry 扫两遍
  不重复 LoadLibrary”的修复须扩到三平台。

## 进程执行与资源边界

- 启进程不用 shell。Windows 传 argv 到 CreateProcess quoting helper；POSIX 用
  posix_spawn/execve。只有 `runtime.kind=shell` 的显式高级模式才进 shell，且须
  额外信任；v1 不做。
- stdin/stdout/stderr 三管分开，异步排水，免得子进程写满 stderr 后双方死锁。
- timeout 到点先温和终止，过 grace period 杀整棵进程树。Windows 用 Job Object；
  POSIX 用 process group。ESC 走同一取消路。
- 输入、stdout、stderr、结构化结果各设字节帽；超限停读并杀进程，不能先吃进
  几百 MiB 再说“超了”。
- cwd 缺省项目根；env 缺省最小继承集（PATH、必要系统变量），密钥只按 allowlist
  显式转交。不能把宿主整份环境连 API key 一锅递给陌生脚本。
- process 插件默认也 `needs_confirm=true`。以后按 manifest 声明 read/write/network
  风险，并与 settings.local 的 hash/trust 账合并；作者自报“只读”不能独自放行。
- OS 进程不是完整 sandbox。真正不可信代码还要容器、AppContainer、sandbox-exec
  替代方案或系统权限隔离，另立安全单。

## Runtime 代码边界

```text
PluginCatalog
  scan package / parse manifest / validate / resolve platform

PluginRuntimeManager
  EmbeddedLuaRuntime
  ProcessPluginRuntime
  NativeLibraryRuntime

PluginToolAdapter : Tool
  name/description/schema/needs_confirm/deferred
  execute -> selected runtime

PluginProcess
  spawn / pipes / timeout / cancel / limits / exit mapping
```

- `PluginToolAdapter` 不再等于 DLL wrapper。它只借 PluginDefinition，执行时委托
  runtime。现有 `PluginTool` 可改名 `NativeLibraryTool` 作迁移桥。
- Catalog 与 runtime 不碰 prompt；ToolRegistry 才把中立定义交给 provider wire。
- Runtime 不写 stdout，不画终端。stderr/log/diagnostic 进 LogSink；工具结果进
  `Tool::Result/TurnItem`。
- 主会话与子代理共享 immutable PluginDefinition/loaded module；每张 registry
  各有轻 adapter。Lua state 与 persistent 资源的并发/寿命由 RuntimeManager
  持有，不靠声明顺序暗保。
- process call 带稳定 call_id，与 turn/item id 对上。超时、取消、崩溃都必须
  发唯一 ItemCompleted。

## 管理面

```text
/plugins
/plugin inspect <id>
/plugin doctor <id>
/plugin test <id> <tool> <json>
/plugin reload <id>
/plugin enable|disable <id>
/plugin init lua|python|process|native
```

- `/plugins` 列 id/version/runtime/language/platform/status/tool count/trust。language
  不给模型，却该给用户看。
- inspect 显示解析后的 command、Schema、权限、来源、hash 与 target，不泄密钥。
- doctor 只探环境，不执行 tool；process 查 command/version，native 查目标/依赖，
  Lua 查脚本载入与 capability。
- test 仍走同一 Schema、确认、timeout 与结果解析，不能另开无防护捷径。
- process/Lua 可在无 active call 时热重载；native library 首版仍建议重启，卸库
  后插件私生线程/静态对象未必真收净。
- install/update/remove、签名与 lockfile 属后续包管理批次。首版仍可投放目录，
  但错误、信任与依赖要看得见。

## 落地次序(一批一枚 commit)

1. **冻结合同**：PluginManifest/Definition/RuntimeKind/ProcessRequest/Response 与错误码；
   JSON parser、Schema 强校验、重复名/坏路径单测先行。
2. **通 process v1**：无 shell spawn、JSON stdin/stdout、stderr、超时、ESC、进程树
   清理、输入输出帽；先拿自带测试 helper 跑三平台。
3. **做 Python scaffold**：init 模板、add 示例、缺 Python/venv/依赖诊断；CI 有
   Python 时跑真脚本，没有则明确 skip。
4. **收现有 Lua**：适配新 Definition/RuntimeManager，legacy `.lua` 不改用法；加
   pure/trusted profile、取消 hook、内存/指令预算。
5. **抽 native loader**：Windows 现实现包进平台接口；Linux 真编 `.so` 真 dlopen，
   macOS 真编 `.dylib` 真 dlopen；三平台共跑 ABI fixture。
6. **升原生 ABI**：struct_size/version range/shutdown/capabilities/buffer 契约；v1
   兼容读取与清楚诊断。
7. **接 Runtime/app-server**：PluginToolAdapter 产中立 item，日志与 stdout 分流，
   Terminal/Web/Tauri 共用同一执行结果。
8. **补管理面与信任**：list/inspect/doctor/test/reload/enable、项目插件 hash 审批、
   settings.local 权限合流。
9. **迁文档与示例**：扩展选择表改成 Lua/process/native/MCP，补 Python、Rust、
   C executable、三平台 native 包示例与排错矩阵。
10. **再议 persistent**：拿 Python 冷启动基准说话。真慢再加有界常驻 host，不能
   未见病便先造握手、心跳与重连。

## 验收

- 用户未安装 Lua，官方 LubanCode 包仍能加载 `.lua` 并调用；构建记录能证实
  Lua 静态链入，不从 PATH 找解释器。
- 同一 `plugin.json` 的 process helper 在 Windows/Linux/macOS 都收到完全相同
  arguments JSON，返回完全相同 Tool::Result。
- Python add 工具输入整数、浮点、负数正常；缺字段、字符串冒充数字在启动
  Schema/调用验参处报清，不靠 Python 栈追到最后。
- nested object/array/null/Unicode/多行字符串走 stdin JSON 不变形；路径含空格、
  中文、引号与 `&;|` 不会变成 shell 命令。
- stdout 混日志、坏 JSON、多枚 JSON、call_id 不合、非零 exit、crash、超时、
  ESC、stderr 塞满、输出超限各有唯一终态，宿主不死、不挂、不留子进程。
- 一枚 Rust/C++ 预编 executable 不要求用户安装 Rust/C++ compiler，能按 process
  合同调用。源码包若没编译，doctor 明说缺产物，不假装能直接执行。
- 同一 C fixture 在 CI 真编成 Windows DLL、Linux SO、macOS dylib，宿主真加载、
  真验 ABI、真调中文输入、真 free 结果、真卸载。
- 错 OS/arch、缺依赖、坏 entry、ABI 不合、Schema 坏、工具重名都只拒目标插件，
  其余插件照挂；诊断不把动态库依赖误报成“没有工具”的主插件。
- main 与多只子代理同拍调用：process 各 call 隔离；同一 Lua state 串行不撞；
  native 按 manifest concurrency policy 串行或由插件自保。Catalog 不重复记账，
  module 不重复加载。
- 模型所见定义不含 language、command、env、path、timeout 等宿主元数据；只有
  稳定工具名、description 与 input_schema。
- 工具总数超阈值时仍走 tool_search 延迟挂载；Plugin 不因新增语言把每轮 prompt
  撑肥。
- `/plugins` 能说清 loaded/unavailable/disabled/untrusted，语言、runtime、路径与
  原因对得上；不再只列“Lua/DLL”。
- 全量测试与三平台 CI 过关；工作树没有靠本机 Python/Lua/编译器偶然存在才绿
  的隐形前提。

## 与现有实现的关系

- 现有 `src/tools/lua_tool.*`、内嵌 Lua、每 state mutex、稳定文件排序都保留。
- 现有 `include/luban_plugin.h` 与 Windows `PluginHost` 是 native-library v1，
  不是废代码；先包 adapter，再跨平台、升 ABI。
- 当前工作树正有 PluginHost 按路径幂等、Lua 并发串行等修补。本单不得回退或
  覆盖这些改动；实施时以合入后的最新代码为准。
- `ToolRegistry`、Hooks、确认、tool_search、session 结果回填照旧共用。Plugin
  只补发现与执行，不复制 AgentLoop。
- MCP 代码不删。等 process plugin 真跑稳、迁移文档齐了，再逐个判断哪些内置
  本地 MCP 示例可降成 Plugin。

## 不做

- 不把 `language` 当 runtime。Python 也能冻成 executable；Rust 也能出 dylib。
- 不扫描任意 `.py/.cpp/.rs` 后靠 import/编译猜 Schema。静态 manifest 才是门。
- 不用位置参数和 shell 字符串承接通用 JSON Schema。
- 不在每次调用时 pip install/cargo build/cmake build。执行与构建分家。
- 不把 process isolation 吹成 sandbox，也不把 native 确认框吹成崩溃防护。
- 不为“取代 MCP”重新实现 resources/prompts/notifications/handshake/reconnect。
- 不一上来做插件市场。协议、故障边界、三平台测试与信任账先站稳。
