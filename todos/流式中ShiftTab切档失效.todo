# 流式中 Shift+Tab 切档失效

状态：待做（bug）

## 现象

模型流式输出期间按 Shift+Tab，确认档（confirm / auto / yolo）纹丝不动；空闲时输入框里按则一切照旧。用户原话「shift+tab 好像失效了，目前是在流式中」。

## 现状怎么铺的

- 空闲路：`ReadLineKeyByKey` → `LineEditorCore::HandleKey`（`src/cli/line_editor.cpp:711`）认 `KeyKind::ShiftTab`，切档后 `mode_changed` 置位，`RedrawEditArea` 用新档重画状态行。这条路是好的。
- 流式路：按键全被 `TurnInputListener::ThreadMain`（`src/cli/console_input.cpp`）接走。它只认 Esc / Ctrl+C / Ctrl+O / Enter / Backspace / Char / Up / Down / Delete——`PK::ShiftTab` 落进末尾那句「其余按键不理会」，活活吞掉。翻旧版（e83e1d3 之前）也是同样清单，所以流式期间从来就没认过 Shift+Tab——不是新近改坏，是一直缺；只是现在流式期间可按键的事多了（排队、取回改写），用户自然指望档位也能切。

## 怎么做

- `ThreadMain` 补一个 `PK::ShiftTab` 分支：与空闲路同一套切档语义——调 `SharedEditor()` 的切档（或提一枚共用的纯函数，别两处各写一份顺序表），拿到新档后：
  - 流式 footer 的状态行按新档重画（`RedrawStreamFooterLocked` 本就每帧带 `PrintStatusLine`，改 `FooterSlot` 里的档位来源即可）；
  - 打一行淡色提示「已切到 auto 档」让用户知道生效了（对齐空闲路 M11 的取舍：原地更新，不铺新行残骸——能只动状态行就不打提示行，视觉验证为准）。
- 档位来源统一：空闲路状态行读 `editor.confirm_mode()`，流式路现在拿的是 `SharedEditor().confirm_mode()`（`RedrawStreamFooterLocked` 里现成）——切完档下一帧自然带出，别另记账。
- Tab 键不跟着做：空闲路 Tab 是补全/选择，流式输入行首版不引入补全交互，维持吞掉。

## 验收

- 流式中按 Shift+Tab：状态行档位段当场换色换字（confirm 默认色 / auto 淡色 / yolo 红），下一轮工具确认按新档走。
- 连按循环切档不铺残骸行；排队消息、待发区浏览、Esc 打断互不干扰。
- 空闲路行为不变（回归）。
- 单测：切档纯函数（若有）+ listener 键位分派的纯逻辑可测部分；真终端手测流式切档。

## 不做

- 不给流式输入行做 Tab 补全交互。
- 不改 Ctrl+C / Esc 的打断语义。
