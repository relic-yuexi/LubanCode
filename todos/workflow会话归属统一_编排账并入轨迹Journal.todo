# workflow 会话归属统一：编排账并入轨迹 Journal

- 状态：已实施（2026-09-04 立单，同日落地；P0-6 发布门工头收官时点名的遗留——"ReserveWorkflowRun/ReserveWorkflowNodeStream 至今无生产消费方，workflow 节点的模型请求不落轨迹 Journal，编排账在 workflow-runs/ 旧路——须另立单"）
- 级别：P1（轨迹完整性合同：每个执行单元只认一本账）
- 类型：LubanCode 产品改进（trajectory 完整性）
- 关联：《P0新轨迹记录_可重放与训练投影设计.todo》（P0-6 已收官，本单是其 §一定案"每场 workflow 有自己的编排目录与 Journal；workflow 中真正执行的 node attempt 各写一份 node JSONL"的落地尾巴）
- 现状：workflow 跑动时编排账落 `<home>/.lubancode/workflow-runs/<run>/{definition,manifest,events,checkpoints}`（旧路），main.jsonl 只落 `control.command.*` 边——**workflow 节点内的模型请求/工具调用不进任何 Trajectory Journal**；`ReserveWorkflowRun/ReserveWorkflowNodeStream`（trajectory directory 已备的口）无消费方。
- 修法：
  - workflow run 起动时经 `ReserveWorkflowRun` 开编排 Journal、每个 node attempt 经 `ReserveWorkflowNodeStream` 开 node stream（复用 P0-C 的延迟开卷/独占创建/fail closed 全套合同）
  - 节点内模型请求/工具事件落各自 node Journal（与子代理同款 ownership：声明才进、无主拒）
  - workflow-runs/ 旧路保留为兼容读（或迁移窗后退役，按既有迁移纪律）
  - parallel/loop/retry 长跑场景验证账不裂
- 验收：workflow 场景 `trajectory verify` 全过（编排账+node 账+main 边引用一致）；旧 workflow-runs 数据可读不迁移不炸；单测钉 reserve→consume 全链与 fail closed。

## 实施账（2026-09-04）

- [x] trajectory 合同层：`workflow.*` 编排事件族十二枚入册（definition.loaded/node.dispatched|retrying|waiting|completed|failed|skipped/branch.started/join.completed/loop.iteration.*/checkpoint.saved，全 Control 面、三档 id 全禁）；`run.started` 补 workflow 首行身份键（workflow_run_id/workflow_id/node_id/node_kind/attempt/map_item_index/loop_iteration/branch_path，§3.6"node JSONL 首行带"的落法；旧读者按未知字段拒，与超前版本同待遇）。（`src/trajectory/event.hpp`、`event.cpp`、`schema.cpp`；verifier 的 node 终态接受/harvest 在 `replay.cpp`——workflow.node.completed|failed 带 relations.child_run_id 算一次接受，`child_terminal_event_hash` 逐位对账）
- [x] 账本消费方：`TrajectorySessionLedger::SpawnWorkflowRun`（ReserveWorkflowRun + definition.json 快照 + 独立 recorder run_kind=workflow + run.started 带 parent_run_id=main，后台派工语义）与 `TrajectoryWorkflowRunBridge/NodeBridge`（编排事实口 + node attempt 账口，node 首行身份齐全；exotic 节点名走 sha 内容寻址文件名）。（`src/runtime/trajectory_session.hpp/.cpp`）
- [x] 执行器挂账：`RuntimeOptions.trajectory_ledger` 接线——run 起动开编排账（fail closed：开不出账 run 停明确失败态）；每 attempt 先开 node stream 再落派发事实（子文件在场才挂 child 边，免孤儿边）；agent 节点把轮桥接进 TurnWiring（boundary/trace/results 三口，ownership 同款），llm/skill 节点挂 SampleOptions.boundary_recorder（prepared 记不住停发模型，§7.4）；node_run_id 加 `-d<派发序号>`（loop 重入同 attempt 撞名会炸独占创建，map 的 -i 路号保留）。旧 workflow-runs/ 路照写照读不迁移。（`src/workflow/runtime.hpp/.cpp`、`host_executors.cpp`、`src/app/commands/workflow_commands.hpp/.cpp`）
- [x] 验收一：workflow 场景 verify 全过——真账本 × 假执行器跑顺序/parallel+map/loop+retry 四景，`VerifySession` 全绿、父子边逐位对账、重试新开文件、loop 重入新派发号。（`tests/unit/workflows/test_workflow_trajectory_runtime.cpp`）
- [x] 验收二：reserve→consume 全链与 fail closed 单测——七场：全链/无主拒/node 账开张失败（无 0 字节残留、无孤儿边）/编排账开张失败/retry 两文件/旧 workflow-runs 兼容读/hash 假账负例（edge.child_hash_mismatch）。（`tests/unit/trajectory/test_workflow_run_ledger.cpp`）
- [x] 真机探针扩证：`p06_workflow_console_probe.py` 起，session 内 `workflows/<run>/{workflow.jsonl,nodes/}` 在场、编排账与各 node 账收口齐全成为硬门槛（取证 + result.txt 判定，不再只落 verify.txt）。
- 留白：subflow 嵌套层的编排账未接（现状 subflow 本就不落 workflow-runs，另立单）；tool 节点宿主合成调用不进 tool.* 事件（ownership 门：声明才进；编排账 node.failed/completed 携带 code/duration 承载事实）；`workflow.store.patched` 与 `workflow.node.ready` 未入册（store 快照经 checkpoint.saved 承载，ready 与 dispatched 合并）；resume 场新开编排账（旧场 workflow-runs 照读，不回写旧 run 的轨迹账）。
