# SearchTool 内置 Ripgrep 后端迁移设计

- 状态：待实现（设计单；不代表当前 LubanCode 已随包携带或调用 ripgrep）
- 日期：2026-08-31
- 级别：P0（核心只读工具的速度、正确性、取消与发行完整性）
- 范围：`src/tools/search.*`、`src/platform/process.*`、观察边界、工具文案、安装脚本、Release 包、三平台 CI、第三方许可证、性能基准
- 依赖：`ToolExecutionContext.cancel`、`platform::ChildProcess`、`ObservationBoundary`、`Tool::Result`、现有 `search` grep/glob 合同
- 不在本单：让模型直接拼 shell 命令、把 `web_search`/`tool_search` 混进来、做常驻搜索 daemon、在 C++ 里重写 regex/ignore/SIMD、运行时联网下载依赖

## 一、先下断语

`search` 工具不撤。撤的是它肚里那套自研搜索内核。

模型仍只看一枚 `search`。路径、权限、观察边界、取消、输出上限与错误合同，仍归 LubanCode。内容搜索和文件枚举交给随发行包固定携带的 `rg`：

```text
model
  -> SearchTool
      -> ParseSearchRequest
      -> BuildSearchPolicy
      -> LocateBundledRipgrep
      -> RipgrepRunner(ChildProcess)
          -> grep: rg --json
          -> glob: rg --files --null
      -> Parse/limit/sanitize
      -> Tool::Result
```

不走两条平级生产路径。不让模型在“内置 search”与“run_command 调 rg”之间猜。不在 `rg` 缺件、坏件、版本不合时静默退回慢内核。

> SearchTool 管产品合同，ripgrep 管搜索算法。

这不是认输后把产品边界一并交出去。恰好相反：算法用成熟件，宿主把安全、语义、诊断与发行攥紧。

## 二、当前真相

### 2.1 现有实现为何慢

当前 `src/tools/search.cpp` 自己做了这些事：

1. `std::filesystem::recursive_directory_iterator` 单线程走目录树。
2. 每个文本文件由 C++ 流逐行读取。
3. 正则走 `std::regex(ECMAScript)`。
4. Glob 自己匹配。
5. 二进制、跳过目录、100 条截断都在工具内手写。

工作树里又试过纯文本 `find` 快路、单次开文件、状态表 Glob 与取消检查。这能削掉几笔固定成本，却碰不到根病：

- 大目录遍历仍是单线程；
- 没有 ripgrep 的 ignore walker；
- 复杂正则仍落 `std::regex`；
- 没有成熟的 literal extraction、自动机、SIMD 与并行文件扫描；
- 若继续补，最终会在 C++ 里另写半个 ripgrep。

故而这批试验只作差分基线。新后端过门前留着；过门后整段删掉，不演成永久 fallback。

### 2.2 可复用底座已经齐

`src/platform/process.*` 已有跨平台子进程底座：

- Windows：`CreateProcessW`、独立管道、Job Object，关停可收进程树。
- POSIX：独立进程组，关停走 `SIGTERM -> SIGKILL`。
- `ChildProcess` 分开读 stdout/stderr，原始字节按块交回调。
- `ToolExecutionContext.cancel` 已能把主轮 ESC、子任务取消与墙钟信号递进工具。
- `ObservationBoundary` 已识别 `.evidence` 与运行时登记的子代理日志目录。

本单不另造第二套进程执行器。`RipgrepRunner` 只在 `ChildProcess` 上做一层窄封装。

### 2.3 `RunProcess` 不适合这条路

`RunProcess` 等进程退出才交完整输出。`rg --max-count 100` 又是“每个文件最多 100 条”，不是全局 100 条。

若拿 `RunProcess`，宿主只能等搜索跑完再截文本。结果虽短，I/O 一点没省。正确做法是：

1. `ChildProcess` 流式读 stdout。
2. grep 按 JSON Lines 分帧；glob 按 NUL 分帧。
3. 收满全局 100 条，立即标 `stopped_at_limit=true`。
4. 调 `Shutdown()` 收整棵进程树。
5. 这次非零退出不算搜索失败；宿主知道进程是自己为截断而停。

## 三、外部依据与版本裁决

核对日期：2026-08-31。

官方资料：

- 仓库与功能说明：https://github.com/BurntSushi/ripgrep
- 用户指南与忽略语义：https://github.com/BurntSushi/ripgrep/blob/master/GUIDE.md
- 发行页：https://github.com/BurntSushi/ripgrep/releases
- 正则语法入口：https://docs.rs/regex/latest/regex/#syntax
- 许可证：上游 README 明示 MIT 或 UNLICENSE 双许可；LubanCode 选 MIT 路分发。

### 3.1 版本不追 latest，钉死 manifest

首版候选钉 `ripgrep 15.2.0`，不是运行时追“最新版”。仓库新增：

```text
third_party/ripgrep/manifest.json
third_party/ripgrep/LICENSE-MIT
THIRD_PARTY_NOTICES.md
```

`manifest.json` 至少记：

```json
{
  "name": "ripgrep",
  "version": "15.2.0",
  "license": "MIT",
  "upstream": "https://github.com/BurntSushi/ripgrep",
  "assets": {
    "windows-x64": {
      "archive": "ripgrep-15.2.0-x86_64-pc-windows-msvc.zip",
      "sha256": "71b2fef860abe467217a538ff31de02f5258807c0129f771846f87bd029aafc5",
      "member": "ripgrep-15.2.0-x86_64-pc-windows-msvc/rg.exe"
    },
    "macos-arm64": {
      "archive": "ripgrep-15.2.0-aarch64-apple-darwin.tar.gz",
      "sha256": "3750b2e93f37e0c692657da574d7019a101c0084da05a790c83fd335bad973e4",
      "member": "ripgrep-15.2.0-aarch64-apple-darwin/rg"
    },
    "linux-x64": {
      "archive": "ripgrep_15.2.0-1_amd64.deb",
      "sha256": "IMPLEMENTATION_GATE_FILL_FROM_UPSTREAM_SHA256_ASSET",
      "member": "usr/bin/rg"
    }
  }
}
```

最后一格不许带占位发布。实现时从上游同版 `.sha256` 资产核出，写进仓库，再由 CI 重算；不在流水线里同时下载 archive 与 checksum 后便自称供应链已钉住。

### 3.2 Linux 不直接拿 15.2.0 x64 musl 包

上游 `15.2.0` 的 `x86_64-unknown-linux-musl` 预编译包已有“超大目录高并发偶发 SIGSEGV”的公开报告（BurntSushi/ripgrep#3494）。本仓 Release 本来就在 Ubuntu 构建，产物也依赖 glibc，故首版优先取上游 amd64 `.deb` 内的 GNU/glibc `usr/bin/rg`。

Linux 资产门：

- [ ] 在 `ubuntu-latest` 解 `.deb`，运行 `rg --version`。
- [ ] 跑本单大树压力夹具至少 100 轮，不崩、不挂。
- [ ] 用 `ldd` 记录运行库；不得添出 runner 镜像没有、目标 Ubuntu 常规环境也没有的私有依赖。
- [ ] 若 `.deb` 资产不宜再分发，改为 Release job 用固定 Rust toolchain 从 `15.2.0` tag 构建 GNU 版；仍产哈希、仍过同一压力门。
- [ ] musl 方案只有上游问题有结论并经本仓压力实测后才可回来，不能因包小便越门。

### 3.3 许可证与署名

- [ ] 选择 MIT 许可，不混写“MIT/Unlicense，随便算”。
- [ ] 原样带 `third_party/ripgrep/LICENSE-MIT`。
- [ ] `THIRD_PARTY_NOTICES.md` 写组件名、版本、上游地址、所选许可证与打包位置。
- [ ] Release 包同时带 LubanCode `LICENSE` 与第三方 notices/license。
- [ ] `scripts/check_release.sh` 查版本、manifest、license 文件与 Release 包布局。
- [ ] README 的“一份原生二进制”改成“一套免安装原生发行包”；不能明明多了 `libexec/rg` 还说只有一枚文件。

## 四、不可破的合同

### 4.1 工具身份不变

- 名字仍是 `search`。
- `EffectClass` 仍是 `ReadOnlyLocal`。
- `Idempotency` 仍是 `Idempotent`。
- 模型不看 `rg` 路径，不看 shell，不填任意 argv。
- `web_search`、`tool_search` 与历史搜索均不受影响。

### 4.2 路径与权限仍归宿主

1. `path` 先由 LubanCode 解析、验存在、判目录或单文件。
2. 不把 `cd` 拼进命令字符串。`ChildProcess::Start(..., cwd_utf8)` 传工作目录。
3. argv 数组直起进程，不经过 `cmd.exe`、PowerShell 或 `/bin/sh`。
4. pattern、glob、path 前都用参数边界；开头是 `-` 也不能变成新 flag。
5. 搜索根外的路径不由 rg 自己扩张。将来若加工作区 scope gate，也加在 SearchTool，不塞进命令文本。

### 4.3 生产只认随包二进制

生产定位顺序只有一条：

```text
ExecutableDir()/libexec/rg.exe   # Windows
ExecutableDir()/libexec/rg       # Linux/macOS
```

明令禁止：

- 找不到便去 `PATH` 碰运气；
- 读 `LUBANCODE_RG_PATH` 后执行任意文件；
- 运行时下载；
- 悄悄调用系统包管理器；
- 缺件时退回自研内核。

理由很硬：`search` 是免确认只读工具。若它能从 `PATH` 或环境变量捡一枚任意程序，便成了不经确认的代码执行口。

测试与开发不靠环境变量开洞：

- 单测向 `SearchTool` 构造器注入 `IRipgrepRunner` 假件。
- 集成测试由 CMake 把已校验的 rg 复制进测试运行目录 `libexec/`。
- 源码开发若缺资产，configure 明报怎样获取测试依赖；不联网则跳过真实 rg 集成组，但纯单测照跑。

### 4.4 用户配置不得暗改工具

每次调用都带 `--no-config`。这样即便父进程有 `RIPGREP_CONFIG_PATH`，用户的颜色、PCRE2、preprocessor、编码、额外 glob 也进不来。

首版不开放这些 rg 能力：

- `--pre` / `--pre-glob`；
- `--search-zip`；
- `--follow`；
- `--engine pcre2` / `--auto-hybrid-regex`；
- 任意 `--type-add`；
- 任意外部 ignore 文件路径。

它们有的会起外部程序，有的会扩大搜索面，有的会改正则复杂度。要开，另立合同。

## 五、模型可见语义

### 5.1 grep 模式

保留：

```json
{
  "mode": "grep",
  "pattern": "SearchTool",
  "path": "src",
  "glob": "**/*.cpp"
}
```

首版可新增一枚可选参数：

```json
"fixed_strings": false
```

- `false`：pattern 按 ripgrep 默认 Rust regex 语法。
- `true`：传 `--fixed-strings`，pattern 每字按字面搜。
- 不新增任意 flags 数组。

输出仍投影成：

```text
tools/search.cpp:332:std::string SearchTool::name() const {
```

100 条上限按“命中行”计。JSON `match` event 一条算一条；同一行含多个 submatch 仍只算一条，与旧工具“返回命中行”口径一致。

### 5.2 glob 模式

保留：

```json
{
  "mode": "glob",
  "pattern": "**/*.cpp",
  "path": "src"
}
```

实现走：

```text
rg --no-config --files --null ... -g <positive-glob> -- <scope>
```

`--files --json` 实测仍吐普通路径，不是 JSON。因此 glob 模式必须用 `--null`，按 NUL 分帧；不能按换行拆，POSIX 文件名可含换行。

用户 glob 只作正向过滤。若 pattern 首字符是 `!`，builder 必须按字面转义，不能让它偷变成 rg 排除规则。Glob 语法从现有简版迁到 ripgrep globset 语法，需在中英文参数说明与 CHANGELOG 明写。

### 5.3 隐藏文件与 ignore 裁决

默认策略：

- 传 `--hidden`，让 `.github`、`.clang-format` 等项目文件仍可被搜。
- 遵守 `.gitignore`、`.ignore`、`.rgignore` 与 Git exclude。
- 不跟符号链接。
- 跳过二进制文件。
- 另加宿主硬排除：`.git/`、`build/`、`node_modules/`、`.evidence/`。

这与旧工具“隐藏文件能搜、但不读 ignore 文件”有一处有意迁移：**被 ignore 的文件默认不再搜。** 理由是代码搜索应避开生成物、依赖与本地密钥；也是 ripgrep 速度的重要组成。

显式点名规则照旧：

- `path` 是某个单文件，即便它隐藏、ignored 或落观察边界，也照常搜；观察边界先给体积提示。
- `path` 逐字点到 `build/`、`.evidence/` 等目录，视为用户明确缩小根；不再把根自身排掉。
- 默认从上层目录递归时，硬排除才生效。

不得用一组死 `-g` 便草草冒充这套规则。`SearchPolicyBuilder` 要看“搜索根是不是边界本身”，再生成相对排除项。

### 5.4 观察边界迁移

`.evidence` 可直接变成排除 glob。运行时登记的任意日志目录还需多一步：

1. 给 `ObservationBoundary` 增线程安全的 `ExcludedDirsSnapshot()`。
2. 只取落在本次 root 之下的目录。
3. 转成 root-relative UTF-8 路径，统一 `/`。
4. 用独立 helper 按 rg globset 规则转义元字符。
5. 生 `-g !<relative-dir>/**`，让 walker 真正剪枝。
6. root 本身在观察边界内时不生排除，改走显式读取提示。

不能只在结果解析后丢掉日志命中。那样 rg 仍会把巨型自生日志读一遍，性能病还在。

## 六、内部结构

### 6.1 请求与策略分家

建议类型：

```cpp
enum class SearchMode { Grep, Glob };

struct SearchRequest {
    SearchMode mode;
    std::string pattern;
    std::filesystem::path root;
    std::string glob;
    bool fixed_strings = false;
    std::size_t max_results = 100;
};

struct SearchPolicy {
    bool include_hidden = true;
    bool respect_ignore_files = true;
    bool follow_symlinks = false;
    bool search_binary = false;
    std::vector<std::string> exclude_globs;
};
```

`ParseSearchRequest()` 只管 schema 与路径。`BuildSearchPolicy()` 只管观察边界、硬排除与显式点名。`BuildRipgrepArgs()` 是纯函数，只收 typed request/policy，不再读全局状态。

### 6.2 runner 接口

```cpp
struct RipgrepRunResult {
    std::vector<SearchHit> hits;
    bool truncated = false;
    bool cancelled = false;
    bool timed_out = false;
    int exit_code = 0;
    std::string stderr_text;
    std::optional<RipgrepStats> stats;
};

class IRipgrepRunner {
public:
    virtual ~IRipgrepRunner() = default;
    virtual std::expected<RipgrepRunResult, SearchBackendError>
    Run(const SearchRequest&, const SearchPolicy&, const ToolExecutionContext&) = 0;
};
```

`SearchTool` 默认持有生产 `BundledRipgrepRunner`；单测注入 fake。不要让工具直接 new `ChildProcess`，不然参数、解析、进程与展示又揉回一档。

### 6.3 grep argv

基线形状：

```text
<exe>/libexec/rg
  --no-config
  --json
  --line-buffered
  --color=never
  --hidden
  --engine=default
  --no-multiline
  [--fixed-strings]
  [-g <user include glob>]
  [-g <host exclusion>]...
  --
  <pattern>
  <scope>
```

目录 root：`cwd=root`，`scope=.`。单文件 root：`cwd=parent`，`scope=filename`。这样输出自然接近旧合同；parser 再剥掉开头 `./` 或 `.\`。

不要加 `--pcre2`。默认引擎的线性时间边界正是本次迁移要买来的东西；look-around/backreference 需另开明确模式。

### 6.4 glob argv

```text
<exe>/libexec/rg
  --no-config
  --files
  --null
  --hidden
  -g <user positive glob>
  [-g <host exclusion>]...
  --
  <scope>
```

同样按 root 设置 cwd。glob stdout 是原始路径字节，不经过终端着色或 locale 格式化。

### 6.5 JSON Lines 解析

grep parser 只认上游稳定事件：

- `begin`：可忽略或记当前文件。
- `match`：抽 `path`、`lines`、`line_number`；生成一条 `SearchHit`。
- `end`：可忽略。
- `summary`：抽 elapsed、searches、matched_lines 等诊断，不拿它改命中正文。

`path`、`lines` 可能是：

```json
{"text":"..."}
```

也可能在非 UTF-8 输入上是：

```json
{"bytes":"<base64>"}
```

两路都要测。bytes 解码后过 `SanitizeExternalText`，不可把非法字节直接塞进 JSON/history。

分帧器要守：

- stdout 未完成帧上限：1 MiB；越线杀树，报 `search_backend_protocol_error`。
- stderr 捕获上限：64 KiB；超出截断并注明。
- 单条模型可见行上限：16 KiB；超长行截断，不许一条命中吃完整个上下文。
- 总结果正文上限：512 KiB；它与 100 条上限谁先到，谁触发主动收树。
- 最后一帧无换行也要处理；坏 JSON 不吞掉装作无命中。

### 6.6 退出码与终态

ripgrep 合同：

| 情形 | rg 退出 | SearchTool 终态 |
|---|---:|---|
| 有命中 | 0 | success |
| 无命中 | 1 | success，回“没搜到” |
| regex/参数/IO 硬错 | 2 或其他 | error，带清洗后的 stderr 摘要 |
| 宿主收满 100 条后停 | 非零/信号 | success + truncated |
| 用户/父任务取消 | 非零/信号 | cancelled error，不冒充无命中 |
| 墙钟超时 | 非零/信号 | timeout error |
| stdout 协议坏/超限 | 非零/信号 | protocol error |
| 起不来/缺文件 | 无退出码 | backend missing/spawn failed |

主动截断、取消、超时三者各有一枚本地原因旗。不可只看进程退出码猜。

### 6.7 并发与生命周期

- 每次工具调用起一枚短命 rg；不做常驻 daemon。
- stdout 与 stderr 回调在不同线程；各自只写受锁状态，或各守一份缓冲。
- JSON parser 只在 stdout 读线程推进，保证事件顺序。
- 主线程用 `ChildProcess::IsAlive()` + 条件变量有界等，不忙转。
- 命中上限由 stdout 线程置 stop reason；真正 `Shutdown()` 由唯一 owner 做，避免读线程自 join。
- 析构前收读线程。任何异常路都不得留下 rg 进程。

若 `ChildProcess` 缺一个干净的有界 `Wait()`，优先给平台抽象补通用 `WaitForExit(timeout, cancel)`；不可在 SearchTool 里复制 Windows/POSIX 等待代码。

## 七、错误、诊断与用户文案

### 7.1 稳定错误码

建议新增：

```text
search_backend_missing
search_backend_version_mismatch
search_backend_spawn_failed
search_backend_protocol_error
search_pattern_invalid
search_cancelled
search_timeout
search_output_limit
```

regex 解析错要把 rg stderr 洗成短句，同时保留 `error_code=search_pattern_invalid`。不能把整条内部 argv、安装路径或任意环境吐给模型。

### 7.2 doctor

`/doctor` 或 `/tools` 诊断至少能看：

```text
search backend: bundled ripgrep 15.2.0
path: <exe>/libexec/rg(.exe)
status: ready | missing | version_mismatch | smoke_failed
regex: Rust regex (default engine)
ignore files: on
```

路径可显示，hash 可缩写。不得把 PATH 候选列一屏；生产根本不查 PATH。

### 7.3 中英文模型文案

同步修改：

- `src/prompts/tools/zh-CN/search.md`
- `src/prompts/tools/en/search.md`
- `SearchTool::description()` fallback
- `tests/unit/tools/test_tool_text.cpp`

文案要说清：

- regex 已是 ripgrep/Rust regex，不再写 ECMAScript；
- 默认尊重 ignore 文件；
- 隐藏项目文件仍搜；
- `fixed_strings=true` 的用途；
- 显式 path 可搜 ignored/观察区；
- 100 条与长行截断。

## 八、发行与安装

### 8.1 包布局

三平台统一：

```text
lubancode/
  lubancode[.exe]
  libexec/
    rg[.exe]
  licenses/
    ripgrep-MIT.txt
  THIRD_PARTY_NOTICES.md
  LICENSE
  README.md
  README.en.md
  docs/
  skills/
  install.*
```

不把 rg 放包根，免得用户误以为 LubanCode 安装器还负责给全系统装 `rg` 命令。`libexec` 不进 PATH。

### 8.2 Release workflow

`.github/workflows/release.yml` 增阶段：

1. 从 committed manifest 读当前矩阵 asset。
2. 下载固定 URL。
3. 重算 committed SHA-256，不合便停。
4. 只解 manifest 指定 member，防 archive 路径穿越与“随手解全包”。
5. 复制到 `dist/lubancode/libexec/rg[.exe]`。
6. Linux/macOS `chmod +x`。
7. 跑 `rg --version`，首行须精确 `ripgrep 15.2.0`。
8. 跑最小 smoke：中文、regex、glob、ignore、无命中退出 1。
9. 打包后重新解 archive，再跑一次包内 `lubancode --version` 与 `libexec/rg --version`。
10. 查 notices/license 都在。

下载与解包脚本宜放 `scripts/fetch_ripgrep.py` 或平台无关 CMake script。它只读 manifest，不问 GitHub API 的 latest。

### 8.3 安装脚本

当前 Windows 安装器会同步 `skills/`、`docs/`，不会自动带新目录。要补：

- `scripts/install.ps1`：原子同步 `libexec/`、`licenses/`、`THIRD_PARTY_NOTICES.md`。
- `scripts/install.sh`：同样同步，并守住执行位。
- `scripts/uninstall.ps1` / `install.sh` 对应删除安装目录内这些官方件；不碰用户别处的 rg。
- 更新安装时先放 staging，再整体换入；不能先删旧 rg，后下载失败留半套。
- `scripts/tests/install.tests.ps1` 与 shell 测试补旧版升级、缺目录、陈旧 rg 替换、用户旁文件不删。

### 8.4 开发构建

两种构建档：

- `LUBANCODE_BUNDLED_RG_DIR=<validated staging>`：CI/Release 明传，配置时校版本，构建后复制。
- 普通本地构建：若仓库缓存目录已有 manifest 对应资产便复制；没有则原生单测照跑，真实 rg 集成测试标 SKIP，并在启动 search 时明确报缺件。

不让普通 `cmake --build` 暗中联网。依赖获取要显式、可缓存、可校验。

## 九、迁移批次

### P0-1：冻结旧合同与差分语料

- [x] 把现有 `tests/unit/tools/test_search.cpp` 用例按“必须保留/有意迁移”分栏。→ A 类 9 条／B 类 8 条，表见 `docs/development/search-ripgrep-migration.md` §一。
- [x] 建 `tests/fixtures/search/`：中文、CRLF、空文件、二进制、超长行、100+ 命中、隐藏文件、ignored 文件、`.git/build/node_modules/.evidence`、嵌套 glob、非法 UTF-8 文件名/正文（POSIX）。→ 清单与构造理由见 `tests/fixtures/search/README.md`；非法 UTF-8 文件名 Windows worktree 下如实标注不可测（含本机 MSYS 实测现象），脚本已交付给 POSIX 环境用。
- [x] 记录旧内核输出 golden；不把 filesystem 遍历顺序钉成合同，比较路径集合与行集合。→ `tests/manual/search_golden_driver.cpp`（golden 子命令）产出 `tests/fixtures/search/golden/old_kernel_golden.json`，24 条场景，按排序后的命中集合 + sha256 摘要比对，不钉行序。
- [x] 给 ECMAScript 与 Rust regex 差异列迁移表：look-around、backreference、Unicode class、转义、非法模式。→ `docs/development/search-ripgrep-migration.md` §四，本批不联网核实，标注了哪些是稳定架构事实、哪些“待 P0-5 真机差分核实”。
- [x] 决定 `fixed_strings` schema；中英文一起审。→ 采纳原建议形状不变（`{"fixed_strings": false}`），理由与中英文文案草稿见 `docs/development/search-ripgrep-migration.md` §五。
- [x] 跑当前内核基准，留 raw JSON/CSV，不只写一行“慢”。→ `tests/manual/search_golden_driver.cpp`（bench 子命令）产出 `tests/fixtures/search/bench/old_kernel_bench_src.json`（本仓 `src/` 768 文件，5 类查询各 7 轮 P50/P95）；结构性发现（命中密度决定截断早晚，进而决定耗时差一到两个数量级）见 `docs/development/search-ripgrep-migration.md` §六。内存占用因 Windows psapi.h 编译问题本批未采集，留白交 P0-7。

验收：每一处行为变化都有表、有测试、有 CHANGELOG 落点。→ 用例分栏表（§一）、fixtures 覆盖表（§二）、正则迁移表（§四）、CHANGELOG 落点表（§七）均在 `docs/development/search-ripgrep-migration.md`；P0-1 本身不改生产行为，故本批没有新增测试断言变化，现有 `test_search.cpp` 17 条原样全绿。

### P0-2：manifest、定位器与假 runner

- [ ] 加 `third_party/ripgrep/manifest.json`、MIT license、notices。
- [ ] 实现 `BundledRipgrepLocator`，只认 `ExecutableDir()/libexec`。
- [ ] 启动/doctor smoke 执行 `--version`，精确校版本。
- [ ] 定义 `IRipgrepRunner`、typed request/policy/result/error。
- [ ] SearchTool 构造器可注入 fake；生产装配注默认 runner。
- [ ] 缺件、不可执行、版本错、spawn fail 各有稳定单测。
- [ ] 证明 PATH 上放一枚假 `rg` 也绝不执行。

验收：读工具不能借 PATH/env 变成任意执行口。

### P0-3：argv builder 与边界策略

- [ ] grep/glob argv builder 写成纯函数。
- [ ] `--no-config`、`--hidden`、默认 regex engine、无 shell、参数边界入测试。
- [ ] pattern/path/glob 以 `-` 开头不串成 flag。
- [ ] 空格、单双引号、中文、`[]{}!*?\`、Windows 盘符都逐项测。
- [ ] 用户正向 glob 不可偷变排除规则。
- [ ] `ObservationBoundary::ExcludedDirsSnapshot()` 与相对排除 glob 落地。
- [ ] 显式点名 ignored/隐藏/边界目录与默认递归排除两路对账。

验收：给同一 typed request，三平台生成同一语义 argv；差别只在 exe 后缀与路径编码。

### P0-4：流式 runner 与解析器

- [ ] ChildProcess 起 rg，stdout/stderr 分流。
- [ ] grep JSONL 分帧；glob NUL 分帧。
- [ ] `text/bytes(base64)` 两路解析。
- [ ] 100 条、512 KiB、16 KiB 单行、1 MiB 协议帧四道墙。
- [ ] 命中满额主动停树，记 truncated，不借退出码冒充失败。
- [ ] cancel/timeout/protocol error 各自收树、各自终态。
- [ ] no-match exit 1 走成功；invalid regex exit 2 走稳定错误。
- [ ] stdout 尾帧、stderr 洪水、坏 JSON、进程半途死、回调与 Shutdown 竞态全测。
- [ ] Windows Job Object 与 POSIX 进程组各做一只“rg 再生孩子”的夹具，证明取消不留孤儿。

验收：搜满 100 条后不再遍历余下大树；按停后进程表无残留。

### P0-5：真实 rg 差分与切主路

- [ ] CI 下载并校验三平台候选资产。
- [ ] 跑 fixture corpus，比较旧内核与新后端。
- [ ] “必须保留”全等；“有意迁移”只准落在批准表里。
- [ ] SearchTool 默认切到 BundledRipgrepRunner。
- [ ] 更新中英文 prompt、fallback 文案、schema 与 tool_text tests。
- [ ] 更新 CHANGELOG，写清 regex/ignore 迁移。
- [ ] PTC `search` 调用照跑；Plan/Explore 注册面不变。

验收：模型仍调用 `search`，调用形状除批准新增字段外不变。

### P0-6：发行包、安装与许可证

- [ ] Release 三矩阵随包带 `libexec/rg`。
- [ ] archive hash、member path、版本、执行位全校。
- [ ] Windows/Linux/macOS 安装后离线可搜。
- [ ] 更新安装能原子替换 rg；卸载只删 LubanCode 安装目录内副本。
- [ ] notices/license 入包，README 改掉“单文件”失实说法。
- [ ] `scripts/check_release.sh` 与安装脚本测试全过。
- [ ] 在断网环境解包运行，不触发下载、不查包管理器。

验收：Release 包缺 rg 或 license，流水线必红；不能发半套。

### P0-7：性能与稳定门

基准至少三档：

| 档位 | 语料 | 目的 |
|---|---|---|
| small | 单文件/几十文件 | 看进程启动固定成本 |
| medium | 本仓，排除 build | 日常 Agent 搜索 |
| large | 10 万文件、含 ignored 生成树 | 看 walker、并行与提前截断 |

查询至少五类：纯字面量、普通 regex、无命中、100+ 高频命中、glob 枚举。

记录：

- 冷盘/热盘分开；
- P50/P95，不拿单次最好值；
- wall time、CPU、峰值内存、扫描文件数、结果数；
- 直接 `rg`、SearchTool+rg、旧内核三列；
- Windows x64、Ubuntu x64、macOS arm64 分开。

首版性能门：

- [ ] medium/large 的 SearchTool+rg 不得比直接 rg 多 20% wall time 或 20ms（取较宽者）。
- [ ] large 高频命中在第 100 条后主动停，耗时不得随余下语料线性增长。
- [ ] small 档若因起进程略慢，如实记录；不得拿 large 倍数遮住。
- [ ] 每平台压力跑 1000 次短搜索，无崩溃、死锁、句柄泄漏、孤儿进程。
- [ ] Linux 另跑大树 100 轮，守住 15.2.0 资产风险门。

不预写“快 10 倍”。数字只从固定脚本与原始结果来。

### P0-8：删旧内核与发布收口

- [ ] 删除 `WalkFiles`、`LooksBinary`、自写 Glob、`std::regex` 搜索等旧实现。
- [ ] 删除只为旧内核服务的测试；合同测试迁到 backend-neutral 层。
- [ ] 不留 runtime fallback，不留隐藏开关，不留 PATH fallback。
- [ ] 若新后端发布后出事，回滚整个 LubanCode 版本；不在一版里养两套语义。
- [ ] 全量 C++ tests、安装 tests、Release package smoke、三平台 E2E 全过。
- [ ] `git diff --check`、第三方 notices、README/CHANGELOG、版本源对账。

验收：生产二进制只有一条 search 执行路；源码里搜不到旧 walker/regex 内核。

## 十、测试清单

### 10.1 参数与安全

- [ ] 缺 mode/pattern、空 pattern、坏类型。
- [ ] path 不存在、目录、单文件、相对、绝对、中文、空格。
- [ ] pattern=`-g`、path=`--help`、glob=`!secret` 不注入 flag。
- [ ] `RIPGREP_CONFIG_PATH` 指向恶意配置，因 `--no-config` 不生效。
- [ ] PATH 首位放假 rg，生产 locator 不执行。
- [ ] symlink 默认不跟；preprocessor/zip/PCRE2 均不可由输入打开。

### 10.2 grep

- [ ] 字面量、Rust regex、`fixed_strings=true`。
- [ ] 中文、emoji、CRLF、末行无换行。
- [ ] 同一行多个 submatch 只返一条。
- [ ] glob 过滤 basename 与带 `/` 相对路径。
- [ ] invalid regex 有稳定错误，不崩。
- [ ] binary 跳过；UTF-16 行为按 rg 默认实测后写入合同，不凭猜测。
- [ ] 超长行、超大结果、100 条截断。

### 10.3 glob

- [ ] `*.cpp` 递归命中各层。
- [ ] `**/*.md` 含根目录与子目录。
- [ ] `src/**/*.hpp`、`docs/**`。
- [ ] 文件名含换行，NUL 分帧不裂。
- [ ] 文件名非法 UTF-8，出门前清洗。
- [ ] 结果超过 100 条主动停。

### 10.4 ignore 与边界

- [ ] `.gitignore`、`.ignore`、`.rgignore` 优先级由真实 rg 夹具证明。
- [ ] `.github` 因 `--hidden` 可搜；`.git` 仍硬排除。
- [ ] ignored 文件默认不搜，显式单文件可搜。
- [ ] build/node_modules/.evidence 默认剪枝，显式 root 可搜。
- [ ] 动态登记日志目录默认剪枝；root 指入该目录时放行并提示体积。
- [ ] Windows 大小写、路径分隔符、盘符根；POSIX symlink 与任意字节文件名。

### 10.5 进程终态

- [ ] exit 0/1/2。
- [ ] 起跑前 cancel、运行中 cancel、命中满额 stop、墙钟 timeout。
- [ ] stdout 坏 JSON、超长未闭帧；stderr 超限。
- [ ] rg 崩溃/被杀；结果不得冒充“没搜到”。
- [ ] 回调与 Shutdown 并发 1000 轮。
- [ ] 工具返回后无 rg/子进程残留。

### 10.6 包与安装

- [ ] 三平台 archive 都有正确 `libexec/rg`。
- [ ] 版本、hash、执行位、license、notices。
- [ ] 新装、覆盖升级、离线安装、卸载。
- [ ] 缺 rg、换错平台 rg、版本错，doctor 与 search 都给稳定诊断。

## 十一、风险与反设计

| 风险 | 错路 | 裁决 |
|---|---|---|
| 小搜索被进程启动拖慢 | 为此保留第二套内核自动切换 | 先测；小档如可接受便统一 rg，不养双语义 |
| rg 缺件 | 去 PATH 找、运行时下载、静默 fallback | 明报安装损坏；Release 门阻止缺件 |
| 模型借 search 起任意程序 | 开 env/path override | 生产只认 exe-relative 固定路径；测试走依赖注入 |
| 用户配置暗改行为 | 继承 RIPGREP_CONFIG_PATH | 每次 `--no-config` |
| ignore 迁移漏结果 | 偷偷采用 rg 默认，不写文档 | 差分表、CHANGELOG、显式 path 逃生口 |
| `.github` 被 hidden 规则漏掉 | 照搬 rg 默认 | 固定 `--hidden`，另硬排 `.git` |
| 动态日志仍被扫描 | 只在 parser 丢命中 | 转成 walker exclude glob，真正剪枝 |
| 全局 100 条没省时间 | 用 `--max-count=100` | 流式计 match event，满额主动收树 |
| JSON 一行吃爆内存 | 无界攒到换行 | 1 MiB 协议帧硬帽 + 主动收树 |
| stdout/stderr 混在一起 | 复用 merged `RunProcess` | ChildProcess 双管道；JSON 与诊断分账 |
| rg 非零全算失败 | 不分 exit 1 与主动 stop | 本地 stop reason + 退出码联合裁决 |
| Linux 静态包偶发崩 | 闭眼取 15.2 musl | GNU/.deb 候选 + 大树压力门 |
| 许可证漏带 | 只在 README 提一句 | license/notices 入 archive 硬门 |
| 旧代码舍不得删 | 永久留 fallback“保险” | 差分过门后删除；版本回滚才是保险 |

## 十二、完成线

这件事只有同时满足下列条件，才可从“待实现”改成“已落地”：

1. 模型仍只调用 `search`，不看 rg 命令行。
2. 生产只走随包 ripgrep，一条路，无 PATH/env/runtime-download/fallback。
3. grep 用 JSONL，glob 用 NUL；100 条与字节上限会提前收树。
4. 取消、超时、无命中、坏正则、协议错各有明确终态。
5. `.github` 可搜，ignore 生效，`.git/build/node_modules/.evidence` 与动态日志边界守住，显式点名仍可读。
6. Windows x64、Ubuntu x64、macOS arm64 的 Release 包、安装与离线 smoke 全过。
7. ripgrep 版本、archive hash、MIT license 与 notices 全钉住。
8. 固定基准给出三平台 P50/P95；不拿口号冒充性能证据。
9. Linux 候选过大树压力门，不携已知不稳资产硬发。
10. 旧 C++ walker/regex/glob 内核删净，全套测试与发行检查全绿。

到那时，才可说：LubanCode 保住了自己的 `search`，也用上了 ripgrep 的本事。
