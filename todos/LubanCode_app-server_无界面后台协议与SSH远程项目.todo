# LubanCode app-server:无界面后台协议与 SSH 远程项目

状态：待做（2026-08-22 用户定调）

## 一句话

给 `lubancode` 添一个无界面后台入口：前端从 stdin 发请求，后台在项目机上读文件、改代码、跑命令，再从 stdout 把正文、工具进度、审批、diff 与结果一笔笔传回来。SSH 只管架通道，活计全落在远端。

```text
本机 LubanCode 前端
    │ 消息 / 审批 / 打断 / 进度
    │
    └──── SSH ────► 远端 lubancode app-server
                         │
                         ├─ 会话与模型请求
                         ├─ 项目文件和工具
                         ├─ 命令、测试与子代理
                         └─ 日志、diff、usage 与终态事件
```

## 用户定调

- 命令长这样：`lubancode app-server`。
- 它不是另一套云服务，也不是换皮 TUI；它只跑后台协议，不画界面，不读键盘交互。
- 远端只须备好 SSH、LubanCode CLI 和既有 provider 凭据。前端 SSH 登录后，从远端用户的 login shell 拉起 `lubancode app-server`。
- 文件、命令、测试、MCP、LSP、技能、插件、凭据与权限规矩都取远端环境；本机只负责展示和发令。
- 不把监听端口裸露到公网。首版走 SSH 承载的 stdio，先把边界做硬。

## 官方参照（2026-08-22 已核）

- Codex App Server 给富客户端承接认证、对话历史、审批与实时事件；协议用双向 JSON-RPC，缺省传输是 stdin/stdout 上逐行一条 JSON。[官方 App Server 说明](https://learn.chatgpt.com/docs/app-server)
- Codex 桌面端连 SSH 项目时，会从远端 login shell 拉起 app-server。读写文件、执行命令都在远端；官方也明说不可把 app-server 直接暴露在共享网或公网。[官方 SSH 说明](https://learn.chatgpt.com/docs/remote-connections)
- LubanCode 借它的进程形状、核心对象和事件次序，不承诺与 Codex 私有协议逐字段兼容。若日后要让现成 Codex 客户端直连，另立兼容单，拿版本化 schema 对表，不能只凭方法同名便冒充兼容。

## 现状盘点（已核）

- `src/app/cli_options.*` 只认旗标与位置问题，尚无子命令；眼下输入 `app-server` 会被当成单发提问。
- 会话编排已拆出 `backend_stack`、`tool_runtime`、`turn_runner`、`interactive_session`、`session_store` 等地基。模型流、工具执行、usage、取消、Hooks 与子代理都有现成事件源，可抽一层事件汇流，不必另造代理内核。
- `BuildCallbacks()` 仍把正文、工具卡、确认问句直接接到终端；app-server 得换成协议回调。核心层不得反过来依赖 stdout 或终端 painter。
- MCP 与 LSP 已各有 JSON-RPC 客户端和分帧代码，可借错误模型与测试手法；它们是下游客户端，不可硬改成 app-server 服务端。
- 依赖里只有 `cpr` 与 `nlohmann-json`，没有 WebSocket 服务库。首版守住 stdio，可免得为一条 SSH 路先拖进整套网络栈。

## 协议底线（v1）

### 一、传输与进程纪律

- `lubancode app-server` 缺省走 stdio：stdin 逐行收 UTF-8 JSON，stdout 逐行吐 UTF-8 JSON；一行只放一条完整消息。
- stdout 是协议专线。日志、诊断、崩溃前说明一概走 stderr；工具子进程输出装进事件字段，绝不能漏到 stdout 搅坏分帧。
- 请求带 `id/method/params`；响应回同一 `id`，二选一带 `result` 或 `error`；通知只带 `method/params`。是否保留标准 `jsonrpc: "2.0"` 字段，在 schema 冻结时一次定死，收发两头不得各说各话。
- 每条连接先 `initialize`，再收 `initialized`。握手前的业务请求报错；重复初始化也报错。初始化结果须回协议版本、LubanCode 版本、平台、能力表。
- EOF、父进程断管、明确 `shutdown/exit` 都要收口：打断在跑回合，停后台任务，刷完能刷的终态，随后退出，不留孤儿进程。
- 给请求入口设有界队列与长度上限。塞满就回“忙，稍后重试”，坏 JSON、未知方法、错参数各有稳定错误码；一条坏消息不能撞死整台服务。

### 二、最小方法面

首版只铺富客户端真要用的路，不把整个 CLI 命令表一锅端：

1. `initialize` / `initialized` / `shutdown`：握手、能力协商与退场。
2. `thread/start` / `thread/resume` / `thread/list` / `thread/read`：新会话、续会话、历史列表、只读详情；底下复用 `SessionStore`，不另存第二本账。
3. `turn/start`：传 `threadId`、文本或图片输入、cwd 与本轮可覆盖配置，立刻回 `turnId`，工作另起执行上下文。
4. `turn/steer` / `turn/interrupt`：给在跑回合追加指令，或按 `threadId + turnId` 精确打断；沿用现成 steering queue 与 cancel flag。
5. `model/list` / `config/read`：让前端画模型选择和运行位置。首版只读，不开放任意远端改配置。
6. 服务端反向请求：工具审批与 `ask_user`。请求须带 `threadId/turnId/itemId`、工具名、参数摘要、风险说明与可选决定；前端回 `accept`、`acceptForSession`、`decline` 或 `cancel`。

### 三、事件账

- 统一落成 `thread/*`、`turn/*`、`item/*` 三层：会话装回合，回合装条目，条目承正文、思考、工具、命令、文件改动、提问、子代理与错误。
- 一轮至少有 `turn/started`、若干 `item/started|delta|completed`、最后一条 `turn/completed`。成功、失败、拒绝、取消各有明白终态。
- 工具调用先发 started，再按需发审批，再发输出增量与 completed。审批没回时不得偷偷执行；断线或回合取消时，一律按 cancel 收口。
- 文本 delta、命令 stdout/stderr、diff、usage、上下文占用和子代理进度均保住顺序。每条事件带单调序号，前端重绘、查漏都有凭据。
- 慢客户端不可堵住代理工作线程。事件队列有界；超限先合并可合并的 delta，再报明确 overflow，不能悄悄丢终态与审批。

## 落地次序

1. **拆核心与终端**：从 `BuildCallbacks/RunTurn` 抽出不碰 TTY 的会话运行接口和结构化事件；现有 TUI 改做同一接口的一位消费者，行为不变。
2. **立协议库**：新增 `src/app_server/`，分开 message/schema、dispatcher、connection、thread runtime。所有 JSON 解析、错误响应、状态机都有纯单测。
3. **接 CLI 子命令**：`ParseCliArgs` 正式识别 `app-server`，帮助、缺参、旗标冲突与退出码写进测试；绝不能再把它送给 one-shot。
4. **通一条完整回合**：initialize → thread/start → turn/start → 文本流 → 工具调用 → usage → turn/completed；用假 backend 跑确定性驱动器。
5. **接审批与打断**：把 `needs_confirm`、Hooks 决策、`ask_user`、会话级总是允许和 ESC/cancel 语义映到双向协议。
6. **接存档与恢复**：thread list/read/resume 复用既有会话文件；服务重启后能续，损坏会话报单项错误，不拖垮别的会话。
7. **做 SSH 冒烟**：本机以前端夹具执行 `ssh <host> lubancode app-server`（CI 可用本机 sshd 或进程管道替身），验证 cwd、环境、PATH、断线退场与 stderr 隔离。
8. **再议 WebSocket**：stdio 稳后另立运输层单。只准绑定 loopback 或 Unix socket；若真要非本机监听，TLS、鉴权、Origin 检查、限流与健康探针缺一不可。

## 验收

- `lubancode app-server` 启动后不画 banner、不碰终端模式；stdout 从头到尾都能逐行解析成协议消息。
- 客户端完成握手，新建远端项目会话，发一句话，能实时收到正文、工具起止、命令输出、文件 diff、usage 与唯一终态。
- `write_file`、`edit_file`、`run_command` 等需确认工具会停住发反向请求。前端允许才执行，拒绝便把原因送回模型；`acceptForSession` 只在本会话生效。
- `ask_user` 能从远端模型一路问到本机前端，再把答案送回原回合，不借 stdin 另开一条私下问答。
- `turn/steer` 能给在跑回合加话；`turn/interrupt` 在硬时限内停住模型流、前台命令与可取消子任务，发 `interrupted` 终态。
- 两个 thread 可并行跑，事件不串台；同一 thread 同拍开两轮时，协议明拒或排队，规矩写死并有测试。
- SSH 断开后 app-server 自退，远端不留后台子进程；再连后 `thread/resume` 能从既有存档续上。
- 假客户端故意喂坏 JSON、超长行、未知方法、重复 id、迟到审批、断管，服务都能报清、收口，不崩、不死锁。
- Windows 与 POSIX 都过编译；协议纯单测、假 backend 集成测试、真实 stdio 进程测试全绿；现有 TUI 与全量测试不回归。

## 不做

- 不在首版造 LubanCode 桌面端、VS Code 扩展或手机端；先把后台接口立稳，客户端另排。
- 不把 app-server 当 CI 批处理器。无人值守任务仍走将来的非交互/SDK 路，不拿双向审批协议硬套。
- 不首期上公网 WebSocket，不内置 SSH 客户端，不代管密钥；SSH 发现、登录和隧道先归调用方。
- 不复制一份 AgentLoop、ToolRegistry、SessionStore 或权限系统；TUI 与 app-server 必须共用同一内核。
- 不把本机文件、凭据或工具偷偷混进远端会话。哪台机器跑 app-server，哪台机器出环境。
- 不口头宣称兼容 Codex app-server。兼容要有 schema 版本、黄金报文与跨版本合同测试作证。
