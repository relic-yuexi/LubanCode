# token 估算选型回测：待 session 轨迹记录修复后重跑

- 状态：立案待前置——用户修完 session 轨迹记录的 bug 后回测定论。
- 日期：2026-09-09
- 级别：P2（估算尺牵动预检/压缩触发/上下文压力显示的准确性）。
- 前置：session 轨迹记录的 bug（用户在修）。**本轮回测数据判为不可信**（用户裁定 MAE 失真），不作选型依据。
- 范围：只读回测与选型建议；不改估算实现（选定后另立实施单）。

## 一、要回答的问题

LubanCode 的 token 估算该用哪种方法。候选与精确公式（用户定义）：

1. **固定 bytes/4**：`base + ceil(未计量 wire UTF-8 bytes / 4) + 新消息数 * 8`。
2. **在线 token/byte**：只拿此前已见标签累计求密度；每份 session、每个 scope 另起一套，初值 0.25 token/byte。
3. **朴素中英字符法**：`base + ceil(汉字码点数 + 其余 wire 码点数 / 4) + 新消息数 * 8`（汉字约 1 token，英文约 4 字符 1 token）。
4. **LubanCode 现行默认尺**（对照行）：ASCII 4 字符/token、非 ASCII 1.5 token/字（轨迹快照已记估算，零成本）。

## 二、已有家当（复用，勿重造）

- 脚本与逐笔样本：`build/token_backtest/`（backtest.py 重放采样、metrics.py 指标、samples.json、probe 脚本）。build/ 不入库，需先抢救或重建。
- 指标口径：MAE、bias（正=高估）、增量 WAPE（Σ|误差|/Σ正实际增量）、P95 绝对误差、低估笔数/总笔、最大低估/高估；总表 + 按 provider/model 分表；按模型自适应组合水位。
- 用户的 MiniMax 参照表（65 笔）：固定 58.69 / 在线 66.75 / 字符 95.63（MAE）。

## 三、本轮（2026-09-09，判为不可信）的初步观察——仅备忘，不作结论

- 现行默认尺未校准时大幅高估（est/actual 中位 1.88、p90 4.2），病根疑似汉字 1.5 token/字 对真实 CJK 分词虚高——若成立，系数 1.5→1 是一行的大便宜。
- 22 笔"wire 涨 4.7 万~13 万字节、实报只涨 240~2000 token"的异常对，成因未定位（疑 provider 兼容层/中转截结果，或 usage 缓存计数语义）——**与用户要修的轨迹 bug 是否同源，修完正好复查**。
- 在线法在含异常对的序列上密度学偏、连环垫底——真实序列（非干净）的固有软肋，重跑时重点验证。
- 重放的守门假设（消息数守门、wire 层修正、首请求剔除）在轨迹修复后应大幅简化——本轮剔除主会话各 turn 首请求是大头，修复后若能重放即为样本扩容。

## 四、验收

- 轨迹 bug 修复后的新数据重跑：样本量、模型分布报清；四行总表 + 分模型表 + 自适应组合。
- 异常对复查：修复后是否消失；不消失则单独立案（provider 层）。
- 选型建议一条（数据说话），**定夺归用户**。
