# 项目记忆从半自动卡片盒升级为可审、可查、少误召的系统

状态：待做（设计与改进）

## 一句话

现版项目记忆已经能存、能搜、能注入，却还像一只半自动卡片盒：模型肯不肯记全凭临场发挥，词法沾上一点边便可能召回，整份索引每轮都跟着进 prompt，用户又看不见为何命中。

先把授权、采集、召回与审阅四条链收紧。别急着上向量库。记得准、召回少、来路看得见，比换一枚花哨检索器要紧。

## 眼下到底有什么

现有主链并非空壳：

```text
外层用户消息
-> BuildTurnContext(query, cwd)
-> 读 index.md 与 catalog.json
-> 按标题、摘要、关键词、路径做词法计分
-> 最多取 max_results 条主题正文
-> SetTurnSystemSuffix
-> 同一 AgentLoop::Run 内每次模型请求共用
```

写入也已接通：

```text
/memory remember 或 memory_save
-> ValidateSaveRequest
-> 原子写 memory-jobs/pending/*.json
-> 隐藏 worker 抢锁
-> upsert 主题 Markdown
-> 重建 catalog.json 与 index.md
-> 成功删 job，失败移 failed
```

已有能力：

- `fact` 与 `preference` 分账。
- 同 id 更新，不重复铺主题。
- Git 主工作树与 linked worktree 按 common git dir 共用项目 key。
- 关联文件指纹变了，旧正文不再注入，只提示核验。
- 记忆不进会话 history，不随 Markdown 导出。
- `/memory list|remember|forget|rebuild` 已有。
- 记忆与项目身份专项测试已经覆盖后台写入、召回、指纹、遗忘、争锁与敏感内容。

## 现有短板

### 1. 文档里的授权边界可被本场命令绕开

文档写明“只有用户全局配置能打开记忆”。配置合并层也确实不准项目配置自行开启。

可交互会话无论总开关是否打开，都会构造 `ProjectMemory`。`/memory on` 随后直接调用 `set_enabled(true)`；`generate` 默认又是 `true`，于是还能补挂 `memory_save`。

这使“全局未授权”与“本场暂时关闭”混成同一枚 bool。陌生项目虽不能靠配置文件开启，交互命令却可能越过文档承诺。须先把这道闸修正，再谈自动学习。

### 2. 采集太随缘

现版不会自动审阅对话，也不会在收轮后提候选。只有两条写入路：用户手敲 `/memory remember`，或主模型主动调用 `memory_save`。

后者受模型习惯、工具延迟挂载、提示注意力与剩余轮数影响。同一句稳定偏好，这回可能记，下回可能不记。用户也看不见模型放过了什么。

### 3. 召回太宽，索引还白吃上下文

- `use` 开启后，`index.md` 会先整段塞进本轮 system suffix；即使没有相关主题，索引仍占 prompt。
- 主题只要 `ScoreEntry > 0` 便可入围。中文双字片段撞上一处，就可能把弱相关主题带进来。
- 默认正文预算 24 KiB、最多 4 条；小库尚可，库大后容易把旧卡片铺得满桌都是。
- 只有最终模型看得到召回包。用户不知道本轮命中了什么，也不知道为何命中。

### 4. 陈旧只会拦，不会回炉

文件指纹变化后，现版会拒绝注入旧正文。这道安全线是对的。可它不会排 refresh，不会告诉用户哪些主题长久失效，也没有“核验后续命”这条顺手路径。

整文件指纹又偏粗。文件里只改一行注释，挂在该文件上的事实也会整条失效。

### 5. 没有一把量好坏的尺

眼下测试能证明链路会跑，却不能回答：该命中的命中了几条？不该命中的误召了多少？每轮多花多少上下文字节？换检索算法以后到底变好还是变坏？

## 想要的样子

### 一、授权与本场状态分开

至少拆成两层：

```text
global_allowed   用户全局配置授予的能力，运行时不可翻开
session_enabled  本场总开关，只能在 global_allowed 范围内开关
```

召回与写入仍各有本场子开关：

```text
session_use
session_learn
```

规矩定死：

- 全局没授权时，`/memory on`、`use on`、`learn on` 都不得把能力凭空打开。
- 此时命令须说明去哪里改全局配置，不能只回一句含混的“未开启”。
- `memory_save` 不注册；即使旧工具引用尚在，也须由运行时 capability gate 拦住。
- 单发模式只可在全局授权下召回，仍不写入。
- 项目配置只能收窄，不能扩权。这条既在 config merge 守，也在运行对象再守一次。

### 二、学习改成三档

```text
off      不提候选，不写入
review   自动提候选，用户审过才入库（建议默认）
auto     自动写入，只给用户明确授权的可信项目
```

不要一上来扫描整场聊天。每个外层回合结束后，只看本轮增量：用户消息、最终回答、已经结构化的工具结果摘要与证据路径。大段日志、网页/MCP 原文、密钥字段和子代理原始输出不得整包送去抽取。

候选只收三类：

- 用户明确说出的长期项目偏好。
- 已由源码、配置或工具结果核验的稳定项目事实。
- 下个月再遇见仍能省一次排查的故障根因与验证办法。

这些仍不收：任务进度、临时分支、端口、PID、猜测、整段聊天、个人资料、凭据。

候选先住待审区：

```text
回合结束
-> 提取 0～3 条候选
-> 过长度、敏感内容与路径校验
-> 用 kind + subject + scope 查重
-> 找到旧主题则提出 update
-> review 档等用户接受、修改或丢弃
-> auto 档才排正式 upsert job
```

增命令：

```text
/memory learn off|review|auto
/memory review
/memory accept <candidate-id>
/memory edit <candidate-id>
/memory reject <candidate-id>
```

候选要耐退出。可放 `memory-candidates/`，仍按项目 key 分账；写入用原子替换。拒绝项只留短哈希与理由，避免同一候选每轮死缠烂打，也别永久保存被拒正文。

### 三、召回只送命中，不送整份索引

`index.md` 留给人看，也可作灾后重建件；正常请求不再整段注入。检索只读机器 catalog 与主题元数据。没有足够强的命中，本轮 memory suffix 只保留极短能力说明，或干脆为空。

首版检索分两层：

1. 硬命中：完整路径、文件名、符号名、配置键、明确关键词。
2. 软排序：BM25 或现有词法分的改良版。

中文双字切分可留。英文与代码标识符要补 camelCase、snake_case、路径段拆分：

```text
BuildTurnContext -> build / turn / context
project_memory.cpp -> project / memory / cpp
```

设一条真正的最低门槛。只撞中一个常见双字片段，不得注入正文。默认改成：

- 最多 1～3 条。
- 正文总预算 4～8 KiB。
- 同分时优先路径/符号硬命中，再看最近核验时间；不能只靠更新时间把无关新卡顶上来。
- `conflict`、`archived` 不参与；`stale` 只给核验提示，不给旧正文。

严格项目规矩仍写 `AGENTS.md`。若确有少量每轮都该看的软偏好，可另设 `pinned`，总预算不超过 1 KiB；不能借 pinned 把整份索引搬回来。

### 四、每次召回都说得清

给召回结果留一份本地 trace，不写进会话 history：

```text
query terms
候选 id
各项分数
命中理由
是否因预算落选
是否因 stale/conflict 被拦
最终注入字节数
```

增命令：

```text
/memory why             看上一轮召回了什么、为何命中
/memory why <id>        看某主题的计分细账
/memory stats           看命中、空召回、误召反馈与注入字节
```

正常界面只打一行淡字即可：

```text
memory: 召回 2 条 · 3.1 KiB
```

不命中便不铺噪声。trace 不得抄出整段主题正文，也不得记用户完整问题；只存归一化词项、id、分数与字节数。

### 五、主题带上范围、证据与寿命

在 schema 下一版补：

```json
{
  "scope": {"kind": "project|subtree|path", "value": "src/agent"},
  "evidence": [{"path": "src/agent/loop.cpp", "symbol": "AgentLoop::Run"}],
  "confidence": "user-stated|verified|inferred",
  "last_verified_at": "...",
  "expires_at": null
}
```

- `preference` 只有用户明说才可记为 `user-stated`。
- `fact` 自动入库须为 `verified`，且至少带一项可核验证据。
- `inferred` 只准进候选区，不准 auto 入正式库。
- 临时规约可带 `expires_at`，到期后不召回，等用户续期或归档。
- scope 参与排序。当前 cwd 落在主题 subtree 内才加分，别让前端偏好跑去干扰 C++ 后端任务。

文件变化后把主题列入 stale 清单：

```text
/memory stale
/memory verify <id>
/memory refresh <id>
```

首版仍可保留整文件指纹，先把回炉流程接通。以后再按 symbol、配置键或小段证据做细粒度指纹，别在第一期一口吃完。

## 检索器怎么选

首版不引入 embedding、向量数据库或知识图谱。

理由很直白：现有库本来就小，最大毛病在采集与门槛，不在语义向量。先用本地 BM25/倒排索引便够。若不想添 SQLite 依赖，可从 catalog 派生一份紧凑倒排 JSON；若项目本就愿意带 SQLite，则用 FTS5，中文词项仍由自家 tokenizer 产出。

等这些条件同时出现，再评估向量检索：

- 单项目活跃主题常态超过数百条。
- 词法评测反复证明同义改写漏召严重。
- 注入预算已经收紧，采集质量也稳定。
- 能接受 embedding 模型、缓存、迁移与离线失败这笔维护账。

即使以后上向量，也走 hybrid：路径与符号硬匹配不能让位，BM25 与向量各取候选，再做轻量重排。

## 分期落刀

### 第一期：修真 bug，立刻减噪

- 拆 `global_allowed` 与本场开关，堵住 `/memory on` 绕授权。
- 正常请求不再注入整份 `index.md`。
- 加最低召回门槛。
- 默认正文预算降到 8 KiB、最多 3 条。
- 加 `/memory why` 最小 trace。

这期不碰自动抽取。改动窄，收益当场可见。

### 第二期：候选审阅箱

- `learn off|review|auto`。
- 回合增量抽取与候选持久化。
- 去重、更新旧 id、接受/修改/拒绝。
- review 设为建议默认；auto 须用户显式授权。

抽取可先借当前主模型在回合收尾生成严格 JSON。须有成本开关、超时与失败降级；抽取失败不影响主会话。后续再接独立便宜模型。

### 第三期：检索与主题 schema

- BM25/倒排索引。
- 标识符拆分、scope、evidence、confidence、过期时间。
- stale 清单与 verify/refresh。
- schema 1 平滑迁移到 schema 2，旧主题照样可读、可 rebuild。

### 第四期：评测后再定向量

- 跑固定语料与真实匿名 trace。
- 只在词法漏召已有明证时试 hybrid embedding。
- 新检索器藏在接口后，Markdown 真本与 job 格式不跟着推倒。

## 评测尺子

建 `tests/fixtures/memory_retrieval/`，至少放：

- 30 条应命中的 query/entry 对。
- 30 条不应命中的近似问法。
- 中英混写、符号名、路径、camelCase、snake_case。
- 同词不同模块的干扰项。
- stale、conflict、archived、expired。
- cwd scope 命中与越区不命中。

每次改检索器都报：

```text
Recall@1 / Recall@3
Precision@1 / Precision@3
空召回正确率
平均注入字节
P95 注入字节
stale 拦截率
```

另做端到端 captured request 测试，确认没命中时 system 不含索引；命中时只含最终主题，不进 history，不进 export。

## 代码落点

- `src/config/config.hpp/.cpp`：全局 capability 与本场状态分层；配置来源保真。
- `src/memory/project_memory.hpp/.cpp`：检索接口、门槛、trace、schema、候选与 stale 流程。
- `src/memory/memory_tool.*`：正式保存与候选提议分开，工具描述写清证据要求。
- `src/app/interactive_session.cpp`：回合收尾抽取、`/memory` 新命令、状态提示与授权闸。
- `src/app/tool_runtime.*`：`memory_save` 注册和每轮过滤都按 capability 判断。
- `src/agent/loop.*`：继续只接 turn suffix；别把召回内容塞进 history。
- `src/cli/i18n.cpp`：中英文案成对。
- `tests/test_project_memory.cpp`：授权绕过、候选、检索阈值、trace、scope、schema 迁移。

## 验收

- 全局未授权时，项目配置与 `/memory on|use on|learn on` 都开不了记忆，也不注册 `memory_save`，不写任何 memory job。
- 全局授权后，本场可随时关开；开关不回写配置。
- 一轮里用户明确说出稳定偏好，review 档出现一条候选；普通闲话不提候选。
- 候选可改、可拒、可接受；拒绝后不会下一轮原样再来。
- 没有相关记忆时，模型请求不含 `index.md`，记忆注入为零或只含极短能力说明。
- 路径、符号、关键词强命中召回正确主题；只撞一个常见中文片段不召回。
- 默认不超过 3 条、8 KiB；预算边界不劈开 UTF-8。
- `/memory why` 能说清命中、落选与 stale 原因，不泄出完整 query 与正文。
- 文件变化后旧事实不注入，并出现在 `/memory stale`；核验后可原 id 更新复活。
- worktree 仍共享同一项目记忆；独立 clone 不误共享。
- 子代理仍不自动灌整库。主代理委托时只带任务所需记忆。
- 记忆仍不进 session JSONL 与导出。
- 旧 schema 1 主题可读、可列、可召回、可 rebuild；升级失败不损坏 Markdown 真本。
- 固定评测集的误召率低于现版，平均注入字节显著下降；结果进 CI，防以后慢慢长歪。

## 不做

- 不把所有聊天自动存档冒充记忆。
- 不让模型未经审阅便保存推断、日志或外来原文。
- 不用向量库遮住采集脏、门槛松、无法解释三桩旧病。
- 不把强制规矩从 `AGENTS.md` 搬进软记忆。
- 不让记忆正文冒充新的系统指令。
- 不为追求“自动”而阻塞主回答；抽取、写入、重建失败都须可降级、可重试。
