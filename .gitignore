# 密钥文件,绝不能提交
测试.md

# lubancode 本地配置(可能带真密钥),别人机器上各配各的,不进仓库。
# 新位置是 .lubancode/ 目录(config.json 住这儿,往后 plugins/、skills/ 也会
# 住这儿);.lubancode.json 是 M6.5 之前的旧位置,读到会自动迁移,这里留着
# 兜底,免得漏网的旧文件被误提交。
.lubancode/
.lubancode.json

# 构建产物
build/
out/

# ripgrep 迁移单:随包 rg 的分期/缓存/发行拼包目录——二进制与上游归档
# 绝不进 Git(单子裁决:归档离线重打走自家 Release 镜像,哈希照核)。
dist/
rg-stage/
rg-cache/

# IDE / 工具生成物
.vs/
.cache/
CMakeUserPresets.json
compile_commands.json
.vscode/
*.user

# vcpkg 本地安装(若在项目内安装)
vcpkg_installed/

# Claude 会话与 worktree
.claude/

# worktree 内的本地构建树(MSVC 临时编验用)
build-msvc/

# Python 字节码缓存不进仓
__pycache__/
*.pyc

# GUI Agent 真机证据目录(桌面截图,绝不入库)
.gui-evidence/

# search 迁移单 P0-1:硬排除目录夹具本地生成物——这组必须含一个字面量
# 叫 ".git" 的子目录才能验真宿主硬排除,git 没法把它当常规内容提交
# (仓库边界保留字),故只在本地/运行时生成,不进版本库。见
# tests/fixtures/search/build_corpus.sh 与 tests/fixtures/search/README.md。
tests/fixtures/search/corpus/excluded_dirs/

# 外壳孵化(阶段 E)的构建产物:Tauri Rust 构建树与生成的移动端骨架、
# Android gradle 构建树与本地配置。Cargo.lock 照应用工程惯例进仓
# (可复现构建),壳二进制绝不进仓。
examples/shells/tauri/target/
examples/shells/tauri/gen/
examples/shells/android/.gradle/
examples/shells/android/build/
examples/shells/android/app/build/
examples/shells/android/local.properties

# Android 壳签名材料(打包发布账 §三):keystore.properties 与密钥库
# 本身绝不进仓——keytool 生成命令与接线见 examples/shells/README.md
# 《Android 签名》;CI 只出 debug APK,release 签名在本机配好再出。
examples/shells/android/keystore.properties
examples/shells/android/*.jks
examples/shells/android/*.keystore

# Agent 会话交接档:随机器走的临时产物,绝不入库(557c62e7 曾误入后移出)
AGENT_HANDOFF.md

# 质量对照跑批临时目录:含拷贝的真 config(带钥匙)与会话轨迹,绝不进 git
eval/deferred_quality/_run/

# LoCoMo-MC10 防作弊基准(记忆系统评测单):492MB 原数据、扰动产物与
# 变换账本(含答案映射)绝不进 git——CC BY-NC 数据 + 防作弊隔离要求。
# 进 git 的只有聚合报告(无原文无答案):retrieval_baseline.md / report.md
# / retrieval_improved.md(改进前后对照,记忆检索与注入改进单)/
# summary_quality.md(摘要质量双库对照,记忆写入侧改进单)与 E2 配对
# 报告 paired_*.md(配对增益与区间分布,同单);paired_r1.md 是记忆幻觉
# 根治单(adversarial 桶)的复跑报告。
eval/locomo/*
!eval/locomo/retrieval_baseline.md
!eval/locomo/report.md
!eval/locomo/retrieval_improved.md
!eval/locomo/summary_quality.md
!eval/locomo/paired_t1.md
!eval/locomo/paired_five.md
!eval/locomo/paired_r1.md
