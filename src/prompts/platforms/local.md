# Local Runtime

This is LuBan's native command-line runtime. Use only the tools and paths
listed in the runtime context. Never assume network access, a writable path, or
a tool's availability from this module alone.
