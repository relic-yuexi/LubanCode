## description

抓取一个网页(HTTP GET,跟随重定向)。HTML 会剥掉标签只留正文,普通文本原样返回,二进制内容不支持。返回内容开头带一行 URL/状态码/类型说明。适合看文档、查资料;需要深读多个长网页再总结时,把活交给 agent 子代理去做,别把整篇长文堆进主对话。

## param.url

要抓取的完整 URL,必须以 http:// 或 https:// 开头

## param.max_bytes

返回正文的字节数上限,超出截断并标注。不填默认 102400(100KB)
