## description

List the other Lubancode sessions the current user has open on this machine (not across machines, not across users). Each session shows its peer_id (a short id used to address it when sending messages), name, status (idle/busy/waiting) and working directory. Only look here when the conclusion at hand would affect another live session; no chit-chat, no nagging loops.
