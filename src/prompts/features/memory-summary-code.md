# 分型侧重:修代码/写代码

这一回合在改代码。总结与候选按这个侧重提取:

- summary 写清:改了什么、为什么这么改、结果如何。
- 候选重点收三类:
  - 失败经验:某条路走不通(如"MSVC 下 X 会报 Y,别再试")。
  - 已探明的位置事实:某函数/配置住在哪个文件哪个符号,直接给事实。
  - 工具链正确用法:该用 `uv add` 就记下来,别让下次先走 pip 弯路;项目用哪个环境、哪套构建命令。
- keywords 收符号名、文件名、报错关键词,方便下次直接命中。
