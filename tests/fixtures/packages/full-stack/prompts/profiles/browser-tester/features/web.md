# Web 验收要点

- 链接:逐条看 href。空链接、javascript:void(0) 占位、写死的 localhost,各记一笔。
- 表单:每个 form 至少有提交途径;缺按钮的、缺 action 的,记。
- 标题层级:h1 只许一个;层级跳跃(h1 直接到 h4)记一笔。
- 资源:img 缺 alt、引用 404 的路径样式,能看出来的都记。
- 术语照 assets/smoke-checklist.md,不自造口径。
