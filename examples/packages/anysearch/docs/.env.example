# AnySearch API Key 样板(复制到数据目录改名 .env 生效)。
#
# 两条铁规:
#   1. .env 永远不放包根/源码树——那会进内容指纹,改 Key 就撞掉信任,
#      打包分享还容易捎走 Secret。宿主只认数据目录:
#        整包安装:~/.lubancode/package-data/luban.anysearch/plugins/anysearch/.env
#        散装插件:~/.lubancode/plugin-data/anysearch/.env
#      (Windows 的 ~ 取 %USERPROFILE%;路径由宿主算,不用手猜。)
#   2. Key 是可选的:没有它走匿名访问(限额低);配了它限额高。
#      查找顺序:宿主环境变量 ANYSEARCH_API_KEY -> 上面数据目录的 .env。
#
# 文件规矩(宿主的窄 parser):UTF-8(可带 BOM);# 注释;KEY=value,
# 单双引号可包整值;不做 shell 展开、变量插值、命令替换。
#
# 申请 Key:https://anysearch.com/console/api-keys
# 本文件只是占位样例,不含任何真实凭据;复制后把占位换成自己的 Key。

ANYSEARCH_API_KEY=<your_api_key_here>
